/* Copyright (c) 2008 The Board of Trustees of The Leland Stanford Junior University */
/* Copyright (c) 2011, 2012 Open Networking Foundation */
/* Copyright (c) 2012, 2013 Big Switch Networks, Inc. */
/* See the file LICENSE.loci which should have been included in the source distribution */
/*
 * Duplication functions for all OF objects
 *
 * AUTOMATICALLY GENERATED FILE.  Edits will be lost on regen.
 *
 * These are test functions for exercising accessors.  You can call
 * of_object_dup for an efficient duplication.
 */

#define DISABLE_WARN_UNUSED_RESULT
#include "loci_log.h"
#include <locitest/of_dup.h>


/* Special try macro for duplicating */
#define _TRY_FREE(op, obj, rv) do { \
        int _rv;                                                             \
        if ((_rv = (op)) < 0) {                                              \
            LOCI_LOG_ERROR("ERROR %d at %s:%d\n", _rv, __FILE__, __LINE__);    \
            of_object_delete((of_object_t *)(obj));                          \
            return (rv);                                                     \
        }                                                                    \
    } while (0)

/**
 * Duplicate an object of type of_aggregate_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_reply_t *
of_aggregate_stats_reply_OF_VERSION_1_0_dup(
    of_aggregate_stats_reply_t *src)
{
    of_aggregate_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_aggregate_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_reply_xid_get(src, &val32);
    of_aggregate_stats_reply_xid_set(dst, val32);

    of_aggregate_stats_reply_flags_get(src, &val16);
    of_aggregate_stats_reply_flags_set(dst, val16);

    of_aggregate_stats_reply_packet_count_get(src, &val64);
    of_aggregate_stats_reply_packet_count_set(dst, val64);

    of_aggregate_stats_reply_byte_count_get(src, &val64);
    of_aggregate_stats_reply_byte_count_set(dst, val64);

    of_aggregate_stats_reply_flow_count_get(src, &val32);
    of_aggregate_stats_reply_flow_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_request_t *
of_aggregate_stats_request_OF_VERSION_1_0_dup(
    of_aggregate_stats_request_t *src)
{
    of_aggregate_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_match_t match;
    uint8_t val8;
    of_port_no_t port_no;

    if ((dst = of_aggregate_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_request_xid_get(src, &val32);
    of_aggregate_stats_request_xid_set(dst, val32);

    of_aggregate_stats_request_flags_get(src, &val16);
    of_aggregate_stats_request_flags_set(dst, val16);

    of_aggregate_stats_request_match_get(src, &match);
    of_aggregate_stats_request_match_set(dst, &match);

    of_aggregate_stats_request_table_id_get(src, &val8);
    of_aggregate_stats_request_table_id_set(dst, val8);

    of_aggregate_stats_request_out_port_get(src, &port_no);
    of_aggregate_stats_request_out_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bad_action_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_action_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_action_error_msg_t *
of_bad_action_error_msg_OF_VERSION_1_0_dup(
    of_bad_action_error_msg_t *src)
{
    of_bad_action_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_action_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_action_error_msg_xid_get(src, &val32);
    of_bad_action_error_msg_xid_set(dst, val32);

    of_bad_action_error_msg_code_get(src, &val16);
    of_bad_action_error_msg_code_set(dst, val16);

    of_bad_action_error_msg_data_get(src, &octets);
    of_bad_action_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_request_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_request_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_request_error_msg_t *
of_bad_request_error_msg_OF_VERSION_1_0_dup(
    of_bad_request_error_msg_t *src)
{
    of_bad_request_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_request_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_request_error_msg_xid_get(src, &val32);
    of_bad_request_error_msg_xid_set(dst, val32);

    of_bad_request_error_msg_code_get(src, &val16);
    of_bad_request_error_msg_code_set(dst, val16);

    of_bad_request_error_msg_data_get(src, &octets);
    of_bad_request_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_reply_t *
of_barrier_reply_OF_VERSION_1_0_dup(
    of_barrier_reply_t *src)
{
    of_barrier_reply_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_reply_xid_get(src, &val32);
    of_barrier_reply_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_request_t *
of_barrier_request_OF_VERSION_1_0_dup(
    of_barrier_request_t *src)
{
    of_barrier_request_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_request_xid_get(src, &val32);
    of_barrier_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_reply_t *
of_bsn_bw_clear_data_reply_OF_VERSION_1_0_dup(
    of_bsn_bw_clear_data_reply_t *src)
{
    of_bsn_bw_clear_data_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_reply_xid_get(src, &val32);
    of_bsn_bw_clear_data_reply_xid_set(dst, val32);

    of_bsn_bw_clear_data_reply_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_reply_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_reply_subtype_get(src, &val32);
    of_bsn_bw_clear_data_reply_subtype_set(dst, val32);

    of_bsn_bw_clear_data_reply_status_get(src, &val32);
    of_bsn_bw_clear_data_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_request_t *
of_bsn_bw_clear_data_request_OF_VERSION_1_0_dup(
    of_bsn_bw_clear_data_request_t *src)
{
    of_bsn_bw_clear_data_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_request_xid_get(src, &val32);
    of_bsn_bw_clear_data_request_xid_set(dst, val32);

    of_bsn_bw_clear_data_request_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_request_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_request_subtype_get(src, &val32);
    of_bsn_bw_clear_data_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_reply_t *
of_bsn_bw_enable_get_reply_OF_VERSION_1_0_dup(
    of_bsn_bw_enable_get_reply_t *src)
{
    of_bsn_bw_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_reply_xid_get(src, &val32);
    of_bsn_bw_enable_get_reply_xid_set(dst, val32);

    of_bsn_bw_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_get_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_get_reply_enabled_get(src, &val32);
    of_bsn_bw_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_request_t *
of_bsn_bw_enable_get_request_OF_VERSION_1_0_dup(
    of_bsn_bw_enable_get_request_t *src)
{
    of_bsn_bw_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_request_xid_get(src, &val32);
    of_bsn_bw_enable_get_request_xid_set(dst, val32);

    of_bsn_bw_enable_get_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_request_subtype_get(src, &val32);
    of_bsn_bw_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_reply_t *
of_bsn_bw_enable_set_reply_OF_VERSION_1_0_dup(
    of_bsn_bw_enable_set_reply_t *src)
{
    of_bsn_bw_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_reply_xid_get(src, &val32);
    of_bsn_bw_enable_set_reply_xid_set(dst, val32);

    of_bsn_bw_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_set_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_set_reply_enable_get(src, &val32);
    of_bsn_bw_enable_set_reply_enable_set(dst, val32);

    of_bsn_bw_enable_set_reply_status_get(src, &val32);
    of_bsn_bw_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_request_t *
of_bsn_bw_enable_set_request_OF_VERSION_1_0_dup(
    of_bsn_bw_enable_set_request_t *src)
{
    of_bsn_bw_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_request_xid_get(src, &val32);
    of_bsn_bw_enable_set_request_xid_set(dst, val32);

    of_bsn_bw_enable_set_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_request_subtype_get(src, &val32);
    of_bsn_bw_enable_set_request_subtype_set(dst, val32);

    of_bsn_bw_enable_set_request_enable_get(src, &val32);
    of_bsn_bw_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_reply_t *
of_bsn_get_interfaces_reply_OF_VERSION_1_0_dup(
    of_bsn_get_interfaces_reply_t *src)
{
    of_bsn_get_interfaces_reply_t *dst;
    uint32_t val32;

    of_list_bsn_interface_t src_list;
    of_list_bsn_interface_t *dst_list;

    if ((dst = of_bsn_get_interfaces_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_reply_xid_get(src, &val32);
    of_bsn_get_interfaces_reply_xid_set(dst, val32);

    of_bsn_get_interfaces_reply_experimenter_get(src, &val32);
    of_bsn_get_interfaces_reply_experimenter_set(dst, val32);

    of_bsn_get_interfaces_reply_subtype_get(src, &val32);
    of_bsn_get_interfaces_reply_subtype_set(dst, val32);

    of_bsn_get_interfaces_reply_interfaces_bind(
        src, &src_list);
    dst_list = of_list_bsn_interface_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_get_interfaces_reply_delete(dst);
        return NULL;
    }
    of_bsn_get_interfaces_reply_interfaces_set(dst, dst_list);
    of_list_bsn_interface_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_request_t *
of_bsn_get_interfaces_request_OF_VERSION_1_0_dup(
    of_bsn_get_interfaces_request_t *src)
{
    of_bsn_get_interfaces_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_interfaces_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_request_xid_get(src, &val32);
    of_bsn_get_interfaces_request_xid_set(dst, val32);

    of_bsn_get_interfaces_request_experimenter_get(src, &val32);
    of_bsn_get_interfaces_request_experimenter_set(dst, val32);

    of_bsn_get_interfaces_request_subtype_get(src, &val32);
    of_bsn_get_interfaces_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_ip_mask_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_ip_mask_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_ip_mask_reply_t *
of_bsn_get_ip_mask_reply_OF_VERSION_1_0_dup(
    of_bsn_get_ip_mask_reply_t *src)
{
    of_bsn_get_ip_mask_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_ip_mask_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_ip_mask_reply_xid_get(src, &val32);
    of_bsn_get_ip_mask_reply_xid_set(dst, val32);

    of_bsn_get_ip_mask_reply_experimenter_get(src, &val32);
    of_bsn_get_ip_mask_reply_experimenter_set(dst, val32);

    of_bsn_get_ip_mask_reply_subtype_get(src, &val32);
    of_bsn_get_ip_mask_reply_subtype_set(dst, val32);

    of_bsn_get_ip_mask_reply_index_get(src, &val8);
    of_bsn_get_ip_mask_reply_index_set(dst, val8);

    of_bsn_get_ip_mask_reply_mask_get(src, &val32);
    of_bsn_get_ip_mask_reply_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_ip_mask_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_ip_mask_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_ip_mask_request_t *
of_bsn_get_ip_mask_request_OF_VERSION_1_0_dup(
    of_bsn_get_ip_mask_request_t *src)
{
    of_bsn_get_ip_mask_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_ip_mask_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_ip_mask_request_xid_get(src, &val32);
    of_bsn_get_ip_mask_request_xid_set(dst, val32);

    of_bsn_get_ip_mask_request_experimenter_get(src, &val32);
    of_bsn_get_ip_mask_request_experimenter_set(dst, val32);

    of_bsn_get_ip_mask_request_subtype_get(src, &val32);
    of_bsn_get_ip_mask_request_subtype_set(dst, val32);

    of_bsn_get_ip_mask_request_index_get(src, &val8);
    of_bsn_get_ip_mask_request_index_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_l2_table_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_l2_table_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_l2_table_reply_t *
of_bsn_get_l2_table_reply_OF_VERSION_1_0_dup(
    of_bsn_get_l2_table_reply_t *src)
{
    of_bsn_get_l2_table_reply_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_get_l2_table_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_l2_table_reply_xid_get(src, &val32);
    of_bsn_get_l2_table_reply_xid_set(dst, val32);

    of_bsn_get_l2_table_reply_experimenter_get(src, &val32);
    of_bsn_get_l2_table_reply_experimenter_set(dst, val32);

    of_bsn_get_l2_table_reply_subtype_get(src, &val32);
    of_bsn_get_l2_table_reply_subtype_set(dst, val32);

    of_bsn_get_l2_table_reply_l2_table_enable_get(src, &val8);
    of_bsn_get_l2_table_reply_l2_table_enable_set(dst, val8);

    of_bsn_get_l2_table_reply_l2_table_priority_get(src, &val16);
    of_bsn_get_l2_table_reply_l2_table_priority_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_l2_table_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_l2_table_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_l2_table_request_t *
of_bsn_get_l2_table_request_OF_VERSION_1_0_dup(
    of_bsn_get_l2_table_request_t *src)
{
    of_bsn_get_l2_table_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_l2_table_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_l2_table_request_xid_get(src, &val32);
    of_bsn_get_l2_table_request_xid_set(dst, val32);

    of_bsn_get_l2_table_request_experimenter_get(src, &val32);
    of_bsn_get_l2_table_request_experimenter_set(dst, val32);

    of_bsn_get_l2_table_request_subtype_get(src, &val32);
    of_bsn_get_l2_table_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_reply_t *
of_bsn_get_mirroring_reply_OF_VERSION_1_0_dup(
    of_bsn_get_mirroring_reply_t *src)
{
    of_bsn_get_mirroring_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_reply_xid_get(src, &val32);
    of_bsn_get_mirroring_reply_xid_set(dst, val32);

    of_bsn_get_mirroring_reply_experimenter_get(src, &val32);
    of_bsn_get_mirroring_reply_experimenter_set(dst, val32);

    of_bsn_get_mirroring_reply_subtype_get(src, &val32);
    of_bsn_get_mirroring_reply_subtype_set(dst, val32);

    of_bsn_get_mirroring_reply_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_reply_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_request_t *
of_bsn_get_mirroring_request_OF_VERSION_1_0_dup(
    of_bsn_get_mirroring_request_t *src)
{
    of_bsn_get_mirroring_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_request_xid_get(src, &val32);
    of_bsn_get_mirroring_request_xid_set(dst, val32);

    of_bsn_get_mirroring_request_experimenter_get(src, &val32);
    of_bsn_get_mirroring_request_experimenter_set(dst, val32);

    of_bsn_get_mirroring_request_subtype_get(src, &val32);
    of_bsn_get_mirroring_request_subtype_set(dst, val32);

    of_bsn_get_mirroring_request_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_request_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_hybrid_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_hybrid_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_hybrid_get_reply_t *
of_bsn_hybrid_get_reply_OF_VERSION_1_0_dup(
    of_bsn_hybrid_get_reply_t *src)
{
    of_bsn_hybrid_get_reply_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_hybrid_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_hybrid_get_reply_xid_get(src, &val32);
    of_bsn_hybrid_get_reply_xid_set(dst, val32);

    of_bsn_hybrid_get_reply_experimenter_get(src, &val32);
    of_bsn_hybrid_get_reply_experimenter_set(dst, val32);

    of_bsn_hybrid_get_reply_subtype_get(src, &val32);
    of_bsn_hybrid_get_reply_subtype_set(dst, val32);

    of_bsn_hybrid_get_reply_hybrid_enable_get(src, &val8);
    of_bsn_hybrid_get_reply_hybrid_enable_set(dst, val8);

    of_bsn_hybrid_get_reply_hybrid_version_get(src, &val16);
    of_bsn_hybrid_get_reply_hybrid_version_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_hybrid_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_hybrid_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_hybrid_get_request_t *
of_bsn_hybrid_get_request_OF_VERSION_1_0_dup(
    of_bsn_hybrid_get_request_t *src)
{
    of_bsn_hybrid_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_hybrid_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_hybrid_get_request_xid_get(src, &val32);
    of_bsn_hybrid_get_request_xid_set(dst, val32);

    of_bsn_hybrid_get_request_experimenter_get(src, &val32);
    of_bsn_hybrid_get_request_experimenter_set(dst, val32);

    of_bsn_hybrid_get_request_subtype_get(src, &val32);
    of_bsn_hybrid_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_reply_t *
of_bsn_pdu_rx_reply_OF_VERSION_1_0_dup(
    of_bsn_pdu_rx_reply_t *src)
{
    of_bsn_pdu_rx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_reply_xid_get(src, &val32);
    of_bsn_pdu_rx_reply_xid_set(dst, val32);

    of_bsn_pdu_rx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_rx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_rx_reply_subtype_get(src, &val32);
    of_bsn_pdu_rx_reply_subtype_set(dst, val32);

    of_bsn_pdu_rx_reply_status_get(src, &val32);
    of_bsn_pdu_rx_reply_status_set(dst, val32);

    of_bsn_pdu_rx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_rx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_rx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_rx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_request_t *
of_bsn_pdu_rx_request_OF_VERSION_1_0_dup(
    of_bsn_pdu_rx_request_t *src)
{
    of_bsn_pdu_rx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_rx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_request_xid_get(src, &val32);
    of_bsn_pdu_rx_request_xid_set(dst, val32);

    of_bsn_pdu_rx_request_experimenter_get(src, &val32);
    of_bsn_pdu_rx_request_experimenter_set(dst, val32);

    of_bsn_pdu_rx_request_subtype_get(src, &val32);
    of_bsn_pdu_rx_request_subtype_set(dst, val32);

    of_bsn_pdu_rx_request_timeout_ms_get(src, &val32);
    of_bsn_pdu_rx_request_timeout_ms_set(dst, val32);

    of_bsn_pdu_rx_request_port_no_get(src, &port_no);
    of_bsn_pdu_rx_request_port_no_set(dst, port_no);

    of_bsn_pdu_rx_request_slot_num_get(src, &val8);
    of_bsn_pdu_rx_request_slot_num_set(dst, val8);

    of_bsn_pdu_rx_request_data_get(src, &octets);
    of_bsn_pdu_rx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_timeout_t *
of_bsn_pdu_rx_timeout_OF_VERSION_1_0_dup(
    of_bsn_pdu_rx_timeout_t *src)
{
    of_bsn_pdu_rx_timeout_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_timeout_xid_get(src, &val32);
    of_bsn_pdu_rx_timeout_xid_set(dst, val32);

    of_bsn_pdu_rx_timeout_experimenter_get(src, &val32);
    of_bsn_pdu_rx_timeout_experimenter_set(dst, val32);

    of_bsn_pdu_rx_timeout_subtype_get(src, &val32);
    of_bsn_pdu_rx_timeout_subtype_set(dst, val32);

    of_bsn_pdu_rx_timeout_port_no_get(src, &port_no);
    of_bsn_pdu_rx_timeout_port_no_set(dst, port_no);

    of_bsn_pdu_rx_timeout_slot_num_get(src, &val8);
    of_bsn_pdu_rx_timeout_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_reply_t *
of_bsn_pdu_tx_reply_OF_VERSION_1_0_dup(
    of_bsn_pdu_tx_reply_t *src)
{
    of_bsn_pdu_tx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_tx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_reply_xid_get(src, &val32);
    of_bsn_pdu_tx_reply_xid_set(dst, val32);

    of_bsn_pdu_tx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_tx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_tx_reply_subtype_get(src, &val32);
    of_bsn_pdu_tx_reply_subtype_set(dst, val32);

    of_bsn_pdu_tx_reply_status_get(src, &val32);
    of_bsn_pdu_tx_reply_status_set(dst, val32);

    of_bsn_pdu_tx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_tx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_tx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_tx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_request_t *
of_bsn_pdu_tx_request_OF_VERSION_1_0_dup(
    of_bsn_pdu_tx_request_t *src)
{
    of_bsn_pdu_tx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_tx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_request_xid_get(src, &val32);
    of_bsn_pdu_tx_request_xid_set(dst, val32);

    of_bsn_pdu_tx_request_experimenter_get(src, &val32);
    of_bsn_pdu_tx_request_experimenter_set(dst, val32);

    of_bsn_pdu_tx_request_subtype_get(src, &val32);
    of_bsn_pdu_tx_request_subtype_set(dst, val32);

    of_bsn_pdu_tx_request_tx_interval_ms_get(src, &val32);
    of_bsn_pdu_tx_request_tx_interval_ms_set(dst, val32);

    of_bsn_pdu_tx_request_port_no_get(src, &port_no);
    of_bsn_pdu_tx_request_port_no_set(dst, port_no);

    of_bsn_pdu_tx_request_slot_num_get(src, &val8);
    of_bsn_pdu_tx_request_slot_num_set(dst, val8);

    of_bsn_pdu_tx_request_data_get(src, &octets);
    of_bsn_pdu_tx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_ip_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_ip_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_ip_mask_t *
of_bsn_set_ip_mask_OF_VERSION_1_0_dup(
    of_bsn_set_ip_mask_t *src)
{
    of_bsn_set_ip_mask_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_ip_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_ip_mask_xid_get(src, &val32);
    of_bsn_set_ip_mask_xid_set(dst, val32);

    of_bsn_set_ip_mask_experimenter_get(src, &val32);
    of_bsn_set_ip_mask_experimenter_set(dst, val32);

    of_bsn_set_ip_mask_subtype_get(src, &val32);
    of_bsn_set_ip_mask_subtype_set(dst, val32);

    of_bsn_set_ip_mask_index_get(src, &val8);
    of_bsn_set_ip_mask_index_set(dst, val8);

    of_bsn_set_ip_mask_mask_get(src, &val32);
    of_bsn_set_ip_mask_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_l2_table_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_l2_table_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_l2_table_reply_t *
of_bsn_set_l2_table_reply_OF_VERSION_1_0_dup(
    of_bsn_set_l2_table_reply_t *src)
{
    of_bsn_set_l2_table_reply_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_set_l2_table_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_l2_table_reply_xid_get(src, &val32);
    of_bsn_set_l2_table_reply_xid_set(dst, val32);

    of_bsn_set_l2_table_reply_experimenter_get(src, &val32);
    of_bsn_set_l2_table_reply_experimenter_set(dst, val32);

    of_bsn_set_l2_table_reply_subtype_get(src, &val32);
    of_bsn_set_l2_table_reply_subtype_set(dst, val32);

    of_bsn_set_l2_table_reply_l2_table_enable_get(src, &val8);
    of_bsn_set_l2_table_reply_l2_table_enable_set(dst, val8);

    of_bsn_set_l2_table_reply_l2_table_priority_get(src, &val16);
    of_bsn_set_l2_table_reply_l2_table_priority_set(dst, val16);

    of_bsn_set_l2_table_reply_status_get(src, &val32);
    of_bsn_set_l2_table_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_l2_table_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_l2_table_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_l2_table_request_t *
of_bsn_set_l2_table_request_OF_VERSION_1_0_dup(
    of_bsn_set_l2_table_request_t *src)
{
    of_bsn_set_l2_table_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_set_l2_table_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_l2_table_request_xid_get(src, &val32);
    of_bsn_set_l2_table_request_xid_set(dst, val32);

    of_bsn_set_l2_table_request_experimenter_get(src, &val32);
    of_bsn_set_l2_table_request_experimenter_set(dst, val32);

    of_bsn_set_l2_table_request_subtype_get(src, &val32);
    of_bsn_set_l2_table_request_subtype_set(dst, val32);

    of_bsn_set_l2_table_request_l2_table_enable_get(src, &val8);
    of_bsn_set_l2_table_request_l2_table_enable_set(dst, val8);

    of_bsn_set_l2_table_request_l2_table_priority_get(src, &val16);
    of_bsn_set_l2_table_request_l2_table_priority_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_mirroring
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_mirroring.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_mirroring_t *
of_bsn_set_mirroring_OF_VERSION_1_0_dup(
    of_bsn_set_mirroring_t *src)
{
    of_bsn_set_mirroring_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_mirroring_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_mirroring_xid_get(src, &val32);
    of_bsn_set_mirroring_xid_set(dst, val32);

    of_bsn_set_mirroring_experimenter_get(src, &val32);
    of_bsn_set_mirroring_experimenter_set(dst, val32);

    of_bsn_set_mirroring_subtype_get(src, &val32);
    of_bsn_set_mirroring_subtype_set(dst, val32);

    of_bsn_set_mirroring_report_mirror_ports_get(src, &val8);
    of_bsn_set_mirroring_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_reply_t *
of_bsn_set_pktin_suppression_reply_OF_VERSION_1_0_dup(
    of_bsn_set_pktin_suppression_reply_t *src)
{
    of_bsn_set_pktin_suppression_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_pktin_suppression_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_reply_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_status_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_request_t *
of_bsn_set_pktin_suppression_request_OF_VERSION_1_0_dup(
    of_bsn_set_pktin_suppression_request_t *src)
{
    of_bsn_set_pktin_suppression_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_bsn_set_pktin_suppression_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_request_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_request_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_request_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_request_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_request_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_request_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_request_enabled_get(src, &val8);
    of_bsn_set_pktin_suppression_request_enabled_set(dst, val8);

    of_bsn_set_pktin_suppression_request_idle_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_idle_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_hard_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_hard_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_priority_get(src, &val16);
    of_bsn_set_pktin_suppression_request_priority_set(dst, val16);

    of_bsn_set_pktin_suppression_request_cookie_get(src, &val64);
    of_bsn_set_pktin_suppression_request_cookie_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_shell_command
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_shell_command.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_shell_command_t *
of_bsn_shell_command_OF_VERSION_1_0_dup(
    of_bsn_shell_command_t *src)
{
    of_bsn_shell_command_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_shell_command_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_shell_command_xid_get(src, &val32);
    of_bsn_shell_command_xid_set(dst, val32);

    of_bsn_shell_command_experimenter_get(src, &val32);
    of_bsn_shell_command_experimenter_set(dst, val32);

    of_bsn_shell_command_subtype_get(src, &val32);
    of_bsn_shell_command_subtype_set(dst, val32);

    of_bsn_shell_command_service_get(src, &val32);
    of_bsn_shell_command_service_set(dst, val32);

    of_bsn_shell_command_data_get(src, &octets);
    of_bsn_shell_command_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_shell_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_shell_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_shell_output_t *
of_bsn_shell_output_OF_VERSION_1_0_dup(
    of_bsn_shell_output_t *src)
{
    of_bsn_shell_output_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_shell_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_shell_output_xid_get(src, &val32);
    of_bsn_shell_output_xid_set(dst, val32);

    of_bsn_shell_output_experimenter_get(src, &val32);
    of_bsn_shell_output_experimenter_set(dst, val32);

    of_bsn_shell_output_subtype_get(src, &val32);
    of_bsn_shell_output_subtype_set(dst, val32);

    of_bsn_shell_output_data_get(src, &octets);
    of_bsn_shell_output_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_shell_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_shell_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_shell_status_t *
of_bsn_shell_status_OF_VERSION_1_0_dup(
    of_bsn_shell_status_t *src)
{
    of_bsn_shell_status_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_shell_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_shell_status_xid_get(src, &val32);
    of_bsn_shell_status_xid_set(dst, val32);

    of_bsn_shell_status_experimenter_get(src, &val32);
    of_bsn_shell_status_experimenter_set(dst, val32);

    of_bsn_shell_status_subtype_get(src, &val32);
    of_bsn_shell_status_subtype_set(dst, val32);

    of_bsn_shell_status_status_get(src, &val32);
    of_bsn_shell_status_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_reply_t *
of_bsn_virtual_port_create_reply_OF_VERSION_1_0_dup(
    of_bsn_virtual_port_create_reply_t *src)
{
    of_bsn_virtual_port_create_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_create_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_reply_xid_get(src, &val32);
    of_bsn_virtual_port_create_reply_xid_set(dst, val32);

    of_bsn_virtual_port_create_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_create_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_create_reply_status_get(src, &val32);
    of_bsn_virtual_port_create_reply_status_set(dst, val32);

    of_bsn_virtual_port_create_reply_vport_no_get(src, &val32);
    of_bsn_virtual_port_create_reply_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_request_t *
of_bsn_virtual_port_create_request_OF_VERSION_1_0_dup(
    of_bsn_virtual_port_create_request_t *src)
{
    of_bsn_virtual_port_create_request_t *dst;
    uint32_t val32;

    of_bsn_vport_q_in_q_t src_bsn_vport;
    of_bsn_vport_q_in_q_t *dst_bsn_vport;

    if ((dst = of_bsn_virtual_port_create_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_request_xid_get(src, &val32);
    of_bsn_virtual_port_create_request_xid_set(dst, val32);

    of_bsn_virtual_port_create_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_request_subtype_get(src, &val32);
    of_bsn_virtual_port_create_request_subtype_set(dst, val32);

    of_bsn_virtual_port_create_request_vport_bind(
        src, &src_bsn_vport);
    dst_bsn_vport = of_bsn_vport_q_in_q_OF_VERSION_1_0_dup(&src_bsn_vport);
    if (dst_bsn_vport == NULL) {
        of_bsn_virtual_port_create_request_delete(dst);
        return NULL;
    }
    of_bsn_virtual_port_create_request_vport_set(dst, dst_bsn_vport);
    of_bsn_vport_q_in_q_delete(dst_bsn_vport);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_reply_t *
of_bsn_virtual_port_remove_reply_OF_VERSION_1_0_dup(
    of_bsn_virtual_port_remove_reply_t *src)
{
    of_bsn_virtual_port_remove_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_reply_xid_get(src, &val32);
    of_bsn_virtual_port_remove_reply_xid_set(dst, val32);

    of_bsn_virtual_port_remove_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_reply_status_get(src, &val32);
    of_bsn_virtual_port_remove_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_request_t *
of_bsn_virtual_port_remove_request_OF_VERSION_1_0_dup(
    of_bsn_virtual_port_remove_request_t *src)
{
    of_bsn_virtual_port_remove_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_request_xid_get(src, &val32);
    of_bsn_virtual_port_remove_request_xid_set(dst, val32);

    of_bsn_virtual_port_remove_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_request_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_request_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_request_vport_no_get(src, &val32);
    of_bsn_virtual_port_remove_request_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_reply_t *
of_desc_stats_reply_OF_VERSION_1_0_dup(
    of_desc_stats_reply_t *src)
{
    of_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_serial_num_t ser_num;

    if ((dst = of_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_reply_xid_get(src, &val32);
    of_desc_stats_reply_xid_set(dst, val32);

    of_desc_stats_reply_flags_get(src, &val16);
    of_desc_stats_reply_flags_set(dst, val16);

    of_desc_stats_reply_mfr_desc_get(src, &desc_str);
    of_desc_stats_reply_mfr_desc_set(dst, desc_str);

    of_desc_stats_reply_hw_desc_get(src, &desc_str);
    of_desc_stats_reply_hw_desc_set(dst, desc_str);

    of_desc_stats_reply_sw_desc_get(src, &desc_str);
    of_desc_stats_reply_sw_desc_set(dst, desc_str);

    of_desc_stats_reply_serial_num_get(src, &ser_num);
    of_desc_stats_reply_serial_num_set(dst, ser_num);

    of_desc_stats_reply_dp_desc_get(src, &desc_str);
    of_desc_stats_reply_dp_desc_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_request_t *
of_desc_stats_request_OF_VERSION_1_0_dup(
    of_desc_stats_request_t *src)
{
    of_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_request_xid_get(src, &val32);
    of_desc_stats_request_xid_set(dst, val32);

    of_desc_stats_request_flags_get(src, &val16);
    of_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_echo_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_reply_t *
of_echo_reply_OF_VERSION_1_0_dup(
    of_echo_reply_t *src)
{
    of_echo_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_reply_xid_get(src, &val32);
    of_echo_reply_xid_set(dst, val32);

    of_echo_reply_data_get(src, &octets);
    of_echo_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_echo_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_request_t *
of_echo_request_OF_VERSION_1_0_dup(
    of_echo_request_t *src)
{
    of_echo_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_request_xid_get(src, &val32);
    of_echo_request_xid_set(dst, val32);

    of_echo_request_data_get(src, &octets);
    of_echo_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_features_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_reply_t *
of_features_reply_OF_VERSION_1_0_dup(
    of_features_reply_t *src)
{
    of_features_reply_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;

    of_list_port_desc_t src_list;
    of_list_port_desc_t *dst_list;

    if ((dst = of_features_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_reply_xid_get(src, &val32);
    of_features_reply_xid_set(dst, val32);

    of_features_reply_datapath_id_get(src, &val64);
    of_features_reply_datapath_id_set(dst, val64);

    of_features_reply_n_buffers_get(src, &val32);
    of_features_reply_n_buffers_set(dst, val32);

    of_features_reply_n_tables_get(src, &val8);
    of_features_reply_n_tables_set(dst, val8);

    of_features_reply_capabilities_get(src, &val32);
    of_features_reply_capabilities_set(dst, val32);

    of_features_reply_actions_get(src, &val32);
    of_features_reply_actions_set(dst, val32);

    of_features_reply_ports_bind(
        src, &src_list);
    dst_list = of_list_port_desc_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_features_reply_delete(dst);
        return NULL;
    }
    of_features_reply_ports_set(dst, dst_list);
    of_list_port_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_features_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_request_t *
of_features_request_OF_VERSION_1_0_dup(
    of_features_request_t *src)
{
    of_features_request_t *dst;
    uint32_t val32;

    if ((dst = of_features_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_request_xid_get(src, &val32);
    of_features_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_flow_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_add_t *
of_flow_add_OF_VERSION_1_0_dup(
    of_flow_add_t *src)
{
    of_flow_add_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_add_xid_get(src, &val32);
    of_flow_add_xid_set(dst, val32);

    of_flow_add_match_get(src, &match);
    of_flow_add_match_set(dst, &match);

    of_flow_add_cookie_get(src, &val64);
    of_flow_add_cookie_set(dst, val64);

    of_flow_add_idle_timeout_get(src, &val16);
    of_flow_add_idle_timeout_set(dst, val16);

    of_flow_add_hard_timeout_get(src, &val16);
    of_flow_add_hard_timeout_set(dst, val16);

    of_flow_add_priority_get(src, &val16);
    of_flow_add_priority_set(dst, val16);

    of_flow_add_buffer_id_get(src, &val32);
    of_flow_add_buffer_id_set(dst, val32);

    of_flow_add_out_port_get(src, &port_no);
    of_flow_add_out_port_set(dst, port_no);

    of_flow_add_flags_get(src, &val16);
    of_flow_add_flags_set(dst, val16);

    of_flow_add_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_add_delete(dst);
        return NULL;
    }
    of_flow_add_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_t *
of_flow_delete_OF_VERSION_1_0_dup(
    of_flow_delete_t *src)
{
    of_flow_delete_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_xid_get(src, &val32);
    of_flow_delete_xid_set(dst, val32);

    of_flow_delete_match_get(src, &match);
    of_flow_delete_match_set(dst, &match);

    of_flow_delete_cookie_get(src, &val64);
    of_flow_delete_cookie_set(dst, val64);

    of_flow_delete_idle_timeout_get(src, &val16);
    of_flow_delete_idle_timeout_set(dst, val16);

    of_flow_delete_hard_timeout_get(src, &val16);
    of_flow_delete_hard_timeout_set(dst, val16);

    of_flow_delete_priority_get(src, &val16);
    of_flow_delete_priority_set(dst, val16);

    of_flow_delete_buffer_id_get(src, &val32);
    of_flow_delete_buffer_id_set(dst, val32);

    of_flow_delete_out_port_get(src, &port_no);
    of_flow_delete_out_port_set(dst, port_no);

    of_flow_delete_flags_get(src, &val16);
    of_flow_delete_flags_set(dst, val16);

    of_flow_delete_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_delete(dst);
        return NULL;
    }
    of_flow_delete_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_strict_t *
of_flow_delete_strict_OF_VERSION_1_0_dup(
    of_flow_delete_strict_t *src)
{
    of_flow_delete_strict_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_delete_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_strict_xid_get(src, &val32);
    of_flow_delete_strict_xid_set(dst, val32);

    of_flow_delete_strict_match_get(src, &match);
    of_flow_delete_strict_match_set(dst, &match);

    of_flow_delete_strict_cookie_get(src, &val64);
    of_flow_delete_strict_cookie_set(dst, val64);

    of_flow_delete_strict_idle_timeout_get(src, &val16);
    of_flow_delete_strict_idle_timeout_set(dst, val16);

    of_flow_delete_strict_hard_timeout_get(src, &val16);
    of_flow_delete_strict_hard_timeout_set(dst, val16);

    of_flow_delete_strict_priority_get(src, &val16);
    of_flow_delete_strict_priority_set(dst, val16);

    of_flow_delete_strict_buffer_id_get(src, &val32);
    of_flow_delete_strict_buffer_id_set(dst, val32);

    of_flow_delete_strict_out_port_get(src, &port_no);
    of_flow_delete_strict_out_port_set(dst, port_no);

    of_flow_delete_strict_flags_get(src, &val16);
    of_flow_delete_strict_flags_set(dst, val16);

    of_flow_delete_strict_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_strict_delete(dst);
        return NULL;
    }
    of_flow_delete_strict_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_mod_failed_error_msg_t *
of_flow_mod_failed_error_msg_OF_VERSION_1_0_dup(
    of_flow_mod_failed_error_msg_t *src)
{
    of_flow_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_mod_failed_error_msg_xid_get(src, &val32);
    of_flow_mod_failed_error_msg_xid_set(dst, val32);

    of_flow_mod_failed_error_msg_code_get(src, &val16);
    of_flow_mod_failed_error_msg_code_set(dst, val16);

    of_flow_mod_failed_error_msg_data_get(src, &octets);
    of_flow_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_t *
of_flow_modify_OF_VERSION_1_0_dup(
    of_flow_modify_t *src)
{
    of_flow_modify_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_xid_get(src, &val32);
    of_flow_modify_xid_set(dst, val32);

    of_flow_modify_match_get(src, &match);
    of_flow_modify_match_set(dst, &match);

    of_flow_modify_cookie_get(src, &val64);
    of_flow_modify_cookie_set(dst, val64);

    of_flow_modify_idle_timeout_get(src, &val16);
    of_flow_modify_idle_timeout_set(dst, val16);

    of_flow_modify_hard_timeout_get(src, &val16);
    of_flow_modify_hard_timeout_set(dst, val16);

    of_flow_modify_priority_get(src, &val16);
    of_flow_modify_priority_set(dst, val16);

    of_flow_modify_buffer_id_get(src, &val32);
    of_flow_modify_buffer_id_set(dst, val32);

    of_flow_modify_out_port_get(src, &port_no);
    of_flow_modify_out_port_set(dst, port_no);

    of_flow_modify_flags_get(src, &val16);
    of_flow_modify_flags_set(dst, val16);

    of_flow_modify_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_delete(dst);
        return NULL;
    }
    of_flow_modify_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_strict_t *
of_flow_modify_strict_OF_VERSION_1_0_dup(
    of_flow_modify_strict_t *src)
{
    of_flow_modify_strict_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_modify_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_strict_xid_get(src, &val32);
    of_flow_modify_strict_xid_set(dst, val32);

    of_flow_modify_strict_match_get(src, &match);
    of_flow_modify_strict_match_set(dst, &match);

    of_flow_modify_strict_cookie_get(src, &val64);
    of_flow_modify_strict_cookie_set(dst, val64);

    of_flow_modify_strict_idle_timeout_get(src, &val16);
    of_flow_modify_strict_idle_timeout_set(dst, val16);

    of_flow_modify_strict_hard_timeout_get(src, &val16);
    of_flow_modify_strict_hard_timeout_set(dst, val16);

    of_flow_modify_strict_priority_get(src, &val16);
    of_flow_modify_strict_priority_set(dst, val16);

    of_flow_modify_strict_buffer_id_get(src, &val32);
    of_flow_modify_strict_buffer_id_set(dst, val32);

    of_flow_modify_strict_out_port_get(src, &port_no);
    of_flow_modify_strict_out_port_set(dst, port_no);

    of_flow_modify_strict_flags_get(src, &val16);
    of_flow_modify_strict_flags_set(dst, val16);

    of_flow_modify_strict_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_strict_delete(dst);
        return NULL;
    }
    of_flow_modify_strict_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_removed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_removed.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_removed_t *
of_flow_removed_OF_VERSION_1_0_dup(
    of_flow_removed_t *src)
{
    of_flow_removed_t *dst;
    uint32_t val32;
    of_match_t match;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;

    if ((dst = of_flow_removed_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_removed_xid_get(src, &val32);
    of_flow_removed_xid_set(dst, val32);

    of_flow_removed_match_get(src, &match);
    of_flow_removed_match_set(dst, &match);

    of_flow_removed_cookie_get(src, &val64);
    of_flow_removed_cookie_set(dst, val64);

    of_flow_removed_priority_get(src, &val16);
    of_flow_removed_priority_set(dst, val16);

    of_flow_removed_reason_get(src, &val8);
    of_flow_removed_reason_set(dst, val8);

    of_flow_removed_duration_sec_get(src, &val32);
    of_flow_removed_duration_sec_set(dst, val32);

    of_flow_removed_duration_nsec_get(src, &val32);
    of_flow_removed_duration_nsec_set(dst, val32);

    of_flow_removed_idle_timeout_get(src, &val16);
    of_flow_removed_idle_timeout_set(dst, val16);

    of_flow_removed_packet_count_get(src, &val64);
    of_flow_removed_packet_count_set(dst, val64);

    of_flow_removed_byte_count_get(src, &val64);
    of_flow_removed_byte_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_reply_t *
of_flow_stats_reply_OF_VERSION_1_0_dup(
    of_flow_stats_reply_t *src)
{
    of_flow_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_flow_stats_entry_t src_list;
    of_list_flow_stats_entry_t *dst_list;

    if ((dst = of_flow_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_reply_xid_get(src, &val32);
    of_flow_stats_reply_xid_set(dst, val32);

    of_flow_stats_reply_flags_get(src, &val16);
    of_flow_stats_reply_flags_set(dst, val16);

    of_flow_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_flow_stats_entry_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_reply_delete(dst);
        return NULL;
    }
    of_flow_stats_reply_entries_set(dst, dst_list);
    of_list_flow_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_request_t *
of_flow_stats_request_OF_VERSION_1_0_dup(
    of_flow_stats_request_t *src)
{
    of_flow_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_match_t match;
    uint8_t val8;
    of_port_no_t port_no;

    if ((dst = of_flow_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_request_xid_get(src, &val32);
    of_flow_stats_request_xid_set(dst, val32);

    of_flow_stats_request_flags_get(src, &val16);
    of_flow_stats_request_flags_set(dst, val16);

    of_flow_stats_request_match_get(src, &match);
    of_flow_stats_request_match_set(dst, &match);

    of_flow_stats_request_table_id_get(src, &val8);
    of_flow_stats_request_table_id_set(dst, val8);

    of_flow_stats_request_out_port_get(src, &port_no);
    of_flow_stats_request_out_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_reply_t *
of_get_config_reply_OF_VERSION_1_0_dup(
    of_get_config_reply_t *src)
{
    of_get_config_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_reply_xid_get(src, &val32);
    of_get_config_reply_xid_set(dst, val32);

    of_get_config_reply_flags_get(src, &val16);
    of_get_config_reply_flags_set(dst, val16);

    of_get_config_reply_miss_send_len_get(src, &val16);
    of_get_config_reply_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_request_t *
of_get_config_request_OF_VERSION_1_0_dup(
    of_get_config_request_t *src)
{
    of_get_config_request_t *dst;
    uint32_t val32;

    if ((dst = of_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_request_xid_get(src, &val32);
    of_get_config_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_header
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_header.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_header_OF_VERSION_1_0_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_hello
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_t *
of_hello_OF_VERSION_1_0_dup(
    of_hello_t *src)
{
    of_hello_t *dst;
    uint32_t val32;

    if ((dst = of_hello_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_xid_get(src, &val32);
    of_hello_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_hello_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_failed_error_msg_t *
of_hello_failed_error_msg_OF_VERSION_1_0_dup(
    of_hello_failed_error_msg_t *src)
{
    of_hello_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_hello_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_failed_error_msg_xid_get(src, &val32);
    of_hello_failed_error_msg_xid_set(dst, val32);

    of_hello_failed_error_msg_code_get(src, &val16);
    of_hello_failed_error_msg_code_set(dst, val16);

    of_hello_failed_error_msg_data_get(src, &octets);
    of_hello_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_nicira_controller_role_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_nicira_controller_role_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_nicira_controller_role_reply_t *
of_nicira_controller_role_reply_OF_VERSION_1_0_dup(
    of_nicira_controller_role_reply_t *src)
{
    of_nicira_controller_role_reply_t *dst;
    uint32_t val32;

    if ((dst = of_nicira_controller_role_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_nicira_controller_role_reply_xid_get(src, &val32);
    of_nicira_controller_role_reply_xid_set(dst, val32);

    of_nicira_controller_role_reply_experimenter_get(src, &val32);
    of_nicira_controller_role_reply_experimenter_set(dst, val32);

    of_nicira_controller_role_reply_subtype_get(src, &val32);
    of_nicira_controller_role_reply_subtype_set(dst, val32);

    of_nicira_controller_role_reply_role_get(src, &val32);
    of_nicira_controller_role_reply_role_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_nicira_controller_role_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_nicira_controller_role_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_nicira_controller_role_request_t *
of_nicira_controller_role_request_OF_VERSION_1_0_dup(
    of_nicira_controller_role_request_t *src)
{
    of_nicira_controller_role_request_t *dst;
    uint32_t val32;

    if ((dst = of_nicira_controller_role_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_nicira_controller_role_request_xid_get(src, &val32);
    of_nicira_controller_role_request_xid_set(dst, val32);

    of_nicira_controller_role_request_experimenter_get(src, &val32);
    of_nicira_controller_role_request_experimenter_set(dst, val32);

    of_nicira_controller_role_request_subtype_get(src, &val32);
    of_nicira_controller_role_request_subtype_set(dst, val32);

    of_nicira_controller_role_request_role_get(src, &val32);
    of_nicira_controller_role_request_role_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_packet_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_in_t *
of_packet_in_OF_VERSION_1_0_dup(
    of_packet_in_t *src)
{
    of_packet_in_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_packet_in_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_in_xid_get(src, &val32);
    of_packet_in_xid_set(dst, val32);

    of_packet_in_buffer_id_get(src, &val32);
    of_packet_in_buffer_id_set(dst, val32);

    of_packet_in_total_len_get(src, &val16);
    of_packet_in_total_len_set(dst, val16);

    of_packet_in_in_port_get(src, &port_no);
    of_packet_in_in_port_set(dst, port_no);

    of_packet_in_reason_get(src, &val8);
    of_packet_in_reason_set(dst, val8);

    of_packet_in_data_get(src, &octets);
    of_packet_in_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_out_t *
of_packet_out_OF_VERSION_1_0_dup(
    of_packet_out_t *src)
{
    of_packet_out_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;
    of_octets_t octets;

    if ((dst = of_packet_out_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_out_xid_get(src, &val32);
    of_packet_out_xid_set(dst, val32);

    of_packet_out_buffer_id_get(src, &val32);
    of_packet_out_buffer_id_set(dst, val32);

    of_packet_out_in_port_get(src, &port_no);
    of_packet_out_in_port_set(dst, port_no);

    of_packet_out_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_out_delete(dst);
        return NULL;
    }
    of_packet_out_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    of_packet_out_data_get(src, &octets);
    of_packet_out_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_t *
of_port_mod_OF_VERSION_1_0_dup(
    of_port_mod_t *src)
{
    of_port_mod_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;

    if ((dst = of_port_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_xid_get(src, &val32);
    of_port_mod_xid_set(dst, val32);

    of_port_mod_port_no_get(src, &port_no);
    of_port_mod_port_no_set(dst, port_no);

    of_port_mod_hw_addr_get(src, &mac_addr);
    of_port_mod_hw_addr_set(dst, mac_addr);

    of_port_mod_config_get(src, &val32);
    of_port_mod_config_set(dst, val32);

    of_port_mod_mask_get(src, &val32);
    of_port_mod_mask_set(dst, val32);

    of_port_mod_advertise_get(src, &val32);
    of_port_mod_advertise_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_failed_error_msg_t *
of_port_mod_failed_error_msg_OF_VERSION_1_0_dup(
    of_port_mod_failed_error_msg_t *src)
{
    of_port_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_port_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_failed_error_msg_xid_get(src, &val32);
    of_port_mod_failed_error_msg_xid_set(dst, val32);

    of_port_mod_failed_error_msg_code_get(src, &val16);
    of_port_mod_failed_error_msg_code_set(dst, val16);

    of_port_mod_failed_error_msg_data_get(src, &octets);
    of_port_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_reply_t *
of_port_stats_reply_OF_VERSION_1_0_dup(
    of_port_stats_reply_t *src)
{
    of_port_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_stats_entry_t src_list;
    of_list_port_stats_entry_t *dst_list;

    if ((dst = of_port_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_reply_xid_get(src, &val32);
    of_port_stats_reply_xid_set(dst, val32);

    of_port_stats_reply_flags_get(src, &val16);
    of_port_stats_reply_flags_set(dst, val16);

    of_port_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_stats_entry_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_reply_delete(dst);
        return NULL;
    }
    of_port_stats_reply_entries_set(dst, dst_list);
    of_list_port_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_request_t *
of_port_stats_request_OF_VERSION_1_0_dup(
    of_port_stats_request_t *src)
{
    of_port_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_port_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_request_xid_get(src, &val32);
    of_port_stats_request_xid_set(dst, val32);

    of_port_stats_request_flags_get(src, &val16);
    of_port_stats_request_flags_set(dst, val16);

    of_port_stats_request_port_no_get(src, &port_no);
    of_port_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_port_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_status_t *
of_port_status_OF_VERSION_1_0_dup(
    of_port_status_t *src)
{
    of_port_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_port_desc_t src_port_desc;
    of_port_desc_t *dst_port_desc;

    if ((dst = of_port_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_status_xid_get(src, &val32);
    of_port_status_xid_set(dst, val32);

    of_port_status_reason_get(src, &val8);
    of_port_status_reason_set(dst, val8);

    of_port_status_desc_bind(
        src, &src_port_desc);
    dst_port_desc = of_port_desc_OF_VERSION_1_0_dup(&src_port_desc);
    if (dst_port_desc == NULL) {
        of_port_status_delete(dst);
        return NULL;
    }
    of_port_status_desc_set(dst, dst_port_desc);
    of_port_desc_delete(dst_port_desc);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_reply_t *
of_queue_get_config_reply_OF_VERSION_1_0_dup(
    of_queue_get_config_reply_t *src)
{
    of_queue_get_config_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_packet_queue_t src_list;
    of_list_packet_queue_t *dst_list;

    if ((dst = of_queue_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_reply_xid_get(src, &val32);
    of_queue_get_config_reply_xid_set(dst, val32);

    of_queue_get_config_reply_port_get(src, &port_no);
    of_queue_get_config_reply_port_set(dst, port_no);

    of_queue_get_config_reply_queues_bind(
        src, &src_list);
    dst_list = of_list_packet_queue_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_get_config_reply_delete(dst);
        return NULL;
    }
    of_queue_get_config_reply_queues_set(dst, dst_list);
    of_list_packet_queue_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_request_t *
of_queue_get_config_request_OF_VERSION_1_0_dup(
    of_queue_get_config_request_t *src)
{
    of_queue_get_config_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_queue_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_request_xid_get(src, &val32);
    of_queue_get_config_request_xid_set(dst, val32);

    of_queue_get_config_request_port_get(src, &port_no);
    of_queue_get_config_request_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_queue_op_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_op_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_op_failed_error_msg_t *
of_queue_op_failed_error_msg_OF_VERSION_1_0_dup(
    of_queue_op_failed_error_msg_t *src)
{
    of_queue_op_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_queue_op_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_op_failed_error_msg_xid_get(src, &val32);
    of_queue_op_failed_error_msg_xid_set(dst, val32);

    of_queue_op_failed_error_msg_code_get(src, &val16);
    of_queue_op_failed_error_msg_code_set(dst, val16);

    of_queue_op_failed_error_msg_data_get(src, &octets);
    of_queue_op_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_reply_t *
of_queue_stats_reply_OF_VERSION_1_0_dup(
    of_queue_stats_reply_t *src)
{
    of_queue_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_stats_entry_t src_list;
    of_list_queue_stats_entry_t *dst_list;

    if ((dst = of_queue_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_reply_xid_get(src, &val32);
    of_queue_stats_reply_xid_set(dst, val32);

    of_queue_stats_reply_flags_get(src, &val16);
    of_queue_stats_reply_flags_set(dst, val16);

    of_queue_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_entry_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_stats_reply_entries_set(dst, dst_list);
    of_list_queue_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_request_t *
of_queue_stats_request_OF_VERSION_1_0_dup(
    of_queue_stats_request_t *src)
{
    of_queue_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_request_xid_get(src, &val32);
    of_queue_stats_request_xid_set(dst, val32);

    of_queue_stats_request_flags_get(src, &val16);
    of_queue_stats_request_flags_set(dst, val16);

    of_queue_stats_request_port_no_get(src, &port_no);
    of_queue_stats_request_port_no_set(dst, port_no);

    of_queue_stats_request_queue_id_get(src, &val32);
    of_queue_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_set_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_set_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_set_config_t *
of_set_config_OF_VERSION_1_0_dup(
    of_set_config_t *src)
{
    of_set_config_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_set_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_set_config_xid_get(src, &val32);
    of_set_config_xid_set(dst, val32);

    of_set_config_flags_get(src, &val16);
    of_set_config_flags_set(dst, val16);

    of_set_config_miss_send_len_get(src, &val16);
    of_set_config_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_t *
of_table_mod_OF_VERSION_1_0_dup(
    of_table_mod_t *src)
{
    of_table_mod_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_table_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_xid_get(src, &val32);
    of_table_mod_xid_set(dst, val32);

    of_table_mod_table_id_get(src, &val8);
    of_table_mod_table_id_set(dst, val8);

    of_table_mod_config_get(src, &val32);
    of_table_mod_config_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_reply_t *
of_table_stats_reply_OF_VERSION_1_0_dup(
    of_table_stats_reply_t *src)
{
    of_table_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_stats_entry_t src_list;
    of_list_table_stats_entry_t *dst_list;

    if ((dst = of_table_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_reply_xid_get(src, &val32);
    of_table_stats_reply_xid_set(dst, val32);

    of_table_stats_reply_flags_get(src, &val16);
    of_table_stats_reply_flags_set(dst, val16);

    of_table_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_stats_entry_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_table_stats_reply_delete(dst);
        return NULL;
    }
    of_table_stats_reply_entries_set(dst, dst_list);
    of_list_table_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_request_t *
of_table_stats_request_OF_VERSION_1_0_dup(
    of_table_stats_request_t *src)
{
    of_table_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_request_xid_get(src, &val32);
    of_table_stats_request_xid_set(dst, val32);

    of_table_stats_request_flags_get(src, &val16);
    of_table_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate a super class object of type of_action
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_OF_VERSION_1_0_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_NICIRA_DEC_TTL) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_DL_SRC) {
        return of_action_set_dl_src_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_VLAN_VID) {
        return of_action_set_vlan_vid_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_VLAN_PCP) {
        return of_action_set_vlan_pcp_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_OUTPUT) {
        return of_action_output_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_DL_DST) {
        return of_action_set_dl_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_SET_TUNNEL_DST) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_ENQUEUE) {
        return of_action_enqueue_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TOS) {
        return of_action_set_nw_tos_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_MIRROR) {
        return of_action_bsn_mirror_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_TP_SRC) {
        return of_action_set_tp_src_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_DST) {
        return of_action_set_nw_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_CHECKSUM) {
        return of_action_bsn_checksum_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_TP_DST) {
        return of_action_set_tp_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_STRIP_VLAN) {
        return of_action_strip_vlan_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_SRC) {
        return of_action_set_nw_src_OF_VERSION_1_0_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_checksum_t *
of_action_bsn_checksum_OF_VERSION_1_0_dup(
    of_action_bsn_checksum_t *src)
{
    of_action_bsn_checksum_t *dst;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_action_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_checksum_experimenter_get(src, &val32);
    of_action_bsn_checksum_experimenter_set(dst, val32);

    of_action_bsn_checksum_subtype_get(src, &val32);
    of_action_bsn_checksum_subtype_set(dst, val32);

    of_action_bsn_checksum_checksum_get(src, &checksum_128);
    of_action_bsn_checksum_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_mirror_t *
of_action_bsn_mirror_OF_VERSION_1_0_dup(
    of_action_bsn_mirror_t *src)
{
    of_action_bsn_mirror_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_action_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_mirror_experimenter_get(src, &val32);
    of_action_bsn_mirror_experimenter_set(dst, val32);

    of_action_bsn_mirror_subtype_get(src, &val32);
    of_action_bsn_mirror_subtype_set(dst, val32);

    of_action_bsn_mirror_dest_port_get(src, &val32);
    of_action_bsn_mirror_dest_port_set(dst, val32);

    of_action_bsn_mirror_vlan_tag_get(src, &val32);
    of_action_bsn_mirror_vlan_tag_set(dst, val32);

    of_action_bsn_mirror_copy_stage_get(src, &val8);
    of_action_bsn_mirror_copy_stage_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_set_tunnel_dst_t *
of_action_bsn_set_tunnel_dst_OF_VERSION_1_0_dup(
    of_action_bsn_set_tunnel_dst_t *src)
{
    of_action_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_bsn_set_tunnel_dst_subtype_set(dst, val32);

    of_action_bsn_set_tunnel_dst_dst_get(src, &val32);
    of_action_bsn_set_tunnel_dst_dst_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_enqueue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_enqueue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_enqueue_t *
of_action_enqueue_OF_VERSION_1_0_dup(
    of_action_enqueue_t *src)
{
    of_action_enqueue_t *dst;
    of_port_no_t port_no;
    uint32_t val32;

    if ((dst = of_action_enqueue_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_enqueue_port_get(src, &port_no);
    of_action_enqueue_port_set(dst, port_no);

    of_action_enqueue_queue_id_get(src, &val32);
    of_action_enqueue_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_nicira_dec_ttl_t *
of_action_nicira_dec_ttl_OF_VERSION_1_0_dup(
    of_action_nicira_dec_ttl_t *src)
{
    of_action_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_output_t *
of_action_output_OF_VERSION_1_0_dup(
    of_action_output_t *src)
{
    of_action_output_t *dst;
    of_port_no_t port_no;
    uint16_t val16;

    if ((dst = of_action_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_output_port_get(src, &port_no);
    of_action_output_port_set(dst, port_no);

    of_action_output_max_len_get(src, &val16);
    of_action_output_max_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_dl_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_dl_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_dl_dst_t *
of_action_set_dl_dst_OF_VERSION_1_0_dup(
    of_action_set_dl_dst_t *src)
{
    of_action_set_dl_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_action_set_dl_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_dl_dst_dl_addr_get(src, &mac_addr);
    of_action_set_dl_dst_dl_addr_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_dl_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_dl_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_dl_src_t *
of_action_set_dl_src_OF_VERSION_1_0_dup(
    of_action_set_dl_src_t *src)
{
    of_action_set_dl_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_action_set_dl_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_dl_src_dl_addr_get(src, &mac_addr);
    of_action_set_dl_src_dl_addr_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_dst_t *
of_action_set_nw_dst_OF_VERSION_1_0_dup(
    of_action_set_nw_dst_t *src)
{
    of_action_set_nw_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_nw_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_dst_nw_addr_get(src, &val32);
    of_action_set_nw_dst_nw_addr_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_src_t *
of_action_set_nw_src_OF_VERSION_1_0_dup(
    of_action_set_nw_src_t *src)
{
    of_action_set_nw_src_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_nw_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_src_nw_addr_get(src, &val32);
    of_action_set_nw_src_nw_addr_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_tos
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_tos.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_tos_t *
of_action_set_nw_tos_OF_VERSION_1_0_dup(
    of_action_set_nw_tos_t *src)
{
    of_action_set_nw_tos_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_tos_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_tos_nw_tos_get(src, &val8);
    of_action_set_nw_tos_nw_tos_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_tp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_tp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_tp_dst_t *
of_action_set_tp_dst_OF_VERSION_1_0_dup(
    of_action_set_tp_dst_t *src)
{
    of_action_set_tp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_tp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_tp_dst_tp_port_get(src, &val16);
    of_action_set_tp_dst_tp_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_tp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_tp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_tp_src_t *
of_action_set_tp_src_OF_VERSION_1_0_dup(
    of_action_set_tp_src_t *src)
{
    of_action_set_tp_src_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_tp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_tp_src_tp_port_get(src, &val16);
    of_action_set_tp_src_tp_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_vlan_pcp_t *
of_action_set_vlan_pcp_OF_VERSION_1_0_dup(
    of_action_set_vlan_pcp_t *src)
{
    of_action_set_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_vlan_pcp_vlan_pcp_get(src, &val8);
    of_action_set_vlan_pcp_vlan_pcp_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_vlan_vid_t *
of_action_set_vlan_vid_OF_VERSION_1_0_dup(
    of_action_set_vlan_vid_t *src)
{
    of_action_set_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_vlan_vid_vlan_vid_get(src, &val16);
    of_action_set_vlan_vid_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_strip_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_strip_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_strip_vlan_t *
of_action_strip_vlan_OF_VERSION_1_0_dup(
    of_action_strip_vlan_t *src)
{
    of_action_strip_vlan_t *dst;

    if ((dst = of_action_strip_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_interface_t *
of_bsn_interface_OF_VERSION_1_0_dup(
    of_bsn_interface_t *src)
{
    of_bsn_interface_t *dst;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_interface_hw_addr_get(src, &mac_addr);
    of_bsn_interface_hw_addr_set(dst, mac_addr);

    of_bsn_interface_name_get(src, &port_name);
    of_bsn_interface_name_set(dst, port_name);

    of_bsn_interface_ipv4_addr_get(src, &ipv4);
    of_bsn_interface_ipv4_addr_set(dst, ipv4);

    of_bsn_interface_ipv4_netmask_get(src, &ipv4);
    of_bsn_interface_ipv4_netmask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_vport
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_vport_OF_VERSION_1_0_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_VPORT_Q_IN_Q) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_0_dup(src);
    }

    if (src->object_id == OF_BSN_VPORT_L2GRE) {
        return of_bsn_vport_l2gre_OF_VERSION_1_0_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_vport_l2gre
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_l2gre.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_l2gre_t *
of_bsn_vport_l2gre_OF_VERSION_1_0_dup(
    of_bsn_vport_l2gre_t *src)
{
    of_bsn_vport_l2gre_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_ipv4_t ipv4;
    uint8_t val8;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_l2gre_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_l2gre_flags_get(src, &val32);
    of_bsn_vport_l2gre_flags_set(dst, val32);

    of_bsn_vport_l2gre_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_loopback_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_loopback_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_local_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_local_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_nh_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_nh_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_src_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_src_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dst_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_dst_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dscp_get(src, &val8);
    of_bsn_vport_l2gre_dscp_set(dst, val8);

    of_bsn_vport_l2gre_ttl_get(src, &val8);
    of_bsn_vport_l2gre_ttl_set(dst, val8);

    of_bsn_vport_l2gre_vpn_get(src, &val32);
    of_bsn_vport_l2gre_vpn_set(dst, val32);

    of_bsn_vport_l2gre_rate_limit_get(src, &val32);
    of_bsn_vport_l2gre_rate_limit_set(dst, val32);

    of_bsn_vport_l2gre_if_name_get(src, &port_name);
    of_bsn_vport_l2gre_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vport_q_in_q
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_q_in_q.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_q_in_q_t *
of_bsn_vport_q_in_q_OF_VERSION_1_0_dup(
    of_bsn_vport_q_in_q_t *src)
{
    of_bsn_vport_q_in_q_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_q_in_q_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_q_in_q_port_no_get(src, &val32);
    of_bsn_vport_q_in_q_port_no_set(dst, val32);

    of_bsn_vport_q_in_q_ingress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_ingress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_egress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_egress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_egress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_egress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_if_name_get(src, &port_name);
    of_bsn_vport_q_in_q_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_entry_t *
of_flow_stats_entry_OF_VERSION_1_0_dup(
    of_flow_stats_entry_t *src)
{
    of_flow_stats_entry_t *dst;
    uint8_t val8;
    of_match_t match;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_entry_table_id_get(src, &val8);
    of_flow_stats_entry_table_id_set(dst, val8);

    of_flow_stats_entry_match_get(src, &match);
    of_flow_stats_entry_match_set(dst, &match);

    of_flow_stats_entry_duration_sec_get(src, &val32);
    of_flow_stats_entry_duration_sec_set(dst, val32);

    of_flow_stats_entry_duration_nsec_get(src, &val32);
    of_flow_stats_entry_duration_nsec_set(dst, val32);

    of_flow_stats_entry_priority_get(src, &val16);
    of_flow_stats_entry_priority_set(dst, val16);

    of_flow_stats_entry_idle_timeout_get(src, &val16);
    of_flow_stats_entry_idle_timeout_set(dst, val16);

    of_flow_stats_entry_hard_timeout_get(src, &val16);
    of_flow_stats_entry_hard_timeout_set(dst, val16);

    of_flow_stats_entry_cookie_get(src, &val64);
    of_flow_stats_entry_cookie_set(dst, val64);

    of_flow_stats_entry_packet_count_get(src, &val64);
    of_flow_stats_entry_packet_count_set(dst, val64);

    of_flow_stats_entry_byte_count_get(src, &val64);
    of_flow_stats_entry_byte_count_set(dst, val64);

    of_flow_stats_entry_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_entry_delete(dst);
        return NULL;
    }
    of_flow_stats_entry_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_match_v1
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_match_v1.
 *
 * The caller is responsible for deleting the returned value
 */
of_match_v1_t *
of_match_v1_OF_VERSION_1_0_dup(
    of_match_v1_t *src)
{
    of_match_v1_t *dst;
    of_wc_bmap_t wc_bmap;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    uint16_t val16;
    uint8_t val8;
    of_ipv4_t ipv4;

    if ((dst = of_match_v1_new(src->version)) == NULL) {
        return NULL;
    }

    of_match_v1_wildcards_get(src, &wc_bmap);
    of_match_v1_wildcards_set(dst, wc_bmap);

    of_match_v1_in_port_get(src, &port_no);
    of_match_v1_in_port_set(dst, port_no);

    of_match_v1_eth_src_get(src, &mac_addr);
    of_match_v1_eth_src_set(dst, mac_addr);

    of_match_v1_eth_dst_get(src, &mac_addr);
    of_match_v1_eth_dst_set(dst, mac_addr);

    of_match_v1_vlan_vid_get(src, &val16);
    of_match_v1_vlan_vid_set(dst, val16);

    of_match_v1_vlan_pcp_get(src, &val8);
    of_match_v1_vlan_pcp_set(dst, val8);

    of_match_v1_eth_type_get(src, &val16);
    of_match_v1_eth_type_set(dst, val16);

    of_match_v1_ip_dscp_get(src, &val8);
    of_match_v1_ip_dscp_set(dst, val8);

    of_match_v1_ip_proto_get(src, &val8);
    of_match_v1_ip_proto_set(dst, val8);

    of_match_v1_ipv4_src_get(src, &ipv4);
    of_match_v1_ipv4_src_set(dst, ipv4);

    of_match_v1_ipv4_dst_get(src, &ipv4);
    of_match_v1_ipv4_dst_set(dst, ipv4);

    of_match_v1_tcp_src_get(src, &val16);
    of_match_v1_tcp_src_set(dst, val16);

    of_match_v1_tcp_dst_get(src, &val16);
    of_match_v1_tcp_dst_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_queue_t *
of_packet_queue_OF_VERSION_1_0_dup(
    of_packet_queue_t *src)
{
    of_packet_queue_t *dst;
    uint32_t val32;

    of_list_queue_prop_t src_list;
    of_list_queue_prop_t *dst_list;

    if ((dst = of_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_queue_queue_id_get(src, &val32);
    of_packet_queue_queue_id_set(dst, val32);

    of_packet_queue_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_prop_OF_VERSION_1_0_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_queue_delete(dst);
        return NULL;
    }
    of_packet_queue_properties_set(dst, dst_list);
    of_list_queue_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_t *
of_port_desc_OF_VERSION_1_0_dup(
    of_port_desc_t *src)
{
    of_port_desc_t *dst;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    uint32_t val32;

    if ((dst = of_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_port_no_get(src, &port_no);
    of_port_desc_port_no_set(dst, port_no);

    of_port_desc_hw_addr_get(src, &mac_addr);
    of_port_desc_hw_addr_set(dst, mac_addr);

    of_port_desc_name_get(src, &port_name);
    of_port_desc_name_set(dst, port_name);

    of_port_desc_config_get(src, &val32);
    of_port_desc_config_set(dst, val32);

    of_port_desc_state_get(src, &val32);
    of_port_desc_state_set(dst, val32);

    of_port_desc_curr_get(src, &val32);
    of_port_desc_curr_set(dst, val32);

    of_port_desc_advertised_get(src, &val32);
    of_port_desc_advertised_set(dst, val32);

    of_port_desc_supported_get(src, &val32);
    of_port_desc_supported_set(dst, val32);

    of_port_desc_peer_get(src, &val32);
    of_port_desc_peer_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_entry_t *
of_port_stats_entry_OF_VERSION_1_0_dup(
    of_port_stats_entry_t *src)
{
    of_port_stats_entry_t *dst;
    of_port_no_t port_no;
    uint64_t val64;

    if ((dst = of_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_entry_port_no_get(src, &port_no);
    of_port_stats_entry_port_no_set(dst, port_no);

    of_port_stats_entry_rx_packets_get(src, &val64);
    of_port_stats_entry_rx_packets_set(dst, val64);

    of_port_stats_entry_tx_packets_get(src, &val64);
    of_port_stats_entry_tx_packets_set(dst, val64);

    of_port_stats_entry_rx_bytes_get(src, &val64);
    of_port_stats_entry_rx_bytes_set(dst, val64);

    of_port_stats_entry_tx_bytes_get(src, &val64);
    of_port_stats_entry_tx_bytes_set(dst, val64);

    of_port_stats_entry_rx_dropped_get(src, &val64);
    of_port_stats_entry_rx_dropped_set(dst, val64);

    of_port_stats_entry_tx_dropped_get(src, &val64);
    of_port_stats_entry_tx_dropped_set(dst, val64);

    of_port_stats_entry_rx_errors_get(src, &val64);
    of_port_stats_entry_rx_errors_set(dst, val64);

    of_port_stats_entry_tx_errors_get(src, &val64);
    of_port_stats_entry_tx_errors_set(dst, val64);

    of_port_stats_entry_rx_frame_err_get(src, &val64);
    of_port_stats_entry_rx_frame_err_set(dst, val64);

    of_port_stats_entry_rx_over_err_get(src, &val64);
    of_port_stats_entry_rx_over_err_set(dst, val64);

    of_port_stats_entry_rx_crc_err_get(src, &val64);
    of_port_stats_entry_rx_crc_err_set(dst, val64);

    of_port_stats_entry_collisions_get(src, &val64);
    of_port_stats_entry_collisions_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_prop_OF_VERSION_1_0_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_PROP_MIN_RATE) {
        return of_queue_prop_min_rate_OF_VERSION_1_0_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_min_rate_t *
of_queue_prop_min_rate_OF_VERSION_1_0_dup(
    of_queue_prop_min_rate_t *src)
{
    of_queue_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_min_rate_rate_get(src, &val16);
    of_queue_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_entry_t *
of_queue_stats_entry_OF_VERSION_1_0_dup(
    of_queue_stats_entry_t *src)
{
    of_queue_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_entry_port_no_get(src, &port_no);
    of_queue_stats_entry_port_no_set(dst, port_no);

    of_queue_stats_entry_queue_id_get(src, &val32);
    of_queue_stats_entry_queue_id_set(dst, val32);

    of_queue_stats_entry_tx_bytes_get(src, &val64);
    of_queue_stats_entry_tx_bytes_set(dst, val64);

    of_queue_stats_entry_tx_packets_get(src, &val64);
    of_queue_stats_entry_tx_packets_set(dst, val64);

    of_queue_stats_entry_tx_errors_get(src, &val64);
    of_queue_stats_entry_tx_errors_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_entry_t *
of_table_stats_entry_OF_VERSION_1_0_dup(
    of_table_stats_entry_t *src)
{
    of_table_stats_entry_t *dst;
    uint8_t val8;
    of_table_name_t table_name;
    of_wc_bmap_t wc_bmap;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_entry_table_id_get(src, &val8);
    of_table_stats_entry_table_id_set(dst, val8);

    of_table_stats_entry_name_get(src, &table_name);
    of_table_stats_entry_name_set(dst, table_name);

    of_table_stats_entry_wildcards_get(src, &wc_bmap);
    of_table_stats_entry_wildcards_set(dst, wc_bmap);

    of_table_stats_entry_max_entries_get(src, &val32);
    of_table_stats_entry_max_entries_set(dst, val32);

    of_table_stats_entry_active_count_get(src, &val32);
    of_table_stats_entry_active_count_set(dst, val32);

    of_table_stats_entry_lookup_count_get(src, &val64);
    of_table_stats_entry_lookup_count_set(dst, val64);

    of_table_stats_entry_matched_count_get(src, &val64);
    of_table_stats_entry_matched_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate a list of type of_list_action
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_t *
of_list_action_OF_VERSION_1_0_dup(
    of_list_action_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_t *dst;

    if ((dst = of_list_action_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_interface_t *
of_list_bsn_interface_OF_VERSION_1_0_dup(
    of_list_bsn_interface_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_interface_t *dst;

    if ((dst = of_list_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_INTERFACE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_interface_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_interface_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_flow_stats_entry_t *
of_list_flow_stats_entry_OF_VERSION_1_0_dup(
    of_list_flow_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_flow_stats_entry_t *dst;

    if ((dst = of_list_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_FLOW_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_flow_stats_entry_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_flow_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_packet_queue_t *
of_list_packet_queue_OF_VERSION_1_0_dup(
    of_list_packet_queue_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_packet_queue_t *dst;

    if ((dst = of_list_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PACKET_QUEUE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_packet_queue_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_packet_queue_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_t *
of_list_port_desc_OF_VERSION_1_0_dup(
    of_list_port_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_t *dst;

    if ((dst = of_list_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_entry_t *
of_list_port_stats_entry_OF_VERSION_1_0_dup(
    of_list_port_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_entry_t *dst;

    if ((dst = of_list_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_entry_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_prop_t *
of_list_queue_prop_OF_VERSION_1_0_dup(
    of_list_queue_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_prop_t *dst;

    if ((dst = of_list_queue_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_prop_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_entry_t *
of_list_queue_stats_entry_OF_VERSION_1_0_dup(
    of_list_queue_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_entry_t *dst;

    if ((dst = of_list_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_entry_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_stats_entry_t *
of_list_table_stats_entry_OF_VERSION_1_0_dup(
    of_list_table_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_stats_entry_t *dst;

    if ((dst = of_list_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_stats_entry_OF_VERSION_1_0_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_reply_t *
of_aggregate_stats_reply_OF_VERSION_1_1_dup(
    of_aggregate_stats_reply_t *src)
{
    of_aggregate_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_aggregate_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_reply_xid_get(src, &val32);
    of_aggregate_stats_reply_xid_set(dst, val32);

    of_aggregate_stats_reply_flags_get(src, &val16);
    of_aggregate_stats_reply_flags_set(dst, val16);

    of_aggregate_stats_reply_packet_count_get(src, &val64);
    of_aggregate_stats_reply_packet_count_set(dst, val64);

    of_aggregate_stats_reply_byte_count_get(src, &val64);
    of_aggregate_stats_reply_byte_count_set(dst, val64);

    of_aggregate_stats_reply_flow_count_get(src, &val32);
    of_aggregate_stats_reply_flow_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_request_t *
of_aggregate_stats_request_OF_VERSION_1_1_dup(
    of_aggregate_stats_request_t *src)
{
    of_aggregate_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_aggregate_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_request_xid_get(src, &val32);
    of_aggregate_stats_request_xid_set(dst, val32);

    of_aggregate_stats_request_flags_get(src, &val16);
    of_aggregate_stats_request_flags_set(dst, val16);

    of_aggregate_stats_request_table_id_get(src, &val8);
    of_aggregate_stats_request_table_id_set(dst, val8);

    of_aggregate_stats_request_out_port_get(src, &port_no);
    of_aggregate_stats_request_out_port_set(dst, port_no);

    of_aggregate_stats_request_out_group_get(src, &val32);
    of_aggregate_stats_request_out_group_set(dst, val32);

    of_aggregate_stats_request_cookie_get(src, &val64);
    of_aggregate_stats_request_cookie_set(dst, val64);

    of_aggregate_stats_request_cookie_mask_get(src, &val64);
    of_aggregate_stats_request_cookie_mask_set(dst, val64);

    of_aggregate_stats_request_match_get(src, &match);
    of_aggregate_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_bad_action_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_action_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_action_error_msg_t *
of_bad_action_error_msg_OF_VERSION_1_1_dup(
    of_bad_action_error_msg_t *src)
{
    of_bad_action_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_action_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_action_error_msg_xid_get(src, &val32);
    of_bad_action_error_msg_xid_set(dst, val32);

    of_bad_action_error_msg_code_get(src, &val16);
    of_bad_action_error_msg_code_set(dst, val16);

    of_bad_action_error_msg_data_get(src, &octets);
    of_bad_action_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_instruction_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_instruction_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_instruction_error_msg_t *
of_bad_instruction_error_msg_OF_VERSION_1_1_dup(
    of_bad_instruction_error_msg_t *src)
{
    of_bad_instruction_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_instruction_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_instruction_error_msg_xid_get(src, &val32);
    of_bad_instruction_error_msg_xid_set(dst, val32);

    of_bad_instruction_error_msg_code_get(src, &val16);
    of_bad_instruction_error_msg_code_set(dst, val16);

    of_bad_instruction_error_msg_data_get(src, &octets);
    of_bad_instruction_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_match_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_match_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_match_error_msg_t *
of_bad_match_error_msg_OF_VERSION_1_1_dup(
    of_bad_match_error_msg_t *src)
{
    of_bad_match_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_match_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_match_error_msg_xid_get(src, &val32);
    of_bad_match_error_msg_xid_set(dst, val32);

    of_bad_match_error_msg_code_get(src, &val16);
    of_bad_match_error_msg_code_set(dst, val16);

    of_bad_match_error_msg_data_get(src, &octets);
    of_bad_match_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_request_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_request_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_request_error_msg_t *
of_bad_request_error_msg_OF_VERSION_1_1_dup(
    of_bad_request_error_msg_t *src)
{
    of_bad_request_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_request_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_request_error_msg_xid_get(src, &val32);
    of_bad_request_error_msg_xid_set(dst, val32);

    of_bad_request_error_msg_code_get(src, &val16);
    of_bad_request_error_msg_code_set(dst, val16);

    of_bad_request_error_msg_data_get(src, &octets);
    of_bad_request_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_reply_t *
of_barrier_reply_OF_VERSION_1_1_dup(
    of_barrier_reply_t *src)
{
    of_barrier_reply_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_reply_xid_get(src, &val32);
    of_barrier_reply_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_request_t *
of_barrier_request_OF_VERSION_1_1_dup(
    of_barrier_request_t *src)
{
    of_barrier_request_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_request_xid_get(src, &val32);
    of_barrier_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_reply_t *
of_bsn_bw_clear_data_reply_OF_VERSION_1_1_dup(
    of_bsn_bw_clear_data_reply_t *src)
{
    of_bsn_bw_clear_data_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_reply_xid_get(src, &val32);
    of_bsn_bw_clear_data_reply_xid_set(dst, val32);

    of_bsn_bw_clear_data_reply_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_reply_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_reply_subtype_get(src, &val32);
    of_bsn_bw_clear_data_reply_subtype_set(dst, val32);

    of_bsn_bw_clear_data_reply_status_get(src, &val32);
    of_bsn_bw_clear_data_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_request_t *
of_bsn_bw_clear_data_request_OF_VERSION_1_1_dup(
    of_bsn_bw_clear_data_request_t *src)
{
    of_bsn_bw_clear_data_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_request_xid_get(src, &val32);
    of_bsn_bw_clear_data_request_xid_set(dst, val32);

    of_bsn_bw_clear_data_request_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_request_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_request_subtype_get(src, &val32);
    of_bsn_bw_clear_data_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_reply_t *
of_bsn_bw_enable_get_reply_OF_VERSION_1_1_dup(
    of_bsn_bw_enable_get_reply_t *src)
{
    of_bsn_bw_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_reply_xid_get(src, &val32);
    of_bsn_bw_enable_get_reply_xid_set(dst, val32);

    of_bsn_bw_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_get_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_get_reply_enabled_get(src, &val32);
    of_bsn_bw_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_request_t *
of_bsn_bw_enable_get_request_OF_VERSION_1_1_dup(
    of_bsn_bw_enable_get_request_t *src)
{
    of_bsn_bw_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_request_xid_get(src, &val32);
    of_bsn_bw_enable_get_request_xid_set(dst, val32);

    of_bsn_bw_enable_get_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_request_subtype_get(src, &val32);
    of_bsn_bw_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_reply_t *
of_bsn_bw_enable_set_reply_OF_VERSION_1_1_dup(
    of_bsn_bw_enable_set_reply_t *src)
{
    of_bsn_bw_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_reply_xid_get(src, &val32);
    of_bsn_bw_enable_set_reply_xid_set(dst, val32);

    of_bsn_bw_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_set_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_set_reply_enable_get(src, &val32);
    of_bsn_bw_enable_set_reply_enable_set(dst, val32);

    of_bsn_bw_enable_set_reply_status_get(src, &val32);
    of_bsn_bw_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_request_t *
of_bsn_bw_enable_set_request_OF_VERSION_1_1_dup(
    of_bsn_bw_enable_set_request_t *src)
{
    of_bsn_bw_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_request_xid_get(src, &val32);
    of_bsn_bw_enable_set_request_xid_set(dst, val32);

    of_bsn_bw_enable_set_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_request_subtype_get(src, &val32);
    of_bsn_bw_enable_set_request_subtype_set(dst, val32);

    of_bsn_bw_enable_set_request_enable_get(src, &val32);
    of_bsn_bw_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_reply_t *
of_bsn_get_interfaces_reply_OF_VERSION_1_1_dup(
    of_bsn_get_interfaces_reply_t *src)
{
    of_bsn_get_interfaces_reply_t *dst;
    uint32_t val32;

    of_list_bsn_interface_t src_list;
    of_list_bsn_interface_t *dst_list;

    if ((dst = of_bsn_get_interfaces_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_reply_xid_get(src, &val32);
    of_bsn_get_interfaces_reply_xid_set(dst, val32);

    of_bsn_get_interfaces_reply_experimenter_get(src, &val32);
    of_bsn_get_interfaces_reply_experimenter_set(dst, val32);

    of_bsn_get_interfaces_reply_subtype_get(src, &val32);
    of_bsn_get_interfaces_reply_subtype_set(dst, val32);

    of_bsn_get_interfaces_reply_interfaces_bind(
        src, &src_list);
    dst_list = of_list_bsn_interface_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_get_interfaces_reply_delete(dst);
        return NULL;
    }
    of_bsn_get_interfaces_reply_interfaces_set(dst, dst_list);
    of_list_bsn_interface_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_request_t *
of_bsn_get_interfaces_request_OF_VERSION_1_1_dup(
    of_bsn_get_interfaces_request_t *src)
{
    of_bsn_get_interfaces_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_interfaces_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_request_xid_get(src, &val32);
    of_bsn_get_interfaces_request_xid_set(dst, val32);

    of_bsn_get_interfaces_request_experimenter_get(src, &val32);
    of_bsn_get_interfaces_request_experimenter_set(dst, val32);

    of_bsn_get_interfaces_request_subtype_get(src, &val32);
    of_bsn_get_interfaces_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_reply_t *
of_bsn_get_mirroring_reply_OF_VERSION_1_1_dup(
    of_bsn_get_mirroring_reply_t *src)
{
    of_bsn_get_mirroring_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_reply_xid_get(src, &val32);
    of_bsn_get_mirroring_reply_xid_set(dst, val32);

    of_bsn_get_mirroring_reply_experimenter_get(src, &val32);
    of_bsn_get_mirroring_reply_experimenter_set(dst, val32);

    of_bsn_get_mirroring_reply_subtype_get(src, &val32);
    of_bsn_get_mirroring_reply_subtype_set(dst, val32);

    of_bsn_get_mirroring_reply_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_reply_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_request_t *
of_bsn_get_mirroring_request_OF_VERSION_1_1_dup(
    of_bsn_get_mirroring_request_t *src)
{
    of_bsn_get_mirroring_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_request_xid_get(src, &val32);
    of_bsn_get_mirroring_request_xid_set(dst, val32);

    of_bsn_get_mirroring_request_experimenter_get(src, &val32);
    of_bsn_get_mirroring_request_experimenter_set(dst, val32);

    of_bsn_get_mirroring_request_subtype_get(src, &val32);
    of_bsn_get_mirroring_request_subtype_set(dst, val32);

    of_bsn_get_mirroring_request_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_request_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_reply_t *
of_bsn_pdu_rx_reply_OF_VERSION_1_1_dup(
    of_bsn_pdu_rx_reply_t *src)
{
    of_bsn_pdu_rx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_reply_xid_get(src, &val32);
    of_bsn_pdu_rx_reply_xid_set(dst, val32);

    of_bsn_pdu_rx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_rx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_rx_reply_subtype_get(src, &val32);
    of_bsn_pdu_rx_reply_subtype_set(dst, val32);

    of_bsn_pdu_rx_reply_status_get(src, &val32);
    of_bsn_pdu_rx_reply_status_set(dst, val32);

    of_bsn_pdu_rx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_rx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_rx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_rx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_request_t *
of_bsn_pdu_rx_request_OF_VERSION_1_1_dup(
    of_bsn_pdu_rx_request_t *src)
{
    of_bsn_pdu_rx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_rx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_request_xid_get(src, &val32);
    of_bsn_pdu_rx_request_xid_set(dst, val32);

    of_bsn_pdu_rx_request_experimenter_get(src, &val32);
    of_bsn_pdu_rx_request_experimenter_set(dst, val32);

    of_bsn_pdu_rx_request_subtype_get(src, &val32);
    of_bsn_pdu_rx_request_subtype_set(dst, val32);

    of_bsn_pdu_rx_request_timeout_ms_get(src, &val32);
    of_bsn_pdu_rx_request_timeout_ms_set(dst, val32);

    of_bsn_pdu_rx_request_port_no_get(src, &port_no);
    of_bsn_pdu_rx_request_port_no_set(dst, port_no);

    of_bsn_pdu_rx_request_slot_num_get(src, &val8);
    of_bsn_pdu_rx_request_slot_num_set(dst, val8);

    of_bsn_pdu_rx_request_data_get(src, &octets);
    of_bsn_pdu_rx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_timeout_t *
of_bsn_pdu_rx_timeout_OF_VERSION_1_1_dup(
    of_bsn_pdu_rx_timeout_t *src)
{
    of_bsn_pdu_rx_timeout_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_timeout_xid_get(src, &val32);
    of_bsn_pdu_rx_timeout_xid_set(dst, val32);

    of_bsn_pdu_rx_timeout_experimenter_get(src, &val32);
    of_bsn_pdu_rx_timeout_experimenter_set(dst, val32);

    of_bsn_pdu_rx_timeout_subtype_get(src, &val32);
    of_bsn_pdu_rx_timeout_subtype_set(dst, val32);

    of_bsn_pdu_rx_timeout_port_no_get(src, &port_no);
    of_bsn_pdu_rx_timeout_port_no_set(dst, port_no);

    of_bsn_pdu_rx_timeout_slot_num_get(src, &val8);
    of_bsn_pdu_rx_timeout_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_reply_t *
of_bsn_pdu_tx_reply_OF_VERSION_1_1_dup(
    of_bsn_pdu_tx_reply_t *src)
{
    of_bsn_pdu_tx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_tx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_reply_xid_get(src, &val32);
    of_bsn_pdu_tx_reply_xid_set(dst, val32);

    of_bsn_pdu_tx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_tx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_tx_reply_subtype_get(src, &val32);
    of_bsn_pdu_tx_reply_subtype_set(dst, val32);

    of_bsn_pdu_tx_reply_status_get(src, &val32);
    of_bsn_pdu_tx_reply_status_set(dst, val32);

    of_bsn_pdu_tx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_tx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_tx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_tx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_request_t *
of_bsn_pdu_tx_request_OF_VERSION_1_1_dup(
    of_bsn_pdu_tx_request_t *src)
{
    of_bsn_pdu_tx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_tx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_request_xid_get(src, &val32);
    of_bsn_pdu_tx_request_xid_set(dst, val32);

    of_bsn_pdu_tx_request_experimenter_get(src, &val32);
    of_bsn_pdu_tx_request_experimenter_set(dst, val32);

    of_bsn_pdu_tx_request_subtype_get(src, &val32);
    of_bsn_pdu_tx_request_subtype_set(dst, val32);

    of_bsn_pdu_tx_request_tx_interval_ms_get(src, &val32);
    of_bsn_pdu_tx_request_tx_interval_ms_set(dst, val32);

    of_bsn_pdu_tx_request_port_no_get(src, &port_no);
    of_bsn_pdu_tx_request_port_no_set(dst, port_no);

    of_bsn_pdu_tx_request_slot_num_get(src, &val8);
    of_bsn_pdu_tx_request_slot_num_set(dst, val8);

    of_bsn_pdu_tx_request_data_get(src, &octets);
    of_bsn_pdu_tx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_mirroring
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_mirroring.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_mirroring_t *
of_bsn_set_mirroring_OF_VERSION_1_1_dup(
    of_bsn_set_mirroring_t *src)
{
    of_bsn_set_mirroring_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_mirroring_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_mirroring_xid_get(src, &val32);
    of_bsn_set_mirroring_xid_set(dst, val32);

    of_bsn_set_mirroring_experimenter_get(src, &val32);
    of_bsn_set_mirroring_experimenter_set(dst, val32);

    of_bsn_set_mirroring_subtype_get(src, &val32);
    of_bsn_set_mirroring_subtype_set(dst, val32);

    of_bsn_set_mirroring_report_mirror_ports_get(src, &val8);
    of_bsn_set_mirroring_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_reply_t *
of_bsn_set_pktin_suppression_reply_OF_VERSION_1_1_dup(
    of_bsn_set_pktin_suppression_reply_t *src)
{
    of_bsn_set_pktin_suppression_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_pktin_suppression_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_reply_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_status_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_request_t *
of_bsn_set_pktin_suppression_request_OF_VERSION_1_1_dup(
    of_bsn_set_pktin_suppression_request_t *src)
{
    of_bsn_set_pktin_suppression_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_bsn_set_pktin_suppression_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_request_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_request_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_request_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_request_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_request_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_request_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_request_enabled_get(src, &val8);
    of_bsn_set_pktin_suppression_request_enabled_set(dst, val8);

    of_bsn_set_pktin_suppression_request_idle_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_idle_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_hard_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_hard_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_priority_get(src, &val16);
    of_bsn_set_pktin_suppression_request_priority_set(dst, val16);

    of_bsn_set_pktin_suppression_request_cookie_get(src, &val64);
    of_bsn_set_pktin_suppression_request_cookie_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_reply_t *
of_bsn_virtual_port_create_reply_OF_VERSION_1_1_dup(
    of_bsn_virtual_port_create_reply_t *src)
{
    of_bsn_virtual_port_create_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_create_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_reply_xid_get(src, &val32);
    of_bsn_virtual_port_create_reply_xid_set(dst, val32);

    of_bsn_virtual_port_create_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_create_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_create_reply_status_get(src, &val32);
    of_bsn_virtual_port_create_reply_status_set(dst, val32);

    of_bsn_virtual_port_create_reply_vport_no_get(src, &val32);
    of_bsn_virtual_port_create_reply_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_request_t *
of_bsn_virtual_port_create_request_OF_VERSION_1_1_dup(
    of_bsn_virtual_port_create_request_t *src)
{
    of_bsn_virtual_port_create_request_t *dst;
    uint32_t val32;

    of_bsn_vport_q_in_q_t src_bsn_vport;
    of_bsn_vport_q_in_q_t *dst_bsn_vport;

    if ((dst = of_bsn_virtual_port_create_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_request_xid_get(src, &val32);
    of_bsn_virtual_port_create_request_xid_set(dst, val32);

    of_bsn_virtual_port_create_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_request_subtype_get(src, &val32);
    of_bsn_virtual_port_create_request_subtype_set(dst, val32);

    of_bsn_virtual_port_create_request_vport_bind(
        src, &src_bsn_vport);
    dst_bsn_vport = of_bsn_vport_q_in_q_OF_VERSION_1_1_dup(&src_bsn_vport);
    if (dst_bsn_vport == NULL) {
        of_bsn_virtual_port_create_request_delete(dst);
        return NULL;
    }
    of_bsn_virtual_port_create_request_vport_set(dst, dst_bsn_vport);
    of_bsn_vport_q_in_q_delete(dst_bsn_vport);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_reply_t *
of_bsn_virtual_port_remove_reply_OF_VERSION_1_1_dup(
    of_bsn_virtual_port_remove_reply_t *src)
{
    of_bsn_virtual_port_remove_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_reply_xid_get(src, &val32);
    of_bsn_virtual_port_remove_reply_xid_set(dst, val32);

    of_bsn_virtual_port_remove_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_reply_status_get(src, &val32);
    of_bsn_virtual_port_remove_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_request_t *
of_bsn_virtual_port_remove_request_OF_VERSION_1_1_dup(
    of_bsn_virtual_port_remove_request_t *src)
{
    of_bsn_virtual_port_remove_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_request_xid_get(src, &val32);
    of_bsn_virtual_port_remove_request_xid_set(dst, val32);

    of_bsn_virtual_port_remove_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_request_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_request_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_request_vport_no_get(src, &val32);
    of_bsn_virtual_port_remove_request_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_reply_t *
of_desc_stats_reply_OF_VERSION_1_1_dup(
    of_desc_stats_reply_t *src)
{
    of_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_serial_num_t ser_num;

    if ((dst = of_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_reply_xid_get(src, &val32);
    of_desc_stats_reply_xid_set(dst, val32);

    of_desc_stats_reply_flags_get(src, &val16);
    of_desc_stats_reply_flags_set(dst, val16);

    of_desc_stats_reply_mfr_desc_get(src, &desc_str);
    of_desc_stats_reply_mfr_desc_set(dst, desc_str);

    of_desc_stats_reply_hw_desc_get(src, &desc_str);
    of_desc_stats_reply_hw_desc_set(dst, desc_str);

    of_desc_stats_reply_sw_desc_get(src, &desc_str);
    of_desc_stats_reply_sw_desc_set(dst, desc_str);

    of_desc_stats_reply_serial_num_get(src, &ser_num);
    of_desc_stats_reply_serial_num_set(dst, ser_num);

    of_desc_stats_reply_dp_desc_get(src, &desc_str);
    of_desc_stats_reply_dp_desc_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_request_t *
of_desc_stats_request_OF_VERSION_1_1_dup(
    of_desc_stats_request_t *src)
{
    of_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_request_xid_get(src, &val32);
    of_desc_stats_request_xid_set(dst, val32);

    of_desc_stats_request_flags_get(src, &val16);
    of_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_echo_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_reply_t *
of_echo_reply_OF_VERSION_1_1_dup(
    of_echo_reply_t *src)
{
    of_echo_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_reply_xid_get(src, &val32);
    of_echo_reply_xid_set(dst, val32);

    of_echo_reply_data_get(src, &octets);
    of_echo_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_echo_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_request_t *
of_echo_request_OF_VERSION_1_1_dup(
    of_echo_request_t *src)
{
    of_echo_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_request_xid_get(src, &val32);
    of_echo_request_xid_set(dst, val32);

    of_echo_request_data_get(src, &octets);
    of_echo_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_features_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_reply_t *
of_features_reply_OF_VERSION_1_1_dup(
    of_features_reply_t *src)
{
    of_features_reply_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;

    of_list_port_desc_t src_list;
    of_list_port_desc_t *dst_list;

    if ((dst = of_features_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_reply_xid_get(src, &val32);
    of_features_reply_xid_set(dst, val32);

    of_features_reply_datapath_id_get(src, &val64);
    of_features_reply_datapath_id_set(dst, val64);

    of_features_reply_n_buffers_get(src, &val32);
    of_features_reply_n_buffers_set(dst, val32);

    of_features_reply_n_tables_get(src, &val8);
    of_features_reply_n_tables_set(dst, val8);

    of_features_reply_capabilities_get(src, &val32);
    of_features_reply_capabilities_set(dst, val32);

    of_features_reply_reserved_get(src, &val32);
    of_features_reply_reserved_set(dst, val32);

    of_features_reply_ports_bind(
        src, &src_list);
    dst_list = of_list_port_desc_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_features_reply_delete(dst);
        return NULL;
    }
    of_features_reply_ports_set(dst, dst_list);
    of_list_port_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_features_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_request_t *
of_features_request_OF_VERSION_1_1_dup(
    of_features_request_t *src)
{
    of_features_request_t *dst;
    uint32_t val32;

    if ((dst = of_features_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_request_xid_get(src, &val32);
    of_features_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_flow_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_add_t *
of_flow_add_OF_VERSION_1_1_dup(
    of_flow_add_t *src)
{
    of_flow_add_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_add_xid_get(src, &val32);
    of_flow_add_xid_set(dst, val32);

    of_flow_add_cookie_get(src, &val64);
    of_flow_add_cookie_set(dst, val64);

    of_flow_add_cookie_mask_get(src, &val64);
    of_flow_add_cookie_mask_set(dst, val64);

    of_flow_add_table_id_get(src, &val8);
    of_flow_add_table_id_set(dst, val8);

    of_flow_add_idle_timeout_get(src, &val16);
    of_flow_add_idle_timeout_set(dst, val16);

    of_flow_add_hard_timeout_get(src, &val16);
    of_flow_add_hard_timeout_set(dst, val16);

    of_flow_add_priority_get(src, &val16);
    of_flow_add_priority_set(dst, val16);

    of_flow_add_buffer_id_get(src, &val32);
    of_flow_add_buffer_id_set(dst, val32);

    of_flow_add_out_port_get(src, &port_no);
    of_flow_add_out_port_set(dst, port_no);

    of_flow_add_out_group_get(src, &val32);
    of_flow_add_out_group_set(dst, val32);

    of_flow_add_flags_get(src, &val16);
    of_flow_add_flags_set(dst, val16);

    of_flow_add_match_get(src, &match);
    of_flow_add_match_set(dst, &match);

    of_flow_add_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_add_delete(dst);
        return NULL;
    }
    of_flow_add_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_t *
of_flow_delete_OF_VERSION_1_1_dup(
    of_flow_delete_t *src)
{
    of_flow_delete_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_xid_get(src, &val32);
    of_flow_delete_xid_set(dst, val32);

    of_flow_delete_cookie_get(src, &val64);
    of_flow_delete_cookie_set(dst, val64);

    of_flow_delete_cookie_mask_get(src, &val64);
    of_flow_delete_cookie_mask_set(dst, val64);

    of_flow_delete_table_id_get(src, &val8);
    of_flow_delete_table_id_set(dst, val8);

    of_flow_delete_idle_timeout_get(src, &val16);
    of_flow_delete_idle_timeout_set(dst, val16);

    of_flow_delete_hard_timeout_get(src, &val16);
    of_flow_delete_hard_timeout_set(dst, val16);

    of_flow_delete_priority_get(src, &val16);
    of_flow_delete_priority_set(dst, val16);

    of_flow_delete_buffer_id_get(src, &val32);
    of_flow_delete_buffer_id_set(dst, val32);

    of_flow_delete_out_port_get(src, &port_no);
    of_flow_delete_out_port_set(dst, port_no);

    of_flow_delete_out_group_get(src, &val32);
    of_flow_delete_out_group_set(dst, val32);

    of_flow_delete_flags_get(src, &val16);
    of_flow_delete_flags_set(dst, val16);

    of_flow_delete_match_get(src, &match);
    of_flow_delete_match_set(dst, &match);

    of_flow_delete_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_delete(dst);
        return NULL;
    }
    of_flow_delete_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_strict_t *
of_flow_delete_strict_OF_VERSION_1_1_dup(
    of_flow_delete_strict_t *src)
{
    of_flow_delete_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_strict_xid_get(src, &val32);
    of_flow_delete_strict_xid_set(dst, val32);

    of_flow_delete_strict_cookie_get(src, &val64);
    of_flow_delete_strict_cookie_set(dst, val64);

    of_flow_delete_strict_cookie_mask_get(src, &val64);
    of_flow_delete_strict_cookie_mask_set(dst, val64);

    of_flow_delete_strict_table_id_get(src, &val8);
    of_flow_delete_strict_table_id_set(dst, val8);

    of_flow_delete_strict_idle_timeout_get(src, &val16);
    of_flow_delete_strict_idle_timeout_set(dst, val16);

    of_flow_delete_strict_hard_timeout_get(src, &val16);
    of_flow_delete_strict_hard_timeout_set(dst, val16);

    of_flow_delete_strict_priority_get(src, &val16);
    of_flow_delete_strict_priority_set(dst, val16);

    of_flow_delete_strict_buffer_id_get(src, &val32);
    of_flow_delete_strict_buffer_id_set(dst, val32);

    of_flow_delete_strict_out_port_get(src, &port_no);
    of_flow_delete_strict_out_port_set(dst, port_no);

    of_flow_delete_strict_out_group_get(src, &val32);
    of_flow_delete_strict_out_group_set(dst, val32);

    of_flow_delete_strict_flags_get(src, &val16);
    of_flow_delete_strict_flags_set(dst, val16);

    of_flow_delete_strict_match_get(src, &match);
    of_flow_delete_strict_match_set(dst, &match);

    of_flow_delete_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_strict_delete(dst);
        return NULL;
    }
    of_flow_delete_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_mod_failed_error_msg_t *
of_flow_mod_failed_error_msg_OF_VERSION_1_1_dup(
    of_flow_mod_failed_error_msg_t *src)
{
    of_flow_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_mod_failed_error_msg_xid_get(src, &val32);
    of_flow_mod_failed_error_msg_xid_set(dst, val32);

    of_flow_mod_failed_error_msg_code_get(src, &val16);
    of_flow_mod_failed_error_msg_code_set(dst, val16);

    of_flow_mod_failed_error_msg_data_get(src, &octets);
    of_flow_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_t *
of_flow_modify_OF_VERSION_1_1_dup(
    of_flow_modify_t *src)
{
    of_flow_modify_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_xid_get(src, &val32);
    of_flow_modify_xid_set(dst, val32);

    of_flow_modify_cookie_get(src, &val64);
    of_flow_modify_cookie_set(dst, val64);

    of_flow_modify_cookie_mask_get(src, &val64);
    of_flow_modify_cookie_mask_set(dst, val64);

    of_flow_modify_table_id_get(src, &val8);
    of_flow_modify_table_id_set(dst, val8);

    of_flow_modify_idle_timeout_get(src, &val16);
    of_flow_modify_idle_timeout_set(dst, val16);

    of_flow_modify_hard_timeout_get(src, &val16);
    of_flow_modify_hard_timeout_set(dst, val16);

    of_flow_modify_priority_get(src, &val16);
    of_flow_modify_priority_set(dst, val16);

    of_flow_modify_buffer_id_get(src, &val32);
    of_flow_modify_buffer_id_set(dst, val32);

    of_flow_modify_out_port_get(src, &port_no);
    of_flow_modify_out_port_set(dst, port_no);

    of_flow_modify_out_group_get(src, &val32);
    of_flow_modify_out_group_set(dst, val32);

    of_flow_modify_flags_get(src, &val16);
    of_flow_modify_flags_set(dst, val16);

    of_flow_modify_match_get(src, &match);
    of_flow_modify_match_set(dst, &match);

    of_flow_modify_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_delete(dst);
        return NULL;
    }
    of_flow_modify_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_strict_t *
of_flow_modify_strict_OF_VERSION_1_1_dup(
    of_flow_modify_strict_t *src)
{
    of_flow_modify_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_strict_xid_get(src, &val32);
    of_flow_modify_strict_xid_set(dst, val32);

    of_flow_modify_strict_cookie_get(src, &val64);
    of_flow_modify_strict_cookie_set(dst, val64);

    of_flow_modify_strict_cookie_mask_get(src, &val64);
    of_flow_modify_strict_cookie_mask_set(dst, val64);

    of_flow_modify_strict_table_id_get(src, &val8);
    of_flow_modify_strict_table_id_set(dst, val8);

    of_flow_modify_strict_idle_timeout_get(src, &val16);
    of_flow_modify_strict_idle_timeout_set(dst, val16);

    of_flow_modify_strict_hard_timeout_get(src, &val16);
    of_flow_modify_strict_hard_timeout_set(dst, val16);

    of_flow_modify_strict_priority_get(src, &val16);
    of_flow_modify_strict_priority_set(dst, val16);

    of_flow_modify_strict_buffer_id_get(src, &val32);
    of_flow_modify_strict_buffer_id_set(dst, val32);

    of_flow_modify_strict_out_port_get(src, &port_no);
    of_flow_modify_strict_out_port_set(dst, port_no);

    of_flow_modify_strict_out_group_get(src, &val32);
    of_flow_modify_strict_out_group_set(dst, val32);

    of_flow_modify_strict_flags_get(src, &val16);
    of_flow_modify_strict_flags_set(dst, val16);

    of_flow_modify_strict_match_get(src, &match);
    of_flow_modify_strict_match_set(dst, &match);

    of_flow_modify_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_strict_delete(dst);
        return NULL;
    }
    of_flow_modify_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_removed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_removed.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_removed_t *
of_flow_removed_OF_VERSION_1_1_dup(
    of_flow_removed_t *src)
{
    of_flow_removed_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_flow_removed_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_removed_xid_get(src, &val32);
    of_flow_removed_xid_set(dst, val32);

    of_flow_removed_cookie_get(src, &val64);
    of_flow_removed_cookie_set(dst, val64);

    of_flow_removed_priority_get(src, &val16);
    of_flow_removed_priority_set(dst, val16);

    of_flow_removed_reason_get(src, &val8);
    of_flow_removed_reason_set(dst, val8);

    of_flow_removed_table_id_get(src, &val8);
    of_flow_removed_table_id_set(dst, val8);

    of_flow_removed_duration_sec_get(src, &val32);
    of_flow_removed_duration_sec_set(dst, val32);

    of_flow_removed_duration_nsec_get(src, &val32);
    of_flow_removed_duration_nsec_set(dst, val32);

    of_flow_removed_idle_timeout_get(src, &val16);
    of_flow_removed_idle_timeout_set(dst, val16);

    of_flow_removed_packet_count_get(src, &val64);
    of_flow_removed_packet_count_set(dst, val64);

    of_flow_removed_byte_count_get(src, &val64);
    of_flow_removed_byte_count_set(dst, val64);

    of_flow_removed_match_get(src, &match);
    of_flow_removed_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_reply_t *
of_flow_stats_reply_OF_VERSION_1_1_dup(
    of_flow_stats_reply_t *src)
{
    of_flow_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_flow_stats_entry_t src_list;
    of_list_flow_stats_entry_t *dst_list;

    if ((dst = of_flow_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_reply_xid_get(src, &val32);
    of_flow_stats_reply_xid_set(dst, val32);

    of_flow_stats_reply_flags_get(src, &val16);
    of_flow_stats_reply_flags_set(dst, val16);

    of_flow_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_flow_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_reply_delete(dst);
        return NULL;
    }
    of_flow_stats_reply_entries_set(dst, dst_list);
    of_list_flow_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_request_t *
of_flow_stats_request_OF_VERSION_1_1_dup(
    of_flow_stats_request_t *src)
{
    of_flow_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_flow_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_request_xid_get(src, &val32);
    of_flow_stats_request_xid_set(dst, val32);

    of_flow_stats_request_flags_get(src, &val16);
    of_flow_stats_request_flags_set(dst, val16);

    of_flow_stats_request_table_id_get(src, &val8);
    of_flow_stats_request_table_id_set(dst, val8);

    of_flow_stats_request_out_port_get(src, &port_no);
    of_flow_stats_request_out_port_set(dst, port_no);

    of_flow_stats_request_out_group_get(src, &val32);
    of_flow_stats_request_out_group_set(dst, val32);

    of_flow_stats_request_cookie_get(src, &val64);
    of_flow_stats_request_cookie_set(dst, val64);

    of_flow_stats_request_cookie_mask_get(src, &val64);
    of_flow_stats_request_cookie_mask_set(dst, val64);

    of_flow_stats_request_match_get(src, &match);
    of_flow_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_reply_t *
of_get_config_reply_OF_VERSION_1_1_dup(
    of_get_config_reply_t *src)
{
    of_get_config_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_reply_xid_get(src, &val32);
    of_get_config_reply_xid_set(dst, val32);

    of_get_config_reply_flags_get(src, &val16);
    of_get_config_reply_flags_set(dst, val16);

    of_get_config_reply_miss_send_len_get(src, &val16);
    of_get_config_reply_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_request_t *
of_get_config_request_OF_VERSION_1_1_dup(
    of_get_config_request_t *src)
{
    of_get_config_request_t *dst;
    uint32_t val32;

    if ((dst = of_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_request_xid_get(src, &val32);
    of_get_config_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_add_t *
of_group_add_OF_VERSION_1_1_dup(
    of_group_add_t *src)
{
    of_group_add_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_add_xid_get(src, &val32);
    of_group_add_xid_set(dst, val32);

    of_group_add_group_type_get(src, &val8);
    of_group_add_group_type_set(dst, val8);

    of_group_add_group_id_get(src, &val32);
    of_group_add_group_id_set(dst, val32);

    of_group_add_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_add_delete(dst);
        return NULL;
    }
    of_group_add_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_delete_t *
of_group_delete_OF_VERSION_1_1_dup(
    of_group_delete_t *src)
{
    of_group_delete_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_delete_xid_get(src, &val32);
    of_group_delete_xid_set(dst, val32);

    of_group_delete_group_type_get(src, &val8);
    of_group_delete_group_type_set(dst, val8);

    of_group_delete_group_id_get(src, &val32);
    of_group_delete_group_id_set(dst, val32);

    of_group_delete_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_delete_delete(dst);
        return NULL;
    }
    of_group_delete_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_reply_t *
of_group_desc_stats_reply_OF_VERSION_1_1_dup(
    of_group_desc_stats_reply_t *src)
{
    of_group_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_desc_stats_entry_t src_list;
    of_list_group_desc_stats_entry_t *dst_list;

    if ((dst = of_group_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_reply_xid_get(src, &val32);
    of_group_desc_stats_reply_xid_set(dst, val32);

    of_group_desc_stats_reply_flags_get(src, &val16);
    of_group_desc_stats_reply_flags_set(dst, val16);

    of_group_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_desc_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_group_desc_stats_reply_entries_set(dst, dst_list);
    of_list_group_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_request_t *
of_group_desc_stats_request_OF_VERSION_1_1_dup(
    of_group_desc_stats_request_t *src)
{
    of_group_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_request_xid_get(src, &val32);
    of_group_desc_stats_request_xid_set(dst, val32);

    of_group_desc_stats_request_flags_get(src, &val16);
    of_group_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_mod_failed_error_msg_t *
of_group_mod_failed_error_msg_OF_VERSION_1_1_dup(
    of_group_mod_failed_error_msg_t *src)
{
    of_group_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_group_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_mod_failed_error_msg_xid_get(src, &val32);
    of_group_mod_failed_error_msg_xid_set(dst, val32);

    of_group_mod_failed_error_msg_code_get(src, &val16);
    of_group_mod_failed_error_msg_code_set(dst, val16);

    of_group_mod_failed_error_msg_data_get(src, &octets);
    of_group_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_group_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_modify_t *
of_group_modify_OF_VERSION_1_1_dup(
    of_group_modify_t *src)
{
    of_group_modify_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_modify_xid_get(src, &val32);
    of_group_modify_xid_set(dst, val32);

    of_group_modify_group_type_get(src, &val8);
    of_group_modify_group_type_set(dst, val8);

    of_group_modify_group_id_get(src, &val32);
    of_group_modify_group_id_set(dst, val32);

    of_group_modify_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_modify_delete(dst);
        return NULL;
    }
    of_group_modify_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_reply_t *
of_group_stats_reply_OF_VERSION_1_1_dup(
    of_group_stats_reply_t *src)
{
    of_group_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_stats_entry_t src_list;
    of_list_group_stats_entry_t *dst_list;

    if ((dst = of_group_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_reply_xid_get(src, &val32);
    of_group_stats_reply_xid_set(dst, val32);

    of_group_stats_reply_flags_get(src, &val16);
    of_group_stats_reply_flags_set(dst, val16);

    of_group_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_reply_delete(dst);
        return NULL;
    }
    of_group_stats_reply_entries_set(dst, dst_list);
    of_list_group_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_request_t *
of_group_stats_request_OF_VERSION_1_1_dup(
    of_group_stats_request_t *src)
{
    of_group_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_request_xid_get(src, &val32);
    of_group_stats_request_xid_set(dst, val32);

    of_group_stats_request_flags_get(src, &val16);
    of_group_stats_request_flags_set(dst, val16);

    of_group_stats_request_group_id_get(src, &val32);
    of_group_stats_request_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_header
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_header.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_header_OF_VERSION_1_1_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_hello
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_t *
of_hello_OF_VERSION_1_1_dup(
    of_hello_t *src)
{
    of_hello_t *dst;
    uint32_t val32;

    if ((dst = of_hello_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_xid_get(src, &val32);
    of_hello_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_hello_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_failed_error_msg_t *
of_hello_failed_error_msg_OF_VERSION_1_1_dup(
    of_hello_failed_error_msg_t *src)
{
    of_hello_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_hello_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_failed_error_msg_xid_get(src, &val32);
    of_hello_failed_error_msg_xid_set(dst, val32);

    of_hello_failed_error_msg_code_get(src, &val16);
    of_hello_failed_error_msg_code_set(dst, val16);

    of_hello_failed_error_msg_data_get(src, &octets);
    of_hello_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_in_t *
of_packet_in_OF_VERSION_1_1_dup(
    of_packet_in_t *src)
{
    of_packet_in_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint16_t val16;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_packet_in_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_in_xid_get(src, &val32);
    of_packet_in_xid_set(dst, val32);

    of_packet_in_buffer_id_get(src, &val32);
    of_packet_in_buffer_id_set(dst, val32);

    of_packet_in_in_port_get(src, &port_no);
    of_packet_in_in_port_set(dst, port_no);

    of_packet_in_in_phy_port_get(src, &port_no);
    of_packet_in_in_phy_port_set(dst, port_no);

    of_packet_in_total_len_get(src, &val16);
    of_packet_in_total_len_set(dst, val16);

    of_packet_in_reason_get(src, &val8);
    of_packet_in_reason_set(dst, val8);

    of_packet_in_table_id_get(src, &val8);
    of_packet_in_table_id_set(dst, val8);

    of_packet_in_data_get(src, &octets);
    of_packet_in_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_out_t *
of_packet_out_OF_VERSION_1_1_dup(
    of_packet_out_t *src)
{
    of_packet_out_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;
    of_octets_t octets;

    if ((dst = of_packet_out_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_out_xid_get(src, &val32);
    of_packet_out_xid_set(dst, val32);

    of_packet_out_buffer_id_get(src, &val32);
    of_packet_out_buffer_id_set(dst, val32);

    of_packet_out_in_port_get(src, &port_no);
    of_packet_out_in_port_set(dst, port_no);

    of_packet_out_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_out_delete(dst);
        return NULL;
    }
    of_packet_out_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    of_packet_out_data_get(src, &octets);
    of_packet_out_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_t *
of_port_mod_OF_VERSION_1_1_dup(
    of_port_mod_t *src)
{
    of_port_mod_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;

    if ((dst = of_port_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_xid_get(src, &val32);
    of_port_mod_xid_set(dst, val32);

    of_port_mod_port_no_get(src, &port_no);
    of_port_mod_port_no_set(dst, port_no);

    of_port_mod_hw_addr_get(src, &mac_addr);
    of_port_mod_hw_addr_set(dst, mac_addr);

    of_port_mod_config_get(src, &val32);
    of_port_mod_config_set(dst, val32);

    of_port_mod_mask_get(src, &val32);
    of_port_mod_mask_set(dst, val32);

    of_port_mod_advertise_get(src, &val32);
    of_port_mod_advertise_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_failed_error_msg_t *
of_port_mod_failed_error_msg_OF_VERSION_1_1_dup(
    of_port_mod_failed_error_msg_t *src)
{
    of_port_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_port_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_failed_error_msg_xid_get(src, &val32);
    of_port_mod_failed_error_msg_xid_set(dst, val32);

    of_port_mod_failed_error_msg_code_get(src, &val16);
    of_port_mod_failed_error_msg_code_set(dst, val16);

    of_port_mod_failed_error_msg_data_get(src, &octets);
    of_port_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_reply_t *
of_port_stats_reply_OF_VERSION_1_1_dup(
    of_port_stats_reply_t *src)
{
    of_port_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_stats_entry_t src_list;
    of_list_port_stats_entry_t *dst_list;

    if ((dst = of_port_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_reply_xid_get(src, &val32);
    of_port_stats_reply_xid_set(dst, val32);

    of_port_stats_reply_flags_get(src, &val16);
    of_port_stats_reply_flags_set(dst, val16);

    of_port_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_reply_delete(dst);
        return NULL;
    }
    of_port_stats_reply_entries_set(dst, dst_list);
    of_list_port_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_request_t *
of_port_stats_request_OF_VERSION_1_1_dup(
    of_port_stats_request_t *src)
{
    of_port_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_port_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_request_xid_get(src, &val32);
    of_port_stats_request_xid_set(dst, val32);

    of_port_stats_request_flags_get(src, &val16);
    of_port_stats_request_flags_set(dst, val16);

    of_port_stats_request_port_no_get(src, &port_no);
    of_port_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_port_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_status_t *
of_port_status_OF_VERSION_1_1_dup(
    of_port_status_t *src)
{
    of_port_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_port_desc_t src_port_desc;
    of_port_desc_t *dst_port_desc;

    if ((dst = of_port_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_status_xid_get(src, &val32);
    of_port_status_xid_set(dst, val32);

    of_port_status_reason_get(src, &val8);
    of_port_status_reason_set(dst, val8);

    of_port_status_desc_bind(
        src, &src_port_desc);
    dst_port_desc = of_port_desc_OF_VERSION_1_1_dup(&src_port_desc);
    if (dst_port_desc == NULL) {
        of_port_status_delete(dst);
        return NULL;
    }
    of_port_status_desc_set(dst, dst_port_desc);
    of_port_desc_delete(dst_port_desc);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_reply_t *
of_queue_get_config_reply_OF_VERSION_1_1_dup(
    of_queue_get_config_reply_t *src)
{
    of_queue_get_config_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_packet_queue_t src_list;
    of_list_packet_queue_t *dst_list;

    if ((dst = of_queue_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_reply_xid_get(src, &val32);
    of_queue_get_config_reply_xid_set(dst, val32);

    of_queue_get_config_reply_port_get(src, &port_no);
    of_queue_get_config_reply_port_set(dst, port_no);

    of_queue_get_config_reply_queues_bind(
        src, &src_list);
    dst_list = of_list_packet_queue_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_get_config_reply_delete(dst);
        return NULL;
    }
    of_queue_get_config_reply_queues_set(dst, dst_list);
    of_list_packet_queue_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_request_t *
of_queue_get_config_request_OF_VERSION_1_1_dup(
    of_queue_get_config_request_t *src)
{
    of_queue_get_config_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_queue_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_request_xid_get(src, &val32);
    of_queue_get_config_request_xid_set(dst, val32);

    of_queue_get_config_request_port_get(src, &port_no);
    of_queue_get_config_request_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_queue_op_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_op_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_op_failed_error_msg_t *
of_queue_op_failed_error_msg_OF_VERSION_1_1_dup(
    of_queue_op_failed_error_msg_t *src)
{
    of_queue_op_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_queue_op_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_op_failed_error_msg_xid_get(src, &val32);
    of_queue_op_failed_error_msg_xid_set(dst, val32);

    of_queue_op_failed_error_msg_code_get(src, &val16);
    of_queue_op_failed_error_msg_code_set(dst, val16);

    of_queue_op_failed_error_msg_data_get(src, &octets);
    of_queue_op_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_reply_t *
of_queue_stats_reply_OF_VERSION_1_1_dup(
    of_queue_stats_reply_t *src)
{
    of_queue_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_stats_entry_t src_list;
    of_list_queue_stats_entry_t *dst_list;

    if ((dst = of_queue_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_reply_xid_get(src, &val32);
    of_queue_stats_reply_xid_set(dst, val32);

    of_queue_stats_reply_flags_get(src, &val16);
    of_queue_stats_reply_flags_set(dst, val16);

    of_queue_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_stats_reply_entries_set(dst, dst_list);
    of_list_queue_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_request_t *
of_queue_stats_request_OF_VERSION_1_1_dup(
    of_queue_stats_request_t *src)
{
    of_queue_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_request_xid_get(src, &val32);
    of_queue_stats_request_xid_set(dst, val32);

    of_queue_stats_request_flags_get(src, &val16);
    of_queue_stats_request_flags_set(dst, val16);

    of_queue_stats_request_port_no_get(src, &port_no);
    of_queue_stats_request_port_no_set(dst, port_no);

    of_queue_stats_request_queue_id_get(src, &val32);
    of_queue_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_set_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_set_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_set_config_t *
of_set_config_OF_VERSION_1_1_dup(
    of_set_config_t *src)
{
    of_set_config_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_set_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_set_config_xid_get(src, &val32);
    of_set_config_xid_set(dst, val32);

    of_set_config_flags_get(src, &val16);
    of_set_config_flags_set(dst, val16);

    of_set_config_miss_send_len_get(src, &val16);
    of_set_config_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_switch_config_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_switch_config_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_switch_config_failed_error_msg_t *
of_switch_config_failed_error_msg_OF_VERSION_1_1_dup(
    of_switch_config_failed_error_msg_t *src)
{
    of_switch_config_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_switch_config_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_switch_config_failed_error_msg_xid_get(src, &val32);
    of_switch_config_failed_error_msg_xid_set(dst, val32);

    of_switch_config_failed_error_msg_code_get(src, &val16);
    of_switch_config_failed_error_msg_code_set(dst, val16);

    of_switch_config_failed_error_msg_data_get(src, &octets);
    of_switch_config_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_t *
of_table_mod_OF_VERSION_1_1_dup(
    of_table_mod_t *src)
{
    of_table_mod_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_table_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_xid_get(src, &val32);
    of_table_mod_xid_set(dst, val32);

    of_table_mod_table_id_get(src, &val8);
    of_table_mod_table_id_set(dst, val8);

    of_table_mod_config_get(src, &val32);
    of_table_mod_config_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_failed_error_msg_t *
of_table_mod_failed_error_msg_OF_VERSION_1_1_dup(
    of_table_mod_failed_error_msg_t *src)
{
    of_table_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_failed_error_msg_xid_get(src, &val32);
    of_table_mod_failed_error_msg_xid_set(dst, val32);

    of_table_mod_failed_error_msg_code_get(src, &val16);
    of_table_mod_failed_error_msg_code_set(dst, val16);

    of_table_mod_failed_error_msg_data_get(src, &octets);
    of_table_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_reply_t *
of_table_stats_reply_OF_VERSION_1_1_dup(
    of_table_stats_reply_t *src)
{
    of_table_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_stats_entry_t src_list;
    of_list_table_stats_entry_t *dst_list;

    if ((dst = of_table_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_reply_xid_get(src, &val32);
    of_table_stats_reply_xid_set(dst, val32);

    of_table_stats_reply_flags_get(src, &val16);
    of_table_stats_reply_flags_set(dst, val16);

    of_table_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_stats_entry_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_table_stats_reply_delete(dst);
        return NULL;
    }
    of_table_stats_reply_entries_set(dst, dst_list);
    of_list_table_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_request_t *
of_table_stats_request_OF_VERSION_1_1_dup(
    of_table_stats_request_t *src)
{
    of_table_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_request_xid_get(src, &val32);
    of_table_stats_request_xid_set(dst, val32);

    of_table_stats_request_flags_get(src, &val16);
    of_table_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate a super class object of type of_action
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_OF_VERSION_1_1_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_SET_MPLS_LABEL) {
        return of_action_set_mpls_label_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_NICIRA_DEC_TTL) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_DL_SRC) {
        return of_action_set_dl_src_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_VLAN_VID) {
        return of_action_set_vlan_vid_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_MPLS) {
        return of_action_pop_mpls_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_IN) {
        return of_action_copy_ttl_in_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_VLAN_PCP) {
        return of_action_set_vlan_pcp_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_OUTPUT) {
        return of_action_output_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_DL_DST) {
        return of_action_set_dl_dst_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_QUEUE) {
        return of_action_set_queue_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_NW_TTL) {
        return of_action_dec_nw_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_VLAN) {
        return of_action_pop_vlan_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_MPLS_TC) {
        return of_action_set_mpls_tc_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_VLAN) {
        return of_action_push_vlan_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_MPLS_TTL) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_SET_TUNNEL_DST) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TTL) {
        return of_action_set_nw_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TOS) {
        return of_action_set_nw_tos_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_MIRROR) {
        return of_action_bsn_mirror_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_GROUP) {
        return of_action_group_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_MPLS) {
        return of_action_push_mpls_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_TP_SRC) {
        return of_action_set_tp_src_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_DST) {
        return of_action_set_nw_dst_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_OUT) {
        return of_action_copy_ttl_out_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_CHECKSUM) {
        return of_action_bsn_checksum_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_TP_DST) {
        return of_action_set_tp_dst_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_ECN) {
        return of_action_set_nw_ecn_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_MPLS_TTL) {
        return of_action_set_mpls_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_SRC) {
        return of_action_set_nw_src_OF_VERSION_1_1_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_checksum_t *
of_action_bsn_checksum_OF_VERSION_1_1_dup(
    of_action_bsn_checksum_t *src)
{
    of_action_bsn_checksum_t *dst;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_action_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_checksum_experimenter_get(src, &val32);
    of_action_bsn_checksum_experimenter_set(dst, val32);

    of_action_bsn_checksum_subtype_get(src, &val32);
    of_action_bsn_checksum_subtype_set(dst, val32);

    of_action_bsn_checksum_checksum_get(src, &checksum_128);
    of_action_bsn_checksum_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_mirror_t *
of_action_bsn_mirror_OF_VERSION_1_1_dup(
    of_action_bsn_mirror_t *src)
{
    of_action_bsn_mirror_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_action_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_mirror_experimenter_get(src, &val32);
    of_action_bsn_mirror_experimenter_set(dst, val32);

    of_action_bsn_mirror_subtype_get(src, &val32);
    of_action_bsn_mirror_subtype_set(dst, val32);

    of_action_bsn_mirror_dest_port_get(src, &val32);
    of_action_bsn_mirror_dest_port_set(dst, val32);

    of_action_bsn_mirror_vlan_tag_get(src, &val32);
    of_action_bsn_mirror_vlan_tag_set(dst, val32);

    of_action_bsn_mirror_copy_stage_get(src, &val8);
    of_action_bsn_mirror_copy_stage_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_set_tunnel_dst_t *
of_action_bsn_set_tunnel_dst_OF_VERSION_1_1_dup(
    of_action_bsn_set_tunnel_dst_t *src)
{
    of_action_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_bsn_set_tunnel_dst_subtype_set(dst, val32);

    of_action_bsn_set_tunnel_dst_dst_get(src, &val32);
    of_action_bsn_set_tunnel_dst_dst_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_in_t *
of_action_copy_ttl_in_OF_VERSION_1_1_dup(
    of_action_copy_ttl_in_t *src)
{
    of_action_copy_ttl_in_t *dst;

    if ((dst = of_action_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_out_t *
of_action_copy_ttl_out_OF_VERSION_1_1_dup(
    of_action_copy_ttl_out_t *src)
{
    of_action_copy_ttl_out_t *dst;

    if ((dst = of_action_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_mpls_ttl_t *
of_action_dec_mpls_ttl_OF_VERSION_1_1_dup(
    of_action_dec_mpls_ttl_t *src)
{
    of_action_dec_mpls_ttl_t *dst;

    if ((dst = of_action_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_nw_ttl_t *
of_action_dec_nw_ttl_OF_VERSION_1_1_dup(
    of_action_dec_nw_ttl_t *src)
{
    of_action_dec_nw_ttl_t *dst;

    if ((dst = of_action_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_group_t *
of_action_group_OF_VERSION_1_1_dup(
    of_action_group_t *src)
{
    of_action_group_t *dst;
    uint32_t val32;

    if ((dst = of_action_group_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_group_group_id_get(src, &val32);
    of_action_group_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_nicira_dec_ttl_t *
of_action_nicira_dec_ttl_OF_VERSION_1_1_dup(
    of_action_nicira_dec_ttl_t *src)
{
    of_action_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_output_t *
of_action_output_OF_VERSION_1_1_dup(
    of_action_output_t *src)
{
    of_action_output_t *dst;
    of_port_no_t port_no;
    uint16_t val16;

    if ((dst = of_action_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_output_port_get(src, &port_no);
    of_action_output_port_set(dst, port_no);

    of_action_output_max_len_get(src, &val16);
    of_action_output_max_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_mpls_t *
of_action_pop_mpls_OF_VERSION_1_1_dup(
    of_action_pop_mpls_t *src)
{
    of_action_pop_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_pop_mpls_ethertype_get(src, &val16);
    of_action_pop_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_vlan_t *
of_action_pop_vlan_OF_VERSION_1_1_dup(
    of_action_pop_vlan_t *src)
{
    of_action_pop_vlan_t *dst;

    if ((dst = of_action_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_mpls_t *
of_action_push_mpls_OF_VERSION_1_1_dup(
    of_action_push_mpls_t *src)
{
    of_action_push_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_mpls_ethertype_get(src, &val16);
    of_action_push_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_vlan_t *
of_action_push_vlan_OF_VERSION_1_1_dup(
    of_action_push_vlan_t *src)
{
    of_action_push_vlan_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_vlan_ethertype_get(src, &val16);
    of_action_push_vlan_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_dl_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_dl_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_dl_dst_t *
of_action_set_dl_dst_OF_VERSION_1_1_dup(
    of_action_set_dl_dst_t *src)
{
    of_action_set_dl_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_action_set_dl_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_dl_dst_dl_addr_get(src, &mac_addr);
    of_action_set_dl_dst_dl_addr_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_dl_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_dl_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_dl_src_t *
of_action_set_dl_src_OF_VERSION_1_1_dup(
    of_action_set_dl_src_t *src)
{
    of_action_set_dl_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_action_set_dl_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_dl_src_dl_addr_get(src, &mac_addr);
    of_action_set_dl_src_dl_addr_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_label_t *
of_action_set_mpls_label_OF_VERSION_1_1_dup(
    of_action_set_mpls_label_t *src)
{
    of_action_set_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_label_mpls_label_get(src, &val32);
    of_action_set_mpls_label_mpls_label_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_tc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_tc.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_tc_t *
of_action_set_mpls_tc_OF_VERSION_1_1_dup(
    of_action_set_mpls_tc_t *src)
{
    of_action_set_mpls_tc_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_mpls_tc_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_tc_mpls_tc_get(src, &val8);
    of_action_set_mpls_tc_mpls_tc_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_ttl_t *
of_action_set_mpls_ttl_OF_VERSION_1_1_dup(
    of_action_set_mpls_ttl_t *src)
{
    of_action_set_mpls_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_ttl_mpls_ttl_get(src, &val8);
    of_action_set_mpls_ttl_mpls_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_dst_t *
of_action_set_nw_dst_OF_VERSION_1_1_dup(
    of_action_set_nw_dst_t *src)
{
    of_action_set_nw_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_nw_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_dst_nw_addr_get(src, &val32);
    of_action_set_nw_dst_nw_addr_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_ecn_t *
of_action_set_nw_ecn_OF_VERSION_1_1_dup(
    of_action_set_nw_ecn_t *src)
{
    of_action_set_nw_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_ecn_nw_ecn_get(src, &val8);
    of_action_set_nw_ecn_nw_ecn_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_src_t *
of_action_set_nw_src_OF_VERSION_1_1_dup(
    of_action_set_nw_src_t *src)
{
    of_action_set_nw_src_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_nw_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_src_nw_addr_get(src, &val32);
    of_action_set_nw_src_nw_addr_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_tos
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_tos.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_tos_t *
of_action_set_nw_tos_OF_VERSION_1_1_dup(
    of_action_set_nw_tos_t *src)
{
    of_action_set_nw_tos_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_tos_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_tos_nw_tos_get(src, &val8);
    of_action_set_nw_tos_nw_tos_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_ttl_t *
of_action_set_nw_ttl_OF_VERSION_1_1_dup(
    of_action_set_nw_ttl_t *src)
{
    of_action_set_nw_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_ttl_nw_ttl_get(src, &val8);
    of_action_set_nw_ttl_nw_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_queue_t *
of_action_set_queue_OF_VERSION_1_1_dup(
    of_action_set_queue_t *src)
{
    of_action_set_queue_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_queue_queue_id_get(src, &val32);
    of_action_set_queue_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_tp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_tp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_tp_dst_t *
of_action_set_tp_dst_OF_VERSION_1_1_dup(
    of_action_set_tp_dst_t *src)
{
    of_action_set_tp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_tp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_tp_dst_tp_port_get(src, &val16);
    of_action_set_tp_dst_tp_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_tp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_tp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_tp_src_t *
of_action_set_tp_src_OF_VERSION_1_1_dup(
    of_action_set_tp_src_t *src)
{
    of_action_set_tp_src_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_tp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_tp_src_tp_port_get(src, &val16);
    of_action_set_tp_src_tp_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_vlan_pcp_t *
of_action_set_vlan_pcp_OF_VERSION_1_1_dup(
    of_action_set_vlan_pcp_t *src)
{
    of_action_set_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_vlan_pcp_vlan_pcp_get(src, &val8);
    of_action_set_vlan_pcp_vlan_pcp_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_vlan_vid_t *
of_action_set_vlan_vid_OF_VERSION_1_1_dup(
    of_action_set_vlan_vid_t *src)
{
    of_action_set_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_action_set_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_vlan_vid_vlan_vid_get(src, &val16);
    of_action_set_vlan_vid_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_interface_t *
of_bsn_interface_OF_VERSION_1_1_dup(
    of_bsn_interface_t *src)
{
    of_bsn_interface_t *dst;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_interface_hw_addr_get(src, &mac_addr);
    of_bsn_interface_hw_addr_set(dst, mac_addr);

    of_bsn_interface_name_get(src, &port_name);
    of_bsn_interface_name_set(dst, port_name);

    of_bsn_interface_ipv4_addr_get(src, &ipv4);
    of_bsn_interface_ipv4_addr_set(dst, ipv4);

    of_bsn_interface_ipv4_netmask_get(src, &ipv4);
    of_bsn_interface_ipv4_netmask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_vport
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_vport_OF_VERSION_1_1_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_VPORT_Q_IN_Q) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_BSN_VPORT_L2GRE) {
        return of_bsn_vport_l2gre_OF_VERSION_1_1_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_vport_l2gre
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_l2gre.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_l2gre_t *
of_bsn_vport_l2gre_OF_VERSION_1_1_dup(
    of_bsn_vport_l2gre_t *src)
{
    of_bsn_vport_l2gre_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_ipv4_t ipv4;
    uint8_t val8;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_l2gre_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_l2gre_flags_get(src, &val32);
    of_bsn_vport_l2gre_flags_set(dst, val32);

    of_bsn_vport_l2gre_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_loopback_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_loopback_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_local_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_local_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_nh_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_nh_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_src_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_src_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dst_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_dst_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dscp_get(src, &val8);
    of_bsn_vport_l2gre_dscp_set(dst, val8);

    of_bsn_vport_l2gre_ttl_get(src, &val8);
    of_bsn_vport_l2gre_ttl_set(dst, val8);

    of_bsn_vport_l2gre_vpn_get(src, &val32);
    of_bsn_vport_l2gre_vpn_set(dst, val32);

    of_bsn_vport_l2gre_rate_limit_get(src, &val32);
    of_bsn_vport_l2gre_rate_limit_set(dst, val32);

    of_bsn_vport_l2gre_if_name_get(src, &port_name);
    of_bsn_vport_l2gre_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vport_q_in_q
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_q_in_q.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_q_in_q_t *
of_bsn_vport_q_in_q_OF_VERSION_1_1_dup(
    of_bsn_vport_q_in_q_t *src)
{
    of_bsn_vport_q_in_q_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_q_in_q_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_q_in_q_port_no_get(src, &val32);
    of_bsn_vport_q_in_q_port_no_set(dst, val32);

    of_bsn_vport_q_in_q_ingress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_ingress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_egress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_egress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_egress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_egress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_if_name_get(src, &port_name);
    of_bsn_vport_q_in_q_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_t *
of_bucket_OF_VERSION_1_1_dup(
    of_bucket_t *src)
{
    of_bucket_t *dst;
    uint16_t val16;
    of_port_no_t port_no;
    uint32_t val32;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_weight_get(src, &val16);
    of_bucket_weight_set(dst, val16);

    of_bucket_watch_port_get(src, &port_no);
    of_bucket_watch_port_set(dst, port_no);

    of_bucket_watch_group_get(src, &val32);
    of_bucket_watch_group_set(dst, val32);

    of_bucket_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_bucket_delete(dst);
        return NULL;
    }
    of_bucket_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_counter_t *
of_bucket_counter_OF_VERSION_1_1_dup(
    of_bucket_counter_t *src)
{
    of_bucket_counter_t *dst;
    uint64_t val64;

    if ((dst = of_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_counter_packet_count_get(src, &val64);
    of_bucket_counter_packet_count_set(dst, val64);

    of_bucket_counter_byte_count_get(src, &val64);
    of_bucket_counter_byte_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_entry_t *
of_flow_stats_entry_OF_VERSION_1_1_dup(
    of_flow_stats_entry_t *src)
{
    of_flow_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_entry_table_id_get(src, &val8);
    of_flow_stats_entry_table_id_set(dst, val8);

    of_flow_stats_entry_duration_sec_get(src, &val32);
    of_flow_stats_entry_duration_sec_set(dst, val32);

    of_flow_stats_entry_duration_nsec_get(src, &val32);
    of_flow_stats_entry_duration_nsec_set(dst, val32);

    of_flow_stats_entry_priority_get(src, &val16);
    of_flow_stats_entry_priority_set(dst, val16);

    of_flow_stats_entry_idle_timeout_get(src, &val16);
    of_flow_stats_entry_idle_timeout_set(dst, val16);

    of_flow_stats_entry_hard_timeout_get(src, &val16);
    of_flow_stats_entry_hard_timeout_set(dst, val16);

    of_flow_stats_entry_cookie_get(src, &val64);
    of_flow_stats_entry_cookie_set(dst, val64);

    of_flow_stats_entry_packet_count_get(src, &val64);
    of_flow_stats_entry_packet_count_set(dst, val64);

    of_flow_stats_entry_byte_count_get(src, &val64);
    of_flow_stats_entry_byte_count_set(dst, val64);

    of_flow_stats_entry_match_get(src, &match);
    of_flow_stats_entry_match_set(dst, &match);

    of_flow_stats_entry_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_entry_delete(dst);
        return NULL;
    }
    of_flow_stats_entry_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_entry_t *
of_group_desc_stats_entry_OF_VERSION_1_1_dup(
    of_group_desc_stats_entry_t *src)
{
    of_group_desc_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_entry_group_type_get(src, &val8);
    of_group_desc_stats_entry_group_type_set(dst, val8);

    of_group_desc_stats_entry_group_id_get(src, &val32);
    of_group_desc_stats_entry_group_id_set(dst, val32);

    of_group_desc_stats_entry_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_group_desc_stats_entry_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_entry_t *
of_group_stats_entry_OF_VERSION_1_1_dup(
    of_group_stats_entry_t *src)
{
    of_group_stats_entry_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_bucket_counter_t src_list;
    of_list_bucket_counter_t *dst_list;

    if ((dst = of_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_entry_group_id_get(src, &val32);
    of_group_stats_entry_group_id_set(dst, val32);

    of_group_stats_entry_ref_count_get(src, &val32);
    of_group_stats_entry_ref_count_set(dst, val32);

    of_group_stats_entry_packet_count_get(src, &val64);
    of_group_stats_entry_packet_count_set(dst, val64);

    of_group_stats_entry_byte_count_get(src, &val64);
    of_group_stats_entry_byte_count_set(dst, val64);

    of_group_stats_entry_bucket_stats_bind(
        src, &src_list);
    dst_list = of_list_bucket_counter_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_entry_delete(dst);
        return NULL;
    }
    of_group_stats_entry_bucket_stats_set(dst, dst_list);
    of_list_bucket_counter_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_OF_VERSION_1_1_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_APPLY_ACTIONS) {
        return of_instruction_apply_actions_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_CLEAR_ACTIONS) {
        return of_instruction_clear_actions_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_GOTO_TABLE) {
        return of_instruction_goto_table_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_METADATA) {
        return of_instruction_write_metadata_OF_VERSION_1_1_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_ACTIONS) {
        return of_instruction_write_actions_OF_VERSION_1_1_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_apply_actions_t *
of_instruction_apply_actions_OF_VERSION_1_1_dup(
    of_instruction_apply_actions_t *src)
{
    of_instruction_apply_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_apply_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_apply_actions_delete(dst);
        return NULL;
    }
    of_instruction_apply_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_clear_actions_t *
of_instruction_clear_actions_OF_VERSION_1_1_dup(
    of_instruction_clear_actions_t *src)
{
    of_instruction_clear_actions_t *dst;

    if ((dst = of_instruction_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_goto_table_t *
of_instruction_goto_table_OF_VERSION_1_1_dup(
    of_instruction_goto_table_t *src)
{
    of_instruction_goto_table_t *dst;
    uint8_t val8;

    if ((dst = of_instruction_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_goto_table_table_id_get(src, &val8);
    of_instruction_goto_table_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_actions_t *
of_instruction_write_actions_OF_VERSION_1_1_dup(
    of_instruction_write_actions_t *src)
{
    of_instruction_write_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_write_actions_delete(dst);
        return NULL;
    }
    of_instruction_write_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_metadata_t *
of_instruction_write_metadata_OF_VERSION_1_1_dup(
    of_instruction_write_metadata_t *src)
{
    of_instruction_write_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_instruction_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_metadata_metadata_get(src, &val64);
    of_instruction_write_metadata_metadata_set(dst, val64);

    of_instruction_write_metadata_metadata_mask_get(src, &val64);
    of_instruction_write_metadata_metadata_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_match_v2
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_match_v2.
 *
 * The caller is responsible for deleting the returned value
 */
of_match_v2_t *
of_match_v2_OF_VERSION_1_1_dup(
    of_match_v2_t *src)
{
    of_match_v2_t *dst;
    of_port_no_t port_no;
    of_wc_bmap_t wc_bmap;
    of_mac_addr_t mac_addr;
    uint16_t val16;
    uint8_t val8;
    of_ipv4_t ipv4;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_match_v2_new(src->version)) == NULL) {
        return NULL;
    }

    of_match_v2_in_port_get(src, &port_no);
    of_match_v2_in_port_set(dst, port_no);

    of_match_v2_wildcards_get(src, &wc_bmap);
    of_match_v2_wildcards_set(dst, wc_bmap);

    of_match_v2_eth_src_get(src, &mac_addr);
    of_match_v2_eth_src_set(dst, mac_addr);

    of_match_v2_eth_src_mask_get(src, &mac_addr);
    of_match_v2_eth_src_mask_set(dst, mac_addr);

    of_match_v2_eth_dst_get(src, &mac_addr);
    of_match_v2_eth_dst_set(dst, mac_addr);

    of_match_v2_eth_dst_mask_get(src, &mac_addr);
    of_match_v2_eth_dst_mask_set(dst, mac_addr);

    of_match_v2_vlan_vid_get(src, &val16);
    of_match_v2_vlan_vid_set(dst, val16);

    of_match_v2_vlan_pcp_get(src, &val8);
    of_match_v2_vlan_pcp_set(dst, val8);

    of_match_v2_eth_type_get(src, &val16);
    of_match_v2_eth_type_set(dst, val16);

    of_match_v2_ip_dscp_get(src, &val8);
    of_match_v2_ip_dscp_set(dst, val8);

    of_match_v2_ip_proto_get(src, &val8);
    of_match_v2_ip_proto_set(dst, val8);

    of_match_v2_ipv4_src_get(src, &ipv4);
    of_match_v2_ipv4_src_set(dst, ipv4);

    of_match_v2_ipv4_src_mask_get(src, &ipv4);
    of_match_v2_ipv4_src_mask_set(dst, ipv4);

    of_match_v2_ipv4_dst_get(src, &ipv4);
    of_match_v2_ipv4_dst_set(dst, ipv4);

    of_match_v2_ipv4_dst_mask_get(src, &ipv4);
    of_match_v2_ipv4_dst_mask_set(dst, ipv4);

    of_match_v2_tcp_src_get(src, &val16);
    of_match_v2_tcp_src_set(dst, val16);

    of_match_v2_tcp_dst_get(src, &val16);
    of_match_v2_tcp_dst_set(dst, val16);

    of_match_v2_mpls_label_get(src, &val32);
    of_match_v2_mpls_label_set(dst, val32);

    of_match_v2_mpls_tc_get(src, &val8);
    of_match_v2_mpls_tc_set(dst, val8);

    of_match_v2_metadata_get(src, &val64);
    of_match_v2_metadata_set(dst, val64);

    of_match_v2_metadata_mask_get(src, &val64);
    of_match_v2_metadata_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_queue_t *
of_packet_queue_OF_VERSION_1_1_dup(
    of_packet_queue_t *src)
{
    of_packet_queue_t *dst;
    uint32_t val32;

    of_list_queue_prop_t src_list;
    of_list_queue_prop_t *dst_list;

    if ((dst = of_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_queue_queue_id_get(src, &val32);
    of_packet_queue_queue_id_set(dst, val32);

    of_packet_queue_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_prop_OF_VERSION_1_1_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_queue_delete(dst);
        return NULL;
    }
    of_packet_queue_properties_set(dst, dst_list);
    of_list_queue_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_t *
of_port_desc_OF_VERSION_1_1_dup(
    of_port_desc_t *src)
{
    of_port_desc_t *dst;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    uint32_t val32;

    if ((dst = of_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_port_no_get(src, &port_no);
    of_port_desc_port_no_set(dst, port_no);

    of_port_desc_hw_addr_get(src, &mac_addr);
    of_port_desc_hw_addr_set(dst, mac_addr);

    of_port_desc_name_get(src, &port_name);
    of_port_desc_name_set(dst, port_name);

    of_port_desc_config_get(src, &val32);
    of_port_desc_config_set(dst, val32);

    of_port_desc_state_get(src, &val32);
    of_port_desc_state_set(dst, val32);

    of_port_desc_curr_get(src, &val32);
    of_port_desc_curr_set(dst, val32);

    of_port_desc_advertised_get(src, &val32);
    of_port_desc_advertised_set(dst, val32);

    of_port_desc_supported_get(src, &val32);
    of_port_desc_supported_set(dst, val32);

    of_port_desc_peer_get(src, &val32);
    of_port_desc_peer_set(dst, val32);

    of_port_desc_curr_speed_get(src, &val32);
    of_port_desc_curr_speed_set(dst, val32);

    of_port_desc_max_speed_get(src, &val32);
    of_port_desc_max_speed_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_entry_t *
of_port_stats_entry_OF_VERSION_1_1_dup(
    of_port_stats_entry_t *src)
{
    of_port_stats_entry_t *dst;
    of_port_no_t port_no;
    uint64_t val64;

    if ((dst = of_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_entry_port_no_get(src, &port_no);
    of_port_stats_entry_port_no_set(dst, port_no);

    of_port_stats_entry_rx_packets_get(src, &val64);
    of_port_stats_entry_rx_packets_set(dst, val64);

    of_port_stats_entry_tx_packets_get(src, &val64);
    of_port_stats_entry_tx_packets_set(dst, val64);

    of_port_stats_entry_rx_bytes_get(src, &val64);
    of_port_stats_entry_rx_bytes_set(dst, val64);

    of_port_stats_entry_tx_bytes_get(src, &val64);
    of_port_stats_entry_tx_bytes_set(dst, val64);

    of_port_stats_entry_rx_dropped_get(src, &val64);
    of_port_stats_entry_rx_dropped_set(dst, val64);

    of_port_stats_entry_tx_dropped_get(src, &val64);
    of_port_stats_entry_tx_dropped_set(dst, val64);

    of_port_stats_entry_rx_errors_get(src, &val64);
    of_port_stats_entry_rx_errors_set(dst, val64);

    of_port_stats_entry_tx_errors_get(src, &val64);
    of_port_stats_entry_tx_errors_set(dst, val64);

    of_port_stats_entry_rx_frame_err_get(src, &val64);
    of_port_stats_entry_rx_frame_err_set(dst, val64);

    of_port_stats_entry_rx_over_err_get(src, &val64);
    of_port_stats_entry_rx_over_err_set(dst, val64);

    of_port_stats_entry_rx_crc_err_get(src, &val64);
    of_port_stats_entry_rx_crc_err_set(dst, val64);

    of_port_stats_entry_collisions_get(src, &val64);
    of_port_stats_entry_collisions_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_prop_OF_VERSION_1_1_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_PROP_MIN_RATE) {
        return of_queue_prop_min_rate_OF_VERSION_1_1_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_min_rate_t *
of_queue_prop_min_rate_OF_VERSION_1_1_dup(
    of_queue_prop_min_rate_t *src)
{
    of_queue_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_min_rate_rate_get(src, &val16);
    of_queue_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_entry_t *
of_queue_stats_entry_OF_VERSION_1_1_dup(
    of_queue_stats_entry_t *src)
{
    of_queue_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_entry_port_no_get(src, &port_no);
    of_queue_stats_entry_port_no_set(dst, port_no);

    of_queue_stats_entry_queue_id_get(src, &val32);
    of_queue_stats_entry_queue_id_set(dst, val32);

    of_queue_stats_entry_tx_bytes_get(src, &val64);
    of_queue_stats_entry_tx_bytes_set(dst, val64);

    of_queue_stats_entry_tx_packets_get(src, &val64);
    of_queue_stats_entry_tx_packets_set(dst, val64);

    of_queue_stats_entry_tx_errors_get(src, &val64);
    of_queue_stats_entry_tx_errors_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_entry_t *
of_table_stats_entry_OF_VERSION_1_1_dup(
    of_table_stats_entry_t *src)
{
    of_table_stats_entry_t *dst;
    uint8_t val8;
    of_table_name_t table_name;
    of_wc_bmap_t wc_bmap;
    of_match_bmap_t match_bmap;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_entry_table_id_get(src, &val8);
    of_table_stats_entry_table_id_set(dst, val8);

    of_table_stats_entry_name_get(src, &table_name);
    of_table_stats_entry_name_set(dst, table_name);

    of_table_stats_entry_wildcards_get(src, &wc_bmap);
    of_table_stats_entry_wildcards_set(dst, wc_bmap);

    of_table_stats_entry_match_get(src, &match_bmap);
    of_table_stats_entry_match_set(dst, match_bmap);

    of_table_stats_entry_instructions_get(src, &val32);
    of_table_stats_entry_instructions_set(dst, val32);

    of_table_stats_entry_write_actions_get(src, &val32);
    of_table_stats_entry_write_actions_set(dst, val32);

    of_table_stats_entry_apply_actions_get(src, &val32);
    of_table_stats_entry_apply_actions_set(dst, val32);

    of_table_stats_entry_config_get(src, &val32);
    of_table_stats_entry_config_set(dst, val32);

    of_table_stats_entry_max_entries_get(src, &val32);
    of_table_stats_entry_max_entries_set(dst, val32);

    of_table_stats_entry_active_count_get(src, &val32);
    of_table_stats_entry_active_count_set(dst, val32);

    of_table_stats_entry_lookup_count_get(src, &val64);
    of_table_stats_entry_lookup_count_set(dst, val64);

    of_table_stats_entry_matched_count_get(src, &val64);
    of_table_stats_entry_matched_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate a list of type of_list_action
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_t *
of_list_action_OF_VERSION_1_1_dup(
    of_list_action_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_t *dst;

    if ((dst = of_list_action_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_interface_t *
of_list_bsn_interface_OF_VERSION_1_1_dup(
    of_list_bsn_interface_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_interface_t *dst;

    if ((dst = of_list_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_INTERFACE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_interface_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_interface_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_t *
of_list_bucket_OF_VERSION_1_1_dup(
    of_list_bucket_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_t *dst;

    if ((dst = of_list_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_counter_t *
of_list_bucket_counter_OF_VERSION_1_1_dup(
    of_list_bucket_counter_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_counter_t *dst;

    if ((dst = of_list_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_COUNTER_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_counter_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_counter_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_flow_stats_entry_t *
of_list_flow_stats_entry_OF_VERSION_1_1_dup(
    of_list_flow_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_flow_stats_entry_t *dst;

    if ((dst = of_list_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_FLOW_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_flow_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_flow_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_desc_stats_entry_t *
of_list_group_desc_stats_entry_OF_VERSION_1_1_dup(
    of_list_group_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_desc_stats_entry_t *dst;

    if ((dst = of_list_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_desc_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_stats_entry_t *
of_list_group_stats_entry_OF_VERSION_1_1_dup(
    of_list_group_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_stats_entry_t *dst;

    if ((dst = of_list_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_t *
of_list_instruction_OF_VERSION_1_1_dup(
    of_list_instruction_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_t *dst;

    if ((dst = of_list_instruction_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_packet_queue_t *
of_list_packet_queue_OF_VERSION_1_1_dup(
    of_list_packet_queue_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_packet_queue_t *dst;

    if ((dst = of_list_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PACKET_QUEUE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_packet_queue_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_packet_queue_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_t *
of_list_port_desc_OF_VERSION_1_1_dup(
    of_list_port_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_t *dst;

    if ((dst = of_list_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_entry_t *
of_list_port_stats_entry_OF_VERSION_1_1_dup(
    of_list_port_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_entry_t *dst;

    if ((dst = of_list_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_prop_t *
of_list_queue_prop_OF_VERSION_1_1_dup(
    of_list_queue_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_prop_t *dst;

    if ((dst = of_list_queue_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_prop_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_entry_t *
of_list_queue_stats_entry_OF_VERSION_1_1_dup(
    of_list_queue_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_entry_t *dst;

    if ((dst = of_list_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_stats_entry_t *
of_list_table_stats_entry_OF_VERSION_1_1_dup(
    of_list_table_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_stats_entry_t *dst;

    if ((dst = of_list_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_stats_entry_OF_VERSION_1_1_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_reply_t *
of_aggregate_stats_reply_OF_VERSION_1_2_dup(
    of_aggregate_stats_reply_t *src)
{
    of_aggregate_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_aggregate_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_reply_xid_get(src, &val32);
    of_aggregate_stats_reply_xid_set(dst, val32);

    of_aggregate_stats_reply_flags_get(src, &val16);
    of_aggregate_stats_reply_flags_set(dst, val16);

    of_aggregate_stats_reply_packet_count_get(src, &val64);
    of_aggregate_stats_reply_packet_count_set(dst, val64);

    of_aggregate_stats_reply_byte_count_get(src, &val64);
    of_aggregate_stats_reply_byte_count_set(dst, val64);

    of_aggregate_stats_reply_flow_count_get(src, &val32);
    of_aggregate_stats_reply_flow_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_request_t *
of_aggregate_stats_request_OF_VERSION_1_2_dup(
    of_aggregate_stats_request_t *src)
{
    of_aggregate_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_aggregate_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_request_xid_get(src, &val32);
    of_aggregate_stats_request_xid_set(dst, val32);

    of_aggregate_stats_request_flags_get(src, &val16);
    of_aggregate_stats_request_flags_set(dst, val16);

    of_aggregate_stats_request_table_id_get(src, &val8);
    of_aggregate_stats_request_table_id_set(dst, val8);

    of_aggregate_stats_request_out_port_get(src, &port_no);
    of_aggregate_stats_request_out_port_set(dst, port_no);

    of_aggregate_stats_request_out_group_get(src, &val32);
    of_aggregate_stats_request_out_group_set(dst, val32);

    of_aggregate_stats_request_cookie_get(src, &val64);
    of_aggregate_stats_request_cookie_set(dst, val64);

    of_aggregate_stats_request_cookie_mask_get(src, &val64);
    of_aggregate_stats_request_cookie_mask_set(dst, val64);

    of_aggregate_stats_request_match_get(src, &match);
    of_aggregate_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_bad_action_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_action_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_action_error_msg_t *
of_bad_action_error_msg_OF_VERSION_1_2_dup(
    of_bad_action_error_msg_t *src)
{
    of_bad_action_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_action_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_action_error_msg_xid_get(src, &val32);
    of_bad_action_error_msg_xid_set(dst, val32);

    of_bad_action_error_msg_code_get(src, &val16);
    of_bad_action_error_msg_code_set(dst, val16);

    of_bad_action_error_msg_data_get(src, &octets);
    of_bad_action_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_instruction_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_instruction_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_instruction_error_msg_t *
of_bad_instruction_error_msg_OF_VERSION_1_2_dup(
    of_bad_instruction_error_msg_t *src)
{
    of_bad_instruction_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_instruction_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_instruction_error_msg_xid_get(src, &val32);
    of_bad_instruction_error_msg_xid_set(dst, val32);

    of_bad_instruction_error_msg_code_get(src, &val16);
    of_bad_instruction_error_msg_code_set(dst, val16);

    of_bad_instruction_error_msg_data_get(src, &octets);
    of_bad_instruction_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_match_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_match_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_match_error_msg_t *
of_bad_match_error_msg_OF_VERSION_1_2_dup(
    of_bad_match_error_msg_t *src)
{
    of_bad_match_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_match_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_match_error_msg_xid_get(src, &val32);
    of_bad_match_error_msg_xid_set(dst, val32);

    of_bad_match_error_msg_code_get(src, &val16);
    of_bad_match_error_msg_code_set(dst, val16);

    of_bad_match_error_msg_data_get(src, &octets);
    of_bad_match_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_request_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_request_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_request_error_msg_t *
of_bad_request_error_msg_OF_VERSION_1_2_dup(
    of_bad_request_error_msg_t *src)
{
    of_bad_request_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_request_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_request_error_msg_xid_get(src, &val32);
    of_bad_request_error_msg_xid_set(dst, val32);

    of_bad_request_error_msg_code_get(src, &val16);
    of_bad_request_error_msg_code_set(dst, val16);

    of_bad_request_error_msg_data_get(src, &octets);
    of_bad_request_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_reply_t *
of_barrier_reply_OF_VERSION_1_2_dup(
    of_barrier_reply_t *src)
{
    of_barrier_reply_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_reply_xid_get(src, &val32);
    of_barrier_reply_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_request_t *
of_barrier_request_OF_VERSION_1_2_dup(
    of_barrier_request_t *src)
{
    of_barrier_request_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_request_xid_get(src, &val32);
    of_barrier_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_reply_t *
of_bsn_bw_clear_data_reply_OF_VERSION_1_2_dup(
    of_bsn_bw_clear_data_reply_t *src)
{
    of_bsn_bw_clear_data_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_reply_xid_get(src, &val32);
    of_bsn_bw_clear_data_reply_xid_set(dst, val32);

    of_bsn_bw_clear_data_reply_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_reply_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_reply_subtype_get(src, &val32);
    of_bsn_bw_clear_data_reply_subtype_set(dst, val32);

    of_bsn_bw_clear_data_reply_status_get(src, &val32);
    of_bsn_bw_clear_data_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_request_t *
of_bsn_bw_clear_data_request_OF_VERSION_1_2_dup(
    of_bsn_bw_clear_data_request_t *src)
{
    of_bsn_bw_clear_data_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_request_xid_get(src, &val32);
    of_bsn_bw_clear_data_request_xid_set(dst, val32);

    of_bsn_bw_clear_data_request_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_request_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_request_subtype_get(src, &val32);
    of_bsn_bw_clear_data_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_reply_t *
of_bsn_bw_enable_get_reply_OF_VERSION_1_2_dup(
    of_bsn_bw_enable_get_reply_t *src)
{
    of_bsn_bw_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_reply_xid_get(src, &val32);
    of_bsn_bw_enable_get_reply_xid_set(dst, val32);

    of_bsn_bw_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_get_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_get_reply_enabled_get(src, &val32);
    of_bsn_bw_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_request_t *
of_bsn_bw_enable_get_request_OF_VERSION_1_2_dup(
    of_bsn_bw_enable_get_request_t *src)
{
    of_bsn_bw_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_request_xid_get(src, &val32);
    of_bsn_bw_enable_get_request_xid_set(dst, val32);

    of_bsn_bw_enable_get_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_request_subtype_get(src, &val32);
    of_bsn_bw_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_reply_t *
of_bsn_bw_enable_set_reply_OF_VERSION_1_2_dup(
    of_bsn_bw_enable_set_reply_t *src)
{
    of_bsn_bw_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_reply_xid_get(src, &val32);
    of_bsn_bw_enable_set_reply_xid_set(dst, val32);

    of_bsn_bw_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_set_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_set_reply_enable_get(src, &val32);
    of_bsn_bw_enable_set_reply_enable_set(dst, val32);

    of_bsn_bw_enable_set_reply_status_get(src, &val32);
    of_bsn_bw_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_request_t *
of_bsn_bw_enable_set_request_OF_VERSION_1_2_dup(
    of_bsn_bw_enable_set_request_t *src)
{
    of_bsn_bw_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_request_xid_get(src, &val32);
    of_bsn_bw_enable_set_request_xid_set(dst, val32);

    of_bsn_bw_enable_set_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_request_subtype_get(src, &val32);
    of_bsn_bw_enable_set_request_subtype_set(dst, val32);

    of_bsn_bw_enable_set_request_enable_get(src, &val32);
    of_bsn_bw_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_reply_t *
of_bsn_get_interfaces_reply_OF_VERSION_1_2_dup(
    of_bsn_get_interfaces_reply_t *src)
{
    of_bsn_get_interfaces_reply_t *dst;
    uint32_t val32;

    of_list_bsn_interface_t src_list;
    of_list_bsn_interface_t *dst_list;

    if ((dst = of_bsn_get_interfaces_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_reply_xid_get(src, &val32);
    of_bsn_get_interfaces_reply_xid_set(dst, val32);

    of_bsn_get_interfaces_reply_experimenter_get(src, &val32);
    of_bsn_get_interfaces_reply_experimenter_set(dst, val32);

    of_bsn_get_interfaces_reply_subtype_get(src, &val32);
    of_bsn_get_interfaces_reply_subtype_set(dst, val32);

    of_bsn_get_interfaces_reply_interfaces_bind(
        src, &src_list);
    dst_list = of_list_bsn_interface_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_get_interfaces_reply_delete(dst);
        return NULL;
    }
    of_bsn_get_interfaces_reply_interfaces_set(dst, dst_list);
    of_list_bsn_interface_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_request_t *
of_bsn_get_interfaces_request_OF_VERSION_1_2_dup(
    of_bsn_get_interfaces_request_t *src)
{
    of_bsn_get_interfaces_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_interfaces_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_request_xid_get(src, &val32);
    of_bsn_get_interfaces_request_xid_set(dst, val32);

    of_bsn_get_interfaces_request_experimenter_get(src, &val32);
    of_bsn_get_interfaces_request_experimenter_set(dst, val32);

    of_bsn_get_interfaces_request_subtype_get(src, &val32);
    of_bsn_get_interfaces_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_reply_t *
of_bsn_get_mirroring_reply_OF_VERSION_1_2_dup(
    of_bsn_get_mirroring_reply_t *src)
{
    of_bsn_get_mirroring_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_reply_xid_get(src, &val32);
    of_bsn_get_mirroring_reply_xid_set(dst, val32);

    of_bsn_get_mirroring_reply_experimenter_get(src, &val32);
    of_bsn_get_mirroring_reply_experimenter_set(dst, val32);

    of_bsn_get_mirroring_reply_subtype_get(src, &val32);
    of_bsn_get_mirroring_reply_subtype_set(dst, val32);

    of_bsn_get_mirroring_reply_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_reply_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_request_t *
of_bsn_get_mirroring_request_OF_VERSION_1_2_dup(
    of_bsn_get_mirroring_request_t *src)
{
    of_bsn_get_mirroring_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_request_xid_get(src, &val32);
    of_bsn_get_mirroring_request_xid_set(dst, val32);

    of_bsn_get_mirroring_request_experimenter_get(src, &val32);
    of_bsn_get_mirroring_request_experimenter_set(dst, val32);

    of_bsn_get_mirroring_request_subtype_get(src, &val32);
    of_bsn_get_mirroring_request_subtype_set(dst, val32);

    of_bsn_get_mirroring_request_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_request_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_reply_t *
of_bsn_pdu_rx_reply_OF_VERSION_1_2_dup(
    of_bsn_pdu_rx_reply_t *src)
{
    of_bsn_pdu_rx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_reply_xid_get(src, &val32);
    of_bsn_pdu_rx_reply_xid_set(dst, val32);

    of_bsn_pdu_rx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_rx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_rx_reply_subtype_get(src, &val32);
    of_bsn_pdu_rx_reply_subtype_set(dst, val32);

    of_bsn_pdu_rx_reply_status_get(src, &val32);
    of_bsn_pdu_rx_reply_status_set(dst, val32);

    of_bsn_pdu_rx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_rx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_rx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_rx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_request_t *
of_bsn_pdu_rx_request_OF_VERSION_1_2_dup(
    of_bsn_pdu_rx_request_t *src)
{
    of_bsn_pdu_rx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_rx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_request_xid_get(src, &val32);
    of_bsn_pdu_rx_request_xid_set(dst, val32);

    of_bsn_pdu_rx_request_experimenter_get(src, &val32);
    of_bsn_pdu_rx_request_experimenter_set(dst, val32);

    of_bsn_pdu_rx_request_subtype_get(src, &val32);
    of_bsn_pdu_rx_request_subtype_set(dst, val32);

    of_bsn_pdu_rx_request_timeout_ms_get(src, &val32);
    of_bsn_pdu_rx_request_timeout_ms_set(dst, val32);

    of_bsn_pdu_rx_request_port_no_get(src, &port_no);
    of_bsn_pdu_rx_request_port_no_set(dst, port_no);

    of_bsn_pdu_rx_request_slot_num_get(src, &val8);
    of_bsn_pdu_rx_request_slot_num_set(dst, val8);

    of_bsn_pdu_rx_request_data_get(src, &octets);
    of_bsn_pdu_rx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_timeout_t *
of_bsn_pdu_rx_timeout_OF_VERSION_1_2_dup(
    of_bsn_pdu_rx_timeout_t *src)
{
    of_bsn_pdu_rx_timeout_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_timeout_xid_get(src, &val32);
    of_bsn_pdu_rx_timeout_xid_set(dst, val32);

    of_bsn_pdu_rx_timeout_experimenter_get(src, &val32);
    of_bsn_pdu_rx_timeout_experimenter_set(dst, val32);

    of_bsn_pdu_rx_timeout_subtype_get(src, &val32);
    of_bsn_pdu_rx_timeout_subtype_set(dst, val32);

    of_bsn_pdu_rx_timeout_port_no_get(src, &port_no);
    of_bsn_pdu_rx_timeout_port_no_set(dst, port_no);

    of_bsn_pdu_rx_timeout_slot_num_get(src, &val8);
    of_bsn_pdu_rx_timeout_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_reply_t *
of_bsn_pdu_tx_reply_OF_VERSION_1_2_dup(
    of_bsn_pdu_tx_reply_t *src)
{
    of_bsn_pdu_tx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_tx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_reply_xid_get(src, &val32);
    of_bsn_pdu_tx_reply_xid_set(dst, val32);

    of_bsn_pdu_tx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_tx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_tx_reply_subtype_get(src, &val32);
    of_bsn_pdu_tx_reply_subtype_set(dst, val32);

    of_bsn_pdu_tx_reply_status_get(src, &val32);
    of_bsn_pdu_tx_reply_status_set(dst, val32);

    of_bsn_pdu_tx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_tx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_tx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_tx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_request_t *
of_bsn_pdu_tx_request_OF_VERSION_1_2_dup(
    of_bsn_pdu_tx_request_t *src)
{
    of_bsn_pdu_tx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_tx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_request_xid_get(src, &val32);
    of_bsn_pdu_tx_request_xid_set(dst, val32);

    of_bsn_pdu_tx_request_experimenter_get(src, &val32);
    of_bsn_pdu_tx_request_experimenter_set(dst, val32);

    of_bsn_pdu_tx_request_subtype_get(src, &val32);
    of_bsn_pdu_tx_request_subtype_set(dst, val32);

    of_bsn_pdu_tx_request_tx_interval_ms_get(src, &val32);
    of_bsn_pdu_tx_request_tx_interval_ms_set(dst, val32);

    of_bsn_pdu_tx_request_port_no_get(src, &port_no);
    of_bsn_pdu_tx_request_port_no_set(dst, port_no);

    of_bsn_pdu_tx_request_slot_num_get(src, &val8);
    of_bsn_pdu_tx_request_slot_num_set(dst, val8);

    of_bsn_pdu_tx_request_data_get(src, &octets);
    of_bsn_pdu_tx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_mirroring
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_mirroring.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_mirroring_t *
of_bsn_set_mirroring_OF_VERSION_1_2_dup(
    of_bsn_set_mirroring_t *src)
{
    of_bsn_set_mirroring_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_mirroring_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_mirroring_xid_get(src, &val32);
    of_bsn_set_mirroring_xid_set(dst, val32);

    of_bsn_set_mirroring_experimenter_get(src, &val32);
    of_bsn_set_mirroring_experimenter_set(dst, val32);

    of_bsn_set_mirroring_subtype_get(src, &val32);
    of_bsn_set_mirroring_subtype_set(dst, val32);

    of_bsn_set_mirroring_report_mirror_ports_get(src, &val8);
    of_bsn_set_mirroring_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_reply_t *
of_bsn_set_pktin_suppression_reply_OF_VERSION_1_2_dup(
    of_bsn_set_pktin_suppression_reply_t *src)
{
    of_bsn_set_pktin_suppression_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_pktin_suppression_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_reply_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_status_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_request_t *
of_bsn_set_pktin_suppression_request_OF_VERSION_1_2_dup(
    of_bsn_set_pktin_suppression_request_t *src)
{
    of_bsn_set_pktin_suppression_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_bsn_set_pktin_suppression_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_request_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_request_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_request_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_request_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_request_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_request_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_request_enabled_get(src, &val8);
    of_bsn_set_pktin_suppression_request_enabled_set(dst, val8);

    of_bsn_set_pktin_suppression_request_idle_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_idle_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_hard_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_hard_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_priority_get(src, &val16);
    of_bsn_set_pktin_suppression_request_priority_set(dst, val16);

    of_bsn_set_pktin_suppression_request_cookie_get(src, &val64);
    of_bsn_set_pktin_suppression_request_cookie_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_reply_t *
of_bsn_virtual_port_create_reply_OF_VERSION_1_2_dup(
    of_bsn_virtual_port_create_reply_t *src)
{
    of_bsn_virtual_port_create_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_create_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_reply_xid_get(src, &val32);
    of_bsn_virtual_port_create_reply_xid_set(dst, val32);

    of_bsn_virtual_port_create_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_create_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_create_reply_status_get(src, &val32);
    of_bsn_virtual_port_create_reply_status_set(dst, val32);

    of_bsn_virtual_port_create_reply_vport_no_get(src, &val32);
    of_bsn_virtual_port_create_reply_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_request_t *
of_bsn_virtual_port_create_request_OF_VERSION_1_2_dup(
    of_bsn_virtual_port_create_request_t *src)
{
    of_bsn_virtual_port_create_request_t *dst;
    uint32_t val32;

    of_bsn_vport_q_in_q_t src_bsn_vport;
    of_bsn_vport_q_in_q_t *dst_bsn_vport;

    if ((dst = of_bsn_virtual_port_create_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_request_xid_get(src, &val32);
    of_bsn_virtual_port_create_request_xid_set(dst, val32);

    of_bsn_virtual_port_create_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_request_subtype_get(src, &val32);
    of_bsn_virtual_port_create_request_subtype_set(dst, val32);

    of_bsn_virtual_port_create_request_vport_bind(
        src, &src_bsn_vport);
    dst_bsn_vport = of_bsn_vport_q_in_q_OF_VERSION_1_2_dup(&src_bsn_vport);
    if (dst_bsn_vport == NULL) {
        of_bsn_virtual_port_create_request_delete(dst);
        return NULL;
    }
    of_bsn_virtual_port_create_request_vport_set(dst, dst_bsn_vport);
    of_bsn_vport_q_in_q_delete(dst_bsn_vport);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_reply_t *
of_bsn_virtual_port_remove_reply_OF_VERSION_1_2_dup(
    of_bsn_virtual_port_remove_reply_t *src)
{
    of_bsn_virtual_port_remove_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_reply_xid_get(src, &val32);
    of_bsn_virtual_port_remove_reply_xid_set(dst, val32);

    of_bsn_virtual_port_remove_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_reply_status_get(src, &val32);
    of_bsn_virtual_port_remove_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_request_t *
of_bsn_virtual_port_remove_request_OF_VERSION_1_2_dup(
    of_bsn_virtual_port_remove_request_t *src)
{
    of_bsn_virtual_port_remove_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_request_xid_get(src, &val32);
    of_bsn_virtual_port_remove_request_xid_set(dst, val32);

    of_bsn_virtual_port_remove_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_request_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_request_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_request_vport_no_get(src, &val32);
    of_bsn_virtual_port_remove_request_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_reply_t *
of_desc_stats_reply_OF_VERSION_1_2_dup(
    of_desc_stats_reply_t *src)
{
    of_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_serial_num_t ser_num;

    if ((dst = of_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_reply_xid_get(src, &val32);
    of_desc_stats_reply_xid_set(dst, val32);

    of_desc_stats_reply_flags_get(src, &val16);
    of_desc_stats_reply_flags_set(dst, val16);

    of_desc_stats_reply_mfr_desc_get(src, &desc_str);
    of_desc_stats_reply_mfr_desc_set(dst, desc_str);

    of_desc_stats_reply_hw_desc_get(src, &desc_str);
    of_desc_stats_reply_hw_desc_set(dst, desc_str);

    of_desc_stats_reply_sw_desc_get(src, &desc_str);
    of_desc_stats_reply_sw_desc_set(dst, desc_str);

    of_desc_stats_reply_serial_num_get(src, &ser_num);
    of_desc_stats_reply_serial_num_set(dst, ser_num);

    of_desc_stats_reply_dp_desc_get(src, &desc_str);
    of_desc_stats_reply_dp_desc_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_request_t *
of_desc_stats_request_OF_VERSION_1_2_dup(
    of_desc_stats_request_t *src)
{
    of_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_request_xid_get(src, &val32);
    of_desc_stats_request_xid_set(dst, val32);

    of_desc_stats_request_flags_get(src, &val16);
    of_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_echo_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_reply_t *
of_echo_reply_OF_VERSION_1_2_dup(
    of_echo_reply_t *src)
{
    of_echo_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_reply_xid_get(src, &val32);
    of_echo_reply_xid_set(dst, val32);

    of_echo_reply_data_get(src, &octets);
    of_echo_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_echo_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_request_t *
of_echo_request_OF_VERSION_1_2_dup(
    of_echo_request_t *src)
{
    of_echo_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_request_xid_get(src, &val32);
    of_echo_request_xid_set(dst, val32);

    of_echo_request_data_get(src, &octets);
    of_echo_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_features_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_reply_t *
of_features_reply_OF_VERSION_1_2_dup(
    of_features_reply_t *src)
{
    of_features_reply_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;

    of_list_port_desc_t src_list;
    of_list_port_desc_t *dst_list;

    if ((dst = of_features_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_reply_xid_get(src, &val32);
    of_features_reply_xid_set(dst, val32);

    of_features_reply_datapath_id_get(src, &val64);
    of_features_reply_datapath_id_set(dst, val64);

    of_features_reply_n_buffers_get(src, &val32);
    of_features_reply_n_buffers_set(dst, val32);

    of_features_reply_n_tables_get(src, &val8);
    of_features_reply_n_tables_set(dst, val8);

    of_features_reply_capabilities_get(src, &val32);
    of_features_reply_capabilities_set(dst, val32);

    of_features_reply_reserved_get(src, &val32);
    of_features_reply_reserved_set(dst, val32);

    of_features_reply_ports_bind(
        src, &src_list);
    dst_list = of_list_port_desc_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_features_reply_delete(dst);
        return NULL;
    }
    of_features_reply_ports_set(dst, dst_list);
    of_list_port_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_features_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_request_t *
of_features_request_OF_VERSION_1_2_dup(
    of_features_request_t *src)
{
    of_features_request_t *dst;
    uint32_t val32;

    if ((dst = of_features_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_request_xid_get(src, &val32);
    of_features_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_flow_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_add_t *
of_flow_add_OF_VERSION_1_2_dup(
    of_flow_add_t *src)
{
    of_flow_add_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_add_xid_get(src, &val32);
    of_flow_add_xid_set(dst, val32);

    of_flow_add_cookie_get(src, &val64);
    of_flow_add_cookie_set(dst, val64);

    of_flow_add_cookie_mask_get(src, &val64);
    of_flow_add_cookie_mask_set(dst, val64);

    of_flow_add_table_id_get(src, &val8);
    of_flow_add_table_id_set(dst, val8);

    of_flow_add_idle_timeout_get(src, &val16);
    of_flow_add_idle_timeout_set(dst, val16);

    of_flow_add_hard_timeout_get(src, &val16);
    of_flow_add_hard_timeout_set(dst, val16);

    of_flow_add_priority_get(src, &val16);
    of_flow_add_priority_set(dst, val16);

    of_flow_add_buffer_id_get(src, &val32);
    of_flow_add_buffer_id_set(dst, val32);

    of_flow_add_out_port_get(src, &port_no);
    of_flow_add_out_port_set(dst, port_no);

    of_flow_add_out_group_get(src, &val32);
    of_flow_add_out_group_set(dst, val32);

    of_flow_add_flags_get(src, &val16);
    of_flow_add_flags_set(dst, val16);

    of_flow_add_match_get(src, &match);
    of_flow_add_match_set(dst, &match);

    of_flow_add_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_add_delete(dst);
        return NULL;
    }
    of_flow_add_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_t *
of_flow_delete_OF_VERSION_1_2_dup(
    of_flow_delete_t *src)
{
    of_flow_delete_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_xid_get(src, &val32);
    of_flow_delete_xid_set(dst, val32);

    of_flow_delete_cookie_get(src, &val64);
    of_flow_delete_cookie_set(dst, val64);

    of_flow_delete_cookie_mask_get(src, &val64);
    of_flow_delete_cookie_mask_set(dst, val64);

    of_flow_delete_table_id_get(src, &val8);
    of_flow_delete_table_id_set(dst, val8);

    of_flow_delete_idle_timeout_get(src, &val16);
    of_flow_delete_idle_timeout_set(dst, val16);

    of_flow_delete_hard_timeout_get(src, &val16);
    of_flow_delete_hard_timeout_set(dst, val16);

    of_flow_delete_priority_get(src, &val16);
    of_flow_delete_priority_set(dst, val16);

    of_flow_delete_buffer_id_get(src, &val32);
    of_flow_delete_buffer_id_set(dst, val32);

    of_flow_delete_out_port_get(src, &port_no);
    of_flow_delete_out_port_set(dst, port_no);

    of_flow_delete_out_group_get(src, &val32);
    of_flow_delete_out_group_set(dst, val32);

    of_flow_delete_flags_get(src, &val16);
    of_flow_delete_flags_set(dst, val16);

    of_flow_delete_match_get(src, &match);
    of_flow_delete_match_set(dst, &match);

    of_flow_delete_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_delete(dst);
        return NULL;
    }
    of_flow_delete_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_strict_t *
of_flow_delete_strict_OF_VERSION_1_2_dup(
    of_flow_delete_strict_t *src)
{
    of_flow_delete_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_strict_xid_get(src, &val32);
    of_flow_delete_strict_xid_set(dst, val32);

    of_flow_delete_strict_cookie_get(src, &val64);
    of_flow_delete_strict_cookie_set(dst, val64);

    of_flow_delete_strict_cookie_mask_get(src, &val64);
    of_flow_delete_strict_cookie_mask_set(dst, val64);

    of_flow_delete_strict_table_id_get(src, &val8);
    of_flow_delete_strict_table_id_set(dst, val8);

    of_flow_delete_strict_idle_timeout_get(src, &val16);
    of_flow_delete_strict_idle_timeout_set(dst, val16);

    of_flow_delete_strict_hard_timeout_get(src, &val16);
    of_flow_delete_strict_hard_timeout_set(dst, val16);

    of_flow_delete_strict_priority_get(src, &val16);
    of_flow_delete_strict_priority_set(dst, val16);

    of_flow_delete_strict_buffer_id_get(src, &val32);
    of_flow_delete_strict_buffer_id_set(dst, val32);

    of_flow_delete_strict_out_port_get(src, &port_no);
    of_flow_delete_strict_out_port_set(dst, port_no);

    of_flow_delete_strict_out_group_get(src, &val32);
    of_flow_delete_strict_out_group_set(dst, val32);

    of_flow_delete_strict_flags_get(src, &val16);
    of_flow_delete_strict_flags_set(dst, val16);

    of_flow_delete_strict_match_get(src, &match);
    of_flow_delete_strict_match_set(dst, &match);

    of_flow_delete_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_strict_delete(dst);
        return NULL;
    }
    of_flow_delete_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_mod_failed_error_msg_t *
of_flow_mod_failed_error_msg_OF_VERSION_1_2_dup(
    of_flow_mod_failed_error_msg_t *src)
{
    of_flow_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_mod_failed_error_msg_xid_get(src, &val32);
    of_flow_mod_failed_error_msg_xid_set(dst, val32);

    of_flow_mod_failed_error_msg_code_get(src, &val16);
    of_flow_mod_failed_error_msg_code_set(dst, val16);

    of_flow_mod_failed_error_msg_data_get(src, &octets);
    of_flow_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_t *
of_flow_modify_OF_VERSION_1_2_dup(
    of_flow_modify_t *src)
{
    of_flow_modify_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_xid_get(src, &val32);
    of_flow_modify_xid_set(dst, val32);

    of_flow_modify_cookie_get(src, &val64);
    of_flow_modify_cookie_set(dst, val64);

    of_flow_modify_cookie_mask_get(src, &val64);
    of_flow_modify_cookie_mask_set(dst, val64);

    of_flow_modify_table_id_get(src, &val8);
    of_flow_modify_table_id_set(dst, val8);

    of_flow_modify_idle_timeout_get(src, &val16);
    of_flow_modify_idle_timeout_set(dst, val16);

    of_flow_modify_hard_timeout_get(src, &val16);
    of_flow_modify_hard_timeout_set(dst, val16);

    of_flow_modify_priority_get(src, &val16);
    of_flow_modify_priority_set(dst, val16);

    of_flow_modify_buffer_id_get(src, &val32);
    of_flow_modify_buffer_id_set(dst, val32);

    of_flow_modify_out_port_get(src, &port_no);
    of_flow_modify_out_port_set(dst, port_no);

    of_flow_modify_out_group_get(src, &val32);
    of_flow_modify_out_group_set(dst, val32);

    of_flow_modify_flags_get(src, &val16);
    of_flow_modify_flags_set(dst, val16);

    of_flow_modify_match_get(src, &match);
    of_flow_modify_match_set(dst, &match);

    of_flow_modify_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_delete(dst);
        return NULL;
    }
    of_flow_modify_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_strict_t *
of_flow_modify_strict_OF_VERSION_1_2_dup(
    of_flow_modify_strict_t *src)
{
    of_flow_modify_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_strict_xid_get(src, &val32);
    of_flow_modify_strict_xid_set(dst, val32);

    of_flow_modify_strict_cookie_get(src, &val64);
    of_flow_modify_strict_cookie_set(dst, val64);

    of_flow_modify_strict_cookie_mask_get(src, &val64);
    of_flow_modify_strict_cookie_mask_set(dst, val64);

    of_flow_modify_strict_table_id_get(src, &val8);
    of_flow_modify_strict_table_id_set(dst, val8);

    of_flow_modify_strict_idle_timeout_get(src, &val16);
    of_flow_modify_strict_idle_timeout_set(dst, val16);

    of_flow_modify_strict_hard_timeout_get(src, &val16);
    of_flow_modify_strict_hard_timeout_set(dst, val16);

    of_flow_modify_strict_priority_get(src, &val16);
    of_flow_modify_strict_priority_set(dst, val16);

    of_flow_modify_strict_buffer_id_get(src, &val32);
    of_flow_modify_strict_buffer_id_set(dst, val32);

    of_flow_modify_strict_out_port_get(src, &port_no);
    of_flow_modify_strict_out_port_set(dst, port_no);

    of_flow_modify_strict_out_group_get(src, &val32);
    of_flow_modify_strict_out_group_set(dst, val32);

    of_flow_modify_strict_flags_get(src, &val16);
    of_flow_modify_strict_flags_set(dst, val16);

    of_flow_modify_strict_match_get(src, &match);
    of_flow_modify_strict_match_set(dst, &match);

    of_flow_modify_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_strict_delete(dst);
        return NULL;
    }
    of_flow_modify_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_removed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_removed.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_removed_t *
of_flow_removed_OF_VERSION_1_2_dup(
    of_flow_removed_t *src)
{
    of_flow_removed_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_flow_removed_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_removed_xid_get(src, &val32);
    of_flow_removed_xid_set(dst, val32);

    of_flow_removed_cookie_get(src, &val64);
    of_flow_removed_cookie_set(dst, val64);

    of_flow_removed_priority_get(src, &val16);
    of_flow_removed_priority_set(dst, val16);

    of_flow_removed_reason_get(src, &val8);
    of_flow_removed_reason_set(dst, val8);

    of_flow_removed_table_id_get(src, &val8);
    of_flow_removed_table_id_set(dst, val8);

    of_flow_removed_duration_sec_get(src, &val32);
    of_flow_removed_duration_sec_set(dst, val32);

    of_flow_removed_duration_nsec_get(src, &val32);
    of_flow_removed_duration_nsec_set(dst, val32);

    of_flow_removed_idle_timeout_get(src, &val16);
    of_flow_removed_idle_timeout_set(dst, val16);

    of_flow_removed_hard_timeout_get(src, &val16);
    of_flow_removed_hard_timeout_set(dst, val16);

    of_flow_removed_packet_count_get(src, &val64);
    of_flow_removed_packet_count_set(dst, val64);

    of_flow_removed_byte_count_get(src, &val64);
    of_flow_removed_byte_count_set(dst, val64);

    of_flow_removed_match_get(src, &match);
    of_flow_removed_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_reply_t *
of_flow_stats_reply_OF_VERSION_1_2_dup(
    of_flow_stats_reply_t *src)
{
    of_flow_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_flow_stats_entry_t src_list;
    of_list_flow_stats_entry_t *dst_list;

    if ((dst = of_flow_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_reply_xid_get(src, &val32);
    of_flow_stats_reply_xid_set(dst, val32);

    of_flow_stats_reply_flags_get(src, &val16);
    of_flow_stats_reply_flags_set(dst, val16);

    of_flow_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_flow_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_reply_delete(dst);
        return NULL;
    }
    of_flow_stats_reply_entries_set(dst, dst_list);
    of_list_flow_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_request_t *
of_flow_stats_request_OF_VERSION_1_2_dup(
    of_flow_stats_request_t *src)
{
    of_flow_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_flow_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_request_xid_get(src, &val32);
    of_flow_stats_request_xid_set(dst, val32);

    of_flow_stats_request_flags_get(src, &val16);
    of_flow_stats_request_flags_set(dst, val16);

    of_flow_stats_request_table_id_get(src, &val8);
    of_flow_stats_request_table_id_set(dst, val8);

    of_flow_stats_request_out_port_get(src, &port_no);
    of_flow_stats_request_out_port_set(dst, port_no);

    of_flow_stats_request_out_group_get(src, &val32);
    of_flow_stats_request_out_group_set(dst, val32);

    of_flow_stats_request_cookie_get(src, &val64);
    of_flow_stats_request_cookie_set(dst, val64);

    of_flow_stats_request_cookie_mask_get(src, &val64);
    of_flow_stats_request_cookie_mask_set(dst, val64);

    of_flow_stats_request_match_get(src, &match);
    of_flow_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_reply_t *
of_get_config_reply_OF_VERSION_1_2_dup(
    of_get_config_reply_t *src)
{
    of_get_config_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_reply_xid_get(src, &val32);
    of_get_config_reply_xid_set(dst, val32);

    of_get_config_reply_flags_get(src, &val16);
    of_get_config_reply_flags_set(dst, val16);

    of_get_config_reply_miss_send_len_get(src, &val16);
    of_get_config_reply_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_request_t *
of_get_config_request_OF_VERSION_1_2_dup(
    of_get_config_request_t *src)
{
    of_get_config_request_t *dst;
    uint32_t val32;

    if ((dst = of_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_request_xid_get(src, &val32);
    of_get_config_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_add_t *
of_group_add_OF_VERSION_1_2_dup(
    of_group_add_t *src)
{
    of_group_add_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_add_xid_get(src, &val32);
    of_group_add_xid_set(dst, val32);

    of_group_add_group_type_get(src, &val8);
    of_group_add_group_type_set(dst, val8);

    of_group_add_group_id_get(src, &val32);
    of_group_add_group_id_set(dst, val32);

    of_group_add_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_add_delete(dst);
        return NULL;
    }
    of_group_add_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_delete_t *
of_group_delete_OF_VERSION_1_2_dup(
    of_group_delete_t *src)
{
    of_group_delete_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_delete_xid_get(src, &val32);
    of_group_delete_xid_set(dst, val32);

    of_group_delete_group_type_get(src, &val8);
    of_group_delete_group_type_set(dst, val8);

    of_group_delete_group_id_get(src, &val32);
    of_group_delete_group_id_set(dst, val32);

    of_group_delete_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_delete_delete(dst);
        return NULL;
    }
    of_group_delete_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_reply_t *
of_group_desc_stats_reply_OF_VERSION_1_2_dup(
    of_group_desc_stats_reply_t *src)
{
    of_group_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_desc_stats_entry_t src_list;
    of_list_group_desc_stats_entry_t *dst_list;

    if ((dst = of_group_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_reply_xid_get(src, &val32);
    of_group_desc_stats_reply_xid_set(dst, val32);

    of_group_desc_stats_reply_flags_get(src, &val16);
    of_group_desc_stats_reply_flags_set(dst, val16);

    of_group_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_desc_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_group_desc_stats_reply_entries_set(dst, dst_list);
    of_list_group_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_request_t *
of_group_desc_stats_request_OF_VERSION_1_2_dup(
    of_group_desc_stats_request_t *src)
{
    of_group_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_request_xid_get(src, &val32);
    of_group_desc_stats_request_xid_set(dst, val32);

    of_group_desc_stats_request_flags_get(src, &val16);
    of_group_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_reply_t *
of_group_features_stats_reply_OF_VERSION_1_2_dup(
    of_group_features_stats_reply_t *src)
{
    of_group_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_reply_xid_get(src, &val32);
    of_group_features_stats_reply_xid_set(dst, val32);

    of_group_features_stats_reply_flags_get(src, &val16);
    of_group_features_stats_reply_flags_set(dst, val16);

    of_group_features_stats_reply_types_get(src, &val32);
    of_group_features_stats_reply_types_set(dst, val32);

    of_group_features_stats_reply_capabilities_get(src, &val32);
    of_group_features_stats_reply_capabilities_set(dst, val32);

    of_group_features_stats_reply_max_groups_all_get(src, &val32);
    of_group_features_stats_reply_max_groups_all_set(dst, val32);

    of_group_features_stats_reply_max_groups_select_get(src, &val32);
    of_group_features_stats_reply_max_groups_select_set(dst, val32);

    of_group_features_stats_reply_max_groups_indirect_get(src, &val32);
    of_group_features_stats_reply_max_groups_indirect_set(dst, val32);

    of_group_features_stats_reply_max_groups_ff_get(src, &val32);
    of_group_features_stats_reply_max_groups_ff_set(dst, val32);

    of_group_features_stats_reply_actions_all_get(src, &val32);
    of_group_features_stats_reply_actions_all_set(dst, val32);

    of_group_features_stats_reply_actions_select_get(src, &val32);
    of_group_features_stats_reply_actions_select_set(dst, val32);

    of_group_features_stats_reply_actions_indirect_get(src, &val32);
    of_group_features_stats_reply_actions_indirect_set(dst, val32);

    of_group_features_stats_reply_actions_ff_get(src, &val32);
    of_group_features_stats_reply_actions_ff_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_request_t *
of_group_features_stats_request_OF_VERSION_1_2_dup(
    of_group_features_stats_request_t *src)
{
    of_group_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_request_xid_get(src, &val32);
    of_group_features_stats_request_xid_set(dst, val32);

    of_group_features_stats_request_flags_get(src, &val16);
    of_group_features_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_mod_failed_error_msg_t *
of_group_mod_failed_error_msg_OF_VERSION_1_2_dup(
    of_group_mod_failed_error_msg_t *src)
{
    of_group_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_group_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_mod_failed_error_msg_xid_get(src, &val32);
    of_group_mod_failed_error_msg_xid_set(dst, val32);

    of_group_mod_failed_error_msg_code_get(src, &val16);
    of_group_mod_failed_error_msg_code_set(dst, val16);

    of_group_mod_failed_error_msg_data_get(src, &octets);
    of_group_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_group_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_modify_t *
of_group_modify_OF_VERSION_1_2_dup(
    of_group_modify_t *src)
{
    of_group_modify_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_modify_xid_get(src, &val32);
    of_group_modify_xid_set(dst, val32);

    of_group_modify_group_type_get(src, &val8);
    of_group_modify_group_type_set(dst, val8);

    of_group_modify_group_id_get(src, &val32);
    of_group_modify_group_id_set(dst, val32);

    of_group_modify_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_modify_delete(dst);
        return NULL;
    }
    of_group_modify_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_reply_t *
of_group_stats_reply_OF_VERSION_1_2_dup(
    of_group_stats_reply_t *src)
{
    of_group_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_stats_entry_t src_list;
    of_list_group_stats_entry_t *dst_list;

    if ((dst = of_group_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_reply_xid_get(src, &val32);
    of_group_stats_reply_xid_set(dst, val32);

    of_group_stats_reply_flags_get(src, &val16);
    of_group_stats_reply_flags_set(dst, val16);

    of_group_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_reply_delete(dst);
        return NULL;
    }
    of_group_stats_reply_entries_set(dst, dst_list);
    of_list_group_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_request_t *
of_group_stats_request_OF_VERSION_1_2_dup(
    of_group_stats_request_t *src)
{
    of_group_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_request_xid_get(src, &val32);
    of_group_stats_request_xid_set(dst, val32);

    of_group_stats_request_flags_get(src, &val16);
    of_group_stats_request_flags_set(dst, val16);

    of_group_stats_request_group_id_get(src, &val32);
    of_group_stats_request_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_header
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_header.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_header_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_hello
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_t *
of_hello_OF_VERSION_1_2_dup(
    of_hello_t *src)
{
    of_hello_t *dst;
    uint32_t val32;

    if ((dst = of_hello_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_xid_get(src, &val32);
    of_hello_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_hello_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_failed_error_msg_t *
of_hello_failed_error_msg_OF_VERSION_1_2_dup(
    of_hello_failed_error_msg_t *src)
{
    of_hello_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_hello_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_failed_error_msg_xid_get(src, &val32);
    of_hello_failed_error_msg_xid_set(dst, val32);

    of_hello_failed_error_msg_code_get(src, &val16);
    of_hello_failed_error_msg_code_set(dst, val16);

    of_hello_failed_error_msg_data_get(src, &octets);
    of_hello_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_in_t *
of_packet_in_OF_VERSION_1_2_dup(
    of_packet_in_t *src)
{
    of_packet_in_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;
    of_octets_t octets;

    if ((dst = of_packet_in_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_in_xid_get(src, &val32);
    of_packet_in_xid_set(dst, val32);

    of_packet_in_buffer_id_get(src, &val32);
    of_packet_in_buffer_id_set(dst, val32);

    of_packet_in_total_len_get(src, &val16);
    of_packet_in_total_len_set(dst, val16);

    of_packet_in_reason_get(src, &val8);
    of_packet_in_reason_set(dst, val8);

    of_packet_in_table_id_get(src, &val8);
    of_packet_in_table_id_set(dst, val8);

    of_packet_in_match_get(src, &match);
    of_packet_in_match_set(dst, &match);

    of_packet_in_data_get(src, &octets);
    of_packet_in_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_out_t *
of_packet_out_OF_VERSION_1_2_dup(
    of_packet_out_t *src)
{
    of_packet_out_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;
    of_octets_t octets;

    if ((dst = of_packet_out_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_out_xid_get(src, &val32);
    of_packet_out_xid_set(dst, val32);

    of_packet_out_buffer_id_get(src, &val32);
    of_packet_out_buffer_id_set(dst, val32);

    of_packet_out_in_port_get(src, &port_no);
    of_packet_out_in_port_set(dst, port_no);

    of_packet_out_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_out_delete(dst);
        return NULL;
    }
    of_packet_out_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    of_packet_out_data_get(src, &octets);
    of_packet_out_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_t *
of_port_mod_OF_VERSION_1_2_dup(
    of_port_mod_t *src)
{
    of_port_mod_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;

    if ((dst = of_port_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_xid_get(src, &val32);
    of_port_mod_xid_set(dst, val32);

    of_port_mod_port_no_get(src, &port_no);
    of_port_mod_port_no_set(dst, port_no);

    of_port_mod_hw_addr_get(src, &mac_addr);
    of_port_mod_hw_addr_set(dst, mac_addr);

    of_port_mod_config_get(src, &val32);
    of_port_mod_config_set(dst, val32);

    of_port_mod_mask_get(src, &val32);
    of_port_mod_mask_set(dst, val32);

    of_port_mod_advertise_get(src, &val32);
    of_port_mod_advertise_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_failed_error_msg_t *
of_port_mod_failed_error_msg_OF_VERSION_1_2_dup(
    of_port_mod_failed_error_msg_t *src)
{
    of_port_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_port_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_failed_error_msg_xid_get(src, &val32);
    of_port_mod_failed_error_msg_xid_set(dst, val32);

    of_port_mod_failed_error_msg_code_get(src, &val16);
    of_port_mod_failed_error_msg_code_set(dst, val16);

    of_port_mod_failed_error_msg_data_get(src, &octets);
    of_port_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_reply_t *
of_port_stats_reply_OF_VERSION_1_2_dup(
    of_port_stats_reply_t *src)
{
    of_port_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_stats_entry_t src_list;
    of_list_port_stats_entry_t *dst_list;

    if ((dst = of_port_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_reply_xid_get(src, &val32);
    of_port_stats_reply_xid_set(dst, val32);

    of_port_stats_reply_flags_get(src, &val16);
    of_port_stats_reply_flags_set(dst, val16);

    of_port_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_reply_delete(dst);
        return NULL;
    }
    of_port_stats_reply_entries_set(dst, dst_list);
    of_list_port_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_request_t *
of_port_stats_request_OF_VERSION_1_2_dup(
    of_port_stats_request_t *src)
{
    of_port_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_port_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_request_xid_get(src, &val32);
    of_port_stats_request_xid_set(dst, val32);

    of_port_stats_request_flags_get(src, &val16);
    of_port_stats_request_flags_set(dst, val16);

    of_port_stats_request_port_no_get(src, &port_no);
    of_port_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_port_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_status_t *
of_port_status_OF_VERSION_1_2_dup(
    of_port_status_t *src)
{
    of_port_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_port_desc_t src_port_desc;
    of_port_desc_t *dst_port_desc;

    if ((dst = of_port_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_status_xid_get(src, &val32);
    of_port_status_xid_set(dst, val32);

    of_port_status_reason_get(src, &val8);
    of_port_status_reason_set(dst, val8);

    of_port_status_desc_bind(
        src, &src_port_desc);
    dst_port_desc = of_port_desc_OF_VERSION_1_2_dup(&src_port_desc);
    if (dst_port_desc == NULL) {
        of_port_status_delete(dst);
        return NULL;
    }
    of_port_status_desc_set(dst, dst_port_desc);
    of_port_desc_delete(dst_port_desc);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_reply_t *
of_queue_get_config_reply_OF_VERSION_1_2_dup(
    of_queue_get_config_reply_t *src)
{
    of_queue_get_config_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_packet_queue_t src_list;
    of_list_packet_queue_t *dst_list;

    if ((dst = of_queue_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_reply_xid_get(src, &val32);
    of_queue_get_config_reply_xid_set(dst, val32);

    of_queue_get_config_reply_port_get(src, &port_no);
    of_queue_get_config_reply_port_set(dst, port_no);

    of_queue_get_config_reply_queues_bind(
        src, &src_list);
    dst_list = of_list_packet_queue_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_get_config_reply_delete(dst);
        return NULL;
    }
    of_queue_get_config_reply_queues_set(dst, dst_list);
    of_list_packet_queue_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_request_t *
of_queue_get_config_request_OF_VERSION_1_2_dup(
    of_queue_get_config_request_t *src)
{
    of_queue_get_config_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_queue_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_request_xid_get(src, &val32);
    of_queue_get_config_request_xid_set(dst, val32);

    of_queue_get_config_request_port_get(src, &port_no);
    of_queue_get_config_request_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_queue_op_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_op_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_op_failed_error_msg_t *
of_queue_op_failed_error_msg_OF_VERSION_1_2_dup(
    of_queue_op_failed_error_msg_t *src)
{
    of_queue_op_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_queue_op_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_op_failed_error_msg_xid_get(src, &val32);
    of_queue_op_failed_error_msg_xid_set(dst, val32);

    of_queue_op_failed_error_msg_code_get(src, &val16);
    of_queue_op_failed_error_msg_code_set(dst, val16);

    of_queue_op_failed_error_msg_data_get(src, &octets);
    of_queue_op_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_reply_t *
of_queue_stats_reply_OF_VERSION_1_2_dup(
    of_queue_stats_reply_t *src)
{
    of_queue_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_stats_entry_t src_list;
    of_list_queue_stats_entry_t *dst_list;

    if ((dst = of_queue_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_reply_xid_get(src, &val32);
    of_queue_stats_reply_xid_set(dst, val32);

    of_queue_stats_reply_flags_get(src, &val16);
    of_queue_stats_reply_flags_set(dst, val16);

    of_queue_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_stats_reply_entries_set(dst, dst_list);
    of_list_queue_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_request_t *
of_queue_stats_request_OF_VERSION_1_2_dup(
    of_queue_stats_request_t *src)
{
    of_queue_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_request_xid_get(src, &val32);
    of_queue_stats_request_xid_set(dst, val32);

    of_queue_stats_request_flags_get(src, &val16);
    of_queue_stats_request_flags_set(dst, val16);

    of_queue_stats_request_port_no_get(src, &port_no);
    of_queue_stats_request_port_no_set(dst, port_no);

    of_queue_stats_request_queue_id_get(src, &val32);
    of_queue_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_role_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_reply_t *
of_role_reply_OF_VERSION_1_2_dup(
    of_role_reply_t *src)
{
    of_role_reply_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_reply_xid_get(src, &val32);
    of_role_reply_xid_set(dst, val32);

    of_role_reply_role_get(src, &val32);
    of_role_reply_role_set(dst, val32);

    of_role_reply_generation_id_get(src, &val64);
    of_role_reply_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_t *
of_role_request_OF_VERSION_1_2_dup(
    of_role_request_t *src)
{
    of_role_request_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_xid_get(src, &val32);
    of_role_request_xid_set(dst, val32);

    of_role_request_role_get(src, &val32);
    of_role_request_role_set(dst, val32);

    of_role_request_generation_id_get(src, &val64);
    of_role_request_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_failed_error_msg_t *
of_role_request_failed_error_msg_OF_VERSION_1_2_dup(
    of_role_request_failed_error_msg_t *src)
{
    of_role_request_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_role_request_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_failed_error_msg_xid_get(src, &val32);
    of_role_request_failed_error_msg_xid_set(dst, val32);

    of_role_request_failed_error_msg_code_get(src, &val16);
    of_role_request_failed_error_msg_code_set(dst, val16);

    of_role_request_failed_error_msg_data_get(src, &octets);
    of_role_request_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_set_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_set_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_set_config_t *
of_set_config_OF_VERSION_1_2_dup(
    of_set_config_t *src)
{
    of_set_config_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_set_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_set_config_xid_get(src, &val32);
    of_set_config_xid_set(dst, val32);

    of_set_config_flags_get(src, &val16);
    of_set_config_flags_set(dst, val16);

    of_set_config_miss_send_len_get(src, &val16);
    of_set_config_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_switch_config_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_switch_config_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_switch_config_failed_error_msg_t *
of_switch_config_failed_error_msg_OF_VERSION_1_2_dup(
    of_switch_config_failed_error_msg_t *src)
{
    of_switch_config_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_switch_config_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_switch_config_failed_error_msg_xid_get(src, &val32);
    of_switch_config_failed_error_msg_xid_set(dst, val32);

    of_switch_config_failed_error_msg_code_get(src, &val16);
    of_switch_config_failed_error_msg_code_set(dst, val16);

    of_switch_config_failed_error_msg_data_get(src, &octets);
    of_switch_config_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_t *
of_table_mod_OF_VERSION_1_2_dup(
    of_table_mod_t *src)
{
    of_table_mod_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_table_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_xid_get(src, &val32);
    of_table_mod_xid_set(dst, val32);

    of_table_mod_table_id_get(src, &val8);
    of_table_mod_table_id_set(dst, val8);

    of_table_mod_config_get(src, &val32);
    of_table_mod_config_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_failed_error_msg_t *
of_table_mod_failed_error_msg_OF_VERSION_1_2_dup(
    of_table_mod_failed_error_msg_t *src)
{
    of_table_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_failed_error_msg_xid_get(src, &val32);
    of_table_mod_failed_error_msg_xid_set(dst, val32);

    of_table_mod_failed_error_msg_code_get(src, &val16);
    of_table_mod_failed_error_msg_code_set(dst, val16);

    of_table_mod_failed_error_msg_data_get(src, &octets);
    of_table_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_reply_t *
of_table_stats_reply_OF_VERSION_1_2_dup(
    of_table_stats_reply_t *src)
{
    of_table_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_stats_entry_t src_list;
    of_list_table_stats_entry_t *dst_list;

    if ((dst = of_table_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_reply_xid_get(src, &val32);
    of_table_stats_reply_xid_set(dst, val32);

    of_table_stats_reply_flags_get(src, &val16);
    of_table_stats_reply_flags_set(dst, val16);

    of_table_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_stats_entry_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_table_stats_reply_delete(dst);
        return NULL;
    }
    of_table_stats_reply_entries_set(dst, dst_list);
    of_list_table_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_request_t *
of_table_stats_request_OF_VERSION_1_2_dup(
    of_table_stats_request_t *src)
{
    of_table_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_request_xid_get(src, &val32);
    of_table_stats_request_xid_set(dst, val32);

    of_table_stats_request_flags_get(src, &val16);
    of_table_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate a super class object of type of_action
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_NICIRA_DEC_TTL) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_MPLS) {
        return of_action_pop_mpls_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_IN) {
        return of_action_copy_ttl_in_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_OUTPUT) {
        return of_action_output_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_FIELD) {
        return of_action_set_field_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_QUEUE) {
        return of_action_set_queue_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_NW_TTL) {
        return of_action_dec_nw_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_VLAN) {
        return of_action_pop_vlan_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_VLAN) {
        return of_action_push_vlan_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_MPLS_TTL) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_SET_TUNNEL_DST) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TTL) {
        return of_action_set_nw_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_MIRROR) {
        return of_action_bsn_mirror_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_GROUP) {
        return of_action_group_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_MPLS) {
        return of_action_push_mpls_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_OUT) {
        return of_action_copy_ttl_out_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_CHECKSUM) {
        return of_action_bsn_checksum_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_MPLS_TTL) {
        return of_action_set_mpls_ttl_OF_VERSION_1_2_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_checksum_t *
of_action_bsn_checksum_OF_VERSION_1_2_dup(
    of_action_bsn_checksum_t *src)
{
    of_action_bsn_checksum_t *dst;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_action_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_checksum_experimenter_get(src, &val32);
    of_action_bsn_checksum_experimenter_set(dst, val32);

    of_action_bsn_checksum_subtype_get(src, &val32);
    of_action_bsn_checksum_subtype_set(dst, val32);

    of_action_bsn_checksum_checksum_get(src, &checksum_128);
    of_action_bsn_checksum_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_mirror_t *
of_action_bsn_mirror_OF_VERSION_1_2_dup(
    of_action_bsn_mirror_t *src)
{
    of_action_bsn_mirror_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_action_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_mirror_experimenter_get(src, &val32);
    of_action_bsn_mirror_experimenter_set(dst, val32);

    of_action_bsn_mirror_subtype_get(src, &val32);
    of_action_bsn_mirror_subtype_set(dst, val32);

    of_action_bsn_mirror_dest_port_get(src, &val32);
    of_action_bsn_mirror_dest_port_set(dst, val32);

    of_action_bsn_mirror_vlan_tag_get(src, &val32);
    of_action_bsn_mirror_vlan_tag_set(dst, val32);

    of_action_bsn_mirror_copy_stage_get(src, &val8);
    of_action_bsn_mirror_copy_stage_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_set_tunnel_dst_t *
of_action_bsn_set_tunnel_dst_OF_VERSION_1_2_dup(
    of_action_bsn_set_tunnel_dst_t *src)
{
    of_action_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_bsn_set_tunnel_dst_subtype_set(dst, val32);

    of_action_bsn_set_tunnel_dst_dst_get(src, &val32);
    of_action_bsn_set_tunnel_dst_dst_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_in_t *
of_action_copy_ttl_in_OF_VERSION_1_2_dup(
    of_action_copy_ttl_in_t *src)
{
    of_action_copy_ttl_in_t *dst;

    if ((dst = of_action_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_out_t *
of_action_copy_ttl_out_OF_VERSION_1_2_dup(
    of_action_copy_ttl_out_t *src)
{
    of_action_copy_ttl_out_t *dst;

    if ((dst = of_action_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_mpls_ttl_t *
of_action_dec_mpls_ttl_OF_VERSION_1_2_dup(
    of_action_dec_mpls_ttl_t *src)
{
    of_action_dec_mpls_ttl_t *dst;

    if ((dst = of_action_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_nw_ttl_t *
of_action_dec_nw_ttl_OF_VERSION_1_2_dup(
    of_action_dec_nw_ttl_t *src)
{
    of_action_dec_nw_ttl_t *dst;

    if ((dst = of_action_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_group_t *
of_action_group_OF_VERSION_1_2_dup(
    of_action_group_t *src)
{
    of_action_group_t *dst;
    uint32_t val32;

    if ((dst = of_action_group_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_group_group_id_get(src, &val32);
    of_action_group_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_nicira_dec_ttl_t *
of_action_nicira_dec_ttl_OF_VERSION_1_2_dup(
    of_action_nicira_dec_ttl_t *src)
{
    of_action_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_output_t *
of_action_output_OF_VERSION_1_2_dup(
    of_action_output_t *src)
{
    of_action_output_t *dst;
    of_port_no_t port_no;
    uint16_t val16;

    if ((dst = of_action_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_output_port_get(src, &port_no);
    of_action_output_port_set(dst, port_no);

    of_action_output_max_len_get(src, &val16);
    of_action_output_max_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_mpls_t *
of_action_pop_mpls_OF_VERSION_1_2_dup(
    of_action_pop_mpls_t *src)
{
    of_action_pop_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_pop_mpls_ethertype_get(src, &val16);
    of_action_pop_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_vlan_t *
of_action_pop_vlan_OF_VERSION_1_2_dup(
    of_action_pop_vlan_t *src)
{
    of_action_pop_vlan_t *dst;

    if ((dst = of_action_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_mpls_t *
of_action_push_mpls_OF_VERSION_1_2_dup(
    of_action_push_mpls_t *src)
{
    of_action_push_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_mpls_ethertype_get(src, &val16);
    of_action_push_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_vlan_t *
of_action_push_vlan_OF_VERSION_1_2_dup(
    of_action_push_vlan_t *src)
{
    of_action_push_vlan_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_vlan_ethertype_get(src, &val16);
    of_action_push_vlan_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_field_t *
of_action_set_field_OF_VERSION_1_2_dup(
    of_action_set_field_t *src)
{
    of_action_set_field_t *dst;

    of_oxm_eth_type_t src_oxm;
    of_oxm_eth_type_t *dst_oxm;

    if ((dst = of_action_set_field_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_field_field_bind(
        src, &src_oxm);
    dst_oxm = of_oxm_eth_type_OF_VERSION_1_2_dup(&src_oxm);
    if (dst_oxm == NULL) {
        of_action_set_field_delete(dst);
        return NULL;
    }
    of_action_set_field_field_set(dst, dst_oxm);
    of_oxm_eth_type_delete(dst_oxm);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_ttl_t *
of_action_set_mpls_ttl_OF_VERSION_1_2_dup(
    of_action_set_mpls_ttl_t *src)
{
    of_action_set_mpls_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_ttl_mpls_ttl_get(src, &val8);
    of_action_set_mpls_ttl_mpls_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_ttl_t *
of_action_set_nw_ttl_OF_VERSION_1_2_dup(
    of_action_set_nw_ttl_t *src)
{
    of_action_set_nw_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_ttl_nw_ttl_get(src, &val8);
    of_action_set_nw_ttl_nw_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_queue_t *
of_action_set_queue_OF_VERSION_1_2_dup(
    of_action_set_queue_t *src)
{
    of_action_set_queue_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_queue_queue_id_get(src, &val32);
    of_action_set_queue_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_interface_t *
of_bsn_interface_OF_VERSION_1_2_dup(
    of_bsn_interface_t *src)
{
    of_bsn_interface_t *dst;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_interface_hw_addr_get(src, &mac_addr);
    of_bsn_interface_hw_addr_set(dst, mac_addr);

    of_bsn_interface_name_get(src, &port_name);
    of_bsn_interface_name_set(dst, port_name);

    of_bsn_interface_ipv4_addr_get(src, &ipv4);
    of_bsn_interface_ipv4_addr_set(dst, ipv4);

    of_bsn_interface_ipv4_netmask_get(src, &ipv4);
    of_bsn_interface_ipv4_netmask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_vport
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_vport_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_VPORT_Q_IN_Q) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_BSN_VPORT_L2GRE) {
        return of_bsn_vport_l2gre_OF_VERSION_1_2_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_vport_l2gre
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_l2gre.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_l2gre_t *
of_bsn_vport_l2gre_OF_VERSION_1_2_dup(
    of_bsn_vport_l2gre_t *src)
{
    of_bsn_vport_l2gre_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_ipv4_t ipv4;
    uint8_t val8;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_l2gre_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_l2gre_flags_get(src, &val32);
    of_bsn_vport_l2gre_flags_set(dst, val32);

    of_bsn_vport_l2gre_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_loopback_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_loopback_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_local_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_local_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_nh_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_nh_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_src_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_src_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dst_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_dst_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dscp_get(src, &val8);
    of_bsn_vport_l2gre_dscp_set(dst, val8);

    of_bsn_vport_l2gre_ttl_get(src, &val8);
    of_bsn_vport_l2gre_ttl_set(dst, val8);

    of_bsn_vport_l2gre_vpn_get(src, &val32);
    of_bsn_vport_l2gre_vpn_set(dst, val32);

    of_bsn_vport_l2gre_rate_limit_get(src, &val32);
    of_bsn_vport_l2gre_rate_limit_set(dst, val32);

    of_bsn_vport_l2gre_if_name_get(src, &port_name);
    of_bsn_vport_l2gre_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vport_q_in_q
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_q_in_q.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_q_in_q_t *
of_bsn_vport_q_in_q_OF_VERSION_1_2_dup(
    of_bsn_vport_q_in_q_t *src)
{
    of_bsn_vport_q_in_q_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_q_in_q_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_q_in_q_port_no_get(src, &val32);
    of_bsn_vport_q_in_q_port_no_set(dst, val32);

    of_bsn_vport_q_in_q_ingress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_ingress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_egress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_egress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_egress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_egress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_if_name_get(src, &port_name);
    of_bsn_vport_q_in_q_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_t *
of_bucket_OF_VERSION_1_2_dup(
    of_bucket_t *src)
{
    of_bucket_t *dst;
    uint16_t val16;
    of_port_no_t port_no;
    uint32_t val32;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_weight_get(src, &val16);
    of_bucket_weight_set(dst, val16);

    of_bucket_watch_port_get(src, &port_no);
    of_bucket_watch_port_set(dst, port_no);

    of_bucket_watch_group_get(src, &val32);
    of_bucket_watch_group_set(dst, val32);

    of_bucket_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_bucket_delete(dst);
        return NULL;
    }
    of_bucket_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_counter_t *
of_bucket_counter_OF_VERSION_1_2_dup(
    of_bucket_counter_t *src)
{
    of_bucket_counter_t *dst;
    uint64_t val64;

    if ((dst = of_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_counter_packet_count_get(src, &val64);
    of_bucket_counter_packet_count_set(dst, val64);

    of_bucket_counter_byte_count_get(src, &val64);
    of_bucket_counter_byte_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_entry_t *
of_flow_stats_entry_OF_VERSION_1_2_dup(
    of_flow_stats_entry_t *src)
{
    of_flow_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_entry_table_id_get(src, &val8);
    of_flow_stats_entry_table_id_set(dst, val8);

    of_flow_stats_entry_duration_sec_get(src, &val32);
    of_flow_stats_entry_duration_sec_set(dst, val32);

    of_flow_stats_entry_duration_nsec_get(src, &val32);
    of_flow_stats_entry_duration_nsec_set(dst, val32);

    of_flow_stats_entry_priority_get(src, &val16);
    of_flow_stats_entry_priority_set(dst, val16);

    of_flow_stats_entry_idle_timeout_get(src, &val16);
    of_flow_stats_entry_idle_timeout_set(dst, val16);

    of_flow_stats_entry_hard_timeout_get(src, &val16);
    of_flow_stats_entry_hard_timeout_set(dst, val16);

    of_flow_stats_entry_cookie_get(src, &val64);
    of_flow_stats_entry_cookie_set(dst, val64);

    of_flow_stats_entry_packet_count_get(src, &val64);
    of_flow_stats_entry_packet_count_set(dst, val64);

    of_flow_stats_entry_byte_count_get(src, &val64);
    of_flow_stats_entry_byte_count_set(dst, val64);

    of_flow_stats_entry_match_get(src, &match);
    of_flow_stats_entry_match_set(dst, &match);

    of_flow_stats_entry_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_entry_delete(dst);
        return NULL;
    }
    of_flow_stats_entry_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_entry_t *
of_group_desc_stats_entry_OF_VERSION_1_2_dup(
    of_group_desc_stats_entry_t *src)
{
    of_group_desc_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_entry_group_type_get(src, &val8);
    of_group_desc_stats_entry_group_type_set(dst, val8);

    of_group_desc_stats_entry_group_id_get(src, &val32);
    of_group_desc_stats_entry_group_id_set(dst, val32);

    of_group_desc_stats_entry_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_group_desc_stats_entry_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_entry_t *
of_group_stats_entry_OF_VERSION_1_2_dup(
    of_group_stats_entry_t *src)
{
    of_group_stats_entry_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_bucket_counter_t src_list;
    of_list_bucket_counter_t *dst_list;

    if ((dst = of_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_entry_group_id_get(src, &val32);
    of_group_stats_entry_group_id_set(dst, val32);

    of_group_stats_entry_ref_count_get(src, &val32);
    of_group_stats_entry_ref_count_set(dst, val32);

    of_group_stats_entry_packet_count_get(src, &val64);
    of_group_stats_entry_packet_count_set(dst, val64);

    of_group_stats_entry_byte_count_get(src, &val64);
    of_group_stats_entry_byte_count_set(dst, val64);

    of_group_stats_entry_bucket_stats_bind(
        src, &src_list);
    dst_list = of_list_bucket_counter_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_entry_delete(dst);
        return NULL;
    }
    of_group_stats_entry_bucket_stats_set(dst, dst_list);
    of_list_bucket_counter_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_APPLY_ACTIONS) {
        return of_instruction_apply_actions_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_CLEAR_ACTIONS) {
        return of_instruction_clear_actions_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_GOTO_TABLE) {
        return of_instruction_goto_table_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_METADATA) {
        return of_instruction_write_metadata_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_ACTIONS) {
        return of_instruction_write_actions_OF_VERSION_1_2_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_apply_actions_t *
of_instruction_apply_actions_OF_VERSION_1_2_dup(
    of_instruction_apply_actions_t *src)
{
    of_instruction_apply_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_apply_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_apply_actions_delete(dst);
        return NULL;
    }
    of_instruction_apply_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_clear_actions_t *
of_instruction_clear_actions_OF_VERSION_1_2_dup(
    of_instruction_clear_actions_t *src)
{
    of_instruction_clear_actions_t *dst;

    if ((dst = of_instruction_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_goto_table_t *
of_instruction_goto_table_OF_VERSION_1_2_dup(
    of_instruction_goto_table_t *src)
{
    of_instruction_goto_table_t *dst;
    uint8_t val8;

    if ((dst = of_instruction_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_goto_table_table_id_get(src, &val8);
    of_instruction_goto_table_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_actions_t *
of_instruction_write_actions_OF_VERSION_1_2_dup(
    of_instruction_write_actions_t *src)
{
    of_instruction_write_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_write_actions_delete(dst);
        return NULL;
    }
    of_instruction_write_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_metadata_t *
of_instruction_write_metadata_OF_VERSION_1_2_dup(
    of_instruction_write_metadata_t *src)
{
    of_instruction_write_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_instruction_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_metadata_metadata_get(src, &val64);
    of_instruction_write_metadata_metadata_set(dst, val64);

    of_instruction_write_metadata_metadata_mask_get(src, &val64);
    of_instruction_write_metadata_metadata_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_match_v3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_match_v3.
 *
 * The caller is responsible for deleting the returned value
 */
of_match_v3_t *
of_match_v3_OF_VERSION_1_2_dup(
    of_match_v3_t *src)
{
    of_match_v3_t *dst;

    of_list_oxm_t src_list;
    of_list_oxm_t *dst_list;

    if ((dst = of_match_v3_new(src->version)) == NULL) {
        return NULL;
    }

    of_match_v3_oxm_list_bind(
        src, &src_list);
    dst_list = of_list_oxm_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_match_v3_delete(dst);
        return NULL;
    }
    of_match_v3_oxm_list_set(dst, dst_list);
    of_list_oxm_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_oxm
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_oxm_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    if (src->object_id == OF_OXM_IN_PORT_MASKED) {
        return of_oxm_in_port_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION_MASKED) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_DST_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT) {
        return of_oxm_in_phy_port_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST_MASKED) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE_MASKED) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT_MASKED) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE) {
        return of_oxm_icmpv4_code_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2_MASKED) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4_MASKED) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST) {
        return of_oxm_udp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE) {
        return of_oxm_eth_type_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST) {
        return of_oxm_eth_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP) {
        return of_oxm_vlan_pcp_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET_MASKED) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP) {
        return of_oxm_arp_op_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE) {
        return of_oxm_icmpv4_type_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL_MASKED) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE) {
        return of_oxm_icmpv6_type_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA_MASKED) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST_MASKED) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP_MASKED) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA) {
        return of_oxm_arp_sha_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE_MASKED) {
        return of_oxm_eth_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST) {
        return of_oxm_tcp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO_MASKED) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN_MASKED) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA_MASKED) {
        return of_oxm_metadata_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_GLOBAL_VRF_ALLOWED_MASKED) {
        return of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_GLOBAL_VRF_ALLOWED) {
        return of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA_MASKED) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC) {
        return of_oxm_sctp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512_MASKED) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC) {
        return of_oxm_udp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_DST_CLASS_ID) {
        return of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST_MASKED) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST_MASKED) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA_MASKED) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST) {
        return of_oxm_sctp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5_MASKED) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE_MASKED) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL_MASKED) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE_MASKED) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS_MASKED) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST) {
        return of_oxm_ipv4_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128_MASKED) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC_MASKED) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC) {
        return of_oxm_ipv4_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5) {
        return of_oxm_bsn_udf5_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4) {
        return of_oxm_bsn_udf4_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7) {
        return of_oxm_bsn_udf7_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS_MASKED) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1) {
        return of_oxm_bsn_udf1_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0) {
        return of_oxm_bsn_udf0_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3) {
        return of_oxm_bsn_udf3_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2) {
        return of_oxm_bsn_udf2_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE) {
        return of_oxm_icmpv6_code_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP_MASKED) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL) {
        return of_oxm_mpls_label_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6) {
        return of_oxm_bsn_udf6_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PORT) {
        return of_oxm_in_port_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7_MASKED) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1_MASKED) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF_MASKED) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC) {
        return of_oxm_tcp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC_MASKED) {
        return of_oxm_udp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN) {
        return of_oxm_ip_ecn_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC) {
        return of_oxm_eth_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST) {
        return of_oxm_ipv6_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC_MASKED) {
        return of_oxm_eth_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF) {
        return of_oxm_bsn_vrf_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3_MASKED) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC_MASKED) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP) {
        return of_oxm_ip_dscp_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID_MASKED) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL_MASKED) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC_MASKED) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST_MASKED) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6_MASKED) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC_MASKED) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID_MASKED) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT_MASKED) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO) {
        return of_oxm_ip_proto_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC) {
        return of_oxm_ipv6_src_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST_MASKED) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA) {
        return of_oxm_arp_spa_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP_MASKED) {
        return of_oxm_arp_op_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC_MASKED) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID) {
        return of_oxm_vlan_vid_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE_MASKED) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC_MASKED) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA) {
        return of_oxm_metadata_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0_MASKED) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA_MASKED) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA) {
        return of_oxm_arp_tha_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC) {
        return of_oxm_mpls_tc_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA) {
        return of_oxm_arp_tpa_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST_MASKED) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL_MASKED) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_2_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_oxm_arp_op
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_t *
of_oxm_arp_op_OF_VERSION_1_2_dup(
    of_oxm_arp_op_t *src)
{
    of_oxm_arp_op_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_value_get(src, &val16);
    of_oxm_arp_op_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_op_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_masked_t *
of_oxm_arp_op_masked_OF_VERSION_1_2_dup(
    of_oxm_arp_op_masked_t *src)
{
    of_oxm_arp_op_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_masked_value_get(src, &val16);
    of_oxm_arp_op_masked_value_set(dst, val16);

    of_oxm_arp_op_masked_value_mask_get(src, &val16);
    of_oxm_arp_op_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_t *
of_oxm_arp_sha_OF_VERSION_1_2_dup(
    of_oxm_arp_sha_t *src)
{
    of_oxm_arp_sha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_value_get(src, &mac_addr);
    of_oxm_arp_sha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_masked_t *
of_oxm_arp_sha_masked_OF_VERSION_1_2_dup(
    of_oxm_arp_sha_masked_t *src)
{
    of_oxm_arp_sha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_masked_value_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_set(dst, mac_addr);

    of_oxm_arp_sha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_t *
of_oxm_arp_spa_OF_VERSION_1_2_dup(
    of_oxm_arp_spa_t *src)
{
    of_oxm_arp_spa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_value_get(src, &val32);
    of_oxm_arp_spa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_masked_t *
of_oxm_arp_spa_masked_OF_VERSION_1_2_dup(
    of_oxm_arp_spa_masked_t *src)
{
    of_oxm_arp_spa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_masked_value_get(src, &val32);
    of_oxm_arp_spa_masked_value_set(dst, val32);

    of_oxm_arp_spa_masked_value_mask_get(src, &val32);
    of_oxm_arp_spa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_t *
of_oxm_arp_tha_OF_VERSION_1_2_dup(
    of_oxm_arp_tha_t *src)
{
    of_oxm_arp_tha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_value_get(src, &mac_addr);
    of_oxm_arp_tha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_masked_t *
of_oxm_arp_tha_masked_OF_VERSION_1_2_dup(
    of_oxm_arp_tha_masked_t *src)
{
    of_oxm_arp_tha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_masked_value_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_set(dst, mac_addr);

    of_oxm_arp_tha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_t *
of_oxm_arp_tpa_OF_VERSION_1_2_dup(
    of_oxm_arp_tpa_t *src)
{
    of_oxm_arp_tpa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_value_get(src, &val32);
    of_oxm_arp_tpa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_masked_t *
of_oxm_arp_tpa_masked_OF_VERSION_1_2_dup(
    of_oxm_arp_tpa_masked_t *src)
{
    of_oxm_arp_tpa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_masked_value_get(src, &val32);
    of_oxm_arp_tpa_masked_value_set(dst, val32);

    of_oxm_arp_tpa_masked_value_mask_get(src, &val32);
    of_oxm_arp_tpa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_t *
of_oxm_bsn_egr_port_group_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_egr_port_group_id_t *src)
{
    of_oxm_bsn_egr_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_masked_t *
of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_egr_port_group_id_masked_t *src)
{
    of_oxm_bsn_egr_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_egr_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_global_vrf_allowed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_global_vrf_allowed.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_global_vrf_allowed_t *
of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_2_dup(
    of_oxm_bsn_global_vrf_allowed_t *src)
{
    of_oxm_bsn_global_vrf_allowed_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_global_vrf_allowed_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_global_vrf_allowed_value_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_global_vrf_allowed_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_global_vrf_allowed_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_global_vrf_allowed_masked_t *
of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_global_vrf_allowed_masked_t *src)
{
    of_oxm_bsn_global_vrf_allowed_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_global_vrf_allowed_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_global_vrf_allowed_masked_value_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_masked_value_set(dst, val8);

    of_oxm_bsn_global_vrf_allowed_masked_value_mask_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_t *
of_oxm_bsn_in_ports_128_OF_VERSION_1_2_dup(
    of_oxm_bsn_in_ports_128_t *src)
{
    of_oxm_bsn_in_ports_128_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_value_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_masked_t *
of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_in_ports_128_masked_t *src)
{
    of_oxm_bsn_in_ports_128_masked_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_masked_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_set(dst, bitmap_128);

    of_oxm_bsn_in_ports_128_masked_value_mask_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_mask_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_t *
of_oxm_bsn_in_ports_512_OF_VERSION_1_2_dup(
    of_oxm_bsn_in_ports_512_t *src)
{
    of_oxm_bsn_in_ports_512_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_value_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_masked_t *
of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_in_ports_512_masked_t *src)
{
    of_oxm_bsn_in_ports_512_masked_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_masked_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_set(dst, bitmap_512);

    of_oxm_bsn_in_ports_512_masked_value_mask_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_mask_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_t *
of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_ingress_port_group_id_t *src)
{
    of_oxm_bsn_ingress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_masked_t *
of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_ingress_port_group_id_masked_t *src)
{
    of_oxm_bsn_ingress_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_ingress_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_t *
of_oxm_bsn_ip_fragmentation_OF_VERSION_1_2_dup(
    of_oxm_bsn_ip_fragmentation_t *src)
{
    of_oxm_bsn_ip_fragmentation_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_masked_t *
of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_ip_fragmentation_masked_t *src)
{
    of_oxm_bsn_ip_fragmentation_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_masked_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_set(dst, val8);

    of_oxm_bsn_ip_fragmentation_masked_value_mask_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_t *
of_oxm_bsn_l2_cache_hit_OF_VERSION_1_2_dup(
    of_oxm_bsn_l2_cache_hit_t *src)
{
    of_oxm_bsn_l2_cache_hit_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_masked_t *
of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_l2_cache_hit_masked_t *src)
{
    of_oxm_bsn_l2_cache_hit_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_masked_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_set(dst, val8);

    of_oxm_bsn_l2_cache_hit_masked_value_mask_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_dst_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_dst_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_dst_class_id_t *
of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_dst_class_id_t *src)
{
    of_oxm_bsn_l3_dst_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_dst_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_dst_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_dst_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_dst_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_dst_class_id_masked_t *
of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_dst_class_id_masked_t *src)
{
    of_oxm_bsn_l3_dst_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_dst_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_dst_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_dst_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_t *
of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_interface_class_id_t *src)
{
    of_oxm_bsn_l3_interface_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_masked_t *
of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_interface_class_id_masked_t *src)
{
    of_oxm_bsn_l3_interface_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_interface_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_t *
of_oxm_bsn_l3_src_class_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_src_class_id_t *src)
{
    of_oxm_bsn_l3_src_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_masked_t *
of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_l3_src_class_id_masked_t *src)
{
    of_oxm_bsn_l3_src_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_src_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_t *
of_oxm_bsn_lag_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_lag_id_t *src)
{
    of_oxm_bsn_lag_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_value_get(src, &val32);
    of_oxm_bsn_lag_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_masked_t *
of_oxm_bsn_lag_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_lag_id_masked_t *src)
{
    of_oxm_bsn_lag_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_masked_value_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_set(dst, val32);

    of_oxm_bsn_lag_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_t *
of_oxm_bsn_tcp_flags_OF_VERSION_1_2_dup(
    of_oxm_bsn_tcp_flags_t *src)
{
    of_oxm_bsn_tcp_flags_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_masked_t *
of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_tcp_flags_masked_t *src)
{
    of_oxm_bsn_tcp_flags_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_masked_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_set(dst, val16);

    of_oxm_bsn_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_t *
of_oxm_bsn_udf0_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf0_t *src)
{
    of_oxm_bsn_udf0_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_value_get(src, &val32);
    of_oxm_bsn_udf0_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_masked_t *
of_oxm_bsn_udf0_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf0_masked_t *src)
{
    of_oxm_bsn_udf0_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_masked_value_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_set(dst, val32);

    of_oxm_bsn_udf0_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_t *
of_oxm_bsn_udf1_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf1_t *src)
{
    of_oxm_bsn_udf1_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_value_get(src, &val32);
    of_oxm_bsn_udf1_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_masked_t *
of_oxm_bsn_udf1_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf1_masked_t *src)
{
    of_oxm_bsn_udf1_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_masked_value_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_set(dst, val32);

    of_oxm_bsn_udf1_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_t *
of_oxm_bsn_udf2_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf2_t *src)
{
    of_oxm_bsn_udf2_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_value_get(src, &val32);
    of_oxm_bsn_udf2_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_masked_t *
of_oxm_bsn_udf2_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf2_masked_t *src)
{
    of_oxm_bsn_udf2_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_masked_value_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_set(dst, val32);

    of_oxm_bsn_udf2_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_t *
of_oxm_bsn_udf3_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf3_t *src)
{
    of_oxm_bsn_udf3_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_value_get(src, &val32);
    of_oxm_bsn_udf3_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_masked_t *
of_oxm_bsn_udf3_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf3_masked_t *src)
{
    of_oxm_bsn_udf3_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_masked_value_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_set(dst, val32);

    of_oxm_bsn_udf3_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_t *
of_oxm_bsn_udf4_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf4_t *src)
{
    of_oxm_bsn_udf4_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_value_get(src, &val32);
    of_oxm_bsn_udf4_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_masked_t *
of_oxm_bsn_udf4_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf4_masked_t *src)
{
    of_oxm_bsn_udf4_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_masked_value_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_set(dst, val32);

    of_oxm_bsn_udf4_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_t *
of_oxm_bsn_udf5_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf5_t *src)
{
    of_oxm_bsn_udf5_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_value_get(src, &val32);
    of_oxm_bsn_udf5_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_masked_t *
of_oxm_bsn_udf5_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf5_masked_t *src)
{
    of_oxm_bsn_udf5_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_masked_value_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_set(dst, val32);

    of_oxm_bsn_udf5_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_t *
of_oxm_bsn_udf6_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf6_t *src)
{
    of_oxm_bsn_udf6_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_value_get(src, &val32);
    of_oxm_bsn_udf6_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_masked_t *
of_oxm_bsn_udf6_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf6_masked_t *src)
{
    of_oxm_bsn_udf6_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_masked_value_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_set(dst, val32);

    of_oxm_bsn_udf6_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_t *
of_oxm_bsn_udf7_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf7_t *src)
{
    of_oxm_bsn_udf7_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_value_get(src, &val32);
    of_oxm_bsn_udf7_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_masked_t *
of_oxm_bsn_udf7_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_udf7_masked_t *src)
{
    of_oxm_bsn_udf7_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_masked_value_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_set(dst, val32);

    of_oxm_bsn_udf7_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_t *
of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_2_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_masked_t *
of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_t *
of_oxm_bsn_vrf_OF_VERSION_1_2_dup(
    of_oxm_bsn_vrf_t *src)
{
    of_oxm_bsn_vrf_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_value_get(src, &val32);
    of_oxm_bsn_vrf_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_masked_t *
of_oxm_bsn_vrf_masked_OF_VERSION_1_2_dup(
    of_oxm_bsn_vrf_masked_t *src)
{
    of_oxm_bsn_vrf_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_masked_value_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_set(dst, val32);

    of_oxm_bsn_vrf_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_t *
of_oxm_eth_dst_OF_VERSION_1_2_dup(
    of_oxm_eth_dst_t *src)
{
    of_oxm_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_value_get(src, &mac_addr);
    of_oxm_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_masked_t *
of_oxm_eth_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_eth_dst_masked_t *src)
{
    of_oxm_eth_dst_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_masked_value_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_set(dst, mac_addr);

    of_oxm_eth_dst_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_t *
of_oxm_eth_src_OF_VERSION_1_2_dup(
    of_oxm_eth_src_t *src)
{
    of_oxm_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_value_get(src, &mac_addr);
    of_oxm_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_masked_t *
of_oxm_eth_src_masked_OF_VERSION_1_2_dup(
    of_oxm_eth_src_masked_t *src)
{
    of_oxm_eth_src_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_masked_value_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_set(dst, mac_addr);

    of_oxm_eth_src_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_t *
of_oxm_eth_type_OF_VERSION_1_2_dup(
    of_oxm_eth_type_t *src)
{
    of_oxm_eth_type_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_value_get(src, &val16);
    of_oxm_eth_type_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_masked_t *
of_oxm_eth_type_masked_OF_VERSION_1_2_dup(
    of_oxm_eth_type_masked_t *src)
{
    of_oxm_eth_type_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_masked_value_get(src, &val16);
    of_oxm_eth_type_masked_value_set(dst, val16);

    of_oxm_eth_type_masked_value_mask_get(src, &val16);
    of_oxm_eth_type_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_t *
of_oxm_icmpv4_code_OF_VERSION_1_2_dup(
    of_oxm_icmpv4_code_t *src)
{
    of_oxm_icmpv4_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_value_get(src, &val8);
    of_oxm_icmpv4_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_masked_t *
of_oxm_icmpv4_code_masked_OF_VERSION_1_2_dup(
    of_oxm_icmpv4_code_masked_t *src)
{
    of_oxm_icmpv4_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_masked_value_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_set(dst, val8);

    of_oxm_icmpv4_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_t *
of_oxm_icmpv4_type_OF_VERSION_1_2_dup(
    of_oxm_icmpv4_type_t *src)
{
    of_oxm_icmpv4_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_value_get(src, &val8);
    of_oxm_icmpv4_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_masked_t *
of_oxm_icmpv4_type_masked_OF_VERSION_1_2_dup(
    of_oxm_icmpv4_type_masked_t *src)
{
    of_oxm_icmpv4_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_masked_value_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_set(dst, val8);

    of_oxm_icmpv4_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_t *
of_oxm_icmpv6_code_OF_VERSION_1_2_dup(
    of_oxm_icmpv6_code_t *src)
{
    of_oxm_icmpv6_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_value_get(src, &val8);
    of_oxm_icmpv6_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_masked_t *
of_oxm_icmpv6_code_masked_OF_VERSION_1_2_dup(
    of_oxm_icmpv6_code_masked_t *src)
{
    of_oxm_icmpv6_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_masked_value_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_set(dst, val8);

    of_oxm_icmpv6_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_t *
of_oxm_icmpv6_type_OF_VERSION_1_2_dup(
    of_oxm_icmpv6_type_t *src)
{
    of_oxm_icmpv6_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_value_get(src, &val8);
    of_oxm_icmpv6_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_masked_t *
of_oxm_icmpv6_type_masked_OF_VERSION_1_2_dup(
    of_oxm_icmpv6_type_masked_t *src)
{
    of_oxm_icmpv6_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_masked_value_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_set(dst, val8);

    of_oxm_icmpv6_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_t *
of_oxm_in_phy_port_OF_VERSION_1_2_dup(
    of_oxm_in_phy_port_t *src)
{
    of_oxm_in_phy_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_value_get(src, &port_no);
    of_oxm_in_phy_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_masked_t *
of_oxm_in_phy_port_masked_OF_VERSION_1_2_dup(
    of_oxm_in_phy_port_masked_t *src)
{
    of_oxm_in_phy_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_masked_value_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_set(dst, port_no);

    of_oxm_in_phy_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_t *
of_oxm_in_port_OF_VERSION_1_2_dup(
    of_oxm_in_port_t *src)
{
    of_oxm_in_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_value_get(src, &port_no);
    of_oxm_in_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_masked_t *
of_oxm_in_port_masked_OF_VERSION_1_2_dup(
    of_oxm_in_port_masked_t *src)
{
    of_oxm_in_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_masked_value_get(src, &port_no);
    of_oxm_in_port_masked_value_set(dst, port_no);

    of_oxm_in_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_t *
of_oxm_ip_dscp_OF_VERSION_1_2_dup(
    of_oxm_ip_dscp_t *src)
{
    of_oxm_ip_dscp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_value_get(src, &val8);
    of_oxm_ip_dscp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_masked_t *
of_oxm_ip_dscp_masked_OF_VERSION_1_2_dup(
    of_oxm_ip_dscp_masked_t *src)
{
    of_oxm_ip_dscp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_masked_value_get(src, &val8);
    of_oxm_ip_dscp_masked_value_set(dst, val8);

    of_oxm_ip_dscp_masked_value_mask_get(src, &val8);
    of_oxm_ip_dscp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_t *
of_oxm_ip_ecn_OF_VERSION_1_2_dup(
    of_oxm_ip_ecn_t *src)
{
    of_oxm_ip_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_value_get(src, &val8);
    of_oxm_ip_ecn_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_masked_t *
of_oxm_ip_ecn_masked_OF_VERSION_1_2_dup(
    of_oxm_ip_ecn_masked_t *src)
{
    of_oxm_ip_ecn_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_masked_value_get(src, &val8);
    of_oxm_ip_ecn_masked_value_set(dst, val8);

    of_oxm_ip_ecn_masked_value_mask_get(src, &val8);
    of_oxm_ip_ecn_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_t *
of_oxm_ip_proto_OF_VERSION_1_2_dup(
    of_oxm_ip_proto_t *src)
{
    of_oxm_ip_proto_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_value_get(src, &val8);
    of_oxm_ip_proto_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_masked_t *
of_oxm_ip_proto_masked_OF_VERSION_1_2_dup(
    of_oxm_ip_proto_masked_t *src)
{
    of_oxm_ip_proto_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_masked_value_get(src, &val8);
    of_oxm_ip_proto_masked_value_set(dst, val8);

    of_oxm_ip_proto_masked_value_mask_get(src, &val8);
    of_oxm_ip_proto_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_t *
of_oxm_ipv4_dst_OF_VERSION_1_2_dup(
    of_oxm_ipv4_dst_t *src)
{
    of_oxm_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_value_get(src, &ipv4);
    of_oxm_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_masked_t *
of_oxm_ipv4_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv4_dst_masked_t *src)
{
    of_oxm_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_t *
of_oxm_ipv4_src_OF_VERSION_1_2_dup(
    of_oxm_ipv4_src_t *src)
{
    of_oxm_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_value_get(src, &ipv4);
    of_oxm_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_masked_t *
of_oxm_ipv4_src_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv4_src_masked_t *src)
{
    of_oxm_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_t *
of_oxm_ipv6_dst_OF_VERSION_1_2_dup(
    of_oxm_ipv6_dst_t *src)
{
    of_oxm_ipv6_dst_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_value_get(src, &ipv6);
    of_oxm_ipv6_dst_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_masked_t *
of_oxm_ipv6_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_dst_masked_t *src)
{
    of_oxm_ipv6_dst_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_masked_value_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_set(dst, ipv6);

    of_oxm_ipv6_dst_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_t *
of_oxm_ipv6_flabel_OF_VERSION_1_2_dup(
    of_oxm_ipv6_flabel_t *src)
{
    of_oxm_ipv6_flabel_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_value_get(src, &val32);
    of_oxm_ipv6_flabel_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_masked_t *
of_oxm_ipv6_flabel_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_flabel_masked_t *src)
{
    of_oxm_ipv6_flabel_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_masked_value_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_set(dst, val32);

    of_oxm_ipv6_flabel_masked_value_mask_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_t *
of_oxm_ipv6_nd_sll_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_sll_t *src)
{
    of_oxm_ipv6_nd_sll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_masked_t *
of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_sll_masked_t *src)
{
    of_oxm_ipv6_nd_sll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_sll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_t *
of_oxm_ipv6_nd_target_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_target_t *src)
{
    of_oxm_ipv6_nd_target_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_masked_t *
of_oxm_ipv6_nd_target_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_target_masked_t *src)
{
    of_oxm_ipv6_nd_target_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_masked_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_set(dst, ipv6);

    of_oxm_ipv6_nd_target_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_t *
of_oxm_ipv6_nd_tll_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_tll_t *src)
{
    of_oxm_ipv6_nd_tll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_masked_t *
of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_nd_tll_masked_t *src)
{
    of_oxm_ipv6_nd_tll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_tll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_t *
of_oxm_ipv6_src_OF_VERSION_1_2_dup(
    of_oxm_ipv6_src_t *src)
{
    of_oxm_ipv6_src_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_value_get(src, &ipv6);
    of_oxm_ipv6_src_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_masked_t *
of_oxm_ipv6_src_masked_OF_VERSION_1_2_dup(
    of_oxm_ipv6_src_masked_t *src)
{
    of_oxm_ipv6_src_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_masked_value_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_set(dst, ipv6);

    of_oxm_ipv6_src_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_t *
of_oxm_metadata_OF_VERSION_1_2_dup(
    of_oxm_metadata_t *src)
{
    of_oxm_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_value_get(src, &val64);
    of_oxm_metadata_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_masked_t *
of_oxm_metadata_masked_OF_VERSION_1_2_dup(
    of_oxm_metadata_masked_t *src)
{
    of_oxm_metadata_masked_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_masked_value_get(src, &val64);
    of_oxm_metadata_masked_value_set(dst, val64);

    of_oxm_metadata_masked_value_mask_get(src, &val64);
    of_oxm_metadata_masked_value_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_t *
of_oxm_mpls_label_OF_VERSION_1_2_dup(
    of_oxm_mpls_label_t *src)
{
    of_oxm_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_value_get(src, &val32);
    of_oxm_mpls_label_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_masked_t *
of_oxm_mpls_label_masked_OF_VERSION_1_2_dup(
    of_oxm_mpls_label_masked_t *src)
{
    of_oxm_mpls_label_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_masked_value_get(src, &val32);
    of_oxm_mpls_label_masked_value_set(dst, val32);

    of_oxm_mpls_label_masked_value_mask_get(src, &val32);
    of_oxm_mpls_label_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_t *
of_oxm_mpls_tc_OF_VERSION_1_2_dup(
    of_oxm_mpls_tc_t *src)
{
    of_oxm_mpls_tc_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_value_get(src, &val8);
    of_oxm_mpls_tc_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_masked_t *
of_oxm_mpls_tc_masked_OF_VERSION_1_2_dup(
    of_oxm_mpls_tc_masked_t *src)
{
    of_oxm_mpls_tc_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_masked_value_get(src, &val8);
    of_oxm_mpls_tc_masked_value_set(dst, val8);

    of_oxm_mpls_tc_masked_value_mask_get(src, &val8);
    of_oxm_mpls_tc_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_t *
of_oxm_ovs_tcp_flags_OF_VERSION_1_2_dup(
    of_oxm_ovs_tcp_flags_t *src)
{
    of_oxm_ovs_tcp_flags_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_masked_t *
of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_2_dup(
    of_oxm_ovs_tcp_flags_masked_t *src)
{
    of_oxm_ovs_tcp_flags_masked_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_masked_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_masked_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_masked_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_set(dst, val16);

    of_oxm_ovs_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_t *
of_oxm_sctp_dst_OF_VERSION_1_2_dup(
    of_oxm_sctp_dst_t *src)
{
    of_oxm_sctp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_value_get(src, &val16);
    of_oxm_sctp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_masked_t *
of_oxm_sctp_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_sctp_dst_masked_t *src)
{
    of_oxm_sctp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_masked_value_get(src, &val16);
    of_oxm_sctp_dst_masked_value_set(dst, val16);

    of_oxm_sctp_dst_masked_value_mask_get(src, &val16);
    of_oxm_sctp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_t *
of_oxm_sctp_src_OF_VERSION_1_2_dup(
    of_oxm_sctp_src_t *src)
{
    of_oxm_sctp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_value_get(src, &val16);
    of_oxm_sctp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_masked_t *
of_oxm_sctp_src_masked_OF_VERSION_1_2_dup(
    of_oxm_sctp_src_masked_t *src)
{
    of_oxm_sctp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_masked_value_get(src, &val16);
    of_oxm_sctp_src_masked_value_set(dst, val16);

    of_oxm_sctp_src_masked_value_mask_get(src, &val16);
    of_oxm_sctp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_t *
of_oxm_tcp_dst_OF_VERSION_1_2_dup(
    of_oxm_tcp_dst_t *src)
{
    of_oxm_tcp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_value_get(src, &val16);
    of_oxm_tcp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_masked_t *
of_oxm_tcp_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_tcp_dst_masked_t *src)
{
    of_oxm_tcp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_masked_value_get(src, &val16);
    of_oxm_tcp_dst_masked_value_set(dst, val16);

    of_oxm_tcp_dst_masked_value_mask_get(src, &val16);
    of_oxm_tcp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_t *
of_oxm_tcp_src_OF_VERSION_1_2_dup(
    of_oxm_tcp_src_t *src)
{
    of_oxm_tcp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_value_get(src, &val16);
    of_oxm_tcp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_masked_t *
of_oxm_tcp_src_masked_OF_VERSION_1_2_dup(
    of_oxm_tcp_src_masked_t *src)
{
    of_oxm_tcp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_masked_value_get(src, &val16);
    of_oxm_tcp_src_masked_value_set(dst, val16);

    of_oxm_tcp_src_masked_value_mask_get(src, &val16);
    of_oxm_tcp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_t *
of_oxm_tunnel_ipv4_dst_OF_VERSION_1_2_dup(
    of_oxm_tunnel_ipv4_dst_t *src)
{
    of_oxm_tunnel_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_masked_t *
of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_tunnel_ipv4_dst_masked_t *src)
{
    of_oxm_tunnel_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_t *
of_oxm_tunnel_ipv4_src_OF_VERSION_1_2_dup(
    of_oxm_tunnel_ipv4_src_t *src)
{
    of_oxm_tunnel_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_masked_t *
of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_2_dup(
    of_oxm_tunnel_ipv4_src_masked_t *src)
{
    of_oxm_tunnel_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_t *
of_oxm_udp_dst_OF_VERSION_1_2_dup(
    of_oxm_udp_dst_t *src)
{
    of_oxm_udp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_value_get(src, &val16);
    of_oxm_udp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_masked_t *
of_oxm_udp_dst_masked_OF_VERSION_1_2_dup(
    of_oxm_udp_dst_masked_t *src)
{
    of_oxm_udp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_masked_value_get(src, &val16);
    of_oxm_udp_dst_masked_value_set(dst, val16);

    of_oxm_udp_dst_masked_value_mask_get(src, &val16);
    of_oxm_udp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_t *
of_oxm_udp_src_OF_VERSION_1_2_dup(
    of_oxm_udp_src_t *src)
{
    of_oxm_udp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_value_get(src, &val16);
    of_oxm_udp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_masked_t *
of_oxm_udp_src_masked_OF_VERSION_1_2_dup(
    of_oxm_udp_src_masked_t *src)
{
    of_oxm_udp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_masked_value_get(src, &val16);
    of_oxm_udp_src_masked_value_set(dst, val16);

    of_oxm_udp_src_masked_value_mask_get(src, &val16);
    of_oxm_udp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_t *
of_oxm_vlan_pcp_OF_VERSION_1_2_dup(
    of_oxm_vlan_pcp_t *src)
{
    of_oxm_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_value_get(src, &val8);
    of_oxm_vlan_pcp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_masked_t *
of_oxm_vlan_pcp_masked_OF_VERSION_1_2_dup(
    of_oxm_vlan_pcp_masked_t *src)
{
    of_oxm_vlan_pcp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_masked_value_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_set(dst, val8);

    of_oxm_vlan_pcp_masked_value_mask_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_t *
of_oxm_vlan_vid_OF_VERSION_1_2_dup(
    of_oxm_vlan_vid_t *src)
{
    of_oxm_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_value_get(src, &val16);
    of_oxm_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_masked_t *
of_oxm_vlan_vid_masked_OF_VERSION_1_2_dup(
    of_oxm_vlan_vid_masked_t *src)
{
    of_oxm_vlan_vid_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_masked_value_get(src, &val16);
    of_oxm_vlan_vid_masked_value_set(dst, val16);

    of_oxm_vlan_vid_masked_value_mask_get(src, &val16);
    of_oxm_vlan_vid_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_queue_t *
of_packet_queue_OF_VERSION_1_2_dup(
    of_packet_queue_t *src)
{
    of_packet_queue_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_queue_prop_t src_list;
    of_list_queue_prop_t *dst_list;

    if ((dst = of_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_queue_queue_id_get(src, &val32);
    of_packet_queue_queue_id_set(dst, val32);

    of_packet_queue_port_get(src, &port_no);
    of_packet_queue_port_set(dst, port_no);

    of_packet_queue_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_prop_OF_VERSION_1_2_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_queue_delete(dst);
        return NULL;
    }
    of_packet_queue_properties_set(dst, dst_list);
    of_list_queue_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_t *
of_port_desc_OF_VERSION_1_2_dup(
    of_port_desc_t *src)
{
    of_port_desc_t *dst;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    uint32_t val32;

    if ((dst = of_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_port_no_get(src, &port_no);
    of_port_desc_port_no_set(dst, port_no);

    of_port_desc_hw_addr_get(src, &mac_addr);
    of_port_desc_hw_addr_set(dst, mac_addr);

    of_port_desc_name_get(src, &port_name);
    of_port_desc_name_set(dst, port_name);

    of_port_desc_config_get(src, &val32);
    of_port_desc_config_set(dst, val32);

    of_port_desc_state_get(src, &val32);
    of_port_desc_state_set(dst, val32);

    of_port_desc_curr_get(src, &val32);
    of_port_desc_curr_set(dst, val32);

    of_port_desc_advertised_get(src, &val32);
    of_port_desc_advertised_set(dst, val32);

    of_port_desc_supported_get(src, &val32);
    of_port_desc_supported_set(dst, val32);

    of_port_desc_peer_get(src, &val32);
    of_port_desc_peer_set(dst, val32);

    of_port_desc_curr_speed_get(src, &val32);
    of_port_desc_curr_speed_set(dst, val32);

    of_port_desc_max_speed_get(src, &val32);
    of_port_desc_max_speed_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_entry_t *
of_port_stats_entry_OF_VERSION_1_2_dup(
    of_port_stats_entry_t *src)
{
    of_port_stats_entry_t *dst;
    of_port_no_t port_no;
    uint64_t val64;

    if ((dst = of_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_entry_port_no_get(src, &port_no);
    of_port_stats_entry_port_no_set(dst, port_no);

    of_port_stats_entry_rx_packets_get(src, &val64);
    of_port_stats_entry_rx_packets_set(dst, val64);

    of_port_stats_entry_tx_packets_get(src, &val64);
    of_port_stats_entry_tx_packets_set(dst, val64);

    of_port_stats_entry_rx_bytes_get(src, &val64);
    of_port_stats_entry_rx_bytes_set(dst, val64);

    of_port_stats_entry_tx_bytes_get(src, &val64);
    of_port_stats_entry_tx_bytes_set(dst, val64);

    of_port_stats_entry_rx_dropped_get(src, &val64);
    of_port_stats_entry_rx_dropped_set(dst, val64);

    of_port_stats_entry_tx_dropped_get(src, &val64);
    of_port_stats_entry_tx_dropped_set(dst, val64);

    of_port_stats_entry_rx_errors_get(src, &val64);
    of_port_stats_entry_rx_errors_set(dst, val64);

    of_port_stats_entry_tx_errors_get(src, &val64);
    of_port_stats_entry_tx_errors_set(dst, val64);

    of_port_stats_entry_rx_frame_err_get(src, &val64);
    of_port_stats_entry_rx_frame_err_set(dst, val64);

    of_port_stats_entry_rx_over_err_get(src, &val64);
    of_port_stats_entry_rx_over_err_set(dst, val64);

    of_port_stats_entry_rx_crc_err_get(src, &val64);
    of_port_stats_entry_rx_crc_err_set(dst, val64);

    of_port_stats_entry_collisions_get(src, &val64);
    of_port_stats_entry_collisions_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_prop_OF_VERSION_1_2_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_PROP_MAX_RATE) {
        return of_queue_prop_max_rate_OF_VERSION_1_2_dup(src);
    }

    if (src->object_id == OF_QUEUE_PROP_MIN_RATE) {
        return of_queue_prop_min_rate_OF_VERSION_1_2_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_prop_max_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_max_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_max_rate_t *
of_queue_prop_max_rate_OF_VERSION_1_2_dup(
    of_queue_prop_max_rate_t *src)
{
    of_queue_prop_max_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_max_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_max_rate_rate_get(src, &val16);
    of_queue_prop_max_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_min_rate_t *
of_queue_prop_min_rate_OF_VERSION_1_2_dup(
    of_queue_prop_min_rate_t *src)
{
    of_queue_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_min_rate_rate_get(src, &val16);
    of_queue_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_entry_t *
of_queue_stats_entry_OF_VERSION_1_2_dup(
    of_queue_stats_entry_t *src)
{
    of_queue_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_entry_port_no_get(src, &port_no);
    of_queue_stats_entry_port_no_set(dst, port_no);

    of_queue_stats_entry_queue_id_get(src, &val32);
    of_queue_stats_entry_queue_id_set(dst, val32);

    of_queue_stats_entry_tx_bytes_get(src, &val64);
    of_queue_stats_entry_tx_bytes_set(dst, val64);

    of_queue_stats_entry_tx_packets_get(src, &val64);
    of_queue_stats_entry_tx_packets_set(dst, val64);

    of_queue_stats_entry_tx_errors_get(src, &val64);
    of_queue_stats_entry_tx_errors_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_entry_t *
of_table_stats_entry_OF_VERSION_1_2_dup(
    of_table_stats_entry_t *src)
{
    of_table_stats_entry_t *dst;
    uint8_t val8;
    of_table_name_t table_name;
    of_match_bmap_t match_bmap;
    of_wc_bmap_t wc_bmap;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_entry_table_id_get(src, &val8);
    of_table_stats_entry_table_id_set(dst, val8);

    of_table_stats_entry_name_get(src, &table_name);
    of_table_stats_entry_name_set(dst, table_name);

    of_table_stats_entry_match_get(src, &match_bmap);
    of_table_stats_entry_match_set(dst, match_bmap);

    of_table_stats_entry_wildcards_get(src, &wc_bmap);
    of_table_stats_entry_wildcards_set(dst, wc_bmap);

    of_table_stats_entry_write_actions_get(src, &val32);
    of_table_stats_entry_write_actions_set(dst, val32);

    of_table_stats_entry_apply_actions_get(src, &val32);
    of_table_stats_entry_apply_actions_set(dst, val32);

    of_table_stats_entry_write_setfields_get(src, &val64);
    of_table_stats_entry_write_setfields_set(dst, val64);

    of_table_stats_entry_apply_setfields_get(src, &val64);
    of_table_stats_entry_apply_setfields_set(dst, val64);

    of_table_stats_entry_metadata_match_get(src, &val64);
    of_table_stats_entry_metadata_match_set(dst, val64);

    of_table_stats_entry_metadata_write_get(src, &val64);
    of_table_stats_entry_metadata_write_set(dst, val64);

    of_table_stats_entry_instructions_get(src, &val32);
    of_table_stats_entry_instructions_set(dst, val32);

    of_table_stats_entry_config_get(src, &val32);
    of_table_stats_entry_config_set(dst, val32);

    of_table_stats_entry_max_entries_get(src, &val32);
    of_table_stats_entry_max_entries_set(dst, val32);

    of_table_stats_entry_active_count_get(src, &val32);
    of_table_stats_entry_active_count_set(dst, val32);

    of_table_stats_entry_lookup_count_get(src, &val64);
    of_table_stats_entry_lookup_count_set(dst, val64);

    of_table_stats_entry_matched_count_get(src, &val64);
    of_table_stats_entry_matched_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate a list of type of_list_action
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_t *
of_list_action_OF_VERSION_1_2_dup(
    of_list_action_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_t *dst;

    if ((dst = of_list_action_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_interface_t *
of_list_bsn_interface_OF_VERSION_1_2_dup(
    of_list_bsn_interface_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_interface_t *dst;

    if ((dst = of_list_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_INTERFACE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_interface_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_interface_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_t *
of_list_bucket_OF_VERSION_1_2_dup(
    of_list_bucket_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_t *dst;

    if ((dst = of_list_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_counter_t *
of_list_bucket_counter_OF_VERSION_1_2_dup(
    of_list_bucket_counter_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_counter_t *dst;

    if ((dst = of_list_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_COUNTER_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_counter_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_counter_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_flow_stats_entry_t *
of_list_flow_stats_entry_OF_VERSION_1_2_dup(
    of_list_flow_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_flow_stats_entry_t *dst;

    if ((dst = of_list_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_FLOW_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_flow_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_flow_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_desc_stats_entry_t *
of_list_group_desc_stats_entry_OF_VERSION_1_2_dup(
    of_list_group_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_desc_stats_entry_t *dst;

    if ((dst = of_list_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_desc_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_stats_entry_t *
of_list_group_stats_entry_OF_VERSION_1_2_dup(
    of_list_group_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_stats_entry_t *dst;

    if ((dst = of_list_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_t *
of_list_instruction_OF_VERSION_1_2_dup(
    of_list_instruction_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_t *dst;

    if ((dst = of_list_instruction_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_oxm
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_oxm_t *
of_list_oxm_OF_VERSION_1_2_dup(
    of_list_oxm_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_oxm_t *dst;

    if ((dst = of_list_oxm_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_OXM_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_oxm_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_oxm_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_packet_queue_t *
of_list_packet_queue_OF_VERSION_1_2_dup(
    of_list_packet_queue_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_packet_queue_t *dst;

    if ((dst = of_list_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PACKET_QUEUE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_packet_queue_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_packet_queue_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_t *
of_list_port_desc_OF_VERSION_1_2_dup(
    of_list_port_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_t *dst;

    if ((dst = of_list_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_entry_t *
of_list_port_stats_entry_OF_VERSION_1_2_dup(
    of_list_port_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_entry_t *dst;

    if ((dst = of_list_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_prop_t *
of_list_queue_prop_OF_VERSION_1_2_dup(
    of_list_queue_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_prop_t *dst;

    if ((dst = of_list_queue_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_prop_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_entry_t *
of_list_queue_stats_entry_OF_VERSION_1_2_dup(
    of_list_queue_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_entry_t *dst;

    if ((dst = of_list_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_stats_entry_t *
of_list_table_stats_entry_OF_VERSION_1_2_dup(
    of_list_table_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_stats_entry_t *dst;

    if ((dst = of_list_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_stats_entry_OF_VERSION_1_2_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_reply_t *
of_aggregate_stats_reply_OF_VERSION_1_3_dup(
    of_aggregate_stats_reply_t *src)
{
    of_aggregate_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_aggregate_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_reply_xid_get(src, &val32);
    of_aggregate_stats_reply_xid_set(dst, val32);

    of_aggregate_stats_reply_flags_get(src, &val16);
    of_aggregate_stats_reply_flags_set(dst, val16);

    of_aggregate_stats_reply_packet_count_get(src, &val64);
    of_aggregate_stats_reply_packet_count_set(dst, val64);

    of_aggregate_stats_reply_byte_count_get(src, &val64);
    of_aggregate_stats_reply_byte_count_set(dst, val64);

    of_aggregate_stats_reply_flow_count_get(src, &val32);
    of_aggregate_stats_reply_flow_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_request_t *
of_aggregate_stats_request_OF_VERSION_1_3_dup(
    of_aggregate_stats_request_t *src)
{
    of_aggregate_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_aggregate_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_request_xid_get(src, &val32);
    of_aggregate_stats_request_xid_set(dst, val32);

    of_aggregate_stats_request_flags_get(src, &val16);
    of_aggregate_stats_request_flags_set(dst, val16);

    of_aggregate_stats_request_table_id_get(src, &val8);
    of_aggregate_stats_request_table_id_set(dst, val8);

    of_aggregate_stats_request_out_port_get(src, &port_no);
    of_aggregate_stats_request_out_port_set(dst, port_no);

    of_aggregate_stats_request_out_group_get(src, &val32);
    of_aggregate_stats_request_out_group_set(dst, val32);

    of_aggregate_stats_request_cookie_get(src, &val64);
    of_aggregate_stats_request_cookie_set(dst, val64);

    of_aggregate_stats_request_cookie_mask_get(src, &val64);
    of_aggregate_stats_request_cookie_mask_set(dst, val64);

    of_aggregate_stats_request_match_get(src, &match);
    of_aggregate_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_async_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_get_reply_t *
of_async_get_reply_OF_VERSION_1_3_dup(
    of_async_get_reply_t *src)
{
    of_async_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_async_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_get_reply_xid_get(src, &val32);
    of_async_get_reply_xid_set(dst, val32);

    of_async_get_reply_packet_in_mask_equal_master_get(src, &val32);
    of_async_get_reply_packet_in_mask_equal_master_set(dst, val32);

    of_async_get_reply_packet_in_mask_slave_get(src, &val32);
    of_async_get_reply_packet_in_mask_slave_set(dst, val32);

    of_async_get_reply_port_status_mask_equal_master_get(src, &val32);
    of_async_get_reply_port_status_mask_equal_master_set(dst, val32);

    of_async_get_reply_port_status_mask_slave_get(src, &val32);
    of_async_get_reply_port_status_mask_slave_set(dst, val32);

    of_async_get_reply_flow_removed_mask_equal_master_get(src, &val32);
    of_async_get_reply_flow_removed_mask_equal_master_set(dst, val32);

    of_async_get_reply_flow_removed_mask_slave_get(src, &val32);
    of_async_get_reply_flow_removed_mask_slave_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_get_request_t *
of_async_get_request_OF_VERSION_1_3_dup(
    of_async_get_request_t *src)
{
    of_async_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_async_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_get_request_xid_get(src, &val32);
    of_async_get_request_xid_set(dst, val32);

    of_async_get_request_packet_in_mask_equal_master_get(src, &val32);
    of_async_get_request_packet_in_mask_equal_master_set(dst, val32);

    of_async_get_request_packet_in_mask_slave_get(src, &val32);
    of_async_get_request_packet_in_mask_slave_set(dst, val32);

    of_async_get_request_port_status_mask_equal_master_get(src, &val32);
    of_async_get_request_port_status_mask_equal_master_set(dst, val32);

    of_async_get_request_port_status_mask_slave_get(src, &val32);
    of_async_get_request_port_status_mask_slave_set(dst, val32);

    of_async_get_request_flow_removed_mask_equal_master_get(src, &val32);
    of_async_get_request_flow_removed_mask_equal_master_set(dst, val32);

    of_async_get_request_flow_removed_mask_slave_get(src, &val32);
    of_async_get_request_flow_removed_mask_slave_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_set
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_set.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_set_t *
of_async_set_OF_VERSION_1_3_dup(
    of_async_set_t *src)
{
    of_async_set_t *dst;
    uint32_t val32;

    if ((dst = of_async_set_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_set_xid_get(src, &val32);
    of_async_set_xid_set(dst, val32);

    of_async_set_packet_in_mask_equal_master_get(src, &val32);
    of_async_set_packet_in_mask_equal_master_set(dst, val32);

    of_async_set_packet_in_mask_slave_get(src, &val32);
    of_async_set_packet_in_mask_slave_set(dst, val32);

    of_async_set_port_status_mask_equal_master_get(src, &val32);
    of_async_set_port_status_mask_equal_master_set(dst, val32);

    of_async_set_port_status_mask_slave_get(src, &val32);
    of_async_set_port_status_mask_slave_set(dst, val32);

    of_async_set_flow_removed_mask_equal_master_get(src, &val32);
    of_async_set_flow_removed_mask_equal_master_set(dst, val32);

    of_async_set_flow_removed_mask_slave_get(src, &val32);
    of_async_set_flow_removed_mask_slave_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bad_action_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_action_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_action_error_msg_t *
of_bad_action_error_msg_OF_VERSION_1_3_dup(
    of_bad_action_error_msg_t *src)
{
    of_bad_action_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_action_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_action_error_msg_xid_get(src, &val32);
    of_bad_action_error_msg_xid_set(dst, val32);

    of_bad_action_error_msg_code_get(src, &val16);
    of_bad_action_error_msg_code_set(dst, val16);

    of_bad_action_error_msg_data_get(src, &octets);
    of_bad_action_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_instruction_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_instruction_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_instruction_error_msg_t *
of_bad_instruction_error_msg_OF_VERSION_1_3_dup(
    of_bad_instruction_error_msg_t *src)
{
    of_bad_instruction_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_instruction_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_instruction_error_msg_xid_get(src, &val32);
    of_bad_instruction_error_msg_xid_set(dst, val32);

    of_bad_instruction_error_msg_code_get(src, &val16);
    of_bad_instruction_error_msg_code_set(dst, val16);

    of_bad_instruction_error_msg_data_get(src, &octets);
    of_bad_instruction_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_match_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_match_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_match_error_msg_t *
of_bad_match_error_msg_OF_VERSION_1_3_dup(
    of_bad_match_error_msg_t *src)
{
    of_bad_match_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_match_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_match_error_msg_xid_get(src, &val32);
    of_bad_match_error_msg_xid_set(dst, val32);

    of_bad_match_error_msg_code_get(src, &val16);
    of_bad_match_error_msg_code_set(dst, val16);

    of_bad_match_error_msg_data_get(src, &octets);
    of_bad_match_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_request_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_request_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_request_error_msg_t *
of_bad_request_error_msg_OF_VERSION_1_3_dup(
    of_bad_request_error_msg_t *src)
{
    of_bad_request_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_request_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_request_error_msg_xid_get(src, &val32);
    of_bad_request_error_msg_xid_set(dst, val32);

    of_bad_request_error_msg_code_get(src, &val16);
    of_bad_request_error_msg_code_set(dst, val16);

    of_bad_request_error_msg_data_get(src, &octets);
    of_bad_request_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_reply_t *
of_barrier_reply_OF_VERSION_1_3_dup(
    of_barrier_reply_t *src)
{
    of_barrier_reply_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_reply_xid_get(src, &val32);
    of_barrier_reply_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_request_t *
of_barrier_request_OF_VERSION_1_3_dup(
    of_barrier_request_t *src)
{
    of_barrier_request_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_request_xid_get(src, &val32);
    of_barrier_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_arp_idle
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_arp_idle.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_arp_idle_t *
of_bsn_arp_idle_OF_VERSION_1_3_dup(
    of_bsn_arp_idle_t *src)
{
    of_bsn_arp_idle_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_arp_idle_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_arp_idle_xid_get(src, &val32);
    of_bsn_arp_idle_xid_set(dst, val32);

    of_bsn_arp_idle_experimenter_get(src, &val32);
    of_bsn_arp_idle_experimenter_set(dst, val32);

    of_bsn_arp_idle_subtype_get(src, &val32);
    of_bsn_arp_idle_subtype_set(dst, val32);

    of_bsn_arp_idle_vlan_vid_get(src, &val16);
    of_bsn_arp_idle_vlan_vid_set(dst, val16);

    of_bsn_arp_idle_ipv4_addr_get(src, &ipv4);
    of_bsn_arp_idle_ipv4_addr_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_reply_t *
of_bsn_bw_clear_data_reply_OF_VERSION_1_3_dup(
    of_bsn_bw_clear_data_reply_t *src)
{
    of_bsn_bw_clear_data_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_reply_xid_get(src, &val32);
    of_bsn_bw_clear_data_reply_xid_set(dst, val32);

    of_bsn_bw_clear_data_reply_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_reply_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_reply_subtype_get(src, &val32);
    of_bsn_bw_clear_data_reply_subtype_set(dst, val32);

    of_bsn_bw_clear_data_reply_status_get(src, &val32);
    of_bsn_bw_clear_data_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_request_t *
of_bsn_bw_clear_data_request_OF_VERSION_1_3_dup(
    of_bsn_bw_clear_data_request_t *src)
{
    of_bsn_bw_clear_data_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_request_xid_get(src, &val32);
    of_bsn_bw_clear_data_request_xid_set(dst, val32);

    of_bsn_bw_clear_data_request_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_request_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_request_subtype_get(src, &val32);
    of_bsn_bw_clear_data_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_reply_t *
of_bsn_bw_enable_get_reply_OF_VERSION_1_3_dup(
    of_bsn_bw_enable_get_reply_t *src)
{
    of_bsn_bw_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_reply_xid_get(src, &val32);
    of_bsn_bw_enable_get_reply_xid_set(dst, val32);

    of_bsn_bw_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_get_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_get_reply_enabled_get(src, &val32);
    of_bsn_bw_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_request_t *
of_bsn_bw_enable_get_request_OF_VERSION_1_3_dup(
    of_bsn_bw_enable_get_request_t *src)
{
    of_bsn_bw_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_request_xid_get(src, &val32);
    of_bsn_bw_enable_get_request_xid_set(dst, val32);

    of_bsn_bw_enable_get_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_request_subtype_get(src, &val32);
    of_bsn_bw_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_reply_t *
of_bsn_bw_enable_set_reply_OF_VERSION_1_3_dup(
    of_bsn_bw_enable_set_reply_t *src)
{
    of_bsn_bw_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_reply_xid_get(src, &val32);
    of_bsn_bw_enable_set_reply_xid_set(dst, val32);

    of_bsn_bw_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_set_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_set_reply_enable_get(src, &val32);
    of_bsn_bw_enable_set_reply_enable_set(dst, val32);

    of_bsn_bw_enable_set_reply_status_get(src, &val32);
    of_bsn_bw_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_request_t *
of_bsn_bw_enable_set_request_OF_VERSION_1_3_dup(
    of_bsn_bw_enable_set_request_t *src)
{
    of_bsn_bw_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_request_xid_get(src, &val32);
    of_bsn_bw_enable_set_request_xid_set(dst, val32);

    of_bsn_bw_enable_set_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_request_subtype_get(src, &val32);
    of_bsn_bw_enable_set_request_subtype_set(dst, val32);

    of_bsn_bw_enable_set_request_enable_get(src, &val32);
    of_bsn_bw_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connections_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connections_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connections_reply_t *
of_bsn_controller_connections_reply_OF_VERSION_1_3_dup(
    of_bsn_controller_connections_reply_t *src)
{
    of_bsn_controller_connections_reply_t *dst;
    uint32_t val32;

    of_list_bsn_controller_connection_t src_list;
    of_list_bsn_controller_connection_t *dst_list;

    if ((dst = of_bsn_controller_connections_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connections_reply_xid_get(src, &val32);
    of_bsn_controller_connections_reply_xid_set(dst, val32);

    of_bsn_controller_connections_reply_experimenter_get(src, &val32);
    of_bsn_controller_connections_reply_experimenter_set(dst, val32);

    of_bsn_controller_connections_reply_subtype_get(src, &val32);
    of_bsn_controller_connections_reply_subtype_set(dst, val32);

    of_bsn_controller_connections_reply_connections_bind(
        src, &src_list);
    dst_list = of_list_bsn_controller_connection_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_controller_connections_reply_delete(dst);
        return NULL;
    }
    of_bsn_controller_connections_reply_connections_set(dst, dst_list);
    of_list_bsn_controller_connection_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connections_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connections_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connections_request_t *
of_bsn_controller_connections_request_OF_VERSION_1_3_dup(
    of_bsn_controller_connections_request_t *src)
{
    of_bsn_controller_connections_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_controller_connections_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connections_request_xid_get(src, &val32);
    of_bsn_controller_connections_request_xid_set(dst, val32);

    of_bsn_controller_connections_request_experimenter_get(src, &val32);
    of_bsn_controller_connections_request_experimenter_set(dst, val32);

    of_bsn_controller_connections_request_subtype_get(src, &val32);
    of_bsn_controller_connections_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_reply_t *
of_bsn_debug_counter_desc_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_desc_stats_reply_t *src)
{
    of_bsn_debug_counter_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_debug_counter_desc_stats_entry_t src_list;
    of_list_bsn_debug_counter_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_debug_counter_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_reply_xid_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_xid_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_flags_get(src, &val16);
    of_bsn_debug_counter_desc_stats_reply_flags_set(dst, val16);

    of_bsn_debug_counter_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_debug_counter_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_debug_counter_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_debug_counter_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_request_t *
of_bsn_debug_counter_desc_stats_request_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_desc_stats_request_t *src)
{
    of_bsn_debug_counter_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_debug_counter_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_request_xid_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_xid_set(dst, val32);

    of_bsn_debug_counter_desc_stats_request_flags_get(src, &val16);
    of_bsn_debug_counter_desc_stats_request_flags_set(dst, val16);

    of_bsn_debug_counter_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_debug_counter_desc_stats_request_subtype_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_reply_t *
of_bsn_debug_counter_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_stats_reply_t *src)
{
    of_bsn_debug_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_debug_counter_stats_entry_t src_list;
    of_list_bsn_debug_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_debug_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_reply_xid_get(src, &val32);
    of_bsn_debug_counter_stats_reply_xid_set(dst, val32);

    of_bsn_debug_counter_stats_reply_flags_get(src, &val16);
    of_bsn_debug_counter_stats_reply_flags_set(dst, val16);

    of_bsn_debug_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_debug_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_debug_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_debug_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_debug_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_debug_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_debug_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_debug_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_request_t *
of_bsn_debug_counter_stats_request_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_stats_request_t *src)
{
    of_bsn_debug_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_debug_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_request_xid_get(src, &val32);
    of_bsn_debug_counter_stats_request_xid_set(dst, val32);

    of_bsn_debug_counter_stats_request_flags_get(src, &val16);
    of_bsn_debug_counter_stats_request_flags_set(dst, val16);

    of_bsn_debug_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_debug_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_debug_counter_stats_request_subtype_get(src, &val32);
    of_bsn_debug_counter_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_error_t *
of_bsn_error_OF_VERSION_1_3_dup(
    of_bsn_error_t *src)
{
    of_bsn_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_error_xid_get(src, &val32);
    of_bsn_error_xid_set(dst, val32);

    of_bsn_error_subtype_get(src, &val16);
    of_bsn_error_subtype_set(dst, val16);

    of_bsn_error_experimenter_get(src, &val32);
    of_bsn_error_experimenter_set(dst, val32);

    of_bsn_error_err_msg_get(src, &desc_str);
    of_bsn_error_err_msg_set(dst, desc_str);

    of_bsn_error_data_get(src, &octets);
    of_bsn_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_reply_t *
of_bsn_flow_checksum_bucket_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_flow_checksum_bucket_stats_reply_t *src)
{
    of_bsn_flow_checksum_bucket_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_flow_checksum_bucket_stats_entry_t src_list;
    of_list_bsn_flow_checksum_bucket_stats_entry_t *dst_list;

    if ((dst = of_bsn_flow_checksum_bucket_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_reply_xid_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_xid_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_flags_get(src, &val16);
    of_bsn_flow_checksum_bucket_stats_reply_flags_set(dst, val16);

    of_bsn_flow_checksum_bucket_stats_reply_experimenter_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_experimenter_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_subtype_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_subtype_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_flow_checksum_bucket_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_flow_checksum_bucket_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_flow_checksum_bucket_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_request_t *
of_bsn_flow_checksum_bucket_stats_request_OF_VERSION_1_3_dup(
    of_bsn_flow_checksum_bucket_stats_request_t *src)
{
    of_bsn_flow_checksum_bucket_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;

    if ((dst = of_bsn_flow_checksum_bucket_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_request_xid_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_xid_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_flags_get(src, &val16);
    of_bsn_flow_checksum_bucket_stats_request_flags_set(dst, val16);

    of_bsn_flow_checksum_bucket_stats_request_experimenter_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_experimenter_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_subtype_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_subtype_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_table_id_get(src, &val8);
    of_bsn_flow_checksum_bucket_stats_request_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_t *
of_bsn_flow_idle_OF_VERSION_1_3_dup(
    of_bsn_flow_idle_t *src)
{
    of_bsn_flow_idle_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_bsn_flow_idle_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_xid_get(src, &val32);
    of_bsn_flow_idle_xid_set(dst, val32);

    of_bsn_flow_idle_experimenter_get(src, &val32);
    of_bsn_flow_idle_experimenter_set(dst, val32);

    of_bsn_flow_idle_subtype_get(src, &val32);
    of_bsn_flow_idle_subtype_set(dst, val32);

    of_bsn_flow_idle_cookie_get(src, &val64);
    of_bsn_flow_idle_cookie_set(dst, val64);

    of_bsn_flow_idle_priority_get(src, &val16);
    of_bsn_flow_idle_priority_set(dst, val16);

    of_bsn_flow_idle_table_id_get(src, &val8);
    of_bsn_flow_idle_table_id_set(dst, val8);

    of_bsn_flow_idle_match_get(src, &match);
    of_bsn_flow_idle_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_get_reply_t *
of_bsn_flow_idle_enable_get_reply_OF_VERSION_1_3_dup(
    of_bsn_flow_idle_enable_get_reply_t *src)
{
    of_bsn_flow_idle_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_get_reply_xid_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_xid_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_enabled_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_get_request_t *
of_bsn_flow_idle_enable_get_request_OF_VERSION_1_3_dup(
    of_bsn_flow_idle_enable_get_request_t *src)
{
    of_bsn_flow_idle_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_get_request_xid_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_xid_set(dst, val32);

    of_bsn_flow_idle_enable_get_request_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_get_request_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_set_reply_t *
of_bsn_flow_idle_enable_set_reply_OF_VERSION_1_3_dup(
    of_bsn_flow_idle_enable_set_reply_t *src)
{
    of_bsn_flow_idle_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_set_reply_xid_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_xid_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_enable_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_enable_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_status_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_set_request_t *
of_bsn_flow_idle_enable_set_request_OF_VERSION_1_3_dup(
    of_bsn_flow_idle_enable_set_request_t *src)
{
    of_bsn_flow_idle_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_set_request_xid_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_xid_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_enable_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_reply_t *
of_bsn_generic_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_generic_stats_reply_t *src)
{
    of_bsn_generic_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_generic_stats_entry_t src_list;
    of_list_bsn_generic_stats_entry_t *dst_list;

    if ((dst = of_bsn_generic_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_reply_xid_get(src, &val32);
    of_bsn_generic_stats_reply_xid_set(dst, val32);

    of_bsn_generic_stats_reply_flags_get(src, &val16);
    of_bsn_generic_stats_reply_flags_set(dst, val16);

    of_bsn_generic_stats_reply_experimenter_get(src, &val32);
    of_bsn_generic_stats_reply_experimenter_set(dst, val32);

    of_bsn_generic_stats_reply_subtype_get(src, &val32);
    of_bsn_generic_stats_reply_subtype_set(dst, val32);

    of_bsn_generic_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_generic_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_generic_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_request_t *
of_bsn_generic_stats_request_OF_VERSION_1_3_dup(
    of_bsn_generic_stats_request_t *src)
{
    of_bsn_generic_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_str64_t str64;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_request_xid_get(src, &val32);
    of_bsn_generic_stats_request_xid_set(dst, val32);

    of_bsn_generic_stats_request_flags_get(src, &val16);
    of_bsn_generic_stats_request_flags_set(dst, val16);

    of_bsn_generic_stats_request_experimenter_get(src, &val32);
    of_bsn_generic_stats_request_experimenter_set(dst, val32);

    of_bsn_generic_stats_request_subtype_get(src, &val32);
    of_bsn_generic_stats_request_subtype_set(dst, val32);

    of_bsn_generic_stats_request_name_get(src, &str64);
    of_bsn_generic_stats_request_name_set(dst, str64);

    of_bsn_generic_stats_request_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_request_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_request_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_reply_t *
of_bsn_gentable_bucket_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_bucket_stats_reply_t *src)
{
    of_bsn_gentable_bucket_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_bucket_stats_entry_t src_list;
    of_list_bsn_gentable_bucket_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_bucket_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_bucket_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_bucket_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_bucket_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_bucket_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_bucket_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_request_t *
of_bsn_gentable_bucket_stats_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_bucket_stats_request_t *src)
{
    of_bsn_gentable_bucket_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_bucket_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_request_xid_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_xid_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_flags_get(src, &val16);
    of_bsn_gentable_bucket_stats_request_flags_set(dst, val16);

    of_bsn_gentable_bucket_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_bucket_stats_request_table_id_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_clear_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_clear_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_clear_reply_t *
of_bsn_gentable_clear_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_clear_reply_t *src)
{
    of_bsn_gentable_clear_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_clear_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_clear_reply_xid_get(src, &val32);
    of_bsn_gentable_clear_reply_xid_set(dst, val32);

    of_bsn_gentable_clear_reply_experimenter_get(src, &val32);
    of_bsn_gentable_clear_reply_experimenter_set(dst, val32);

    of_bsn_gentable_clear_reply_subtype_get(src, &val32);
    of_bsn_gentable_clear_reply_subtype_set(dst, val32);

    of_bsn_gentable_clear_reply_table_id_get(src, &val16);
    of_bsn_gentable_clear_reply_table_id_set(dst, val16);

    of_bsn_gentable_clear_reply_deleted_count_get(src, &val32);
    of_bsn_gentable_clear_reply_deleted_count_set(dst, val32);

    of_bsn_gentable_clear_reply_error_count_get(src, &val32);
    of_bsn_gentable_clear_reply_error_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_clear_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_clear_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_clear_request_t *
of_bsn_gentable_clear_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_clear_request_t *src)
{
    of_bsn_gentable_clear_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_clear_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_clear_request_xid_get(src, &val32);
    of_bsn_gentable_clear_request_xid_set(dst, val32);

    of_bsn_gentable_clear_request_experimenter_get(src, &val32);
    of_bsn_gentable_clear_request_experimenter_set(dst, val32);

    of_bsn_gentable_clear_request_subtype_get(src, &val32);
    of_bsn_gentable_clear_request_subtype_set(dst, val32);

    of_bsn_gentable_clear_request_table_id_get(src, &val16);
    of_bsn_gentable_clear_request_table_id_set(dst, val16);

    of_bsn_gentable_clear_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_clear_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_clear_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_clear_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_reply_t *
of_bsn_gentable_desc_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_desc_stats_reply_t *src)
{
    of_bsn_gentable_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_desc_stats_entry_t src_list;
    of_list_bsn_gentable_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_desc_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_request_t *
of_bsn_gentable_desc_stats_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_desc_stats_request_t *src)
{
    of_bsn_gentable_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_request_xid_get(src, &val32);
    of_bsn_gentable_desc_stats_request_xid_set(dst, val32);

    of_bsn_gentable_desc_stats_request_flags_get(src, &val16);
    of_bsn_gentable_desc_stats_request_flags_set(dst, val16);

    of_bsn_gentable_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_desc_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_add_t *
of_bsn_gentable_entry_add_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_add_t *src)
{
    of_bsn_gentable_entry_add_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_add_xid_get(src, &val32);
    of_bsn_gentable_entry_add_xid_set(dst, val32);

    of_bsn_gentable_entry_add_experimenter_get(src, &val32);
    of_bsn_gentable_entry_add_experimenter_set(dst, val32);

    of_bsn_gentable_entry_add_subtype_get(src, &val32);
    of_bsn_gentable_entry_add_subtype_set(dst, val32);

    of_bsn_gentable_entry_add_table_id_get(src, &val16);
    of_bsn_gentable_entry_add_table_id_set(dst, val16);

    of_bsn_gentable_entry_add_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_add_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_add_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_add_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_add_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_add_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_add_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_add_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_delete_t *
of_bsn_gentable_entry_delete_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_delete_t *src)
{
    of_bsn_gentable_entry_delete_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_delete_xid_get(src, &val32);
    of_bsn_gentable_entry_delete_xid_set(dst, val32);

    of_bsn_gentable_entry_delete_experimenter_get(src, &val32);
    of_bsn_gentable_entry_delete_experimenter_set(dst, val32);

    of_bsn_gentable_entry_delete_subtype_get(src, &val32);
    of_bsn_gentable_entry_delete_subtype_set(dst, val32);

    of_bsn_gentable_entry_delete_table_id_get(src, &val16);
    of_bsn_gentable_entry_delete_table_id_set(dst, val16);

    of_bsn_gentable_entry_delete_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_delete_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_delete_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_reply_t *
of_bsn_gentable_entry_desc_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_desc_stats_reply_t *src)
{
    of_bsn_gentable_entry_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_entry_desc_stats_entry_t src_list;
    of_list_bsn_gentable_entry_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_entry_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_entry_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_request_t *
of_bsn_gentable_entry_desc_stats_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_desc_stats_request_t *src)
{
    of_bsn_gentable_entry_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_entry_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_request_xid_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_xid_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_flags_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_request_flags_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_request_table_id_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_desc_stats_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_reply_t *
of_bsn_gentable_entry_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_stats_reply_t *src)
{
    of_bsn_gentable_entry_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_entry_stats_entry_t src_list;
    of_list_bsn_gentable_entry_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_entry_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_entry_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_entry_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_entry_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_request_t *
of_bsn_gentable_entry_stats_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_stats_request_t *src)
{
    of_bsn_gentable_entry_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_entry_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_request_xid_get(src, &val32);
    of_bsn_gentable_entry_stats_request_xid_set(dst, val32);

    of_bsn_gentable_entry_stats_request_flags_get(src, &val16);
    of_bsn_gentable_entry_stats_request_flags_set(dst, val16);

    of_bsn_gentable_entry_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_entry_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_entry_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_entry_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_entry_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_entry_stats_request_table_id_set(dst, val16);

    of_bsn_gentable_entry_stats_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_stats_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_stats_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_entry_stats_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_error_t *
of_bsn_gentable_error_OF_VERSION_1_3_dup(
    of_bsn_gentable_error_t *src)
{
    of_bsn_gentable_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_gentable_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_error_xid_get(src, &val32);
    of_bsn_gentable_error_xid_set(dst, val32);

    of_bsn_gentable_error_subtype_get(src, &val16);
    of_bsn_gentable_error_subtype_set(dst, val16);

    of_bsn_gentable_error_experimenter_get(src, &val32);
    of_bsn_gentable_error_experimenter_set(dst, val32);

    of_bsn_gentable_error_table_id_get(src, &val32);
    of_bsn_gentable_error_table_id_set(dst, val32);

    of_bsn_gentable_error_err_msg_get(src, &desc_str);
    of_bsn_gentable_error_err_msg_set(dst, desc_str);

    of_bsn_gentable_error_data_get(src, &octets);
    of_bsn_gentable_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_full_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_full_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_full_error_t *
of_bsn_gentable_full_error_OF_VERSION_1_3_dup(
    of_bsn_gentable_full_error_t *src)
{
    of_bsn_gentable_full_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_gentable_full_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_full_error_xid_get(src, &val32);
    of_bsn_gentable_full_error_xid_set(dst, val32);

    of_bsn_gentable_full_error_subtype_get(src, &val16);
    of_bsn_gentable_full_error_subtype_set(dst, val16);

    of_bsn_gentable_full_error_experimenter_get(src, &val32);
    of_bsn_gentable_full_error_experimenter_set(dst, val32);

    of_bsn_gentable_full_error_table_id_get(src, &val32);
    of_bsn_gentable_full_error_table_id_set(dst, val32);

    of_bsn_gentable_full_error_err_msg_get(src, &desc_str);
    of_bsn_gentable_full_error_err_msg_set(dst, desc_str);

    of_bsn_gentable_full_error_data_get(src, &octets);
    of_bsn_gentable_full_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_set_buckets_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_set_buckets_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_set_buckets_size_t *
of_bsn_gentable_set_buckets_size_OF_VERSION_1_3_dup(
    of_bsn_gentable_set_buckets_size_t *src)
{
    of_bsn_gentable_set_buckets_size_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_set_buckets_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_set_buckets_size_xid_get(src, &val32);
    of_bsn_gentable_set_buckets_size_xid_set(dst, val32);

    of_bsn_gentable_set_buckets_size_experimenter_get(src, &val32);
    of_bsn_gentable_set_buckets_size_experimenter_set(dst, val32);

    of_bsn_gentable_set_buckets_size_subtype_get(src, &val32);
    of_bsn_gentable_set_buckets_size_subtype_set(dst, val32);

    of_bsn_gentable_set_buckets_size_table_id_get(src, &val16);
    of_bsn_gentable_set_buckets_size_table_id_set(dst, val16);

    of_bsn_gentable_set_buckets_size_buckets_size_get(src, &val32);
    of_bsn_gentable_set_buckets_size_buckets_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_reply_t *
of_bsn_gentable_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_gentable_stats_reply_t *src)
{
    of_bsn_gentable_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_stats_entry_t src_list;
    of_list_bsn_gentable_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_request_t *
of_bsn_gentable_stats_request_OF_VERSION_1_3_dup(
    of_bsn_gentable_stats_request_t *src)
{
    of_bsn_gentable_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_request_xid_get(src, &val32);
    of_bsn_gentable_stats_request_xid_set(dst, val32);

    of_bsn_gentable_stats_request_flags_get(src, &val16);
    of_bsn_gentable_stats_request_flags_set(dst, val16);

    of_bsn_gentable_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_reply_t *
of_bsn_get_interfaces_reply_OF_VERSION_1_3_dup(
    of_bsn_get_interfaces_reply_t *src)
{
    of_bsn_get_interfaces_reply_t *dst;
    uint32_t val32;

    of_list_bsn_interface_t src_list;
    of_list_bsn_interface_t *dst_list;

    if ((dst = of_bsn_get_interfaces_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_reply_xid_get(src, &val32);
    of_bsn_get_interfaces_reply_xid_set(dst, val32);

    of_bsn_get_interfaces_reply_experimenter_get(src, &val32);
    of_bsn_get_interfaces_reply_experimenter_set(dst, val32);

    of_bsn_get_interfaces_reply_subtype_get(src, &val32);
    of_bsn_get_interfaces_reply_subtype_set(dst, val32);

    of_bsn_get_interfaces_reply_interfaces_bind(
        src, &src_list);
    dst_list = of_list_bsn_interface_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_get_interfaces_reply_delete(dst);
        return NULL;
    }
    of_bsn_get_interfaces_reply_interfaces_set(dst, dst_list);
    of_list_bsn_interface_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_request_t *
of_bsn_get_interfaces_request_OF_VERSION_1_3_dup(
    of_bsn_get_interfaces_request_t *src)
{
    of_bsn_get_interfaces_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_interfaces_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_request_xid_get(src, &val32);
    of_bsn_get_interfaces_request_xid_set(dst, val32);

    of_bsn_get_interfaces_request_experimenter_get(src, &val32);
    of_bsn_get_interfaces_request_experimenter_set(dst, val32);

    of_bsn_get_interfaces_request_subtype_get(src, &val32);
    of_bsn_get_interfaces_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_reply_t *
of_bsn_get_mirroring_reply_OF_VERSION_1_3_dup(
    of_bsn_get_mirroring_reply_t *src)
{
    of_bsn_get_mirroring_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_reply_xid_get(src, &val32);
    of_bsn_get_mirroring_reply_xid_set(dst, val32);

    of_bsn_get_mirroring_reply_experimenter_get(src, &val32);
    of_bsn_get_mirroring_reply_experimenter_set(dst, val32);

    of_bsn_get_mirroring_reply_subtype_get(src, &val32);
    of_bsn_get_mirroring_reply_subtype_set(dst, val32);

    of_bsn_get_mirroring_reply_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_reply_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_request_t *
of_bsn_get_mirroring_request_OF_VERSION_1_3_dup(
    of_bsn_get_mirroring_request_t *src)
{
    of_bsn_get_mirroring_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_request_xid_get(src, &val32);
    of_bsn_get_mirroring_request_xid_set(dst, val32);

    of_bsn_get_mirroring_request_experimenter_get(src, &val32);
    of_bsn_get_mirroring_request_experimenter_set(dst, val32);

    of_bsn_get_mirroring_request_subtype_get(src, &val32);
    of_bsn_get_mirroring_request_subtype_set(dst, val32);

    of_bsn_get_mirroring_request_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_request_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_switch_pipeline_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_switch_pipeline_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_switch_pipeline_reply_t *
of_bsn_get_switch_pipeline_reply_OF_VERSION_1_3_dup(
    of_bsn_get_switch_pipeline_reply_t *src)
{
    of_bsn_get_switch_pipeline_reply_t *dst;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_get_switch_pipeline_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_switch_pipeline_reply_xid_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_xid_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_experimenter_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_experimenter_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_subtype_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_subtype_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_pipeline_get(src, &desc_str);
    of_bsn_get_switch_pipeline_reply_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_switch_pipeline_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_switch_pipeline_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_switch_pipeline_request_t *
of_bsn_get_switch_pipeline_request_OF_VERSION_1_3_dup(
    of_bsn_get_switch_pipeline_request_t *src)
{
    of_bsn_get_switch_pipeline_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_switch_pipeline_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_switch_pipeline_request_xid_get(src, &val32);
    of_bsn_get_switch_pipeline_request_xid_set(dst, val32);

    of_bsn_get_switch_pipeline_request_experimenter_get(src, &val32);
    of_bsn_get_switch_pipeline_request_experimenter_set(dst, val32);

    of_bsn_get_switch_pipeline_request_subtype_get(src, &val32);
    of_bsn_get_switch_pipeline_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_image_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_image_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_image_desc_stats_reply_t *
of_bsn_image_desc_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_image_desc_stats_reply_t *src)
{
    of_bsn_image_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_image_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_image_desc_stats_reply_xid_get(src, &val32);
    of_bsn_image_desc_stats_reply_xid_set(dst, val32);

    of_bsn_image_desc_stats_reply_flags_get(src, &val16);
    of_bsn_image_desc_stats_reply_flags_set(dst, val16);

    of_bsn_image_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_image_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_image_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_image_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_image_desc_stats_reply_image_checksum_get(src, &desc_str);
    of_bsn_image_desc_stats_reply_image_checksum_set(dst, desc_str);

    of_bsn_image_desc_stats_reply_startup_config_checksum_get(src, &desc_str);
    of_bsn_image_desc_stats_reply_startup_config_checksum_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_image_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_image_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_image_desc_stats_request_t *
of_bsn_image_desc_stats_request_OF_VERSION_1_3_dup(
    of_bsn_image_desc_stats_request_t *src)
{
    of_bsn_image_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_image_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_image_desc_stats_request_xid_get(src, &val32);
    of_bsn_image_desc_stats_request_xid_set(dst, val32);

    of_bsn_image_desc_stats_request_flags_get(src, &val16);
    of_bsn_image_desc_stats_request_flags_set(dst, val16);

    of_bsn_image_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_image_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_image_desc_stats_request_subtype_get(src, &val32);
    of_bsn_image_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_convergence_notif
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_convergence_notif.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_convergence_notif_t *
of_bsn_lacp_convergence_notif_OF_VERSION_1_3_dup(
    of_bsn_lacp_convergence_notif_t *src)
{
    of_bsn_lacp_convergence_notif_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_lacp_convergence_notif_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_convergence_notif_xid_get(src, &val32);
    of_bsn_lacp_convergence_notif_xid_set(dst, val32);

    of_bsn_lacp_convergence_notif_experimenter_get(src, &val32);
    of_bsn_lacp_convergence_notif_experimenter_set(dst, val32);

    of_bsn_lacp_convergence_notif_subtype_get(src, &val32);
    of_bsn_lacp_convergence_notif_subtype_set(dst, val32);

    of_bsn_lacp_convergence_notif_convergence_status_get(src, &val8);
    of_bsn_lacp_convergence_notif_convergence_status_set(dst, val8);

    of_bsn_lacp_convergence_notif_port_no_get(src, &port_no);
    of_bsn_lacp_convergence_notif_port_no_set(dst, port_no);

    of_bsn_lacp_convergence_notif_actor_sys_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_sys_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_convergence_notif_actor_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_convergence_notif_actor_port_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_port_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_port_num_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_port_num_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_key_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_key_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_sys_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_sys_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_convergence_notif_partner_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_convergence_notif_partner_port_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_port_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_port_num_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_port_num_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_key_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_reply_t *
of_bsn_lacp_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_lacp_stats_reply_t *src)
{
    of_bsn_lacp_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_lacp_stats_entry_t src_list;
    of_list_bsn_lacp_stats_entry_t *dst_list;

    if ((dst = of_bsn_lacp_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_reply_xid_get(src, &val32);
    of_bsn_lacp_stats_reply_xid_set(dst, val32);

    of_bsn_lacp_stats_reply_flags_get(src, &val16);
    of_bsn_lacp_stats_reply_flags_set(dst, val16);

    of_bsn_lacp_stats_reply_experimenter_get(src, &val32);
    of_bsn_lacp_stats_reply_experimenter_set(dst, val32);

    of_bsn_lacp_stats_reply_subtype_get(src, &val32);
    of_bsn_lacp_stats_reply_subtype_set(dst, val32);

    of_bsn_lacp_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_lacp_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_lacp_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_lacp_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_request_t *
of_bsn_lacp_stats_request_OF_VERSION_1_3_dup(
    of_bsn_lacp_stats_request_t *src)
{
    of_bsn_lacp_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_lacp_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_request_xid_get(src, &val32);
    of_bsn_lacp_stats_request_xid_set(dst, val32);

    of_bsn_lacp_stats_request_flags_get(src, &val16);
    of_bsn_lacp_stats_request_flags_set(dst, val16);

    of_bsn_lacp_stats_request_experimenter_get(src, &val32);
    of_bsn_lacp_stats_request_experimenter_set(dst, val32);

    of_bsn_lacp_stats_request_subtype_get(src, &val32);
    of_bsn_lacp_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_log
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_log.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_log_t *
of_bsn_log_OF_VERSION_1_3_dup(
    of_bsn_log_t *src)
{
    of_bsn_log_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_log_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_log_xid_get(src, &val32);
    of_bsn_log_xid_set(dst, val32);

    of_bsn_log_experimenter_get(src, &val32);
    of_bsn_log_experimenter_set(dst, val32);

    of_bsn_log_subtype_get(src, &val32);
    of_bsn_log_subtype_set(dst, val32);

    of_bsn_log_loglevel_get(src, &val8);
    of_bsn_log_loglevel_set(dst, val8);

    of_bsn_log_data_get(src, &octets);
    of_bsn_log_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_command_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_command_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_command_reply_t *
of_bsn_lua_command_reply_OF_VERSION_1_3_dup(
    of_bsn_lua_command_reply_t *src)
{
    of_bsn_lua_command_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_command_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_command_reply_xid_get(src, &val32);
    of_bsn_lua_command_reply_xid_set(dst, val32);

    of_bsn_lua_command_reply_experimenter_get(src, &val32);
    of_bsn_lua_command_reply_experimenter_set(dst, val32);

    of_bsn_lua_command_reply_subtype_get(src, &val32);
    of_bsn_lua_command_reply_subtype_set(dst, val32);

    of_bsn_lua_command_reply_data_get(src, &octets);
    of_bsn_lua_command_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_command_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_command_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_command_request_t *
of_bsn_lua_command_request_OF_VERSION_1_3_dup(
    of_bsn_lua_command_request_t *src)
{
    of_bsn_lua_command_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_command_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_command_request_xid_get(src, &val32);
    of_bsn_lua_command_request_xid_set(dst, val32);

    of_bsn_lua_command_request_experimenter_get(src, &val32);
    of_bsn_lua_command_request_experimenter_set(dst, val32);

    of_bsn_lua_command_request_subtype_get(src, &val32);
    of_bsn_lua_command_request_subtype_set(dst, val32);

    of_bsn_lua_command_request_data_get(src, &octets);
    of_bsn_lua_command_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_notification
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_notification.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_notification_t *
of_bsn_lua_notification_OF_VERSION_1_3_dup(
    of_bsn_lua_notification_t *src)
{
    of_bsn_lua_notification_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_notification_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_notification_xid_get(src, &val32);
    of_bsn_lua_notification_xid_set(dst, val32);

    of_bsn_lua_notification_experimenter_get(src, &val32);
    of_bsn_lua_notification_experimenter_set(dst, val32);

    of_bsn_lua_notification_subtype_get(src, &val32);
    of_bsn_lua_notification_subtype_set(dst, val32);

    of_bsn_lua_notification_data_get(src, &octets);
    of_bsn_lua_notification_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_upload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_upload.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_upload_t *
of_bsn_lua_upload_OF_VERSION_1_3_dup(
    of_bsn_lua_upload_t *src)
{
    of_bsn_lua_upload_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_str64_t str64;
    of_octets_t octets;

    if ((dst = of_bsn_lua_upload_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_upload_xid_get(src, &val32);
    of_bsn_lua_upload_xid_set(dst, val32);

    of_bsn_lua_upload_experimenter_get(src, &val32);
    of_bsn_lua_upload_experimenter_set(dst, val32);

    of_bsn_lua_upload_subtype_get(src, &val32);
    of_bsn_lua_upload_subtype_set(dst, val32);

    of_bsn_lua_upload_flags_get(src, &val16);
    of_bsn_lua_upload_flags_set(dst, val16);

    of_bsn_lua_upload_filename_get(src, &str64);
    of_bsn_lua_upload_filename_set(dst, str64);

    of_bsn_lua_upload_data_get(src, &octets);
    of_bsn_lua_upload_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_reply_t *
of_bsn_pdu_rx_reply_OF_VERSION_1_3_dup(
    of_bsn_pdu_rx_reply_t *src)
{
    of_bsn_pdu_rx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_reply_xid_get(src, &val32);
    of_bsn_pdu_rx_reply_xid_set(dst, val32);

    of_bsn_pdu_rx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_rx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_rx_reply_subtype_get(src, &val32);
    of_bsn_pdu_rx_reply_subtype_set(dst, val32);

    of_bsn_pdu_rx_reply_status_get(src, &val32);
    of_bsn_pdu_rx_reply_status_set(dst, val32);

    of_bsn_pdu_rx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_rx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_rx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_rx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_request_t *
of_bsn_pdu_rx_request_OF_VERSION_1_3_dup(
    of_bsn_pdu_rx_request_t *src)
{
    of_bsn_pdu_rx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_rx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_request_xid_get(src, &val32);
    of_bsn_pdu_rx_request_xid_set(dst, val32);

    of_bsn_pdu_rx_request_experimenter_get(src, &val32);
    of_bsn_pdu_rx_request_experimenter_set(dst, val32);

    of_bsn_pdu_rx_request_subtype_get(src, &val32);
    of_bsn_pdu_rx_request_subtype_set(dst, val32);

    of_bsn_pdu_rx_request_timeout_ms_get(src, &val32);
    of_bsn_pdu_rx_request_timeout_ms_set(dst, val32);

    of_bsn_pdu_rx_request_port_no_get(src, &port_no);
    of_bsn_pdu_rx_request_port_no_set(dst, port_no);

    of_bsn_pdu_rx_request_slot_num_get(src, &val8);
    of_bsn_pdu_rx_request_slot_num_set(dst, val8);

    of_bsn_pdu_rx_request_data_get(src, &octets);
    of_bsn_pdu_rx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_timeout_t *
of_bsn_pdu_rx_timeout_OF_VERSION_1_3_dup(
    of_bsn_pdu_rx_timeout_t *src)
{
    of_bsn_pdu_rx_timeout_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_timeout_xid_get(src, &val32);
    of_bsn_pdu_rx_timeout_xid_set(dst, val32);

    of_bsn_pdu_rx_timeout_experimenter_get(src, &val32);
    of_bsn_pdu_rx_timeout_experimenter_set(dst, val32);

    of_bsn_pdu_rx_timeout_subtype_get(src, &val32);
    of_bsn_pdu_rx_timeout_subtype_set(dst, val32);

    of_bsn_pdu_rx_timeout_port_no_get(src, &port_no);
    of_bsn_pdu_rx_timeout_port_no_set(dst, port_no);

    of_bsn_pdu_rx_timeout_slot_num_get(src, &val8);
    of_bsn_pdu_rx_timeout_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_reply_t *
of_bsn_pdu_tx_reply_OF_VERSION_1_3_dup(
    of_bsn_pdu_tx_reply_t *src)
{
    of_bsn_pdu_tx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_tx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_reply_xid_get(src, &val32);
    of_bsn_pdu_tx_reply_xid_set(dst, val32);

    of_bsn_pdu_tx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_tx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_tx_reply_subtype_get(src, &val32);
    of_bsn_pdu_tx_reply_subtype_set(dst, val32);

    of_bsn_pdu_tx_reply_status_get(src, &val32);
    of_bsn_pdu_tx_reply_status_set(dst, val32);

    of_bsn_pdu_tx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_tx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_tx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_tx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_request_t *
of_bsn_pdu_tx_request_OF_VERSION_1_3_dup(
    of_bsn_pdu_tx_request_t *src)
{
    of_bsn_pdu_tx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_tx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_request_xid_get(src, &val32);
    of_bsn_pdu_tx_request_xid_set(dst, val32);

    of_bsn_pdu_tx_request_experimenter_get(src, &val32);
    of_bsn_pdu_tx_request_experimenter_set(dst, val32);

    of_bsn_pdu_tx_request_subtype_get(src, &val32);
    of_bsn_pdu_tx_request_subtype_set(dst, val32);

    of_bsn_pdu_tx_request_tx_interval_ms_get(src, &val32);
    of_bsn_pdu_tx_request_tx_interval_ms_set(dst, val32);

    of_bsn_pdu_tx_request_port_no_get(src, &port_no);
    of_bsn_pdu_tx_request_port_no_set(dst, port_no);

    of_bsn_pdu_tx_request_slot_num_get(src, &val8);
    of_bsn_pdu_tx_request_slot_num_set(dst, val8);

    of_bsn_pdu_tx_request_data_get(src, &octets);
    of_bsn_pdu_tx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_reply_t *
of_bsn_port_counter_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_port_counter_stats_reply_t *src)
{
    of_bsn_port_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_port_counter_stats_entry_t src_list;
    of_list_bsn_port_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_port_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_reply_xid_get(src, &val32);
    of_bsn_port_counter_stats_reply_xid_set(dst, val32);

    of_bsn_port_counter_stats_reply_flags_get(src, &val16);
    of_bsn_port_counter_stats_reply_flags_set(dst, val16);

    of_bsn_port_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_port_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_port_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_port_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_port_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_port_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_port_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_port_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_request_t *
of_bsn_port_counter_stats_request_OF_VERSION_1_3_dup(
    of_bsn_port_counter_stats_request_t *src)
{
    of_bsn_port_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_bsn_port_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_request_xid_get(src, &val32);
    of_bsn_port_counter_stats_request_xid_set(dst, val32);

    of_bsn_port_counter_stats_request_flags_get(src, &val16);
    of_bsn_port_counter_stats_request_flags_set(dst, val16);

    of_bsn_port_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_port_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_port_counter_stats_request_subtype_get(src, &val32);
    of_bsn_port_counter_stats_request_subtype_set(dst, val32);

    of_bsn_port_counter_stats_request_port_no_get(src, &port_no);
    of_bsn_port_counter_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_role_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_role_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_role_status_t *
of_bsn_role_status_OF_VERSION_1_3_dup(
    of_bsn_role_status_t *src)
{
    of_bsn_role_status_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint64_t val64;

    if ((dst = of_bsn_role_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_role_status_xid_get(src, &val32);
    of_bsn_role_status_xid_set(dst, val32);

    of_bsn_role_status_experimenter_get(src, &val32);
    of_bsn_role_status_experimenter_set(dst, val32);

    of_bsn_role_status_subtype_get(src, &val32);
    of_bsn_role_status_subtype_set(dst, val32);

    of_bsn_role_status_role_get(src, &val32);
    of_bsn_role_status_role_set(dst, val32);

    of_bsn_role_status_reason_get(src, &val8);
    of_bsn_role_status_reason_set(dst, val8);

    of_bsn_role_status_generation_id_get(src, &val64);
    of_bsn_role_status_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_aux_cxns_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_aux_cxns_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_aux_cxns_reply_t *
of_bsn_set_aux_cxns_reply_OF_VERSION_1_3_dup(
    of_bsn_set_aux_cxns_reply_t *src)
{
    of_bsn_set_aux_cxns_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_aux_cxns_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_aux_cxns_reply_xid_get(src, &val32);
    of_bsn_set_aux_cxns_reply_xid_set(dst, val32);

    of_bsn_set_aux_cxns_reply_experimenter_get(src, &val32);
    of_bsn_set_aux_cxns_reply_experimenter_set(dst, val32);

    of_bsn_set_aux_cxns_reply_subtype_get(src, &val32);
    of_bsn_set_aux_cxns_reply_subtype_set(dst, val32);

    of_bsn_set_aux_cxns_reply_num_aux_get(src, &val32);
    of_bsn_set_aux_cxns_reply_num_aux_set(dst, val32);

    of_bsn_set_aux_cxns_reply_status_get(src, &val32);
    of_bsn_set_aux_cxns_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_aux_cxns_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_aux_cxns_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_aux_cxns_request_t *
of_bsn_set_aux_cxns_request_OF_VERSION_1_3_dup(
    of_bsn_set_aux_cxns_request_t *src)
{
    of_bsn_set_aux_cxns_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_aux_cxns_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_aux_cxns_request_xid_get(src, &val32);
    of_bsn_set_aux_cxns_request_xid_set(dst, val32);

    of_bsn_set_aux_cxns_request_experimenter_get(src, &val32);
    of_bsn_set_aux_cxns_request_experimenter_set(dst, val32);

    of_bsn_set_aux_cxns_request_subtype_get(src, &val32);
    of_bsn_set_aux_cxns_request_subtype_set(dst, val32);

    of_bsn_set_aux_cxns_request_num_aux_get(src, &val32);
    of_bsn_set_aux_cxns_request_num_aux_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_lacp_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_lacp_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_lacp_reply_t *
of_bsn_set_lacp_reply_OF_VERSION_1_3_dup(
    of_bsn_set_lacp_reply_t *src)
{
    of_bsn_set_lacp_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_bsn_set_lacp_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_lacp_reply_xid_get(src, &val32);
    of_bsn_set_lacp_reply_xid_set(dst, val32);

    of_bsn_set_lacp_reply_experimenter_get(src, &val32);
    of_bsn_set_lacp_reply_experimenter_set(dst, val32);

    of_bsn_set_lacp_reply_subtype_get(src, &val32);
    of_bsn_set_lacp_reply_subtype_set(dst, val32);

    of_bsn_set_lacp_reply_status_get(src, &val32);
    of_bsn_set_lacp_reply_status_set(dst, val32);

    of_bsn_set_lacp_reply_port_no_get(src, &port_no);
    of_bsn_set_lacp_reply_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_lacp_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_lacp_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_lacp_request_t *
of_bsn_set_lacp_request_OF_VERSION_1_3_dup(
    of_bsn_set_lacp_request_t *src)
{
    of_bsn_set_lacp_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_set_lacp_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_lacp_request_xid_get(src, &val32);
    of_bsn_set_lacp_request_xid_set(dst, val32);

    of_bsn_set_lacp_request_experimenter_get(src, &val32);
    of_bsn_set_lacp_request_experimenter_set(dst, val32);

    of_bsn_set_lacp_request_subtype_get(src, &val32);
    of_bsn_set_lacp_request_subtype_set(dst, val32);

    of_bsn_set_lacp_request_enabled_get(src, &val8);
    of_bsn_set_lacp_request_enabled_set(dst, val8);

    of_bsn_set_lacp_request_port_no_get(src, &port_no);
    of_bsn_set_lacp_request_port_no_set(dst, port_no);

    of_bsn_set_lacp_request_actor_sys_priority_get(src, &val16);
    of_bsn_set_lacp_request_actor_sys_priority_set(dst, val16);

    of_bsn_set_lacp_request_actor_sys_mac_get(src, &mac_addr);
    of_bsn_set_lacp_request_actor_sys_mac_set(dst, mac_addr);

    of_bsn_set_lacp_request_actor_port_priority_get(src, &val16);
    of_bsn_set_lacp_request_actor_port_priority_set(dst, val16);

    of_bsn_set_lacp_request_actor_port_num_get(src, &val16);
    of_bsn_set_lacp_request_actor_port_num_set(dst, val16);

    of_bsn_set_lacp_request_actor_key_get(src, &val16);
    of_bsn_set_lacp_request_actor_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_mirroring
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_mirroring.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_mirroring_t *
of_bsn_set_mirroring_OF_VERSION_1_3_dup(
    of_bsn_set_mirroring_t *src)
{
    of_bsn_set_mirroring_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_mirroring_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_mirroring_xid_get(src, &val32);
    of_bsn_set_mirroring_xid_set(dst, val32);

    of_bsn_set_mirroring_experimenter_get(src, &val32);
    of_bsn_set_mirroring_experimenter_set(dst, val32);

    of_bsn_set_mirroring_subtype_get(src, &val32);
    of_bsn_set_mirroring_subtype_set(dst, val32);

    of_bsn_set_mirroring_report_mirror_ports_get(src, &val8);
    of_bsn_set_mirroring_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_reply_t *
of_bsn_set_pktin_suppression_reply_OF_VERSION_1_3_dup(
    of_bsn_set_pktin_suppression_reply_t *src)
{
    of_bsn_set_pktin_suppression_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_pktin_suppression_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_reply_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_status_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_request_t *
of_bsn_set_pktin_suppression_request_OF_VERSION_1_3_dup(
    of_bsn_set_pktin_suppression_request_t *src)
{
    of_bsn_set_pktin_suppression_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_bsn_set_pktin_suppression_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_request_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_request_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_request_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_request_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_request_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_request_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_request_enabled_get(src, &val8);
    of_bsn_set_pktin_suppression_request_enabled_set(dst, val8);

    of_bsn_set_pktin_suppression_request_idle_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_idle_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_hard_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_hard_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_priority_get(src, &val16);
    of_bsn_set_pktin_suppression_request_priority_set(dst, val16);

    of_bsn_set_pktin_suppression_request_cookie_get(src, &val64);
    of_bsn_set_pktin_suppression_request_cookie_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_switch_pipeline_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_switch_pipeline_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_switch_pipeline_reply_t *
of_bsn_set_switch_pipeline_reply_OF_VERSION_1_3_dup(
    of_bsn_set_switch_pipeline_reply_t *src)
{
    of_bsn_set_switch_pipeline_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_switch_pipeline_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_switch_pipeline_reply_xid_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_xid_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_experimenter_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_experimenter_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_subtype_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_subtype_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_status_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_switch_pipeline_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_switch_pipeline_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_switch_pipeline_request_t *
of_bsn_set_switch_pipeline_request_OF_VERSION_1_3_dup(
    of_bsn_set_switch_pipeline_request_t *src)
{
    of_bsn_set_switch_pipeline_request_t *dst;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_set_switch_pipeline_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_switch_pipeline_request_xid_get(src, &val32);
    of_bsn_set_switch_pipeline_request_xid_set(dst, val32);

    of_bsn_set_switch_pipeline_request_experimenter_get(src, &val32);
    of_bsn_set_switch_pipeline_request_experimenter_set(dst, val32);

    of_bsn_set_switch_pipeline_request_subtype_get(src, &val32);
    of_bsn_set_switch_pipeline_request_subtype_set(dst, val32);

    of_bsn_set_switch_pipeline_request_pipeline_get(src, &desc_str);
    of_bsn_set_switch_pipeline_request_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_reply_t *
of_bsn_switch_pipeline_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_switch_pipeline_stats_reply_t *src)
{
    of_bsn_switch_pipeline_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_switch_pipeline_stats_entry_t src_list;
    of_list_bsn_switch_pipeline_stats_entry_t *dst_list;

    if ((dst = of_bsn_switch_pipeline_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_reply_xid_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_xid_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_flags_get(src, &val16);
    of_bsn_switch_pipeline_stats_reply_flags_set(dst, val16);

    of_bsn_switch_pipeline_stats_reply_experimenter_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_experimenter_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_subtype_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_subtype_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_switch_pipeline_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_switch_pipeline_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_switch_pipeline_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_request_t *
of_bsn_switch_pipeline_stats_request_OF_VERSION_1_3_dup(
    of_bsn_switch_pipeline_stats_request_t *src)
{
    of_bsn_switch_pipeline_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_switch_pipeline_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_request_xid_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_xid_set(dst, val32);

    of_bsn_switch_pipeline_stats_request_flags_get(src, &val16);
    of_bsn_switch_pipeline_stats_request_flags_set(dst, val16);

    of_bsn_switch_pipeline_stats_request_experimenter_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_experimenter_set(dst, val32);

    of_bsn_switch_pipeline_stats_request_subtype_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_reply_t *
of_bsn_table_checksum_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_table_checksum_stats_reply_t *src)
{
    of_bsn_table_checksum_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_table_checksum_stats_entry_t src_list;
    of_list_bsn_table_checksum_stats_entry_t *dst_list;

    if ((dst = of_bsn_table_checksum_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_reply_xid_get(src, &val32);
    of_bsn_table_checksum_stats_reply_xid_set(dst, val32);

    of_bsn_table_checksum_stats_reply_flags_get(src, &val16);
    of_bsn_table_checksum_stats_reply_flags_set(dst, val16);

    of_bsn_table_checksum_stats_reply_experimenter_get(src, &val32);
    of_bsn_table_checksum_stats_reply_experimenter_set(dst, val32);

    of_bsn_table_checksum_stats_reply_subtype_get(src, &val32);
    of_bsn_table_checksum_stats_reply_subtype_set(dst, val32);

    of_bsn_table_checksum_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_table_checksum_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_table_checksum_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_table_checksum_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_request_t *
of_bsn_table_checksum_stats_request_OF_VERSION_1_3_dup(
    of_bsn_table_checksum_stats_request_t *src)
{
    of_bsn_table_checksum_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_table_checksum_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_request_xid_get(src, &val32);
    of_bsn_table_checksum_stats_request_xid_set(dst, val32);

    of_bsn_table_checksum_stats_request_flags_get(src, &val16);
    of_bsn_table_checksum_stats_request_flags_set(dst, val16);

    of_bsn_table_checksum_stats_request_experimenter_get(src, &val32);
    of_bsn_table_checksum_stats_request_experimenter_set(dst, val32);

    of_bsn_table_checksum_stats_request_subtype_get(src, &val32);
    of_bsn_table_checksum_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_set_buckets_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_set_buckets_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_set_buckets_size_t *
of_bsn_table_set_buckets_size_OF_VERSION_1_3_dup(
    of_bsn_table_set_buckets_size_t *src)
{
    of_bsn_table_set_buckets_size_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_table_set_buckets_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_set_buckets_size_xid_get(src, &val32);
    of_bsn_table_set_buckets_size_xid_set(dst, val32);

    of_bsn_table_set_buckets_size_experimenter_get(src, &val32);
    of_bsn_table_set_buckets_size_experimenter_set(dst, val32);

    of_bsn_table_set_buckets_size_subtype_get(src, &val32);
    of_bsn_table_set_buckets_size_subtype_set(dst, val32);

    of_bsn_table_set_buckets_size_table_id_get(src, &val8);
    of_bsn_table_set_buckets_size_table_id_set(dst, val8);

    of_bsn_table_set_buckets_size_buckets_size_get(src, &val32);
    of_bsn_table_set_buckets_size_buckets_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_time_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_time_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_time_reply_t *
of_bsn_time_reply_OF_VERSION_1_3_dup(
    of_bsn_time_reply_t *src)
{
    of_bsn_time_reply_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_bsn_time_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_time_reply_xid_get(src, &val32);
    of_bsn_time_reply_xid_set(dst, val32);

    of_bsn_time_reply_experimenter_get(src, &val32);
    of_bsn_time_reply_experimenter_set(dst, val32);

    of_bsn_time_reply_subtype_get(src, &val32);
    of_bsn_time_reply_subtype_set(dst, val32);

    of_bsn_time_reply_time_ms_get(src, &val64);
    of_bsn_time_reply_time_ms_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_time_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_time_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_time_request_t *
of_bsn_time_request_OF_VERSION_1_3_dup(
    of_bsn_time_request_t *src)
{
    of_bsn_time_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_time_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_time_request_xid_get(src, &val32);
    of_bsn_time_request_xid_set(dst, val32);

    of_bsn_time_request_experimenter_get(src, &val32);
    of_bsn_time_request_experimenter_set(dst, val32);

    of_bsn_time_request_subtype_get(src, &val32);
    of_bsn_time_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_reply_t *
of_bsn_virtual_port_create_reply_OF_VERSION_1_3_dup(
    of_bsn_virtual_port_create_reply_t *src)
{
    of_bsn_virtual_port_create_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_create_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_reply_xid_get(src, &val32);
    of_bsn_virtual_port_create_reply_xid_set(dst, val32);

    of_bsn_virtual_port_create_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_create_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_create_reply_status_get(src, &val32);
    of_bsn_virtual_port_create_reply_status_set(dst, val32);

    of_bsn_virtual_port_create_reply_vport_no_get(src, &val32);
    of_bsn_virtual_port_create_reply_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_request_t *
of_bsn_virtual_port_create_request_OF_VERSION_1_3_dup(
    of_bsn_virtual_port_create_request_t *src)
{
    of_bsn_virtual_port_create_request_t *dst;
    uint32_t val32;

    of_bsn_vport_q_in_q_t src_bsn_vport;
    of_bsn_vport_q_in_q_t *dst_bsn_vport;

    if ((dst = of_bsn_virtual_port_create_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_request_xid_get(src, &val32);
    of_bsn_virtual_port_create_request_xid_set(dst, val32);

    of_bsn_virtual_port_create_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_request_subtype_get(src, &val32);
    of_bsn_virtual_port_create_request_subtype_set(dst, val32);

    of_bsn_virtual_port_create_request_vport_bind(
        src, &src_bsn_vport);
    dst_bsn_vport = of_bsn_vport_q_in_q_OF_VERSION_1_3_dup(&src_bsn_vport);
    if (dst_bsn_vport == NULL) {
        of_bsn_virtual_port_create_request_delete(dst);
        return NULL;
    }
    of_bsn_virtual_port_create_request_vport_set(dst, dst_bsn_vport);
    of_bsn_vport_q_in_q_delete(dst_bsn_vport);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_reply_t *
of_bsn_virtual_port_remove_reply_OF_VERSION_1_3_dup(
    of_bsn_virtual_port_remove_reply_t *src)
{
    of_bsn_virtual_port_remove_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_reply_xid_get(src, &val32);
    of_bsn_virtual_port_remove_reply_xid_set(dst, val32);

    of_bsn_virtual_port_remove_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_reply_status_get(src, &val32);
    of_bsn_virtual_port_remove_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_request_t *
of_bsn_virtual_port_remove_request_OF_VERSION_1_3_dup(
    of_bsn_virtual_port_remove_request_t *src)
{
    of_bsn_virtual_port_remove_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_request_xid_get(src, &val32);
    of_bsn_virtual_port_remove_request_xid_set(dst, val32);

    of_bsn_virtual_port_remove_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_request_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_request_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_request_vport_no_get(src, &val32);
    of_bsn_virtual_port_remove_request_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_clear
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_clear.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_clear_t *
of_bsn_vlan_counter_clear_OF_VERSION_1_3_dup(
    of_bsn_vlan_counter_clear_t *src)
{
    of_bsn_vlan_counter_clear_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vlan_counter_clear_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_clear_xid_get(src, &val32);
    of_bsn_vlan_counter_clear_xid_set(dst, val32);

    of_bsn_vlan_counter_clear_experimenter_get(src, &val32);
    of_bsn_vlan_counter_clear_experimenter_set(dst, val32);

    of_bsn_vlan_counter_clear_subtype_get(src, &val32);
    of_bsn_vlan_counter_clear_subtype_set(dst, val32);

    of_bsn_vlan_counter_clear_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_clear_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_reply_t *
of_bsn_vlan_counter_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_vlan_counter_stats_reply_t *src)
{
    of_bsn_vlan_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_vlan_counter_stats_entry_t src_list;
    of_list_bsn_vlan_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_vlan_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_reply_xid_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_xid_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_flags_get(src, &val16);
    of_bsn_vlan_counter_stats_reply_flags_set(dst, val16);

    of_bsn_vlan_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vlan_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_vlan_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_vlan_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_request_t *
of_bsn_vlan_counter_stats_request_OF_VERSION_1_3_dup(
    of_bsn_vlan_counter_stats_request_t *src)
{
    of_bsn_vlan_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vlan_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_request_xid_get(src, &val32);
    of_bsn_vlan_counter_stats_request_xid_set(dst, val32);

    of_bsn_vlan_counter_stats_request_flags_get(src, &val16);
    of_bsn_vlan_counter_stats_request_flags_set(dst, val16);

    of_bsn_vlan_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_vlan_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_vlan_counter_stats_request_subtype_get(src, &val32);
    of_bsn_vlan_counter_stats_request_subtype_set(dst, val32);

    of_bsn_vlan_counter_stats_request_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_stats_request_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_reply_t *
of_bsn_vrf_counter_stats_reply_OF_VERSION_1_3_dup(
    of_bsn_vrf_counter_stats_reply_t *src)
{
    of_bsn_vrf_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_vrf_counter_stats_entry_t src_list;
    of_list_bsn_vrf_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_vrf_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_reply_xid_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_xid_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_flags_get(src, &val16);
    of_bsn_vrf_counter_stats_reply_flags_set(dst, val16);

    of_bsn_vrf_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vrf_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_vrf_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_vrf_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_request_t *
of_bsn_vrf_counter_stats_request_OF_VERSION_1_3_dup(
    of_bsn_vrf_counter_stats_request_t *src)
{
    of_bsn_vrf_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vrf_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_request_xid_get(src, &val32);
    of_bsn_vrf_counter_stats_request_xid_set(dst, val32);

    of_bsn_vrf_counter_stats_request_flags_get(src, &val16);
    of_bsn_vrf_counter_stats_request_flags_set(dst, val16);

    of_bsn_vrf_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_vrf_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_vrf_counter_stats_request_subtype_get(src, &val32);
    of_bsn_vrf_counter_stats_request_subtype_set(dst, val32);

    of_bsn_vrf_counter_stats_request_vrf_get(src, &val32);
    of_bsn_vrf_counter_stats_request_vrf_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_reply_t *
of_desc_stats_reply_OF_VERSION_1_3_dup(
    of_desc_stats_reply_t *src)
{
    of_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_serial_num_t ser_num;

    if ((dst = of_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_reply_xid_get(src, &val32);
    of_desc_stats_reply_xid_set(dst, val32);

    of_desc_stats_reply_flags_get(src, &val16);
    of_desc_stats_reply_flags_set(dst, val16);

    of_desc_stats_reply_mfr_desc_get(src, &desc_str);
    of_desc_stats_reply_mfr_desc_set(dst, desc_str);

    of_desc_stats_reply_hw_desc_get(src, &desc_str);
    of_desc_stats_reply_hw_desc_set(dst, desc_str);

    of_desc_stats_reply_sw_desc_get(src, &desc_str);
    of_desc_stats_reply_sw_desc_set(dst, desc_str);

    of_desc_stats_reply_serial_num_get(src, &ser_num);
    of_desc_stats_reply_serial_num_set(dst, ser_num);

    of_desc_stats_reply_dp_desc_get(src, &desc_str);
    of_desc_stats_reply_dp_desc_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_request_t *
of_desc_stats_request_OF_VERSION_1_3_dup(
    of_desc_stats_request_t *src)
{
    of_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_request_xid_get(src, &val32);
    of_desc_stats_request_xid_set(dst, val32);

    of_desc_stats_request_flags_get(src, &val16);
    of_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_echo_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_reply_t *
of_echo_reply_OF_VERSION_1_3_dup(
    of_echo_reply_t *src)
{
    of_echo_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_reply_xid_get(src, &val32);
    of_echo_reply_xid_set(dst, val32);

    of_echo_reply_data_get(src, &octets);
    of_echo_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_echo_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_request_t *
of_echo_request_OF_VERSION_1_3_dup(
    of_echo_request_t *src)
{
    of_echo_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_request_xid_get(src, &val32);
    of_echo_request_xid_set(dst, val32);

    of_echo_request_data_get(src, &octets);
    of_echo_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_features_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_reply_t *
of_features_reply_OF_VERSION_1_3_dup(
    of_features_reply_t *src)
{
    of_features_reply_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;

    if ((dst = of_features_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_reply_xid_get(src, &val32);
    of_features_reply_xid_set(dst, val32);

    of_features_reply_datapath_id_get(src, &val64);
    of_features_reply_datapath_id_set(dst, val64);

    of_features_reply_n_buffers_get(src, &val32);
    of_features_reply_n_buffers_set(dst, val32);

    of_features_reply_n_tables_get(src, &val8);
    of_features_reply_n_tables_set(dst, val8);

    of_features_reply_auxiliary_id_get(src, &val8);
    of_features_reply_auxiliary_id_set(dst, val8);

    of_features_reply_capabilities_get(src, &val32);
    of_features_reply_capabilities_set(dst, val32);

    of_features_reply_reserved_get(src, &val32);
    of_features_reply_reserved_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_features_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_request_t *
of_features_request_OF_VERSION_1_3_dup(
    of_features_request_t *src)
{
    of_features_request_t *dst;
    uint32_t val32;

    if ((dst = of_features_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_request_xid_get(src, &val32);
    of_features_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_flow_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_add_t *
of_flow_add_OF_VERSION_1_3_dup(
    of_flow_add_t *src)
{
    of_flow_add_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_add_xid_get(src, &val32);
    of_flow_add_xid_set(dst, val32);

    of_flow_add_cookie_get(src, &val64);
    of_flow_add_cookie_set(dst, val64);

    of_flow_add_cookie_mask_get(src, &val64);
    of_flow_add_cookie_mask_set(dst, val64);

    of_flow_add_table_id_get(src, &val8);
    of_flow_add_table_id_set(dst, val8);

    of_flow_add_idle_timeout_get(src, &val16);
    of_flow_add_idle_timeout_set(dst, val16);

    of_flow_add_hard_timeout_get(src, &val16);
    of_flow_add_hard_timeout_set(dst, val16);

    of_flow_add_priority_get(src, &val16);
    of_flow_add_priority_set(dst, val16);

    of_flow_add_buffer_id_get(src, &val32);
    of_flow_add_buffer_id_set(dst, val32);

    of_flow_add_out_port_get(src, &port_no);
    of_flow_add_out_port_set(dst, port_no);

    of_flow_add_out_group_get(src, &val32);
    of_flow_add_out_group_set(dst, val32);

    of_flow_add_flags_get(src, &val16);
    of_flow_add_flags_set(dst, val16);

    of_flow_add_match_get(src, &match);
    of_flow_add_match_set(dst, &match);

    of_flow_add_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_add_delete(dst);
        return NULL;
    }
    of_flow_add_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_t *
of_flow_delete_OF_VERSION_1_3_dup(
    of_flow_delete_t *src)
{
    of_flow_delete_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_xid_get(src, &val32);
    of_flow_delete_xid_set(dst, val32);

    of_flow_delete_cookie_get(src, &val64);
    of_flow_delete_cookie_set(dst, val64);

    of_flow_delete_cookie_mask_get(src, &val64);
    of_flow_delete_cookie_mask_set(dst, val64);

    of_flow_delete_table_id_get(src, &val8);
    of_flow_delete_table_id_set(dst, val8);

    of_flow_delete_idle_timeout_get(src, &val16);
    of_flow_delete_idle_timeout_set(dst, val16);

    of_flow_delete_hard_timeout_get(src, &val16);
    of_flow_delete_hard_timeout_set(dst, val16);

    of_flow_delete_priority_get(src, &val16);
    of_flow_delete_priority_set(dst, val16);

    of_flow_delete_buffer_id_get(src, &val32);
    of_flow_delete_buffer_id_set(dst, val32);

    of_flow_delete_out_port_get(src, &port_no);
    of_flow_delete_out_port_set(dst, port_no);

    of_flow_delete_out_group_get(src, &val32);
    of_flow_delete_out_group_set(dst, val32);

    of_flow_delete_flags_get(src, &val16);
    of_flow_delete_flags_set(dst, val16);

    of_flow_delete_match_get(src, &match);
    of_flow_delete_match_set(dst, &match);

    of_flow_delete_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_delete(dst);
        return NULL;
    }
    of_flow_delete_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_strict_t *
of_flow_delete_strict_OF_VERSION_1_3_dup(
    of_flow_delete_strict_t *src)
{
    of_flow_delete_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_strict_xid_get(src, &val32);
    of_flow_delete_strict_xid_set(dst, val32);

    of_flow_delete_strict_cookie_get(src, &val64);
    of_flow_delete_strict_cookie_set(dst, val64);

    of_flow_delete_strict_cookie_mask_get(src, &val64);
    of_flow_delete_strict_cookie_mask_set(dst, val64);

    of_flow_delete_strict_table_id_get(src, &val8);
    of_flow_delete_strict_table_id_set(dst, val8);

    of_flow_delete_strict_idle_timeout_get(src, &val16);
    of_flow_delete_strict_idle_timeout_set(dst, val16);

    of_flow_delete_strict_hard_timeout_get(src, &val16);
    of_flow_delete_strict_hard_timeout_set(dst, val16);

    of_flow_delete_strict_priority_get(src, &val16);
    of_flow_delete_strict_priority_set(dst, val16);

    of_flow_delete_strict_buffer_id_get(src, &val32);
    of_flow_delete_strict_buffer_id_set(dst, val32);

    of_flow_delete_strict_out_port_get(src, &port_no);
    of_flow_delete_strict_out_port_set(dst, port_no);

    of_flow_delete_strict_out_group_get(src, &val32);
    of_flow_delete_strict_out_group_set(dst, val32);

    of_flow_delete_strict_flags_get(src, &val16);
    of_flow_delete_strict_flags_set(dst, val16);

    of_flow_delete_strict_match_get(src, &match);
    of_flow_delete_strict_match_set(dst, &match);

    of_flow_delete_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_strict_delete(dst);
        return NULL;
    }
    of_flow_delete_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_mod_failed_error_msg_t *
of_flow_mod_failed_error_msg_OF_VERSION_1_3_dup(
    of_flow_mod_failed_error_msg_t *src)
{
    of_flow_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_mod_failed_error_msg_xid_get(src, &val32);
    of_flow_mod_failed_error_msg_xid_set(dst, val32);

    of_flow_mod_failed_error_msg_code_get(src, &val16);
    of_flow_mod_failed_error_msg_code_set(dst, val16);

    of_flow_mod_failed_error_msg_data_get(src, &octets);
    of_flow_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_t *
of_flow_modify_OF_VERSION_1_3_dup(
    of_flow_modify_t *src)
{
    of_flow_modify_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_xid_get(src, &val32);
    of_flow_modify_xid_set(dst, val32);

    of_flow_modify_cookie_get(src, &val64);
    of_flow_modify_cookie_set(dst, val64);

    of_flow_modify_cookie_mask_get(src, &val64);
    of_flow_modify_cookie_mask_set(dst, val64);

    of_flow_modify_table_id_get(src, &val8);
    of_flow_modify_table_id_set(dst, val8);

    of_flow_modify_idle_timeout_get(src, &val16);
    of_flow_modify_idle_timeout_set(dst, val16);

    of_flow_modify_hard_timeout_get(src, &val16);
    of_flow_modify_hard_timeout_set(dst, val16);

    of_flow_modify_priority_get(src, &val16);
    of_flow_modify_priority_set(dst, val16);

    of_flow_modify_buffer_id_get(src, &val32);
    of_flow_modify_buffer_id_set(dst, val32);

    of_flow_modify_out_port_get(src, &port_no);
    of_flow_modify_out_port_set(dst, port_no);

    of_flow_modify_out_group_get(src, &val32);
    of_flow_modify_out_group_set(dst, val32);

    of_flow_modify_flags_get(src, &val16);
    of_flow_modify_flags_set(dst, val16);

    of_flow_modify_match_get(src, &match);
    of_flow_modify_match_set(dst, &match);

    of_flow_modify_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_delete(dst);
        return NULL;
    }
    of_flow_modify_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_strict_t *
of_flow_modify_strict_OF_VERSION_1_3_dup(
    of_flow_modify_strict_t *src)
{
    of_flow_modify_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_strict_xid_get(src, &val32);
    of_flow_modify_strict_xid_set(dst, val32);

    of_flow_modify_strict_cookie_get(src, &val64);
    of_flow_modify_strict_cookie_set(dst, val64);

    of_flow_modify_strict_cookie_mask_get(src, &val64);
    of_flow_modify_strict_cookie_mask_set(dst, val64);

    of_flow_modify_strict_table_id_get(src, &val8);
    of_flow_modify_strict_table_id_set(dst, val8);

    of_flow_modify_strict_idle_timeout_get(src, &val16);
    of_flow_modify_strict_idle_timeout_set(dst, val16);

    of_flow_modify_strict_hard_timeout_get(src, &val16);
    of_flow_modify_strict_hard_timeout_set(dst, val16);

    of_flow_modify_strict_priority_get(src, &val16);
    of_flow_modify_strict_priority_set(dst, val16);

    of_flow_modify_strict_buffer_id_get(src, &val32);
    of_flow_modify_strict_buffer_id_set(dst, val32);

    of_flow_modify_strict_out_port_get(src, &port_no);
    of_flow_modify_strict_out_port_set(dst, port_no);

    of_flow_modify_strict_out_group_get(src, &val32);
    of_flow_modify_strict_out_group_set(dst, val32);

    of_flow_modify_strict_flags_get(src, &val16);
    of_flow_modify_strict_flags_set(dst, val16);

    of_flow_modify_strict_match_get(src, &match);
    of_flow_modify_strict_match_set(dst, &match);

    of_flow_modify_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_strict_delete(dst);
        return NULL;
    }
    of_flow_modify_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_removed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_removed.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_removed_t *
of_flow_removed_OF_VERSION_1_3_dup(
    of_flow_removed_t *src)
{
    of_flow_removed_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_flow_removed_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_removed_xid_get(src, &val32);
    of_flow_removed_xid_set(dst, val32);

    of_flow_removed_cookie_get(src, &val64);
    of_flow_removed_cookie_set(dst, val64);

    of_flow_removed_priority_get(src, &val16);
    of_flow_removed_priority_set(dst, val16);

    of_flow_removed_reason_get(src, &val8);
    of_flow_removed_reason_set(dst, val8);

    of_flow_removed_table_id_get(src, &val8);
    of_flow_removed_table_id_set(dst, val8);

    of_flow_removed_duration_sec_get(src, &val32);
    of_flow_removed_duration_sec_set(dst, val32);

    of_flow_removed_duration_nsec_get(src, &val32);
    of_flow_removed_duration_nsec_set(dst, val32);

    of_flow_removed_idle_timeout_get(src, &val16);
    of_flow_removed_idle_timeout_set(dst, val16);

    of_flow_removed_hard_timeout_get(src, &val16);
    of_flow_removed_hard_timeout_set(dst, val16);

    of_flow_removed_packet_count_get(src, &val64);
    of_flow_removed_packet_count_set(dst, val64);

    of_flow_removed_byte_count_get(src, &val64);
    of_flow_removed_byte_count_set(dst, val64);

    of_flow_removed_match_get(src, &match);
    of_flow_removed_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_reply_t *
of_flow_stats_reply_OF_VERSION_1_3_dup(
    of_flow_stats_reply_t *src)
{
    of_flow_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_flow_stats_entry_t src_list;
    of_list_flow_stats_entry_t *dst_list;

    if ((dst = of_flow_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_reply_xid_get(src, &val32);
    of_flow_stats_reply_xid_set(dst, val32);

    of_flow_stats_reply_flags_get(src, &val16);
    of_flow_stats_reply_flags_set(dst, val16);

    of_flow_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_flow_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_reply_delete(dst);
        return NULL;
    }
    of_flow_stats_reply_entries_set(dst, dst_list);
    of_list_flow_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_request_t *
of_flow_stats_request_OF_VERSION_1_3_dup(
    of_flow_stats_request_t *src)
{
    of_flow_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_flow_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_request_xid_get(src, &val32);
    of_flow_stats_request_xid_set(dst, val32);

    of_flow_stats_request_flags_get(src, &val16);
    of_flow_stats_request_flags_set(dst, val16);

    of_flow_stats_request_table_id_get(src, &val8);
    of_flow_stats_request_table_id_set(dst, val8);

    of_flow_stats_request_out_port_get(src, &port_no);
    of_flow_stats_request_out_port_set(dst, port_no);

    of_flow_stats_request_out_group_get(src, &val32);
    of_flow_stats_request_out_group_set(dst, val32);

    of_flow_stats_request_cookie_get(src, &val64);
    of_flow_stats_request_cookie_set(dst, val64);

    of_flow_stats_request_cookie_mask_get(src, &val64);
    of_flow_stats_request_cookie_mask_set(dst, val64);

    of_flow_stats_request_match_get(src, &match);
    of_flow_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_reply_t *
of_get_config_reply_OF_VERSION_1_3_dup(
    of_get_config_reply_t *src)
{
    of_get_config_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_reply_xid_get(src, &val32);
    of_get_config_reply_xid_set(dst, val32);

    of_get_config_reply_flags_get(src, &val16);
    of_get_config_reply_flags_set(dst, val16);

    of_get_config_reply_miss_send_len_get(src, &val16);
    of_get_config_reply_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_request_t *
of_get_config_request_OF_VERSION_1_3_dup(
    of_get_config_request_t *src)
{
    of_get_config_request_t *dst;
    uint32_t val32;

    if ((dst = of_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_request_xid_get(src, &val32);
    of_get_config_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_add_t *
of_group_add_OF_VERSION_1_3_dup(
    of_group_add_t *src)
{
    of_group_add_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_add_xid_get(src, &val32);
    of_group_add_xid_set(dst, val32);

    of_group_add_group_type_get(src, &val8);
    of_group_add_group_type_set(dst, val8);

    of_group_add_group_id_get(src, &val32);
    of_group_add_group_id_set(dst, val32);

    of_group_add_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_add_delete(dst);
        return NULL;
    }
    of_group_add_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_delete_t *
of_group_delete_OF_VERSION_1_3_dup(
    of_group_delete_t *src)
{
    of_group_delete_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_delete_xid_get(src, &val32);
    of_group_delete_xid_set(dst, val32);

    of_group_delete_group_type_get(src, &val8);
    of_group_delete_group_type_set(dst, val8);

    of_group_delete_group_id_get(src, &val32);
    of_group_delete_group_id_set(dst, val32);

    of_group_delete_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_delete_delete(dst);
        return NULL;
    }
    of_group_delete_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_reply_t *
of_group_desc_stats_reply_OF_VERSION_1_3_dup(
    of_group_desc_stats_reply_t *src)
{
    of_group_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_desc_stats_entry_t src_list;
    of_list_group_desc_stats_entry_t *dst_list;

    if ((dst = of_group_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_reply_xid_get(src, &val32);
    of_group_desc_stats_reply_xid_set(dst, val32);

    of_group_desc_stats_reply_flags_get(src, &val16);
    of_group_desc_stats_reply_flags_set(dst, val16);

    of_group_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_desc_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_group_desc_stats_reply_entries_set(dst, dst_list);
    of_list_group_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_request_t *
of_group_desc_stats_request_OF_VERSION_1_3_dup(
    of_group_desc_stats_request_t *src)
{
    of_group_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_request_xid_get(src, &val32);
    of_group_desc_stats_request_xid_set(dst, val32);

    of_group_desc_stats_request_flags_get(src, &val16);
    of_group_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_reply_t *
of_group_features_stats_reply_OF_VERSION_1_3_dup(
    of_group_features_stats_reply_t *src)
{
    of_group_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_reply_xid_get(src, &val32);
    of_group_features_stats_reply_xid_set(dst, val32);

    of_group_features_stats_reply_flags_get(src, &val16);
    of_group_features_stats_reply_flags_set(dst, val16);

    of_group_features_stats_reply_types_get(src, &val32);
    of_group_features_stats_reply_types_set(dst, val32);

    of_group_features_stats_reply_capabilities_get(src, &val32);
    of_group_features_stats_reply_capabilities_set(dst, val32);

    of_group_features_stats_reply_max_groups_all_get(src, &val32);
    of_group_features_stats_reply_max_groups_all_set(dst, val32);

    of_group_features_stats_reply_max_groups_select_get(src, &val32);
    of_group_features_stats_reply_max_groups_select_set(dst, val32);

    of_group_features_stats_reply_max_groups_indirect_get(src, &val32);
    of_group_features_stats_reply_max_groups_indirect_set(dst, val32);

    of_group_features_stats_reply_max_groups_ff_get(src, &val32);
    of_group_features_stats_reply_max_groups_ff_set(dst, val32);

    of_group_features_stats_reply_actions_all_get(src, &val32);
    of_group_features_stats_reply_actions_all_set(dst, val32);

    of_group_features_stats_reply_actions_select_get(src, &val32);
    of_group_features_stats_reply_actions_select_set(dst, val32);

    of_group_features_stats_reply_actions_indirect_get(src, &val32);
    of_group_features_stats_reply_actions_indirect_set(dst, val32);

    of_group_features_stats_reply_actions_ff_get(src, &val32);
    of_group_features_stats_reply_actions_ff_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_request_t *
of_group_features_stats_request_OF_VERSION_1_3_dup(
    of_group_features_stats_request_t *src)
{
    of_group_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_request_xid_get(src, &val32);
    of_group_features_stats_request_xid_set(dst, val32);

    of_group_features_stats_request_flags_get(src, &val16);
    of_group_features_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_mod_failed_error_msg_t *
of_group_mod_failed_error_msg_OF_VERSION_1_3_dup(
    of_group_mod_failed_error_msg_t *src)
{
    of_group_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_group_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_mod_failed_error_msg_xid_get(src, &val32);
    of_group_mod_failed_error_msg_xid_set(dst, val32);

    of_group_mod_failed_error_msg_code_get(src, &val16);
    of_group_mod_failed_error_msg_code_set(dst, val16);

    of_group_mod_failed_error_msg_data_get(src, &octets);
    of_group_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_group_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_modify_t *
of_group_modify_OF_VERSION_1_3_dup(
    of_group_modify_t *src)
{
    of_group_modify_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_modify_xid_get(src, &val32);
    of_group_modify_xid_set(dst, val32);

    of_group_modify_group_type_get(src, &val8);
    of_group_modify_group_type_set(dst, val8);

    of_group_modify_group_id_get(src, &val32);
    of_group_modify_group_id_set(dst, val32);

    of_group_modify_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_modify_delete(dst);
        return NULL;
    }
    of_group_modify_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_reply_t *
of_group_stats_reply_OF_VERSION_1_3_dup(
    of_group_stats_reply_t *src)
{
    of_group_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_stats_entry_t src_list;
    of_list_group_stats_entry_t *dst_list;

    if ((dst = of_group_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_reply_xid_get(src, &val32);
    of_group_stats_reply_xid_set(dst, val32);

    of_group_stats_reply_flags_get(src, &val16);
    of_group_stats_reply_flags_set(dst, val16);

    of_group_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_reply_delete(dst);
        return NULL;
    }
    of_group_stats_reply_entries_set(dst, dst_list);
    of_list_group_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_request_t *
of_group_stats_request_OF_VERSION_1_3_dup(
    of_group_stats_request_t *src)
{
    of_group_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_request_xid_get(src, &val32);
    of_group_stats_request_xid_set(dst, val32);

    of_group_stats_request_flags_get(src, &val16);
    of_group_stats_request_flags_set(dst, val16);

    of_group_stats_request_group_id_get(src, &val32);
    of_group_stats_request_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_header
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_header.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_header_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_hello
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_t *
of_hello_OF_VERSION_1_3_dup(
    of_hello_t *src)
{
    of_hello_t *dst;
    uint32_t val32;

    of_list_hello_elem_t src_list;
    of_list_hello_elem_t *dst_list;

    if ((dst = of_hello_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_xid_get(src, &val32);
    of_hello_xid_set(dst, val32);

    of_hello_elements_bind(
        src, &src_list);
    dst_list = of_list_hello_elem_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_hello_delete(dst);
        return NULL;
    }
    of_hello_elements_set(dst, dst_list);
    of_list_hello_elem_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_hello_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_failed_error_msg_t *
of_hello_failed_error_msg_OF_VERSION_1_3_dup(
    of_hello_failed_error_msg_t *src)
{
    of_hello_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_hello_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_failed_error_msg_xid_get(src, &val32);
    of_hello_failed_error_msg_xid_set(dst, val32);

    of_hello_failed_error_msg_code_get(src, &val16);
    of_hello_failed_error_msg_code_set(dst, val16);

    of_hello_failed_error_msg_data_get(src, &octets);
    of_hello_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_stats_reply_t *
of_meter_config_stats_reply_OF_VERSION_1_3_dup(
    of_meter_config_stats_reply_t *src)
{
    of_meter_config_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_config_t src_list;
    of_list_meter_config_t *dst_list;

    if ((dst = of_meter_config_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_stats_reply_xid_get(src, &val32);
    of_meter_config_stats_reply_xid_set(dst, val32);

    of_meter_config_stats_reply_flags_get(src, &val16);
    of_meter_config_stats_reply_flags_set(dst, val16);

    of_meter_config_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_config_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_config_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_config_stats_reply_entries_set(dst, dst_list);
    of_list_meter_config_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_stats_request_t *
of_meter_config_stats_request_OF_VERSION_1_3_dup(
    of_meter_config_stats_request_t *src)
{
    of_meter_config_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_config_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_stats_request_xid_get(src, &val32);
    of_meter_config_stats_request_xid_set(dst, val32);

    of_meter_config_stats_request_flags_get(src, &val16);
    of_meter_config_stats_request_flags_set(dst, val16);

    of_meter_config_stats_request_meter_id_get(src, &val32);
    of_meter_config_stats_request_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_stats_reply_t *
of_meter_features_stats_reply_OF_VERSION_1_3_dup(
    of_meter_features_stats_reply_t *src)
{
    of_meter_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_meter_features_t src_features;
    of_meter_features_t *dst_features;

    if ((dst = of_meter_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_stats_reply_xid_get(src, &val32);
    of_meter_features_stats_reply_xid_set(dst, val32);

    of_meter_features_stats_reply_flags_get(src, &val16);
    of_meter_features_stats_reply_flags_set(dst, val16);

    of_meter_features_stats_reply_features_bind(
        src, &src_features);
    dst_features = of_meter_features_OF_VERSION_1_3_dup(&src_features);
    if (dst_features == NULL) {
        of_meter_features_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_features_stats_reply_features_set(dst, dst_features);
    of_meter_features_delete(dst_features);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_stats_request_t *
of_meter_features_stats_request_OF_VERSION_1_3_dup(
    of_meter_features_stats_request_t *src)
{
    of_meter_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_stats_request_xid_get(src, &val32);
    of_meter_features_stats_request_xid_set(dst, val32);

    of_meter_features_stats_request_flags_get(src, &val16);
    of_meter_features_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_meter_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_mod_t *
of_meter_mod_OF_VERSION_1_3_dup(
    of_meter_mod_t *src)
{
    of_meter_mod_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_band_t src_list;
    of_list_meter_band_t *dst_list;

    if ((dst = of_meter_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_mod_xid_get(src, &val32);
    of_meter_mod_xid_set(dst, val32);

    of_meter_mod_flags_get(src, &val16);
    of_meter_mod_flags_set(dst, val16);

    of_meter_mod_meter_id_get(src, &val32);
    of_meter_mod_meter_id_set(dst, val32);

    of_meter_mod_meters_bind(
        src, &src_list);
    dst_list = of_list_meter_band_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_mod_delete(dst);
        return NULL;
    }
    of_meter_mod_meters_set(dst, dst_list);
    of_list_meter_band_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_mod_failed_error_msg_t *
of_meter_mod_failed_error_msg_OF_VERSION_1_3_dup(
    of_meter_mod_failed_error_msg_t *src)
{
    of_meter_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_meter_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_mod_failed_error_msg_xid_get(src, &val32);
    of_meter_mod_failed_error_msg_xid_set(dst, val32);

    of_meter_mod_failed_error_msg_code_get(src, &val16);
    of_meter_mod_failed_error_msg_code_set(dst, val16);

    of_meter_mod_failed_error_msg_data_get(src, &octets);
    of_meter_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_reply_t *
of_meter_stats_reply_OF_VERSION_1_3_dup(
    of_meter_stats_reply_t *src)
{
    of_meter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_stats_t src_list;
    of_list_meter_stats_t *dst_list;

    if ((dst = of_meter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_reply_xid_get(src, &val32);
    of_meter_stats_reply_xid_set(dst, val32);

    of_meter_stats_reply_flags_get(src, &val16);
    of_meter_stats_reply_flags_set(dst, val16);

    of_meter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_stats_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_stats_reply_entries_set(dst, dst_list);
    of_list_meter_stats_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_request_t *
of_meter_stats_request_OF_VERSION_1_3_dup(
    of_meter_stats_request_t *src)
{
    of_meter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_request_xid_get(src, &val32);
    of_meter_stats_request_xid_set(dst, val32);

    of_meter_stats_request_flags_get(src, &val16);
    of_meter_stats_request_flags_set(dst, val16);

    of_meter_stats_request_meter_id_get(src, &val32);
    of_meter_stats_request_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_packet_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_in_t *
of_packet_in_OF_VERSION_1_3_dup(
    of_packet_in_t *src)
{
    of_packet_in_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    uint64_t val64;
    of_match_t match;
    of_octets_t octets;

    if ((dst = of_packet_in_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_in_xid_get(src, &val32);
    of_packet_in_xid_set(dst, val32);

    of_packet_in_buffer_id_get(src, &val32);
    of_packet_in_buffer_id_set(dst, val32);

    of_packet_in_total_len_get(src, &val16);
    of_packet_in_total_len_set(dst, val16);

    of_packet_in_reason_get(src, &val8);
    of_packet_in_reason_set(dst, val8);

    of_packet_in_table_id_get(src, &val8);
    of_packet_in_table_id_set(dst, val8);

    of_packet_in_cookie_get(src, &val64);
    of_packet_in_cookie_set(dst, val64);

    of_packet_in_match_get(src, &match);
    of_packet_in_match_set(dst, &match);

    of_packet_in_data_get(src, &octets);
    of_packet_in_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_out_t *
of_packet_out_OF_VERSION_1_3_dup(
    of_packet_out_t *src)
{
    of_packet_out_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;
    of_octets_t octets;

    if ((dst = of_packet_out_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_out_xid_get(src, &val32);
    of_packet_out_xid_set(dst, val32);

    of_packet_out_buffer_id_get(src, &val32);
    of_packet_out_buffer_id_set(dst, val32);

    of_packet_out_in_port_get(src, &port_no);
    of_packet_out_in_port_set(dst, port_no);

    of_packet_out_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_out_delete(dst);
        return NULL;
    }
    of_packet_out_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    of_packet_out_data_get(src, &octets);
    of_packet_out_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_stats_reply_t *
of_port_desc_stats_reply_OF_VERSION_1_3_dup(
    of_port_desc_stats_reply_t *src)
{
    of_port_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_desc_t src_list;
    of_list_port_desc_t *dst_list;

    if ((dst = of_port_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_stats_reply_xid_get(src, &val32);
    of_port_desc_stats_reply_xid_set(dst, val32);

    of_port_desc_stats_reply_flags_get(src, &val16);
    of_port_desc_stats_reply_flags_set(dst, val16);

    of_port_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_desc_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_port_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_port_desc_stats_reply_entries_set(dst, dst_list);
    of_list_port_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_stats_request_t *
of_port_desc_stats_request_OF_VERSION_1_3_dup(
    of_port_desc_stats_request_t *src)
{
    of_port_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_port_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_stats_request_xid_get(src, &val32);
    of_port_desc_stats_request_xid_set(dst, val32);

    of_port_desc_stats_request_flags_get(src, &val16);
    of_port_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_t *
of_port_mod_OF_VERSION_1_3_dup(
    of_port_mod_t *src)
{
    of_port_mod_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;

    if ((dst = of_port_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_xid_get(src, &val32);
    of_port_mod_xid_set(dst, val32);

    of_port_mod_port_no_get(src, &port_no);
    of_port_mod_port_no_set(dst, port_no);

    of_port_mod_hw_addr_get(src, &mac_addr);
    of_port_mod_hw_addr_set(dst, mac_addr);

    of_port_mod_config_get(src, &val32);
    of_port_mod_config_set(dst, val32);

    of_port_mod_mask_get(src, &val32);
    of_port_mod_mask_set(dst, val32);

    of_port_mod_advertise_get(src, &val32);
    of_port_mod_advertise_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_failed_error_msg_t *
of_port_mod_failed_error_msg_OF_VERSION_1_3_dup(
    of_port_mod_failed_error_msg_t *src)
{
    of_port_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_port_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_failed_error_msg_xid_get(src, &val32);
    of_port_mod_failed_error_msg_xid_set(dst, val32);

    of_port_mod_failed_error_msg_code_get(src, &val16);
    of_port_mod_failed_error_msg_code_set(dst, val16);

    of_port_mod_failed_error_msg_data_get(src, &octets);
    of_port_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_reply_t *
of_port_stats_reply_OF_VERSION_1_3_dup(
    of_port_stats_reply_t *src)
{
    of_port_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_stats_entry_t src_list;
    of_list_port_stats_entry_t *dst_list;

    if ((dst = of_port_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_reply_xid_get(src, &val32);
    of_port_stats_reply_xid_set(dst, val32);

    of_port_stats_reply_flags_get(src, &val16);
    of_port_stats_reply_flags_set(dst, val16);

    of_port_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_reply_delete(dst);
        return NULL;
    }
    of_port_stats_reply_entries_set(dst, dst_list);
    of_list_port_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_request_t *
of_port_stats_request_OF_VERSION_1_3_dup(
    of_port_stats_request_t *src)
{
    of_port_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_port_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_request_xid_get(src, &val32);
    of_port_stats_request_xid_set(dst, val32);

    of_port_stats_request_flags_get(src, &val16);
    of_port_stats_request_flags_set(dst, val16);

    of_port_stats_request_port_no_get(src, &port_no);
    of_port_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_port_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_status_t *
of_port_status_OF_VERSION_1_3_dup(
    of_port_status_t *src)
{
    of_port_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_port_desc_t src_port_desc;
    of_port_desc_t *dst_port_desc;

    if ((dst = of_port_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_status_xid_get(src, &val32);
    of_port_status_xid_set(dst, val32);

    of_port_status_reason_get(src, &val8);
    of_port_status_reason_set(dst, val8);

    of_port_status_desc_bind(
        src, &src_port_desc);
    dst_port_desc = of_port_desc_OF_VERSION_1_3_dup(&src_port_desc);
    if (dst_port_desc == NULL) {
        of_port_status_delete(dst);
        return NULL;
    }
    of_port_status_desc_set(dst, dst_port_desc);
    of_port_desc_delete(dst_port_desc);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_reply_t *
of_queue_get_config_reply_OF_VERSION_1_3_dup(
    of_queue_get_config_reply_t *src)
{
    of_queue_get_config_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_packet_queue_t src_list;
    of_list_packet_queue_t *dst_list;

    if ((dst = of_queue_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_reply_xid_get(src, &val32);
    of_queue_get_config_reply_xid_set(dst, val32);

    of_queue_get_config_reply_port_get(src, &port_no);
    of_queue_get_config_reply_port_set(dst, port_no);

    of_queue_get_config_reply_queues_bind(
        src, &src_list);
    dst_list = of_list_packet_queue_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_get_config_reply_delete(dst);
        return NULL;
    }
    of_queue_get_config_reply_queues_set(dst, dst_list);
    of_list_packet_queue_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_get_config_request_t *
of_queue_get_config_request_OF_VERSION_1_3_dup(
    of_queue_get_config_request_t *src)
{
    of_queue_get_config_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_queue_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_get_config_request_xid_get(src, &val32);
    of_queue_get_config_request_xid_set(dst, val32);

    of_queue_get_config_request_port_get(src, &port_no);
    of_queue_get_config_request_port_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_queue_op_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_op_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_op_failed_error_msg_t *
of_queue_op_failed_error_msg_OF_VERSION_1_3_dup(
    of_queue_op_failed_error_msg_t *src)
{
    of_queue_op_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_queue_op_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_op_failed_error_msg_xid_get(src, &val32);
    of_queue_op_failed_error_msg_xid_set(dst, val32);

    of_queue_op_failed_error_msg_code_get(src, &val16);
    of_queue_op_failed_error_msg_code_set(dst, val16);

    of_queue_op_failed_error_msg_data_get(src, &octets);
    of_queue_op_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_reply_t *
of_queue_stats_reply_OF_VERSION_1_3_dup(
    of_queue_stats_reply_t *src)
{
    of_queue_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_stats_entry_t src_list;
    of_list_queue_stats_entry_t *dst_list;

    if ((dst = of_queue_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_reply_xid_get(src, &val32);
    of_queue_stats_reply_xid_set(dst, val32);

    of_queue_stats_reply_flags_get(src, &val16);
    of_queue_stats_reply_flags_set(dst, val16);

    of_queue_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_stats_reply_entries_set(dst, dst_list);
    of_list_queue_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_request_t *
of_queue_stats_request_OF_VERSION_1_3_dup(
    of_queue_stats_request_t *src)
{
    of_queue_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_request_xid_get(src, &val32);
    of_queue_stats_request_xid_set(dst, val32);

    of_queue_stats_request_flags_get(src, &val16);
    of_queue_stats_request_flags_set(dst, val16);

    of_queue_stats_request_port_no_get(src, &port_no);
    of_queue_stats_request_port_no_set(dst, port_no);

    of_queue_stats_request_queue_id_get(src, &val32);
    of_queue_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_role_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_reply_t *
of_role_reply_OF_VERSION_1_3_dup(
    of_role_reply_t *src)
{
    of_role_reply_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_reply_xid_get(src, &val32);
    of_role_reply_xid_set(dst, val32);

    of_role_reply_role_get(src, &val32);
    of_role_reply_role_set(dst, val32);

    of_role_reply_generation_id_get(src, &val64);
    of_role_reply_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_t *
of_role_request_OF_VERSION_1_3_dup(
    of_role_request_t *src)
{
    of_role_request_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_xid_get(src, &val32);
    of_role_request_xid_set(dst, val32);

    of_role_request_role_get(src, &val32);
    of_role_request_role_set(dst, val32);

    of_role_request_generation_id_get(src, &val64);
    of_role_request_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_failed_error_msg_t *
of_role_request_failed_error_msg_OF_VERSION_1_3_dup(
    of_role_request_failed_error_msg_t *src)
{
    of_role_request_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_role_request_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_failed_error_msg_xid_get(src, &val32);
    of_role_request_failed_error_msg_xid_set(dst, val32);

    of_role_request_failed_error_msg_code_get(src, &val16);
    of_role_request_failed_error_msg_code_set(dst, val16);

    of_role_request_failed_error_msg_data_get(src, &octets);
    of_role_request_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_set_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_set_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_set_config_t *
of_set_config_OF_VERSION_1_3_dup(
    of_set_config_t *src)
{
    of_set_config_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_set_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_set_config_xid_get(src, &val32);
    of_set_config_xid_set(dst, val32);

    of_set_config_flags_get(src, &val16);
    of_set_config_flags_set(dst, val16);

    of_set_config_miss_send_len_get(src, &val16);
    of_set_config_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_switch_config_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_switch_config_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_switch_config_failed_error_msg_t *
of_switch_config_failed_error_msg_OF_VERSION_1_3_dup(
    of_switch_config_failed_error_msg_t *src)
{
    of_switch_config_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_switch_config_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_switch_config_failed_error_msg_xid_get(src, &val32);
    of_switch_config_failed_error_msg_xid_set(dst, val32);

    of_switch_config_failed_error_msg_code_get(src, &val16);
    of_switch_config_failed_error_msg_code_set(dst, val16);

    of_switch_config_failed_error_msg_data_get(src, &octets);
    of_switch_config_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_failed_error_msg_t *
of_table_features_failed_error_msg_OF_VERSION_1_3_dup(
    of_table_features_failed_error_msg_t *src)
{
    of_table_features_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_features_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_failed_error_msg_xid_get(src, &val32);
    of_table_features_failed_error_msg_xid_set(dst, val32);

    of_table_features_failed_error_msg_code_get(src, &val16);
    of_table_features_failed_error_msg_code_set(dst, val16);

    of_table_features_failed_error_msg_data_get(src, &octets);
    of_table_features_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_stats_reply_t *
of_table_features_stats_reply_OF_VERSION_1_3_dup(
    of_table_features_stats_reply_t *src)
{
    of_table_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_features_t src_list;
    of_list_table_features_t *dst_list;

    if ((dst = of_table_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_stats_reply_xid_get(src, &val32);
    of_table_features_stats_reply_xid_set(dst, val32);

    of_table_features_stats_reply_flags_get(src, &val16);
    of_table_features_stats_reply_flags_set(dst, val16);

    of_table_features_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_features_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_stats_reply_delete(dst);
        return NULL;
    }
    of_table_features_stats_reply_entries_set(dst, dst_list);
    of_list_table_features_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_stats_request_t *
of_table_features_stats_request_OF_VERSION_1_3_dup(
    of_table_features_stats_request_t *src)
{
    of_table_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_features_t src_list;
    of_list_table_features_t *dst_list;

    if ((dst = of_table_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_stats_request_xid_get(src, &val32);
    of_table_features_stats_request_xid_set(dst, val32);

    of_table_features_stats_request_flags_get(src, &val16);
    of_table_features_stats_request_flags_set(dst, val16);

    of_table_features_stats_request_entries_bind(
        src, &src_list);
    dst_list = of_list_table_features_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_stats_request_delete(dst);
        return NULL;
    }
    of_table_features_stats_request_entries_set(dst, dst_list);
    of_list_table_features_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_t *
of_table_mod_OF_VERSION_1_3_dup(
    of_table_mod_t *src)
{
    of_table_mod_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_table_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_xid_get(src, &val32);
    of_table_mod_xid_set(dst, val32);

    of_table_mod_table_id_get(src, &val8);
    of_table_mod_table_id_set(dst, val8);

    of_table_mod_config_get(src, &val32);
    of_table_mod_config_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_failed_error_msg_t *
of_table_mod_failed_error_msg_OF_VERSION_1_3_dup(
    of_table_mod_failed_error_msg_t *src)
{
    of_table_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_failed_error_msg_xid_get(src, &val32);
    of_table_mod_failed_error_msg_xid_set(dst, val32);

    of_table_mod_failed_error_msg_code_get(src, &val16);
    of_table_mod_failed_error_msg_code_set(dst, val16);

    of_table_mod_failed_error_msg_data_get(src, &octets);
    of_table_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_reply_t *
of_table_stats_reply_OF_VERSION_1_3_dup(
    of_table_stats_reply_t *src)
{
    of_table_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_stats_entry_t src_list;
    of_list_table_stats_entry_t *dst_list;

    if ((dst = of_table_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_reply_xid_get(src, &val32);
    of_table_stats_reply_xid_set(dst, val32);

    of_table_stats_reply_flags_get(src, &val16);
    of_table_stats_reply_flags_set(dst, val16);

    of_table_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_stats_entry_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_stats_reply_delete(dst);
        return NULL;
    }
    of_table_stats_reply_entries_set(dst, dst_list);
    of_list_table_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_request_t *
of_table_stats_request_OF_VERSION_1_3_dup(
    of_table_stats_request_t *src)
{
    of_table_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_request_xid_get(src, &val32);
    of_table_stats_request_xid_set(dst, val32);

    of_table_stats_request_flags_get(src, &val16);
    of_table_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate a super class object of type of_action
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_NICIRA_DEC_TTL) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_PBB) {
        return of_action_pop_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_MPLS) {
        return of_action_pop_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_IN) {
        return of_action_copy_ttl_in_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_OUTPUT) {
        return of_action_output_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_FIELD) {
        return of_action_set_field_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_QUEUE) {
        return of_action_set_queue_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_NW_TTL) {
        return of_action_dec_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_VLAN) {
        return of_action_pop_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_VLAN) {
        return of_action_push_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_MPLS_TTL) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_SET_TUNNEL_DST) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TTL) {
        return of_action_set_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_GENTABLE) {
        return of_action_bsn_gentable_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_MIRROR) {
        return of_action_bsn_mirror_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_GROUP) {
        return of_action_group_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_MPLS) {
        return of_action_push_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_OUT) {
        return of_action_copy_ttl_out_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_CHECKSUM) {
        return of_action_bsn_checksum_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_PBB) {
        return of_action_push_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_MPLS_TTL) {
        return of_action_set_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_checksum_t *
of_action_bsn_checksum_OF_VERSION_1_3_dup(
    of_action_bsn_checksum_t *src)
{
    of_action_bsn_checksum_t *dst;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_action_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_checksum_experimenter_get(src, &val32);
    of_action_bsn_checksum_experimenter_set(dst, val32);

    of_action_bsn_checksum_subtype_get(src, &val32);
    of_action_bsn_checksum_subtype_set(dst, val32);

    of_action_bsn_checksum_checksum_get(src, &checksum_128);
    of_action_bsn_checksum_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_gentable
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_gentable.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_gentable_t *
of_action_bsn_gentable_OF_VERSION_1_3_dup(
    of_action_bsn_gentable_t *src)
{
    of_action_bsn_gentable_t *dst;
    uint32_t val32;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_action_bsn_gentable_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_gentable_experimenter_get(src, &val32);
    of_action_bsn_gentable_experimenter_set(dst, val32);

    of_action_bsn_gentable_subtype_get(src, &val32);
    of_action_bsn_gentable_subtype_set(dst, val32);

    of_action_bsn_gentable_table_id_get(src, &val32);
    of_action_bsn_gentable_table_id_set(dst, val32);

    of_action_bsn_gentable_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_action_bsn_gentable_delete(dst);
        return NULL;
    }
    of_action_bsn_gentable_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_mirror_t *
of_action_bsn_mirror_OF_VERSION_1_3_dup(
    of_action_bsn_mirror_t *src)
{
    of_action_bsn_mirror_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_action_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_mirror_experimenter_get(src, &val32);
    of_action_bsn_mirror_experimenter_set(dst, val32);

    of_action_bsn_mirror_subtype_get(src, &val32);
    of_action_bsn_mirror_subtype_set(dst, val32);

    of_action_bsn_mirror_dest_port_get(src, &val32);
    of_action_bsn_mirror_dest_port_set(dst, val32);

    of_action_bsn_mirror_vlan_tag_get(src, &val32);
    of_action_bsn_mirror_vlan_tag_set(dst, val32);

    of_action_bsn_mirror_copy_stage_get(src, &val8);
    of_action_bsn_mirror_copy_stage_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_set_tunnel_dst_t *
of_action_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(
    of_action_bsn_set_tunnel_dst_t *src)
{
    of_action_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_bsn_set_tunnel_dst_subtype_set(dst, val32);

    of_action_bsn_set_tunnel_dst_dst_get(src, &val32);
    of_action_bsn_set_tunnel_dst_dst_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_in_t *
of_action_copy_ttl_in_OF_VERSION_1_3_dup(
    of_action_copy_ttl_in_t *src)
{
    of_action_copy_ttl_in_t *dst;

    if ((dst = of_action_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_out_t *
of_action_copy_ttl_out_OF_VERSION_1_3_dup(
    of_action_copy_ttl_out_t *src)
{
    of_action_copy_ttl_out_t *dst;

    if ((dst = of_action_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_mpls_ttl_t *
of_action_dec_mpls_ttl_OF_VERSION_1_3_dup(
    of_action_dec_mpls_ttl_t *src)
{
    of_action_dec_mpls_ttl_t *dst;

    if ((dst = of_action_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_nw_ttl_t *
of_action_dec_nw_ttl_OF_VERSION_1_3_dup(
    of_action_dec_nw_ttl_t *src)
{
    of_action_dec_nw_ttl_t *dst;

    if ((dst = of_action_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_group_t *
of_action_group_OF_VERSION_1_3_dup(
    of_action_group_t *src)
{
    of_action_group_t *dst;
    uint32_t val32;

    if ((dst = of_action_group_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_group_group_id_get(src, &val32);
    of_action_group_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_action_id
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_id_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_ID_BSN_SET_TUNNEL_DST) {
        return of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_CHECKSUM) {
        return of_action_id_bsn_checksum_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_GROUP) {
        return of_action_id_group_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_VLAN) {
        return of_action_id_push_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_PBB) {
        return of_action_id_pop_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_MIRROR) {
        return of_action_id_bsn_mirror_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_MPLS) {
        return of_action_id_push_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_DEC_MPLS_TTL) {
        return of_action_id_dec_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_FIELD) {
        return of_action_id_set_field_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_OUTPUT) {
        return of_action_id_output_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_NICIRA_DEC_TTL) {
        return of_action_id_nicira_dec_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_COPY_TTL_OUT) {
        return of_action_id_copy_ttl_out_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_QUEUE) {
        return of_action_id_set_queue_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_COPY_TTL_IN) {
        return of_action_id_copy_ttl_in_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_GENTABLE) {
        return of_action_id_bsn_gentable_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_MPLS) {
        return of_action_id_pop_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_PBB) {
        return of_action_id_push_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_VLAN) {
        return of_action_id_pop_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_MPLS_TTL) {
        return of_action_id_set_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_DEC_NW_TTL) {
        return of_action_id_dec_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_NW_TTL) {
        return of_action_id_set_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_id_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_checksum_t *
of_action_id_bsn_checksum_OF_VERSION_1_3_dup(
    of_action_id_bsn_checksum_t *src)
{
    of_action_id_bsn_checksum_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_checksum_experimenter_get(src, &val32);
    of_action_id_bsn_checksum_experimenter_set(dst, val32);

    of_action_id_bsn_checksum_subtype_get(src, &val32);
    of_action_id_bsn_checksum_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_gentable
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_gentable.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_gentable_t *
of_action_id_bsn_gentable_OF_VERSION_1_3_dup(
    of_action_id_bsn_gentable_t *src)
{
    of_action_id_bsn_gentable_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_gentable_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_gentable_experimenter_get(src, &val32);
    of_action_id_bsn_gentable_experimenter_set(dst, val32);

    of_action_id_bsn_gentable_subtype_get(src, &val32);
    of_action_id_bsn_gentable_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_mirror_t *
of_action_id_bsn_mirror_OF_VERSION_1_3_dup(
    of_action_id_bsn_mirror_t *src)
{
    of_action_id_bsn_mirror_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_mirror_experimenter_get(src, &val32);
    of_action_id_bsn_mirror_experimenter_set(dst, val32);

    of_action_id_bsn_mirror_subtype_get(src, &val32);
    of_action_id_bsn_mirror_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_set_tunnel_dst_t *
of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(
    of_action_id_bsn_set_tunnel_dst_t *src)
{
    of_action_id_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_id_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_id_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_id_bsn_set_tunnel_dst_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_copy_ttl_in_t *
of_action_id_copy_ttl_in_OF_VERSION_1_3_dup(
    of_action_id_copy_ttl_in_t *src)
{
    of_action_id_copy_ttl_in_t *dst;

    if ((dst = of_action_id_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_copy_ttl_out_t *
of_action_id_copy_ttl_out_OF_VERSION_1_3_dup(
    of_action_id_copy_ttl_out_t *src)
{
    of_action_id_copy_ttl_out_t *dst;

    if ((dst = of_action_id_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_dec_mpls_ttl_t *
of_action_id_dec_mpls_ttl_OF_VERSION_1_3_dup(
    of_action_id_dec_mpls_ttl_t *src)
{
    of_action_id_dec_mpls_ttl_t *dst;

    if ((dst = of_action_id_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_dec_nw_ttl_t *
of_action_id_dec_nw_ttl_OF_VERSION_1_3_dup(
    of_action_id_dec_nw_ttl_t *src)
{
    of_action_id_dec_nw_ttl_t *dst;

    if ((dst = of_action_id_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_group_t *
of_action_id_group_OF_VERSION_1_3_dup(
    of_action_id_group_t *src)
{
    of_action_id_group_t *dst;

    if ((dst = of_action_id_group_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_nicira_dec_ttl_t *
of_action_id_nicira_dec_ttl_OF_VERSION_1_3_dup(
    of_action_id_nicira_dec_ttl_t *src)
{
    of_action_id_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_id_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_id_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_id_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_id_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_output_t *
of_action_id_output_OF_VERSION_1_3_dup(
    of_action_id_output_t *src)
{
    of_action_id_output_t *dst;

    if ((dst = of_action_id_output_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_mpls_t *
of_action_id_pop_mpls_OF_VERSION_1_3_dup(
    of_action_id_pop_mpls_t *src)
{
    of_action_id_pop_mpls_t *dst;

    if ((dst = of_action_id_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_pbb_t *
of_action_id_pop_pbb_OF_VERSION_1_3_dup(
    of_action_id_pop_pbb_t *src)
{
    of_action_id_pop_pbb_t *dst;

    if ((dst = of_action_id_pop_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_vlan_t *
of_action_id_pop_vlan_OF_VERSION_1_3_dup(
    of_action_id_pop_vlan_t *src)
{
    of_action_id_pop_vlan_t *dst;

    if ((dst = of_action_id_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_mpls_t *
of_action_id_push_mpls_OF_VERSION_1_3_dup(
    of_action_id_push_mpls_t *src)
{
    of_action_id_push_mpls_t *dst;

    if ((dst = of_action_id_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_pbb_t *
of_action_id_push_pbb_OF_VERSION_1_3_dup(
    of_action_id_push_pbb_t *src)
{
    of_action_id_push_pbb_t *dst;

    if ((dst = of_action_id_push_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_vlan_t *
of_action_id_push_vlan_OF_VERSION_1_3_dup(
    of_action_id_push_vlan_t *src)
{
    of_action_id_push_vlan_t *dst;

    if ((dst = of_action_id_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_field_t *
of_action_id_set_field_OF_VERSION_1_3_dup(
    of_action_id_set_field_t *src)
{
    of_action_id_set_field_t *dst;

    if ((dst = of_action_id_set_field_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_mpls_ttl_t *
of_action_id_set_mpls_ttl_OF_VERSION_1_3_dup(
    of_action_id_set_mpls_ttl_t *src)
{
    of_action_id_set_mpls_ttl_t *dst;

    if ((dst = of_action_id_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_nw_ttl_t *
of_action_id_set_nw_ttl_OF_VERSION_1_3_dup(
    of_action_id_set_nw_ttl_t *src)
{
    of_action_id_set_nw_ttl_t *dst;

    if ((dst = of_action_id_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_queue_t *
of_action_id_set_queue_OF_VERSION_1_3_dup(
    of_action_id_set_queue_t *src)
{
    of_action_id_set_queue_t *dst;

    if ((dst = of_action_id_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_nicira_dec_ttl_t *
of_action_nicira_dec_ttl_OF_VERSION_1_3_dup(
    of_action_nicira_dec_ttl_t *src)
{
    of_action_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_output_t *
of_action_output_OF_VERSION_1_3_dup(
    of_action_output_t *src)
{
    of_action_output_t *dst;
    of_port_no_t port_no;
    uint16_t val16;

    if ((dst = of_action_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_output_port_get(src, &port_no);
    of_action_output_port_set(dst, port_no);

    of_action_output_max_len_get(src, &val16);
    of_action_output_max_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_mpls_t *
of_action_pop_mpls_OF_VERSION_1_3_dup(
    of_action_pop_mpls_t *src)
{
    of_action_pop_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_pop_mpls_ethertype_get(src, &val16);
    of_action_pop_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_pbb_t *
of_action_pop_pbb_OF_VERSION_1_3_dup(
    of_action_pop_pbb_t *src)
{
    of_action_pop_pbb_t *dst;

    if ((dst = of_action_pop_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_vlan_t *
of_action_pop_vlan_OF_VERSION_1_3_dup(
    of_action_pop_vlan_t *src)
{
    of_action_pop_vlan_t *dst;

    if ((dst = of_action_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_mpls_t *
of_action_push_mpls_OF_VERSION_1_3_dup(
    of_action_push_mpls_t *src)
{
    of_action_push_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_mpls_ethertype_get(src, &val16);
    of_action_push_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_pbb_t *
of_action_push_pbb_OF_VERSION_1_3_dup(
    of_action_push_pbb_t *src)
{
    of_action_push_pbb_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_pbb_ethertype_get(src, &val16);
    of_action_push_pbb_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_vlan_t *
of_action_push_vlan_OF_VERSION_1_3_dup(
    of_action_push_vlan_t *src)
{
    of_action_push_vlan_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_vlan_ethertype_get(src, &val16);
    of_action_push_vlan_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_field_t *
of_action_set_field_OF_VERSION_1_3_dup(
    of_action_set_field_t *src)
{
    of_action_set_field_t *dst;

    of_oxm_eth_type_t src_oxm;
    of_oxm_eth_type_t *dst_oxm;

    if ((dst = of_action_set_field_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_field_field_bind(
        src, &src_oxm);
    dst_oxm = of_oxm_eth_type_OF_VERSION_1_3_dup(&src_oxm);
    if (dst_oxm == NULL) {
        of_action_set_field_delete(dst);
        return NULL;
    }
    of_action_set_field_field_set(dst, dst_oxm);
    of_oxm_eth_type_delete(dst_oxm);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_ttl_t *
of_action_set_mpls_ttl_OF_VERSION_1_3_dup(
    of_action_set_mpls_ttl_t *src)
{
    of_action_set_mpls_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_ttl_mpls_ttl_get(src, &val8);
    of_action_set_mpls_ttl_mpls_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_ttl_t *
of_action_set_nw_ttl_OF_VERSION_1_3_dup(
    of_action_set_nw_ttl_t *src)
{
    of_action_set_nw_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_ttl_nw_ttl_get(src, &val8);
    of_action_set_nw_ttl_nw_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_queue_t *
of_action_set_queue_OF_VERSION_1_3_dup(
    of_action_set_queue_t *src)
{
    of_action_set_queue_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_queue_queue_id_get(src, &val32);
    of_action_set_queue_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connection
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connection.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connection_t *
of_bsn_controller_connection_OF_VERSION_1_3_dup(
    of_bsn_controller_connection_t *src)
{
    of_bsn_controller_connection_t *dst;
    uint8_t val8;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_controller_connection_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connection_state_get(src, &val8);
    of_bsn_controller_connection_state_set(dst, val8);

    of_bsn_controller_connection_auxiliary_id_get(src, &val8);
    of_bsn_controller_connection_auxiliary_id_set(dst, val8);

    of_bsn_controller_connection_role_get(src, &val32);
    of_bsn_controller_connection_role_set(dst, val32);

    of_bsn_controller_connection_uri_get(src, &desc_str);
    of_bsn_controller_connection_uri_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_entry_t *
of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_desc_stats_entry_t *src)
{
    of_bsn_debug_counter_desc_stats_entry_t *dst;
    uint64_t val64;
    of_str64_t str64;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_debug_counter_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_entry_counter_id_get(src, &val64);
    of_bsn_debug_counter_desc_stats_entry_counter_id_set(dst, val64);

    of_bsn_debug_counter_desc_stats_entry_name_get(src, &str64);
    of_bsn_debug_counter_desc_stats_entry_name_set(dst, str64);

    of_bsn_debug_counter_desc_stats_entry_description_get(src, &desc_str);
    of_bsn_debug_counter_desc_stats_entry_description_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_entry_t *
of_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_debug_counter_stats_entry_t *src)
{
    of_bsn_debug_counter_stats_entry_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_debug_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_entry_counter_id_get(src, &val64);
    of_bsn_debug_counter_stats_entry_counter_id_set(dst, val64);

    of_bsn_debug_counter_stats_entry_value_get(src, &val64);
    of_bsn_debug_counter_stats_entry_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_entry_t *
of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_flow_checksum_bucket_stats_entry_t *src)
{
    of_bsn_flow_checksum_bucket_stats_entry_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_flow_checksum_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_entry_checksum_get(src, &val64);
    of_bsn_flow_checksum_bucket_stats_entry_checksum_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_entry_t *
of_bsn_generic_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_generic_stats_entry_t *src)
{
    of_bsn_generic_stats_entry_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_entry_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_entry_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_entry_t *
of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_gentable_bucket_stats_entry_t *src)
{
    of_bsn_gentable_bucket_stats_entry_t *dst;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_bucket_stats_entry_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_entry_t *
of_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_gentable_desc_stats_entry_t *src)
{
    of_bsn_gentable_desc_stats_entry_t *dst;
    uint16_t val16;
    of_table_name_t table_name;
    uint32_t val32;

    if ((dst = of_bsn_gentable_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_entry_table_id_get(src, &val16);
    of_bsn_gentable_desc_stats_entry_table_id_set(dst, val16);

    of_bsn_gentable_desc_stats_entry_name_get(src, &table_name);
    of_bsn_gentable_desc_stats_entry_name_set(dst, table_name);

    of_bsn_gentable_desc_stats_entry_buckets_size_get(src, &val32);
    of_bsn_gentable_desc_stats_entry_buckets_size_set(dst, val32);

    of_bsn_gentable_desc_stats_entry_max_entries_get(src, &val32);
    of_bsn_gentable_desc_stats_entry_max_entries_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_entry_t *
of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_desc_stats_entry_t *src)
{
    of_bsn_gentable_entry_desc_stats_entry_t *dst;
    of_checksum_128_t checksum_128;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_entry_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_desc_stats_entry_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_entry_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_desc_stats_entry_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_entry_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_entry_t *
of_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_gentable_entry_stats_entry_t *src)
{
    of_bsn_gentable_entry_stats_entry_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_entry_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_entry_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_stats_entry_stats_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_entry_stats_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_entry_t *
of_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_gentable_stats_entry_t *src)
{
    of_bsn_gentable_stats_entry_t *dst;
    uint16_t val16;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_entry_table_id_get(src, &val16);
    of_bsn_gentable_stats_entry_table_id_set(dst, val16);

    of_bsn_gentable_stats_entry_entry_count_get(src, &val32);
    of_bsn_gentable_stats_entry_entry_count_set(dst, val32);

    of_bsn_gentable_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_stats_entry_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_interface_t *
of_bsn_interface_OF_VERSION_1_3_dup(
    of_bsn_interface_t *src)
{
    of_bsn_interface_t *dst;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_interface_hw_addr_get(src, &mac_addr);
    of_bsn_interface_hw_addr_set(dst, mac_addr);

    of_bsn_interface_name_get(src, &port_name);
    of_bsn_interface_name_set(dst, port_name);

    of_bsn_interface_ipv4_addr_get(src, &ipv4);
    of_bsn_interface_ipv4_addr_set(dst, ipv4);

    of_bsn_interface_ipv4_netmask_get(src, &ipv4);
    of_bsn_interface_ipv4_netmask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_entry_t *
of_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_lacp_stats_entry_t *src)
{
    of_bsn_lacp_stats_entry_t *dst;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;
    uint8_t val8;

    if ((dst = of_bsn_lacp_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_entry_port_no_get(src, &port_no);
    of_bsn_lacp_stats_entry_port_no_set(dst, port_no);

    of_bsn_lacp_stats_entry_actor_sys_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_sys_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_stats_entry_actor_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_stats_entry_actor_port_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_port_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_port_num_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_port_num_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_key_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_key_set(dst, val16);

    of_bsn_lacp_stats_entry_convergence_status_get(src, &val8);
    of_bsn_lacp_stats_entry_convergence_status_set(dst, val8);

    of_bsn_lacp_stats_entry_partner_sys_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_sys_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_stats_entry_partner_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_stats_entry_partner_port_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_port_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_port_num_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_port_num_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_key_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_entry_t *
of_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_port_counter_stats_entry_t *src)
{
    of_bsn_port_counter_stats_entry_t *dst;
    of_port_no_t port_no;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_port_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_entry_port_no_get(src, &port_no);
    of_bsn_port_counter_stats_entry_port_no_set(dst, port_no);

    of_bsn_port_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_port_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_port_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_entry_t *
of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_switch_pipeline_stats_entry_t *src)
{
    of_bsn_switch_pipeline_stats_entry_t *dst;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_switch_pipeline_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_entry_pipeline_get(src, &desc_str);
    of_bsn_switch_pipeline_stats_entry_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_entry_t *
of_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_table_checksum_stats_entry_t *src)
{
    of_bsn_table_checksum_stats_entry_t *dst;
    uint8_t val8;
    uint64_t val64;

    if ((dst = of_bsn_table_checksum_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_entry_table_id_get(src, &val8);
    of_bsn_table_checksum_stats_entry_table_id_set(dst, val8);

    of_bsn_table_checksum_stats_entry_checksum_get(src, &val64);
    of_bsn_table_checksum_stats_entry_checksum_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_tlv
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_tlv_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_TLV_CONVERGENCE_STATUS) {
        return of_bsn_tlv_convergence_status_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DROP) {
        return of_bsn_tlv_drop_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CIRCUIT_ID) {
        return of_bsn_tlv_circuit_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_SEQUENCED) {
        return of_bsn_tlv_mpls_sequenced_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT_USAGE) {
        return of_bsn_tlv_port_usage_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_SYSTEM_MAC) {
        return of_bsn_tlv_actor_system_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REQUEST_PACKETS) {
        return of_bsn_tlv_request_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ANCHOR) {
        return of_bsn_tlv_anchor_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_PORT_NUM) {
        return of_bsn_tlv_partner_port_num_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UINT64_LIST) {
        return of_bsn_tlv_uint64_list_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_VLAN_ON_EGRESS) {
        return of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_INTERFACE_CLASS_ID) {
        return of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BROADCAST_RATE) {
        return of_bsn_tlv_broadcast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_LENGTH) {
        return of_bsn_tlv_udf_length_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_LOOPBACK_PORT) {
        return of_bsn_tlv_loopback_port_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_GTP_HEADER_MATCH) {
        return of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QUEUE_ID) {
        return of_bsn_tlv_queue_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_GATEWAY_MAC) {
        return of_bsn_tlv_external_gateway_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RATE_UNIT) {
        return of_bsn_tlv_rate_unit_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ECN) {
        return of_bsn_tlv_ecn_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_VID) {
        return of_bsn_tlv_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERNAL_MAC) {
        return of_bsn_tlv_internal_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HEADER_SIZE) {
        return of_bsn_tlv_header_size_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VFI) {
        return of_bsn_tlv_vfi_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_SYSTEM_PRIORITY) {
        return of_bsn_tlv_partner_system_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_SRC) {
        return of_bsn_tlv_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STATUS) {
        return of_bsn_tlv_status_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4) {
        return of_bsn_tlv_ipv4_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6) {
        return of_bsn_tlv_ipv6_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_PORT_NUM) {
        return of_bsn_tlv_actor_port_num_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EGRESS_ONLY) {
        return of_bsn_tlv_egress_only_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_KNOWN_MULTICAST_RATE) {
        return of_bsn_tlv_known_multicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXT_HOP_IPV4) {
        return of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_NETMASK) {
        return of_bsn_tlv_ipv4_netmask_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_DST_CLASS_ID) {
        return of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REPLY_PACKETS) {
        return of_bsn_tlv_reply_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_GTP_PORT_MATCH) {
        return of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CPU_LAG) {
        return of_bsn_tlv_cpu_lag_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_GENERATION_ID) {
        return of_bsn_tlv_generation_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QUEUE_WEIGHT) {
        return of_bsn_tlv_queue_weight_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EGRESS_PORT_GROUP_ID) {
        return of_bsn_tlv_egress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_SRC) {
        return of_bsn_tlv_tcp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_LABEL) {
        return of_bsn_tlv_mpls_label_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RX_BYTES) {
        return of_bsn_tlv_rx_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RX_PACKETS) {
        return of_bsn_tlv_rx_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_FLAGS) {
        return of_bsn_tlv_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_SEED) {
        return of_bsn_tlv_hash_seed_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MISS_PACKETS) {
        return of_bsn_tlv_miss_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_IP) {
        return of_bsn_tlv_external_ip_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_SRC) {
        return of_bsn_tlv_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_SYSTEM_PRIORITY) {
        return of_bsn_tlv_actor_system_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IGMP_SNOOPING) {
        return of_bsn_tlv_igmp_snooping_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INGRESS_PORT_GROUP_ID) {
        return of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_MAC) {
        return of_bsn_tlv_external_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEGATE) {
        return of_bsn_tlv_negate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMPV6_CHKSUM) {
        return of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_NETMASK) {
        return of_bsn_tlv_external_netmask_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IP_PROTO) {
        return of_bsn_tlv_ip_proto_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_STATE) {
        return of_bsn_tlv_actor_state_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_ID) {
        return of_bsn_tlv_udf_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_PREFIX) {
        return of_bsn_tlv_ipv6_prefix_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_SYSTEM_MAC) {
        return of_bsn_tlv_partner_system_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DISABLE_SRC_MAC_CHECK) {
        return of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ENHANCED_HASH_CAPABILITY) {
        return of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_CONTROL_WORD) {
        return of_bsn_tlv_mpls_control_word_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_TYPE) {
        return of_bsn_tlv_eth_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SET_LOOPBACK_MODE) {
        return of_bsn_tlv_set_loopback_mode_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT) {
        return of_bsn_tlv_port_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MAC_MASK) {
        return of_bsn_tlv_mac_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_KEY) {
        return of_bsn_tlv_actor_key_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VPN_KEY) {
        return of_bsn_tlv_vpn_key_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_MPLS_L3_ON_INGRESS) {
        return of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXTHOP_TYPE_VXLAN) {
        return of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MCG_TYPE_VXLAN) {
        return of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DECAP) {
        return of_bsn_tlv_decap_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DATA) {
        return of_bsn_tlv_data_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_VID_MASK) {
        return of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QOS_PRIORITY) {
        return of_bsn_tlv_qos_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TX_PACKETS) {
        return of_bsn_tlv_tx_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BROADCAST_QUERY_TIMEOUT) {
        return of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NAME) {
        return of_bsn_tlv_name_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_DST) {
        return of_bsn_tlv_tcp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_OFFSET) {
        return of_bsn_tlv_offset_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_TIME) {
        return of_bsn_tlv_idle_time_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_NOTIFICATION) {
        return of_bsn_tlv_idle_notification_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L2_MULTICAST_LOOKUP) {
        return of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_PACKET_TYPE) {
        return of_bsn_tlv_hash_packet_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXT_HOP_MAC) {
        return of_bsn_tlv_next_hop_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNICAST_RATE) {
        return of_bsn_tlv_unicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_OFFSET) {
        return of_bsn_tlv_udf_offset_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_PORT_PRIORITY) {
        return of_bsn_tlv_actor_port_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_APPLY_PACKETS) {
        return of_bsn_tlv_apply_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_SRC) {
        return of_bsn_tlv_ipv6_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_USE_PACKET_STATE) {
        return of_bsn_tlv_use_packet_state_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDP_SRC) {
        return of_bsn_tlv_udp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_MPLS_L2_ON_INGRESS) {
        return of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_TYPE) {
        return of_bsn_tlv_icmp_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_TYPE) {
        return of_bsn_tlv_hash_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MULTICAST_INTERFACE_ID) {
        return of_bsn_tlv_multicast_interface_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_GATEWAY_IP) {
        return of_bsn_tlv_external_gateway_ip_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_ID) {
        return of_bsn_tlv_icmp_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_PORT_PRIORITY) {
        return of_bsn_tlv_partner_port_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TTL) {
        return of_bsn_tlv_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_DST) {
        return of_bsn_tlv_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DATA_MASK) {
        return of_bsn_tlv_data_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SAMPLING_RATE) {
        return of_bsn_tlv_sampling_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MAC) {
        return of_bsn_tlv_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SUB_AGENT_ID) {
        return of_bsn_tlv_sub_agent_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RATE_LIMIT) {
        return of_bsn_tlv_rate_limit_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TX_BYTES) {
        return of_bsn_tlv_tx_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_AUTO_NEGOTIATION) {
        return of_bsn_tlv_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_DST) {
        return of_bsn_tlv_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_ANCHOR) {
        return of_bsn_tlv_udf_anchor_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT_VXLAN_MODE) {
        return of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_PACKET_FIELD) {
        return of_bsn_tlv_hash_packet_field_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERNAL_GATEWAY_MAC) {
        return of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_SRC_CLASS_ID) {
        return of_bsn_tlv_l3_src_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_STATE) {
        return of_bsn_tlv_partner_state_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DSCP) {
        return of_bsn_tlv_dscp_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNKNOWN_MULTICAST_RATE) {
        return of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNICAST_QUERY_TIMEOUT) {
        return of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_KEY) {
        return of_bsn_tlv_partner_key_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VFP_CLASS_ID) {
        return of_bsn_tlv_vfp_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDP_DST) {
        return of_bsn_tlv_udp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VRF) {
        return of_bsn_tlv_vrf_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERVAL) {
        return of_bsn_tlv_interval_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VNI) {
        return of_bsn_tlv_vni_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TUNNEL_CAPABILITY) {
        return of_bsn_tlv_tunnel_capability_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_TIMEOUT) {
        return of_bsn_tlv_idle_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CRC_ENABLED) {
        return of_bsn_tlv_crc_enabled_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NDP_OFFLOAD) {
        return of_bsn_tlv_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARENT_PORT) {
        return of_bsn_tlv_parent_port_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VXLAN_EGRESS_LAG) {
        return of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PRIORITY) {
        return of_bsn_tlv_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_PCP) {
        return of_bsn_tlv_vlan_pcp_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_DST) {
        return of_bsn_tlv_ipv6_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_CODE) {
        return of_bsn_tlv_icmp_code_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NDP_STATIC) {
        return of_bsn_tlv_ndp_static_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REFERENCE) {
        return of_bsn_tlv_reference_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BUCKET) {
        return of_bsn_tlv_bucket_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PUSH_VLAN_ON_INGRESS) {
        return of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_APPLY_BYTES) {
        return of_bsn_tlv_apply_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNTAGGED) {
        return of_bsn_tlv_untagged_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_MAC_LIST) {
        return of_bsn_tlv_vlan_mac_list_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_key_t *
of_bsn_tlv_actor_key_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_key_t *src)
{
    of_bsn_tlv_actor_key_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_key_value_get(src, &val16);
    of_bsn_tlv_actor_key_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_port_num
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_port_num.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_port_num_t *
of_bsn_tlv_actor_port_num_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_port_num_t *src)
{
    of_bsn_tlv_actor_port_num_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_port_num_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_port_num_value_get(src, &val16);
    of_bsn_tlv_actor_port_num_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_port_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_port_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_port_priority_t *
of_bsn_tlv_actor_port_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_port_priority_t *src)
{
    of_bsn_tlv_actor_port_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_port_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_port_priority_value_get(src, &val16);
    of_bsn_tlv_actor_port_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_state_t *
of_bsn_tlv_actor_state_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_state_t *src)
{
    of_bsn_tlv_actor_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_actor_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_state_value_get(src, &val8);
    of_bsn_tlv_actor_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_system_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_system_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_system_mac_t *
of_bsn_tlv_actor_system_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_system_mac_t *src)
{
    of_bsn_tlv_actor_system_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_actor_system_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_system_mac_value_get(src, &mac_addr);
    of_bsn_tlv_actor_system_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_system_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_system_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_system_priority_t *
of_bsn_tlv_actor_system_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_actor_system_priority_t *src)
{
    of_bsn_tlv_actor_system_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_system_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_system_priority_value_get(src, &val16);
    of_bsn_tlv_actor_system_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_anchor
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_anchor.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_anchor_t *
of_bsn_tlv_anchor_OF_VERSION_1_3_dup(
    of_bsn_tlv_anchor_t *src)
{
    of_bsn_tlv_anchor_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_anchor_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_anchor_value_get(src, &val16);
    of_bsn_tlv_anchor_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_apply_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_apply_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_apply_bytes_t *
of_bsn_tlv_apply_bytes_OF_VERSION_1_3_dup(
    of_bsn_tlv_apply_bytes_t *src)
{
    of_bsn_tlv_apply_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_apply_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_apply_bytes_value_get(src, &val64);
    of_bsn_tlv_apply_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_apply_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_apply_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_apply_packets_t *
of_bsn_tlv_apply_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_apply_packets_t *src)
{
    of_bsn_tlv_apply_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_apply_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_apply_packets_value_get(src, &val64);
    of_bsn_tlv_apply_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_auto_negotiation_t *
of_bsn_tlv_auto_negotiation_OF_VERSION_1_3_dup(
    of_bsn_tlv_auto_negotiation_t *src)
{
    of_bsn_tlv_auto_negotiation_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_auto_negotiation_value_get(src, &val8);
    of_bsn_tlv_auto_negotiation_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_broadcast_query_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_broadcast_query_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_broadcast_query_timeout_t *
of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_3_dup(
    of_bsn_tlv_broadcast_query_timeout_t *src)
{
    of_bsn_tlv_broadcast_query_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_broadcast_query_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_broadcast_query_timeout_value_get(src, &val32);
    of_bsn_tlv_broadcast_query_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_broadcast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_broadcast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_broadcast_rate_t *
of_bsn_tlv_broadcast_rate_OF_VERSION_1_3_dup(
    of_bsn_tlv_broadcast_rate_t *src)
{
    of_bsn_tlv_broadcast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_broadcast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_broadcast_rate_value_get(src, &val32);
    of_bsn_tlv_broadcast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_bucket_t *
of_bsn_tlv_bucket_OF_VERSION_1_3_dup(
    of_bsn_tlv_bucket_t *src)
{
    of_bsn_tlv_bucket_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_tlv_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_bucket_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_bucket_delete(dst);
        return NULL;
    }
    of_bsn_tlv_bucket_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_circuit_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_circuit_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_circuit_id_t *
of_bsn_tlv_circuit_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_circuit_id_t *src)
{
    of_bsn_tlv_circuit_id_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_circuit_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_circuit_id_value_get(src, &octets);
    of_bsn_tlv_circuit_id_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_convergence_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_convergence_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_convergence_status_t *
of_bsn_tlv_convergence_status_OF_VERSION_1_3_dup(
    of_bsn_tlv_convergence_status_t *src)
{
    of_bsn_tlv_convergence_status_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_convergence_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_convergence_status_value_get(src, &val8);
    of_bsn_tlv_convergence_status_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_cpu_lag
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_cpu_lag.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_cpu_lag_t *
of_bsn_tlv_cpu_lag_OF_VERSION_1_3_dup(
    of_bsn_tlv_cpu_lag_t *src)
{
    of_bsn_tlv_cpu_lag_t *dst;

    if ((dst = of_bsn_tlv_cpu_lag_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_crc_enabled
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_crc_enabled.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_crc_enabled_t *
of_bsn_tlv_crc_enabled_OF_VERSION_1_3_dup(
    of_bsn_tlv_crc_enabled_t *src)
{
    of_bsn_tlv_crc_enabled_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_crc_enabled_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_crc_enabled_value_get(src, &val8);
    of_bsn_tlv_crc_enabled_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_data
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_data.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_data_t *
of_bsn_tlv_data_OF_VERSION_1_3_dup(
    of_bsn_tlv_data_t *src)
{
    of_bsn_tlv_data_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_data_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_data_value_get(src, &octets);
    of_bsn_tlv_data_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_data_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_data_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_data_mask_t *
of_bsn_tlv_data_mask_OF_VERSION_1_3_dup(
    of_bsn_tlv_data_mask_t *src)
{
    of_bsn_tlv_data_mask_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_data_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_data_mask_value_get(src, &octets);
    of_bsn_tlv_data_mask_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_decap
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_decap.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_decap_t *
of_bsn_tlv_decap_OF_VERSION_1_3_dup(
    of_bsn_tlv_decap_t *src)
{
    of_bsn_tlv_decap_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_decap_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_decap_value_get(src, &val16);
    of_bsn_tlv_decap_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_disable_src_mac_check_t *
of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_3_dup(
    of_bsn_tlv_disable_src_mac_check_t *src)
{
    of_bsn_tlv_disable_src_mac_check_t *dst;

    if ((dst = of_bsn_tlv_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_drop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_drop.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_drop_t *
of_bsn_tlv_drop_OF_VERSION_1_3_dup(
    of_bsn_tlv_drop_t *src)
{
    of_bsn_tlv_drop_t *dst;

    if ((dst = of_bsn_tlv_drop_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_dscp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_dscp.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_dscp_t *
of_bsn_tlv_dscp_OF_VERSION_1_3_dup(
    of_bsn_tlv_dscp_t *src)
{
    of_bsn_tlv_dscp_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_dscp_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_dscp_value_get(src, &val16);
    of_bsn_tlv_dscp_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ecn_t *
of_bsn_tlv_ecn_OF_VERSION_1_3_dup(
    of_bsn_tlv_ecn_t *src)
{
    of_bsn_tlv_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ecn_value_get(src, &val8);
    of_bsn_tlv_ecn_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_egress_only
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_egress_only.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_egress_only_t *
of_bsn_tlv_egress_only_OF_VERSION_1_3_dup(
    of_bsn_tlv_egress_only_t *src)
{
    of_bsn_tlv_egress_only_t *dst;

    if ((dst = of_bsn_tlv_egress_only_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_egress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_egress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_egress_port_group_id_t *
of_bsn_tlv_egress_port_group_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_egress_port_group_id_t *src)
{
    of_bsn_tlv_egress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_egress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_egress_port_group_id_value_get(src, &val32);
    of_bsn_tlv_egress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_enhanced_hash_capability
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_enhanced_hash_capability.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_enhanced_hash_capability_t *
of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_3_dup(
    of_bsn_tlv_enhanced_hash_capability_t *src)
{
    of_bsn_tlv_enhanced_hash_capability_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_enhanced_hash_capability_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_enhanced_hash_capability_value_get(src, &val64);
    of_bsn_tlv_enhanced_hash_capability_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_dst_t *
of_bsn_tlv_eth_dst_OF_VERSION_1_3_dup(
    of_bsn_tlv_eth_dst_t *src)
{
    of_bsn_tlv_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_dst_value_get(src, &mac_addr);
    of_bsn_tlv_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_src_t *
of_bsn_tlv_eth_src_OF_VERSION_1_3_dup(
    of_bsn_tlv_eth_src_t *src)
{
    of_bsn_tlv_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_src_value_get(src, &mac_addr);
    of_bsn_tlv_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_type_t *
of_bsn_tlv_eth_type_OF_VERSION_1_3_dup(
    of_bsn_tlv_eth_type_t *src)
{
    of_bsn_tlv_eth_type_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_eth_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_type_value_get(src, &val16);
    of_bsn_tlv_eth_type_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_gateway_ip
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_gateway_ip.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_gateway_ip_t *
of_bsn_tlv_external_gateway_ip_OF_VERSION_1_3_dup(
    of_bsn_tlv_external_gateway_ip_t *src)
{
    of_bsn_tlv_external_gateway_ip_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_gateway_ip_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_gateway_ip_value_get(src, &ipv4);
    of_bsn_tlv_external_gateway_ip_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_gateway_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_gateway_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_gateway_mac_t *
of_bsn_tlv_external_gateway_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_external_gateway_mac_t *src)
{
    of_bsn_tlv_external_gateway_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_external_gateway_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_gateway_mac_value_get(src, &mac_addr);
    of_bsn_tlv_external_gateway_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_ip
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_ip.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_ip_t *
of_bsn_tlv_external_ip_OF_VERSION_1_3_dup(
    of_bsn_tlv_external_ip_t *src)
{
    of_bsn_tlv_external_ip_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_ip_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_ip_value_get(src, &ipv4);
    of_bsn_tlv_external_ip_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_mac_t *
of_bsn_tlv_external_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_external_mac_t *src)
{
    of_bsn_tlv_external_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_external_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_mac_value_get(src, &mac_addr);
    of_bsn_tlv_external_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_netmask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_netmask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_netmask_t *
of_bsn_tlv_external_netmask_OF_VERSION_1_3_dup(
    of_bsn_tlv_external_netmask_t *src)
{
    of_bsn_tlv_external_netmask_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_netmask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_netmask_value_get(src, &ipv4);
    of_bsn_tlv_external_netmask_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_generation_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_generation_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_generation_id_t *
of_bsn_tlv_generation_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_generation_id_t *src)
{
    of_bsn_tlv_generation_id_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_generation_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_generation_id_value_get(src, &val64);
    of_bsn_tlv_generation_id_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_gtp_header_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_gtp_header_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_gtp_header_match_t *
of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_gtp_header_match_t *src)
{
    of_bsn_tlv_hash_gtp_header_match_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_gtp_header_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_gtp_header_match_first_header_byte_get(src, &val8);
    of_bsn_tlv_hash_gtp_header_match_first_header_byte_set(dst, val8);

    of_bsn_tlv_hash_gtp_header_match_first_header_mask_get(src, &val8);
    of_bsn_tlv_hash_gtp_header_match_first_header_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_gtp_port_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_gtp_port_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_gtp_port_match_t *
of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_gtp_port_match_t *src)
{
    of_bsn_tlv_hash_gtp_port_match_t *dst;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_tlv_hash_gtp_port_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_gtp_port_match_match_get(src, &val8);
    of_bsn_tlv_hash_gtp_port_match_match_set(dst, val8);

    of_bsn_tlv_hash_gtp_port_match_src_port_get(src, &val16);
    of_bsn_tlv_hash_gtp_port_match_src_port_set(dst, val16);

    of_bsn_tlv_hash_gtp_port_match_dst_port_get(src, &val16);
    of_bsn_tlv_hash_gtp_port_match_dst_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_packet_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_packet_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_packet_field_t *
of_bsn_tlv_hash_packet_field_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_packet_field_t *src)
{
    of_bsn_tlv_hash_packet_field_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_hash_packet_field_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_packet_field_value_get(src, &val64);
    of_bsn_tlv_hash_packet_field_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_packet_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_packet_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_packet_type_t *
of_bsn_tlv_hash_packet_type_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_packet_type_t *src)
{
    of_bsn_tlv_hash_packet_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_packet_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_packet_type_value_get(src, &val8);
    of_bsn_tlv_hash_packet_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_seed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_seed.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_seed_t *
of_bsn_tlv_hash_seed_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_seed_t *src)
{
    of_bsn_tlv_hash_seed_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_hash_seed_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_seed_seed1_get(src, &val32);
    of_bsn_tlv_hash_seed_seed1_set(dst, val32);

    of_bsn_tlv_hash_seed_seed2_get(src, &val32);
    of_bsn_tlv_hash_seed_seed2_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_type_t *
of_bsn_tlv_hash_type_OF_VERSION_1_3_dup(
    of_bsn_tlv_hash_type_t *src)
{
    of_bsn_tlv_hash_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_type_value_get(src, &val8);
    of_bsn_tlv_hash_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_header_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_header_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_header_size_t *
of_bsn_tlv_header_size_OF_VERSION_1_3_dup(
    of_bsn_tlv_header_size_t *src)
{
    of_bsn_tlv_header_size_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_header_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_header_size_value_get(src, &val32);
    of_bsn_tlv_header_size_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_code_t *
of_bsn_tlv_icmp_code_OF_VERSION_1_3_dup(
    of_bsn_tlv_icmp_code_t *src)
{
    of_bsn_tlv_icmp_code_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_icmp_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_code_value_get(src, &val8);
    of_bsn_tlv_icmp_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_id_t *
of_bsn_tlv_icmp_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_icmp_id_t *src)
{
    of_bsn_tlv_icmp_id_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_icmp_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_id_value_get(src, &val16);
    of_bsn_tlv_icmp_id_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_type_t *
of_bsn_tlv_icmp_type_OF_VERSION_1_3_dup(
    of_bsn_tlv_icmp_type_t *src)
{
    of_bsn_tlv_icmp_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_icmp_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_type_value_get(src, &val8);
    of_bsn_tlv_icmp_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmpv6_chksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmpv6_chksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmpv6_chksum_t *
of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_3_dup(
    of_bsn_tlv_icmpv6_chksum_t *src)
{
    of_bsn_tlv_icmpv6_chksum_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_icmpv6_chksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmpv6_chksum_value_get(src, &val16);
    of_bsn_tlv_icmpv6_chksum_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_notification
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_notification.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_notification_t *
of_bsn_tlv_idle_notification_OF_VERSION_1_3_dup(
    of_bsn_tlv_idle_notification_t *src)
{
    of_bsn_tlv_idle_notification_t *dst;

    if ((dst = of_bsn_tlv_idle_notification_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_time
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_time.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_time_t *
of_bsn_tlv_idle_time_OF_VERSION_1_3_dup(
    of_bsn_tlv_idle_time_t *src)
{
    of_bsn_tlv_idle_time_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_idle_time_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_idle_time_value_get(src, &val64);
    of_bsn_tlv_idle_time_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_timeout_t *
of_bsn_tlv_idle_timeout_OF_VERSION_1_3_dup(
    of_bsn_tlv_idle_timeout_t *src)
{
    of_bsn_tlv_idle_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_idle_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_idle_timeout_value_get(src, &val32);
    of_bsn_tlv_idle_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_igmp_snooping
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_igmp_snooping.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_igmp_snooping_t *
of_bsn_tlv_igmp_snooping_OF_VERSION_1_3_dup(
    of_bsn_tlv_igmp_snooping_t *src)
{
    of_bsn_tlv_igmp_snooping_t *dst;

    if ((dst = of_bsn_tlv_igmp_snooping_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ingress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ingress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ingress_port_group_id_t *
of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_ingress_port_group_id_t *src)
{
    of_bsn_tlv_ingress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_ingress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ingress_port_group_id_value_get(src, &val32);
    of_bsn_tlv_ingress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_internal_gateway_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_internal_gateway_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_internal_gateway_mac_t *
of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_internal_gateway_mac_t *src)
{
    of_bsn_tlv_internal_gateway_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_internal_gateway_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_internal_gateway_mac_value_get(src, &mac_addr);
    of_bsn_tlv_internal_gateway_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_internal_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_internal_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_internal_mac_t *
of_bsn_tlv_internal_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_internal_mac_t *src)
{
    of_bsn_tlv_internal_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_internal_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_internal_mac_value_get(src, &mac_addr);
    of_bsn_tlv_internal_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_interval
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_interval.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_interval_t *
of_bsn_tlv_interval_OF_VERSION_1_3_dup(
    of_bsn_tlv_interval_t *src)
{
    of_bsn_tlv_interval_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_interval_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_interval_value_get(src, &val32);
    of_bsn_tlv_interval_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ip_proto
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ip_proto.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ip_proto_t *
of_bsn_tlv_ip_proto_OF_VERSION_1_3_dup(
    of_bsn_tlv_ip_proto_t *src)
{
    of_bsn_tlv_ip_proto_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ip_proto_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ip_proto_value_get(src, &val8);
    of_bsn_tlv_ip_proto_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_t *
of_bsn_tlv_ipv4_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv4_t *src)
{
    of_bsn_tlv_ipv4_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_dst_t *
of_bsn_tlv_ipv4_dst_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv4_dst_t *src)
{
    of_bsn_tlv_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_dst_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_netmask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_netmask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_netmask_t *
of_bsn_tlv_ipv4_netmask_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv4_netmask_t *src)
{
    of_bsn_tlv_ipv4_netmask_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_netmask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_netmask_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_netmask_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_src_t *
of_bsn_tlv_ipv4_src_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv4_src_t *src)
{
    of_bsn_tlv_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_src_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_t *
of_bsn_tlv_ipv6_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv6_t *src)
{
    of_bsn_tlv_ipv6_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_dst_t *
of_bsn_tlv_ipv6_dst_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv6_dst_t *src)
{
    of_bsn_tlv_ipv6_dst_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_dst_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_dst_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_prefix
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_prefix.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_prefix_t *
of_bsn_tlv_ipv6_prefix_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv6_prefix_t *src)
{
    of_bsn_tlv_ipv6_prefix_t *dst;
    of_ipv6_t ipv6;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ipv6_prefix_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_prefix_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_prefix_value_set(dst, ipv6);

    of_bsn_tlv_ipv6_prefix_prefix_length_get(src, &val8);
    of_bsn_tlv_ipv6_prefix_prefix_length_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_src_t *
of_bsn_tlv_ipv6_src_OF_VERSION_1_3_dup(
    of_bsn_tlv_ipv6_src_t *src)
{
    of_bsn_tlv_ipv6_src_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_src_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_src_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_known_multicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_known_multicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_known_multicast_rate_t *
of_bsn_tlv_known_multicast_rate_OF_VERSION_1_3_dup(
    of_bsn_tlv_known_multicast_rate_t *src)
{
    of_bsn_tlv_known_multicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_known_multicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_known_multicast_rate_value_get(src, &val32);
    of_bsn_tlv_known_multicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l2_multicast_lookup
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l2_multicast_lookup.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l2_multicast_lookup_t *
of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_3_dup(
    of_bsn_tlv_l2_multicast_lookup_t *src)
{
    of_bsn_tlv_l2_multicast_lookup_t *dst;

    if ((dst = of_bsn_tlv_l2_multicast_lookup_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_dst_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_dst_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_dst_class_id_t *
of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_l3_dst_class_id_t *src)
{
    of_bsn_tlv_l3_dst_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_dst_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_dst_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_dst_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_interface_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_interface_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_interface_class_id_t *
of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_l3_interface_class_id_t *src)
{
    of_bsn_tlv_l3_interface_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_interface_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_interface_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_interface_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_src_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_src_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_src_class_id_t *
of_bsn_tlv_l3_src_class_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_l3_src_class_id_t *src)
{
    of_bsn_tlv_l3_src_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_src_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_src_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_src_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_loopback_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_loopback_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_loopback_port_t *
of_bsn_tlv_loopback_port_OF_VERSION_1_3_dup(
    of_bsn_tlv_loopback_port_t *src)
{
    of_bsn_tlv_loopback_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_loopback_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_loopback_port_value_get(src, &port_no);
    of_bsn_tlv_loopback_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mac_t *
of_bsn_tlv_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_mac_t *src)
{
    of_bsn_tlv_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mac_value_get(src, &mac_addr);
    of_bsn_tlv_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mac_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mac_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mac_mask_t *
of_bsn_tlv_mac_mask_OF_VERSION_1_3_dup(
    of_bsn_tlv_mac_mask_t *src)
{
    of_bsn_tlv_mac_mask_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_mac_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mac_mask_value_get(src, &mac_addr);
    of_bsn_tlv_mac_mask_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mcg_type_vxlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mcg_type_vxlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mcg_type_vxlan_t *
of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_3_dup(
    of_bsn_tlv_mcg_type_vxlan_t *src)
{
    of_bsn_tlv_mcg_type_vxlan_t *dst;

    if ((dst = of_bsn_tlv_mcg_type_vxlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_miss_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_miss_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_miss_packets_t *
of_bsn_tlv_miss_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_miss_packets_t *src)
{
    of_bsn_tlv_miss_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_miss_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_miss_packets_value_get(src, &val64);
    of_bsn_tlv_miss_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_control_word
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_control_word.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_control_word_t *
of_bsn_tlv_mpls_control_word_OF_VERSION_1_3_dup(
    of_bsn_tlv_mpls_control_word_t *src)
{
    of_bsn_tlv_mpls_control_word_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_mpls_control_word_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_control_word_value_get(src, &val8);
    of_bsn_tlv_mpls_control_word_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_label_t *
of_bsn_tlv_mpls_label_OF_VERSION_1_3_dup(
    of_bsn_tlv_mpls_label_t *src)
{
    of_bsn_tlv_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_label_value_get(src, &val32);
    of_bsn_tlv_mpls_label_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_sequenced
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_sequenced.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_sequenced_t *
of_bsn_tlv_mpls_sequenced_OF_VERSION_1_3_dup(
    of_bsn_tlv_mpls_sequenced_t *src)
{
    of_bsn_tlv_mpls_sequenced_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_mpls_sequenced_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_sequenced_value_get(src, &val8);
    of_bsn_tlv_mpls_sequenced_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_multicast_interface_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_multicast_interface_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_multicast_interface_id_t *
of_bsn_tlv_multicast_interface_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_multicast_interface_id_t *src)
{
    of_bsn_tlv_multicast_interface_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_multicast_interface_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_multicast_interface_id_value_get(src, &val32);
    of_bsn_tlv_multicast_interface_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_name
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_name.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_name_t *
of_bsn_tlv_name_OF_VERSION_1_3_dup(
    of_bsn_tlv_name_t *src)
{
    of_bsn_tlv_name_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_name_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_name_value_get(src, &octets);
    of_bsn_tlv_name_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ndp_offload_t *
of_bsn_tlv_ndp_offload_OF_VERSION_1_3_dup(
    of_bsn_tlv_ndp_offload_t *src)
{
    of_bsn_tlv_ndp_offload_t *dst;

    if ((dst = of_bsn_tlv_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ndp_static
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ndp_static.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ndp_static_t *
of_bsn_tlv_ndp_static_OF_VERSION_1_3_dup(
    of_bsn_tlv_ndp_static_t *src)
{
    of_bsn_tlv_ndp_static_t *dst;

    if ((dst = of_bsn_tlv_ndp_static_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_negate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_negate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_negate_t *
of_bsn_tlv_negate_OF_VERSION_1_3_dup(
    of_bsn_tlv_negate_t *src)
{
    of_bsn_tlv_negate_t *dst;

    if ((dst = of_bsn_tlv_negate_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_next_hop_ipv4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_next_hop_ipv4.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_next_hop_ipv4_t *
of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_3_dup(
    of_bsn_tlv_next_hop_ipv4_t *src)
{
    of_bsn_tlv_next_hop_ipv4_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_next_hop_ipv4_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_next_hop_ipv4_value_get(src, &ipv4);
    of_bsn_tlv_next_hop_ipv4_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_next_hop_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_next_hop_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_next_hop_mac_t *
of_bsn_tlv_next_hop_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_next_hop_mac_t *src)
{
    of_bsn_tlv_next_hop_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_next_hop_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_next_hop_mac_value_get(src, &mac_addr);
    of_bsn_tlv_next_hop_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_nexthop_type_vxlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_nexthop_type_vxlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_nexthop_type_vxlan_t *
of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_3_dup(
    of_bsn_tlv_nexthop_type_vxlan_t *src)
{
    of_bsn_tlv_nexthop_type_vxlan_t *dst;

    if ((dst = of_bsn_tlv_nexthop_type_vxlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_offset
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_offset.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_offset_t *
of_bsn_tlv_offset_OF_VERSION_1_3_dup(
    of_bsn_tlv_offset_t *src)
{
    of_bsn_tlv_offset_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_offset_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_offset_value_get(src, &val16);
    of_bsn_tlv_offset_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_parent_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_parent_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_parent_port_t *
of_bsn_tlv_parent_port_OF_VERSION_1_3_dup(
    of_bsn_tlv_parent_port_t *src)
{
    of_bsn_tlv_parent_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_parent_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_parent_port_value_get(src, &port_no);
    of_bsn_tlv_parent_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_key_t *
of_bsn_tlv_partner_key_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_key_t *src)
{
    of_bsn_tlv_partner_key_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_key_value_get(src, &val16);
    of_bsn_tlv_partner_key_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_port_num
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_port_num.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_port_num_t *
of_bsn_tlv_partner_port_num_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_port_num_t *src)
{
    of_bsn_tlv_partner_port_num_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_port_num_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_port_num_value_get(src, &val16);
    of_bsn_tlv_partner_port_num_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_port_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_port_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_port_priority_t *
of_bsn_tlv_partner_port_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_port_priority_t *src)
{
    of_bsn_tlv_partner_port_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_port_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_port_priority_value_get(src, &val16);
    of_bsn_tlv_partner_port_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_state_t *
of_bsn_tlv_partner_state_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_state_t *src)
{
    of_bsn_tlv_partner_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_partner_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_state_value_get(src, &val8);
    of_bsn_tlv_partner_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_system_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_system_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_system_mac_t *
of_bsn_tlv_partner_system_mac_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_system_mac_t *src)
{
    of_bsn_tlv_partner_system_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_partner_system_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_system_mac_value_get(src, &mac_addr);
    of_bsn_tlv_partner_system_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_system_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_system_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_system_priority_t *
of_bsn_tlv_partner_system_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_partner_system_priority_t *src)
{
    of_bsn_tlv_partner_system_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_system_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_system_priority_value_get(src, &val16);
    of_bsn_tlv_partner_system_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_t *
of_bsn_tlv_port_OF_VERSION_1_3_dup(
    of_bsn_tlv_port_t *src)
{
    of_bsn_tlv_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_value_get(src, &port_no);
    of_bsn_tlv_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port_usage
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port_usage.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_usage_t *
of_bsn_tlv_port_usage_OF_VERSION_1_3_dup(
    of_bsn_tlv_port_usage_t *src)
{
    of_bsn_tlv_port_usage_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_port_usage_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_usage_value_get(src, &val16);
    of_bsn_tlv_port_usage_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port_vxlan_mode
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port_vxlan_mode.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_vxlan_mode_t *
of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_3_dup(
    of_bsn_tlv_port_vxlan_mode_t *src)
{
    of_bsn_tlv_port_vxlan_mode_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_port_vxlan_mode_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_vxlan_mode_value_get(src, &val8);
    of_bsn_tlv_port_vxlan_mode_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_priority_t *
of_bsn_tlv_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_priority_t *src)
{
    of_bsn_tlv_priority_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_priority_value_get(src, &val32);
    of_bsn_tlv_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_push_vlan_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_push_vlan_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_push_vlan_on_ingress_t *
of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_3_dup(
    of_bsn_tlv_push_vlan_on_ingress_t *src)
{
    of_bsn_tlv_push_vlan_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_push_vlan_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_qos_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_qos_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_qos_priority_t *
of_bsn_tlv_qos_priority_OF_VERSION_1_3_dup(
    of_bsn_tlv_qos_priority_t *src)
{
    of_bsn_tlv_qos_priority_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_qos_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_qos_priority_value_get(src, &val32);
    of_bsn_tlv_qos_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_queue_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_queue_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_queue_id_t *
of_bsn_tlv_queue_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_queue_id_t *src)
{
    of_bsn_tlv_queue_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_queue_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_queue_id_value_get(src, &val32);
    of_bsn_tlv_queue_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_queue_weight
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_queue_weight.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_queue_weight_t *
of_bsn_tlv_queue_weight_OF_VERSION_1_3_dup(
    of_bsn_tlv_queue_weight_t *src)
{
    of_bsn_tlv_queue_weight_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_queue_weight_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_queue_weight_value_get(src, &val32);
    of_bsn_tlv_queue_weight_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rate_limit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rate_limit.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rate_limit_t *
of_bsn_tlv_rate_limit_OF_VERSION_1_3_dup(
    of_bsn_tlv_rate_limit_t *src)
{
    of_bsn_tlv_rate_limit_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_rate_limit_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rate_limit_value_get(src, &val32);
    of_bsn_tlv_rate_limit_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rate_unit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rate_unit.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rate_unit_t *
of_bsn_tlv_rate_unit_OF_VERSION_1_3_dup(
    of_bsn_tlv_rate_unit_t *src)
{
    of_bsn_tlv_rate_unit_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_rate_unit_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rate_unit_value_get(src, &val8);
    of_bsn_tlv_rate_unit_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_reference
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_reference.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_reference_t *
of_bsn_tlv_reference_OF_VERSION_1_3_dup(
    of_bsn_tlv_reference_t *src)
{
    of_bsn_tlv_reference_t *dst;
    uint16_t val16;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_tlv_reference_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_reference_table_id_get(src, &val16);
    of_bsn_tlv_reference_table_id_set(dst, val16);

    of_bsn_tlv_reference_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_reference_delete(dst);
        return NULL;
    }
    of_bsn_tlv_reference_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_reply_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_reply_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_reply_packets_t *
of_bsn_tlv_reply_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_reply_packets_t *src)
{
    of_bsn_tlv_reply_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_reply_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_reply_packets_value_get(src, &val64);
    of_bsn_tlv_reply_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_request_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_request_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_request_packets_t *
of_bsn_tlv_request_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_request_packets_t *src)
{
    of_bsn_tlv_request_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_request_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_request_packets_value_get(src, &val64);
    of_bsn_tlv_request_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rx_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rx_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rx_bytes_t *
of_bsn_tlv_rx_bytes_OF_VERSION_1_3_dup(
    of_bsn_tlv_rx_bytes_t *src)
{
    of_bsn_tlv_rx_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_rx_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rx_bytes_value_get(src, &val64);
    of_bsn_tlv_rx_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rx_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rx_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rx_packets_t *
of_bsn_tlv_rx_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_rx_packets_t *src)
{
    of_bsn_tlv_rx_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_rx_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rx_packets_value_get(src, &val64);
    of_bsn_tlv_rx_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_sampling_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_sampling_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_sampling_rate_t *
of_bsn_tlv_sampling_rate_OF_VERSION_1_3_dup(
    of_bsn_tlv_sampling_rate_t *src)
{
    of_bsn_tlv_sampling_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_sampling_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_sampling_rate_value_get(src, &val32);
    of_bsn_tlv_sampling_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_set_loopback_mode
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_set_loopback_mode.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_set_loopback_mode_t *
of_bsn_tlv_set_loopback_mode_OF_VERSION_1_3_dup(
    of_bsn_tlv_set_loopback_mode_t *src)
{
    of_bsn_tlv_set_loopback_mode_t *dst;

    if ((dst = of_bsn_tlv_set_loopback_mode_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_status_t *
of_bsn_tlv_status_OF_VERSION_1_3_dup(
    of_bsn_tlv_status_t *src)
{
    of_bsn_tlv_status_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_status_value_get(src, &val8);
    of_bsn_tlv_status_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_mpls_l2_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_mpls_l2_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_mpls_l2_on_ingress_t *
of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_3_dup(
    of_bsn_tlv_strip_mpls_l2_on_ingress_t *src)
{
    of_bsn_tlv_strip_mpls_l2_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_strip_mpls_l2_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_mpls_l3_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_mpls_l3_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_mpls_l3_on_ingress_t *
of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_3_dup(
    of_bsn_tlv_strip_mpls_l3_on_ingress_t *src)
{
    of_bsn_tlv_strip_mpls_l3_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_strip_mpls_l3_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_vlan_on_egress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_vlan_on_egress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_vlan_on_egress_t *
of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_3_dup(
    of_bsn_tlv_strip_vlan_on_egress_t *src)
{
    of_bsn_tlv_strip_vlan_on_egress_t *dst;

    if ((dst = of_bsn_tlv_strip_vlan_on_egress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_sub_agent_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_sub_agent_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_sub_agent_id_t *
of_bsn_tlv_sub_agent_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_sub_agent_id_t *src)
{
    of_bsn_tlv_sub_agent_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_sub_agent_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_sub_agent_id_value_get(src, &val32);
    of_bsn_tlv_sub_agent_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_dst_t *
of_bsn_tlv_tcp_dst_OF_VERSION_1_3_dup(
    of_bsn_tlv_tcp_dst_t *src)
{
    of_bsn_tlv_tcp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_dst_value_get(src, &val16);
    of_bsn_tlv_tcp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_flags_t *
of_bsn_tlv_tcp_flags_OF_VERSION_1_3_dup(
    of_bsn_tlv_tcp_flags_t *src)
{
    of_bsn_tlv_tcp_flags_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_flags_value_get(src, &val16);
    of_bsn_tlv_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_src_t *
of_bsn_tlv_tcp_src_OF_VERSION_1_3_dup(
    of_bsn_tlv_tcp_src_t *src)
{
    of_bsn_tlv_tcp_src_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_src_value_get(src, &val16);
    of_bsn_tlv_tcp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ttl_t *
of_bsn_tlv_ttl_OF_VERSION_1_3_dup(
    of_bsn_tlv_ttl_t *src)
{
    of_bsn_tlv_ttl_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ttl_value_get(src, &val16);
    of_bsn_tlv_ttl_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tunnel_capability
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tunnel_capability.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tunnel_capability_t *
of_bsn_tlv_tunnel_capability_OF_VERSION_1_3_dup(
    of_bsn_tlv_tunnel_capability_t *src)
{
    of_bsn_tlv_tunnel_capability_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tunnel_capability_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tunnel_capability_value_get(src, &val64);
    of_bsn_tlv_tunnel_capability_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tx_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tx_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tx_bytes_t *
of_bsn_tlv_tx_bytes_OF_VERSION_1_3_dup(
    of_bsn_tlv_tx_bytes_t *src)
{
    of_bsn_tlv_tx_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tx_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tx_bytes_value_get(src, &val64);
    of_bsn_tlv_tx_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tx_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tx_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tx_packets_t *
of_bsn_tlv_tx_packets_OF_VERSION_1_3_dup(
    of_bsn_tlv_tx_packets_t *src)
{
    of_bsn_tlv_tx_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tx_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tx_packets_value_get(src, &val64);
    of_bsn_tlv_tx_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_anchor
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_anchor.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_anchor_t *
of_bsn_tlv_udf_anchor_OF_VERSION_1_3_dup(
    of_bsn_tlv_udf_anchor_t *src)
{
    of_bsn_tlv_udf_anchor_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_anchor_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_anchor_value_get(src, &val16);
    of_bsn_tlv_udf_anchor_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_id_t *
of_bsn_tlv_udf_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_udf_id_t *src)
{
    of_bsn_tlv_udf_id_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_id_value_get(src, &val16);
    of_bsn_tlv_udf_id_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_length
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_length.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_length_t *
of_bsn_tlv_udf_length_OF_VERSION_1_3_dup(
    of_bsn_tlv_udf_length_t *src)
{
    of_bsn_tlv_udf_length_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_length_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_length_value_get(src, &val16);
    of_bsn_tlv_udf_length_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_offset
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_offset.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_offset_t *
of_bsn_tlv_udf_offset_OF_VERSION_1_3_dup(
    of_bsn_tlv_udf_offset_t *src)
{
    of_bsn_tlv_udf_offset_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_offset_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_offset_value_get(src, &val16);
    of_bsn_tlv_udf_offset_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udp_dst_t *
of_bsn_tlv_udp_dst_OF_VERSION_1_3_dup(
    of_bsn_tlv_udp_dst_t *src)
{
    of_bsn_tlv_udp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udp_dst_value_get(src, &val16);
    of_bsn_tlv_udp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udp_src_t *
of_bsn_tlv_udp_src_OF_VERSION_1_3_dup(
    of_bsn_tlv_udp_src_t *src)
{
    of_bsn_tlv_udp_src_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udp_src_value_get(src, &val16);
    of_bsn_tlv_udp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_uint64_list
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_uint64_list.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_uint64_list_t *
of_bsn_tlv_uint64_list_OF_VERSION_1_3_dup(
    of_bsn_tlv_uint64_list_t *src)
{
    of_bsn_tlv_uint64_list_t *dst;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_tlv_uint64_list_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_uint64_list_value_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_uint64_list_delete(dst);
        return NULL;
    }
    of_bsn_tlv_uint64_list_value_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unicast_query_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unicast_query_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unicast_query_timeout_t *
of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_3_dup(
    of_bsn_tlv_unicast_query_timeout_t *src)
{
    of_bsn_tlv_unicast_query_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unicast_query_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unicast_query_timeout_value_get(src, &val32);
    of_bsn_tlv_unicast_query_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unicast_rate_t *
of_bsn_tlv_unicast_rate_OF_VERSION_1_3_dup(
    of_bsn_tlv_unicast_rate_t *src)
{
    of_bsn_tlv_unicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unicast_rate_value_get(src, &val32);
    of_bsn_tlv_unicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unknown_multicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unknown_multicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unknown_multicast_rate_t *
of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_3_dup(
    of_bsn_tlv_unknown_multicast_rate_t *src)
{
    of_bsn_tlv_unknown_multicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unknown_multicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unknown_multicast_rate_value_get(src, &val32);
    of_bsn_tlv_unknown_multicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_untagged
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_untagged.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_untagged_t *
of_bsn_tlv_untagged_OF_VERSION_1_3_dup(
    of_bsn_tlv_untagged_t *src)
{
    of_bsn_tlv_untagged_t *dst;

    if ((dst = of_bsn_tlv_untagged_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_use_packet_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_use_packet_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_use_packet_state_t *
of_bsn_tlv_use_packet_state_OF_VERSION_1_3_dup(
    of_bsn_tlv_use_packet_state_t *src)
{
    of_bsn_tlv_use_packet_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_use_packet_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_use_packet_state_value_get(src, &val8);
    of_bsn_tlv_use_packet_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vfi
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vfi.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vfi_t *
of_bsn_tlv_vfi_OF_VERSION_1_3_dup(
    of_bsn_tlv_vfi_t *src)
{
    of_bsn_tlv_vfi_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vfi_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vfi_value_get(src, &val16);
    of_bsn_tlv_vfi_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vfp_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vfp_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vfp_class_id_t *
of_bsn_tlv_vfp_class_id_OF_VERSION_1_3_dup(
    of_bsn_tlv_vfp_class_id_t *src)
{
    of_bsn_tlv_vfp_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vfp_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vfp_class_id_value_get(src, &val32);
    of_bsn_tlv_vfp_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_mac_list
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_mac_list.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_mac_list_t *
of_bsn_tlv_vlan_mac_list_OF_VERSION_1_3_dup(
    of_bsn_tlv_vlan_mac_list_t *src)
{
    of_bsn_tlv_vlan_mac_list_t *dst;

    of_list_bsn_vlan_mac_t src_list;
    of_list_bsn_vlan_mac_t *dst_list;

    if ((dst = of_bsn_tlv_vlan_mac_list_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_mac_list_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_vlan_mac_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_vlan_mac_list_delete(dst);
        return NULL;
    }
    of_bsn_tlv_vlan_mac_list_key_set(dst, dst_list);
    of_list_bsn_vlan_mac_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_pcp_t *
of_bsn_tlv_vlan_pcp_OF_VERSION_1_3_dup(
    of_bsn_tlv_vlan_pcp_t *src)
{
    of_bsn_tlv_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_pcp_value_get(src, &val8);
    of_bsn_tlv_vlan_pcp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_vid_t *
of_bsn_tlv_vlan_vid_OF_VERSION_1_3_dup(
    of_bsn_tlv_vlan_vid_t *src)
{
    of_bsn_tlv_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_vid_value_get(src, &val16);
    of_bsn_tlv_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_vid_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_vid_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_vid_mask_t *
of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_3_dup(
    of_bsn_tlv_vlan_vid_mask_t *src)
{
    of_bsn_tlv_vlan_vid_mask_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vlan_vid_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_vid_mask_value_get(src, &val16);
    of_bsn_tlv_vlan_vid_mask_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vni
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vni.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vni_t *
of_bsn_tlv_vni_OF_VERSION_1_3_dup(
    of_bsn_tlv_vni_t *src)
{
    of_bsn_tlv_vni_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vni_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vni_value_get(src, &val32);
    of_bsn_tlv_vni_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vpn_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vpn_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vpn_key_t *
of_bsn_tlv_vpn_key_OF_VERSION_1_3_dup(
    of_bsn_tlv_vpn_key_t *src)
{
    of_bsn_tlv_vpn_key_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vpn_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vpn_key_value_get(src, &val32);
    of_bsn_tlv_vpn_key_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vrf
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vrf.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vrf_t *
of_bsn_tlv_vrf_OF_VERSION_1_3_dup(
    of_bsn_tlv_vrf_t *src)
{
    of_bsn_tlv_vrf_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vrf_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vrf_value_get(src, &val32);
    of_bsn_tlv_vrf_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vxlan_egress_lag
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vxlan_egress_lag.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vxlan_egress_lag_t *
of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_3_dup(
    of_bsn_tlv_vxlan_egress_lag_t *src)
{
    of_bsn_tlv_vxlan_egress_lag_t *dst;

    if ((dst = of_bsn_tlv_vxlan_egress_lag_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_entry_t *
of_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_vlan_counter_stats_entry_t *src)
{
    of_bsn_vlan_counter_stats_entry_t *dst;
    uint16_t val16;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_vlan_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_entry_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_stats_entry_vlan_vid_set(dst, val16);

    of_bsn_vlan_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vlan_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_vlan_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_mac_t *
of_bsn_vlan_mac_OF_VERSION_1_3_dup(
    of_bsn_vlan_mac_t *src)
{
    of_bsn_vlan_mac_t *dst;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_vlan_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_mac_vlan_vid_get(src, &val16);
    of_bsn_vlan_mac_vlan_vid_set(dst, val16);

    of_bsn_vlan_mac_mac_get(src, &mac_addr);
    of_bsn_vlan_mac_mac_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_vport
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_vport_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_VPORT_Q_IN_Q) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_BSN_VPORT_L2GRE) {
        return of_bsn_vport_l2gre_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_vport_l2gre
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_l2gre.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_l2gre_t *
of_bsn_vport_l2gre_OF_VERSION_1_3_dup(
    of_bsn_vport_l2gre_t *src)
{
    of_bsn_vport_l2gre_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_ipv4_t ipv4;
    uint8_t val8;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_l2gre_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_l2gre_flags_get(src, &val32);
    of_bsn_vport_l2gre_flags_set(dst, val32);

    of_bsn_vport_l2gre_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_loopback_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_loopback_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_local_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_local_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_nh_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_nh_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_src_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_src_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dst_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_dst_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dscp_get(src, &val8);
    of_bsn_vport_l2gre_dscp_set(dst, val8);

    of_bsn_vport_l2gre_ttl_get(src, &val8);
    of_bsn_vport_l2gre_ttl_set(dst, val8);

    of_bsn_vport_l2gre_vpn_get(src, &val32);
    of_bsn_vport_l2gre_vpn_set(dst, val32);

    of_bsn_vport_l2gre_rate_limit_get(src, &val32);
    of_bsn_vport_l2gre_rate_limit_set(dst, val32);

    of_bsn_vport_l2gre_if_name_get(src, &port_name);
    of_bsn_vport_l2gre_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vport_q_in_q
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_q_in_q.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_q_in_q_t *
of_bsn_vport_q_in_q_OF_VERSION_1_3_dup(
    of_bsn_vport_q_in_q_t *src)
{
    of_bsn_vport_q_in_q_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_q_in_q_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_q_in_q_port_no_get(src, &val32);
    of_bsn_vport_q_in_q_port_no_set(dst, val32);

    of_bsn_vport_q_in_q_ingress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_ingress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_egress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_egress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_egress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_egress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_if_name_get(src, &port_name);
    of_bsn_vport_q_in_q_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_entry_t *
of_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(
    of_bsn_vrf_counter_stats_entry_t *src)
{
    of_bsn_vrf_counter_stats_entry_t *dst;
    uint32_t val32;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_vrf_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_entry_vrf_get(src, &val32);
    of_bsn_vrf_counter_stats_entry_vrf_set(dst, val32);

    of_bsn_vrf_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vrf_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_vrf_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_t *
of_bucket_OF_VERSION_1_3_dup(
    of_bucket_t *src)
{
    of_bucket_t *dst;
    uint16_t val16;
    of_port_no_t port_no;
    uint32_t val32;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_weight_get(src, &val16);
    of_bucket_weight_set(dst, val16);

    of_bucket_watch_port_get(src, &port_no);
    of_bucket_watch_port_set(dst, port_no);

    of_bucket_watch_group_get(src, &val32);
    of_bucket_watch_group_set(dst, val32);

    of_bucket_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_bucket_delete(dst);
        return NULL;
    }
    of_bucket_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_counter_t *
of_bucket_counter_OF_VERSION_1_3_dup(
    of_bucket_counter_t *src)
{
    of_bucket_counter_t *dst;
    uint64_t val64;

    if ((dst = of_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_counter_packet_count_get(src, &val64);
    of_bucket_counter_packet_count_set(dst, val64);

    of_bucket_counter_byte_count_get(src, &val64);
    of_bucket_counter_byte_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_entry_t *
of_flow_stats_entry_OF_VERSION_1_3_dup(
    of_flow_stats_entry_t *src)
{
    of_flow_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_entry_table_id_get(src, &val8);
    of_flow_stats_entry_table_id_set(dst, val8);

    of_flow_stats_entry_duration_sec_get(src, &val32);
    of_flow_stats_entry_duration_sec_set(dst, val32);

    of_flow_stats_entry_duration_nsec_get(src, &val32);
    of_flow_stats_entry_duration_nsec_set(dst, val32);

    of_flow_stats_entry_priority_get(src, &val16);
    of_flow_stats_entry_priority_set(dst, val16);

    of_flow_stats_entry_idle_timeout_get(src, &val16);
    of_flow_stats_entry_idle_timeout_set(dst, val16);

    of_flow_stats_entry_hard_timeout_get(src, &val16);
    of_flow_stats_entry_hard_timeout_set(dst, val16);

    of_flow_stats_entry_flags_get(src, &val16);
    of_flow_stats_entry_flags_set(dst, val16);

    of_flow_stats_entry_cookie_get(src, &val64);
    of_flow_stats_entry_cookie_set(dst, val64);

    of_flow_stats_entry_packet_count_get(src, &val64);
    of_flow_stats_entry_packet_count_set(dst, val64);

    of_flow_stats_entry_byte_count_get(src, &val64);
    of_flow_stats_entry_byte_count_set(dst, val64);

    of_flow_stats_entry_match_get(src, &match);
    of_flow_stats_entry_match_set(dst, &match);

    of_flow_stats_entry_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_entry_delete(dst);
        return NULL;
    }
    of_flow_stats_entry_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_entry_t *
of_group_desc_stats_entry_OF_VERSION_1_3_dup(
    of_group_desc_stats_entry_t *src)
{
    of_group_desc_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_entry_group_type_get(src, &val8);
    of_group_desc_stats_entry_group_type_set(dst, val8);

    of_group_desc_stats_entry_group_id_get(src, &val32);
    of_group_desc_stats_entry_group_id_set(dst, val32);

    of_group_desc_stats_entry_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_group_desc_stats_entry_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_entry_t *
of_group_stats_entry_OF_VERSION_1_3_dup(
    of_group_stats_entry_t *src)
{
    of_group_stats_entry_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_bucket_counter_t src_list;
    of_list_bucket_counter_t *dst_list;

    if ((dst = of_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_entry_group_id_get(src, &val32);
    of_group_stats_entry_group_id_set(dst, val32);

    of_group_stats_entry_ref_count_get(src, &val32);
    of_group_stats_entry_ref_count_set(dst, val32);

    of_group_stats_entry_packet_count_get(src, &val64);
    of_group_stats_entry_packet_count_set(dst, val64);

    of_group_stats_entry_byte_count_get(src, &val64);
    of_group_stats_entry_byte_count_set(dst, val64);

    of_group_stats_entry_duration_sec_get(src, &val32);
    of_group_stats_entry_duration_sec_set(dst, val32);

    of_group_stats_entry_duration_nsec_get(src, &val32);
    of_group_stats_entry_duration_nsec_set(dst, val32);

    of_group_stats_entry_bucket_stats_bind(
        src, &src_list);
    dst_list = of_list_bucket_counter_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_entry_delete(dst);
        return NULL;
    }
    of_group_stats_entry_bucket_stats_set(dst, dst_list);
    of_list_bucket_counter_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_hello_elem
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_elem.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_hello_elem_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_HELLO_ELEM_VERSIONBITMAP) {
        return of_hello_elem_versionbitmap_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_hello_elem_versionbitmap
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_elem_versionbitmap.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_elem_versionbitmap_t *
of_hello_elem_versionbitmap_OF_VERSION_1_3_dup(
    of_hello_elem_versionbitmap_t *src)
{
    of_hello_elem_versionbitmap_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_hello_elem_versionbitmap_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_elem_versionbitmap_bitmaps_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_hello_elem_versionbitmap_delete(dst);
        return NULL;
    }
    of_hello_elem_versionbitmap_bitmaps_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_APPLY_ACTIONS) {
        return of_instruction_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PACKET_OF_DEATH) {
        return of_instruction_bsn_packet_of_death_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PERMIT) {
        return of_instruction_bsn_permit_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PRIORITIZE_PDUS) {
        return of_instruction_bsn_prioritize_pdus_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_L3) {
        return of_instruction_bsn_disable_l3_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DENY) {
        return of_instruction_bsn_deny_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_CLEAR_ACTIONS) {
        return of_instruction_clear_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_ARP_OFFLOAD) {
        return of_instruction_bsn_arp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_GOTO_TABLE) {
        return of_instruction_goto_table_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_SPAN_DESTINATION) {
        return of_instruction_bsn_span_destination_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_METADATA) {
        return of_instruction_write_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_INTERNAL_PRIORITY) {
        return of_instruction_bsn_internal_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_HASH_SELECT) {
        return of_instruction_bsn_hash_select_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DHCP_OFFLOAD) {
        return of_instruction_bsn_dhcp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_AUTO_NEGOTIATION) {
        return of_instruction_bsn_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_REQUIRE_VLAN_XLATE) {
        return of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_METER) {
        return of_instruction_meter_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_NDP_OFFLOAD) {
        return of_instruction_bsn_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_VLAN_COUNTERS) {
        return of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_SRC_MAC_CHECK) {
        return of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_ACTIONS) {
        return of_instruction_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_SPLIT_HORIZON_CHECK) {
        return of_instruction_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_apply_actions_t *
of_instruction_apply_actions_OF_VERSION_1_3_dup(
    of_instruction_apply_actions_t *src)
{
    of_instruction_apply_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_apply_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_apply_actions_delete(dst);
        return NULL;
    }
    of_instruction_apply_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_arp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_arp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_arp_offload_t *
of_instruction_bsn_arp_offload_OF_VERSION_1_3_dup(
    of_instruction_bsn_arp_offload_t *src)
{
    of_instruction_bsn_arp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_arp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_arp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_arp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_arp_offload_subtype_get(src, &val32);
    of_instruction_bsn_arp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_auto_negotiation_t *
of_instruction_bsn_auto_negotiation_OF_VERSION_1_3_dup(
    of_instruction_bsn_auto_negotiation_t *src)
{
    of_instruction_bsn_auto_negotiation_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_auto_negotiation_experimenter_get(src, &val32);
    of_instruction_bsn_auto_negotiation_experimenter_set(dst, val32);

    of_instruction_bsn_auto_negotiation_subtype_get(src, &val32);
    of_instruction_bsn_auto_negotiation_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_deny
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_deny.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_deny_t *
of_instruction_bsn_deny_OF_VERSION_1_3_dup(
    of_instruction_bsn_deny_t *src)
{
    of_instruction_bsn_deny_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_deny_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_deny_experimenter_get(src, &val32);
    of_instruction_bsn_deny_experimenter_set(dst, val32);

    of_instruction_bsn_deny_subtype_get(src, &val32);
    of_instruction_bsn_deny_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_dhcp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_dhcp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_dhcp_offload_t *
of_instruction_bsn_dhcp_offload_OF_VERSION_1_3_dup(
    of_instruction_bsn_dhcp_offload_t *src)
{
    of_instruction_bsn_dhcp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_dhcp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_dhcp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_dhcp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_dhcp_offload_subtype_get(src, &val32);
    of_instruction_bsn_dhcp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_l3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_l3.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_l3_t *
of_instruction_bsn_disable_l3_OF_VERSION_1_3_dup(
    of_instruction_bsn_disable_l3_t *src)
{
    of_instruction_bsn_disable_l3_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_l3_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_l3_experimenter_get(src, &val32);
    of_instruction_bsn_disable_l3_experimenter_set(dst, val32);

    of_instruction_bsn_disable_l3_subtype_get(src, &val32);
    of_instruction_bsn_disable_l3_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_split_horizon_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_split_horizon_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_split_horizon_check_t *
of_instruction_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(
    of_instruction_bsn_disable_split_horizon_check_t *src)
{
    of_instruction_bsn_disable_split_horizon_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_split_horizon_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_split_horizon_check_experimenter_get(src, &val32);
    of_instruction_bsn_disable_split_horizon_check_experimenter_set(dst, val32);

    of_instruction_bsn_disable_split_horizon_check_subtype_get(src, &val32);
    of_instruction_bsn_disable_split_horizon_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_src_mac_check_t *
of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(
    of_instruction_bsn_disable_src_mac_check_t *src)
{
    of_instruction_bsn_disable_src_mac_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_src_mac_check_experimenter_get(src, &val32);
    of_instruction_bsn_disable_src_mac_check_experimenter_set(dst, val32);

    of_instruction_bsn_disable_src_mac_check_subtype_get(src, &val32);
    of_instruction_bsn_disable_src_mac_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_vlan_counters
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_vlan_counters.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_vlan_counters_t *
of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(
    of_instruction_bsn_disable_vlan_counters_t *src)
{
    of_instruction_bsn_disable_vlan_counters_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_vlan_counters_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_vlan_counters_experimenter_get(src, &val32);
    of_instruction_bsn_disable_vlan_counters_experimenter_set(dst, val32);

    of_instruction_bsn_disable_vlan_counters_subtype_get(src, &val32);
    of_instruction_bsn_disable_vlan_counters_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_hash_select
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_hash_select.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_hash_select_t *
of_instruction_bsn_hash_select_OF_VERSION_1_3_dup(
    of_instruction_bsn_hash_select_t *src)
{
    of_instruction_bsn_hash_select_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_hash_select_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_hash_select_experimenter_get(src, &val32);
    of_instruction_bsn_hash_select_experimenter_set(dst, val32);

    of_instruction_bsn_hash_select_subtype_get(src, &val32);
    of_instruction_bsn_hash_select_subtype_set(dst, val32);

    of_instruction_bsn_hash_select_flags_get(src, &val32);
    of_instruction_bsn_hash_select_flags_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_internal_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_internal_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_internal_priority_t *
of_instruction_bsn_internal_priority_OF_VERSION_1_3_dup(
    of_instruction_bsn_internal_priority_t *src)
{
    of_instruction_bsn_internal_priority_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_internal_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_internal_priority_experimenter_get(src, &val32);
    of_instruction_bsn_internal_priority_experimenter_set(dst, val32);

    of_instruction_bsn_internal_priority_subtype_get(src, &val32);
    of_instruction_bsn_internal_priority_subtype_set(dst, val32);

    of_instruction_bsn_internal_priority_value_get(src, &val32);
    of_instruction_bsn_internal_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_ndp_offload_t *
of_instruction_bsn_ndp_offload_OF_VERSION_1_3_dup(
    of_instruction_bsn_ndp_offload_t *src)
{
    of_instruction_bsn_ndp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_ndp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_ndp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_ndp_offload_subtype_get(src, &val32);
    of_instruction_bsn_ndp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_packet_of_death
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_packet_of_death.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_packet_of_death_t *
of_instruction_bsn_packet_of_death_OF_VERSION_1_3_dup(
    of_instruction_bsn_packet_of_death_t *src)
{
    of_instruction_bsn_packet_of_death_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_packet_of_death_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_packet_of_death_experimenter_get(src, &val32);
    of_instruction_bsn_packet_of_death_experimenter_set(dst, val32);

    of_instruction_bsn_packet_of_death_subtype_get(src, &val32);
    of_instruction_bsn_packet_of_death_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_permit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_permit.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_permit_t *
of_instruction_bsn_permit_OF_VERSION_1_3_dup(
    of_instruction_bsn_permit_t *src)
{
    of_instruction_bsn_permit_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_permit_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_permit_experimenter_get(src, &val32);
    of_instruction_bsn_permit_experimenter_set(dst, val32);

    of_instruction_bsn_permit_subtype_get(src, &val32);
    of_instruction_bsn_permit_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_prioritize_pdus
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_prioritize_pdus.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_prioritize_pdus_t *
of_instruction_bsn_prioritize_pdus_OF_VERSION_1_3_dup(
    of_instruction_bsn_prioritize_pdus_t *src)
{
    of_instruction_bsn_prioritize_pdus_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_prioritize_pdus_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_prioritize_pdus_experimenter_get(src, &val32);
    of_instruction_bsn_prioritize_pdus_experimenter_set(dst, val32);

    of_instruction_bsn_prioritize_pdus_subtype_get(src, &val32);
    of_instruction_bsn_prioritize_pdus_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_require_vlan_xlate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_require_vlan_xlate.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_require_vlan_xlate_t *
of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(
    of_instruction_bsn_require_vlan_xlate_t *src)
{
    of_instruction_bsn_require_vlan_xlate_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_require_vlan_xlate_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_require_vlan_xlate_experimenter_get(src, &val32);
    of_instruction_bsn_require_vlan_xlate_experimenter_set(dst, val32);

    of_instruction_bsn_require_vlan_xlate_subtype_get(src, &val32);
    of_instruction_bsn_require_vlan_xlate_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_span_destination
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_span_destination.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_span_destination_t *
of_instruction_bsn_span_destination_OF_VERSION_1_3_dup(
    of_instruction_bsn_span_destination_t *src)
{
    of_instruction_bsn_span_destination_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_span_destination_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_span_destination_experimenter_get(src, &val32);
    of_instruction_bsn_span_destination_experimenter_set(dst, val32);

    of_instruction_bsn_span_destination_subtype_get(src, &val32);
    of_instruction_bsn_span_destination_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_clear_actions_t *
of_instruction_clear_actions_OF_VERSION_1_3_dup(
    of_instruction_clear_actions_t *src)
{
    of_instruction_clear_actions_t *dst;

    if ((dst = of_instruction_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_goto_table_t *
of_instruction_goto_table_OF_VERSION_1_3_dup(
    of_instruction_goto_table_t *src)
{
    of_instruction_goto_table_t *dst;
    uint8_t val8;

    if ((dst = of_instruction_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_goto_table_table_id_get(src, &val8);
    of_instruction_goto_table_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction_id
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_id_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_ID_BSN_NDP_OFFLOAD) {
        return of_instruction_id_bsn_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_INTERNAL_PRIORITY) {
        return of_instruction_id_bsn_internal_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_AUTO_NEGOTIATION) {
        return of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_METER) {
        return of_instruction_id_meter_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DHCP_OFFLOAD) {
        return of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_REQUIRE_VLAN_XLATE) {
        return of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_HASH_SELECT) {
        return of_instruction_id_bsn_hash_select_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_VLAN_COUNTERS) {
        return of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_WRITE_ACTIONS) {
        return of_instruction_id_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_SRC_MAC_CHECK) {
        return of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_APPLY_ACTIONS) {
        return of_instruction_id_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PACKET_OF_DEATH) {
        return of_instruction_id_bsn_packet_of_death_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_SPLIT_HORIZON_CHECK) {
        return of_instruction_id_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_ARP_OFFLOAD) {
        return of_instruction_id_bsn_arp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PRIORITIZE_PDUS) {
        return of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PERMIT) {
        return of_instruction_id_bsn_permit_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DENY) {
        return of_instruction_id_bsn_deny_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_WRITE_METADATA) {
        return of_instruction_id_write_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_CLEAR_ACTIONS) {
        return of_instruction_id_clear_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_GOTO_TABLE) {
        return of_instruction_id_goto_table_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_L3) {
        return of_instruction_id_bsn_disable_l3_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_SPAN_DESTINATION) {
        return of_instruction_id_bsn_span_destination_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_id_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_apply_actions_t *
of_instruction_id_apply_actions_OF_VERSION_1_3_dup(
    of_instruction_id_apply_actions_t *src)
{
    of_instruction_id_apply_actions_t *dst;

    if ((dst = of_instruction_id_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_arp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_arp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_arp_offload_t *
of_instruction_id_bsn_arp_offload_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_arp_offload_t *src)
{
    of_instruction_id_bsn_arp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_arp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_arp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_arp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_arp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_arp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_auto_negotiation_t *
of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_auto_negotiation_t *src)
{
    of_instruction_id_bsn_auto_negotiation_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_auto_negotiation_experimenter_get(src, &val32);
    of_instruction_id_bsn_auto_negotiation_experimenter_set(dst, val32);

    of_instruction_id_bsn_auto_negotiation_subtype_get(src, &val32);
    of_instruction_id_bsn_auto_negotiation_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_deny
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_deny.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_deny_t *
of_instruction_id_bsn_deny_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_deny_t *src)
{
    of_instruction_id_bsn_deny_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_deny_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_deny_experimenter_get(src, &val32);
    of_instruction_id_bsn_deny_experimenter_set(dst, val32);

    of_instruction_id_bsn_deny_subtype_get(src, &val32);
    of_instruction_id_bsn_deny_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_dhcp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_dhcp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_dhcp_offload_t *
of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_dhcp_offload_t *src)
{
    of_instruction_id_bsn_dhcp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_dhcp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_dhcp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_dhcp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_dhcp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_dhcp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_l3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_l3.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_l3_t *
of_instruction_id_bsn_disable_l3_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_disable_l3_t *src)
{
    of_instruction_id_bsn_disable_l3_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_l3_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_l3_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_l3_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_l3_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_l3_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_split_horizon_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_split_horizon_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_split_horizon_check_t *
of_instruction_id_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_disable_split_horizon_check_t *src)
{
    of_instruction_id_bsn_disable_split_horizon_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_split_horizon_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_split_horizon_check_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_split_horizon_check_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_split_horizon_check_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_split_horizon_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_src_mac_check_t *
of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_disable_src_mac_check_t *src)
{
    of_instruction_id_bsn_disable_src_mac_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_src_mac_check_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_src_mac_check_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_src_mac_check_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_src_mac_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_vlan_counters
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_vlan_counters.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_vlan_counters_t *
of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_disable_vlan_counters_t *src)
{
    of_instruction_id_bsn_disable_vlan_counters_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_vlan_counters_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_vlan_counters_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_vlan_counters_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_vlan_counters_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_vlan_counters_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_hash_select
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_hash_select.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_hash_select_t *
of_instruction_id_bsn_hash_select_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_hash_select_t *src)
{
    of_instruction_id_bsn_hash_select_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_hash_select_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_hash_select_experimenter_get(src, &val32);
    of_instruction_id_bsn_hash_select_experimenter_set(dst, val32);

    of_instruction_id_bsn_hash_select_subtype_get(src, &val32);
    of_instruction_id_bsn_hash_select_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_internal_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_internal_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_internal_priority_t *
of_instruction_id_bsn_internal_priority_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_internal_priority_t *src)
{
    of_instruction_id_bsn_internal_priority_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_internal_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_internal_priority_experimenter_get(src, &val32);
    of_instruction_id_bsn_internal_priority_experimenter_set(dst, val32);

    of_instruction_id_bsn_internal_priority_subtype_get(src, &val32);
    of_instruction_id_bsn_internal_priority_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_ndp_offload_t *
of_instruction_id_bsn_ndp_offload_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_ndp_offload_t *src)
{
    of_instruction_id_bsn_ndp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_ndp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_ndp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_ndp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_ndp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_packet_of_death
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_packet_of_death.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_packet_of_death_t *
of_instruction_id_bsn_packet_of_death_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_packet_of_death_t *src)
{
    of_instruction_id_bsn_packet_of_death_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_packet_of_death_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_packet_of_death_experimenter_get(src, &val32);
    of_instruction_id_bsn_packet_of_death_experimenter_set(dst, val32);

    of_instruction_id_bsn_packet_of_death_subtype_get(src, &val32);
    of_instruction_id_bsn_packet_of_death_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_permit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_permit.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_permit_t *
of_instruction_id_bsn_permit_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_permit_t *src)
{
    of_instruction_id_bsn_permit_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_permit_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_permit_experimenter_get(src, &val32);
    of_instruction_id_bsn_permit_experimenter_set(dst, val32);

    of_instruction_id_bsn_permit_subtype_get(src, &val32);
    of_instruction_id_bsn_permit_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_prioritize_pdus
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_prioritize_pdus.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_prioritize_pdus_t *
of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_prioritize_pdus_t *src)
{
    of_instruction_id_bsn_prioritize_pdus_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_prioritize_pdus_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_prioritize_pdus_experimenter_get(src, &val32);
    of_instruction_id_bsn_prioritize_pdus_experimenter_set(dst, val32);

    of_instruction_id_bsn_prioritize_pdus_subtype_get(src, &val32);
    of_instruction_id_bsn_prioritize_pdus_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_require_vlan_xlate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_require_vlan_xlate.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_require_vlan_xlate_t *
of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_require_vlan_xlate_t *src)
{
    of_instruction_id_bsn_require_vlan_xlate_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_require_vlan_xlate_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_require_vlan_xlate_experimenter_get(src, &val32);
    of_instruction_id_bsn_require_vlan_xlate_experimenter_set(dst, val32);

    of_instruction_id_bsn_require_vlan_xlate_subtype_get(src, &val32);
    of_instruction_id_bsn_require_vlan_xlate_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_span_destination
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_span_destination.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_span_destination_t *
of_instruction_id_bsn_span_destination_OF_VERSION_1_3_dup(
    of_instruction_id_bsn_span_destination_t *src)
{
    of_instruction_id_bsn_span_destination_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_span_destination_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_span_destination_experimenter_get(src, &val32);
    of_instruction_id_bsn_span_destination_experimenter_set(dst, val32);

    of_instruction_id_bsn_span_destination_subtype_get(src, &val32);
    of_instruction_id_bsn_span_destination_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_clear_actions_t *
of_instruction_id_clear_actions_OF_VERSION_1_3_dup(
    of_instruction_id_clear_actions_t *src)
{
    of_instruction_id_clear_actions_t *dst;

    if ((dst = of_instruction_id_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_goto_table_t *
of_instruction_id_goto_table_OF_VERSION_1_3_dup(
    of_instruction_id_goto_table_t *src)
{
    of_instruction_id_goto_table_t *dst;

    if ((dst = of_instruction_id_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_meter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_meter.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_meter_t *
of_instruction_id_meter_OF_VERSION_1_3_dup(
    of_instruction_id_meter_t *src)
{
    of_instruction_id_meter_t *dst;

    if ((dst = of_instruction_id_meter_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_write_actions_t *
of_instruction_id_write_actions_OF_VERSION_1_3_dup(
    of_instruction_id_write_actions_t *src)
{
    of_instruction_id_write_actions_t *dst;

    if ((dst = of_instruction_id_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_write_metadata_t *
of_instruction_id_write_metadata_OF_VERSION_1_3_dup(
    of_instruction_id_write_metadata_t *src)
{
    of_instruction_id_write_metadata_t *dst;

    if ((dst = of_instruction_id_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_meter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_meter.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_meter_t *
of_instruction_meter_OF_VERSION_1_3_dup(
    of_instruction_meter_t *src)
{
    of_instruction_meter_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_meter_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_meter_meter_id_get(src, &val32);
    of_instruction_meter_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_actions_t *
of_instruction_write_actions_OF_VERSION_1_3_dup(
    of_instruction_write_actions_t *src)
{
    of_instruction_write_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_write_actions_delete(dst);
        return NULL;
    }
    of_instruction_write_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_metadata_t *
of_instruction_write_metadata_OF_VERSION_1_3_dup(
    of_instruction_write_metadata_t *src)
{
    of_instruction_write_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_instruction_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_metadata_metadata_get(src, &val64);
    of_instruction_write_metadata_metadata_set(dst, val64);

    of_instruction_write_metadata_metadata_mask_get(src, &val64);
    of_instruction_write_metadata_metadata_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_match_v3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_match_v3.
 *
 * The caller is responsible for deleting the returned value
 */
of_match_v3_t *
of_match_v3_OF_VERSION_1_3_dup(
    of_match_v3_t *src)
{
    of_match_v3_t *dst;

    of_list_oxm_t src_list;
    of_list_oxm_t *dst_list;

    if ((dst = of_match_v3_new(src->version)) == NULL) {
        return NULL;
    }

    of_match_v3_oxm_list_bind(
        src, &src_list);
    dst_list = of_list_oxm_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_match_v3_delete(dst);
        return NULL;
    }
    of_match_v3_oxm_list_set(dst, dst_list);
    of_list_oxm_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_meter_band
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_meter_band_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_METER_BAND_DROP) {
        return of_meter_band_drop_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_METER_BAND_DSCP_REMARK) {
        return of_meter_band_dscp_remark_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_METER_BAND_EXPERIMENTER) {
        return of_meter_band_experimenter_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_meter_band_drop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_drop.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_drop_t *
of_meter_band_drop_OF_VERSION_1_3_dup(
    of_meter_band_drop_t *src)
{
    of_meter_band_drop_t *dst;
    uint32_t val32;

    if ((dst = of_meter_band_drop_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_drop_rate_get(src, &val32);
    of_meter_band_drop_rate_set(dst, val32);

    of_meter_band_drop_burst_size_get(src, &val32);
    of_meter_band_drop_burst_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_dscp_remark
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_dscp_remark.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_dscp_remark_t *
of_meter_band_dscp_remark_OF_VERSION_1_3_dup(
    of_meter_band_dscp_remark_t *src)
{
    of_meter_band_dscp_remark_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_meter_band_dscp_remark_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_dscp_remark_rate_get(src, &val32);
    of_meter_band_dscp_remark_rate_set(dst, val32);

    of_meter_band_dscp_remark_burst_size_get(src, &val32);
    of_meter_band_dscp_remark_burst_size_set(dst, val32);

    of_meter_band_dscp_remark_prec_level_get(src, &val8);
    of_meter_band_dscp_remark_prec_level_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_experimenter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_experimenter.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_experimenter_t *
of_meter_band_experimenter_OF_VERSION_1_3_dup(
    of_meter_band_experimenter_t *src)
{
    of_meter_band_experimenter_t *dst;
    uint32_t val32;

    if ((dst = of_meter_band_experimenter_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_experimenter_rate_get(src, &val32);
    of_meter_band_experimenter_rate_set(dst, val32);

    of_meter_band_experimenter_burst_size_get(src, &val32);
    of_meter_band_experimenter_burst_size_set(dst, val32);

    of_meter_band_experimenter_experimenter_get(src, &val32);
    of_meter_band_experimenter_experimenter_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_stats_t *
of_meter_band_stats_OF_VERSION_1_3_dup(
    of_meter_band_stats_t *src)
{
    of_meter_band_stats_t *dst;
    uint64_t val64;

    if ((dst = of_meter_band_stats_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_stats_packet_band_count_get(src, &val64);
    of_meter_band_stats_packet_band_count_set(dst, val64);

    of_meter_band_stats_byte_band_count_get(src, &val64);
    of_meter_band_stats_byte_band_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_t *
of_meter_config_OF_VERSION_1_3_dup(
    of_meter_config_t *src)
{
    of_meter_config_t *dst;
    uint16_t val16;
    uint32_t val32;

    of_list_meter_band_t src_list;
    of_list_meter_band_t *dst_list;

    if ((dst = of_meter_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_flags_get(src, &val16);
    of_meter_config_flags_set(dst, val16);

    of_meter_config_meter_id_get(src, &val32);
    of_meter_config_meter_id_set(dst, val32);

    of_meter_config_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_band_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_config_delete(dst);
        return NULL;
    }
    of_meter_config_entries_set(dst, dst_list);
    of_list_meter_band_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_t *
of_meter_features_OF_VERSION_1_3_dup(
    of_meter_features_t *src)
{
    of_meter_features_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_meter_features_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_max_meter_get(src, &val32);
    of_meter_features_max_meter_set(dst, val32);

    of_meter_features_band_types_get(src, &val32);
    of_meter_features_band_types_set(dst, val32);

    of_meter_features_capabilities_get(src, &val32);
    of_meter_features_capabilities_set(dst, val32);

    of_meter_features_max_bands_get(src, &val8);
    of_meter_features_max_bands_set(dst, val8);

    of_meter_features_max_color_get(src, &val8);
    of_meter_features_max_color_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_t *
of_meter_stats_OF_VERSION_1_3_dup(
    of_meter_stats_t *src)
{
    of_meter_stats_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_meter_band_stats_t src_list;
    of_list_meter_band_stats_t *dst_list;

    if ((dst = of_meter_stats_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_meter_id_get(src, &val32);
    of_meter_stats_meter_id_set(dst, val32);

    of_meter_stats_flow_count_get(src, &val32);
    of_meter_stats_flow_count_set(dst, val32);

    of_meter_stats_packet_in_count_get(src, &val64);
    of_meter_stats_packet_in_count_set(dst, val64);

    of_meter_stats_byte_in_count_get(src, &val64);
    of_meter_stats_byte_in_count_set(dst, val64);

    of_meter_stats_duration_sec_get(src, &val32);
    of_meter_stats_duration_sec_set(dst, val32);

    of_meter_stats_duration_nsec_get(src, &val32);
    of_meter_stats_duration_nsec_set(dst, val32);

    of_meter_stats_band_stats_bind(
        src, &src_list);
    dst_list = of_list_meter_band_stats_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_stats_delete(dst);
        return NULL;
    }
    of_meter_stats_band_stats_set(dst, dst_list);
    of_list_meter_band_stats_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_oxm
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_oxm_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_OXM_IN_PORT_MASKED) {
        return of_oxm_in_port_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_EXTHDR_MASKED) {
        return of_oxm_ipv6_exthdr_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION_MASKED) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_DST_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_BOS_MASKED) {
        return of_oxm_mpls_bos_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT) {
        return of_oxm_in_phy_port_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST_MASKED) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VFI_MASKED) {
        return of_oxm_bsn_vfi_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_VLAN_VID) {
        return of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE_MASKED) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT_MASKED) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE) {
        return of_oxm_icmpv4_code_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2_MASKED) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4_MASKED) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST) {
        return of_oxm_udp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE) {
        return of_oxm_eth_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST) {
        return of_oxm_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP) {
        return of_oxm_vlan_pcp_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET_MASKED) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP) {
        return of_oxm_arp_op_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE) {
        return of_oxm_icmpv4_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL_MASKED) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE) {
        return of_oxm_icmpv6_type_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA_MASKED) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST_MASKED) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP_MASKED) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA) {
        return of_oxm_arp_sha_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE_MASKED) {
        return of_oxm_eth_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST) {
        return of_oxm_tcp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO_MASKED) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN_MASKED) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA_MASKED) {
        return of_oxm_metadata_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_GLOBAL_VRF_ALLOWED_MASKED) {
        return of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_GLOBAL_VRF_ALLOWED) {
        return of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA_MASKED) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC) {
        return of_oxm_sctp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512_MASKED) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC) {
        return of_oxm_udp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_DST_CLASS_ID) {
        return of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST_MASKED) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_BOS) {
        return of_oxm_mpls_bos_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST_MASKED) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA_MASKED) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST) {
        return of_oxm_sctp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5_MASKED) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_DST_MASKED) {
        return of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE_MASKED) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL_MASKED) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE_MASKED) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS_MASKED) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST) {
        return of_oxm_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128_MASKED) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_SRC) {
        return of_oxm_bsn_inner_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC_MASKED) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VXLAN_NETWORK_ID_MASKED) {
        return of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_DST) {
        return of_oxm_bsn_inner_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC) {
        return of_oxm_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_SRC_MASKED) {
        return of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5) {
        return of_oxm_bsn_udf5_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4) {
        return of_oxm_bsn_udf4_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7) {
        return of_oxm_bsn_udf7_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS_MASKED) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1) {
        return of_oxm_bsn_udf1_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0) {
        return of_oxm_bsn_udf0_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3) {
        return of_oxm_bsn_udf3_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2) {
        return of_oxm_bsn_udf2_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE) {
        return of_oxm_icmpv6_code_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP_MASKED) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL) {
        return of_oxm_mpls_label_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_VLAN_VID_MASKED) {
        return of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6) {
        return of_oxm_bsn_udf6_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PORT) {
        return of_oxm_in_port_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7_MASKED) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1_MASKED) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF_MASKED) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC) {
        return of_oxm_tcp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC_MASKED) {
        return of_oxm_udp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN) {
        return of_oxm_ip_ecn_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC) {
        return of_oxm_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST) {
        return of_oxm_ipv6_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC_MASKED) {
        return of_oxm_eth_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF) {
        return of_oxm_bsn_vrf_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3_MASKED) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC_MASKED) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP) {
        return of_oxm_ip_dscp_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID_MASKED) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL_MASKED) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC_MASKED) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST_MASKED) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6_MASKED) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC_MASKED) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID_MASKED) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT_MASKED) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO) {
        return of_oxm_ip_proto_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC) {
        return of_oxm_ipv6_src_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST_MASKED) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VFI) {
        return of_oxm_bsn_vfi_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA) {
        return of_oxm_arp_spa_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP_MASKED) {
        return of_oxm_arp_op_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_ID_MASKED) {
        return of_oxm_tunnel_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC_MASKED) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID) {
        return of_oxm_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE_MASKED) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC_MASKED) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_ID) {
        return of_oxm_tunnel_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA) {
        return of_oxm_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0_MASKED) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA_MASKED) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA) {
        return of_oxm_arp_tha_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC) {
        return of_oxm_mpls_tc_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VXLAN_NETWORK_ID) {
        return of_oxm_bsn_vxlan_network_id_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA) {
        return of_oxm_arp_tpa_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST_MASKED) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_EXTHDR) {
        return of_oxm_ipv6_exthdr_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL_MASKED) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_oxm_arp_op
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_t *
of_oxm_arp_op_OF_VERSION_1_3_dup(
    of_oxm_arp_op_t *src)
{
    of_oxm_arp_op_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_value_get(src, &val16);
    of_oxm_arp_op_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_op_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_masked_t *
of_oxm_arp_op_masked_OF_VERSION_1_3_dup(
    of_oxm_arp_op_masked_t *src)
{
    of_oxm_arp_op_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_masked_value_get(src, &val16);
    of_oxm_arp_op_masked_value_set(dst, val16);

    of_oxm_arp_op_masked_value_mask_get(src, &val16);
    of_oxm_arp_op_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_t *
of_oxm_arp_sha_OF_VERSION_1_3_dup(
    of_oxm_arp_sha_t *src)
{
    of_oxm_arp_sha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_value_get(src, &mac_addr);
    of_oxm_arp_sha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_masked_t *
of_oxm_arp_sha_masked_OF_VERSION_1_3_dup(
    of_oxm_arp_sha_masked_t *src)
{
    of_oxm_arp_sha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_masked_value_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_set(dst, mac_addr);

    of_oxm_arp_sha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_t *
of_oxm_arp_spa_OF_VERSION_1_3_dup(
    of_oxm_arp_spa_t *src)
{
    of_oxm_arp_spa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_value_get(src, &val32);
    of_oxm_arp_spa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_masked_t *
of_oxm_arp_spa_masked_OF_VERSION_1_3_dup(
    of_oxm_arp_spa_masked_t *src)
{
    of_oxm_arp_spa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_masked_value_get(src, &val32);
    of_oxm_arp_spa_masked_value_set(dst, val32);

    of_oxm_arp_spa_masked_value_mask_get(src, &val32);
    of_oxm_arp_spa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_t *
of_oxm_arp_tha_OF_VERSION_1_3_dup(
    of_oxm_arp_tha_t *src)
{
    of_oxm_arp_tha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_value_get(src, &mac_addr);
    of_oxm_arp_tha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_masked_t *
of_oxm_arp_tha_masked_OF_VERSION_1_3_dup(
    of_oxm_arp_tha_masked_t *src)
{
    of_oxm_arp_tha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_masked_value_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_set(dst, mac_addr);

    of_oxm_arp_tha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_t *
of_oxm_arp_tpa_OF_VERSION_1_3_dup(
    of_oxm_arp_tpa_t *src)
{
    of_oxm_arp_tpa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_value_get(src, &val32);
    of_oxm_arp_tpa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_masked_t *
of_oxm_arp_tpa_masked_OF_VERSION_1_3_dup(
    of_oxm_arp_tpa_masked_t *src)
{
    of_oxm_arp_tpa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_masked_value_get(src, &val32);
    of_oxm_arp_tpa_masked_value_set(dst, val32);

    of_oxm_arp_tpa_masked_value_mask_get(src, &val32);
    of_oxm_arp_tpa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_t *
of_oxm_bsn_egr_port_group_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_egr_port_group_id_t *src)
{
    of_oxm_bsn_egr_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_masked_t *
of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_egr_port_group_id_masked_t *src)
{
    of_oxm_bsn_egr_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_egr_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_global_vrf_allowed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_global_vrf_allowed.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_global_vrf_allowed_t *
of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_3_dup(
    of_oxm_bsn_global_vrf_allowed_t *src)
{
    of_oxm_bsn_global_vrf_allowed_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_global_vrf_allowed_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_global_vrf_allowed_value_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_global_vrf_allowed_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_global_vrf_allowed_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_global_vrf_allowed_masked_t *
of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_global_vrf_allowed_masked_t *src)
{
    of_oxm_bsn_global_vrf_allowed_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_global_vrf_allowed_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_global_vrf_allowed_masked_value_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_masked_value_set(dst, val8);

    of_oxm_bsn_global_vrf_allowed_masked_value_mask_get(src, &val8);
    of_oxm_bsn_global_vrf_allowed_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_t *
of_oxm_bsn_in_ports_128_OF_VERSION_1_3_dup(
    of_oxm_bsn_in_ports_128_t *src)
{
    of_oxm_bsn_in_ports_128_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_value_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_masked_t *
of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_in_ports_128_masked_t *src)
{
    of_oxm_bsn_in_ports_128_masked_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_masked_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_set(dst, bitmap_128);

    of_oxm_bsn_in_ports_128_masked_value_mask_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_mask_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_t *
of_oxm_bsn_in_ports_512_OF_VERSION_1_3_dup(
    of_oxm_bsn_in_ports_512_t *src)
{
    of_oxm_bsn_in_ports_512_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_value_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_masked_t *
of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_in_ports_512_masked_t *src)
{
    of_oxm_bsn_in_ports_512_masked_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_masked_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_set(dst, bitmap_512);

    of_oxm_bsn_in_ports_512_masked_value_mask_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_mask_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_t *
of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_ingress_port_group_id_t *src)
{
    of_oxm_bsn_ingress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_masked_t *
of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_ingress_port_group_id_masked_t *src)
{
    of_oxm_bsn_ingress_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_ingress_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_dst_t *
of_oxm_bsn_inner_eth_dst_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_eth_dst_t *src)
{
    of_oxm_bsn_inner_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_dst_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_dst_masked_t *
of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_eth_dst_masked_t *src)
{
    of_oxm_bsn_inner_eth_dst_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_dst_masked_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_masked_value_set(dst, mac_addr);

    of_oxm_bsn_inner_eth_dst_masked_value_mask_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_src_t *
of_oxm_bsn_inner_eth_src_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_eth_src_t *src)
{
    of_oxm_bsn_inner_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_src_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_src_masked_t *
of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_eth_src_masked_t *src)
{
    of_oxm_bsn_inner_eth_src_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_src_masked_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_masked_value_set(dst, mac_addr);

    of_oxm_bsn_inner_eth_src_masked_value_mask_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_vlan_vid_t *
of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_vlan_vid_t *src)
{
    of_oxm_bsn_inner_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_inner_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_vlan_vid_value_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_vlan_vid_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_vlan_vid_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_vlan_vid_masked_t *
of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_inner_vlan_vid_masked_t *src)
{
    of_oxm_bsn_inner_vlan_vid_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_inner_vlan_vid_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_vlan_vid_masked_value_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_masked_value_set(dst, val16);

    of_oxm_bsn_inner_vlan_vid_masked_value_mask_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_t *
of_oxm_bsn_ip_fragmentation_OF_VERSION_1_3_dup(
    of_oxm_bsn_ip_fragmentation_t *src)
{
    of_oxm_bsn_ip_fragmentation_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_masked_t *
of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_ip_fragmentation_masked_t *src)
{
    of_oxm_bsn_ip_fragmentation_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_masked_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_set(dst, val8);

    of_oxm_bsn_ip_fragmentation_masked_value_mask_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_t *
of_oxm_bsn_l2_cache_hit_OF_VERSION_1_3_dup(
    of_oxm_bsn_l2_cache_hit_t *src)
{
    of_oxm_bsn_l2_cache_hit_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_masked_t *
of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_l2_cache_hit_masked_t *src)
{
    of_oxm_bsn_l2_cache_hit_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_masked_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_set(dst, val8);

    of_oxm_bsn_l2_cache_hit_masked_value_mask_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_dst_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_dst_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_dst_class_id_t *
of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_dst_class_id_t *src)
{
    of_oxm_bsn_l3_dst_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_dst_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_dst_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_dst_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_dst_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_dst_class_id_masked_t *
of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_dst_class_id_masked_t *src)
{
    of_oxm_bsn_l3_dst_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_dst_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_dst_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_dst_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_dst_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_t *
of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_interface_class_id_t *src)
{
    of_oxm_bsn_l3_interface_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_masked_t *
of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_interface_class_id_masked_t *src)
{
    of_oxm_bsn_l3_interface_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_interface_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_t *
of_oxm_bsn_l3_src_class_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_src_class_id_t *src)
{
    of_oxm_bsn_l3_src_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_masked_t *
of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_l3_src_class_id_masked_t *src)
{
    of_oxm_bsn_l3_src_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_src_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_t *
of_oxm_bsn_lag_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_lag_id_t *src)
{
    of_oxm_bsn_lag_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_value_get(src, &val32);
    of_oxm_bsn_lag_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_masked_t *
of_oxm_bsn_lag_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_lag_id_masked_t *src)
{
    of_oxm_bsn_lag_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_masked_value_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_set(dst, val32);

    of_oxm_bsn_lag_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_t *
of_oxm_bsn_tcp_flags_OF_VERSION_1_3_dup(
    of_oxm_bsn_tcp_flags_t *src)
{
    of_oxm_bsn_tcp_flags_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_masked_t *
of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_tcp_flags_masked_t *src)
{
    of_oxm_bsn_tcp_flags_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_masked_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_set(dst, val16);

    of_oxm_bsn_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_t *
of_oxm_bsn_udf0_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf0_t *src)
{
    of_oxm_bsn_udf0_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_value_get(src, &val32);
    of_oxm_bsn_udf0_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_masked_t *
of_oxm_bsn_udf0_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf0_masked_t *src)
{
    of_oxm_bsn_udf0_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_masked_value_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_set(dst, val32);

    of_oxm_bsn_udf0_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_t *
of_oxm_bsn_udf1_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf1_t *src)
{
    of_oxm_bsn_udf1_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_value_get(src, &val32);
    of_oxm_bsn_udf1_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_masked_t *
of_oxm_bsn_udf1_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf1_masked_t *src)
{
    of_oxm_bsn_udf1_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_masked_value_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_set(dst, val32);

    of_oxm_bsn_udf1_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_t *
of_oxm_bsn_udf2_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf2_t *src)
{
    of_oxm_bsn_udf2_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_value_get(src, &val32);
    of_oxm_bsn_udf2_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_masked_t *
of_oxm_bsn_udf2_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf2_masked_t *src)
{
    of_oxm_bsn_udf2_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_masked_value_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_set(dst, val32);

    of_oxm_bsn_udf2_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_t *
of_oxm_bsn_udf3_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf3_t *src)
{
    of_oxm_bsn_udf3_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_value_get(src, &val32);
    of_oxm_bsn_udf3_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_masked_t *
of_oxm_bsn_udf3_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf3_masked_t *src)
{
    of_oxm_bsn_udf3_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_masked_value_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_set(dst, val32);

    of_oxm_bsn_udf3_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_t *
of_oxm_bsn_udf4_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf4_t *src)
{
    of_oxm_bsn_udf4_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_value_get(src, &val32);
    of_oxm_bsn_udf4_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_masked_t *
of_oxm_bsn_udf4_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf4_masked_t *src)
{
    of_oxm_bsn_udf4_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_masked_value_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_set(dst, val32);

    of_oxm_bsn_udf4_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_t *
of_oxm_bsn_udf5_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf5_t *src)
{
    of_oxm_bsn_udf5_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_value_get(src, &val32);
    of_oxm_bsn_udf5_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_masked_t *
of_oxm_bsn_udf5_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf5_masked_t *src)
{
    of_oxm_bsn_udf5_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_masked_value_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_set(dst, val32);

    of_oxm_bsn_udf5_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_t *
of_oxm_bsn_udf6_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf6_t *src)
{
    of_oxm_bsn_udf6_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_value_get(src, &val32);
    of_oxm_bsn_udf6_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_masked_t *
of_oxm_bsn_udf6_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf6_masked_t *src)
{
    of_oxm_bsn_udf6_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_masked_value_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_set(dst, val32);

    of_oxm_bsn_udf6_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_t *
of_oxm_bsn_udf7_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf7_t *src)
{
    of_oxm_bsn_udf7_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_value_get(src, &val32);
    of_oxm_bsn_udf7_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_masked_t *
of_oxm_bsn_udf7_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_udf7_masked_t *src)
{
    of_oxm_bsn_udf7_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_masked_value_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_set(dst, val32);

    of_oxm_bsn_udf7_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vfi
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vfi.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vfi_t *
of_oxm_bsn_vfi_OF_VERSION_1_3_dup(
    of_oxm_bsn_vfi_t *src)
{
    of_oxm_bsn_vfi_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_vfi_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vfi_value_get(src, &val16);
    of_oxm_bsn_vfi_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vfi_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vfi_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vfi_masked_t *
of_oxm_bsn_vfi_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_vfi_masked_t *src)
{
    of_oxm_bsn_vfi_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_vfi_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vfi_masked_value_get(src, &val16);
    of_oxm_bsn_vfi_masked_value_set(dst, val16);

    of_oxm_bsn_vfi_masked_value_mask_get(src, &val16);
    of_oxm_bsn_vfi_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_t *
of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_masked_t *
of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_t *
of_oxm_bsn_vrf_OF_VERSION_1_3_dup(
    of_oxm_bsn_vrf_t *src)
{
    of_oxm_bsn_vrf_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_value_get(src, &val32);
    of_oxm_bsn_vrf_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_masked_t *
of_oxm_bsn_vrf_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_vrf_masked_t *src)
{
    of_oxm_bsn_vrf_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_masked_value_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_set(dst, val32);

    of_oxm_bsn_vrf_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vxlan_network_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vxlan_network_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vxlan_network_id_t *
of_oxm_bsn_vxlan_network_id_OF_VERSION_1_3_dup(
    of_oxm_bsn_vxlan_network_id_t *src)
{
    of_oxm_bsn_vxlan_network_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vxlan_network_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vxlan_network_id_value_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vxlan_network_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vxlan_network_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vxlan_network_id_masked_t *
of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_3_dup(
    of_oxm_bsn_vxlan_network_id_masked_t *src)
{
    of_oxm_bsn_vxlan_network_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vxlan_network_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vxlan_network_id_masked_value_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_masked_value_set(dst, val32);

    of_oxm_bsn_vxlan_network_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_t *
of_oxm_eth_dst_OF_VERSION_1_3_dup(
    of_oxm_eth_dst_t *src)
{
    of_oxm_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_value_get(src, &mac_addr);
    of_oxm_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_masked_t *
of_oxm_eth_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_eth_dst_masked_t *src)
{
    of_oxm_eth_dst_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_masked_value_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_set(dst, mac_addr);

    of_oxm_eth_dst_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_t *
of_oxm_eth_src_OF_VERSION_1_3_dup(
    of_oxm_eth_src_t *src)
{
    of_oxm_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_value_get(src, &mac_addr);
    of_oxm_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_masked_t *
of_oxm_eth_src_masked_OF_VERSION_1_3_dup(
    of_oxm_eth_src_masked_t *src)
{
    of_oxm_eth_src_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_masked_value_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_set(dst, mac_addr);

    of_oxm_eth_src_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_t *
of_oxm_eth_type_OF_VERSION_1_3_dup(
    of_oxm_eth_type_t *src)
{
    of_oxm_eth_type_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_value_get(src, &val16);
    of_oxm_eth_type_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_masked_t *
of_oxm_eth_type_masked_OF_VERSION_1_3_dup(
    of_oxm_eth_type_masked_t *src)
{
    of_oxm_eth_type_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_masked_value_get(src, &val16);
    of_oxm_eth_type_masked_value_set(dst, val16);

    of_oxm_eth_type_masked_value_mask_get(src, &val16);
    of_oxm_eth_type_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_t *
of_oxm_icmpv4_code_OF_VERSION_1_3_dup(
    of_oxm_icmpv4_code_t *src)
{
    of_oxm_icmpv4_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_value_get(src, &val8);
    of_oxm_icmpv4_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_masked_t *
of_oxm_icmpv4_code_masked_OF_VERSION_1_3_dup(
    of_oxm_icmpv4_code_masked_t *src)
{
    of_oxm_icmpv4_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_masked_value_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_set(dst, val8);

    of_oxm_icmpv4_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_t *
of_oxm_icmpv4_type_OF_VERSION_1_3_dup(
    of_oxm_icmpv4_type_t *src)
{
    of_oxm_icmpv4_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_value_get(src, &val8);
    of_oxm_icmpv4_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_masked_t *
of_oxm_icmpv4_type_masked_OF_VERSION_1_3_dup(
    of_oxm_icmpv4_type_masked_t *src)
{
    of_oxm_icmpv4_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_masked_value_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_set(dst, val8);

    of_oxm_icmpv4_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_t *
of_oxm_icmpv6_code_OF_VERSION_1_3_dup(
    of_oxm_icmpv6_code_t *src)
{
    of_oxm_icmpv6_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_value_get(src, &val8);
    of_oxm_icmpv6_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_masked_t *
of_oxm_icmpv6_code_masked_OF_VERSION_1_3_dup(
    of_oxm_icmpv6_code_masked_t *src)
{
    of_oxm_icmpv6_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_masked_value_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_set(dst, val8);

    of_oxm_icmpv6_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_t *
of_oxm_icmpv6_type_OF_VERSION_1_3_dup(
    of_oxm_icmpv6_type_t *src)
{
    of_oxm_icmpv6_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_value_get(src, &val8);
    of_oxm_icmpv6_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_masked_t *
of_oxm_icmpv6_type_masked_OF_VERSION_1_3_dup(
    of_oxm_icmpv6_type_masked_t *src)
{
    of_oxm_icmpv6_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_masked_value_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_set(dst, val8);

    of_oxm_icmpv6_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_t *
of_oxm_in_phy_port_OF_VERSION_1_3_dup(
    of_oxm_in_phy_port_t *src)
{
    of_oxm_in_phy_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_value_get(src, &port_no);
    of_oxm_in_phy_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_masked_t *
of_oxm_in_phy_port_masked_OF_VERSION_1_3_dup(
    of_oxm_in_phy_port_masked_t *src)
{
    of_oxm_in_phy_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_masked_value_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_set(dst, port_no);

    of_oxm_in_phy_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_t *
of_oxm_in_port_OF_VERSION_1_3_dup(
    of_oxm_in_port_t *src)
{
    of_oxm_in_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_value_get(src, &port_no);
    of_oxm_in_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_masked_t *
of_oxm_in_port_masked_OF_VERSION_1_3_dup(
    of_oxm_in_port_masked_t *src)
{
    of_oxm_in_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_masked_value_get(src, &port_no);
    of_oxm_in_port_masked_value_set(dst, port_no);

    of_oxm_in_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_t *
of_oxm_ip_dscp_OF_VERSION_1_3_dup(
    of_oxm_ip_dscp_t *src)
{
    of_oxm_ip_dscp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_value_get(src, &val8);
    of_oxm_ip_dscp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_masked_t *
of_oxm_ip_dscp_masked_OF_VERSION_1_3_dup(
    of_oxm_ip_dscp_masked_t *src)
{
    of_oxm_ip_dscp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_masked_value_get(src, &val8);
    of_oxm_ip_dscp_masked_value_set(dst, val8);

    of_oxm_ip_dscp_masked_value_mask_get(src, &val8);
    of_oxm_ip_dscp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_t *
of_oxm_ip_ecn_OF_VERSION_1_3_dup(
    of_oxm_ip_ecn_t *src)
{
    of_oxm_ip_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_value_get(src, &val8);
    of_oxm_ip_ecn_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_masked_t *
of_oxm_ip_ecn_masked_OF_VERSION_1_3_dup(
    of_oxm_ip_ecn_masked_t *src)
{
    of_oxm_ip_ecn_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_masked_value_get(src, &val8);
    of_oxm_ip_ecn_masked_value_set(dst, val8);

    of_oxm_ip_ecn_masked_value_mask_get(src, &val8);
    of_oxm_ip_ecn_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_t *
of_oxm_ip_proto_OF_VERSION_1_3_dup(
    of_oxm_ip_proto_t *src)
{
    of_oxm_ip_proto_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_value_get(src, &val8);
    of_oxm_ip_proto_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_masked_t *
of_oxm_ip_proto_masked_OF_VERSION_1_3_dup(
    of_oxm_ip_proto_masked_t *src)
{
    of_oxm_ip_proto_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_masked_value_get(src, &val8);
    of_oxm_ip_proto_masked_value_set(dst, val8);

    of_oxm_ip_proto_masked_value_mask_get(src, &val8);
    of_oxm_ip_proto_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_t *
of_oxm_ipv4_dst_OF_VERSION_1_3_dup(
    of_oxm_ipv4_dst_t *src)
{
    of_oxm_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_value_get(src, &ipv4);
    of_oxm_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_masked_t *
of_oxm_ipv4_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv4_dst_masked_t *src)
{
    of_oxm_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_t *
of_oxm_ipv4_src_OF_VERSION_1_3_dup(
    of_oxm_ipv4_src_t *src)
{
    of_oxm_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_value_get(src, &ipv4);
    of_oxm_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_masked_t *
of_oxm_ipv4_src_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv4_src_masked_t *src)
{
    of_oxm_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_t *
of_oxm_ipv6_dst_OF_VERSION_1_3_dup(
    of_oxm_ipv6_dst_t *src)
{
    of_oxm_ipv6_dst_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_value_get(src, &ipv6);
    of_oxm_ipv6_dst_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_masked_t *
of_oxm_ipv6_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_dst_masked_t *src)
{
    of_oxm_ipv6_dst_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_masked_value_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_set(dst, ipv6);

    of_oxm_ipv6_dst_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_exthdr
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_exthdr.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_exthdr_t *
of_oxm_ipv6_exthdr_OF_VERSION_1_3_dup(
    of_oxm_ipv6_exthdr_t *src)
{
    of_oxm_ipv6_exthdr_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_ipv6_exthdr_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_exthdr_value_get(src, &val16);
    of_oxm_ipv6_exthdr_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_exthdr_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_exthdr_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_exthdr_masked_t *
of_oxm_ipv6_exthdr_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_exthdr_masked_t *src)
{
    of_oxm_ipv6_exthdr_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_ipv6_exthdr_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_exthdr_masked_value_get(src, &val16);
    of_oxm_ipv6_exthdr_masked_value_set(dst, val16);

    of_oxm_ipv6_exthdr_masked_value_mask_get(src, &val16);
    of_oxm_ipv6_exthdr_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_t *
of_oxm_ipv6_flabel_OF_VERSION_1_3_dup(
    of_oxm_ipv6_flabel_t *src)
{
    of_oxm_ipv6_flabel_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_value_get(src, &val32);
    of_oxm_ipv6_flabel_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_masked_t *
of_oxm_ipv6_flabel_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_flabel_masked_t *src)
{
    of_oxm_ipv6_flabel_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_masked_value_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_set(dst, val32);

    of_oxm_ipv6_flabel_masked_value_mask_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_t *
of_oxm_ipv6_nd_sll_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_sll_t *src)
{
    of_oxm_ipv6_nd_sll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_masked_t *
of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_sll_masked_t *src)
{
    of_oxm_ipv6_nd_sll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_sll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_t *
of_oxm_ipv6_nd_target_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_target_t *src)
{
    of_oxm_ipv6_nd_target_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_masked_t *
of_oxm_ipv6_nd_target_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_target_masked_t *src)
{
    of_oxm_ipv6_nd_target_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_masked_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_set(dst, ipv6);

    of_oxm_ipv6_nd_target_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_t *
of_oxm_ipv6_nd_tll_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_tll_t *src)
{
    of_oxm_ipv6_nd_tll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_masked_t *
of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_nd_tll_masked_t *src)
{
    of_oxm_ipv6_nd_tll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_tll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_t *
of_oxm_ipv6_src_OF_VERSION_1_3_dup(
    of_oxm_ipv6_src_t *src)
{
    of_oxm_ipv6_src_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_value_get(src, &ipv6);
    of_oxm_ipv6_src_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_masked_t *
of_oxm_ipv6_src_masked_OF_VERSION_1_3_dup(
    of_oxm_ipv6_src_masked_t *src)
{
    of_oxm_ipv6_src_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_masked_value_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_set(dst, ipv6);

    of_oxm_ipv6_src_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_t *
of_oxm_metadata_OF_VERSION_1_3_dup(
    of_oxm_metadata_t *src)
{
    of_oxm_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_value_get(src, &val64);
    of_oxm_metadata_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_masked_t *
of_oxm_metadata_masked_OF_VERSION_1_3_dup(
    of_oxm_metadata_masked_t *src)
{
    of_oxm_metadata_masked_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_masked_value_get(src, &val64);
    of_oxm_metadata_masked_value_set(dst, val64);

    of_oxm_metadata_masked_value_mask_get(src, &val64);
    of_oxm_metadata_masked_value_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_bos
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_bos.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_bos_t *
of_oxm_mpls_bos_OF_VERSION_1_3_dup(
    of_oxm_mpls_bos_t *src)
{
    of_oxm_mpls_bos_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_bos_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_bos_value_get(src, &val8);
    of_oxm_mpls_bos_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_bos_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_bos_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_bos_masked_t *
of_oxm_mpls_bos_masked_OF_VERSION_1_3_dup(
    of_oxm_mpls_bos_masked_t *src)
{
    of_oxm_mpls_bos_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_bos_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_bos_masked_value_get(src, &val8);
    of_oxm_mpls_bos_masked_value_set(dst, val8);

    of_oxm_mpls_bos_masked_value_mask_get(src, &val8);
    of_oxm_mpls_bos_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_t *
of_oxm_mpls_label_OF_VERSION_1_3_dup(
    of_oxm_mpls_label_t *src)
{
    of_oxm_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_value_get(src, &val32);
    of_oxm_mpls_label_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_masked_t *
of_oxm_mpls_label_masked_OF_VERSION_1_3_dup(
    of_oxm_mpls_label_masked_t *src)
{
    of_oxm_mpls_label_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_masked_value_get(src, &val32);
    of_oxm_mpls_label_masked_value_set(dst, val32);

    of_oxm_mpls_label_masked_value_mask_get(src, &val32);
    of_oxm_mpls_label_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_t *
of_oxm_mpls_tc_OF_VERSION_1_3_dup(
    of_oxm_mpls_tc_t *src)
{
    of_oxm_mpls_tc_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_value_get(src, &val8);
    of_oxm_mpls_tc_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_masked_t *
of_oxm_mpls_tc_masked_OF_VERSION_1_3_dup(
    of_oxm_mpls_tc_masked_t *src)
{
    of_oxm_mpls_tc_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_masked_value_get(src, &val8);
    of_oxm_mpls_tc_masked_value_set(dst, val8);

    of_oxm_mpls_tc_masked_value_mask_get(src, &val8);
    of_oxm_mpls_tc_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_t *
of_oxm_ovs_tcp_flags_OF_VERSION_1_3_dup(
    of_oxm_ovs_tcp_flags_t *src)
{
    of_oxm_ovs_tcp_flags_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_masked_t *
of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_3_dup(
    of_oxm_ovs_tcp_flags_masked_t *src)
{
    of_oxm_ovs_tcp_flags_masked_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_masked_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_masked_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_masked_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_set(dst, val16);

    of_oxm_ovs_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_t *
of_oxm_sctp_dst_OF_VERSION_1_3_dup(
    of_oxm_sctp_dst_t *src)
{
    of_oxm_sctp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_value_get(src, &val16);
    of_oxm_sctp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_masked_t *
of_oxm_sctp_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_sctp_dst_masked_t *src)
{
    of_oxm_sctp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_masked_value_get(src, &val16);
    of_oxm_sctp_dst_masked_value_set(dst, val16);

    of_oxm_sctp_dst_masked_value_mask_get(src, &val16);
    of_oxm_sctp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_t *
of_oxm_sctp_src_OF_VERSION_1_3_dup(
    of_oxm_sctp_src_t *src)
{
    of_oxm_sctp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_value_get(src, &val16);
    of_oxm_sctp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_masked_t *
of_oxm_sctp_src_masked_OF_VERSION_1_3_dup(
    of_oxm_sctp_src_masked_t *src)
{
    of_oxm_sctp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_masked_value_get(src, &val16);
    of_oxm_sctp_src_masked_value_set(dst, val16);

    of_oxm_sctp_src_masked_value_mask_get(src, &val16);
    of_oxm_sctp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_t *
of_oxm_tcp_dst_OF_VERSION_1_3_dup(
    of_oxm_tcp_dst_t *src)
{
    of_oxm_tcp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_value_get(src, &val16);
    of_oxm_tcp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_masked_t *
of_oxm_tcp_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_tcp_dst_masked_t *src)
{
    of_oxm_tcp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_masked_value_get(src, &val16);
    of_oxm_tcp_dst_masked_value_set(dst, val16);

    of_oxm_tcp_dst_masked_value_mask_get(src, &val16);
    of_oxm_tcp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_t *
of_oxm_tcp_src_OF_VERSION_1_3_dup(
    of_oxm_tcp_src_t *src)
{
    of_oxm_tcp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_value_get(src, &val16);
    of_oxm_tcp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_masked_t *
of_oxm_tcp_src_masked_OF_VERSION_1_3_dup(
    of_oxm_tcp_src_masked_t *src)
{
    of_oxm_tcp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_masked_value_get(src, &val16);
    of_oxm_tcp_src_masked_value_set(dst, val16);

    of_oxm_tcp_src_masked_value_mask_get(src, &val16);
    of_oxm_tcp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_id_t *
of_oxm_tunnel_id_OF_VERSION_1_3_dup(
    of_oxm_tunnel_id_t *src)
{
    of_oxm_tunnel_id_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_tunnel_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_id_value_get(src, &val64);
    of_oxm_tunnel_id_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_id_masked_t *
of_oxm_tunnel_id_masked_OF_VERSION_1_3_dup(
    of_oxm_tunnel_id_masked_t *src)
{
    of_oxm_tunnel_id_masked_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_tunnel_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_id_masked_value_get(src, &val64);
    of_oxm_tunnel_id_masked_value_set(dst, val64);

    of_oxm_tunnel_id_masked_value_mask_get(src, &val64);
    of_oxm_tunnel_id_masked_value_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_t *
of_oxm_tunnel_ipv4_dst_OF_VERSION_1_3_dup(
    of_oxm_tunnel_ipv4_dst_t *src)
{
    of_oxm_tunnel_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_masked_t *
of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_tunnel_ipv4_dst_masked_t *src)
{
    of_oxm_tunnel_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_t *
of_oxm_tunnel_ipv4_src_OF_VERSION_1_3_dup(
    of_oxm_tunnel_ipv4_src_t *src)
{
    of_oxm_tunnel_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_masked_t *
of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_3_dup(
    of_oxm_tunnel_ipv4_src_masked_t *src)
{
    of_oxm_tunnel_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_t *
of_oxm_udp_dst_OF_VERSION_1_3_dup(
    of_oxm_udp_dst_t *src)
{
    of_oxm_udp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_value_get(src, &val16);
    of_oxm_udp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_masked_t *
of_oxm_udp_dst_masked_OF_VERSION_1_3_dup(
    of_oxm_udp_dst_masked_t *src)
{
    of_oxm_udp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_masked_value_get(src, &val16);
    of_oxm_udp_dst_masked_value_set(dst, val16);

    of_oxm_udp_dst_masked_value_mask_get(src, &val16);
    of_oxm_udp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_t *
of_oxm_udp_src_OF_VERSION_1_3_dup(
    of_oxm_udp_src_t *src)
{
    of_oxm_udp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_value_get(src, &val16);
    of_oxm_udp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_masked_t *
of_oxm_udp_src_masked_OF_VERSION_1_3_dup(
    of_oxm_udp_src_masked_t *src)
{
    of_oxm_udp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_masked_value_get(src, &val16);
    of_oxm_udp_src_masked_value_set(dst, val16);

    of_oxm_udp_src_masked_value_mask_get(src, &val16);
    of_oxm_udp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_t *
of_oxm_vlan_pcp_OF_VERSION_1_3_dup(
    of_oxm_vlan_pcp_t *src)
{
    of_oxm_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_value_get(src, &val8);
    of_oxm_vlan_pcp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_masked_t *
of_oxm_vlan_pcp_masked_OF_VERSION_1_3_dup(
    of_oxm_vlan_pcp_masked_t *src)
{
    of_oxm_vlan_pcp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_masked_value_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_set(dst, val8);

    of_oxm_vlan_pcp_masked_value_mask_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_t *
of_oxm_vlan_vid_OF_VERSION_1_3_dup(
    of_oxm_vlan_vid_t *src)
{
    of_oxm_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_value_get(src, &val16);
    of_oxm_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_masked_t *
of_oxm_vlan_vid_masked_OF_VERSION_1_3_dup(
    of_oxm_vlan_vid_masked_t *src)
{
    of_oxm_vlan_vid_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_masked_value_get(src, &val16);
    of_oxm_vlan_vid_masked_value_set(dst, val16);

    of_oxm_vlan_vid_masked_value_mask_get(src, &val16);
    of_oxm_vlan_vid_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_queue_t *
of_packet_queue_OF_VERSION_1_3_dup(
    of_packet_queue_t *src)
{
    of_packet_queue_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_queue_prop_t src_list;
    of_list_queue_prop_t *dst_list;

    if ((dst = of_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_queue_queue_id_get(src, &val32);
    of_packet_queue_queue_id_set(dst, val32);

    of_packet_queue_port_get(src, &port_no);
    of_packet_queue_port_set(dst, port_no);

    of_packet_queue_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_prop_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_queue_delete(dst);
        return NULL;
    }
    of_packet_queue_properties_set(dst, dst_list);
    of_list_queue_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_t *
of_port_desc_OF_VERSION_1_3_dup(
    of_port_desc_t *src)
{
    of_port_desc_t *dst;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    uint32_t val32;

    if ((dst = of_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_port_no_get(src, &port_no);
    of_port_desc_port_no_set(dst, port_no);

    of_port_desc_hw_addr_get(src, &mac_addr);
    of_port_desc_hw_addr_set(dst, mac_addr);

    of_port_desc_name_get(src, &port_name);
    of_port_desc_name_set(dst, port_name);

    of_port_desc_config_get(src, &val32);
    of_port_desc_config_set(dst, val32);

    of_port_desc_state_get(src, &val32);
    of_port_desc_state_set(dst, val32);

    of_port_desc_curr_get(src, &val32);
    of_port_desc_curr_set(dst, val32);

    of_port_desc_advertised_get(src, &val32);
    of_port_desc_advertised_set(dst, val32);

    of_port_desc_supported_get(src, &val32);
    of_port_desc_supported_set(dst, val32);

    of_port_desc_peer_get(src, &val32);
    of_port_desc_peer_set(dst, val32);

    of_port_desc_curr_speed_get(src, &val32);
    of_port_desc_curr_speed_set(dst, val32);

    of_port_desc_max_speed_get(src, &val32);
    of_port_desc_max_speed_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_entry_t *
of_port_stats_entry_OF_VERSION_1_3_dup(
    of_port_stats_entry_t *src)
{
    of_port_stats_entry_t *dst;
    of_port_no_t port_no;
    uint64_t val64;
    uint32_t val32;

    if ((dst = of_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_entry_port_no_get(src, &port_no);
    of_port_stats_entry_port_no_set(dst, port_no);

    of_port_stats_entry_rx_packets_get(src, &val64);
    of_port_stats_entry_rx_packets_set(dst, val64);

    of_port_stats_entry_tx_packets_get(src, &val64);
    of_port_stats_entry_tx_packets_set(dst, val64);

    of_port_stats_entry_rx_bytes_get(src, &val64);
    of_port_stats_entry_rx_bytes_set(dst, val64);

    of_port_stats_entry_tx_bytes_get(src, &val64);
    of_port_stats_entry_tx_bytes_set(dst, val64);

    of_port_stats_entry_rx_dropped_get(src, &val64);
    of_port_stats_entry_rx_dropped_set(dst, val64);

    of_port_stats_entry_tx_dropped_get(src, &val64);
    of_port_stats_entry_tx_dropped_set(dst, val64);

    of_port_stats_entry_rx_errors_get(src, &val64);
    of_port_stats_entry_rx_errors_set(dst, val64);

    of_port_stats_entry_tx_errors_get(src, &val64);
    of_port_stats_entry_tx_errors_set(dst, val64);

    of_port_stats_entry_rx_frame_err_get(src, &val64);
    of_port_stats_entry_rx_frame_err_set(dst, val64);

    of_port_stats_entry_rx_over_err_get(src, &val64);
    of_port_stats_entry_rx_over_err_set(dst, val64);

    of_port_stats_entry_rx_crc_err_get(src, &val64);
    of_port_stats_entry_rx_crc_err_set(dst, val64);

    of_port_stats_entry_collisions_get(src, &val64);
    of_port_stats_entry_collisions_set(dst, val64);

    of_port_stats_entry_duration_sec_get(src, &val32);
    of_port_stats_entry_duration_sec_set(dst, val32);

    of_port_stats_entry_duration_nsec_get(src, &val32);
    of_port_stats_entry_duration_nsec_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_prop_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_PROP_MAX_RATE) {
        return of_queue_prop_max_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_QUEUE_PROP_MIN_RATE) {
        return of_queue_prop_min_rate_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_prop_max_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_max_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_max_rate_t *
of_queue_prop_max_rate_OF_VERSION_1_3_dup(
    of_queue_prop_max_rate_t *src)
{
    of_queue_prop_max_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_max_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_max_rate_rate_get(src, &val16);
    of_queue_prop_max_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_min_rate_t *
of_queue_prop_min_rate_OF_VERSION_1_3_dup(
    of_queue_prop_min_rate_t *src)
{
    of_queue_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_min_rate_rate_get(src, &val16);
    of_queue_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_entry_t *
of_queue_stats_entry_OF_VERSION_1_3_dup(
    of_queue_stats_entry_t *src)
{
    of_queue_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_entry_port_no_get(src, &port_no);
    of_queue_stats_entry_port_no_set(dst, port_no);

    of_queue_stats_entry_queue_id_get(src, &val32);
    of_queue_stats_entry_queue_id_set(dst, val32);

    of_queue_stats_entry_tx_bytes_get(src, &val64);
    of_queue_stats_entry_tx_bytes_set(dst, val64);

    of_queue_stats_entry_tx_packets_get(src, &val64);
    of_queue_stats_entry_tx_packets_set(dst, val64);

    of_queue_stats_entry_tx_errors_get(src, &val64);
    of_queue_stats_entry_tx_errors_set(dst, val64);

    of_queue_stats_entry_duration_sec_get(src, &val32);
    of_queue_stats_entry_duration_sec_set(dst, val32);

    of_queue_stats_entry_duration_nsec_get(src, &val32);
    of_queue_stats_entry_duration_nsec_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_table_feature_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_table_feature_prop_OF_VERSION_1_3_dup(
    of_object_t *src)
{

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_SETFIELD_MISS) {
        return of_table_feature_prop_write_setfield_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_ACTIONS) {
        return of_table_feature_prop_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_NEXT_TABLES) {
        return of_table_feature_prop_next_tables_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_SETFIELD_MISS) {
        return of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_ACTIONS) {
        return of_table_feature_prop_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_INSTRUCTIONS_MISS) {
        return of_table_feature_prop_instructions_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WILDCARDS) {
        return of_table_feature_prop_wildcards_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_INSTRUCTIONS) {
        return of_table_feature_prop_instructions_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_SETFIELD) {
        return of_table_feature_prop_write_setfield_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_SETFIELD) {
        return of_table_feature_prop_apply_setfield_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_ACTIONS_MISS) {
        return of_table_feature_prop_apply_actions_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_MATCH) {
        return of_table_feature_prop_match_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_NEXT_TABLES_MISS) {
        return of_table_feature_prop_next_tables_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_ACTIONS_MISS) {
        return of_table_feature_prop_write_actions_miss_OF_VERSION_1_3_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_actions_t *
of_table_feature_prop_apply_actions_OF_VERSION_1_3_dup(
    of_table_feature_prop_apply_actions_t *src)
{
    of_table_feature_prop_apply_actions_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_actions_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_actions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_actions_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_actions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_actions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_actions_miss_t *
of_table_feature_prop_apply_actions_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_apply_actions_miss_t *src)
{
    of_table_feature_prop_apply_actions_miss_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_apply_actions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_actions_miss_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_actions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_actions_miss_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_setfield
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_setfield.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_setfield_t *
of_table_feature_prop_apply_setfield_OF_VERSION_1_3_dup(
    of_table_feature_prop_apply_setfield_t *src)
{
    of_table_feature_prop_apply_setfield_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_apply_setfield_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_setfield_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_setfield_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_setfield_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_setfield_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_setfield_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_setfield_miss_t *
of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_apply_setfield_miss_t *src)
{
    of_table_feature_prop_apply_setfield_miss_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_apply_setfield_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_setfield_miss_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_setfield_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_setfield_miss_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_instructions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_instructions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_instructions_t *
of_table_feature_prop_instructions_OF_VERSION_1_3_dup(
    of_table_feature_prop_instructions_t *src)
{
    of_table_feature_prop_instructions_t *dst;

    of_list_instruction_id_t src_list;
    of_list_instruction_id_t *dst_list;

    if ((dst = of_table_feature_prop_instructions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_instructions_instruction_ids_bind(
        src, &src_list);
    dst_list = of_list_instruction_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_instructions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_instructions_instruction_ids_set(dst, dst_list);
    of_list_instruction_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_instructions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_instructions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_instructions_miss_t *
of_table_feature_prop_instructions_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_instructions_miss_t *src)
{
    of_table_feature_prop_instructions_miss_t *dst;

    of_list_instruction_id_t src_list;
    of_list_instruction_id_t *dst_list;

    if ((dst = of_table_feature_prop_instructions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_instructions_miss_instruction_ids_bind(
        src, &src_list);
    dst_list = of_list_instruction_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_instructions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_instructions_miss_instruction_ids_set(dst, dst_list);
    of_list_instruction_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_match_t *
of_table_feature_prop_match_OF_VERSION_1_3_dup(
    of_table_feature_prop_match_t *src)
{
    of_table_feature_prop_match_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_match_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_match_delete(dst);
        return NULL;
    }
    of_table_feature_prop_match_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_next_tables
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_next_tables.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_next_tables_t *
of_table_feature_prop_next_tables_OF_VERSION_1_3_dup(
    of_table_feature_prop_next_tables_t *src)
{
    of_table_feature_prop_next_tables_t *dst;

    of_list_uint8_t src_list;
    of_list_uint8_t *dst_list;

    if ((dst = of_table_feature_prop_next_tables_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_next_tables_next_table_ids_bind(
        src, &src_list);
    dst_list = of_list_uint8_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_next_tables_delete(dst);
        return NULL;
    }
    of_table_feature_prop_next_tables_next_table_ids_set(dst, dst_list);
    of_list_uint8_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_next_tables_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_next_tables_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_next_tables_miss_t *
of_table_feature_prop_next_tables_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_next_tables_miss_t *src)
{
    of_table_feature_prop_next_tables_miss_t *dst;

    of_list_uint8_t src_list;
    of_list_uint8_t *dst_list;

    if ((dst = of_table_feature_prop_next_tables_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_next_tables_miss_next_table_ids_bind(
        src, &src_list);
    dst_list = of_list_uint8_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_next_tables_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_next_tables_miss_next_table_ids_set(dst, dst_list);
    of_list_uint8_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_wildcards
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_wildcards.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_wildcards_t *
of_table_feature_prop_wildcards_OF_VERSION_1_3_dup(
    of_table_feature_prop_wildcards_t *src)
{
    of_table_feature_prop_wildcards_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_wildcards_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_wildcards_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_wildcards_delete(dst);
        return NULL;
    }
    of_table_feature_prop_wildcards_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_actions_t *
of_table_feature_prop_write_actions_OF_VERSION_1_3_dup(
    of_table_feature_prop_write_actions_t *src)
{
    of_table_feature_prop_write_actions_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_actions_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_actions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_actions_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_actions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_actions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_actions_miss_t *
of_table_feature_prop_write_actions_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_write_actions_miss_t *src)
{
    of_table_feature_prop_write_actions_miss_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_write_actions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_actions_miss_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_actions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_actions_miss_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_setfield
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_setfield.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_setfield_t *
of_table_feature_prop_write_setfield_OF_VERSION_1_3_dup(
    of_table_feature_prop_write_setfield_t *src)
{
    of_table_feature_prop_write_setfield_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_write_setfield_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_setfield_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_setfield_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_setfield_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_setfield_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_setfield_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_setfield_miss_t *
of_table_feature_prop_write_setfield_miss_OF_VERSION_1_3_dup(
    of_table_feature_prop_write_setfield_miss_t *src)
{
    of_table_feature_prop_write_setfield_miss_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_write_setfield_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_setfield_miss_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_setfield_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_setfield_miss_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_t *
of_table_features_OF_VERSION_1_3_dup(
    of_table_features_t *src)
{
    of_table_features_t *dst;
    uint8_t val8;
    of_table_name_t table_name;
    uint64_t val64;
    uint32_t val32;

    of_list_table_feature_prop_t src_list;
    of_list_table_feature_prop_t *dst_list;

    if ((dst = of_table_features_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_table_id_get(src, &val8);
    of_table_features_table_id_set(dst, val8);

    of_table_features_name_get(src, &table_name);
    of_table_features_name_set(dst, table_name);

    of_table_features_metadata_match_get(src, &val64);
    of_table_features_metadata_match_set(dst, val64);

    of_table_features_metadata_write_get(src, &val64);
    of_table_features_metadata_write_set(dst, val64);

    of_table_features_config_get(src, &val32);
    of_table_features_config_set(dst, val32);

    of_table_features_max_entries_get(src, &val32);
    of_table_features_max_entries_set(dst, val32);

    of_table_features_properties_bind(
        src, &src_list);
    dst_list = of_list_table_feature_prop_OF_VERSION_1_3_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_delete(dst);
        return NULL;
    }
    of_table_features_properties_set(dst, dst_list);
    of_list_table_feature_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_entry_t *
of_table_stats_entry_OF_VERSION_1_3_dup(
    of_table_stats_entry_t *src)
{
    of_table_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_entry_table_id_get(src, &val8);
    of_table_stats_entry_table_id_set(dst, val8);

    of_table_stats_entry_active_count_get(src, &val32);
    of_table_stats_entry_active_count_set(dst, val32);

    of_table_stats_entry_lookup_count_get(src, &val64);
    of_table_stats_entry_lookup_count_set(dst, val64);

    of_table_stats_entry_matched_count_get(src, &val64);
    of_table_stats_entry_matched_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_uint32
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint32.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint32_t *
of_uint32_OF_VERSION_1_3_dup(
    of_uint32_t *src)
{
    of_uint32_t *dst;
    uint32_t val32;

    if ((dst = of_uint32_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint32_value_get(src, &val32);
    of_uint32_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_uint64
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint64.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint64_t *
of_uint64_OF_VERSION_1_3_dup(
    of_uint64_t *src)
{
    of_uint64_t *dst;
    uint64_t val64;

    if ((dst = of_uint64_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint64_value_get(src, &val64);
    of_uint64_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_uint8
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint8.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint8_t *
of_uint8_OF_VERSION_1_3_dup(
    of_uint8_t *src)
{
    of_uint8_t *dst;
    uint8_t val8;

    if ((dst = of_uint8_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint8_value_get(src, &val8);
    of_uint8_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate a list of type of_list_action
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_t *
of_list_action_OF_VERSION_1_3_dup(
    of_list_action_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_t *dst;

    if ((dst = of_list_action_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_action_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_id_t *
of_list_action_id_OF_VERSION_1_3_dup(
    of_list_action_id_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_id_t *dst;

    if ((dst = of_list_action_id_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ID_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_id_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_id_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_controller_connection
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_controller_connection.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_controller_connection_t *
of_list_bsn_controller_connection_OF_VERSION_1_3_dup(
    of_list_bsn_controller_connection_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_controller_connection_t *dst;

    if ((dst = of_list_bsn_controller_connection_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_CONTROLLER_CONNECTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_controller_connection_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_controller_connection_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_debug_counter_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_debug_counter_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_debug_counter_desc_stats_entry_t *
of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_debug_counter_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_debug_counter_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_debug_counter_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_DEBUG_COUNTER_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_debug_counter_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_debug_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_debug_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_debug_counter_stats_entry_t *
of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_debug_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_debug_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_debug_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_DEBUG_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_debug_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_flow_checksum_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_flow_checksum_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_flow_checksum_bucket_stats_entry_t *
of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_flow_checksum_bucket_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_flow_checksum_bucket_stats_entry_t *dst;

    if ((dst = of_list_bsn_flow_checksum_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_FLOW_CHECKSUM_BUCKET_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_flow_checksum_bucket_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_generic_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_generic_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_generic_stats_entry_t *
of_list_bsn_generic_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_generic_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_generic_stats_entry_t *dst;

    if ((dst = of_list_bsn_generic_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENERIC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_generic_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_generic_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_bucket_stats_entry_t *
of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_gentable_bucket_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_bucket_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_BUCKET_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_bucket_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_desc_stats_entry_t *
of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_gentable_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_entry_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_entry_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_entry_desc_stats_entry_t *
of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_gentable_entry_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_entry_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_entry_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_ENTRY_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_entry_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_entry_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_entry_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_entry_stats_entry_t *
of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_gentable_entry_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_entry_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_entry_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_ENTRY_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_entry_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_stats_entry_t *
of_list_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_gentable_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_interface_t *
of_list_bsn_interface_OF_VERSION_1_3_dup(
    of_list_bsn_interface_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_interface_t *dst;

    if ((dst = of_list_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_INTERFACE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_interface_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_interface_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_lacp_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_lacp_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_lacp_stats_entry_t *
of_list_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_lacp_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_lacp_stats_entry_t *dst;

    if ((dst = of_list_bsn_lacp_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_LACP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_lacp_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_port_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_port_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_port_counter_stats_entry_t *
of_list_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_port_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_port_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_port_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_PORT_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_port_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_switch_pipeline_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_switch_pipeline_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_switch_pipeline_stats_entry_t *
of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_switch_pipeline_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_switch_pipeline_stats_entry_t *dst;

    if ((dst = of_list_bsn_switch_pipeline_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_SWITCH_PIPELINE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_switch_pipeline_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_table_checksum_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_table_checksum_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_table_checksum_stats_entry_t *
of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_table_checksum_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_table_checksum_stats_entry_t *dst;

    if ((dst = of_list_bsn_table_checksum_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_TABLE_CHECKSUM_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_table_checksum_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_tlv
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_tlv.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_tlv_t *
of_list_bsn_tlv_OF_VERSION_1_3_dup(
    of_list_bsn_tlv_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_tlv_t *dst;

    if ((dst = of_list_bsn_tlv_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_TLV_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_tlv_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_tlv_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vlan_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vlan_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vlan_counter_stats_entry_t *
of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_vlan_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vlan_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_vlan_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VLAN_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vlan_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vlan_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vlan_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vlan_mac_t *
of_list_bsn_vlan_mac_OF_VERSION_1_3_dup(
    of_list_bsn_vlan_mac_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vlan_mac_t *dst;

    if ((dst = of_list_bsn_vlan_mac_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VLAN_MAC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vlan_mac_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vlan_mac_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vrf_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vrf_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vrf_counter_stats_entry_t *
of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(
    of_list_bsn_vrf_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vrf_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_vrf_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VRF_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vrf_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_t *
of_list_bucket_OF_VERSION_1_3_dup(
    of_list_bucket_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_t *dst;

    if ((dst = of_list_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_counter_t *
of_list_bucket_counter_OF_VERSION_1_3_dup(
    of_list_bucket_counter_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_counter_t *dst;

    if ((dst = of_list_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_COUNTER_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_counter_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_counter_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_flow_stats_entry_t *
of_list_flow_stats_entry_OF_VERSION_1_3_dup(
    of_list_flow_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_flow_stats_entry_t *dst;

    if ((dst = of_list_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_FLOW_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_flow_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_flow_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_desc_stats_entry_t *
of_list_group_desc_stats_entry_OF_VERSION_1_3_dup(
    of_list_group_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_desc_stats_entry_t *dst;

    if ((dst = of_list_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_desc_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_stats_entry_t *
of_list_group_stats_entry_OF_VERSION_1_3_dup(
    of_list_group_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_stats_entry_t *dst;

    if ((dst = of_list_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_hello_elem
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_hello_elem.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_hello_elem_t *
of_list_hello_elem_OF_VERSION_1_3_dup(
    of_list_hello_elem_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_hello_elem_t *dst;

    if ((dst = of_list_hello_elem_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_HELLO_ELEM_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_hello_elem_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_hello_elem_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_t *
of_list_instruction_OF_VERSION_1_3_dup(
    of_list_instruction_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_t *dst;

    if ((dst = of_list_instruction_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_id_t *
of_list_instruction_id_OF_VERSION_1_3_dup(
    of_list_instruction_id_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_id_t *dst;

    if ((dst = of_list_instruction_id_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ID_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_id_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_id_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_band
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_band.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_band_t *
of_list_meter_band_OF_VERSION_1_3_dup(
    of_list_meter_band_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_band_t *dst;

    if ((dst = of_list_meter_band_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_BAND_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_band_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_band_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_band_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_band_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_band_stats_t *
of_list_meter_band_stats_OF_VERSION_1_3_dup(
    of_list_meter_band_stats_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_band_stats_t *dst;

    if ((dst = of_list_meter_band_stats_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_BAND_STATS_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_band_stats_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_band_stats_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_config_t *
of_list_meter_config_OF_VERSION_1_3_dup(
    of_list_meter_config_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_config_t *dst;

    if ((dst = of_list_meter_config_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_CONFIG_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_config_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_config_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_stats_t *
of_list_meter_stats_OF_VERSION_1_3_dup(
    of_list_meter_stats_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_stats_t *dst;

    if ((dst = of_list_meter_stats_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_STATS_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_stats_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_stats_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_oxm
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_oxm_t *
of_list_oxm_OF_VERSION_1_3_dup(
    of_list_oxm_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_oxm_t *dst;

    if ((dst = of_list_oxm_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_OXM_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_oxm_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_oxm_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_packet_queue_t *
of_list_packet_queue_OF_VERSION_1_3_dup(
    of_list_packet_queue_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_packet_queue_t *dst;

    if ((dst = of_list_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PACKET_QUEUE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_packet_queue_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_packet_queue_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_t *
of_list_port_desc_OF_VERSION_1_3_dup(
    of_list_port_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_t *dst;

    if ((dst = of_list_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_entry_t *
of_list_port_stats_entry_OF_VERSION_1_3_dup(
    of_list_port_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_entry_t *dst;

    if ((dst = of_list_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_prop_t *
of_list_queue_prop_OF_VERSION_1_3_dup(
    of_list_queue_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_prop_t *dst;

    if ((dst = of_list_queue_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_prop_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_entry_t *
of_list_queue_stats_entry_OF_VERSION_1_3_dup(
    of_list_queue_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_entry_t *dst;

    if ((dst = of_list_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_feature_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_feature_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_feature_prop_t *
of_list_table_feature_prop_OF_VERSION_1_3_dup(
    of_list_table_feature_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_feature_prop_t *dst;

    if ((dst = of_list_table_feature_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_FEATURE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_feature_prop_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_feature_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_features_t *
of_list_table_features_OF_VERSION_1_3_dup(
    of_list_table_features_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_features_t *dst;

    if ((dst = of_list_table_features_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_FEATURES_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_features_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_features_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_stats_entry_t *
of_list_table_stats_entry_OF_VERSION_1_3_dup(
    of_list_table_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_stats_entry_t *dst;

    if ((dst = of_list_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_stats_entry_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint32
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint32.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint32_t *
of_list_uint32_OF_VERSION_1_3_dup(
    of_list_uint32_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint32_t *dst;

    if ((dst = of_list_uint32_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT32_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint32_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint32_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint64
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint64.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint64_t *
of_list_uint64_OF_VERSION_1_3_dup(
    of_list_uint64_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint64_t *dst;

    if ((dst = of_list_uint64_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT64_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint64_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint64_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint8
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint8.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint8_t *
of_list_uint8_OF_VERSION_1_3_dup(
    of_list_uint8_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint8_t *dst;

    if ((dst = of_list_uint8_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT8_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint8_OF_VERSION_1_3_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint8_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_reply_t *
of_aggregate_stats_reply_OF_VERSION_1_4_dup(
    of_aggregate_stats_reply_t *src)
{
    of_aggregate_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_aggregate_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_reply_xid_get(src, &val32);
    of_aggregate_stats_reply_xid_set(dst, val32);

    of_aggregate_stats_reply_flags_get(src, &val16);
    of_aggregate_stats_reply_flags_set(dst, val16);

    of_aggregate_stats_reply_packet_count_get(src, &val64);
    of_aggregate_stats_reply_packet_count_set(dst, val64);

    of_aggregate_stats_reply_byte_count_get(src, &val64);
    of_aggregate_stats_reply_byte_count_set(dst, val64);

    of_aggregate_stats_reply_flow_count_get(src, &val32);
    of_aggregate_stats_reply_flow_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_aggregate_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_aggregate_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_aggregate_stats_request_t *
of_aggregate_stats_request_OF_VERSION_1_4_dup(
    of_aggregate_stats_request_t *src)
{
    of_aggregate_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_aggregate_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_aggregate_stats_request_xid_get(src, &val32);
    of_aggregate_stats_request_xid_set(dst, val32);

    of_aggregate_stats_request_flags_get(src, &val16);
    of_aggregate_stats_request_flags_set(dst, val16);

    of_aggregate_stats_request_table_id_get(src, &val8);
    of_aggregate_stats_request_table_id_set(dst, val8);

    of_aggregate_stats_request_out_port_get(src, &port_no);
    of_aggregate_stats_request_out_port_set(dst, port_no);

    of_aggregate_stats_request_out_group_get(src, &val32);
    of_aggregate_stats_request_out_group_set(dst, val32);

    of_aggregate_stats_request_cookie_get(src, &val64);
    of_aggregate_stats_request_cookie_set(dst, val64);

    of_aggregate_stats_request_cookie_mask_get(src, &val64);
    of_aggregate_stats_request_cookie_mask_set(dst, val64);

    of_aggregate_stats_request_match_get(src, &match);
    of_aggregate_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_failed_error_msg_t *
of_async_config_failed_error_msg_OF_VERSION_1_4_dup(
    of_async_config_failed_error_msg_t *src)
{
    of_async_config_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_async_config_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_failed_error_msg_xid_get(src, &val32);
    of_async_config_failed_error_msg_xid_set(dst, val32);

    of_async_config_failed_error_msg_code_get(src, &val16);
    of_async_config_failed_error_msg_code_set(dst, val16);

    of_async_config_failed_error_msg_data_get(src, &octets);
    of_async_config_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_async_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_get_reply_t *
of_async_get_reply_OF_VERSION_1_4_dup(
    of_async_get_reply_t *src)
{
    of_async_get_reply_t *dst;
    uint32_t val32;

    of_list_async_config_prop_t src_list;
    of_list_async_config_prop_t *dst_list;

    if ((dst = of_async_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_get_reply_xid_get(src, &val32);
    of_async_get_reply_xid_set(dst, val32);

    of_async_get_reply_properties_bind(
        src, &src_list);
    dst_list = of_list_async_config_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_async_get_reply_delete(dst);
        return NULL;
    }
    of_async_get_reply_properties_set(dst, dst_list);
    of_list_async_config_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_async_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_get_request_t *
of_async_get_request_OF_VERSION_1_4_dup(
    of_async_get_request_t *src)
{
    of_async_get_request_t *dst;
    uint32_t val32;

    of_list_async_config_prop_t src_list;
    of_list_async_config_prop_t *dst_list;

    if ((dst = of_async_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_get_request_xid_get(src, &val32);
    of_async_get_request_xid_set(dst, val32);

    of_async_get_request_properties_bind(
        src, &src_list);
    dst_list = of_list_async_config_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_async_get_request_delete(dst);
        return NULL;
    }
    of_async_get_request_properties_set(dst, dst_list);
    of_list_async_config_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_async_set
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_set.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_set_t *
of_async_set_OF_VERSION_1_4_dup(
    of_async_set_t *src)
{
    of_async_set_t *dst;
    uint32_t val32;

    of_list_async_config_prop_t src_list;
    of_list_async_config_prop_t *dst_list;

    if ((dst = of_async_set_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_set_xid_get(src, &val32);
    of_async_set_xid_set(dst, val32);

    of_async_set_properties_bind(
        src, &src_list);
    dst_list = of_list_async_config_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_async_set_delete(dst);
        return NULL;
    }
    of_async_set_properties_set(dst, dst_list);
    of_list_async_config_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bad_action_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_action_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_action_error_msg_t *
of_bad_action_error_msg_OF_VERSION_1_4_dup(
    of_bad_action_error_msg_t *src)
{
    of_bad_action_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_action_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_action_error_msg_xid_get(src, &val32);
    of_bad_action_error_msg_xid_set(dst, val32);

    of_bad_action_error_msg_code_get(src, &val16);
    of_bad_action_error_msg_code_set(dst, val16);

    of_bad_action_error_msg_data_get(src, &octets);
    of_bad_action_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_instruction_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_instruction_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_instruction_error_msg_t *
of_bad_instruction_error_msg_OF_VERSION_1_4_dup(
    of_bad_instruction_error_msg_t *src)
{
    of_bad_instruction_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_instruction_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_instruction_error_msg_xid_get(src, &val32);
    of_bad_instruction_error_msg_xid_set(dst, val32);

    of_bad_instruction_error_msg_code_get(src, &val16);
    of_bad_instruction_error_msg_code_set(dst, val16);

    of_bad_instruction_error_msg_data_get(src, &octets);
    of_bad_instruction_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_match_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_match_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_match_error_msg_t *
of_bad_match_error_msg_OF_VERSION_1_4_dup(
    of_bad_match_error_msg_t *src)
{
    of_bad_match_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_match_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_match_error_msg_xid_get(src, &val32);
    of_bad_match_error_msg_xid_set(dst, val32);

    of_bad_match_error_msg_code_get(src, &val16);
    of_bad_match_error_msg_code_set(dst, val16);

    of_bad_match_error_msg_data_get(src, &octets);
    of_bad_match_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_property_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_property_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_property_error_msg_t *
of_bad_property_error_msg_OF_VERSION_1_4_dup(
    of_bad_property_error_msg_t *src)
{
    of_bad_property_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_property_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_property_error_msg_xid_get(src, &val32);
    of_bad_property_error_msg_xid_set(dst, val32);

    of_bad_property_error_msg_code_get(src, &val16);
    of_bad_property_error_msg_code_set(dst, val16);

    of_bad_property_error_msg_data_get(src, &octets);
    of_bad_property_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bad_request_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bad_request_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bad_request_error_msg_t *
of_bad_request_error_msg_OF_VERSION_1_4_dup(
    of_bad_request_error_msg_t *src)
{
    of_bad_request_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bad_request_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bad_request_error_msg_xid_get(src, &val32);
    of_bad_request_error_msg_xid_set(dst, val32);

    of_bad_request_error_msg_code_get(src, &val16);
    of_bad_request_error_msg_code_set(dst, val16);

    of_bad_request_error_msg_data_get(src, &octets);
    of_bad_request_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_reply_t *
of_barrier_reply_OF_VERSION_1_4_dup(
    of_barrier_reply_t *src)
{
    of_barrier_reply_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_reply_xid_get(src, &val32);
    of_barrier_reply_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_barrier_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_barrier_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_barrier_request_t *
of_barrier_request_OF_VERSION_1_4_dup(
    of_barrier_request_t *src)
{
    of_barrier_request_t *dst;
    uint32_t val32;

    if ((dst = of_barrier_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_barrier_request_xid_get(src, &val32);
    of_barrier_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_arp_idle
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_arp_idle.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_arp_idle_t *
of_bsn_arp_idle_OF_VERSION_1_4_dup(
    of_bsn_arp_idle_t *src)
{
    of_bsn_arp_idle_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_arp_idle_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_arp_idle_xid_get(src, &val32);
    of_bsn_arp_idle_xid_set(dst, val32);

    of_bsn_arp_idle_experimenter_get(src, &val32);
    of_bsn_arp_idle_experimenter_set(dst, val32);

    of_bsn_arp_idle_subtype_get(src, &val32);
    of_bsn_arp_idle_subtype_set(dst, val32);

    of_bsn_arp_idle_vlan_vid_get(src, &val16);
    of_bsn_arp_idle_vlan_vid_set(dst, val16);

    of_bsn_arp_idle_ipv4_addr_get(src, &ipv4);
    of_bsn_arp_idle_ipv4_addr_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_reply_t *
of_bsn_bw_clear_data_reply_OF_VERSION_1_4_dup(
    of_bsn_bw_clear_data_reply_t *src)
{
    of_bsn_bw_clear_data_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_reply_xid_get(src, &val32);
    of_bsn_bw_clear_data_reply_xid_set(dst, val32);

    of_bsn_bw_clear_data_reply_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_reply_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_reply_subtype_get(src, &val32);
    of_bsn_bw_clear_data_reply_subtype_set(dst, val32);

    of_bsn_bw_clear_data_reply_status_get(src, &val32);
    of_bsn_bw_clear_data_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_clear_data_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_clear_data_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_clear_data_request_t *
of_bsn_bw_clear_data_request_OF_VERSION_1_4_dup(
    of_bsn_bw_clear_data_request_t *src)
{
    of_bsn_bw_clear_data_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_clear_data_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_clear_data_request_xid_get(src, &val32);
    of_bsn_bw_clear_data_request_xid_set(dst, val32);

    of_bsn_bw_clear_data_request_experimenter_get(src, &val32);
    of_bsn_bw_clear_data_request_experimenter_set(dst, val32);

    of_bsn_bw_clear_data_request_subtype_get(src, &val32);
    of_bsn_bw_clear_data_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_reply_t *
of_bsn_bw_enable_get_reply_OF_VERSION_1_4_dup(
    of_bsn_bw_enable_get_reply_t *src)
{
    of_bsn_bw_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_reply_xid_get(src, &val32);
    of_bsn_bw_enable_get_reply_xid_set(dst, val32);

    of_bsn_bw_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_get_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_get_reply_enabled_get(src, &val32);
    of_bsn_bw_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_get_request_t *
of_bsn_bw_enable_get_request_OF_VERSION_1_4_dup(
    of_bsn_bw_enable_get_request_t *src)
{
    of_bsn_bw_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_get_request_xid_get(src, &val32);
    of_bsn_bw_enable_get_request_xid_set(dst, val32);

    of_bsn_bw_enable_get_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_get_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_get_request_subtype_get(src, &val32);
    of_bsn_bw_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_reply_t *
of_bsn_bw_enable_set_reply_OF_VERSION_1_4_dup(
    of_bsn_bw_enable_set_reply_t *src)
{
    of_bsn_bw_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_reply_xid_get(src, &val32);
    of_bsn_bw_enable_set_reply_xid_set(dst, val32);

    of_bsn_bw_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_reply_subtype_get(src, &val32);
    of_bsn_bw_enable_set_reply_subtype_set(dst, val32);

    of_bsn_bw_enable_set_reply_enable_get(src, &val32);
    of_bsn_bw_enable_set_reply_enable_set(dst, val32);

    of_bsn_bw_enable_set_reply_status_get(src, &val32);
    of_bsn_bw_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_bw_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_bw_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_bw_enable_set_request_t *
of_bsn_bw_enable_set_request_OF_VERSION_1_4_dup(
    of_bsn_bw_enable_set_request_t *src)
{
    of_bsn_bw_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_bw_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_bw_enable_set_request_xid_get(src, &val32);
    of_bsn_bw_enable_set_request_xid_set(dst, val32);

    of_bsn_bw_enable_set_request_experimenter_get(src, &val32);
    of_bsn_bw_enable_set_request_experimenter_set(dst, val32);

    of_bsn_bw_enable_set_request_subtype_get(src, &val32);
    of_bsn_bw_enable_set_request_subtype_set(dst, val32);

    of_bsn_bw_enable_set_request_enable_get(src, &val32);
    of_bsn_bw_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connections_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connections_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connections_reply_t *
of_bsn_controller_connections_reply_OF_VERSION_1_4_dup(
    of_bsn_controller_connections_reply_t *src)
{
    of_bsn_controller_connections_reply_t *dst;
    uint32_t val32;

    of_list_bsn_controller_connection_t src_list;
    of_list_bsn_controller_connection_t *dst_list;

    if ((dst = of_bsn_controller_connections_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connections_reply_xid_get(src, &val32);
    of_bsn_controller_connections_reply_xid_set(dst, val32);

    of_bsn_controller_connections_reply_experimenter_get(src, &val32);
    of_bsn_controller_connections_reply_experimenter_set(dst, val32);

    of_bsn_controller_connections_reply_subtype_get(src, &val32);
    of_bsn_controller_connections_reply_subtype_set(dst, val32);

    of_bsn_controller_connections_reply_connections_bind(
        src, &src_list);
    dst_list = of_list_bsn_controller_connection_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_controller_connections_reply_delete(dst);
        return NULL;
    }
    of_bsn_controller_connections_reply_connections_set(dst, dst_list);
    of_list_bsn_controller_connection_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connections_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connections_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connections_request_t *
of_bsn_controller_connections_request_OF_VERSION_1_4_dup(
    of_bsn_controller_connections_request_t *src)
{
    of_bsn_controller_connections_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_controller_connections_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connections_request_xid_get(src, &val32);
    of_bsn_controller_connections_request_xid_set(dst, val32);

    of_bsn_controller_connections_request_experimenter_get(src, &val32);
    of_bsn_controller_connections_request_experimenter_set(dst, val32);

    of_bsn_controller_connections_request_subtype_get(src, &val32);
    of_bsn_controller_connections_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_reply_t *
of_bsn_debug_counter_desc_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_desc_stats_reply_t *src)
{
    of_bsn_debug_counter_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_debug_counter_desc_stats_entry_t src_list;
    of_list_bsn_debug_counter_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_debug_counter_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_reply_xid_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_xid_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_flags_get(src, &val16);
    of_bsn_debug_counter_desc_stats_reply_flags_set(dst, val16);

    of_bsn_debug_counter_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_debug_counter_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_debug_counter_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_debug_counter_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_debug_counter_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_debug_counter_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_request_t *
of_bsn_debug_counter_desc_stats_request_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_desc_stats_request_t *src)
{
    of_bsn_debug_counter_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_debug_counter_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_request_xid_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_xid_set(dst, val32);

    of_bsn_debug_counter_desc_stats_request_flags_get(src, &val16);
    of_bsn_debug_counter_desc_stats_request_flags_set(dst, val16);

    of_bsn_debug_counter_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_debug_counter_desc_stats_request_subtype_get(src, &val32);
    of_bsn_debug_counter_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_reply_t *
of_bsn_debug_counter_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_stats_reply_t *src)
{
    of_bsn_debug_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_debug_counter_stats_entry_t src_list;
    of_list_bsn_debug_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_debug_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_reply_xid_get(src, &val32);
    of_bsn_debug_counter_stats_reply_xid_set(dst, val32);

    of_bsn_debug_counter_stats_reply_flags_get(src, &val16);
    of_bsn_debug_counter_stats_reply_flags_set(dst, val16);

    of_bsn_debug_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_debug_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_debug_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_debug_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_debug_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_debug_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_debug_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_debug_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_request_t *
of_bsn_debug_counter_stats_request_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_stats_request_t *src)
{
    of_bsn_debug_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_debug_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_request_xid_get(src, &val32);
    of_bsn_debug_counter_stats_request_xid_set(dst, val32);

    of_bsn_debug_counter_stats_request_flags_get(src, &val16);
    of_bsn_debug_counter_stats_request_flags_set(dst, val16);

    of_bsn_debug_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_debug_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_debug_counter_stats_request_subtype_get(src, &val32);
    of_bsn_debug_counter_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_error_t *
of_bsn_error_OF_VERSION_1_4_dup(
    of_bsn_error_t *src)
{
    of_bsn_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_error_xid_get(src, &val32);
    of_bsn_error_xid_set(dst, val32);

    of_bsn_error_subtype_get(src, &val16);
    of_bsn_error_subtype_set(dst, val16);

    of_bsn_error_experimenter_get(src, &val32);
    of_bsn_error_experimenter_set(dst, val32);

    of_bsn_error_err_msg_get(src, &desc_str);
    of_bsn_error_err_msg_set(dst, desc_str);

    of_bsn_error_data_get(src, &octets);
    of_bsn_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_reply_t *
of_bsn_flow_checksum_bucket_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_flow_checksum_bucket_stats_reply_t *src)
{
    of_bsn_flow_checksum_bucket_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_flow_checksum_bucket_stats_entry_t src_list;
    of_list_bsn_flow_checksum_bucket_stats_entry_t *dst_list;

    if ((dst = of_bsn_flow_checksum_bucket_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_reply_xid_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_xid_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_flags_get(src, &val16);
    of_bsn_flow_checksum_bucket_stats_reply_flags_set(dst, val16);

    of_bsn_flow_checksum_bucket_stats_reply_experimenter_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_experimenter_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_subtype_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_reply_subtype_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_flow_checksum_bucket_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_flow_checksum_bucket_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_flow_checksum_bucket_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_request_t *
of_bsn_flow_checksum_bucket_stats_request_OF_VERSION_1_4_dup(
    of_bsn_flow_checksum_bucket_stats_request_t *src)
{
    of_bsn_flow_checksum_bucket_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;

    if ((dst = of_bsn_flow_checksum_bucket_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_request_xid_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_xid_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_flags_get(src, &val16);
    of_bsn_flow_checksum_bucket_stats_request_flags_set(dst, val16);

    of_bsn_flow_checksum_bucket_stats_request_experimenter_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_experimenter_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_subtype_get(src, &val32);
    of_bsn_flow_checksum_bucket_stats_request_subtype_set(dst, val32);

    of_bsn_flow_checksum_bucket_stats_request_table_id_get(src, &val8);
    of_bsn_flow_checksum_bucket_stats_request_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_t *
of_bsn_flow_idle_OF_VERSION_1_4_dup(
    of_bsn_flow_idle_t *src)
{
    of_bsn_flow_idle_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_bsn_flow_idle_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_xid_get(src, &val32);
    of_bsn_flow_idle_xid_set(dst, val32);

    of_bsn_flow_idle_experimenter_get(src, &val32);
    of_bsn_flow_idle_experimenter_set(dst, val32);

    of_bsn_flow_idle_subtype_get(src, &val32);
    of_bsn_flow_idle_subtype_set(dst, val32);

    of_bsn_flow_idle_cookie_get(src, &val64);
    of_bsn_flow_idle_cookie_set(dst, val64);

    of_bsn_flow_idle_priority_get(src, &val16);
    of_bsn_flow_idle_priority_set(dst, val16);

    of_bsn_flow_idle_table_id_get(src, &val8);
    of_bsn_flow_idle_table_id_set(dst, val8);

    of_bsn_flow_idle_match_get(src, &match);
    of_bsn_flow_idle_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_get_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_get_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_get_reply_t *
of_bsn_flow_idle_enable_get_reply_OF_VERSION_1_4_dup(
    of_bsn_flow_idle_enable_get_reply_t *src)
{
    of_bsn_flow_idle_enable_get_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_get_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_get_reply_xid_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_xid_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_get_reply_enabled_get(src, &val32);
    of_bsn_flow_idle_enable_get_reply_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_get_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_get_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_get_request_t *
of_bsn_flow_idle_enable_get_request_OF_VERSION_1_4_dup(
    of_bsn_flow_idle_enable_get_request_t *src)
{
    of_bsn_flow_idle_enable_get_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_get_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_get_request_xid_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_xid_set(dst, val32);

    of_bsn_flow_idle_enable_get_request_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_get_request_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_get_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_set_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_set_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_set_reply_t *
of_bsn_flow_idle_enable_set_reply_OF_VERSION_1_4_dup(
    of_bsn_flow_idle_enable_set_reply_t *src)
{
    of_bsn_flow_idle_enable_set_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_set_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_set_reply_xid_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_xid_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_enable_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_enable_set(dst, val32);

    of_bsn_flow_idle_enable_set_reply_status_get(src, &val32);
    of_bsn_flow_idle_enable_set_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_idle_enable_set_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_idle_enable_set_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_idle_enable_set_request_t *
of_bsn_flow_idle_enable_set_request_OF_VERSION_1_4_dup(
    of_bsn_flow_idle_enable_set_request_t *src)
{
    of_bsn_flow_idle_enable_set_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_flow_idle_enable_set_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_idle_enable_set_request_xid_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_xid_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_experimenter_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_experimenter_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_subtype_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_subtype_set(dst, val32);

    of_bsn_flow_idle_enable_set_request_enable_get(src, &val32);
    of_bsn_flow_idle_enable_set_request_enable_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_async
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_async.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_async_t *
of_bsn_generic_async_OF_VERSION_1_4_dup(
    of_bsn_generic_async_t *src)
{
    of_bsn_generic_async_t *dst;
    uint32_t val32;
    of_str64_t str64;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_async_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_async_xid_get(src, &val32);
    of_bsn_generic_async_xid_set(dst, val32);

    of_bsn_generic_async_experimenter_get(src, &val32);
    of_bsn_generic_async_experimenter_set(dst, val32);

    of_bsn_generic_async_subtype_get(src, &val32);
    of_bsn_generic_async_subtype_set(dst, val32);

    of_bsn_generic_async_name_get(src, &str64);
    of_bsn_generic_async_name_set(dst, str64);

    of_bsn_generic_async_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_async_delete(dst);
        return NULL;
    }
    of_bsn_generic_async_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_command
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_command.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_command_t *
of_bsn_generic_command_OF_VERSION_1_4_dup(
    of_bsn_generic_command_t *src)
{
    of_bsn_generic_command_t *dst;
    uint32_t val32;
    of_str64_t str64;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_command_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_command_xid_get(src, &val32);
    of_bsn_generic_command_xid_set(dst, val32);

    of_bsn_generic_command_experimenter_get(src, &val32);
    of_bsn_generic_command_experimenter_set(dst, val32);

    of_bsn_generic_command_subtype_get(src, &val32);
    of_bsn_generic_command_subtype_set(dst, val32);

    of_bsn_generic_command_name_get(src, &str64);
    of_bsn_generic_command_name_set(dst, str64);

    of_bsn_generic_command_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_command_delete(dst);
        return NULL;
    }
    of_bsn_generic_command_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_reply_t *
of_bsn_generic_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_generic_stats_reply_t *src)
{
    of_bsn_generic_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_generic_stats_entry_t src_list;
    of_list_bsn_generic_stats_entry_t *dst_list;

    if ((dst = of_bsn_generic_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_reply_xid_get(src, &val32);
    of_bsn_generic_stats_reply_xid_set(dst, val32);

    of_bsn_generic_stats_reply_flags_get(src, &val16);
    of_bsn_generic_stats_reply_flags_set(dst, val16);

    of_bsn_generic_stats_reply_experimenter_get(src, &val32);
    of_bsn_generic_stats_reply_experimenter_set(dst, val32);

    of_bsn_generic_stats_reply_subtype_get(src, &val32);
    of_bsn_generic_stats_reply_subtype_set(dst, val32);

    of_bsn_generic_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_generic_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_generic_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_request_t *
of_bsn_generic_stats_request_OF_VERSION_1_4_dup(
    of_bsn_generic_stats_request_t *src)
{
    of_bsn_generic_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_str64_t str64;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_request_xid_get(src, &val32);
    of_bsn_generic_stats_request_xid_set(dst, val32);

    of_bsn_generic_stats_request_flags_get(src, &val16);
    of_bsn_generic_stats_request_flags_set(dst, val16);

    of_bsn_generic_stats_request_experimenter_get(src, &val32);
    of_bsn_generic_stats_request_experimenter_set(dst, val32);

    of_bsn_generic_stats_request_subtype_get(src, &val32);
    of_bsn_generic_stats_request_subtype_set(dst, val32);

    of_bsn_generic_stats_request_name_get(src, &str64);
    of_bsn_generic_stats_request_name_set(dst, str64);

    of_bsn_generic_stats_request_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_request_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_request_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_reply_t *
of_bsn_gentable_bucket_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_bucket_stats_reply_t *src)
{
    of_bsn_gentable_bucket_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_bucket_stats_entry_t src_list;
    of_list_bsn_gentable_bucket_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_bucket_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_bucket_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_bucket_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_bucket_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_bucket_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_bucket_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_bucket_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_bucket_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_request_t *
of_bsn_gentable_bucket_stats_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_bucket_stats_request_t *src)
{
    of_bsn_gentable_bucket_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_bucket_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_request_xid_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_xid_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_flags_get(src, &val16);
    of_bsn_gentable_bucket_stats_request_flags_set(dst, val16);

    of_bsn_gentable_bucket_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_bucket_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_bucket_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_bucket_stats_request_table_id_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_clear_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_clear_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_clear_reply_t *
of_bsn_gentable_clear_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_clear_reply_t *src)
{
    of_bsn_gentable_clear_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_clear_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_clear_reply_xid_get(src, &val32);
    of_bsn_gentable_clear_reply_xid_set(dst, val32);

    of_bsn_gentable_clear_reply_experimenter_get(src, &val32);
    of_bsn_gentable_clear_reply_experimenter_set(dst, val32);

    of_bsn_gentable_clear_reply_subtype_get(src, &val32);
    of_bsn_gentable_clear_reply_subtype_set(dst, val32);

    of_bsn_gentable_clear_reply_table_id_get(src, &val16);
    of_bsn_gentable_clear_reply_table_id_set(dst, val16);

    of_bsn_gentable_clear_reply_deleted_count_get(src, &val32);
    of_bsn_gentable_clear_reply_deleted_count_set(dst, val32);

    of_bsn_gentable_clear_reply_error_count_get(src, &val32);
    of_bsn_gentable_clear_reply_error_count_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_clear_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_clear_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_clear_request_t *
of_bsn_gentable_clear_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_clear_request_t *src)
{
    of_bsn_gentable_clear_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_clear_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_clear_request_xid_get(src, &val32);
    of_bsn_gentable_clear_request_xid_set(dst, val32);

    of_bsn_gentable_clear_request_experimenter_get(src, &val32);
    of_bsn_gentable_clear_request_experimenter_set(dst, val32);

    of_bsn_gentable_clear_request_subtype_get(src, &val32);
    of_bsn_gentable_clear_request_subtype_set(dst, val32);

    of_bsn_gentable_clear_request_table_id_get(src, &val16);
    of_bsn_gentable_clear_request_table_id_set(dst, val16);

    of_bsn_gentable_clear_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_clear_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_clear_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_clear_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_reply_t *
of_bsn_gentable_desc_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_desc_stats_reply_t *src)
{
    of_bsn_gentable_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_desc_stats_entry_t src_list;
    of_list_bsn_gentable_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_desc_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_request_t *
of_bsn_gentable_desc_stats_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_desc_stats_request_t *src)
{
    of_bsn_gentable_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_request_xid_get(src, &val32);
    of_bsn_gentable_desc_stats_request_xid_set(dst, val32);

    of_bsn_gentable_desc_stats_request_flags_get(src, &val16);
    of_bsn_gentable_desc_stats_request_flags_set(dst, val16);

    of_bsn_gentable_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_desc_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_add_t *
of_bsn_gentable_entry_add_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_add_t *src)
{
    of_bsn_gentable_entry_add_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_add_xid_get(src, &val32);
    of_bsn_gentable_entry_add_xid_set(dst, val32);

    of_bsn_gentable_entry_add_experimenter_get(src, &val32);
    of_bsn_gentable_entry_add_experimenter_set(dst, val32);

    of_bsn_gentable_entry_add_subtype_get(src, &val32);
    of_bsn_gentable_entry_add_subtype_set(dst, val32);

    of_bsn_gentable_entry_add_table_id_get(src, &val16);
    of_bsn_gentable_entry_add_table_id_set(dst, val16);

    of_bsn_gentable_entry_add_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_add_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_add_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_add_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_add_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_add_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_add_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_add_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_delete_t *
of_bsn_gentable_entry_delete_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_delete_t *src)
{
    of_bsn_gentable_entry_delete_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_delete_xid_get(src, &val32);
    of_bsn_gentable_entry_delete_xid_set(dst, val32);

    of_bsn_gentable_entry_delete_experimenter_get(src, &val32);
    of_bsn_gentable_entry_delete_experimenter_set(dst, val32);

    of_bsn_gentable_entry_delete_subtype_get(src, &val32);
    of_bsn_gentable_entry_delete_subtype_set(dst, val32);

    of_bsn_gentable_entry_delete_table_id_get(src, &val16);
    of_bsn_gentable_entry_delete_table_id_set(dst, val16);

    of_bsn_gentable_entry_delete_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_delete_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_delete_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_reply_t *
of_bsn_gentable_entry_desc_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_desc_stats_reply_t *src)
{
    of_bsn_gentable_entry_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_entry_desc_stats_entry_t src_list;
    of_list_bsn_gentable_entry_desc_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_entry_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_entry_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_request_t *
of_bsn_gentable_entry_desc_stats_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_desc_stats_request_t *src)
{
    of_bsn_gentable_entry_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_entry_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_request_xid_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_xid_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_flags_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_request_flags_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_entry_desc_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_entry_desc_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_entry_desc_stats_request_table_id_set(dst, val16);

    of_bsn_gentable_entry_desc_stats_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_desc_stats_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_reply_t *
of_bsn_gentable_entry_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_stats_reply_t *src)
{
    of_bsn_gentable_entry_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_entry_stats_entry_t src_list;
    of_list_bsn_gentable_entry_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_entry_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_entry_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_entry_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_entry_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_entry_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_entry_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_request_t *
of_bsn_gentable_entry_stats_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_stats_request_t *src)
{
    of_bsn_gentable_entry_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_entry_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_request_xid_get(src, &val32);
    of_bsn_gentable_entry_stats_request_xid_set(dst, val32);

    of_bsn_gentable_entry_stats_request_flags_get(src, &val16);
    of_bsn_gentable_entry_stats_request_flags_set(dst, val16);

    of_bsn_gentable_entry_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_entry_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_entry_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_entry_stats_request_subtype_set(dst, val32);

    of_bsn_gentable_entry_stats_request_table_id_get(src, &val16);
    of_bsn_gentable_entry_stats_request_table_id_set(dst, val16);

    of_bsn_gentable_entry_stats_request_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_stats_request_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_stats_request_checksum_mask_get(src, &checksum_128);
    of_bsn_gentable_entry_stats_request_checksum_mask_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_error_t *
of_bsn_gentable_error_OF_VERSION_1_4_dup(
    of_bsn_gentable_error_t *src)
{
    of_bsn_gentable_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_gentable_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_error_xid_get(src, &val32);
    of_bsn_gentable_error_xid_set(dst, val32);

    of_bsn_gentable_error_subtype_get(src, &val16);
    of_bsn_gentable_error_subtype_set(dst, val16);

    of_bsn_gentable_error_experimenter_get(src, &val32);
    of_bsn_gentable_error_experimenter_set(dst, val32);

    of_bsn_gentable_error_table_id_get(src, &val32);
    of_bsn_gentable_error_table_id_set(dst, val32);

    of_bsn_gentable_error_err_msg_get(src, &desc_str);
    of_bsn_gentable_error_err_msg_set(dst, desc_str);

    of_bsn_gentable_error_data_get(src, &octets);
    of_bsn_gentable_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_full_error
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_full_error.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_full_error_t *
of_bsn_gentable_full_error_OF_VERSION_1_4_dup(
    of_bsn_gentable_full_error_t *src)
{
    of_bsn_gentable_full_error_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_octets_t octets;

    if ((dst = of_bsn_gentable_full_error_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_full_error_xid_get(src, &val32);
    of_bsn_gentable_full_error_xid_set(dst, val32);

    of_bsn_gentable_full_error_subtype_get(src, &val16);
    of_bsn_gentable_full_error_subtype_set(dst, val16);

    of_bsn_gentable_full_error_experimenter_get(src, &val32);
    of_bsn_gentable_full_error_experimenter_set(dst, val32);

    of_bsn_gentable_full_error_table_id_get(src, &val32);
    of_bsn_gentable_full_error_table_id_set(dst, val32);

    of_bsn_gentable_full_error_err_msg_get(src, &desc_str);
    of_bsn_gentable_full_error_err_msg_set(dst, desc_str);

    of_bsn_gentable_full_error_data_get(src, &octets);
    of_bsn_gentable_full_error_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_set_buckets_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_set_buckets_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_set_buckets_size_t *
of_bsn_gentable_set_buckets_size_OF_VERSION_1_4_dup(
    of_bsn_gentable_set_buckets_size_t *src)
{
    of_bsn_gentable_set_buckets_size_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_set_buckets_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_set_buckets_size_xid_get(src, &val32);
    of_bsn_gentable_set_buckets_size_xid_set(dst, val32);

    of_bsn_gentable_set_buckets_size_experimenter_get(src, &val32);
    of_bsn_gentable_set_buckets_size_experimenter_set(dst, val32);

    of_bsn_gentable_set_buckets_size_subtype_get(src, &val32);
    of_bsn_gentable_set_buckets_size_subtype_set(dst, val32);

    of_bsn_gentable_set_buckets_size_table_id_get(src, &val16);
    of_bsn_gentable_set_buckets_size_table_id_set(dst, val16);

    of_bsn_gentable_set_buckets_size_buckets_size_get(src, &val32);
    of_bsn_gentable_set_buckets_size_buckets_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_reply_t *
of_bsn_gentable_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_gentable_stats_reply_t *src)
{
    of_bsn_gentable_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_gentable_stats_entry_t src_list;
    of_list_bsn_gentable_stats_entry_t *dst_list;

    if ((dst = of_bsn_gentable_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_reply_xid_get(src, &val32);
    of_bsn_gentable_stats_reply_xid_set(dst, val32);

    of_bsn_gentable_stats_reply_flags_get(src, &val16);
    of_bsn_gentable_stats_reply_flags_set(dst, val16);

    of_bsn_gentable_stats_reply_experimenter_get(src, &val32);
    of_bsn_gentable_stats_reply_experimenter_set(dst, val32);

    of_bsn_gentable_stats_reply_subtype_get(src, &val32);
    of_bsn_gentable_stats_reply_subtype_set(dst, val32);

    of_bsn_gentable_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_gentable_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_gentable_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_request_t *
of_bsn_gentable_stats_request_OF_VERSION_1_4_dup(
    of_bsn_gentable_stats_request_t *src)
{
    of_bsn_gentable_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_gentable_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_request_xid_get(src, &val32);
    of_bsn_gentable_stats_request_xid_set(dst, val32);

    of_bsn_gentable_stats_request_flags_get(src, &val16);
    of_bsn_gentable_stats_request_flags_set(dst, val16);

    of_bsn_gentable_stats_request_experimenter_get(src, &val32);
    of_bsn_gentable_stats_request_experimenter_set(dst, val32);

    of_bsn_gentable_stats_request_subtype_get(src, &val32);
    of_bsn_gentable_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_reply_t *
of_bsn_get_interfaces_reply_OF_VERSION_1_4_dup(
    of_bsn_get_interfaces_reply_t *src)
{
    of_bsn_get_interfaces_reply_t *dst;
    uint32_t val32;

    of_list_bsn_interface_t src_list;
    of_list_bsn_interface_t *dst_list;

    if ((dst = of_bsn_get_interfaces_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_reply_xid_get(src, &val32);
    of_bsn_get_interfaces_reply_xid_set(dst, val32);

    of_bsn_get_interfaces_reply_experimenter_get(src, &val32);
    of_bsn_get_interfaces_reply_experimenter_set(dst, val32);

    of_bsn_get_interfaces_reply_subtype_get(src, &val32);
    of_bsn_get_interfaces_reply_subtype_set(dst, val32);

    of_bsn_get_interfaces_reply_interfaces_bind(
        src, &src_list);
    dst_list = of_list_bsn_interface_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_get_interfaces_reply_delete(dst);
        return NULL;
    }
    of_bsn_get_interfaces_reply_interfaces_set(dst, dst_list);
    of_list_bsn_interface_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_interfaces_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_interfaces_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_interfaces_request_t *
of_bsn_get_interfaces_request_OF_VERSION_1_4_dup(
    of_bsn_get_interfaces_request_t *src)
{
    of_bsn_get_interfaces_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_interfaces_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_interfaces_request_xid_get(src, &val32);
    of_bsn_get_interfaces_request_xid_set(dst, val32);

    of_bsn_get_interfaces_request_experimenter_get(src, &val32);
    of_bsn_get_interfaces_request_experimenter_set(dst, val32);

    of_bsn_get_interfaces_request_subtype_get(src, &val32);
    of_bsn_get_interfaces_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_reply_t *
of_bsn_get_mirroring_reply_OF_VERSION_1_4_dup(
    of_bsn_get_mirroring_reply_t *src)
{
    of_bsn_get_mirroring_reply_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_reply_xid_get(src, &val32);
    of_bsn_get_mirroring_reply_xid_set(dst, val32);

    of_bsn_get_mirroring_reply_experimenter_get(src, &val32);
    of_bsn_get_mirroring_reply_experimenter_set(dst, val32);

    of_bsn_get_mirroring_reply_subtype_get(src, &val32);
    of_bsn_get_mirroring_reply_subtype_set(dst, val32);

    of_bsn_get_mirroring_reply_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_reply_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_mirroring_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_mirroring_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_mirroring_request_t *
of_bsn_get_mirroring_request_OF_VERSION_1_4_dup(
    of_bsn_get_mirroring_request_t *src)
{
    of_bsn_get_mirroring_request_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_get_mirroring_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_mirroring_request_xid_get(src, &val32);
    of_bsn_get_mirroring_request_xid_set(dst, val32);

    of_bsn_get_mirroring_request_experimenter_get(src, &val32);
    of_bsn_get_mirroring_request_experimenter_set(dst, val32);

    of_bsn_get_mirroring_request_subtype_get(src, &val32);
    of_bsn_get_mirroring_request_subtype_set(dst, val32);

    of_bsn_get_mirroring_request_report_mirror_ports_get(src, &val8);
    of_bsn_get_mirroring_request_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_switch_pipeline_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_switch_pipeline_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_switch_pipeline_reply_t *
of_bsn_get_switch_pipeline_reply_OF_VERSION_1_4_dup(
    of_bsn_get_switch_pipeline_reply_t *src)
{
    of_bsn_get_switch_pipeline_reply_t *dst;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_get_switch_pipeline_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_switch_pipeline_reply_xid_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_xid_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_experimenter_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_experimenter_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_subtype_get(src, &val32);
    of_bsn_get_switch_pipeline_reply_subtype_set(dst, val32);

    of_bsn_get_switch_pipeline_reply_pipeline_get(src, &desc_str);
    of_bsn_get_switch_pipeline_reply_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_get_switch_pipeline_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_get_switch_pipeline_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_get_switch_pipeline_request_t *
of_bsn_get_switch_pipeline_request_OF_VERSION_1_4_dup(
    of_bsn_get_switch_pipeline_request_t *src)
{
    of_bsn_get_switch_pipeline_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_get_switch_pipeline_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_get_switch_pipeline_request_xid_get(src, &val32);
    of_bsn_get_switch_pipeline_request_xid_set(dst, val32);

    of_bsn_get_switch_pipeline_request_experimenter_get(src, &val32);
    of_bsn_get_switch_pipeline_request_experimenter_set(dst, val32);

    of_bsn_get_switch_pipeline_request_subtype_get(src, &val32);
    of_bsn_get_switch_pipeline_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_image_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_image_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_image_desc_stats_reply_t *
of_bsn_image_desc_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_image_desc_stats_reply_t *src)
{
    of_bsn_image_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_image_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_image_desc_stats_reply_xid_get(src, &val32);
    of_bsn_image_desc_stats_reply_xid_set(dst, val32);

    of_bsn_image_desc_stats_reply_flags_get(src, &val16);
    of_bsn_image_desc_stats_reply_flags_set(dst, val16);

    of_bsn_image_desc_stats_reply_experimenter_get(src, &val32);
    of_bsn_image_desc_stats_reply_experimenter_set(dst, val32);

    of_bsn_image_desc_stats_reply_subtype_get(src, &val32);
    of_bsn_image_desc_stats_reply_subtype_set(dst, val32);

    of_bsn_image_desc_stats_reply_image_checksum_get(src, &desc_str);
    of_bsn_image_desc_stats_reply_image_checksum_set(dst, desc_str);

    of_bsn_image_desc_stats_reply_startup_config_checksum_get(src, &desc_str);
    of_bsn_image_desc_stats_reply_startup_config_checksum_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_image_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_image_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_image_desc_stats_request_t *
of_bsn_image_desc_stats_request_OF_VERSION_1_4_dup(
    of_bsn_image_desc_stats_request_t *src)
{
    of_bsn_image_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_image_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_image_desc_stats_request_xid_get(src, &val32);
    of_bsn_image_desc_stats_request_xid_set(dst, val32);

    of_bsn_image_desc_stats_request_flags_get(src, &val16);
    of_bsn_image_desc_stats_request_flags_set(dst, val16);

    of_bsn_image_desc_stats_request_experimenter_get(src, &val32);
    of_bsn_image_desc_stats_request_experimenter_set(dst, val32);

    of_bsn_image_desc_stats_request_subtype_get(src, &val32);
    of_bsn_image_desc_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_convergence_notif
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_convergence_notif.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_convergence_notif_t *
of_bsn_lacp_convergence_notif_OF_VERSION_1_4_dup(
    of_bsn_lacp_convergence_notif_t *src)
{
    of_bsn_lacp_convergence_notif_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_lacp_convergence_notif_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_convergence_notif_xid_get(src, &val32);
    of_bsn_lacp_convergence_notif_xid_set(dst, val32);

    of_bsn_lacp_convergence_notif_experimenter_get(src, &val32);
    of_bsn_lacp_convergence_notif_experimenter_set(dst, val32);

    of_bsn_lacp_convergence_notif_subtype_get(src, &val32);
    of_bsn_lacp_convergence_notif_subtype_set(dst, val32);

    of_bsn_lacp_convergence_notif_convergence_status_get(src, &val8);
    of_bsn_lacp_convergence_notif_convergence_status_set(dst, val8);

    of_bsn_lacp_convergence_notif_port_no_get(src, &port_no);
    of_bsn_lacp_convergence_notif_port_no_set(dst, port_no);

    of_bsn_lacp_convergence_notif_actor_sys_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_sys_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_convergence_notif_actor_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_convergence_notif_actor_port_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_port_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_port_num_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_port_num_set(dst, val16);

    of_bsn_lacp_convergence_notif_actor_key_get(src, &val16);
    of_bsn_lacp_convergence_notif_actor_key_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_sys_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_sys_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_convergence_notif_partner_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_convergence_notif_partner_port_priority_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_port_priority_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_port_num_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_port_num_set(dst, val16);

    of_bsn_lacp_convergence_notif_partner_key_get(src, &val16);
    of_bsn_lacp_convergence_notif_partner_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_reply_t *
of_bsn_lacp_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_lacp_stats_reply_t *src)
{
    of_bsn_lacp_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_lacp_stats_entry_t src_list;
    of_list_bsn_lacp_stats_entry_t *dst_list;

    if ((dst = of_bsn_lacp_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_reply_xid_get(src, &val32);
    of_bsn_lacp_stats_reply_xid_set(dst, val32);

    of_bsn_lacp_stats_reply_flags_get(src, &val16);
    of_bsn_lacp_stats_reply_flags_set(dst, val16);

    of_bsn_lacp_stats_reply_experimenter_get(src, &val32);
    of_bsn_lacp_stats_reply_experimenter_set(dst, val32);

    of_bsn_lacp_stats_reply_subtype_get(src, &val32);
    of_bsn_lacp_stats_reply_subtype_set(dst, val32);

    of_bsn_lacp_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_lacp_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_lacp_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_lacp_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_request_t *
of_bsn_lacp_stats_request_OF_VERSION_1_4_dup(
    of_bsn_lacp_stats_request_t *src)
{
    of_bsn_lacp_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_lacp_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_request_xid_get(src, &val32);
    of_bsn_lacp_stats_request_xid_set(dst, val32);

    of_bsn_lacp_stats_request_flags_get(src, &val16);
    of_bsn_lacp_stats_request_flags_set(dst, val16);

    of_bsn_lacp_stats_request_experimenter_get(src, &val32);
    of_bsn_lacp_stats_request_experimenter_set(dst, val32);

    of_bsn_lacp_stats_request_subtype_get(src, &val32);
    of_bsn_lacp_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_log
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_log.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_log_t *
of_bsn_log_OF_VERSION_1_4_dup(
    of_bsn_log_t *src)
{
    of_bsn_log_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_log_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_log_xid_get(src, &val32);
    of_bsn_log_xid_set(dst, val32);

    of_bsn_log_experimenter_get(src, &val32);
    of_bsn_log_experimenter_set(dst, val32);

    of_bsn_log_subtype_get(src, &val32);
    of_bsn_log_subtype_set(dst, val32);

    of_bsn_log_loglevel_get(src, &val8);
    of_bsn_log_loglevel_set(dst, val8);

    of_bsn_log_data_get(src, &octets);
    of_bsn_log_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_command_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_command_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_command_reply_t *
of_bsn_lua_command_reply_OF_VERSION_1_4_dup(
    of_bsn_lua_command_reply_t *src)
{
    of_bsn_lua_command_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_command_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_command_reply_xid_get(src, &val32);
    of_bsn_lua_command_reply_xid_set(dst, val32);

    of_bsn_lua_command_reply_experimenter_get(src, &val32);
    of_bsn_lua_command_reply_experimenter_set(dst, val32);

    of_bsn_lua_command_reply_subtype_get(src, &val32);
    of_bsn_lua_command_reply_subtype_set(dst, val32);

    of_bsn_lua_command_reply_data_get(src, &octets);
    of_bsn_lua_command_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_command_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_command_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_command_request_t *
of_bsn_lua_command_request_OF_VERSION_1_4_dup(
    of_bsn_lua_command_request_t *src)
{
    of_bsn_lua_command_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_command_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_command_request_xid_get(src, &val32);
    of_bsn_lua_command_request_xid_set(dst, val32);

    of_bsn_lua_command_request_experimenter_get(src, &val32);
    of_bsn_lua_command_request_experimenter_set(dst, val32);

    of_bsn_lua_command_request_subtype_get(src, &val32);
    of_bsn_lua_command_request_subtype_set(dst, val32);

    of_bsn_lua_command_request_data_get(src, &octets);
    of_bsn_lua_command_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_notification
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_notification.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_notification_t *
of_bsn_lua_notification_OF_VERSION_1_4_dup(
    of_bsn_lua_notification_t *src)
{
    of_bsn_lua_notification_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_bsn_lua_notification_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_notification_xid_get(src, &val32);
    of_bsn_lua_notification_xid_set(dst, val32);

    of_bsn_lua_notification_experimenter_get(src, &val32);
    of_bsn_lua_notification_experimenter_set(dst, val32);

    of_bsn_lua_notification_subtype_get(src, &val32);
    of_bsn_lua_notification_subtype_set(dst, val32);

    of_bsn_lua_notification_data_get(src, &octets);
    of_bsn_lua_notification_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lua_upload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lua_upload.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lua_upload_t *
of_bsn_lua_upload_OF_VERSION_1_4_dup(
    of_bsn_lua_upload_t *src)
{
    of_bsn_lua_upload_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_str64_t str64;
    of_octets_t octets;

    if ((dst = of_bsn_lua_upload_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lua_upload_xid_get(src, &val32);
    of_bsn_lua_upload_xid_set(dst, val32);

    of_bsn_lua_upload_experimenter_get(src, &val32);
    of_bsn_lua_upload_experimenter_set(dst, val32);

    of_bsn_lua_upload_subtype_get(src, &val32);
    of_bsn_lua_upload_subtype_set(dst, val32);

    of_bsn_lua_upload_flags_get(src, &val16);
    of_bsn_lua_upload_flags_set(dst, val16);

    of_bsn_lua_upload_filename_get(src, &str64);
    of_bsn_lua_upload_filename_set(dst, str64);

    of_bsn_lua_upload_data_get(src, &octets);
    of_bsn_lua_upload_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_reply_t *
of_bsn_pdu_rx_reply_OF_VERSION_1_4_dup(
    of_bsn_pdu_rx_reply_t *src)
{
    of_bsn_pdu_rx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_reply_xid_get(src, &val32);
    of_bsn_pdu_rx_reply_xid_set(dst, val32);

    of_bsn_pdu_rx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_rx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_rx_reply_subtype_get(src, &val32);
    of_bsn_pdu_rx_reply_subtype_set(dst, val32);

    of_bsn_pdu_rx_reply_status_get(src, &val32);
    of_bsn_pdu_rx_reply_status_set(dst, val32);

    of_bsn_pdu_rx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_rx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_rx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_rx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_request_t *
of_bsn_pdu_rx_request_OF_VERSION_1_4_dup(
    of_bsn_pdu_rx_request_t *src)
{
    of_bsn_pdu_rx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_rx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_request_xid_get(src, &val32);
    of_bsn_pdu_rx_request_xid_set(dst, val32);

    of_bsn_pdu_rx_request_experimenter_get(src, &val32);
    of_bsn_pdu_rx_request_experimenter_set(dst, val32);

    of_bsn_pdu_rx_request_subtype_get(src, &val32);
    of_bsn_pdu_rx_request_subtype_set(dst, val32);

    of_bsn_pdu_rx_request_timeout_ms_get(src, &val32);
    of_bsn_pdu_rx_request_timeout_ms_set(dst, val32);

    of_bsn_pdu_rx_request_port_no_get(src, &port_no);
    of_bsn_pdu_rx_request_port_no_set(dst, port_no);

    of_bsn_pdu_rx_request_slot_num_get(src, &val8);
    of_bsn_pdu_rx_request_slot_num_set(dst, val8);

    of_bsn_pdu_rx_request_data_get(src, &octets);
    of_bsn_pdu_rx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_rx_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_rx_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_rx_timeout_t *
of_bsn_pdu_rx_timeout_OF_VERSION_1_4_dup(
    of_bsn_pdu_rx_timeout_t *src)
{
    of_bsn_pdu_rx_timeout_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_rx_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_rx_timeout_xid_get(src, &val32);
    of_bsn_pdu_rx_timeout_xid_set(dst, val32);

    of_bsn_pdu_rx_timeout_experimenter_get(src, &val32);
    of_bsn_pdu_rx_timeout_experimenter_set(dst, val32);

    of_bsn_pdu_rx_timeout_subtype_get(src, &val32);
    of_bsn_pdu_rx_timeout_subtype_set(dst, val32);

    of_bsn_pdu_rx_timeout_port_no_get(src, &port_no);
    of_bsn_pdu_rx_timeout_port_no_set(dst, port_no);

    of_bsn_pdu_rx_timeout_slot_num_get(src, &val8);
    of_bsn_pdu_rx_timeout_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_reply_t *
of_bsn_pdu_tx_reply_OF_VERSION_1_4_dup(
    of_bsn_pdu_tx_reply_t *src)
{
    of_bsn_pdu_tx_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;

    if ((dst = of_bsn_pdu_tx_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_reply_xid_get(src, &val32);
    of_bsn_pdu_tx_reply_xid_set(dst, val32);

    of_bsn_pdu_tx_reply_experimenter_get(src, &val32);
    of_bsn_pdu_tx_reply_experimenter_set(dst, val32);

    of_bsn_pdu_tx_reply_subtype_get(src, &val32);
    of_bsn_pdu_tx_reply_subtype_set(dst, val32);

    of_bsn_pdu_tx_reply_status_get(src, &val32);
    of_bsn_pdu_tx_reply_status_set(dst, val32);

    of_bsn_pdu_tx_reply_port_no_get(src, &port_no);
    of_bsn_pdu_tx_reply_port_no_set(dst, port_no);

    of_bsn_pdu_tx_reply_slot_num_get(src, &val8);
    of_bsn_pdu_tx_reply_slot_num_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_pdu_tx_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_pdu_tx_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_pdu_tx_request_t *
of_bsn_pdu_tx_request_OF_VERSION_1_4_dup(
    of_bsn_pdu_tx_request_t *src)
{
    of_bsn_pdu_tx_request_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    uint8_t val8;
    of_octets_t octets;

    if ((dst = of_bsn_pdu_tx_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_pdu_tx_request_xid_get(src, &val32);
    of_bsn_pdu_tx_request_xid_set(dst, val32);

    of_bsn_pdu_tx_request_experimenter_get(src, &val32);
    of_bsn_pdu_tx_request_experimenter_set(dst, val32);

    of_bsn_pdu_tx_request_subtype_get(src, &val32);
    of_bsn_pdu_tx_request_subtype_set(dst, val32);

    of_bsn_pdu_tx_request_tx_interval_ms_get(src, &val32);
    of_bsn_pdu_tx_request_tx_interval_ms_set(dst, val32);

    of_bsn_pdu_tx_request_port_no_get(src, &port_no);
    of_bsn_pdu_tx_request_port_no_set(dst, port_no);

    of_bsn_pdu_tx_request_slot_num_get(src, &val8);
    of_bsn_pdu_tx_request_slot_num_set(dst, val8);

    of_bsn_pdu_tx_request_data_get(src, &octets);
    of_bsn_pdu_tx_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_reply_t *
of_bsn_port_counter_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_port_counter_stats_reply_t *src)
{
    of_bsn_port_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_port_counter_stats_entry_t src_list;
    of_list_bsn_port_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_port_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_reply_xid_get(src, &val32);
    of_bsn_port_counter_stats_reply_xid_set(dst, val32);

    of_bsn_port_counter_stats_reply_flags_get(src, &val16);
    of_bsn_port_counter_stats_reply_flags_set(dst, val16);

    of_bsn_port_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_port_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_port_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_port_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_port_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_port_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_port_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_port_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_request_t *
of_bsn_port_counter_stats_request_OF_VERSION_1_4_dup(
    of_bsn_port_counter_stats_request_t *src)
{
    of_bsn_port_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_bsn_port_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_request_xid_get(src, &val32);
    of_bsn_port_counter_stats_request_xid_set(dst, val32);

    of_bsn_port_counter_stats_request_flags_get(src, &val16);
    of_bsn_port_counter_stats_request_flags_set(dst, val16);

    of_bsn_port_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_port_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_port_counter_stats_request_subtype_get(src, &val32);
    of_bsn_port_counter_stats_request_subtype_set(dst, val32);

    of_bsn_port_counter_stats_request_port_no_get(src, &port_no);
    of_bsn_port_counter_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_aux_cxns_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_aux_cxns_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_aux_cxns_reply_t *
of_bsn_set_aux_cxns_reply_OF_VERSION_1_4_dup(
    of_bsn_set_aux_cxns_reply_t *src)
{
    of_bsn_set_aux_cxns_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_aux_cxns_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_aux_cxns_reply_xid_get(src, &val32);
    of_bsn_set_aux_cxns_reply_xid_set(dst, val32);

    of_bsn_set_aux_cxns_reply_experimenter_get(src, &val32);
    of_bsn_set_aux_cxns_reply_experimenter_set(dst, val32);

    of_bsn_set_aux_cxns_reply_subtype_get(src, &val32);
    of_bsn_set_aux_cxns_reply_subtype_set(dst, val32);

    of_bsn_set_aux_cxns_reply_num_aux_get(src, &val32);
    of_bsn_set_aux_cxns_reply_num_aux_set(dst, val32);

    of_bsn_set_aux_cxns_reply_status_get(src, &val32);
    of_bsn_set_aux_cxns_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_aux_cxns_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_aux_cxns_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_aux_cxns_request_t *
of_bsn_set_aux_cxns_request_OF_VERSION_1_4_dup(
    of_bsn_set_aux_cxns_request_t *src)
{
    of_bsn_set_aux_cxns_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_aux_cxns_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_aux_cxns_request_xid_get(src, &val32);
    of_bsn_set_aux_cxns_request_xid_set(dst, val32);

    of_bsn_set_aux_cxns_request_experimenter_get(src, &val32);
    of_bsn_set_aux_cxns_request_experimenter_set(dst, val32);

    of_bsn_set_aux_cxns_request_subtype_get(src, &val32);
    of_bsn_set_aux_cxns_request_subtype_set(dst, val32);

    of_bsn_set_aux_cxns_request_num_aux_get(src, &val32);
    of_bsn_set_aux_cxns_request_num_aux_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_lacp_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_lacp_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_lacp_reply_t *
of_bsn_set_lacp_reply_OF_VERSION_1_4_dup(
    of_bsn_set_lacp_reply_t *src)
{
    of_bsn_set_lacp_reply_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    if ((dst = of_bsn_set_lacp_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_lacp_reply_xid_get(src, &val32);
    of_bsn_set_lacp_reply_xid_set(dst, val32);

    of_bsn_set_lacp_reply_experimenter_get(src, &val32);
    of_bsn_set_lacp_reply_experimenter_set(dst, val32);

    of_bsn_set_lacp_reply_subtype_get(src, &val32);
    of_bsn_set_lacp_reply_subtype_set(dst, val32);

    of_bsn_set_lacp_reply_status_get(src, &val32);
    of_bsn_set_lacp_reply_status_set(dst, val32);

    of_bsn_set_lacp_reply_port_no_get(src, &port_no);
    of_bsn_set_lacp_reply_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_lacp_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_lacp_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_lacp_request_t *
of_bsn_set_lacp_request_OF_VERSION_1_4_dup(
    of_bsn_set_lacp_request_t *src)
{
    of_bsn_set_lacp_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_set_lacp_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_lacp_request_xid_get(src, &val32);
    of_bsn_set_lacp_request_xid_set(dst, val32);

    of_bsn_set_lacp_request_experimenter_get(src, &val32);
    of_bsn_set_lacp_request_experimenter_set(dst, val32);

    of_bsn_set_lacp_request_subtype_get(src, &val32);
    of_bsn_set_lacp_request_subtype_set(dst, val32);

    of_bsn_set_lacp_request_enabled_get(src, &val8);
    of_bsn_set_lacp_request_enabled_set(dst, val8);

    of_bsn_set_lacp_request_port_no_get(src, &port_no);
    of_bsn_set_lacp_request_port_no_set(dst, port_no);

    of_bsn_set_lacp_request_actor_sys_priority_get(src, &val16);
    of_bsn_set_lacp_request_actor_sys_priority_set(dst, val16);

    of_bsn_set_lacp_request_actor_sys_mac_get(src, &mac_addr);
    of_bsn_set_lacp_request_actor_sys_mac_set(dst, mac_addr);

    of_bsn_set_lacp_request_actor_port_priority_get(src, &val16);
    of_bsn_set_lacp_request_actor_port_priority_set(dst, val16);

    of_bsn_set_lacp_request_actor_port_num_get(src, &val16);
    of_bsn_set_lacp_request_actor_port_num_set(dst, val16);

    of_bsn_set_lacp_request_actor_key_get(src, &val16);
    of_bsn_set_lacp_request_actor_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_mirroring
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_mirroring.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_mirroring_t *
of_bsn_set_mirroring_OF_VERSION_1_4_dup(
    of_bsn_set_mirroring_t *src)
{
    of_bsn_set_mirroring_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_set_mirroring_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_mirroring_xid_get(src, &val32);
    of_bsn_set_mirroring_xid_set(dst, val32);

    of_bsn_set_mirroring_experimenter_get(src, &val32);
    of_bsn_set_mirroring_experimenter_set(dst, val32);

    of_bsn_set_mirroring_subtype_get(src, &val32);
    of_bsn_set_mirroring_subtype_set(dst, val32);

    of_bsn_set_mirroring_report_mirror_ports_get(src, &val8);
    of_bsn_set_mirroring_report_mirror_ports_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_reply_t *
of_bsn_set_pktin_suppression_reply_OF_VERSION_1_4_dup(
    of_bsn_set_pktin_suppression_reply_t *src)
{
    of_bsn_set_pktin_suppression_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_pktin_suppression_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_reply_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_reply_status_get(src, &val32);
    of_bsn_set_pktin_suppression_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_pktin_suppression_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_pktin_suppression_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_pktin_suppression_request_t *
of_bsn_set_pktin_suppression_request_OF_VERSION_1_4_dup(
    of_bsn_set_pktin_suppression_request_t *src)
{
    of_bsn_set_pktin_suppression_request_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint16_t val16;
    uint64_t val64;

    if ((dst = of_bsn_set_pktin_suppression_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_pktin_suppression_request_xid_get(src, &val32);
    of_bsn_set_pktin_suppression_request_xid_set(dst, val32);

    of_bsn_set_pktin_suppression_request_experimenter_get(src, &val32);
    of_bsn_set_pktin_suppression_request_experimenter_set(dst, val32);

    of_bsn_set_pktin_suppression_request_subtype_get(src, &val32);
    of_bsn_set_pktin_suppression_request_subtype_set(dst, val32);

    of_bsn_set_pktin_suppression_request_enabled_get(src, &val8);
    of_bsn_set_pktin_suppression_request_enabled_set(dst, val8);

    of_bsn_set_pktin_suppression_request_idle_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_idle_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_hard_timeout_get(src, &val16);
    of_bsn_set_pktin_suppression_request_hard_timeout_set(dst, val16);

    of_bsn_set_pktin_suppression_request_priority_get(src, &val16);
    of_bsn_set_pktin_suppression_request_priority_set(dst, val16);

    of_bsn_set_pktin_suppression_request_cookie_get(src, &val64);
    of_bsn_set_pktin_suppression_request_cookie_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_switch_pipeline_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_switch_pipeline_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_switch_pipeline_reply_t *
of_bsn_set_switch_pipeline_reply_OF_VERSION_1_4_dup(
    of_bsn_set_switch_pipeline_reply_t *src)
{
    of_bsn_set_switch_pipeline_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_set_switch_pipeline_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_switch_pipeline_reply_xid_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_xid_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_experimenter_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_experimenter_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_subtype_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_subtype_set(dst, val32);

    of_bsn_set_switch_pipeline_reply_status_get(src, &val32);
    of_bsn_set_switch_pipeline_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_set_switch_pipeline_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_set_switch_pipeline_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_set_switch_pipeline_request_t *
of_bsn_set_switch_pipeline_request_OF_VERSION_1_4_dup(
    of_bsn_set_switch_pipeline_request_t *src)
{
    of_bsn_set_switch_pipeline_request_t *dst;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_set_switch_pipeline_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_set_switch_pipeline_request_xid_get(src, &val32);
    of_bsn_set_switch_pipeline_request_xid_set(dst, val32);

    of_bsn_set_switch_pipeline_request_experimenter_get(src, &val32);
    of_bsn_set_switch_pipeline_request_experimenter_set(dst, val32);

    of_bsn_set_switch_pipeline_request_subtype_get(src, &val32);
    of_bsn_set_switch_pipeline_request_subtype_set(dst, val32);

    of_bsn_set_switch_pipeline_request_pipeline_get(src, &desc_str);
    of_bsn_set_switch_pipeline_request_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_reply_t *
of_bsn_switch_pipeline_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_switch_pipeline_stats_reply_t *src)
{
    of_bsn_switch_pipeline_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_switch_pipeline_stats_entry_t src_list;
    of_list_bsn_switch_pipeline_stats_entry_t *dst_list;

    if ((dst = of_bsn_switch_pipeline_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_reply_xid_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_xid_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_flags_get(src, &val16);
    of_bsn_switch_pipeline_stats_reply_flags_set(dst, val16);

    of_bsn_switch_pipeline_stats_reply_experimenter_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_experimenter_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_subtype_get(src, &val32);
    of_bsn_switch_pipeline_stats_reply_subtype_set(dst, val32);

    of_bsn_switch_pipeline_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_switch_pipeline_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_switch_pipeline_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_switch_pipeline_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_request_t *
of_bsn_switch_pipeline_stats_request_OF_VERSION_1_4_dup(
    of_bsn_switch_pipeline_stats_request_t *src)
{
    of_bsn_switch_pipeline_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_switch_pipeline_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_request_xid_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_xid_set(dst, val32);

    of_bsn_switch_pipeline_stats_request_flags_get(src, &val16);
    of_bsn_switch_pipeline_stats_request_flags_set(dst, val16);

    of_bsn_switch_pipeline_stats_request_experimenter_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_experimenter_set(dst, val32);

    of_bsn_switch_pipeline_stats_request_subtype_get(src, &val32);
    of_bsn_switch_pipeline_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_reply_t *
of_bsn_table_checksum_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_table_checksum_stats_reply_t *src)
{
    of_bsn_table_checksum_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_table_checksum_stats_entry_t src_list;
    of_list_bsn_table_checksum_stats_entry_t *dst_list;

    if ((dst = of_bsn_table_checksum_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_reply_xid_get(src, &val32);
    of_bsn_table_checksum_stats_reply_xid_set(dst, val32);

    of_bsn_table_checksum_stats_reply_flags_get(src, &val16);
    of_bsn_table_checksum_stats_reply_flags_set(dst, val16);

    of_bsn_table_checksum_stats_reply_experimenter_get(src, &val32);
    of_bsn_table_checksum_stats_reply_experimenter_set(dst, val32);

    of_bsn_table_checksum_stats_reply_subtype_get(src, &val32);
    of_bsn_table_checksum_stats_reply_subtype_set(dst, val32);

    of_bsn_table_checksum_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_table_checksum_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_table_checksum_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_table_checksum_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_request_t *
of_bsn_table_checksum_stats_request_OF_VERSION_1_4_dup(
    of_bsn_table_checksum_stats_request_t *src)
{
    of_bsn_table_checksum_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_table_checksum_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_request_xid_get(src, &val32);
    of_bsn_table_checksum_stats_request_xid_set(dst, val32);

    of_bsn_table_checksum_stats_request_flags_get(src, &val16);
    of_bsn_table_checksum_stats_request_flags_set(dst, val16);

    of_bsn_table_checksum_stats_request_experimenter_get(src, &val32);
    of_bsn_table_checksum_stats_request_experimenter_set(dst, val32);

    of_bsn_table_checksum_stats_request_subtype_get(src, &val32);
    of_bsn_table_checksum_stats_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_set_buckets_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_set_buckets_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_set_buckets_size_t *
of_bsn_table_set_buckets_size_OF_VERSION_1_4_dup(
    of_bsn_table_set_buckets_size_t *src)
{
    of_bsn_table_set_buckets_size_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_bsn_table_set_buckets_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_set_buckets_size_xid_get(src, &val32);
    of_bsn_table_set_buckets_size_xid_set(dst, val32);

    of_bsn_table_set_buckets_size_experimenter_get(src, &val32);
    of_bsn_table_set_buckets_size_experimenter_set(dst, val32);

    of_bsn_table_set_buckets_size_subtype_get(src, &val32);
    of_bsn_table_set_buckets_size_subtype_set(dst, val32);

    of_bsn_table_set_buckets_size_table_id_get(src, &val8);
    of_bsn_table_set_buckets_size_table_id_set(dst, val8);

    of_bsn_table_set_buckets_size_buckets_size_get(src, &val32);
    of_bsn_table_set_buckets_size_buckets_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_takeover
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_takeover.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_takeover_t *
of_bsn_takeover_OF_VERSION_1_4_dup(
    of_bsn_takeover_t *src)
{
    of_bsn_takeover_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_takeover_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_takeover_xid_get(src, &val32);
    of_bsn_takeover_xid_set(dst, val32);

    of_bsn_takeover_experimenter_get(src, &val32);
    of_bsn_takeover_experimenter_set(dst, val32);

    of_bsn_takeover_subtype_get(src, &val32);
    of_bsn_takeover_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_time_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_time_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_time_reply_t *
of_bsn_time_reply_OF_VERSION_1_4_dup(
    of_bsn_time_reply_t *src)
{
    of_bsn_time_reply_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_bsn_time_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_time_reply_xid_get(src, &val32);
    of_bsn_time_reply_xid_set(dst, val32);

    of_bsn_time_reply_experimenter_get(src, &val32);
    of_bsn_time_reply_experimenter_set(dst, val32);

    of_bsn_time_reply_subtype_get(src, &val32);
    of_bsn_time_reply_subtype_set(dst, val32);

    of_bsn_time_reply_time_ms_get(src, &val64);
    of_bsn_time_reply_time_ms_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_time_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_time_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_time_request_t *
of_bsn_time_request_OF_VERSION_1_4_dup(
    of_bsn_time_request_t *src)
{
    of_bsn_time_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_time_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_time_request_xid_get(src, &val32);
    of_bsn_time_request_xid_set(dst, val32);

    of_bsn_time_request_experimenter_get(src, &val32);
    of_bsn_time_request_experimenter_set(dst, val32);

    of_bsn_time_request_subtype_get(src, &val32);
    of_bsn_time_request_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_reply_t *
of_bsn_virtual_port_create_reply_OF_VERSION_1_4_dup(
    of_bsn_virtual_port_create_reply_t *src)
{
    of_bsn_virtual_port_create_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_create_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_reply_xid_get(src, &val32);
    of_bsn_virtual_port_create_reply_xid_set(dst, val32);

    of_bsn_virtual_port_create_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_create_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_create_reply_status_get(src, &val32);
    of_bsn_virtual_port_create_reply_status_set(dst, val32);

    of_bsn_virtual_port_create_reply_vport_no_get(src, &val32);
    of_bsn_virtual_port_create_reply_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_create_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_create_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_create_request_t *
of_bsn_virtual_port_create_request_OF_VERSION_1_4_dup(
    of_bsn_virtual_port_create_request_t *src)
{
    of_bsn_virtual_port_create_request_t *dst;
    uint32_t val32;

    of_bsn_vport_q_in_q_t src_bsn_vport;
    of_bsn_vport_q_in_q_t *dst_bsn_vport;

    if ((dst = of_bsn_virtual_port_create_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_create_request_xid_get(src, &val32);
    of_bsn_virtual_port_create_request_xid_set(dst, val32);

    of_bsn_virtual_port_create_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_create_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_create_request_subtype_get(src, &val32);
    of_bsn_virtual_port_create_request_subtype_set(dst, val32);

    of_bsn_virtual_port_create_request_vport_bind(
        src, &src_bsn_vport);
    dst_bsn_vport = of_bsn_vport_q_in_q_OF_VERSION_1_4_dup(&src_bsn_vport);
    if (dst_bsn_vport == NULL) {
        of_bsn_virtual_port_create_request_delete(dst);
        return NULL;
    }
    of_bsn_virtual_port_create_request_vport_set(dst, dst_bsn_vport);
    of_bsn_vport_q_in_q_delete(dst_bsn_vport);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_reply_t *
of_bsn_virtual_port_remove_reply_OF_VERSION_1_4_dup(
    of_bsn_virtual_port_remove_reply_t *src)
{
    of_bsn_virtual_port_remove_reply_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_reply_xid_get(src, &val32);
    of_bsn_virtual_port_remove_reply_xid_set(dst, val32);

    of_bsn_virtual_port_remove_reply_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_reply_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_reply_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_reply_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_reply_status_get(src, &val32);
    of_bsn_virtual_port_remove_reply_status_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_virtual_port_remove_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_virtual_port_remove_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_virtual_port_remove_request_t *
of_bsn_virtual_port_remove_request_OF_VERSION_1_4_dup(
    of_bsn_virtual_port_remove_request_t *src)
{
    of_bsn_virtual_port_remove_request_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_virtual_port_remove_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_virtual_port_remove_request_xid_get(src, &val32);
    of_bsn_virtual_port_remove_request_xid_set(dst, val32);

    of_bsn_virtual_port_remove_request_experimenter_get(src, &val32);
    of_bsn_virtual_port_remove_request_experimenter_set(dst, val32);

    of_bsn_virtual_port_remove_request_subtype_get(src, &val32);
    of_bsn_virtual_port_remove_request_subtype_set(dst, val32);

    of_bsn_virtual_port_remove_request_vport_no_get(src, &val32);
    of_bsn_virtual_port_remove_request_vport_no_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_clear
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_clear.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_clear_t *
of_bsn_vlan_counter_clear_OF_VERSION_1_4_dup(
    of_bsn_vlan_counter_clear_t *src)
{
    of_bsn_vlan_counter_clear_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vlan_counter_clear_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_clear_xid_get(src, &val32);
    of_bsn_vlan_counter_clear_xid_set(dst, val32);

    of_bsn_vlan_counter_clear_experimenter_get(src, &val32);
    of_bsn_vlan_counter_clear_experimenter_set(dst, val32);

    of_bsn_vlan_counter_clear_subtype_get(src, &val32);
    of_bsn_vlan_counter_clear_subtype_set(dst, val32);

    of_bsn_vlan_counter_clear_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_clear_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_reply_t *
of_bsn_vlan_counter_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_vlan_counter_stats_reply_t *src)
{
    of_bsn_vlan_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_vlan_counter_stats_entry_t src_list;
    of_list_bsn_vlan_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_vlan_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_reply_xid_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_xid_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_flags_get(src, &val16);
    of_bsn_vlan_counter_stats_reply_flags_set(dst, val16);

    of_bsn_vlan_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_vlan_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_vlan_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vlan_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_vlan_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_vlan_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_request_t *
of_bsn_vlan_counter_stats_request_OF_VERSION_1_4_dup(
    of_bsn_vlan_counter_stats_request_t *src)
{
    of_bsn_vlan_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vlan_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_request_xid_get(src, &val32);
    of_bsn_vlan_counter_stats_request_xid_set(dst, val32);

    of_bsn_vlan_counter_stats_request_flags_get(src, &val16);
    of_bsn_vlan_counter_stats_request_flags_set(dst, val16);

    of_bsn_vlan_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_vlan_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_vlan_counter_stats_request_subtype_get(src, &val32);
    of_bsn_vlan_counter_stats_request_subtype_set(dst, val32);

    of_bsn_vlan_counter_stats_request_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_stats_request_vlan_vid_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_reply_t *
of_bsn_vrf_counter_stats_reply_OF_VERSION_1_4_dup(
    of_bsn_vrf_counter_stats_reply_t *src)
{
    of_bsn_vrf_counter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bsn_vrf_counter_stats_entry_t src_list;
    of_list_bsn_vrf_counter_stats_entry_t *dst_list;

    if ((dst = of_bsn_vrf_counter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_reply_xid_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_xid_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_flags_get(src, &val16);
    of_bsn_vrf_counter_stats_reply_flags_set(dst, val16);

    of_bsn_vrf_counter_stats_reply_experimenter_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_experimenter_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_subtype_get(src, &val32);
    of_bsn_vrf_counter_stats_reply_subtype_set(dst, val32);

    of_bsn_vrf_counter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vrf_counter_stats_reply_delete(dst);
        return NULL;
    }
    of_bsn_vrf_counter_stats_reply_entries_set(dst, dst_list);
    of_list_bsn_vrf_counter_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_request_t *
of_bsn_vrf_counter_stats_request_OF_VERSION_1_4_dup(
    of_bsn_vrf_counter_stats_request_t *src)
{
    of_bsn_vrf_counter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_bsn_vrf_counter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_request_xid_get(src, &val32);
    of_bsn_vrf_counter_stats_request_xid_set(dst, val32);

    of_bsn_vrf_counter_stats_request_flags_get(src, &val16);
    of_bsn_vrf_counter_stats_request_flags_set(dst, val16);

    of_bsn_vrf_counter_stats_request_experimenter_get(src, &val32);
    of_bsn_vrf_counter_stats_request_experimenter_set(dst, val32);

    of_bsn_vrf_counter_stats_request_subtype_get(src, &val32);
    of_bsn_vrf_counter_stats_request_subtype_set(dst, val32);

    of_bsn_vrf_counter_stats_request_vrf_get(src, &val32);
    of_bsn_vrf_counter_stats_request_vrf_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bundle_add_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bundle_add_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bundle_add_msg_t *
of_bundle_add_msg_OF_VERSION_1_4_dup(
    of_bundle_add_msg_t *src)
{
    of_bundle_add_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bundle_add_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bundle_add_msg_xid_get(src, &val32);
    of_bundle_add_msg_xid_set(dst, val32);

    of_bundle_add_msg_bundle_id_get(src, &val32);
    of_bundle_add_msg_bundle_id_set(dst, val32);

    of_bundle_add_msg_flags_get(src, &val16);
    of_bundle_add_msg_flags_set(dst, val16);

    of_bundle_add_msg_data_get(src, &octets);
    of_bundle_add_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bundle_ctrl_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bundle_ctrl_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bundle_ctrl_msg_t *
of_bundle_ctrl_msg_OF_VERSION_1_4_dup(
    of_bundle_ctrl_msg_t *src)
{
    of_bundle_ctrl_msg_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_bundle_prop_t src_list;
    of_list_bundle_prop_t *dst_list;

    if ((dst = of_bundle_ctrl_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bundle_ctrl_msg_xid_get(src, &val32);
    of_bundle_ctrl_msg_xid_set(dst, val32);

    of_bundle_ctrl_msg_bundle_id_get(src, &val32);
    of_bundle_ctrl_msg_bundle_id_set(dst, val32);

    of_bundle_ctrl_msg_bundle_ctrl_type_get(src, &val16);
    of_bundle_ctrl_msg_bundle_ctrl_type_set(dst, val16);

    of_bundle_ctrl_msg_flags_get(src, &val16);
    of_bundle_ctrl_msg_flags_set(dst, val16);

    of_bundle_ctrl_msg_properties_bind(
        src, &src_list);
    dst_list = of_list_bundle_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bundle_ctrl_msg_delete(dst);
        return NULL;
    }
    of_bundle_ctrl_msg_properties_set(dst, dst_list);
    of_list_bundle_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bundle_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bundle_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_bundle_failed_error_msg_t *
of_bundle_failed_error_msg_OF_VERSION_1_4_dup(
    of_bundle_failed_error_msg_t *src)
{
    of_bundle_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_bundle_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_bundle_failed_error_msg_xid_get(src, &val32);
    of_bundle_failed_error_msg_xid_set(dst, val32);

    of_bundle_failed_error_msg_code_get(src, &val16);
    of_bundle_failed_error_msg_code_set(dst, val16);

    of_bundle_failed_error_msg_data_get(src, &octets);
    of_bundle_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_reply_t *
of_desc_stats_reply_OF_VERSION_1_4_dup(
    of_desc_stats_reply_t *src)
{
    of_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_desc_str_t desc_str;
    of_serial_num_t ser_num;

    if ((dst = of_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_reply_xid_get(src, &val32);
    of_desc_stats_reply_xid_set(dst, val32);

    of_desc_stats_reply_flags_get(src, &val16);
    of_desc_stats_reply_flags_set(dst, val16);

    of_desc_stats_reply_mfr_desc_get(src, &desc_str);
    of_desc_stats_reply_mfr_desc_set(dst, desc_str);

    of_desc_stats_reply_hw_desc_get(src, &desc_str);
    of_desc_stats_reply_hw_desc_set(dst, desc_str);

    of_desc_stats_reply_sw_desc_get(src, &desc_str);
    of_desc_stats_reply_sw_desc_set(dst, desc_str);

    of_desc_stats_reply_serial_num_get(src, &ser_num);
    of_desc_stats_reply_serial_num_set(dst, ser_num);

    of_desc_stats_reply_dp_desc_get(src, &desc_str);
    of_desc_stats_reply_dp_desc_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_desc_stats_request_t *
of_desc_stats_request_OF_VERSION_1_4_dup(
    of_desc_stats_request_t *src)
{
    of_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_desc_stats_request_xid_get(src, &val32);
    of_desc_stats_request_xid_set(dst, val32);

    of_desc_stats_request_flags_get(src, &val16);
    of_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_echo_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_reply_t *
of_echo_reply_OF_VERSION_1_4_dup(
    of_echo_reply_t *src)
{
    of_echo_reply_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_reply_xid_get(src, &val32);
    of_echo_reply_xid_set(dst, val32);

    of_echo_reply_data_get(src, &octets);
    of_echo_reply_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_echo_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_echo_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_echo_request_t *
of_echo_request_OF_VERSION_1_4_dup(
    of_echo_request_t *src)
{
    of_echo_request_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_echo_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_echo_request_xid_get(src, &val32);
    of_echo_request_xid_set(dst, val32);

    of_echo_request_data_get(src, &octets);
    of_echo_request_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_features_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_reply_t *
of_features_reply_OF_VERSION_1_4_dup(
    of_features_reply_t *src)
{
    of_features_reply_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;

    if ((dst = of_features_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_reply_xid_get(src, &val32);
    of_features_reply_xid_set(dst, val32);

    of_features_reply_datapath_id_get(src, &val64);
    of_features_reply_datapath_id_set(dst, val64);

    of_features_reply_n_buffers_get(src, &val32);
    of_features_reply_n_buffers_set(dst, val32);

    of_features_reply_n_tables_get(src, &val8);
    of_features_reply_n_tables_set(dst, val8);

    of_features_reply_auxiliary_id_get(src, &val8);
    of_features_reply_auxiliary_id_set(dst, val8);

    of_features_reply_capabilities_get(src, &val32);
    of_features_reply_capabilities_set(dst, val32);

    of_features_reply_reserved_get(src, &val32);
    of_features_reply_reserved_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_features_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_features_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_features_request_t *
of_features_request_OF_VERSION_1_4_dup(
    of_features_request_t *src)
{
    of_features_request_t *dst;
    uint32_t val32;

    if ((dst = of_features_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_features_request_xid_get(src, &val32);
    of_features_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_flow_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_add_t *
of_flow_add_OF_VERSION_1_4_dup(
    of_flow_add_t *src)
{
    of_flow_add_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_add_xid_get(src, &val32);
    of_flow_add_xid_set(dst, val32);

    of_flow_add_cookie_get(src, &val64);
    of_flow_add_cookie_set(dst, val64);

    of_flow_add_cookie_mask_get(src, &val64);
    of_flow_add_cookie_mask_set(dst, val64);

    of_flow_add_table_id_get(src, &val8);
    of_flow_add_table_id_set(dst, val8);

    of_flow_add_idle_timeout_get(src, &val16);
    of_flow_add_idle_timeout_set(dst, val16);

    of_flow_add_hard_timeout_get(src, &val16);
    of_flow_add_hard_timeout_set(dst, val16);

    of_flow_add_priority_get(src, &val16);
    of_flow_add_priority_set(dst, val16);

    of_flow_add_buffer_id_get(src, &val32);
    of_flow_add_buffer_id_set(dst, val32);

    of_flow_add_out_port_get(src, &port_no);
    of_flow_add_out_port_set(dst, port_no);

    of_flow_add_out_group_get(src, &val32);
    of_flow_add_out_group_set(dst, val32);

    of_flow_add_flags_get(src, &val16);
    of_flow_add_flags_set(dst, val16);

    of_flow_add_importance_get(src, &val16);
    of_flow_add_importance_set(dst, val16);

    of_flow_add_match_get(src, &match);
    of_flow_add_match_set(dst, &match);

    of_flow_add_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_add_delete(dst);
        return NULL;
    }
    of_flow_add_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_t *
of_flow_delete_OF_VERSION_1_4_dup(
    of_flow_delete_t *src)
{
    of_flow_delete_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_xid_get(src, &val32);
    of_flow_delete_xid_set(dst, val32);

    of_flow_delete_cookie_get(src, &val64);
    of_flow_delete_cookie_set(dst, val64);

    of_flow_delete_cookie_mask_get(src, &val64);
    of_flow_delete_cookie_mask_set(dst, val64);

    of_flow_delete_table_id_get(src, &val8);
    of_flow_delete_table_id_set(dst, val8);

    of_flow_delete_idle_timeout_get(src, &val16);
    of_flow_delete_idle_timeout_set(dst, val16);

    of_flow_delete_hard_timeout_get(src, &val16);
    of_flow_delete_hard_timeout_set(dst, val16);

    of_flow_delete_priority_get(src, &val16);
    of_flow_delete_priority_set(dst, val16);

    of_flow_delete_buffer_id_get(src, &val32);
    of_flow_delete_buffer_id_set(dst, val32);

    of_flow_delete_out_port_get(src, &port_no);
    of_flow_delete_out_port_set(dst, port_no);

    of_flow_delete_out_group_get(src, &val32);
    of_flow_delete_out_group_set(dst, val32);

    of_flow_delete_flags_get(src, &val16);
    of_flow_delete_flags_set(dst, val16);

    of_flow_delete_importance_get(src, &val16);
    of_flow_delete_importance_set(dst, val16);

    of_flow_delete_match_get(src, &match);
    of_flow_delete_match_set(dst, &match);

    of_flow_delete_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_delete(dst);
        return NULL;
    }
    of_flow_delete_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_delete_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_delete_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_delete_strict_t *
of_flow_delete_strict_OF_VERSION_1_4_dup(
    of_flow_delete_strict_t *src)
{
    of_flow_delete_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_delete_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_delete_strict_xid_get(src, &val32);
    of_flow_delete_strict_xid_set(dst, val32);

    of_flow_delete_strict_cookie_get(src, &val64);
    of_flow_delete_strict_cookie_set(dst, val64);

    of_flow_delete_strict_cookie_mask_get(src, &val64);
    of_flow_delete_strict_cookie_mask_set(dst, val64);

    of_flow_delete_strict_table_id_get(src, &val8);
    of_flow_delete_strict_table_id_set(dst, val8);

    of_flow_delete_strict_idle_timeout_get(src, &val16);
    of_flow_delete_strict_idle_timeout_set(dst, val16);

    of_flow_delete_strict_hard_timeout_get(src, &val16);
    of_flow_delete_strict_hard_timeout_set(dst, val16);

    of_flow_delete_strict_priority_get(src, &val16);
    of_flow_delete_strict_priority_set(dst, val16);

    of_flow_delete_strict_buffer_id_get(src, &val32);
    of_flow_delete_strict_buffer_id_set(dst, val32);

    of_flow_delete_strict_out_port_get(src, &port_no);
    of_flow_delete_strict_out_port_set(dst, port_no);

    of_flow_delete_strict_out_group_get(src, &val32);
    of_flow_delete_strict_out_group_set(dst, val32);

    of_flow_delete_strict_flags_get(src, &val16);
    of_flow_delete_strict_flags_set(dst, val16);

    of_flow_delete_strict_importance_get(src, &val16);
    of_flow_delete_strict_importance_set(dst, val16);

    of_flow_delete_strict_match_get(src, &match);
    of_flow_delete_strict_match_set(dst, &match);

    of_flow_delete_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_delete_strict_delete(dst);
        return NULL;
    }
    of_flow_delete_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_mod_failed_error_msg_t *
of_flow_mod_failed_error_msg_OF_VERSION_1_4_dup(
    of_flow_mod_failed_error_msg_t *src)
{
    of_flow_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_mod_failed_error_msg_xid_get(src, &val32);
    of_flow_mod_failed_error_msg_xid_set(dst, val32);

    of_flow_mod_failed_error_msg_code_get(src, &val16);
    of_flow_mod_failed_error_msg_code_set(dst, val16);

    of_flow_mod_failed_error_msg_data_get(src, &octets);
    of_flow_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_t *
of_flow_modify_OF_VERSION_1_4_dup(
    of_flow_modify_t *src)
{
    of_flow_modify_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_xid_get(src, &val32);
    of_flow_modify_xid_set(dst, val32);

    of_flow_modify_cookie_get(src, &val64);
    of_flow_modify_cookie_set(dst, val64);

    of_flow_modify_cookie_mask_get(src, &val64);
    of_flow_modify_cookie_mask_set(dst, val64);

    of_flow_modify_table_id_get(src, &val8);
    of_flow_modify_table_id_set(dst, val8);

    of_flow_modify_idle_timeout_get(src, &val16);
    of_flow_modify_idle_timeout_set(dst, val16);

    of_flow_modify_hard_timeout_get(src, &val16);
    of_flow_modify_hard_timeout_set(dst, val16);

    of_flow_modify_priority_get(src, &val16);
    of_flow_modify_priority_set(dst, val16);

    of_flow_modify_buffer_id_get(src, &val32);
    of_flow_modify_buffer_id_set(dst, val32);

    of_flow_modify_out_port_get(src, &port_no);
    of_flow_modify_out_port_set(dst, port_no);

    of_flow_modify_out_group_get(src, &val32);
    of_flow_modify_out_group_set(dst, val32);

    of_flow_modify_flags_get(src, &val16);
    of_flow_modify_flags_set(dst, val16);

    of_flow_modify_importance_get(src, &val16);
    of_flow_modify_importance_set(dst, val16);

    of_flow_modify_match_get(src, &match);
    of_flow_modify_match_set(dst, &match);

    of_flow_modify_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_delete(dst);
        return NULL;
    }
    of_flow_modify_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_modify_strict
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_modify_strict.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_modify_strict_t *
of_flow_modify_strict_OF_VERSION_1_4_dup(
    of_flow_modify_strict_t *src)
{
    of_flow_modify_strict_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint8_t val8;
    uint16_t val16;
    of_port_no_t port_no;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_modify_strict_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_modify_strict_xid_get(src, &val32);
    of_flow_modify_strict_xid_set(dst, val32);

    of_flow_modify_strict_cookie_get(src, &val64);
    of_flow_modify_strict_cookie_set(dst, val64);

    of_flow_modify_strict_cookie_mask_get(src, &val64);
    of_flow_modify_strict_cookie_mask_set(dst, val64);

    of_flow_modify_strict_table_id_get(src, &val8);
    of_flow_modify_strict_table_id_set(dst, val8);

    of_flow_modify_strict_idle_timeout_get(src, &val16);
    of_flow_modify_strict_idle_timeout_set(dst, val16);

    of_flow_modify_strict_hard_timeout_get(src, &val16);
    of_flow_modify_strict_hard_timeout_set(dst, val16);

    of_flow_modify_strict_priority_get(src, &val16);
    of_flow_modify_strict_priority_set(dst, val16);

    of_flow_modify_strict_buffer_id_get(src, &val32);
    of_flow_modify_strict_buffer_id_set(dst, val32);

    of_flow_modify_strict_out_port_get(src, &port_no);
    of_flow_modify_strict_out_port_set(dst, port_no);

    of_flow_modify_strict_out_group_get(src, &val32);
    of_flow_modify_strict_out_group_set(dst, val32);

    of_flow_modify_strict_flags_get(src, &val16);
    of_flow_modify_strict_flags_set(dst, val16);

    of_flow_modify_strict_importance_get(src, &val16);
    of_flow_modify_strict_importance_set(dst, val16);

    of_flow_modify_strict_match_get(src, &match);
    of_flow_modify_strict_match_set(dst, &match);

    of_flow_modify_strict_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_modify_strict_delete(dst);
        return NULL;
    }
    of_flow_modify_strict_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_monitor_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_monitor_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_monitor_failed_error_msg_t *
of_flow_monitor_failed_error_msg_OF_VERSION_1_4_dup(
    of_flow_monitor_failed_error_msg_t *src)
{
    of_flow_monitor_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_flow_monitor_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_monitor_failed_error_msg_xid_get(src, &val32);
    of_flow_monitor_failed_error_msg_xid_set(dst, val32);

    of_flow_monitor_failed_error_msg_code_get(src, &val16);
    of_flow_monitor_failed_error_msg_code_set(dst, val16);

    of_flow_monitor_failed_error_msg_data_get(src, &octets);
    of_flow_monitor_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_flow_removed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_removed.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_removed_t *
of_flow_removed_OF_VERSION_1_4_dup(
    of_flow_removed_t *src)
{
    of_flow_removed_t *dst;
    uint32_t val32;
    uint64_t val64;
    uint16_t val16;
    uint8_t val8;
    of_match_t match;

    if ((dst = of_flow_removed_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_removed_xid_get(src, &val32);
    of_flow_removed_xid_set(dst, val32);

    of_flow_removed_cookie_get(src, &val64);
    of_flow_removed_cookie_set(dst, val64);

    of_flow_removed_priority_get(src, &val16);
    of_flow_removed_priority_set(dst, val16);

    of_flow_removed_reason_get(src, &val8);
    of_flow_removed_reason_set(dst, val8);

    of_flow_removed_table_id_get(src, &val8);
    of_flow_removed_table_id_set(dst, val8);

    of_flow_removed_duration_sec_get(src, &val32);
    of_flow_removed_duration_sec_set(dst, val32);

    of_flow_removed_duration_nsec_get(src, &val32);
    of_flow_removed_duration_nsec_set(dst, val32);

    of_flow_removed_idle_timeout_get(src, &val16);
    of_flow_removed_idle_timeout_set(dst, val16);

    of_flow_removed_hard_timeout_get(src, &val16);
    of_flow_removed_hard_timeout_set(dst, val16);

    of_flow_removed_packet_count_get(src, &val64);
    of_flow_removed_packet_count_set(dst, val64);

    of_flow_removed_byte_count_get(src, &val64);
    of_flow_removed_byte_count_set(dst, val64);

    of_flow_removed_match_get(src, &match);
    of_flow_removed_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_reply_t *
of_flow_stats_reply_OF_VERSION_1_4_dup(
    of_flow_stats_reply_t *src)
{
    of_flow_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_flow_stats_entry_t src_list;
    of_list_flow_stats_entry_t *dst_list;

    if ((dst = of_flow_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_reply_xid_get(src, &val32);
    of_flow_stats_reply_xid_set(dst, val32);

    of_flow_stats_reply_flags_get(src, &val16);
    of_flow_stats_reply_flags_set(dst, val16);

    of_flow_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_flow_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_reply_delete(dst);
        return NULL;
    }
    of_flow_stats_reply_entries_set(dst, dst_list);
    of_list_flow_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_flow_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_request_t *
of_flow_stats_request_OF_VERSION_1_4_dup(
    of_flow_stats_request_t *src)
{
    of_flow_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    of_port_no_t port_no;
    uint64_t val64;
    of_match_t match;

    if ((dst = of_flow_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_request_xid_get(src, &val32);
    of_flow_stats_request_xid_set(dst, val32);

    of_flow_stats_request_flags_get(src, &val16);
    of_flow_stats_request_flags_set(dst, val16);

    of_flow_stats_request_table_id_get(src, &val8);
    of_flow_stats_request_table_id_set(dst, val8);

    of_flow_stats_request_out_port_get(src, &port_no);
    of_flow_stats_request_out_port_set(dst, port_no);

    of_flow_stats_request_out_group_get(src, &val32);
    of_flow_stats_request_out_group_set(dst, val32);

    of_flow_stats_request_cookie_get(src, &val64);
    of_flow_stats_request_cookie_set(dst, val64);

    of_flow_stats_request_cookie_mask_get(src, &val64);
    of_flow_stats_request_cookie_mask_set(dst, val64);

    of_flow_stats_request_match_get(src, &match);
    of_flow_stats_request_match_set(dst, &match);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_reply_t *
of_get_config_reply_OF_VERSION_1_4_dup(
    of_get_config_reply_t *src)
{
    of_get_config_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_get_config_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_reply_xid_get(src, &val32);
    of_get_config_reply_xid_set(dst, val32);

    of_get_config_reply_flags_get(src, &val16);
    of_get_config_reply_flags_set(dst, val16);

    of_get_config_reply_miss_send_len_get(src, &val16);
    of_get_config_reply_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_get_config_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_get_config_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_get_config_request_t *
of_get_config_request_OF_VERSION_1_4_dup(
    of_get_config_request_t *src)
{
    of_get_config_request_t *dst;
    uint32_t val32;

    if ((dst = of_get_config_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_get_config_request_xid_get(src, &val32);
    of_get_config_request_xid_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_add
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_add.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_add_t *
of_group_add_OF_VERSION_1_4_dup(
    of_group_add_t *src)
{
    of_group_add_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_add_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_add_xid_get(src, &val32);
    of_group_add_xid_set(dst, val32);

    of_group_add_group_type_get(src, &val8);
    of_group_add_group_type_set(dst, val8);

    of_group_add_group_id_get(src, &val32);
    of_group_add_group_id_set(dst, val32);

    of_group_add_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_add_delete(dst);
        return NULL;
    }
    of_group_add_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_delete
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_delete.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_delete_t *
of_group_delete_OF_VERSION_1_4_dup(
    of_group_delete_t *src)
{
    of_group_delete_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_delete_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_delete_xid_get(src, &val32);
    of_group_delete_xid_set(dst, val32);

    of_group_delete_group_type_get(src, &val8);
    of_group_delete_group_type_set(dst, val8);

    of_group_delete_group_id_get(src, &val32);
    of_group_delete_group_id_set(dst, val32);

    of_group_delete_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_delete_delete(dst);
        return NULL;
    }
    of_group_delete_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_reply_t *
of_group_desc_stats_reply_OF_VERSION_1_4_dup(
    of_group_desc_stats_reply_t *src)
{
    of_group_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_desc_stats_entry_t src_list;
    of_list_group_desc_stats_entry_t *dst_list;

    if ((dst = of_group_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_reply_xid_get(src, &val32);
    of_group_desc_stats_reply_xid_set(dst, val32);

    of_group_desc_stats_reply_flags_get(src, &val16);
    of_group_desc_stats_reply_flags_set(dst, val16);

    of_group_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_desc_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_group_desc_stats_reply_entries_set(dst, dst_list);
    of_list_group_desc_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_request_t *
of_group_desc_stats_request_OF_VERSION_1_4_dup(
    of_group_desc_stats_request_t *src)
{
    of_group_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_request_xid_get(src, &val32);
    of_group_desc_stats_request_xid_set(dst, val32);

    of_group_desc_stats_request_flags_get(src, &val16);
    of_group_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_reply_t *
of_group_features_stats_reply_OF_VERSION_1_4_dup(
    of_group_features_stats_reply_t *src)
{
    of_group_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_reply_xid_get(src, &val32);
    of_group_features_stats_reply_xid_set(dst, val32);

    of_group_features_stats_reply_flags_get(src, &val16);
    of_group_features_stats_reply_flags_set(dst, val16);

    of_group_features_stats_reply_types_get(src, &val32);
    of_group_features_stats_reply_types_set(dst, val32);

    of_group_features_stats_reply_capabilities_get(src, &val32);
    of_group_features_stats_reply_capabilities_set(dst, val32);

    of_group_features_stats_reply_max_groups_all_get(src, &val32);
    of_group_features_stats_reply_max_groups_all_set(dst, val32);

    of_group_features_stats_reply_max_groups_select_get(src, &val32);
    of_group_features_stats_reply_max_groups_select_set(dst, val32);

    of_group_features_stats_reply_max_groups_indirect_get(src, &val32);
    of_group_features_stats_reply_max_groups_indirect_set(dst, val32);

    of_group_features_stats_reply_max_groups_ff_get(src, &val32);
    of_group_features_stats_reply_max_groups_ff_set(dst, val32);

    of_group_features_stats_reply_actions_all_get(src, &val32);
    of_group_features_stats_reply_actions_all_set(dst, val32);

    of_group_features_stats_reply_actions_select_get(src, &val32);
    of_group_features_stats_reply_actions_select_set(dst, val32);

    of_group_features_stats_reply_actions_indirect_get(src, &val32);
    of_group_features_stats_reply_actions_indirect_set(dst, val32);

    of_group_features_stats_reply_actions_ff_get(src, &val32);
    of_group_features_stats_reply_actions_ff_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_group_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_features_stats_request_t *
of_group_features_stats_request_OF_VERSION_1_4_dup(
    of_group_features_stats_request_t *src)
{
    of_group_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_features_stats_request_xid_get(src, &val32);
    of_group_features_stats_request_xid_set(dst, val32);

    of_group_features_stats_request_flags_get(src, &val16);
    of_group_features_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_group_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_mod_failed_error_msg_t *
of_group_mod_failed_error_msg_OF_VERSION_1_4_dup(
    of_group_mod_failed_error_msg_t *src)
{
    of_group_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_group_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_mod_failed_error_msg_xid_get(src, &val32);
    of_group_mod_failed_error_msg_xid_set(dst, val32);

    of_group_mod_failed_error_msg_code_get(src, &val16);
    of_group_mod_failed_error_msg_code_set(dst, val16);

    of_group_mod_failed_error_msg_data_get(src, &octets);
    of_group_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_group_modify
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_modify.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_modify_t *
of_group_modify_OF_VERSION_1_4_dup(
    of_group_modify_t *src)
{
    of_group_modify_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_modify_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_modify_xid_get(src, &val32);
    of_group_modify_xid_set(dst, val32);

    of_group_modify_group_type_get(src, &val8);
    of_group_modify_group_type_set(dst, val8);

    of_group_modify_group_id_get(src, &val32);
    of_group_modify_group_id_set(dst, val32);

    of_group_modify_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_modify_delete(dst);
        return NULL;
    }
    of_group_modify_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_reply_t *
of_group_stats_reply_OF_VERSION_1_4_dup(
    of_group_stats_reply_t *src)
{
    of_group_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_group_stats_entry_t src_list;
    of_list_group_stats_entry_t *dst_list;

    if ((dst = of_group_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_reply_xid_get(src, &val32);
    of_group_stats_reply_xid_set(dst, val32);

    of_group_stats_reply_flags_get(src, &val16);
    of_group_stats_reply_flags_set(dst, val16);

    of_group_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_group_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_reply_delete(dst);
        return NULL;
    }
    of_group_stats_reply_entries_set(dst, dst_list);
    of_list_group_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_request_t *
of_group_stats_request_OF_VERSION_1_4_dup(
    of_group_stats_request_t *src)
{
    of_group_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_group_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_request_xid_get(src, &val32);
    of_group_stats_request_xid_set(dst, val32);

    of_group_stats_request_flags_get(src, &val16);
    of_group_stats_request_flags_set(dst, val16);

    of_group_stats_request_group_id_get(src, &val32);
    of_group_stats_request_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_header
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_header.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_header_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_hello
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_t *
of_hello_OF_VERSION_1_4_dup(
    of_hello_t *src)
{
    of_hello_t *dst;
    uint32_t val32;

    of_list_hello_elem_t src_list;
    of_list_hello_elem_t *dst_list;

    if ((dst = of_hello_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_xid_get(src, &val32);
    of_hello_xid_set(dst, val32);

    of_hello_elements_bind(
        src, &src_list);
    dst_list = of_list_hello_elem_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_hello_delete(dst);
        return NULL;
    }
    of_hello_elements_set(dst, dst_list);
    of_list_hello_elem_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_hello_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_failed_error_msg_t *
of_hello_failed_error_msg_OF_VERSION_1_4_dup(
    of_hello_failed_error_msg_t *src)
{
    of_hello_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_hello_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_failed_error_msg_xid_get(src, &val32);
    of_hello_failed_error_msg_xid_set(dst, val32);

    of_hello_failed_error_msg_code_get(src, &val16);
    of_hello_failed_error_msg_code_set(dst, val16);

    of_hello_failed_error_msg_data_get(src, &octets);
    of_hello_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_stats_reply_t *
of_meter_config_stats_reply_OF_VERSION_1_4_dup(
    of_meter_config_stats_reply_t *src)
{
    of_meter_config_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_config_t src_list;
    of_list_meter_config_t *dst_list;

    if ((dst = of_meter_config_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_stats_reply_xid_get(src, &val32);
    of_meter_config_stats_reply_xid_set(dst, val32);

    of_meter_config_stats_reply_flags_get(src, &val16);
    of_meter_config_stats_reply_flags_set(dst, val16);

    of_meter_config_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_config_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_config_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_config_stats_reply_entries_set(dst, dst_list);
    of_list_meter_config_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_stats_request_t *
of_meter_config_stats_request_OF_VERSION_1_4_dup(
    of_meter_config_stats_request_t *src)
{
    of_meter_config_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_config_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_stats_request_xid_get(src, &val32);
    of_meter_config_stats_request_xid_set(dst, val32);

    of_meter_config_stats_request_flags_get(src, &val16);
    of_meter_config_stats_request_flags_set(dst, val16);

    of_meter_config_stats_request_meter_id_get(src, &val32);
    of_meter_config_stats_request_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_stats_reply_t *
of_meter_features_stats_reply_OF_VERSION_1_4_dup(
    of_meter_features_stats_reply_t *src)
{
    of_meter_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_meter_features_t src_features;
    of_meter_features_t *dst_features;

    if ((dst = of_meter_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_stats_reply_xid_get(src, &val32);
    of_meter_features_stats_reply_xid_set(dst, val32);

    of_meter_features_stats_reply_flags_get(src, &val16);
    of_meter_features_stats_reply_flags_set(dst, val16);

    of_meter_features_stats_reply_features_bind(
        src, &src_features);
    dst_features = of_meter_features_OF_VERSION_1_4_dup(&src_features);
    if (dst_features == NULL) {
        of_meter_features_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_features_stats_reply_features_set(dst, dst_features);
    of_meter_features_delete(dst_features);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_stats_request_t *
of_meter_features_stats_request_OF_VERSION_1_4_dup(
    of_meter_features_stats_request_t *src)
{
    of_meter_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_stats_request_xid_get(src, &val32);
    of_meter_features_stats_request_xid_set(dst, val32);

    of_meter_features_stats_request_flags_get(src, &val16);
    of_meter_features_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_meter_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_mod_t *
of_meter_mod_OF_VERSION_1_4_dup(
    of_meter_mod_t *src)
{
    of_meter_mod_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_band_t src_list;
    of_list_meter_band_t *dst_list;

    if ((dst = of_meter_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_mod_xid_get(src, &val32);
    of_meter_mod_xid_set(dst, val32);

    of_meter_mod_flags_get(src, &val16);
    of_meter_mod_flags_set(dst, val16);

    of_meter_mod_meter_id_get(src, &val32);
    of_meter_mod_meter_id_set(dst, val32);

    of_meter_mod_bands_bind(
        src, &src_list);
    dst_list = of_list_meter_band_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_mod_delete(dst);
        return NULL;
    }
    of_meter_mod_bands_set(dst, dst_list);
    of_list_meter_band_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_mod_failed_error_msg_t *
of_meter_mod_failed_error_msg_OF_VERSION_1_4_dup(
    of_meter_mod_failed_error_msg_t *src)
{
    of_meter_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_meter_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_mod_failed_error_msg_xid_get(src, &val32);
    of_meter_mod_failed_error_msg_xid_set(dst, val32);

    of_meter_mod_failed_error_msg_code_get(src, &val16);
    of_meter_mod_failed_error_msg_code_set(dst, val16);

    of_meter_mod_failed_error_msg_data_get(src, &octets);
    of_meter_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_reply_t *
of_meter_stats_reply_OF_VERSION_1_4_dup(
    of_meter_stats_reply_t *src)
{
    of_meter_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_meter_stats_t src_list;
    of_list_meter_stats_t *dst_list;

    if ((dst = of_meter_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_reply_xid_get(src, &val32);
    of_meter_stats_reply_xid_set(dst, val32);

    of_meter_stats_reply_flags_get(src, &val16);
    of_meter_stats_reply_flags_set(dst, val16);

    of_meter_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_stats_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_stats_reply_delete(dst);
        return NULL;
    }
    of_meter_stats_reply_entries_set(dst, dst_list);
    of_list_meter_stats_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_request_t *
of_meter_stats_request_OF_VERSION_1_4_dup(
    of_meter_stats_request_t *src)
{
    of_meter_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_meter_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_request_xid_get(src, &val32);
    of_meter_stats_request_xid_set(dst, val32);

    of_meter_stats_request_flags_get(src, &val16);
    of_meter_stats_request_flags_set(dst, val16);

    of_meter_stats_request_meter_id_get(src, &val32);
    of_meter_stats_request_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_packet_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_in_t *
of_packet_in_OF_VERSION_1_4_dup(
    of_packet_in_t *src)
{
    of_packet_in_t *dst;
    uint32_t val32;
    uint16_t val16;
    uint8_t val8;
    uint64_t val64;
    of_match_t match;
    of_octets_t octets;

    if ((dst = of_packet_in_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_in_xid_get(src, &val32);
    of_packet_in_xid_set(dst, val32);

    of_packet_in_buffer_id_get(src, &val32);
    of_packet_in_buffer_id_set(dst, val32);

    of_packet_in_total_len_get(src, &val16);
    of_packet_in_total_len_set(dst, val16);

    of_packet_in_reason_get(src, &val8);
    of_packet_in_reason_set(dst, val8);

    of_packet_in_table_id_get(src, &val8);
    of_packet_in_table_id_set(dst, val8);

    of_packet_in_cookie_get(src, &val64);
    of_packet_in_cookie_set(dst, val64);

    of_packet_in_match_get(src, &match);
    of_packet_in_match_set(dst, &match);

    of_packet_in_data_get(src, &octets);
    of_packet_in_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_packet_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_out_t *
of_packet_out_OF_VERSION_1_4_dup(
    of_packet_out_t *src)
{
    of_packet_out_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_action_t src_list;
    of_list_action_t *dst_list;
    of_octets_t octets;

    if ((dst = of_packet_out_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_out_xid_get(src, &val32);
    of_packet_out_xid_set(dst, val32);

    of_packet_out_buffer_id_get(src, &val32);
    of_packet_out_buffer_id_set(dst, val32);

    of_packet_out_in_port_get(src, &port_no);
    of_packet_out_in_port_set(dst, port_no);

    of_packet_out_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_out_delete(dst);
        return NULL;
    }
    of_packet_out_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    of_packet_out_data_get(src, &octets);
    of_packet_out_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_stats_reply_t *
of_port_desc_stats_reply_OF_VERSION_1_4_dup(
    of_port_desc_stats_reply_t *src)
{
    of_port_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_desc_t src_list;
    of_list_port_desc_t *dst_list;

    if ((dst = of_port_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_stats_reply_xid_get(src, &val32);
    of_port_desc_stats_reply_xid_set(dst, val32);

    of_port_desc_stats_reply_flags_get(src, &val16);
    of_port_desc_stats_reply_flags_set(dst, val16);

    of_port_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_desc_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_port_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_port_desc_stats_reply_entries_set(dst, dst_list);
    of_list_port_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_stats_request_t *
of_port_desc_stats_request_OF_VERSION_1_4_dup(
    of_port_desc_stats_request_t *src)
{
    of_port_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_port_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_stats_request_xid_get(src, &val32);
    of_port_desc_stats_request_xid_set(dst, val32);

    of_port_desc_stats_request_flags_get(src, &val16);
    of_port_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_t *
of_port_mod_OF_VERSION_1_4_dup(
    of_port_mod_t *src)
{
    of_port_mod_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;

    of_list_port_mod_prop_t src_list;
    of_list_port_mod_prop_t *dst_list;

    if ((dst = of_port_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_xid_get(src, &val32);
    of_port_mod_xid_set(dst, val32);

    of_port_mod_port_no_get(src, &port_no);
    of_port_mod_port_no_set(dst, port_no);

    of_port_mod_hw_addr_get(src, &mac_addr);
    of_port_mod_hw_addr_set(dst, mac_addr);

    of_port_mod_config_get(src, &val32);
    of_port_mod_config_set(dst, val32);

    of_port_mod_mask_get(src, &val32);
    of_port_mod_mask_set(dst, val32);

    of_port_mod_properties_bind(
        src, &src_list);
    dst_list = of_list_port_mod_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_port_mod_delete(dst);
        return NULL;
    }
    of_port_mod_properties_set(dst, dst_list);
    of_list_port_mod_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_failed_error_msg_t *
of_port_mod_failed_error_msg_OF_VERSION_1_4_dup(
    of_port_mod_failed_error_msg_t *src)
{
    of_port_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_port_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_failed_error_msg_xid_get(src, &val32);
    of_port_mod_failed_error_msg_xid_set(dst, val32);

    of_port_mod_failed_error_msg_code_get(src, &val16);
    of_port_mod_failed_error_msg_code_set(dst, val16);

    of_port_mod_failed_error_msg_data_get(src, &octets);
    of_port_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_reply_t *
of_port_stats_reply_OF_VERSION_1_4_dup(
    of_port_stats_reply_t *src)
{
    of_port_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_port_stats_entry_t src_list;
    of_list_port_stats_entry_t *dst_list;

    if ((dst = of_port_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_reply_xid_get(src, &val32);
    of_port_stats_reply_xid_set(dst, val32);

    of_port_stats_reply_flags_get(src, &val16);
    of_port_stats_reply_flags_set(dst, val16);

    of_port_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_port_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_reply_delete(dst);
        return NULL;
    }
    of_port_stats_reply_entries_set(dst, dst_list);
    of_list_port_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_request_t *
of_port_stats_request_OF_VERSION_1_4_dup(
    of_port_stats_request_t *src)
{
    of_port_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_port_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_request_xid_get(src, &val32);
    of_port_stats_request_xid_set(dst, val32);

    of_port_stats_request_flags_get(src, &val16);
    of_port_stats_request_flags_set(dst, val16);

    of_port_stats_request_port_no_get(src, &port_no);
    of_port_stats_request_port_no_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_port_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_status_t *
of_port_status_OF_VERSION_1_4_dup(
    of_port_status_t *src)
{
    of_port_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_port_desc_t src_port_desc;
    of_port_desc_t *dst_port_desc;

    if ((dst = of_port_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_status_xid_get(src, &val32);
    of_port_status_xid_set(dst, val32);

    of_port_status_reason_get(src, &val8);
    of_port_status_reason_set(dst, val8);

    of_port_status_desc_bind(
        src, &src_port_desc);
    dst_port_desc = of_port_desc_OF_VERSION_1_4_dup(&src_port_desc);
    if (dst_port_desc == NULL) {
        of_port_status_delete(dst);
        return NULL;
    }
    of_port_status_desc_set(dst, dst_port_desc);
    of_port_desc_delete(dst_port_desc);

    return dst;
}

/**
 * Duplicate an object of type of_queue_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_stats_reply_t *
of_queue_desc_stats_reply_OF_VERSION_1_4_dup(
    of_queue_desc_stats_reply_t *src)
{
    of_queue_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_desc_t src_list;
    of_list_queue_desc_t *dst_list;

    if ((dst = of_queue_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_stats_reply_xid_get(src, &val32);
    of_queue_desc_stats_reply_xid_set(dst, val32);

    of_queue_desc_stats_reply_flags_get(src, &val16);
    of_queue_desc_stats_reply_flags_set(dst, val16);

    of_queue_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_desc_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_desc_stats_reply_entries_set(dst, dst_list);
    of_list_queue_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_stats_request_t *
of_queue_desc_stats_request_OF_VERSION_1_4_dup(
    of_queue_desc_stats_request_t *src)
{
    of_queue_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_stats_request_xid_get(src, &val32);
    of_queue_desc_stats_request_xid_set(dst, val32);

    of_queue_desc_stats_request_flags_get(src, &val16);
    of_queue_desc_stats_request_flags_set(dst, val16);

    of_queue_desc_stats_request_port_no_get(src, &port_no);
    of_queue_desc_stats_request_port_no_set(dst, port_no);

    of_queue_desc_stats_request_queue_id_get(src, &val32);
    of_queue_desc_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_queue_op_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_op_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_op_failed_error_msg_t *
of_queue_op_failed_error_msg_OF_VERSION_1_4_dup(
    of_queue_op_failed_error_msg_t *src)
{
    of_queue_op_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_queue_op_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_op_failed_error_msg_xid_get(src, &val32);
    of_queue_op_failed_error_msg_xid_set(dst, val32);

    of_queue_op_failed_error_msg_code_get(src, &val16);
    of_queue_op_failed_error_msg_code_set(dst, val16);

    of_queue_op_failed_error_msg_data_get(src, &octets);
    of_queue_op_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_reply_t *
of_queue_stats_reply_OF_VERSION_1_4_dup(
    of_queue_stats_reply_t *src)
{
    of_queue_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_queue_stats_entry_t src_list;
    of_list_queue_stats_entry_t *dst_list;

    if ((dst = of_queue_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_reply_xid_get(src, &val32);
    of_queue_stats_reply_xid_set(dst, val32);

    of_queue_stats_reply_flags_get(src, &val16);
    of_queue_stats_reply_flags_set(dst, val16);

    of_queue_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_reply_delete(dst);
        return NULL;
    }
    of_queue_stats_reply_entries_set(dst, dst_list);
    of_list_queue_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_request_t *
of_queue_stats_request_OF_VERSION_1_4_dup(
    of_queue_stats_request_t *src)
{
    of_queue_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_no_t port_no;

    if ((dst = of_queue_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_request_xid_get(src, &val32);
    of_queue_stats_request_xid_set(dst, val32);

    of_queue_stats_request_flags_get(src, &val16);
    of_queue_stats_request_flags_set(dst, val16);

    of_queue_stats_request_port_no_get(src, &port_no);
    of_queue_stats_request_port_no_set(dst, port_no);

    of_queue_stats_request_queue_id_get(src, &val32);
    of_queue_stats_request_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_requestforward
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_requestforward.
 *
 * The caller is responsible for deleting the returned value
 */
of_requestforward_t *
of_requestforward_OF_VERSION_1_4_dup(
    of_requestforward_t *src)
{
    of_requestforward_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_requestforward_new(src->version)) == NULL) {
        return NULL;
    }

    of_requestforward_xid_get(src, &val32);
    of_requestforward_xid_set(dst, val32);

    of_requestforward_role_get(src, &val32);
    of_requestforward_role_set(dst, val32);

    of_requestforward_data_get(src, &octets);
    of_requestforward_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_role_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_reply_t *
of_role_reply_OF_VERSION_1_4_dup(
    of_role_reply_t *src)
{
    of_role_reply_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_reply_xid_get(src, &val32);
    of_role_reply_xid_set(dst, val32);

    of_role_reply_role_get(src, &val32);
    of_role_reply_role_set(dst, val32);

    of_role_reply_generation_id_get(src, &val64);
    of_role_reply_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_t *
of_role_request_OF_VERSION_1_4_dup(
    of_role_request_t *src)
{
    of_role_request_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_role_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_xid_get(src, &val32);
    of_role_request_xid_set(dst, val32);

    of_role_request_role_get(src, &val32);
    of_role_request_role_set(dst, val32);

    of_role_request_generation_id_get(src, &val64);
    of_role_request_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_role_request_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_request_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_request_failed_error_msg_t *
of_role_request_failed_error_msg_OF_VERSION_1_4_dup(
    of_role_request_failed_error_msg_t *src)
{
    of_role_request_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_role_request_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_request_failed_error_msg_xid_get(src, &val32);
    of_role_request_failed_error_msg_xid_set(dst, val32);

    of_role_request_failed_error_msg_code_get(src, &val16);
    of_role_request_failed_error_msg_code_set(dst, val16);

    of_role_request_failed_error_msg_data_get(src, &octets);
    of_role_request_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_role_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_role_status_t *
of_role_status_OF_VERSION_1_4_dup(
    of_role_status_t *src)
{
    of_role_status_t *dst;
    uint32_t val32;
    uint8_t val8;
    uint64_t val64;

    of_list_role_prop_t src_list;
    of_list_role_prop_t *dst_list;

    if ((dst = of_role_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_role_status_xid_get(src, &val32);
    of_role_status_xid_set(dst, val32);

    of_role_status_role_get(src, &val32);
    of_role_status_role_set(dst, val32);

    of_role_status_reason_get(src, &val8);
    of_role_status_reason_set(dst, val8);

    of_role_status_generation_id_get(src, &val64);
    of_role_status_generation_id_set(dst, val64);

    of_role_status_properties_bind(
        src, &src_list);
    dst_list = of_list_role_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_role_status_delete(dst);
        return NULL;
    }
    of_role_status_properties_set(dst, dst_list);
    of_list_role_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_set_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_set_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_set_config_t *
of_set_config_OF_VERSION_1_4_dup(
    of_set_config_t *src)
{
    of_set_config_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_set_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_set_config_xid_get(src, &val32);
    of_set_config_xid_set(dst, val32);

    of_set_config_flags_get(src, &val16);
    of_set_config_flags_set(dst, val16);

    of_set_config_miss_send_len_get(src, &val16);
    of_set_config_miss_send_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_switch_config_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_switch_config_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_switch_config_failed_error_msg_t *
of_switch_config_failed_error_msg_OF_VERSION_1_4_dup(
    of_switch_config_failed_error_msg_t *src)
{
    of_switch_config_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_switch_config_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_switch_config_failed_error_msg_xid_get(src, &val32);
    of_switch_config_failed_error_msg_xid_set(dst, val32);

    of_switch_config_failed_error_msg_code_get(src, &val16);
    of_switch_config_failed_error_msg_code_set(dst, val16);

    of_switch_config_failed_error_msg_data_get(src, &octets);
    of_switch_config_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_desc_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_desc_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_desc_stats_reply_t *
of_table_desc_stats_reply_OF_VERSION_1_4_dup(
    of_table_desc_stats_reply_t *src)
{
    of_table_desc_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_desc_t src_list;
    of_list_table_desc_t *dst_list;

    if ((dst = of_table_desc_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_desc_stats_reply_xid_get(src, &val32);
    of_table_desc_stats_reply_xid_set(dst, val32);

    of_table_desc_stats_reply_flags_get(src, &val16);
    of_table_desc_stats_reply_flags_set(dst, val16);

    of_table_desc_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_desc_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_desc_stats_reply_delete(dst);
        return NULL;
    }
    of_table_desc_stats_reply_entries_set(dst, dst_list);
    of_list_table_desc_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_desc_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_desc_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_desc_stats_request_t *
of_table_desc_stats_request_OF_VERSION_1_4_dup(
    of_table_desc_stats_request_t *src)
{
    of_table_desc_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_desc_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_desc_stats_request_xid_get(src, &val32);
    of_table_desc_stats_request_xid_set(dst, val32);

    of_table_desc_stats_request_flags_get(src, &val16);
    of_table_desc_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_failed_error_msg_t *
of_table_features_failed_error_msg_OF_VERSION_1_4_dup(
    of_table_features_failed_error_msg_t *src)
{
    of_table_features_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_features_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_failed_error_msg_xid_get(src, &val32);
    of_table_features_failed_error_msg_xid_set(dst, val32);

    of_table_features_failed_error_msg_code_get(src, &val16);
    of_table_features_failed_error_msg_code_set(dst, val16);

    of_table_features_failed_error_msg_data_get(src, &octets);
    of_table_features_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_stats_reply_t *
of_table_features_stats_reply_OF_VERSION_1_4_dup(
    of_table_features_stats_reply_t *src)
{
    of_table_features_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_features_t src_list;
    of_list_table_features_t *dst_list;

    if ((dst = of_table_features_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_stats_reply_xid_get(src, &val32);
    of_table_features_stats_reply_xid_set(dst, val32);

    of_table_features_stats_reply_flags_get(src, &val16);
    of_table_features_stats_reply_flags_set(dst, val16);

    of_table_features_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_features_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_stats_reply_delete(dst);
        return NULL;
    }
    of_table_features_stats_reply_entries_set(dst, dst_list);
    of_list_table_features_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_features_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_stats_request_t *
of_table_features_stats_request_OF_VERSION_1_4_dup(
    of_table_features_stats_request_t *src)
{
    of_table_features_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_features_t src_list;
    of_list_table_features_t *dst_list;

    if ((dst = of_table_features_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_stats_request_xid_get(src, &val32);
    of_table_features_stats_request_xid_set(dst, val32);

    of_table_features_stats_request_flags_get(src, &val16);
    of_table_features_stats_request_flags_set(dst, val16);

    of_table_features_stats_request_entries_bind(
        src, &src_list);
    dst_list = of_list_table_features_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_stats_request_delete(dst);
        return NULL;
    }
    of_table_features_stats_request_entries_set(dst, dst_list);
    of_list_table_features_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_t *
of_table_mod_OF_VERSION_1_4_dup(
    of_table_mod_t *src)
{
    of_table_mod_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_list_table_mod_prop_t src_list;
    of_list_table_mod_prop_t *dst_list;

    if ((dst = of_table_mod_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_xid_get(src, &val32);
    of_table_mod_xid_set(dst, val32);

    of_table_mod_table_id_get(src, &val8);
    of_table_mod_table_id_set(dst, val8);

    of_table_mod_config_get(src, &val32);
    of_table_mod_config_set(dst, val32);

    of_table_mod_properties_bind(
        src, &src_list);
    dst_list = of_list_table_mod_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_mod_delete(dst);
        return NULL;
    }
    of_table_mod_properties_set(dst, dst_list);
    of_list_table_mod_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_mod_failed_error_msg
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_failed_error_msg.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_failed_error_msg_t *
of_table_mod_failed_error_msg_OF_VERSION_1_4_dup(
    of_table_mod_failed_error_msg_t *src)
{
    of_table_mod_failed_error_msg_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_octets_t octets;

    if ((dst = of_table_mod_failed_error_msg_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_failed_error_msg_xid_get(src, &val32);
    of_table_mod_failed_error_msg_xid_set(dst, val32);

    of_table_mod_failed_error_msg_code_get(src, &val16);
    of_table_mod_failed_error_msg_code_set(dst, val16);

    of_table_mod_failed_error_msg_data_get(src, &octets);
    of_table_mod_failed_error_msg_data_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_reply
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_reply.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_reply_t *
of_table_stats_reply_OF_VERSION_1_4_dup(
    of_table_stats_reply_t *src)
{
    of_table_stats_reply_t *dst;
    uint32_t val32;
    uint16_t val16;

    of_list_table_stats_entry_t src_list;
    of_list_table_stats_entry_t *dst_list;

    if ((dst = of_table_stats_reply_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_reply_xid_get(src, &val32);
    of_table_stats_reply_xid_set(dst, val32);

    of_table_stats_reply_flags_get(src, &val16);
    of_table_stats_reply_flags_set(dst, val16);

    of_table_stats_reply_entries_bind(
        src, &src_list);
    dst_list = of_list_table_stats_entry_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_stats_reply_delete(dst);
        return NULL;
    }
    of_table_stats_reply_entries_set(dst, dst_list);
    of_list_table_stats_entry_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_request
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_request.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_request_t *
of_table_stats_request_OF_VERSION_1_4_dup(
    of_table_stats_request_t *src)
{
    of_table_stats_request_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_table_stats_request_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_request_xid_get(src, &val32);
    of_table_stats_request_xid_set(dst, val32);

    of_table_stats_request_flags_get(src, &val16);
    of_table_stats_request_flags_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_table_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_status_t *
of_table_status_OF_VERSION_1_4_dup(
    of_table_status_t *src)
{
    of_table_status_t *dst;
    uint32_t val32;
    uint8_t val8;

    of_table_desc_t src_table_desc;
    of_table_desc_t *dst_table_desc;

    if ((dst = of_table_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_status_xid_get(src, &val32);
    of_table_status_xid_set(dst, val32);

    of_table_status_role_get(src, &val32);
    of_table_status_role_set(dst, val32);

    of_table_status_reason_get(src, &val8);
    of_table_status_reason_set(dst, val8);

    of_table_status_table_bind(
        src, &src_table_desc);
    dst_table_desc = of_table_desc_OF_VERSION_1_4_dup(&src_table_desc);
    if (dst_table_desc == NULL) {
        of_table_status_delete(dst);
        return NULL;
    }
    of_table_status_table_set(dst, dst_table_desc);
    of_table_desc_delete(dst_table_desc);

    return dst;
}

/**
 * Duplicate a super class object of type of_action
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_NICIRA_DEC_TTL) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_PBB) {
        return of_action_pop_pbb_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_MPLS) {
        return of_action_pop_mpls_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_IN) {
        return of_action_copy_ttl_in_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_OUTPUT) {
        return of_action_output_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_FIELD) {
        return of_action_set_field_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_QUEUE) {
        return of_action_set_queue_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_NW_TTL) {
        return of_action_dec_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_POP_VLAN) {
        return of_action_pop_vlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_VLAN) {
        return of_action_push_vlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_DEC_MPLS_TTL) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_SET_TUNNEL_DST) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_NW_TTL) {
        return of_action_set_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_GENTABLE) {
        return of_action_bsn_gentable_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_MIRROR) {
        return of_action_bsn_mirror_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_GROUP) {
        return of_action_group_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_MPLS) {
        return of_action_push_mpls_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_COPY_TTL_OUT) {
        return of_action_copy_ttl_out_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_BSN_CHECKSUM) {
        return of_action_bsn_checksum_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_PUSH_PBB) {
        return of_action_push_pbb_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_SET_MPLS_TTL) {
        return of_action_set_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_checksum_t *
of_action_bsn_checksum_OF_VERSION_1_4_dup(
    of_action_bsn_checksum_t *src)
{
    of_action_bsn_checksum_t *dst;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_action_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_checksum_experimenter_get(src, &val32);
    of_action_bsn_checksum_experimenter_set(dst, val32);

    of_action_bsn_checksum_subtype_get(src, &val32);
    of_action_bsn_checksum_subtype_set(dst, val32);

    of_action_bsn_checksum_checksum_get(src, &checksum_128);
    of_action_bsn_checksum_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_gentable
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_gentable.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_gentable_t *
of_action_bsn_gentable_OF_VERSION_1_4_dup(
    of_action_bsn_gentable_t *src)
{
    of_action_bsn_gentable_t *dst;
    uint32_t val32;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_action_bsn_gentable_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_gentable_experimenter_get(src, &val32);
    of_action_bsn_gentable_experimenter_set(dst, val32);

    of_action_bsn_gentable_subtype_get(src, &val32);
    of_action_bsn_gentable_subtype_set(dst, val32);

    of_action_bsn_gentable_table_id_get(src, &val32);
    of_action_bsn_gentable_table_id_set(dst, val32);

    of_action_bsn_gentable_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_action_bsn_gentable_delete(dst);
        return NULL;
    }
    of_action_bsn_gentable_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_mirror_t *
of_action_bsn_mirror_OF_VERSION_1_4_dup(
    of_action_bsn_mirror_t *src)
{
    of_action_bsn_mirror_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_action_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_mirror_experimenter_get(src, &val32);
    of_action_bsn_mirror_experimenter_set(dst, val32);

    of_action_bsn_mirror_subtype_get(src, &val32);
    of_action_bsn_mirror_subtype_set(dst, val32);

    of_action_bsn_mirror_dest_port_get(src, &val32);
    of_action_bsn_mirror_dest_port_set(dst, val32);

    of_action_bsn_mirror_vlan_tag_get(src, &val32);
    of_action_bsn_mirror_vlan_tag_set(dst, val32);

    of_action_bsn_mirror_copy_stage_get(src, &val8);
    of_action_bsn_mirror_copy_stage_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_bsn_set_tunnel_dst_t *
of_action_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(
    of_action_bsn_set_tunnel_dst_t *src)
{
    of_action_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_bsn_set_tunnel_dst_subtype_set(dst, val32);

    of_action_bsn_set_tunnel_dst_dst_get(src, &val32);
    of_action_bsn_set_tunnel_dst_dst_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_in_t *
of_action_copy_ttl_in_OF_VERSION_1_4_dup(
    of_action_copy_ttl_in_t *src)
{
    of_action_copy_ttl_in_t *dst;

    if ((dst = of_action_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_copy_ttl_out_t *
of_action_copy_ttl_out_OF_VERSION_1_4_dup(
    of_action_copy_ttl_out_t *src)
{
    of_action_copy_ttl_out_t *dst;

    if ((dst = of_action_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_mpls_ttl_t *
of_action_dec_mpls_ttl_OF_VERSION_1_4_dup(
    of_action_dec_mpls_ttl_t *src)
{
    of_action_dec_mpls_ttl_t *dst;

    if ((dst = of_action_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_dec_nw_ttl_t *
of_action_dec_nw_ttl_OF_VERSION_1_4_dup(
    of_action_dec_nw_ttl_t *src)
{
    of_action_dec_nw_ttl_t *dst;

    if ((dst = of_action_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_group_t *
of_action_group_OF_VERSION_1_4_dup(
    of_action_group_t *src)
{
    of_action_group_t *dst;
    uint32_t val32;

    if ((dst = of_action_group_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_group_group_id_get(src, &val32);
    of_action_group_group_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_action_id
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_action_id_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ACTION_ID_BSN_SET_TUNNEL_DST) {
        return of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_CHECKSUM) {
        return of_action_id_bsn_checksum_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_GROUP) {
        return of_action_id_group_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_VLAN) {
        return of_action_id_push_vlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_PBB) {
        return of_action_id_pop_pbb_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_MIRROR) {
        return of_action_id_bsn_mirror_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_MPLS) {
        return of_action_id_push_mpls_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_DEC_MPLS_TTL) {
        return of_action_id_dec_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_FIELD) {
        return of_action_id_set_field_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_OUTPUT) {
        return of_action_id_output_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_NICIRA_DEC_TTL) {
        return of_action_id_nicira_dec_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_COPY_TTL_OUT) {
        return of_action_id_copy_ttl_out_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_QUEUE) {
        return of_action_id_set_queue_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_COPY_TTL_IN) {
        return of_action_id_copy_ttl_in_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_BSN_GENTABLE) {
        return of_action_id_bsn_gentable_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_MPLS) {
        return of_action_id_pop_mpls_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_PUSH_PBB) {
        return of_action_id_push_pbb_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_POP_VLAN) {
        return of_action_id_pop_vlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_MPLS_TTL) {
        return of_action_id_set_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_DEC_NW_TTL) {
        return of_action_id_dec_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ACTION_ID_SET_NW_TTL) {
        return of_action_id_set_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_action_id_bsn_checksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_checksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_checksum_t *
of_action_id_bsn_checksum_OF_VERSION_1_4_dup(
    of_action_id_bsn_checksum_t *src)
{
    of_action_id_bsn_checksum_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_checksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_checksum_experimenter_get(src, &val32);
    of_action_id_bsn_checksum_experimenter_set(dst, val32);

    of_action_id_bsn_checksum_subtype_get(src, &val32);
    of_action_id_bsn_checksum_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_gentable
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_gentable.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_gentable_t *
of_action_id_bsn_gentable_OF_VERSION_1_4_dup(
    of_action_id_bsn_gentable_t *src)
{
    of_action_id_bsn_gentable_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_gentable_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_gentable_experimenter_get(src, &val32);
    of_action_id_bsn_gentable_experimenter_set(dst, val32);

    of_action_id_bsn_gentable_subtype_get(src, &val32);
    of_action_id_bsn_gentable_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_mirror
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_mirror.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_mirror_t *
of_action_id_bsn_mirror_OF_VERSION_1_4_dup(
    of_action_id_bsn_mirror_t *src)
{
    of_action_id_bsn_mirror_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_mirror_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_mirror_experimenter_get(src, &val32);
    of_action_id_bsn_mirror_experimenter_set(dst, val32);

    of_action_id_bsn_mirror_subtype_get(src, &val32);
    of_action_id_bsn_mirror_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_bsn_set_tunnel_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_bsn_set_tunnel_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_bsn_set_tunnel_dst_t *
of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(
    of_action_id_bsn_set_tunnel_dst_t *src)
{
    of_action_id_bsn_set_tunnel_dst_t *dst;
    uint32_t val32;

    if ((dst = of_action_id_bsn_set_tunnel_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_bsn_set_tunnel_dst_experimenter_get(src, &val32);
    of_action_id_bsn_set_tunnel_dst_experimenter_set(dst, val32);

    of_action_id_bsn_set_tunnel_dst_subtype_get(src, &val32);
    of_action_id_bsn_set_tunnel_dst_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_copy_ttl_in
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_copy_ttl_in.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_copy_ttl_in_t *
of_action_id_copy_ttl_in_OF_VERSION_1_4_dup(
    of_action_id_copy_ttl_in_t *src)
{
    of_action_id_copy_ttl_in_t *dst;

    if ((dst = of_action_id_copy_ttl_in_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_copy_ttl_out
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_copy_ttl_out.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_copy_ttl_out_t *
of_action_id_copy_ttl_out_OF_VERSION_1_4_dup(
    of_action_id_copy_ttl_out_t *src)
{
    of_action_id_copy_ttl_out_t *dst;

    if ((dst = of_action_id_copy_ttl_out_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_dec_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_dec_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_dec_mpls_ttl_t *
of_action_id_dec_mpls_ttl_OF_VERSION_1_4_dup(
    of_action_id_dec_mpls_ttl_t *src)
{
    of_action_id_dec_mpls_ttl_t *dst;

    if ((dst = of_action_id_dec_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_dec_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_dec_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_dec_nw_ttl_t *
of_action_id_dec_nw_ttl_OF_VERSION_1_4_dup(
    of_action_id_dec_nw_ttl_t *src)
{
    of_action_id_dec_nw_ttl_t *dst;

    if ((dst = of_action_id_dec_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_group
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_group.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_group_t *
of_action_id_group_OF_VERSION_1_4_dup(
    of_action_id_group_t *src)
{
    of_action_id_group_t *dst;

    if ((dst = of_action_id_group_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_nicira_dec_ttl_t *
of_action_id_nicira_dec_ttl_OF_VERSION_1_4_dup(
    of_action_id_nicira_dec_ttl_t *src)
{
    of_action_id_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_id_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_id_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_id_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_id_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_id_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_id_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_output_t *
of_action_id_output_OF_VERSION_1_4_dup(
    of_action_id_output_t *src)
{
    of_action_id_output_t *dst;

    if ((dst = of_action_id_output_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_mpls_t *
of_action_id_pop_mpls_OF_VERSION_1_4_dup(
    of_action_id_pop_mpls_t *src)
{
    of_action_id_pop_mpls_t *dst;

    if ((dst = of_action_id_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_pbb_t *
of_action_id_pop_pbb_OF_VERSION_1_4_dup(
    of_action_id_pop_pbb_t *src)
{
    of_action_id_pop_pbb_t *dst;

    if ((dst = of_action_id_pop_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_pop_vlan_t *
of_action_id_pop_vlan_OF_VERSION_1_4_dup(
    of_action_id_pop_vlan_t *src)
{
    of_action_id_pop_vlan_t *dst;

    if ((dst = of_action_id_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_mpls_t *
of_action_id_push_mpls_OF_VERSION_1_4_dup(
    of_action_id_push_mpls_t *src)
{
    of_action_id_push_mpls_t *dst;

    if ((dst = of_action_id_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_pbb_t *
of_action_id_push_pbb_OF_VERSION_1_4_dup(
    of_action_id_push_pbb_t *src)
{
    of_action_id_push_pbb_t *dst;

    if ((dst = of_action_id_push_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_push_vlan_t *
of_action_id_push_vlan_OF_VERSION_1_4_dup(
    of_action_id_push_vlan_t *src)
{
    of_action_id_push_vlan_t *dst;

    if ((dst = of_action_id_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_field_t *
of_action_id_set_field_OF_VERSION_1_4_dup(
    of_action_id_set_field_t *src)
{
    of_action_id_set_field_t *dst;

    if ((dst = of_action_id_set_field_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_mpls_ttl_t *
of_action_id_set_mpls_ttl_OF_VERSION_1_4_dup(
    of_action_id_set_mpls_ttl_t *src)
{
    of_action_id_set_mpls_ttl_t *dst;

    if ((dst = of_action_id_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_nw_ttl_t *
of_action_id_set_nw_ttl_OF_VERSION_1_4_dup(
    of_action_id_set_nw_ttl_t *src)
{
    of_action_id_set_nw_ttl_t *dst;

    if ((dst = of_action_id_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_id_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_id_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_id_set_queue_t *
of_action_id_set_queue_OF_VERSION_1_4_dup(
    of_action_id_set_queue_t *src)
{
    of_action_id_set_queue_t *dst;

    if ((dst = of_action_id_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_nicira_dec_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_nicira_dec_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_nicira_dec_ttl_t *
of_action_nicira_dec_ttl_OF_VERSION_1_4_dup(
    of_action_nicira_dec_ttl_t *src)
{
    of_action_nicira_dec_ttl_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_action_nicira_dec_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_nicira_dec_ttl_experimenter_get(src, &val32);
    of_action_nicira_dec_ttl_experimenter_set(dst, val32);

    of_action_nicira_dec_ttl_subtype_get(src, &val16);
    of_action_nicira_dec_ttl_subtype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_output
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_output.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_output_t *
of_action_output_OF_VERSION_1_4_dup(
    of_action_output_t *src)
{
    of_action_output_t *dst;
    of_port_no_t port_no;
    uint16_t val16;

    if ((dst = of_action_output_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_output_port_get(src, &port_no);
    of_action_output_port_set(dst, port_no);

    of_action_output_max_len_get(src, &val16);
    of_action_output_max_len_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_mpls_t *
of_action_pop_mpls_OF_VERSION_1_4_dup(
    of_action_pop_mpls_t *src)
{
    of_action_pop_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_pop_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_pop_mpls_ethertype_get(src, &val16);
    of_action_pop_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_pbb_t *
of_action_pop_pbb_OF_VERSION_1_4_dup(
    of_action_pop_pbb_t *src)
{
    of_action_pop_pbb_t *dst;

    if ((dst = of_action_pop_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_pop_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_pop_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_pop_vlan_t *
of_action_pop_vlan_OF_VERSION_1_4_dup(
    of_action_pop_vlan_t *src)
{
    of_action_pop_vlan_t *dst;

    if ((dst = of_action_pop_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_action_push_mpls
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_mpls.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_mpls_t *
of_action_push_mpls_OF_VERSION_1_4_dup(
    of_action_push_mpls_t *src)
{
    of_action_push_mpls_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_mpls_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_mpls_ethertype_get(src, &val16);
    of_action_push_mpls_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_pbb
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_pbb.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_pbb_t *
of_action_push_pbb_OF_VERSION_1_4_dup(
    of_action_push_pbb_t *src)
{
    of_action_push_pbb_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_pbb_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_pbb_ethertype_get(src, &val16);
    of_action_push_pbb_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_push_vlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_push_vlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_push_vlan_t *
of_action_push_vlan_OF_VERSION_1_4_dup(
    of_action_push_vlan_t *src)
{
    of_action_push_vlan_t *dst;
    uint16_t val16;

    if ((dst = of_action_push_vlan_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_push_vlan_ethertype_get(src, &val16);
    of_action_push_vlan_ethertype_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_field_t *
of_action_set_field_OF_VERSION_1_4_dup(
    of_action_set_field_t *src)
{
    of_action_set_field_t *dst;

    of_oxm_eth_type_t src_oxm;
    of_oxm_eth_type_t *dst_oxm;

    if ((dst = of_action_set_field_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_field_field_bind(
        src, &src_oxm);
    dst_oxm = of_oxm_eth_type_OF_VERSION_1_4_dup(&src_oxm);
    if (dst_oxm == NULL) {
        of_action_set_field_delete(dst);
        return NULL;
    }
    of_action_set_field_field_set(dst, dst_oxm);
    of_oxm_eth_type_delete(dst_oxm);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_mpls_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_mpls_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_mpls_ttl_t *
of_action_set_mpls_ttl_OF_VERSION_1_4_dup(
    of_action_set_mpls_ttl_t *src)
{
    of_action_set_mpls_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_mpls_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_mpls_ttl_mpls_ttl_get(src, &val8);
    of_action_set_mpls_ttl_mpls_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_nw_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_nw_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_nw_ttl_t *
of_action_set_nw_ttl_OF_VERSION_1_4_dup(
    of_action_set_nw_ttl_t *src)
{
    of_action_set_nw_ttl_t *dst;
    uint8_t val8;

    if ((dst = of_action_set_nw_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_nw_ttl_nw_ttl_get(src, &val8);
    of_action_set_nw_ttl_nw_ttl_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_action_set_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_action_set_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_action_set_queue_t *
of_action_set_queue_OF_VERSION_1_4_dup(
    of_action_set_queue_t *src)
{
    of_action_set_queue_t *dst;
    uint32_t val32;

    if ((dst = of_action_set_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_action_set_queue_queue_id_get(src, &val32);
    of_action_set_queue_queue_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_async_config_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_async_config_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_ASYNC_CONFIG_PROP_ROLE_STATUS_SLAVE) {
        return of_async_config_prop_role_status_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_PACKET_IN_SLAVE) {
        return of_async_config_prop_packet_in_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_REQUESTFORWARD_SLAVE) {
        return of_async_config_prop_requestforward_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_PACKET_IN_MASTER) {
        return of_async_config_prop_packet_in_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_FLOW_REMOVED_MASTER) {
        return of_async_config_prop_flow_removed_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_EXPERIMENTER_SLAVE) {
        return of_async_config_prop_experimenter_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_EXPERIMENTER_MASTER) {
        return of_async_config_prop_experimenter_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_TABLE_STATUS_SLAVE) {
        return of_async_config_prop_table_status_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_PORT_STATUS_MASTER) {
        return of_async_config_prop_port_status_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_ROLE_STATUS_MASTER) {
        return of_async_config_prop_role_status_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_PORT_STATUS_SLAVE) {
        return of_async_config_prop_port_status_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_TABLE_STATUS_MASTER) {
        return of_async_config_prop_table_status_master_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_FLOW_REMOVED_SLAVE) {
        return of_async_config_prop_flow_removed_slave_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_ASYNC_CONFIG_PROP_REQUESTFORWARD_MASTER) {
        return of_async_config_prop_requestforward_master_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_async_config_prop_experimenter_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_experimenter_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_experimenter_master_t *
of_async_config_prop_experimenter_master_OF_VERSION_1_4_dup(
    of_async_config_prop_experimenter_master_t *src)
{
    of_async_config_prop_experimenter_master_t *dst;

    if ((dst = of_async_config_prop_experimenter_master_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_experimenter_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_experimenter_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_experimenter_slave_t *
of_async_config_prop_experimenter_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_experimenter_slave_t *src)
{
    of_async_config_prop_experimenter_slave_t *dst;

    if ((dst = of_async_config_prop_experimenter_slave_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_flow_removed_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_flow_removed_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_flow_removed_master_t *
of_async_config_prop_flow_removed_master_OF_VERSION_1_4_dup(
    of_async_config_prop_flow_removed_master_t *src)
{
    of_async_config_prop_flow_removed_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_flow_removed_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_flow_removed_master_mask_get(src, &val32);
    of_async_config_prop_flow_removed_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_flow_removed_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_flow_removed_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_flow_removed_slave_t *
of_async_config_prop_flow_removed_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_flow_removed_slave_t *src)
{
    of_async_config_prop_flow_removed_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_flow_removed_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_flow_removed_slave_mask_get(src, &val32);
    of_async_config_prop_flow_removed_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_packet_in_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_packet_in_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_packet_in_master_t *
of_async_config_prop_packet_in_master_OF_VERSION_1_4_dup(
    of_async_config_prop_packet_in_master_t *src)
{
    of_async_config_prop_packet_in_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_packet_in_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_packet_in_master_mask_get(src, &val32);
    of_async_config_prop_packet_in_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_packet_in_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_packet_in_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_packet_in_slave_t *
of_async_config_prop_packet_in_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_packet_in_slave_t *src)
{
    of_async_config_prop_packet_in_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_packet_in_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_packet_in_slave_mask_get(src, &val32);
    of_async_config_prop_packet_in_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_port_status_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_port_status_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_port_status_master_t *
of_async_config_prop_port_status_master_OF_VERSION_1_4_dup(
    of_async_config_prop_port_status_master_t *src)
{
    of_async_config_prop_port_status_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_port_status_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_port_status_master_mask_get(src, &val32);
    of_async_config_prop_port_status_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_port_status_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_port_status_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_port_status_slave_t *
of_async_config_prop_port_status_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_port_status_slave_t *src)
{
    of_async_config_prop_port_status_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_port_status_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_port_status_slave_mask_get(src, &val32);
    of_async_config_prop_port_status_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_requestforward_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_requestforward_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_requestforward_master_t *
of_async_config_prop_requestforward_master_OF_VERSION_1_4_dup(
    of_async_config_prop_requestforward_master_t *src)
{
    of_async_config_prop_requestforward_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_requestforward_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_requestforward_master_mask_get(src, &val32);
    of_async_config_prop_requestforward_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_requestforward_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_requestforward_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_requestforward_slave_t *
of_async_config_prop_requestforward_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_requestforward_slave_t *src)
{
    of_async_config_prop_requestforward_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_requestforward_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_requestforward_slave_mask_get(src, &val32);
    of_async_config_prop_requestforward_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_role_status_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_role_status_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_role_status_master_t *
of_async_config_prop_role_status_master_OF_VERSION_1_4_dup(
    of_async_config_prop_role_status_master_t *src)
{
    of_async_config_prop_role_status_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_role_status_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_role_status_master_mask_get(src, &val32);
    of_async_config_prop_role_status_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_role_status_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_role_status_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_role_status_slave_t *
of_async_config_prop_role_status_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_role_status_slave_t *src)
{
    of_async_config_prop_role_status_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_role_status_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_role_status_slave_mask_get(src, &val32);
    of_async_config_prop_role_status_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_table_status_master
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_table_status_master.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_table_status_master_t *
of_async_config_prop_table_status_master_OF_VERSION_1_4_dup(
    of_async_config_prop_table_status_master_t *src)
{
    of_async_config_prop_table_status_master_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_table_status_master_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_table_status_master_mask_get(src, &val32);
    of_async_config_prop_table_status_master_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_async_config_prop_table_status_slave
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_async_config_prop_table_status_slave.
 *
 * The caller is responsible for deleting the returned value
 */
of_async_config_prop_table_status_slave_t *
of_async_config_prop_table_status_slave_OF_VERSION_1_4_dup(
    of_async_config_prop_table_status_slave_t *src)
{
    of_async_config_prop_table_status_slave_t *dst;
    uint32_t val32;

    if ((dst = of_async_config_prop_table_status_slave_new(src->version)) == NULL) {
        return NULL;
    }

    of_async_config_prop_table_status_slave_mask_get(src, &val32);
    of_async_config_prop_table_status_slave_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_controller_connection
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_controller_connection.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_controller_connection_t *
of_bsn_controller_connection_OF_VERSION_1_4_dup(
    of_bsn_controller_connection_t *src)
{
    of_bsn_controller_connection_t *dst;
    uint8_t val8;
    uint32_t val32;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_controller_connection_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_controller_connection_state_get(src, &val8);
    of_bsn_controller_connection_state_set(dst, val8);

    of_bsn_controller_connection_auxiliary_id_get(src, &val8);
    of_bsn_controller_connection_auxiliary_id_set(dst, val8);

    of_bsn_controller_connection_role_get(src, &val32);
    of_bsn_controller_connection_role_set(dst, val32);

    of_bsn_controller_connection_uri_get(src, &desc_str);
    of_bsn_controller_connection_uri_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_desc_stats_entry_t *
of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_desc_stats_entry_t *src)
{
    of_bsn_debug_counter_desc_stats_entry_t *dst;
    uint64_t val64;
    of_str64_t str64;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_debug_counter_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_desc_stats_entry_counter_id_get(src, &val64);
    of_bsn_debug_counter_desc_stats_entry_counter_id_set(dst, val64);

    of_bsn_debug_counter_desc_stats_entry_name_get(src, &str64);
    of_bsn_debug_counter_desc_stats_entry_name_set(dst, str64);

    of_bsn_debug_counter_desc_stats_entry_description_get(src, &desc_str);
    of_bsn_debug_counter_desc_stats_entry_description_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_debug_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_debug_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_debug_counter_stats_entry_t *
of_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_debug_counter_stats_entry_t *src)
{
    of_bsn_debug_counter_stats_entry_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_debug_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_debug_counter_stats_entry_counter_id_get(src, &val64);
    of_bsn_debug_counter_stats_entry_counter_id_set(dst, val64);

    of_bsn_debug_counter_stats_entry_value_get(src, &val64);
    of_bsn_debug_counter_stats_entry_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_flow_checksum_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_flow_checksum_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_flow_checksum_bucket_stats_entry_t *
of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_flow_checksum_bucket_stats_entry_t *src)
{
    of_bsn_flow_checksum_bucket_stats_entry_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_flow_checksum_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_flow_checksum_bucket_stats_entry_checksum_get(src, &val64);
    of_bsn_flow_checksum_bucket_stats_entry_checksum_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_generic_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_generic_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_generic_stats_entry_t *
of_bsn_generic_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_generic_stats_entry_t *src)
{
    of_bsn_generic_stats_entry_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_generic_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_generic_stats_entry_tlvs_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_generic_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_generic_stats_entry_tlvs_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_bucket_stats_entry_t *
of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_gentable_bucket_stats_entry_t *src)
{
    of_bsn_gentable_bucket_stats_entry_t *dst;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_bucket_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_bucket_stats_entry_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_desc_stats_entry_t *
of_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_gentable_desc_stats_entry_t *src)
{
    of_bsn_gentable_desc_stats_entry_t *dst;
    uint16_t val16;
    of_table_name_t table_name;
    uint32_t val32;

    if ((dst = of_bsn_gentable_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_desc_stats_entry_table_id_get(src, &val16);
    of_bsn_gentable_desc_stats_entry_table_id_set(dst, val16);

    of_bsn_gentable_desc_stats_entry_name_get(src, &table_name);
    of_bsn_gentable_desc_stats_entry_name_set(dst, table_name);

    of_bsn_gentable_desc_stats_entry_buckets_size_get(src, &val32);
    of_bsn_gentable_desc_stats_entry_buckets_size_set(dst, val32);

    of_bsn_gentable_desc_stats_entry_max_entries_get(src, &val32);
    of_bsn_gentable_desc_stats_entry_max_entries_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_desc_stats_entry_t *
of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_desc_stats_entry_t *src)
{
    of_bsn_gentable_entry_desc_stats_entry_t *dst;
    of_checksum_128_t checksum_128;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_desc_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_entry_desc_stats_entry_checksum_set(dst, checksum_128);

    of_bsn_gentable_entry_desc_stats_entry_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_entry_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_desc_stats_entry_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_desc_stats_entry_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_entry_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_entry_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_entry_stats_entry_t *
of_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_gentable_entry_stats_entry_t *src)
{
    of_bsn_gentable_entry_stats_entry_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_gentable_entry_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_entry_stats_entry_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_entry_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    of_bsn_gentable_entry_stats_entry_stats_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_gentable_entry_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_gentable_entry_stats_entry_stats_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_gentable_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_gentable_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_gentable_stats_entry_t *
of_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_gentable_stats_entry_t *src)
{
    of_bsn_gentable_stats_entry_t *dst;
    uint16_t val16;
    uint32_t val32;
    of_checksum_128_t checksum_128;

    if ((dst = of_bsn_gentable_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_gentable_stats_entry_table_id_get(src, &val16);
    of_bsn_gentable_stats_entry_table_id_set(dst, val16);

    of_bsn_gentable_stats_entry_entry_count_get(src, &val32);
    of_bsn_gentable_stats_entry_entry_count_set(dst, val32);

    of_bsn_gentable_stats_entry_checksum_get(src, &checksum_128);
    of_bsn_gentable_stats_entry_checksum_set(dst, checksum_128);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_interface_t *
of_bsn_interface_OF_VERSION_1_4_dup(
    of_bsn_interface_t *src)
{
    of_bsn_interface_t *dst;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_interface_hw_addr_get(src, &mac_addr);
    of_bsn_interface_hw_addr_set(dst, mac_addr);

    of_bsn_interface_name_get(src, &port_name);
    of_bsn_interface_name_set(dst, port_name);

    of_bsn_interface_ipv4_addr_get(src, &ipv4);
    of_bsn_interface_ipv4_addr_set(dst, ipv4);

    of_bsn_interface_ipv4_netmask_get(src, &ipv4);
    of_bsn_interface_ipv4_netmask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_lacp_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_lacp_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_lacp_stats_entry_t *
of_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_lacp_stats_entry_t *src)
{
    of_bsn_lacp_stats_entry_t *dst;
    of_port_no_t port_no;
    uint16_t val16;
    of_mac_addr_t mac_addr;
    uint8_t val8;

    if ((dst = of_bsn_lacp_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_lacp_stats_entry_port_no_get(src, &port_no);
    of_bsn_lacp_stats_entry_port_no_set(dst, port_no);

    of_bsn_lacp_stats_entry_actor_sys_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_sys_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_stats_entry_actor_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_stats_entry_actor_port_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_port_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_port_num_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_port_num_set(dst, val16);

    of_bsn_lacp_stats_entry_actor_key_get(src, &val16);
    of_bsn_lacp_stats_entry_actor_key_set(dst, val16);

    of_bsn_lacp_stats_entry_convergence_status_get(src, &val8);
    of_bsn_lacp_stats_entry_convergence_status_set(dst, val8);

    of_bsn_lacp_stats_entry_partner_sys_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_sys_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_sys_mac_get(src, &mac_addr);
    of_bsn_lacp_stats_entry_partner_sys_mac_set(dst, mac_addr);

    of_bsn_lacp_stats_entry_partner_port_priority_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_port_priority_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_port_num_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_port_num_set(dst, val16);

    of_bsn_lacp_stats_entry_partner_key_get(src, &val16);
    of_bsn_lacp_stats_entry_partner_key_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_port_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_port_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_port_counter_stats_entry_t *
of_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_port_counter_stats_entry_t *src)
{
    of_bsn_port_counter_stats_entry_t *dst;
    of_port_no_t port_no;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_port_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_port_counter_stats_entry_port_no_get(src, &port_no);
    of_bsn_port_counter_stats_entry_port_no_set(dst, port_no);

    of_bsn_port_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_port_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_port_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_switch_pipeline_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_switch_pipeline_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_switch_pipeline_stats_entry_t *
of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_switch_pipeline_stats_entry_t *src)
{
    of_bsn_switch_pipeline_stats_entry_t *dst;
    of_desc_str_t desc_str;

    if ((dst = of_bsn_switch_pipeline_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_switch_pipeline_stats_entry_pipeline_get(src, &desc_str);
    of_bsn_switch_pipeline_stats_entry_pipeline_set(dst, desc_str);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_table_checksum_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_table_checksum_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_table_checksum_stats_entry_t *
of_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_table_checksum_stats_entry_t *src)
{
    of_bsn_table_checksum_stats_entry_t *dst;
    uint8_t val8;
    uint64_t val64;

    if ((dst = of_bsn_table_checksum_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_table_checksum_stats_entry_table_id_get(src, &val8);
    of_bsn_table_checksum_stats_entry_table_id_set(dst, val8);

    of_bsn_table_checksum_stats_entry_checksum_get(src, &val64);
    of_bsn_table_checksum_stats_entry_checksum_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_tlv
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_tlv_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_TLV_CONVERGENCE_STATUS) {
        return of_bsn_tlv_convergence_status_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DROP) {
        return of_bsn_tlv_drop_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CIRCUIT_ID) {
        return of_bsn_tlv_circuit_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_SEQUENCED) {
        return of_bsn_tlv_mpls_sequenced_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT_USAGE) {
        return of_bsn_tlv_port_usage_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_SYSTEM_MAC) {
        return of_bsn_tlv_actor_system_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REQUEST_PACKETS) {
        return of_bsn_tlv_request_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ANCHOR) {
        return of_bsn_tlv_anchor_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_PORT_NUM) {
        return of_bsn_tlv_partner_port_num_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UINT64_LIST) {
        return of_bsn_tlv_uint64_list_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_VLAN_ON_EGRESS) {
        return of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_INTERFACE_CLASS_ID) {
        return of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BROADCAST_RATE) {
        return of_bsn_tlv_broadcast_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_LENGTH) {
        return of_bsn_tlv_udf_length_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_LOOPBACK_PORT) {
        return of_bsn_tlv_loopback_port_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_GTP_HEADER_MATCH) {
        return of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QUEUE_ID) {
        return of_bsn_tlv_queue_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_GATEWAY_MAC) {
        return of_bsn_tlv_external_gateway_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RATE_UNIT) {
        return of_bsn_tlv_rate_unit_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ECN) {
        return of_bsn_tlv_ecn_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_VID) {
        return of_bsn_tlv_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERNAL_MAC) {
        return of_bsn_tlv_internal_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HEADER_SIZE) {
        return of_bsn_tlv_header_size_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VFI) {
        return of_bsn_tlv_vfi_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_SYSTEM_PRIORITY) {
        return of_bsn_tlv_partner_system_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_SRC) {
        return of_bsn_tlv_eth_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STATUS) {
        return of_bsn_tlv_status_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4) {
        return of_bsn_tlv_ipv4_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6) {
        return of_bsn_tlv_ipv6_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_PORT_NUM) {
        return of_bsn_tlv_actor_port_num_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EGRESS_ONLY) {
        return of_bsn_tlv_egress_only_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_KNOWN_MULTICAST_RATE) {
        return of_bsn_tlv_known_multicast_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXT_HOP_IPV4) {
        return of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_NETMASK) {
        return of_bsn_tlv_ipv4_netmask_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_DST_CLASS_ID) {
        return of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REPLY_PACKETS) {
        return of_bsn_tlv_reply_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_GTP_PORT_MATCH) {
        return of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CPU_LAG) {
        return of_bsn_tlv_cpu_lag_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_GENERATION_ID) {
        return of_bsn_tlv_generation_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QUEUE_WEIGHT) {
        return of_bsn_tlv_queue_weight_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EGRESS_PORT_GROUP_ID) {
        return of_bsn_tlv_egress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_SRC) {
        return of_bsn_tlv_tcp_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_LABEL) {
        return of_bsn_tlv_mpls_label_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RX_BYTES) {
        return of_bsn_tlv_rx_bytes_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RX_PACKETS) {
        return of_bsn_tlv_rx_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_FLAGS) {
        return of_bsn_tlv_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_SEED) {
        return of_bsn_tlv_hash_seed_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MISS_PACKETS) {
        return of_bsn_tlv_miss_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_IP) {
        return of_bsn_tlv_external_ip_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_SRC) {
        return of_bsn_tlv_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_SYSTEM_PRIORITY) {
        return of_bsn_tlv_actor_system_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IGMP_SNOOPING) {
        return of_bsn_tlv_igmp_snooping_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INGRESS_PORT_GROUP_ID) {
        return of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_MAC) {
        return of_bsn_tlv_external_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEGATE) {
        return of_bsn_tlv_negate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMPV6_CHKSUM) {
        return of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_NETMASK) {
        return of_bsn_tlv_external_netmask_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IP_PROTO) {
        return of_bsn_tlv_ip_proto_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_STATE) {
        return of_bsn_tlv_actor_state_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_ID) {
        return of_bsn_tlv_udf_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_PREFIX) {
        return of_bsn_tlv_ipv6_prefix_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_SYSTEM_MAC) {
        return of_bsn_tlv_partner_system_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DISABLE_SRC_MAC_CHECK) {
        return of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ENHANCED_HASH_CAPABILITY) {
        return of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MPLS_CONTROL_WORD) {
        return of_bsn_tlv_mpls_control_word_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_TYPE) {
        return of_bsn_tlv_eth_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SET_LOOPBACK_MODE) {
        return of_bsn_tlv_set_loopback_mode_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT) {
        return of_bsn_tlv_port_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MAC_MASK) {
        return of_bsn_tlv_mac_mask_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_KEY) {
        return of_bsn_tlv_actor_key_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VPN_KEY) {
        return of_bsn_tlv_vpn_key_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_MPLS_L3_ON_INGRESS) {
        return of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXTHOP_TYPE_VXLAN) {
        return of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MCG_TYPE_VXLAN) {
        return of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DECAP) {
        return of_bsn_tlv_decap_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DATA) {
        return of_bsn_tlv_data_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_VID_MASK) {
        return of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_QOS_PRIORITY) {
        return of_bsn_tlv_qos_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TX_PACKETS) {
        return of_bsn_tlv_tx_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BROADCAST_QUERY_TIMEOUT) {
        return of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NAME) {
        return of_bsn_tlv_name_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TCP_DST) {
        return of_bsn_tlv_tcp_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_OFFSET) {
        return of_bsn_tlv_offset_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_TIME) {
        return of_bsn_tlv_idle_time_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_NOTIFICATION) {
        return of_bsn_tlv_idle_notification_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L2_MULTICAST_LOOKUP) {
        return of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_PACKET_TYPE) {
        return of_bsn_tlv_hash_packet_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NEXT_HOP_MAC) {
        return of_bsn_tlv_next_hop_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNICAST_RATE) {
        return of_bsn_tlv_unicast_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_OFFSET) {
        return of_bsn_tlv_udf_offset_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ACTOR_PORT_PRIORITY) {
        return of_bsn_tlv_actor_port_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_APPLY_PACKETS) {
        return of_bsn_tlv_apply_packets_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_SRC) {
        return of_bsn_tlv_ipv6_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_USE_PACKET_STATE) {
        return of_bsn_tlv_use_packet_state_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDP_SRC) {
        return of_bsn_tlv_udp_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_STRIP_MPLS_L2_ON_INGRESS) {
        return of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_TYPE) {
        return of_bsn_tlv_icmp_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_TYPE) {
        return of_bsn_tlv_hash_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MULTICAST_INTERFACE_ID) {
        return of_bsn_tlv_multicast_interface_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_EXTERNAL_GATEWAY_IP) {
        return of_bsn_tlv_external_gateway_ip_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_ID) {
        return of_bsn_tlv_icmp_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_PORT_PRIORITY) {
        return of_bsn_tlv_partner_port_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TTL) {
        return of_bsn_tlv_ttl_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ETH_DST) {
        return of_bsn_tlv_eth_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DATA_MASK) {
        return of_bsn_tlv_data_mask_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SAMPLING_RATE) {
        return of_bsn_tlv_sampling_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_MAC) {
        return of_bsn_tlv_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_SUB_AGENT_ID) {
        return of_bsn_tlv_sub_agent_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_RATE_LIMIT) {
        return of_bsn_tlv_rate_limit_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TX_BYTES) {
        return of_bsn_tlv_tx_bytes_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_AUTO_NEGOTIATION) {
        return of_bsn_tlv_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV4_DST) {
        return of_bsn_tlv_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDF_ANCHOR) {
        return of_bsn_tlv_udf_anchor_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PORT_VXLAN_MODE) {
        return of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_HASH_PACKET_FIELD) {
        return of_bsn_tlv_hash_packet_field_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERNAL_GATEWAY_MAC) {
        return of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_L3_SRC_CLASS_ID) {
        return of_bsn_tlv_l3_src_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_STATE) {
        return of_bsn_tlv_partner_state_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_DSCP) {
        return of_bsn_tlv_dscp_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNKNOWN_MULTICAST_RATE) {
        return of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNICAST_QUERY_TIMEOUT) {
        return of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARTNER_KEY) {
        return of_bsn_tlv_partner_key_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VFP_CLASS_ID) {
        return of_bsn_tlv_vfp_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UDP_DST) {
        return of_bsn_tlv_udp_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VRF) {
        return of_bsn_tlv_vrf_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_INTERVAL) {
        return of_bsn_tlv_interval_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VNI) {
        return of_bsn_tlv_vni_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_TUNNEL_CAPABILITY) {
        return of_bsn_tlv_tunnel_capability_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IDLE_TIMEOUT) {
        return of_bsn_tlv_idle_timeout_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_CRC_ENABLED) {
        return of_bsn_tlv_crc_enabled_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NDP_OFFLOAD) {
        return of_bsn_tlv_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PARENT_PORT) {
        return of_bsn_tlv_parent_port_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VXLAN_EGRESS_LAG) {
        return of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PRIORITY) {
        return of_bsn_tlv_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_PCP) {
        return of_bsn_tlv_vlan_pcp_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_IPV6_DST) {
        return of_bsn_tlv_ipv6_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_ICMP_CODE) {
        return of_bsn_tlv_icmp_code_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_NDP_STATIC) {
        return of_bsn_tlv_ndp_static_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_REFERENCE) {
        return of_bsn_tlv_reference_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_BUCKET) {
        return of_bsn_tlv_bucket_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_PUSH_VLAN_ON_INGRESS) {
        return of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_APPLY_BYTES) {
        return of_bsn_tlv_apply_bytes_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_UNTAGGED) {
        return of_bsn_tlv_untagged_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_TLV_VLAN_MAC_LIST) {
        return of_bsn_tlv_vlan_mac_list_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_key_t *
of_bsn_tlv_actor_key_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_key_t *src)
{
    of_bsn_tlv_actor_key_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_key_value_get(src, &val16);
    of_bsn_tlv_actor_key_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_port_num
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_port_num.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_port_num_t *
of_bsn_tlv_actor_port_num_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_port_num_t *src)
{
    of_bsn_tlv_actor_port_num_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_port_num_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_port_num_value_get(src, &val16);
    of_bsn_tlv_actor_port_num_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_port_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_port_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_port_priority_t *
of_bsn_tlv_actor_port_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_port_priority_t *src)
{
    of_bsn_tlv_actor_port_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_port_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_port_priority_value_get(src, &val16);
    of_bsn_tlv_actor_port_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_state_t *
of_bsn_tlv_actor_state_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_state_t *src)
{
    of_bsn_tlv_actor_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_actor_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_state_value_get(src, &val8);
    of_bsn_tlv_actor_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_system_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_system_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_system_mac_t *
of_bsn_tlv_actor_system_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_system_mac_t *src)
{
    of_bsn_tlv_actor_system_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_actor_system_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_system_mac_value_get(src, &mac_addr);
    of_bsn_tlv_actor_system_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_actor_system_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_actor_system_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_actor_system_priority_t *
of_bsn_tlv_actor_system_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_actor_system_priority_t *src)
{
    of_bsn_tlv_actor_system_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_actor_system_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_actor_system_priority_value_get(src, &val16);
    of_bsn_tlv_actor_system_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_anchor
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_anchor.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_anchor_t *
of_bsn_tlv_anchor_OF_VERSION_1_4_dup(
    of_bsn_tlv_anchor_t *src)
{
    of_bsn_tlv_anchor_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_anchor_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_anchor_value_get(src, &val16);
    of_bsn_tlv_anchor_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_apply_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_apply_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_apply_bytes_t *
of_bsn_tlv_apply_bytes_OF_VERSION_1_4_dup(
    of_bsn_tlv_apply_bytes_t *src)
{
    of_bsn_tlv_apply_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_apply_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_apply_bytes_value_get(src, &val64);
    of_bsn_tlv_apply_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_apply_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_apply_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_apply_packets_t *
of_bsn_tlv_apply_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_apply_packets_t *src)
{
    of_bsn_tlv_apply_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_apply_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_apply_packets_value_get(src, &val64);
    of_bsn_tlv_apply_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_auto_negotiation_t *
of_bsn_tlv_auto_negotiation_OF_VERSION_1_4_dup(
    of_bsn_tlv_auto_negotiation_t *src)
{
    of_bsn_tlv_auto_negotiation_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_auto_negotiation_value_get(src, &val8);
    of_bsn_tlv_auto_negotiation_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_broadcast_query_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_broadcast_query_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_broadcast_query_timeout_t *
of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_4_dup(
    of_bsn_tlv_broadcast_query_timeout_t *src)
{
    of_bsn_tlv_broadcast_query_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_broadcast_query_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_broadcast_query_timeout_value_get(src, &val32);
    of_bsn_tlv_broadcast_query_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_broadcast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_broadcast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_broadcast_rate_t *
of_bsn_tlv_broadcast_rate_OF_VERSION_1_4_dup(
    of_bsn_tlv_broadcast_rate_t *src)
{
    of_bsn_tlv_broadcast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_broadcast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_broadcast_rate_value_get(src, &val32);
    of_bsn_tlv_broadcast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_bucket_t *
of_bsn_tlv_bucket_OF_VERSION_1_4_dup(
    of_bsn_tlv_bucket_t *src)
{
    of_bsn_tlv_bucket_t *dst;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_tlv_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_bucket_value_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_bucket_delete(dst);
        return NULL;
    }
    of_bsn_tlv_bucket_value_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_circuit_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_circuit_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_circuit_id_t *
of_bsn_tlv_circuit_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_circuit_id_t *src)
{
    of_bsn_tlv_circuit_id_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_circuit_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_circuit_id_value_get(src, &octets);
    of_bsn_tlv_circuit_id_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_convergence_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_convergence_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_convergence_status_t *
of_bsn_tlv_convergence_status_OF_VERSION_1_4_dup(
    of_bsn_tlv_convergence_status_t *src)
{
    of_bsn_tlv_convergence_status_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_convergence_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_convergence_status_value_get(src, &val8);
    of_bsn_tlv_convergence_status_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_cpu_lag
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_cpu_lag.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_cpu_lag_t *
of_bsn_tlv_cpu_lag_OF_VERSION_1_4_dup(
    of_bsn_tlv_cpu_lag_t *src)
{
    of_bsn_tlv_cpu_lag_t *dst;

    if ((dst = of_bsn_tlv_cpu_lag_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_crc_enabled
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_crc_enabled.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_crc_enabled_t *
of_bsn_tlv_crc_enabled_OF_VERSION_1_4_dup(
    of_bsn_tlv_crc_enabled_t *src)
{
    of_bsn_tlv_crc_enabled_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_crc_enabled_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_crc_enabled_value_get(src, &val8);
    of_bsn_tlv_crc_enabled_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_data
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_data.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_data_t *
of_bsn_tlv_data_OF_VERSION_1_4_dup(
    of_bsn_tlv_data_t *src)
{
    of_bsn_tlv_data_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_data_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_data_value_get(src, &octets);
    of_bsn_tlv_data_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_data_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_data_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_data_mask_t *
of_bsn_tlv_data_mask_OF_VERSION_1_4_dup(
    of_bsn_tlv_data_mask_t *src)
{
    of_bsn_tlv_data_mask_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_data_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_data_mask_value_get(src, &octets);
    of_bsn_tlv_data_mask_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_decap
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_decap.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_decap_t *
of_bsn_tlv_decap_OF_VERSION_1_4_dup(
    of_bsn_tlv_decap_t *src)
{
    of_bsn_tlv_decap_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_decap_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_decap_value_get(src, &val16);
    of_bsn_tlv_decap_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_disable_src_mac_check_t *
of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_4_dup(
    of_bsn_tlv_disable_src_mac_check_t *src)
{
    of_bsn_tlv_disable_src_mac_check_t *dst;

    if ((dst = of_bsn_tlv_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_drop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_drop.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_drop_t *
of_bsn_tlv_drop_OF_VERSION_1_4_dup(
    of_bsn_tlv_drop_t *src)
{
    of_bsn_tlv_drop_t *dst;

    if ((dst = of_bsn_tlv_drop_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_dscp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_dscp.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_dscp_t *
of_bsn_tlv_dscp_OF_VERSION_1_4_dup(
    of_bsn_tlv_dscp_t *src)
{
    of_bsn_tlv_dscp_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_dscp_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_dscp_value_get(src, &val16);
    of_bsn_tlv_dscp_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ecn_t *
of_bsn_tlv_ecn_OF_VERSION_1_4_dup(
    of_bsn_tlv_ecn_t *src)
{
    of_bsn_tlv_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ecn_value_get(src, &val8);
    of_bsn_tlv_ecn_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_egress_only
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_egress_only.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_egress_only_t *
of_bsn_tlv_egress_only_OF_VERSION_1_4_dup(
    of_bsn_tlv_egress_only_t *src)
{
    of_bsn_tlv_egress_only_t *dst;

    if ((dst = of_bsn_tlv_egress_only_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_egress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_egress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_egress_port_group_id_t *
of_bsn_tlv_egress_port_group_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_egress_port_group_id_t *src)
{
    of_bsn_tlv_egress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_egress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_egress_port_group_id_value_get(src, &val32);
    of_bsn_tlv_egress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_enhanced_hash_capability
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_enhanced_hash_capability.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_enhanced_hash_capability_t *
of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_4_dup(
    of_bsn_tlv_enhanced_hash_capability_t *src)
{
    of_bsn_tlv_enhanced_hash_capability_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_enhanced_hash_capability_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_enhanced_hash_capability_value_get(src, &val64);
    of_bsn_tlv_enhanced_hash_capability_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_dst_t *
of_bsn_tlv_eth_dst_OF_VERSION_1_4_dup(
    of_bsn_tlv_eth_dst_t *src)
{
    of_bsn_tlv_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_dst_value_get(src, &mac_addr);
    of_bsn_tlv_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_src_t *
of_bsn_tlv_eth_src_OF_VERSION_1_4_dup(
    of_bsn_tlv_eth_src_t *src)
{
    of_bsn_tlv_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_src_value_get(src, &mac_addr);
    of_bsn_tlv_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_eth_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_eth_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_eth_type_t *
of_bsn_tlv_eth_type_OF_VERSION_1_4_dup(
    of_bsn_tlv_eth_type_t *src)
{
    of_bsn_tlv_eth_type_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_eth_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_eth_type_value_get(src, &val16);
    of_bsn_tlv_eth_type_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_gateway_ip
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_gateway_ip.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_gateway_ip_t *
of_bsn_tlv_external_gateway_ip_OF_VERSION_1_4_dup(
    of_bsn_tlv_external_gateway_ip_t *src)
{
    of_bsn_tlv_external_gateway_ip_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_gateway_ip_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_gateway_ip_value_get(src, &ipv4);
    of_bsn_tlv_external_gateway_ip_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_gateway_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_gateway_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_gateway_mac_t *
of_bsn_tlv_external_gateway_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_external_gateway_mac_t *src)
{
    of_bsn_tlv_external_gateway_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_external_gateway_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_gateway_mac_value_get(src, &mac_addr);
    of_bsn_tlv_external_gateway_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_ip
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_ip.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_ip_t *
of_bsn_tlv_external_ip_OF_VERSION_1_4_dup(
    of_bsn_tlv_external_ip_t *src)
{
    of_bsn_tlv_external_ip_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_ip_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_ip_value_get(src, &ipv4);
    of_bsn_tlv_external_ip_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_mac_t *
of_bsn_tlv_external_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_external_mac_t *src)
{
    of_bsn_tlv_external_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_external_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_mac_value_get(src, &mac_addr);
    of_bsn_tlv_external_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_external_netmask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_external_netmask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_external_netmask_t *
of_bsn_tlv_external_netmask_OF_VERSION_1_4_dup(
    of_bsn_tlv_external_netmask_t *src)
{
    of_bsn_tlv_external_netmask_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_external_netmask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_external_netmask_value_get(src, &ipv4);
    of_bsn_tlv_external_netmask_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_generation_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_generation_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_generation_id_t *
of_bsn_tlv_generation_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_generation_id_t *src)
{
    of_bsn_tlv_generation_id_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_generation_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_generation_id_value_get(src, &val64);
    of_bsn_tlv_generation_id_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_gtp_header_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_gtp_header_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_gtp_header_match_t *
of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_gtp_header_match_t *src)
{
    of_bsn_tlv_hash_gtp_header_match_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_gtp_header_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_gtp_header_match_first_header_byte_get(src, &val8);
    of_bsn_tlv_hash_gtp_header_match_first_header_byte_set(dst, val8);

    of_bsn_tlv_hash_gtp_header_match_first_header_mask_get(src, &val8);
    of_bsn_tlv_hash_gtp_header_match_first_header_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_gtp_port_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_gtp_port_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_gtp_port_match_t *
of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_gtp_port_match_t *src)
{
    of_bsn_tlv_hash_gtp_port_match_t *dst;
    uint8_t val8;
    uint16_t val16;

    if ((dst = of_bsn_tlv_hash_gtp_port_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_gtp_port_match_match_get(src, &val8);
    of_bsn_tlv_hash_gtp_port_match_match_set(dst, val8);

    of_bsn_tlv_hash_gtp_port_match_src_port_get(src, &val16);
    of_bsn_tlv_hash_gtp_port_match_src_port_set(dst, val16);

    of_bsn_tlv_hash_gtp_port_match_dst_port_get(src, &val16);
    of_bsn_tlv_hash_gtp_port_match_dst_port_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_packet_field
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_packet_field.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_packet_field_t *
of_bsn_tlv_hash_packet_field_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_packet_field_t *src)
{
    of_bsn_tlv_hash_packet_field_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_hash_packet_field_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_packet_field_value_get(src, &val64);
    of_bsn_tlv_hash_packet_field_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_packet_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_packet_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_packet_type_t *
of_bsn_tlv_hash_packet_type_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_packet_type_t *src)
{
    of_bsn_tlv_hash_packet_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_packet_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_packet_type_value_get(src, &val8);
    of_bsn_tlv_hash_packet_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_seed
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_seed.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_seed_t *
of_bsn_tlv_hash_seed_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_seed_t *src)
{
    of_bsn_tlv_hash_seed_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_hash_seed_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_seed_seed1_get(src, &val32);
    of_bsn_tlv_hash_seed_seed1_set(dst, val32);

    of_bsn_tlv_hash_seed_seed2_get(src, &val32);
    of_bsn_tlv_hash_seed_seed2_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_hash_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_hash_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_hash_type_t *
of_bsn_tlv_hash_type_OF_VERSION_1_4_dup(
    of_bsn_tlv_hash_type_t *src)
{
    of_bsn_tlv_hash_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_hash_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_hash_type_value_get(src, &val8);
    of_bsn_tlv_hash_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_header_size
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_header_size.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_header_size_t *
of_bsn_tlv_header_size_OF_VERSION_1_4_dup(
    of_bsn_tlv_header_size_t *src)
{
    of_bsn_tlv_header_size_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_header_size_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_header_size_value_get(src, &val32);
    of_bsn_tlv_header_size_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_code_t *
of_bsn_tlv_icmp_code_OF_VERSION_1_4_dup(
    of_bsn_tlv_icmp_code_t *src)
{
    of_bsn_tlv_icmp_code_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_icmp_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_code_value_get(src, &val8);
    of_bsn_tlv_icmp_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_id_t *
of_bsn_tlv_icmp_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_icmp_id_t *src)
{
    of_bsn_tlv_icmp_id_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_icmp_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_id_value_get(src, &val16);
    of_bsn_tlv_icmp_id_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmp_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmp_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmp_type_t *
of_bsn_tlv_icmp_type_OF_VERSION_1_4_dup(
    of_bsn_tlv_icmp_type_t *src)
{
    of_bsn_tlv_icmp_type_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_icmp_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmp_type_value_get(src, &val8);
    of_bsn_tlv_icmp_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_icmpv6_chksum
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_icmpv6_chksum.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_icmpv6_chksum_t *
of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_4_dup(
    of_bsn_tlv_icmpv6_chksum_t *src)
{
    of_bsn_tlv_icmpv6_chksum_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_icmpv6_chksum_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_icmpv6_chksum_value_get(src, &val16);
    of_bsn_tlv_icmpv6_chksum_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_notification
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_notification.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_notification_t *
of_bsn_tlv_idle_notification_OF_VERSION_1_4_dup(
    of_bsn_tlv_idle_notification_t *src)
{
    of_bsn_tlv_idle_notification_t *dst;

    if ((dst = of_bsn_tlv_idle_notification_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_time
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_time.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_time_t *
of_bsn_tlv_idle_time_OF_VERSION_1_4_dup(
    of_bsn_tlv_idle_time_t *src)
{
    of_bsn_tlv_idle_time_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_idle_time_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_idle_time_value_get(src, &val64);
    of_bsn_tlv_idle_time_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_idle_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_idle_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_idle_timeout_t *
of_bsn_tlv_idle_timeout_OF_VERSION_1_4_dup(
    of_bsn_tlv_idle_timeout_t *src)
{
    of_bsn_tlv_idle_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_idle_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_idle_timeout_value_get(src, &val32);
    of_bsn_tlv_idle_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_igmp_snooping
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_igmp_snooping.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_igmp_snooping_t *
of_bsn_tlv_igmp_snooping_OF_VERSION_1_4_dup(
    of_bsn_tlv_igmp_snooping_t *src)
{
    of_bsn_tlv_igmp_snooping_t *dst;

    if ((dst = of_bsn_tlv_igmp_snooping_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ingress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ingress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ingress_port_group_id_t *
of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_ingress_port_group_id_t *src)
{
    of_bsn_tlv_ingress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_ingress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ingress_port_group_id_value_get(src, &val32);
    of_bsn_tlv_ingress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_internal_gateway_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_internal_gateway_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_internal_gateway_mac_t *
of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_internal_gateway_mac_t *src)
{
    of_bsn_tlv_internal_gateway_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_internal_gateway_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_internal_gateway_mac_value_get(src, &mac_addr);
    of_bsn_tlv_internal_gateway_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_internal_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_internal_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_internal_mac_t *
of_bsn_tlv_internal_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_internal_mac_t *src)
{
    of_bsn_tlv_internal_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_internal_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_internal_mac_value_get(src, &mac_addr);
    of_bsn_tlv_internal_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_interval
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_interval.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_interval_t *
of_bsn_tlv_interval_OF_VERSION_1_4_dup(
    of_bsn_tlv_interval_t *src)
{
    of_bsn_tlv_interval_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_interval_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_interval_value_get(src, &val32);
    of_bsn_tlv_interval_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ip_proto
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ip_proto.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ip_proto_t *
of_bsn_tlv_ip_proto_OF_VERSION_1_4_dup(
    of_bsn_tlv_ip_proto_t *src)
{
    of_bsn_tlv_ip_proto_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ip_proto_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ip_proto_value_get(src, &val8);
    of_bsn_tlv_ip_proto_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_t *
of_bsn_tlv_ipv4_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv4_t *src)
{
    of_bsn_tlv_ipv4_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_dst_t *
of_bsn_tlv_ipv4_dst_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv4_dst_t *src)
{
    of_bsn_tlv_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_dst_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_netmask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_netmask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_netmask_t *
of_bsn_tlv_ipv4_netmask_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv4_netmask_t *src)
{
    of_bsn_tlv_ipv4_netmask_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_netmask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_netmask_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_netmask_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv4_src_t *
of_bsn_tlv_ipv4_src_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv4_src_t *src)
{
    of_bsn_tlv_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv4_src_value_get(src, &ipv4);
    of_bsn_tlv_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_t *
of_bsn_tlv_ipv6_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv6_t *src)
{
    of_bsn_tlv_ipv6_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_dst_t *
of_bsn_tlv_ipv6_dst_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv6_dst_t *src)
{
    of_bsn_tlv_ipv6_dst_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_dst_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_dst_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_prefix
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_prefix.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_prefix_t *
of_bsn_tlv_ipv6_prefix_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv6_prefix_t *src)
{
    of_bsn_tlv_ipv6_prefix_t *dst;
    of_ipv6_t ipv6;
    uint8_t val8;

    if ((dst = of_bsn_tlv_ipv6_prefix_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_prefix_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_prefix_value_set(dst, ipv6);

    of_bsn_tlv_ipv6_prefix_prefix_length_get(src, &val8);
    of_bsn_tlv_ipv6_prefix_prefix_length_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ipv6_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ipv6_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ipv6_src_t *
of_bsn_tlv_ipv6_src_OF_VERSION_1_4_dup(
    of_bsn_tlv_ipv6_src_t *src)
{
    of_bsn_tlv_ipv6_src_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_bsn_tlv_ipv6_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ipv6_src_value_get(src, &ipv6);
    of_bsn_tlv_ipv6_src_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_known_multicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_known_multicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_known_multicast_rate_t *
of_bsn_tlv_known_multicast_rate_OF_VERSION_1_4_dup(
    of_bsn_tlv_known_multicast_rate_t *src)
{
    of_bsn_tlv_known_multicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_known_multicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_known_multicast_rate_value_get(src, &val32);
    of_bsn_tlv_known_multicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l2_multicast_lookup
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l2_multicast_lookup.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l2_multicast_lookup_t *
of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_4_dup(
    of_bsn_tlv_l2_multicast_lookup_t *src)
{
    of_bsn_tlv_l2_multicast_lookup_t *dst;

    if ((dst = of_bsn_tlv_l2_multicast_lookup_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_dst_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_dst_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_dst_class_id_t *
of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_l3_dst_class_id_t *src)
{
    of_bsn_tlv_l3_dst_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_dst_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_dst_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_dst_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_interface_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_interface_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_interface_class_id_t *
of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_l3_interface_class_id_t *src)
{
    of_bsn_tlv_l3_interface_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_interface_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_interface_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_interface_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_l3_src_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_l3_src_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_l3_src_class_id_t *
of_bsn_tlv_l3_src_class_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_l3_src_class_id_t *src)
{
    of_bsn_tlv_l3_src_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_l3_src_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_l3_src_class_id_value_get(src, &val32);
    of_bsn_tlv_l3_src_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_loopback_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_loopback_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_loopback_port_t *
of_bsn_tlv_loopback_port_OF_VERSION_1_4_dup(
    of_bsn_tlv_loopback_port_t *src)
{
    of_bsn_tlv_loopback_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_loopback_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_loopback_port_value_get(src, &port_no);
    of_bsn_tlv_loopback_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mac_t *
of_bsn_tlv_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_mac_t *src)
{
    of_bsn_tlv_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mac_value_get(src, &mac_addr);
    of_bsn_tlv_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mac_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mac_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mac_mask_t *
of_bsn_tlv_mac_mask_OF_VERSION_1_4_dup(
    of_bsn_tlv_mac_mask_t *src)
{
    of_bsn_tlv_mac_mask_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_mac_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mac_mask_value_get(src, &mac_addr);
    of_bsn_tlv_mac_mask_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mcg_type_vxlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mcg_type_vxlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mcg_type_vxlan_t *
of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_4_dup(
    of_bsn_tlv_mcg_type_vxlan_t *src)
{
    of_bsn_tlv_mcg_type_vxlan_t *dst;

    if ((dst = of_bsn_tlv_mcg_type_vxlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_miss_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_miss_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_miss_packets_t *
of_bsn_tlv_miss_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_miss_packets_t *src)
{
    of_bsn_tlv_miss_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_miss_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_miss_packets_value_get(src, &val64);
    of_bsn_tlv_miss_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_control_word
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_control_word.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_control_word_t *
of_bsn_tlv_mpls_control_word_OF_VERSION_1_4_dup(
    of_bsn_tlv_mpls_control_word_t *src)
{
    of_bsn_tlv_mpls_control_word_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_mpls_control_word_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_control_word_value_get(src, &val8);
    of_bsn_tlv_mpls_control_word_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_label_t *
of_bsn_tlv_mpls_label_OF_VERSION_1_4_dup(
    of_bsn_tlv_mpls_label_t *src)
{
    of_bsn_tlv_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_label_value_get(src, &val32);
    of_bsn_tlv_mpls_label_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_mpls_sequenced
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_mpls_sequenced.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_mpls_sequenced_t *
of_bsn_tlv_mpls_sequenced_OF_VERSION_1_4_dup(
    of_bsn_tlv_mpls_sequenced_t *src)
{
    of_bsn_tlv_mpls_sequenced_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_mpls_sequenced_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_mpls_sequenced_value_get(src, &val8);
    of_bsn_tlv_mpls_sequenced_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_multicast_interface_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_multicast_interface_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_multicast_interface_id_t *
of_bsn_tlv_multicast_interface_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_multicast_interface_id_t *src)
{
    of_bsn_tlv_multicast_interface_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_multicast_interface_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_multicast_interface_id_value_get(src, &val32);
    of_bsn_tlv_multicast_interface_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_name
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_name.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_name_t *
of_bsn_tlv_name_OF_VERSION_1_4_dup(
    of_bsn_tlv_name_t *src)
{
    of_bsn_tlv_name_t *dst;
    of_octets_t octets;

    if ((dst = of_bsn_tlv_name_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_name_value_get(src, &octets);
    of_bsn_tlv_name_value_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ndp_offload_t *
of_bsn_tlv_ndp_offload_OF_VERSION_1_4_dup(
    of_bsn_tlv_ndp_offload_t *src)
{
    of_bsn_tlv_ndp_offload_t *dst;

    if ((dst = of_bsn_tlv_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ndp_static
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ndp_static.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ndp_static_t *
of_bsn_tlv_ndp_static_OF_VERSION_1_4_dup(
    of_bsn_tlv_ndp_static_t *src)
{
    of_bsn_tlv_ndp_static_t *dst;

    if ((dst = of_bsn_tlv_ndp_static_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_negate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_negate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_negate_t *
of_bsn_tlv_negate_OF_VERSION_1_4_dup(
    of_bsn_tlv_negate_t *src)
{
    of_bsn_tlv_negate_t *dst;

    if ((dst = of_bsn_tlv_negate_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_next_hop_ipv4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_next_hop_ipv4.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_next_hop_ipv4_t *
of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_4_dup(
    of_bsn_tlv_next_hop_ipv4_t *src)
{
    of_bsn_tlv_next_hop_ipv4_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_bsn_tlv_next_hop_ipv4_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_next_hop_ipv4_value_get(src, &ipv4);
    of_bsn_tlv_next_hop_ipv4_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_next_hop_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_next_hop_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_next_hop_mac_t *
of_bsn_tlv_next_hop_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_next_hop_mac_t *src)
{
    of_bsn_tlv_next_hop_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_next_hop_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_next_hop_mac_value_get(src, &mac_addr);
    of_bsn_tlv_next_hop_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_nexthop_type_vxlan
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_nexthop_type_vxlan.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_nexthop_type_vxlan_t *
of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_4_dup(
    of_bsn_tlv_nexthop_type_vxlan_t *src)
{
    of_bsn_tlv_nexthop_type_vxlan_t *dst;

    if ((dst = of_bsn_tlv_nexthop_type_vxlan_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_offset
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_offset.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_offset_t *
of_bsn_tlv_offset_OF_VERSION_1_4_dup(
    of_bsn_tlv_offset_t *src)
{
    of_bsn_tlv_offset_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_offset_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_offset_value_get(src, &val16);
    of_bsn_tlv_offset_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_parent_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_parent_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_parent_port_t *
of_bsn_tlv_parent_port_OF_VERSION_1_4_dup(
    of_bsn_tlv_parent_port_t *src)
{
    of_bsn_tlv_parent_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_parent_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_parent_port_value_get(src, &port_no);
    of_bsn_tlv_parent_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_key_t *
of_bsn_tlv_partner_key_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_key_t *src)
{
    of_bsn_tlv_partner_key_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_key_value_get(src, &val16);
    of_bsn_tlv_partner_key_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_port_num
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_port_num.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_port_num_t *
of_bsn_tlv_partner_port_num_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_port_num_t *src)
{
    of_bsn_tlv_partner_port_num_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_port_num_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_port_num_value_get(src, &val16);
    of_bsn_tlv_partner_port_num_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_port_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_port_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_port_priority_t *
of_bsn_tlv_partner_port_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_port_priority_t *src)
{
    of_bsn_tlv_partner_port_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_port_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_port_priority_value_get(src, &val16);
    of_bsn_tlv_partner_port_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_state_t *
of_bsn_tlv_partner_state_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_state_t *src)
{
    of_bsn_tlv_partner_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_partner_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_state_value_get(src, &val8);
    of_bsn_tlv_partner_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_system_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_system_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_system_mac_t *
of_bsn_tlv_partner_system_mac_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_system_mac_t *src)
{
    of_bsn_tlv_partner_system_mac_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_tlv_partner_system_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_system_mac_value_get(src, &mac_addr);
    of_bsn_tlv_partner_system_mac_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_partner_system_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_partner_system_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_partner_system_priority_t *
of_bsn_tlv_partner_system_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_partner_system_priority_t *src)
{
    of_bsn_tlv_partner_system_priority_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_partner_system_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_partner_system_priority_value_get(src, &val16);
    of_bsn_tlv_partner_system_priority_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_t *
of_bsn_tlv_port_OF_VERSION_1_4_dup(
    of_bsn_tlv_port_t *src)
{
    of_bsn_tlv_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_bsn_tlv_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_value_get(src, &port_no);
    of_bsn_tlv_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port_usage
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port_usage.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_usage_t *
of_bsn_tlv_port_usage_OF_VERSION_1_4_dup(
    of_bsn_tlv_port_usage_t *src)
{
    of_bsn_tlv_port_usage_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_port_usage_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_usage_value_get(src, &val16);
    of_bsn_tlv_port_usage_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_port_vxlan_mode
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_port_vxlan_mode.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_port_vxlan_mode_t *
of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_4_dup(
    of_bsn_tlv_port_vxlan_mode_t *src)
{
    of_bsn_tlv_port_vxlan_mode_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_port_vxlan_mode_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_port_vxlan_mode_value_get(src, &val8);
    of_bsn_tlv_port_vxlan_mode_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_priority_t *
of_bsn_tlv_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_priority_t *src)
{
    of_bsn_tlv_priority_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_priority_value_get(src, &val32);
    of_bsn_tlv_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_push_vlan_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_push_vlan_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_push_vlan_on_ingress_t *
of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_4_dup(
    of_bsn_tlv_push_vlan_on_ingress_t *src)
{
    of_bsn_tlv_push_vlan_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_push_vlan_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_qos_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_qos_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_qos_priority_t *
of_bsn_tlv_qos_priority_OF_VERSION_1_4_dup(
    of_bsn_tlv_qos_priority_t *src)
{
    of_bsn_tlv_qos_priority_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_qos_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_qos_priority_value_get(src, &val32);
    of_bsn_tlv_qos_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_queue_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_queue_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_queue_id_t *
of_bsn_tlv_queue_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_queue_id_t *src)
{
    of_bsn_tlv_queue_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_queue_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_queue_id_value_get(src, &val32);
    of_bsn_tlv_queue_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_queue_weight
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_queue_weight.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_queue_weight_t *
of_bsn_tlv_queue_weight_OF_VERSION_1_4_dup(
    of_bsn_tlv_queue_weight_t *src)
{
    of_bsn_tlv_queue_weight_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_queue_weight_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_queue_weight_value_get(src, &val32);
    of_bsn_tlv_queue_weight_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rate_limit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rate_limit.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rate_limit_t *
of_bsn_tlv_rate_limit_OF_VERSION_1_4_dup(
    of_bsn_tlv_rate_limit_t *src)
{
    of_bsn_tlv_rate_limit_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_rate_limit_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rate_limit_value_get(src, &val32);
    of_bsn_tlv_rate_limit_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rate_unit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rate_unit.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rate_unit_t *
of_bsn_tlv_rate_unit_OF_VERSION_1_4_dup(
    of_bsn_tlv_rate_unit_t *src)
{
    of_bsn_tlv_rate_unit_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_rate_unit_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rate_unit_value_get(src, &val8);
    of_bsn_tlv_rate_unit_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_reference
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_reference.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_reference_t *
of_bsn_tlv_reference_OF_VERSION_1_4_dup(
    of_bsn_tlv_reference_t *src)
{
    of_bsn_tlv_reference_t *dst;
    uint16_t val16;

    of_list_bsn_tlv_t src_list;
    of_list_bsn_tlv_t *dst_list;

    if ((dst = of_bsn_tlv_reference_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_reference_table_id_get(src, &val16);
    of_bsn_tlv_reference_table_id_set(dst, val16);

    of_bsn_tlv_reference_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_tlv_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_reference_delete(dst);
        return NULL;
    }
    of_bsn_tlv_reference_key_set(dst, dst_list);
    of_list_bsn_tlv_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_reply_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_reply_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_reply_packets_t *
of_bsn_tlv_reply_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_reply_packets_t *src)
{
    of_bsn_tlv_reply_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_reply_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_reply_packets_value_get(src, &val64);
    of_bsn_tlv_reply_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_request_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_request_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_request_packets_t *
of_bsn_tlv_request_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_request_packets_t *src)
{
    of_bsn_tlv_request_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_request_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_request_packets_value_get(src, &val64);
    of_bsn_tlv_request_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rx_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rx_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rx_bytes_t *
of_bsn_tlv_rx_bytes_OF_VERSION_1_4_dup(
    of_bsn_tlv_rx_bytes_t *src)
{
    of_bsn_tlv_rx_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_rx_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rx_bytes_value_get(src, &val64);
    of_bsn_tlv_rx_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_rx_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_rx_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_rx_packets_t *
of_bsn_tlv_rx_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_rx_packets_t *src)
{
    of_bsn_tlv_rx_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_rx_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_rx_packets_value_get(src, &val64);
    of_bsn_tlv_rx_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_sampling_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_sampling_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_sampling_rate_t *
of_bsn_tlv_sampling_rate_OF_VERSION_1_4_dup(
    of_bsn_tlv_sampling_rate_t *src)
{
    of_bsn_tlv_sampling_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_sampling_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_sampling_rate_value_get(src, &val32);
    of_bsn_tlv_sampling_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_set_loopback_mode
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_set_loopback_mode.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_set_loopback_mode_t *
of_bsn_tlv_set_loopback_mode_OF_VERSION_1_4_dup(
    of_bsn_tlv_set_loopback_mode_t *src)
{
    of_bsn_tlv_set_loopback_mode_t *dst;

    if ((dst = of_bsn_tlv_set_loopback_mode_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_status
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_status.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_status_t *
of_bsn_tlv_status_OF_VERSION_1_4_dup(
    of_bsn_tlv_status_t *src)
{
    of_bsn_tlv_status_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_status_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_status_value_get(src, &val8);
    of_bsn_tlv_status_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_mpls_l2_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_mpls_l2_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_mpls_l2_on_ingress_t *
of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_4_dup(
    of_bsn_tlv_strip_mpls_l2_on_ingress_t *src)
{
    of_bsn_tlv_strip_mpls_l2_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_strip_mpls_l2_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_mpls_l3_on_ingress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_mpls_l3_on_ingress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_mpls_l3_on_ingress_t *
of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_4_dup(
    of_bsn_tlv_strip_mpls_l3_on_ingress_t *src)
{
    of_bsn_tlv_strip_mpls_l3_on_ingress_t *dst;

    if ((dst = of_bsn_tlv_strip_mpls_l3_on_ingress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_strip_vlan_on_egress
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_strip_vlan_on_egress.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_strip_vlan_on_egress_t *
of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_4_dup(
    of_bsn_tlv_strip_vlan_on_egress_t *src)
{
    of_bsn_tlv_strip_vlan_on_egress_t *dst;

    if ((dst = of_bsn_tlv_strip_vlan_on_egress_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_sub_agent_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_sub_agent_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_sub_agent_id_t *
of_bsn_tlv_sub_agent_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_sub_agent_id_t *src)
{
    of_bsn_tlv_sub_agent_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_sub_agent_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_sub_agent_id_value_get(src, &val32);
    of_bsn_tlv_sub_agent_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_dst_t *
of_bsn_tlv_tcp_dst_OF_VERSION_1_4_dup(
    of_bsn_tlv_tcp_dst_t *src)
{
    of_bsn_tlv_tcp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_dst_value_get(src, &val16);
    of_bsn_tlv_tcp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_flags_t *
of_bsn_tlv_tcp_flags_OF_VERSION_1_4_dup(
    of_bsn_tlv_tcp_flags_t *src)
{
    of_bsn_tlv_tcp_flags_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_flags_value_get(src, &val16);
    of_bsn_tlv_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tcp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tcp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tcp_src_t *
of_bsn_tlv_tcp_src_OF_VERSION_1_4_dup(
    of_bsn_tlv_tcp_src_t *src)
{
    of_bsn_tlv_tcp_src_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_tcp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tcp_src_value_get(src, &val16);
    of_bsn_tlv_tcp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_ttl
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_ttl.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_ttl_t *
of_bsn_tlv_ttl_OF_VERSION_1_4_dup(
    of_bsn_tlv_ttl_t *src)
{
    of_bsn_tlv_ttl_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_ttl_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_ttl_value_get(src, &val16);
    of_bsn_tlv_ttl_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tunnel_capability
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tunnel_capability.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tunnel_capability_t *
of_bsn_tlv_tunnel_capability_OF_VERSION_1_4_dup(
    of_bsn_tlv_tunnel_capability_t *src)
{
    of_bsn_tlv_tunnel_capability_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tunnel_capability_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tunnel_capability_value_get(src, &val64);
    of_bsn_tlv_tunnel_capability_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tx_bytes
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tx_bytes.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tx_bytes_t *
of_bsn_tlv_tx_bytes_OF_VERSION_1_4_dup(
    of_bsn_tlv_tx_bytes_t *src)
{
    of_bsn_tlv_tx_bytes_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tx_bytes_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tx_bytes_value_get(src, &val64);
    of_bsn_tlv_tx_bytes_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_tx_packets
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_tx_packets.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_tx_packets_t *
of_bsn_tlv_tx_packets_OF_VERSION_1_4_dup(
    of_bsn_tlv_tx_packets_t *src)
{
    of_bsn_tlv_tx_packets_t *dst;
    uint64_t val64;

    if ((dst = of_bsn_tlv_tx_packets_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_tx_packets_value_get(src, &val64);
    of_bsn_tlv_tx_packets_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_anchor
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_anchor.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_anchor_t *
of_bsn_tlv_udf_anchor_OF_VERSION_1_4_dup(
    of_bsn_tlv_udf_anchor_t *src)
{
    of_bsn_tlv_udf_anchor_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_anchor_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_anchor_value_get(src, &val16);
    of_bsn_tlv_udf_anchor_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_id_t *
of_bsn_tlv_udf_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_udf_id_t *src)
{
    of_bsn_tlv_udf_id_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_id_value_get(src, &val16);
    of_bsn_tlv_udf_id_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_length
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_length.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_length_t *
of_bsn_tlv_udf_length_OF_VERSION_1_4_dup(
    of_bsn_tlv_udf_length_t *src)
{
    of_bsn_tlv_udf_length_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_length_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_length_value_get(src, &val16);
    of_bsn_tlv_udf_length_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udf_offset
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udf_offset.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udf_offset_t *
of_bsn_tlv_udf_offset_OF_VERSION_1_4_dup(
    of_bsn_tlv_udf_offset_t *src)
{
    of_bsn_tlv_udf_offset_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udf_offset_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udf_offset_value_get(src, &val16);
    of_bsn_tlv_udf_offset_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udp_dst_t *
of_bsn_tlv_udp_dst_OF_VERSION_1_4_dup(
    of_bsn_tlv_udp_dst_t *src)
{
    of_bsn_tlv_udp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udp_dst_value_get(src, &val16);
    of_bsn_tlv_udp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_udp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_udp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_udp_src_t *
of_bsn_tlv_udp_src_OF_VERSION_1_4_dup(
    of_bsn_tlv_udp_src_t *src)
{
    of_bsn_tlv_udp_src_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_udp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_udp_src_value_get(src, &val16);
    of_bsn_tlv_udp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_uint64_list
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_uint64_list.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_uint64_list_t *
of_bsn_tlv_uint64_list_OF_VERSION_1_4_dup(
    of_bsn_tlv_uint64_list_t *src)
{
    of_bsn_tlv_uint64_list_t *dst;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_tlv_uint64_list_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_uint64_list_value_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_uint64_list_delete(dst);
        return NULL;
    }
    of_bsn_tlv_uint64_list_value_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unicast_query_timeout
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unicast_query_timeout.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unicast_query_timeout_t *
of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_4_dup(
    of_bsn_tlv_unicast_query_timeout_t *src)
{
    of_bsn_tlv_unicast_query_timeout_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unicast_query_timeout_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unicast_query_timeout_value_get(src, &val32);
    of_bsn_tlv_unicast_query_timeout_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unicast_rate_t *
of_bsn_tlv_unicast_rate_OF_VERSION_1_4_dup(
    of_bsn_tlv_unicast_rate_t *src)
{
    of_bsn_tlv_unicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unicast_rate_value_get(src, &val32);
    of_bsn_tlv_unicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_unknown_multicast_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_unknown_multicast_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_unknown_multicast_rate_t *
of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_4_dup(
    of_bsn_tlv_unknown_multicast_rate_t *src)
{
    of_bsn_tlv_unknown_multicast_rate_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_unknown_multicast_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_unknown_multicast_rate_value_get(src, &val32);
    of_bsn_tlv_unknown_multicast_rate_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_untagged
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_untagged.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_untagged_t *
of_bsn_tlv_untagged_OF_VERSION_1_4_dup(
    of_bsn_tlv_untagged_t *src)
{
    of_bsn_tlv_untagged_t *dst;

    if ((dst = of_bsn_tlv_untagged_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_use_packet_state
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_use_packet_state.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_use_packet_state_t *
of_bsn_tlv_use_packet_state_OF_VERSION_1_4_dup(
    of_bsn_tlv_use_packet_state_t *src)
{
    of_bsn_tlv_use_packet_state_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_use_packet_state_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_use_packet_state_value_get(src, &val8);
    of_bsn_tlv_use_packet_state_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vfi
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vfi.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vfi_t *
of_bsn_tlv_vfi_OF_VERSION_1_4_dup(
    of_bsn_tlv_vfi_t *src)
{
    of_bsn_tlv_vfi_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vfi_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vfi_value_get(src, &val16);
    of_bsn_tlv_vfi_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vfp_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vfp_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vfp_class_id_t *
of_bsn_tlv_vfp_class_id_OF_VERSION_1_4_dup(
    of_bsn_tlv_vfp_class_id_t *src)
{
    of_bsn_tlv_vfp_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vfp_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vfp_class_id_value_get(src, &val32);
    of_bsn_tlv_vfp_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_mac_list
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_mac_list.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_mac_list_t *
of_bsn_tlv_vlan_mac_list_OF_VERSION_1_4_dup(
    of_bsn_tlv_vlan_mac_list_t *src)
{
    of_bsn_tlv_vlan_mac_list_t *dst;

    of_list_bsn_vlan_mac_t src_list;
    of_list_bsn_vlan_mac_t *dst_list;

    if ((dst = of_bsn_tlv_vlan_mac_list_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_mac_list_key_bind(
        src, &src_list);
    dst_list = of_list_bsn_vlan_mac_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_tlv_vlan_mac_list_delete(dst);
        return NULL;
    }
    of_bsn_tlv_vlan_mac_list_key_set(dst, dst_list);
    of_list_bsn_vlan_mac_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_pcp_t *
of_bsn_tlv_vlan_pcp_OF_VERSION_1_4_dup(
    of_bsn_tlv_vlan_pcp_t *src)
{
    of_bsn_tlv_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_bsn_tlv_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_pcp_value_get(src, &val8);
    of_bsn_tlv_vlan_pcp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_vid_t *
of_bsn_tlv_vlan_vid_OF_VERSION_1_4_dup(
    of_bsn_tlv_vlan_vid_t *src)
{
    of_bsn_tlv_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_vid_value_get(src, &val16);
    of_bsn_tlv_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vlan_vid_mask
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vlan_vid_mask.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vlan_vid_mask_t *
of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_4_dup(
    of_bsn_tlv_vlan_vid_mask_t *src)
{
    of_bsn_tlv_vlan_vid_mask_t *dst;
    uint16_t val16;

    if ((dst = of_bsn_tlv_vlan_vid_mask_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vlan_vid_mask_value_get(src, &val16);
    of_bsn_tlv_vlan_vid_mask_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vni
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vni.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vni_t *
of_bsn_tlv_vni_OF_VERSION_1_4_dup(
    of_bsn_tlv_vni_t *src)
{
    of_bsn_tlv_vni_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vni_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vni_value_get(src, &val32);
    of_bsn_tlv_vni_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vpn_key
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vpn_key.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vpn_key_t *
of_bsn_tlv_vpn_key_OF_VERSION_1_4_dup(
    of_bsn_tlv_vpn_key_t *src)
{
    of_bsn_tlv_vpn_key_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vpn_key_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vpn_key_value_get(src, &val32);
    of_bsn_tlv_vpn_key_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vrf
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vrf.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vrf_t *
of_bsn_tlv_vrf_OF_VERSION_1_4_dup(
    of_bsn_tlv_vrf_t *src)
{
    of_bsn_tlv_vrf_t *dst;
    uint32_t val32;

    if ((dst = of_bsn_tlv_vrf_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_tlv_vrf_value_get(src, &val32);
    of_bsn_tlv_vrf_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_tlv_vxlan_egress_lag
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_tlv_vxlan_egress_lag.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_tlv_vxlan_egress_lag_t *
of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_4_dup(
    of_bsn_tlv_vxlan_egress_lag_t *src)
{
    of_bsn_tlv_vxlan_egress_lag_t *dst;

    if ((dst = of_bsn_tlv_vxlan_egress_lag_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_counter_stats_entry_t *
of_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_vlan_counter_stats_entry_t *src)
{
    of_bsn_vlan_counter_stats_entry_t *dst;
    uint16_t val16;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_vlan_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_counter_stats_entry_vlan_vid_get(src, &val16);
    of_bsn_vlan_counter_stats_entry_vlan_vid_set(dst, val16);

    of_bsn_vlan_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vlan_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_vlan_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vlan_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vlan_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vlan_mac_t *
of_bsn_vlan_mac_OF_VERSION_1_4_dup(
    of_bsn_vlan_mac_t *src)
{
    of_bsn_vlan_mac_t *dst;
    uint16_t val16;
    of_mac_addr_t mac_addr;

    if ((dst = of_bsn_vlan_mac_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vlan_mac_vlan_vid_get(src, &val16);
    of_bsn_vlan_mac_vlan_vid_set(dst, val16);

    of_bsn_vlan_mac_mac_get(src, &mac_addr);
    of_bsn_vlan_mac_mac_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate a super class object of type of_bsn_vport
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bsn_vport_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_BSN_VPORT_Q_IN_Q) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_BSN_VPORT_L2GRE) {
        return of_bsn_vport_l2gre_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_bsn_vport_l2gre
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_l2gre.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_l2gre_t *
of_bsn_vport_l2gre_OF_VERSION_1_4_dup(
    of_bsn_vport_l2gre_t *src)
{
    of_bsn_vport_l2gre_t *dst;
    uint32_t val32;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_ipv4_t ipv4;
    uint8_t val8;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_l2gre_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_l2gre_flags_get(src, &val32);
    of_bsn_vport_l2gre_flags_set(dst, val32);

    of_bsn_vport_l2gre_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_loopback_port_no_get(src, &port_no);
    of_bsn_vport_l2gre_loopback_port_no_set(dst, port_no);

    of_bsn_vport_l2gre_local_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_local_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_nh_mac_get(src, &mac_addr);
    of_bsn_vport_l2gre_nh_mac_set(dst, mac_addr);

    of_bsn_vport_l2gre_src_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_src_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dst_ip_get(src, &ipv4);
    of_bsn_vport_l2gre_dst_ip_set(dst, ipv4);

    of_bsn_vport_l2gre_dscp_get(src, &val8);
    of_bsn_vport_l2gre_dscp_set(dst, val8);

    of_bsn_vport_l2gre_ttl_get(src, &val8);
    of_bsn_vport_l2gre_ttl_set(dst, val8);

    of_bsn_vport_l2gre_vpn_get(src, &val32);
    of_bsn_vport_l2gre_vpn_set(dst, val32);

    of_bsn_vport_l2gre_rate_limit_get(src, &val32);
    of_bsn_vport_l2gre_rate_limit_set(dst, val32);

    of_bsn_vport_l2gre_if_name_get(src, &port_name);
    of_bsn_vport_l2gre_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vport_q_in_q
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vport_q_in_q.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vport_q_in_q_t *
of_bsn_vport_q_in_q_OF_VERSION_1_4_dup(
    of_bsn_vport_q_in_q_t *src)
{
    of_bsn_vport_q_in_q_t *dst;
    uint32_t val32;
    uint16_t val16;
    of_port_name_t port_name;

    if ((dst = of_bsn_vport_q_in_q_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vport_q_in_q_port_no_get(src, &val32);
    of_bsn_vport_q_in_q_port_no_set(dst, val32);

    of_bsn_vport_q_in_q_ingress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_ingress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_ingress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_egress_tpid_get(src, &val16);
    of_bsn_vport_q_in_q_egress_tpid_set(dst, val16);

    of_bsn_vport_q_in_q_egress_vlan_id_get(src, &val16);
    of_bsn_vport_q_in_q_egress_vlan_id_set(dst, val16);

    of_bsn_vport_q_in_q_if_name_get(src, &port_name);
    of_bsn_vport_q_in_q_if_name_set(dst, port_name);

    return dst;
}

/**
 * Duplicate an object of type of_bsn_vrf_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bsn_vrf_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_bsn_vrf_counter_stats_entry_t *
of_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(
    of_bsn_vrf_counter_stats_entry_t *src)
{
    of_bsn_vrf_counter_stats_entry_t *dst;
    uint32_t val32;

    of_list_uint64_t src_list;
    of_list_uint64_t *dst_list;

    if ((dst = of_bsn_vrf_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_bsn_vrf_counter_stats_entry_vrf_get(src, &val32);
    of_bsn_vrf_counter_stats_entry_vrf_set(dst, val32);

    of_bsn_vrf_counter_stats_entry_values_bind(
        src, &src_list);
    dst_list = of_list_uint64_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bsn_vrf_counter_stats_entry_delete(dst);
        return NULL;
    }
    of_bsn_vrf_counter_stats_entry_values_set(dst, dst_list);
    of_list_uint64_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_t *
of_bucket_OF_VERSION_1_4_dup(
    of_bucket_t *src)
{
    of_bucket_t *dst;
    uint16_t val16;
    of_port_no_t port_no;
    uint32_t val32;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_weight_get(src, &val16);
    of_bucket_weight_set(dst, val16);

    of_bucket_watch_port_get(src, &port_no);
    of_bucket_watch_port_set(dst, port_no);

    of_bucket_watch_group_get(src, &val32);
    of_bucket_watch_group_set(dst, val32);

    of_bucket_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_bucket_delete(dst);
        return NULL;
    }
    of_bucket_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_bucket_counter_t *
of_bucket_counter_OF_VERSION_1_4_dup(
    of_bucket_counter_t *src)
{
    of_bucket_counter_t *dst;
    uint64_t val64;

    if ((dst = of_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    of_bucket_counter_packet_count_get(src, &val64);
    of_bucket_counter_packet_count_set(dst, val64);

    of_bucket_counter_byte_count_get(src, &val64);
    of_bucket_counter_byte_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate a super class object of type of_bundle_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_bundle_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_bundle_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_flow_stats_entry_t *
of_flow_stats_entry_OF_VERSION_1_4_dup(
    of_flow_stats_entry_t *src)
{
    of_flow_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint16_t val16;
    uint64_t val64;
    of_match_t match;

    of_list_instruction_t src_list;
    of_list_instruction_t *dst_list;

    if ((dst = of_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_flow_stats_entry_table_id_get(src, &val8);
    of_flow_stats_entry_table_id_set(dst, val8);

    of_flow_stats_entry_duration_sec_get(src, &val32);
    of_flow_stats_entry_duration_sec_set(dst, val32);

    of_flow_stats_entry_duration_nsec_get(src, &val32);
    of_flow_stats_entry_duration_nsec_set(dst, val32);

    of_flow_stats_entry_priority_get(src, &val16);
    of_flow_stats_entry_priority_set(dst, val16);

    of_flow_stats_entry_idle_timeout_get(src, &val16);
    of_flow_stats_entry_idle_timeout_set(dst, val16);

    of_flow_stats_entry_hard_timeout_get(src, &val16);
    of_flow_stats_entry_hard_timeout_set(dst, val16);

    of_flow_stats_entry_flags_get(src, &val16);
    of_flow_stats_entry_flags_set(dst, val16);

    of_flow_stats_entry_importance_get(src, &val16);
    of_flow_stats_entry_importance_set(dst, val16);

    of_flow_stats_entry_cookie_get(src, &val64);
    of_flow_stats_entry_cookie_set(dst, val64);

    of_flow_stats_entry_packet_count_get(src, &val64);
    of_flow_stats_entry_packet_count_set(dst, val64);

    of_flow_stats_entry_byte_count_get(src, &val64);
    of_flow_stats_entry_byte_count_set(dst, val64);

    of_flow_stats_entry_match_get(src, &match);
    of_flow_stats_entry_match_set(dst, &match);

    of_flow_stats_entry_instructions_bind(
        src, &src_list);
    dst_list = of_list_instruction_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_flow_stats_entry_delete(dst);
        return NULL;
    }
    of_flow_stats_entry_instructions_set(dst, dst_list);
    of_list_instruction_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_desc_stats_entry_t *
of_group_desc_stats_entry_OF_VERSION_1_4_dup(
    of_group_desc_stats_entry_t *src)
{
    of_group_desc_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;

    of_list_bucket_t src_list;
    of_list_bucket_t *dst_list;

    if ((dst = of_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_desc_stats_entry_group_type_get(src, &val8);
    of_group_desc_stats_entry_group_type_set(dst, val8);

    of_group_desc_stats_entry_group_id_get(src, &val32);
    of_group_desc_stats_entry_group_id_set(dst, val32);

    of_group_desc_stats_entry_buckets_bind(
        src, &src_list);
    dst_list = of_list_bucket_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_desc_stats_entry_delete(dst);
        return NULL;
    }
    of_group_desc_stats_entry_buckets_set(dst, dst_list);
    of_list_bucket_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_group_stats_entry_t *
of_group_stats_entry_OF_VERSION_1_4_dup(
    of_group_stats_entry_t *src)
{
    of_group_stats_entry_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_bucket_counter_t src_list;
    of_list_bucket_counter_t *dst_list;

    if ((dst = of_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_group_stats_entry_group_id_get(src, &val32);
    of_group_stats_entry_group_id_set(dst, val32);

    of_group_stats_entry_ref_count_get(src, &val32);
    of_group_stats_entry_ref_count_set(dst, val32);

    of_group_stats_entry_packet_count_get(src, &val64);
    of_group_stats_entry_packet_count_set(dst, val64);

    of_group_stats_entry_byte_count_get(src, &val64);
    of_group_stats_entry_byte_count_set(dst, val64);

    of_group_stats_entry_duration_sec_get(src, &val32);
    of_group_stats_entry_duration_sec_set(dst, val32);

    of_group_stats_entry_duration_nsec_get(src, &val32);
    of_group_stats_entry_duration_nsec_set(dst, val32);

    of_group_stats_entry_bucket_stats_bind(
        src, &src_list);
    dst_list = of_list_bucket_counter_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_group_stats_entry_delete(dst);
        return NULL;
    }
    of_group_stats_entry_bucket_stats_set(dst, dst_list);
    of_list_bucket_counter_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_hello_elem
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_elem.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_hello_elem_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_HELLO_ELEM_VERSIONBITMAP) {
        return of_hello_elem_versionbitmap_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_hello_elem_versionbitmap
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_hello_elem_versionbitmap.
 *
 * The caller is responsible for deleting the returned value
 */
of_hello_elem_versionbitmap_t *
of_hello_elem_versionbitmap_OF_VERSION_1_4_dup(
    of_hello_elem_versionbitmap_t *src)
{
    of_hello_elem_versionbitmap_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_hello_elem_versionbitmap_new(src->version)) == NULL) {
        return NULL;
    }

    of_hello_elem_versionbitmap_bitmaps_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_hello_elem_versionbitmap_delete(dst);
        return NULL;
    }
    of_hello_elem_versionbitmap_bitmaps_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_APPLY_ACTIONS) {
        return of_instruction_apply_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PACKET_OF_DEATH) {
        return of_instruction_bsn_packet_of_death_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PERMIT) {
        return of_instruction_bsn_permit_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_PRIORITIZE_PDUS) {
        return of_instruction_bsn_prioritize_pdus_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_L3) {
        return of_instruction_bsn_disable_l3_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DENY) {
        return of_instruction_bsn_deny_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_CLEAR_ACTIONS) {
        return of_instruction_clear_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_ARP_OFFLOAD) {
        return of_instruction_bsn_arp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_GOTO_TABLE) {
        return of_instruction_goto_table_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_SPAN_DESTINATION) {
        return of_instruction_bsn_span_destination_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_METADATA) {
        return of_instruction_write_metadata_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_INTERNAL_PRIORITY) {
        return of_instruction_bsn_internal_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_HASH_SELECT) {
        return of_instruction_bsn_hash_select_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DHCP_OFFLOAD) {
        return of_instruction_bsn_dhcp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_AUTO_NEGOTIATION) {
        return of_instruction_bsn_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_REQUIRE_VLAN_XLATE) {
        return of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_METER) {
        return of_instruction_meter_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_NDP_OFFLOAD) {
        return of_instruction_bsn_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_VLAN_COUNTERS) {
        return of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_BSN_DISABLE_SRC_MAC_CHECK) {
        return of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_WRITE_ACTIONS) {
        return of_instruction_write_actions_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_apply_actions_t *
of_instruction_apply_actions_OF_VERSION_1_4_dup(
    of_instruction_apply_actions_t *src)
{
    of_instruction_apply_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_apply_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_apply_actions_delete(dst);
        return NULL;
    }
    of_instruction_apply_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_arp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_arp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_arp_offload_t *
of_instruction_bsn_arp_offload_OF_VERSION_1_4_dup(
    of_instruction_bsn_arp_offload_t *src)
{
    of_instruction_bsn_arp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_arp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_arp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_arp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_arp_offload_subtype_get(src, &val32);
    of_instruction_bsn_arp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_auto_negotiation_t *
of_instruction_bsn_auto_negotiation_OF_VERSION_1_4_dup(
    of_instruction_bsn_auto_negotiation_t *src)
{
    of_instruction_bsn_auto_negotiation_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_auto_negotiation_experimenter_get(src, &val32);
    of_instruction_bsn_auto_negotiation_experimenter_set(dst, val32);

    of_instruction_bsn_auto_negotiation_subtype_get(src, &val32);
    of_instruction_bsn_auto_negotiation_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_deny
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_deny.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_deny_t *
of_instruction_bsn_deny_OF_VERSION_1_4_dup(
    of_instruction_bsn_deny_t *src)
{
    of_instruction_bsn_deny_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_deny_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_deny_experimenter_get(src, &val32);
    of_instruction_bsn_deny_experimenter_set(dst, val32);

    of_instruction_bsn_deny_subtype_get(src, &val32);
    of_instruction_bsn_deny_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_dhcp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_dhcp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_dhcp_offload_t *
of_instruction_bsn_dhcp_offload_OF_VERSION_1_4_dup(
    of_instruction_bsn_dhcp_offload_t *src)
{
    of_instruction_bsn_dhcp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_dhcp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_dhcp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_dhcp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_dhcp_offload_subtype_get(src, &val32);
    of_instruction_bsn_dhcp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_l3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_l3.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_l3_t *
of_instruction_bsn_disable_l3_OF_VERSION_1_4_dup(
    of_instruction_bsn_disable_l3_t *src)
{
    of_instruction_bsn_disable_l3_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_l3_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_l3_experimenter_get(src, &val32);
    of_instruction_bsn_disable_l3_experimenter_set(dst, val32);

    of_instruction_bsn_disable_l3_subtype_get(src, &val32);
    of_instruction_bsn_disable_l3_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_src_mac_check_t *
of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(
    of_instruction_bsn_disable_src_mac_check_t *src)
{
    of_instruction_bsn_disable_src_mac_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_src_mac_check_experimenter_get(src, &val32);
    of_instruction_bsn_disable_src_mac_check_experimenter_set(dst, val32);

    of_instruction_bsn_disable_src_mac_check_subtype_get(src, &val32);
    of_instruction_bsn_disable_src_mac_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_disable_vlan_counters
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_disable_vlan_counters.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_disable_vlan_counters_t *
of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(
    of_instruction_bsn_disable_vlan_counters_t *src)
{
    of_instruction_bsn_disable_vlan_counters_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_disable_vlan_counters_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_disable_vlan_counters_experimenter_get(src, &val32);
    of_instruction_bsn_disable_vlan_counters_experimenter_set(dst, val32);

    of_instruction_bsn_disable_vlan_counters_subtype_get(src, &val32);
    of_instruction_bsn_disable_vlan_counters_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_hash_select
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_hash_select.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_hash_select_t *
of_instruction_bsn_hash_select_OF_VERSION_1_4_dup(
    of_instruction_bsn_hash_select_t *src)
{
    of_instruction_bsn_hash_select_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_hash_select_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_hash_select_experimenter_get(src, &val32);
    of_instruction_bsn_hash_select_experimenter_set(dst, val32);

    of_instruction_bsn_hash_select_subtype_get(src, &val32);
    of_instruction_bsn_hash_select_subtype_set(dst, val32);

    of_instruction_bsn_hash_select_flags_get(src, &val32);
    of_instruction_bsn_hash_select_flags_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_internal_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_internal_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_internal_priority_t *
of_instruction_bsn_internal_priority_OF_VERSION_1_4_dup(
    of_instruction_bsn_internal_priority_t *src)
{
    of_instruction_bsn_internal_priority_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_internal_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_internal_priority_experimenter_get(src, &val32);
    of_instruction_bsn_internal_priority_experimenter_set(dst, val32);

    of_instruction_bsn_internal_priority_subtype_get(src, &val32);
    of_instruction_bsn_internal_priority_subtype_set(dst, val32);

    of_instruction_bsn_internal_priority_value_get(src, &val32);
    of_instruction_bsn_internal_priority_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_ndp_offload_t *
of_instruction_bsn_ndp_offload_OF_VERSION_1_4_dup(
    of_instruction_bsn_ndp_offload_t *src)
{
    of_instruction_bsn_ndp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_ndp_offload_experimenter_get(src, &val32);
    of_instruction_bsn_ndp_offload_experimenter_set(dst, val32);

    of_instruction_bsn_ndp_offload_subtype_get(src, &val32);
    of_instruction_bsn_ndp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_packet_of_death
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_packet_of_death.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_packet_of_death_t *
of_instruction_bsn_packet_of_death_OF_VERSION_1_4_dup(
    of_instruction_bsn_packet_of_death_t *src)
{
    of_instruction_bsn_packet_of_death_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_packet_of_death_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_packet_of_death_experimenter_get(src, &val32);
    of_instruction_bsn_packet_of_death_experimenter_set(dst, val32);

    of_instruction_bsn_packet_of_death_subtype_get(src, &val32);
    of_instruction_bsn_packet_of_death_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_permit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_permit.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_permit_t *
of_instruction_bsn_permit_OF_VERSION_1_4_dup(
    of_instruction_bsn_permit_t *src)
{
    of_instruction_bsn_permit_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_permit_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_permit_experimenter_get(src, &val32);
    of_instruction_bsn_permit_experimenter_set(dst, val32);

    of_instruction_bsn_permit_subtype_get(src, &val32);
    of_instruction_bsn_permit_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_prioritize_pdus
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_prioritize_pdus.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_prioritize_pdus_t *
of_instruction_bsn_prioritize_pdus_OF_VERSION_1_4_dup(
    of_instruction_bsn_prioritize_pdus_t *src)
{
    of_instruction_bsn_prioritize_pdus_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_prioritize_pdus_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_prioritize_pdus_experimenter_get(src, &val32);
    of_instruction_bsn_prioritize_pdus_experimenter_set(dst, val32);

    of_instruction_bsn_prioritize_pdus_subtype_get(src, &val32);
    of_instruction_bsn_prioritize_pdus_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_require_vlan_xlate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_require_vlan_xlate.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_require_vlan_xlate_t *
of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(
    of_instruction_bsn_require_vlan_xlate_t *src)
{
    of_instruction_bsn_require_vlan_xlate_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_require_vlan_xlate_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_require_vlan_xlate_experimenter_get(src, &val32);
    of_instruction_bsn_require_vlan_xlate_experimenter_set(dst, val32);

    of_instruction_bsn_require_vlan_xlate_subtype_get(src, &val32);
    of_instruction_bsn_require_vlan_xlate_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_bsn_span_destination
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_bsn_span_destination.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_bsn_span_destination_t *
of_instruction_bsn_span_destination_OF_VERSION_1_4_dup(
    of_instruction_bsn_span_destination_t *src)
{
    of_instruction_bsn_span_destination_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_bsn_span_destination_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_bsn_span_destination_experimenter_get(src, &val32);
    of_instruction_bsn_span_destination_experimenter_set(dst, val32);

    of_instruction_bsn_span_destination_subtype_get(src, &val32);
    of_instruction_bsn_span_destination_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_clear_actions_t *
of_instruction_clear_actions_OF_VERSION_1_4_dup(
    of_instruction_clear_actions_t *src)
{
    of_instruction_clear_actions_t *dst;

    if ((dst = of_instruction_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_goto_table_t *
of_instruction_goto_table_OF_VERSION_1_4_dup(
    of_instruction_goto_table_t *src)
{
    of_instruction_goto_table_t *dst;
    uint8_t val8;

    if ((dst = of_instruction_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_goto_table_table_id_get(src, &val8);
    of_instruction_goto_table_table_id_set(dst, val8);

    return dst;
}

/**
 * Duplicate a super class object of type of_instruction_id
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_instruction_id_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_INSTRUCTION_ID_BSN_NDP_OFFLOAD) {
        return of_instruction_id_bsn_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_INTERNAL_PRIORITY) {
        return of_instruction_id_bsn_internal_priority_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_AUTO_NEGOTIATION) {
        return of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_METER) {
        return of_instruction_id_meter_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DHCP_OFFLOAD) {
        return of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_REQUIRE_VLAN_XLATE) {
        return of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_HASH_SELECT) {
        return of_instruction_id_bsn_hash_select_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_VLAN_COUNTERS) {
        return of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_WRITE_ACTIONS) {
        return of_instruction_id_write_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_SRC_MAC_CHECK) {
        return of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_APPLY_ACTIONS) {
        return of_instruction_id_apply_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PACKET_OF_DEATH) {
        return of_instruction_id_bsn_packet_of_death_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_ARP_OFFLOAD) {
        return of_instruction_id_bsn_arp_offload_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PRIORITIZE_PDUS) {
        return of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_PERMIT) {
        return of_instruction_id_bsn_permit_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DENY) {
        return of_instruction_id_bsn_deny_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_WRITE_METADATA) {
        return of_instruction_id_write_metadata_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_CLEAR_ACTIONS) {
        return of_instruction_id_clear_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_GOTO_TABLE) {
        return of_instruction_id_goto_table_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_DISABLE_L3) {
        return of_instruction_id_bsn_disable_l3_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_INSTRUCTION_ID_BSN_SPAN_DESTINATION) {
        return of_instruction_id_bsn_span_destination_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_instruction_id_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_apply_actions_t *
of_instruction_id_apply_actions_OF_VERSION_1_4_dup(
    of_instruction_id_apply_actions_t *src)
{
    of_instruction_id_apply_actions_t *dst;

    if ((dst = of_instruction_id_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_arp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_arp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_arp_offload_t *
of_instruction_id_bsn_arp_offload_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_arp_offload_t *src)
{
    of_instruction_id_bsn_arp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_arp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_arp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_arp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_arp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_arp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_auto_negotiation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_auto_negotiation.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_auto_negotiation_t *
of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_auto_negotiation_t *src)
{
    of_instruction_id_bsn_auto_negotiation_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_auto_negotiation_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_auto_negotiation_experimenter_get(src, &val32);
    of_instruction_id_bsn_auto_negotiation_experimenter_set(dst, val32);

    of_instruction_id_bsn_auto_negotiation_subtype_get(src, &val32);
    of_instruction_id_bsn_auto_negotiation_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_deny
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_deny.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_deny_t *
of_instruction_id_bsn_deny_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_deny_t *src)
{
    of_instruction_id_bsn_deny_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_deny_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_deny_experimenter_get(src, &val32);
    of_instruction_id_bsn_deny_experimenter_set(dst, val32);

    of_instruction_id_bsn_deny_subtype_get(src, &val32);
    of_instruction_id_bsn_deny_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_dhcp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_dhcp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_dhcp_offload_t *
of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_dhcp_offload_t *src)
{
    of_instruction_id_bsn_dhcp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_dhcp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_dhcp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_dhcp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_dhcp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_dhcp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_l3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_l3.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_l3_t *
of_instruction_id_bsn_disable_l3_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_disable_l3_t *src)
{
    of_instruction_id_bsn_disable_l3_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_l3_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_l3_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_l3_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_l3_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_l3_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_src_mac_check
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_src_mac_check.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_src_mac_check_t *
of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_disable_src_mac_check_t *src)
{
    of_instruction_id_bsn_disable_src_mac_check_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_src_mac_check_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_src_mac_check_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_src_mac_check_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_src_mac_check_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_src_mac_check_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_disable_vlan_counters
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_disable_vlan_counters.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_disable_vlan_counters_t *
of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_disable_vlan_counters_t *src)
{
    of_instruction_id_bsn_disable_vlan_counters_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_disable_vlan_counters_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_disable_vlan_counters_experimenter_get(src, &val32);
    of_instruction_id_bsn_disable_vlan_counters_experimenter_set(dst, val32);

    of_instruction_id_bsn_disable_vlan_counters_subtype_get(src, &val32);
    of_instruction_id_bsn_disable_vlan_counters_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_hash_select
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_hash_select.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_hash_select_t *
of_instruction_id_bsn_hash_select_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_hash_select_t *src)
{
    of_instruction_id_bsn_hash_select_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_hash_select_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_hash_select_experimenter_get(src, &val32);
    of_instruction_id_bsn_hash_select_experimenter_set(dst, val32);

    of_instruction_id_bsn_hash_select_subtype_get(src, &val32);
    of_instruction_id_bsn_hash_select_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_internal_priority
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_internal_priority.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_internal_priority_t *
of_instruction_id_bsn_internal_priority_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_internal_priority_t *src)
{
    of_instruction_id_bsn_internal_priority_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_internal_priority_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_internal_priority_experimenter_get(src, &val32);
    of_instruction_id_bsn_internal_priority_experimenter_set(dst, val32);

    of_instruction_id_bsn_internal_priority_subtype_get(src, &val32);
    of_instruction_id_bsn_internal_priority_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_ndp_offload
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_ndp_offload.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_ndp_offload_t *
of_instruction_id_bsn_ndp_offload_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_ndp_offload_t *src)
{
    of_instruction_id_bsn_ndp_offload_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_ndp_offload_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_ndp_offload_experimenter_get(src, &val32);
    of_instruction_id_bsn_ndp_offload_experimenter_set(dst, val32);

    of_instruction_id_bsn_ndp_offload_subtype_get(src, &val32);
    of_instruction_id_bsn_ndp_offload_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_packet_of_death
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_packet_of_death.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_packet_of_death_t *
of_instruction_id_bsn_packet_of_death_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_packet_of_death_t *src)
{
    of_instruction_id_bsn_packet_of_death_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_packet_of_death_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_packet_of_death_experimenter_get(src, &val32);
    of_instruction_id_bsn_packet_of_death_experimenter_set(dst, val32);

    of_instruction_id_bsn_packet_of_death_subtype_get(src, &val32);
    of_instruction_id_bsn_packet_of_death_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_permit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_permit.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_permit_t *
of_instruction_id_bsn_permit_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_permit_t *src)
{
    of_instruction_id_bsn_permit_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_permit_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_permit_experimenter_get(src, &val32);
    of_instruction_id_bsn_permit_experimenter_set(dst, val32);

    of_instruction_id_bsn_permit_subtype_get(src, &val32);
    of_instruction_id_bsn_permit_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_prioritize_pdus
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_prioritize_pdus.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_prioritize_pdus_t *
of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_prioritize_pdus_t *src)
{
    of_instruction_id_bsn_prioritize_pdus_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_prioritize_pdus_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_prioritize_pdus_experimenter_get(src, &val32);
    of_instruction_id_bsn_prioritize_pdus_experimenter_set(dst, val32);

    of_instruction_id_bsn_prioritize_pdus_subtype_get(src, &val32);
    of_instruction_id_bsn_prioritize_pdus_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_require_vlan_xlate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_require_vlan_xlate.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_require_vlan_xlate_t *
of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_require_vlan_xlate_t *src)
{
    of_instruction_id_bsn_require_vlan_xlate_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_require_vlan_xlate_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_require_vlan_xlate_experimenter_get(src, &val32);
    of_instruction_id_bsn_require_vlan_xlate_experimenter_set(dst, val32);

    of_instruction_id_bsn_require_vlan_xlate_subtype_get(src, &val32);
    of_instruction_id_bsn_require_vlan_xlate_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_bsn_span_destination
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_bsn_span_destination.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_bsn_span_destination_t *
of_instruction_id_bsn_span_destination_OF_VERSION_1_4_dup(
    of_instruction_id_bsn_span_destination_t *src)
{
    of_instruction_id_bsn_span_destination_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_id_bsn_span_destination_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_id_bsn_span_destination_experimenter_get(src, &val32);
    of_instruction_id_bsn_span_destination_experimenter_set(dst, val32);

    of_instruction_id_bsn_span_destination_subtype_get(src, &val32);
    of_instruction_id_bsn_span_destination_subtype_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_clear_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_clear_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_clear_actions_t *
of_instruction_id_clear_actions_OF_VERSION_1_4_dup(
    of_instruction_id_clear_actions_t *src)
{
    of_instruction_id_clear_actions_t *dst;

    if ((dst = of_instruction_id_clear_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_goto_table
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_goto_table.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_goto_table_t *
of_instruction_id_goto_table_OF_VERSION_1_4_dup(
    of_instruction_id_goto_table_t *src)
{
    of_instruction_id_goto_table_t *dst;

    if ((dst = of_instruction_id_goto_table_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_meter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_meter.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_meter_t *
of_instruction_id_meter_OF_VERSION_1_4_dup(
    of_instruction_id_meter_t *src)
{
    of_instruction_id_meter_t *dst;

    if ((dst = of_instruction_id_meter_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_write_actions_t *
of_instruction_id_write_actions_OF_VERSION_1_4_dup(
    of_instruction_id_write_actions_t *src)
{
    of_instruction_id_write_actions_t *dst;

    if ((dst = of_instruction_id_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_id_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_id_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_id_write_metadata_t *
of_instruction_id_write_metadata_OF_VERSION_1_4_dup(
    of_instruction_id_write_metadata_t *src)
{
    of_instruction_id_write_metadata_t *dst;

    if ((dst = of_instruction_id_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    return dst;
}

/**
 * Duplicate an object of type of_instruction_meter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_meter.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_meter_t *
of_instruction_meter_OF_VERSION_1_4_dup(
    of_instruction_meter_t *src)
{
    of_instruction_meter_t *dst;
    uint32_t val32;

    if ((dst = of_instruction_meter_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_meter_meter_id_get(src, &val32);
    of_instruction_meter_meter_id_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_actions_t *
of_instruction_write_actions_OF_VERSION_1_4_dup(
    of_instruction_write_actions_t *src)
{
    of_instruction_write_actions_t *dst;

    of_list_action_t src_list;
    of_list_action_t *dst_list;

    if ((dst = of_instruction_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_actions_actions_bind(
        src, &src_list);
    dst_list = of_list_action_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_instruction_write_actions_delete(dst);
        return NULL;
    }
    of_instruction_write_actions_actions_set(dst, dst_list);
    of_list_action_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_instruction_write_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_instruction_write_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_instruction_write_metadata_t *
of_instruction_write_metadata_OF_VERSION_1_4_dup(
    of_instruction_write_metadata_t *src)
{
    of_instruction_write_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_instruction_write_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_instruction_write_metadata_metadata_get(src, &val64);
    of_instruction_write_metadata_metadata_set(dst, val64);

    of_instruction_write_metadata_metadata_mask_get(src, &val64);
    of_instruction_write_metadata_metadata_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_match_v3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_match_v3.
 *
 * The caller is responsible for deleting the returned value
 */
of_match_v3_t *
of_match_v3_OF_VERSION_1_4_dup(
    of_match_v3_t *src)
{
    of_match_v3_t *dst;

    of_list_oxm_t src_list;
    of_list_oxm_t *dst_list;

    if ((dst = of_match_v3_new(src->version)) == NULL) {
        return NULL;
    }

    of_match_v3_oxm_list_bind(
        src, &src_list);
    dst_list = of_list_oxm_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_match_v3_delete(dst);
        return NULL;
    }
    of_match_v3_oxm_list_set(dst, dst_list);
    of_list_oxm_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_meter_band
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_meter_band_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_METER_BAND_DROP) {
        return of_meter_band_drop_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_METER_BAND_DSCP_REMARK) {
        return of_meter_band_dscp_remark_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_METER_BAND_EXPERIMENTER) {
        return of_meter_band_experimenter_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_meter_band_drop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_drop.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_drop_t *
of_meter_band_drop_OF_VERSION_1_4_dup(
    of_meter_band_drop_t *src)
{
    of_meter_band_drop_t *dst;
    uint32_t val32;

    if ((dst = of_meter_band_drop_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_drop_rate_get(src, &val32);
    of_meter_band_drop_rate_set(dst, val32);

    of_meter_band_drop_burst_size_get(src, &val32);
    of_meter_band_drop_burst_size_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_dscp_remark
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_dscp_remark.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_dscp_remark_t *
of_meter_band_dscp_remark_OF_VERSION_1_4_dup(
    of_meter_band_dscp_remark_t *src)
{
    of_meter_band_dscp_remark_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_meter_band_dscp_remark_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_dscp_remark_rate_get(src, &val32);
    of_meter_band_dscp_remark_rate_set(dst, val32);

    of_meter_band_dscp_remark_burst_size_get(src, &val32);
    of_meter_band_dscp_remark_burst_size_set(dst, val32);

    of_meter_band_dscp_remark_prec_level_get(src, &val8);
    of_meter_band_dscp_remark_prec_level_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_experimenter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_experimenter.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_experimenter_t *
of_meter_band_experimenter_OF_VERSION_1_4_dup(
    of_meter_band_experimenter_t *src)
{
    of_meter_band_experimenter_t *dst;
    uint32_t val32;

    if ((dst = of_meter_band_experimenter_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_experimenter_rate_get(src, &val32);
    of_meter_band_experimenter_rate_set(dst, val32);

    of_meter_band_experimenter_burst_size_get(src, &val32);
    of_meter_band_experimenter_burst_size_set(dst, val32);

    of_meter_band_experimenter_experimenter_get(src, &val32);
    of_meter_band_experimenter_experimenter_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_meter_band_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_band_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_band_stats_t *
of_meter_band_stats_OF_VERSION_1_4_dup(
    of_meter_band_stats_t *src)
{
    of_meter_band_stats_t *dst;
    uint64_t val64;

    if ((dst = of_meter_band_stats_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_band_stats_packet_band_count_get(src, &val64);
    of_meter_band_stats_packet_band_count_set(dst, val64);

    of_meter_band_stats_byte_band_count_get(src, &val64);
    of_meter_band_stats_byte_band_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_meter_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_config_t *
of_meter_config_OF_VERSION_1_4_dup(
    of_meter_config_t *src)
{
    of_meter_config_t *dst;
    uint16_t val16;
    uint32_t val32;

    of_list_meter_band_t src_list;
    of_list_meter_band_t *dst_list;

    if ((dst = of_meter_config_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_config_flags_get(src, &val16);
    of_meter_config_flags_set(dst, val16);

    of_meter_config_meter_id_get(src, &val32);
    of_meter_config_meter_id_set(dst, val32);

    of_meter_config_entries_bind(
        src, &src_list);
    dst_list = of_list_meter_band_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_config_delete(dst);
        return NULL;
    }
    of_meter_config_entries_set(dst, dst_list);
    of_list_meter_band_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_meter_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_features_t *
of_meter_features_OF_VERSION_1_4_dup(
    of_meter_features_t *src)
{
    of_meter_features_t *dst;
    uint32_t val32;
    uint8_t val8;

    if ((dst = of_meter_features_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_features_max_meter_get(src, &val32);
    of_meter_features_max_meter_set(dst, val32);

    of_meter_features_band_types_get(src, &val32);
    of_meter_features_band_types_set(dst, val32);

    of_meter_features_capabilities_get(src, &val32);
    of_meter_features_capabilities_set(dst, val32);

    of_meter_features_max_bands_get(src, &val8);
    of_meter_features_max_bands_set(dst, val8);

    of_meter_features_max_color_get(src, &val8);
    of_meter_features_max_color_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_meter_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_meter_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_meter_stats_t *
of_meter_stats_OF_VERSION_1_4_dup(
    of_meter_stats_t *src)
{
    of_meter_stats_t *dst;
    uint32_t val32;
    uint64_t val64;

    of_list_meter_band_stats_t src_list;
    of_list_meter_band_stats_t *dst_list;

    if ((dst = of_meter_stats_new(src->version)) == NULL) {
        return NULL;
    }

    of_meter_stats_meter_id_get(src, &val32);
    of_meter_stats_meter_id_set(dst, val32);

    of_meter_stats_flow_count_get(src, &val32);
    of_meter_stats_flow_count_set(dst, val32);

    of_meter_stats_packet_in_count_get(src, &val64);
    of_meter_stats_packet_in_count_set(dst, val64);

    of_meter_stats_byte_in_count_get(src, &val64);
    of_meter_stats_byte_in_count_set(dst, val64);

    of_meter_stats_duration_sec_get(src, &val32);
    of_meter_stats_duration_sec_set(dst, val32);

    of_meter_stats_duration_nsec_get(src, &val32);
    of_meter_stats_duration_nsec_set(dst, val32);

    of_meter_stats_band_stats_bind(
        src, &src_list);
    dst_list = of_list_meter_band_stats_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_meter_stats_delete(dst);
        return NULL;
    }
    of_meter_stats_band_stats_set(dst, dst_list);
    of_list_meter_band_stats_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_oxm
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_oxm_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_OXM_IN_PORT_MASKED) {
        return of_oxm_in_port_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_EXTHDR_MASKED) {
        return of_oxm_ipv6_exthdr_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION_MASKED) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_BOS_MASKED) {
        return of_oxm_mpls_bos_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT) {
        return of_oxm_in_phy_port_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST_MASKED) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VFI_MASKED) {
        return of_oxm_bsn_vfi_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_VLAN_VID) {
        return of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE_MASKED) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PHY_PORT_MASKED) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE) {
        return of_oxm_icmpv4_code_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2_MASKED) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4_MASKED) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST) {
        return of_oxm_udp_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE) {
        return of_oxm_eth_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST) {
        return of_oxm_eth_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP) {
        return of_oxm_vlan_pcp_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TARGET_MASKED) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP) {
        return of_oxm_arp_op_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE) {
        return of_oxm_icmpv4_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL_MASKED) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE) {
        return of_oxm_icmpv6_type_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA_MASKED) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_DST_MASKED) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_PCP_MASKED) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA) {
        return of_oxm_arp_sha_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_TYPE_MASKED) {
        return of_oxm_eth_type_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST) {
        return of_oxm_tcp_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO_MASKED) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN_MASKED) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_PBB_UCA) {
        return of_oxm_pbb_uca_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA_MASKED) {
        return of_oxm_metadata_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_EGR_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA_MASKED) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC) {
        return of_oxm_sctp_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512_MASKED) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC) {
        return of_oxm_udp_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST_MASKED) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_BOS) {
        return of_oxm_mpls_bos_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_INTERFACE_CLASS_ID) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST_MASKED) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SHA_MASKED) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L3_SRC_CLASS_ID_MASKED) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_DST) {
        return of_oxm_sctp_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5_MASKED) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_DST_MASKED) {
        return of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_TYPE_MASKED) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_TLL_MASKED) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV4_CODE_MASKED) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS_MASKED) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST) {
        return of_oxm_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_128_MASKED) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_SRC) {
        return of_oxm_bsn_inner_eth_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_DST) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC_MASKED) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VXLAN_NETWORK_ID_MASKED) {
        return of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_DST) {
        return of_oxm_bsn_inner_eth_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_SRC) {
        return of_oxm_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_ETH_SRC_MASKED) {
        return of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF5) {
        return of_oxm_bsn_udf5_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF4) {
        return of_oxm_bsn_udf4_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7) {
        return of_oxm_bsn_udf7_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_TCP_FLAGS_MASKED) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1) {
        return of_oxm_bsn_udf1_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0) {
        return of_oxm_bsn_udf0_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3) {
        return of_oxm_bsn_udf3_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF2) {
        return of_oxm_bsn_udf2_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_CODE) {
        return of_oxm_icmpv6_code_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP_MASKED) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL) {
        return of_oxm_mpls_label_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INNER_VLAN_VID_MASKED) {
        return of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6) {
        return of_oxm_bsn_udf6_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IN_PORT) {
        return of_oxm_in_port_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF7_MASKED) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF1_MASKED) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF_MASKED) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC) {
        return of_oxm_tcp_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_SRC_MASKED) {
        return of_oxm_udp_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_ECN) {
        return of_oxm_ip_ecn_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC) {
        return of_oxm_eth_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_DST) {
        return of_oxm_ipv6_dst_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ETH_SRC_MASKED) {
        return of_oxm_eth_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VRF) {
        return of_oxm_bsn_vrf_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF3_MASKED) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IP_FRAGMENTATION) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC_MASKED) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_DSCP) {
        return of_oxm_ip_dscp_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID_MASKED) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_ND_SLL) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_FLABEL_MASKED) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC_MASKED) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_DST_MASKED) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF6_MASKED) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_SCTP_SRC_MASKED) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_LAG_ID_MASKED) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_L2_CACHE_HIT_MASKED) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IP_PROTO) {
        return of_oxm_ip_proto_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_IN_PORTS_512) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_SRC) {
        return of_oxm_ipv6_src_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_UDP_DST_MASKED) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VFI) {
        return of_oxm_bsn_vfi_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_SPA) {
        return of_oxm_arp_spa_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_OP_MASKED) {
        return of_oxm_arp_op_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_ID_MASKED) {
        return of_oxm_tunnel_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_IPV4_SRC_MASKED) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_VLAN_VID) {
        return of_oxm_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ICMPV6_TYPE_MASKED) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TCP_SRC_MASKED) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_INGRESS_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_TUNNEL_ID) {
        return of_oxm_tunnel_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_METADATA) {
        return of_oxm_metadata_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_OVS_TCP_FLAGS) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_UDF0_MASKED) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA_MASKED) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID_MASKED) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_PBB_UCA_MASKED) {
        return of_oxm_pbb_uca_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_THA) {
        return of_oxm_arp_tha_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_TC) {
        return of_oxm_mpls_tc_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_BSN_VXLAN_NETWORK_ID) {
        return of_oxm_bsn_vxlan_network_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_ARP_TPA) {
        return of_oxm_arp_tpa_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV4_DST_MASKED) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_IPV6_EXTHDR) {
        return of_oxm_ipv6_exthdr_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_OXM_MPLS_LABEL_MASKED) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_oxm_arp_op
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_t *
of_oxm_arp_op_OF_VERSION_1_4_dup(
    of_oxm_arp_op_t *src)
{
    of_oxm_arp_op_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_value_get(src, &val16);
    of_oxm_arp_op_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_op_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_op_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_op_masked_t *
of_oxm_arp_op_masked_OF_VERSION_1_4_dup(
    of_oxm_arp_op_masked_t *src)
{
    of_oxm_arp_op_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_arp_op_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_op_masked_value_get(src, &val16);
    of_oxm_arp_op_masked_value_set(dst, val16);

    of_oxm_arp_op_masked_value_mask_get(src, &val16);
    of_oxm_arp_op_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_t *
of_oxm_arp_sha_OF_VERSION_1_4_dup(
    of_oxm_arp_sha_t *src)
{
    of_oxm_arp_sha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_value_get(src, &mac_addr);
    of_oxm_arp_sha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_sha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_sha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_sha_masked_t *
of_oxm_arp_sha_masked_OF_VERSION_1_4_dup(
    of_oxm_arp_sha_masked_t *src)
{
    of_oxm_arp_sha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_sha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_sha_masked_value_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_set(dst, mac_addr);

    of_oxm_arp_sha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_sha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_t *
of_oxm_arp_spa_OF_VERSION_1_4_dup(
    of_oxm_arp_spa_t *src)
{
    of_oxm_arp_spa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_value_get(src, &val32);
    of_oxm_arp_spa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_spa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_spa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_spa_masked_t *
of_oxm_arp_spa_masked_OF_VERSION_1_4_dup(
    of_oxm_arp_spa_masked_t *src)
{
    of_oxm_arp_spa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_spa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_spa_masked_value_get(src, &val32);
    of_oxm_arp_spa_masked_value_set(dst, val32);

    of_oxm_arp_spa_masked_value_mask_get(src, &val32);
    of_oxm_arp_spa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_t *
of_oxm_arp_tha_OF_VERSION_1_4_dup(
    of_oxm_arp_tha_t *src)
{
    of_oxm_arp_tha_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_value_get(src, &mac_addr);
    of_oxm_arp_tha_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tha_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tha_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tha_masked_t *
of_oxm_arp_tha_masked_OF_VERSION_1_4_dup(
    of_oxm_arp_tha_masked_t *src)
{
    of_oxm_arp_tha_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_arp_tha_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tha_masked_value_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_set(dst, mac_addr);

    of_oxm_arp_tha_masked_value_mask_get(src, &mac_addr);
    of_oxm_arp_tha_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_t *
of_oxm_arp_tpa_OF_VERSION_1_4_dup(
    of_oxm_arp_tpa_t *src)
{
    of_oxm_arp_tpa_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_value_get(src, &val32);
    of_oxm_arp_tpa_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_arp_tpa_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_arp_tpa_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_arp_tpa_masked_t *
of_oxm_arp_tpa_masked_OF_VERSION_1_4_dup(
    of_oxm_arp_tpa_masked_t *src)
{
    of_oxm_arp_tpa_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_arp_tpa_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_arp_tpa_masked_value_get(src, &val32);
    of_oxm_arp_tpa_masked_value_set(dst, val32);

    of_oxm_arp_tpa_masked_value_mask_get(src, &val32);
    of_oxm_arp_tpa_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_t *
of_oxm_bsn_egr_port_group_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_egr_port_group_id_t *src)
{
    of_oxm_bsn_egr_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_egr_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_egr_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_egr_port_group_id_masked_t *
of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_egr_port_group_id_masked_t *src)
{
    of_oxm_bsn_egr_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_egr_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_egr_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_egr_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_egr_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_t *
of_oxm_bsn_in_ports_128_OF_VERSION_1_4_dup(
    of_oxm_bsn_in_ports_128_t *src)
{
    of_oxm_bsn_in_ports_128_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_value_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_128_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_128_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_128_masked_t *
of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_in_ports_128_masked_t *src)
{
    of_oxm_bsn_in_ports_128_masked_t *dst;
    of_bitmap_128_t bitmap_128;

    if ((dst = of_oxm_bsn_in_ports_128_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_128_masked_value_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_set(dst, bitmap_128);

    of_oxm_bsn_in_ports_128_masked_value_mask_get(src, &bitmap_128);
    of_oxm_bsn_in_ports_128_masked_value_mask_set(dst, bitmap_128);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_t *
of_oxm_bsn_in_ports_512_OF_VERSION_1_4_dup(
    of_oxm_bsn_in_ports_512_t *src)
{
    of_oxm_bsn_in_ports_512_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_value_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_in_ports_512_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_in_ports_512_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_in_ports_512_masked_t *
of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_in_ports_512_masked_t *src)
{
    of_oxm_bsn_in_ports_512_masked_t *dst;
    of_bitmap_512_t bitmap_512;

    if ((dst = of_oxm_bsn_in_ports_512_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_in_ports_512_masked_value_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_set(dst, bitmap_512);

    of_oxm_bsn_in_ports_512_masked_value_mask_get(src, &bitmap_512);
    of_oxm_bsn_in_ports_512_masked_value_mask_set(dst, bitmap_512);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_t *
of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_ingress_port_group_id_t *src)
{
    of_oxm_bsn_ingress_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ingress_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ingress_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ingress_port_group_id_masked_t *
of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_ingress_port_group_id_masked_t *src)
{
    of_oxm_bsn_ingress_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_ingress_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ingress_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_ingress_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_ingress_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_dst_t *
of_oxm_bsn_inner_eth_dst_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_eth_dst_t *src)
{
    of_oxm_bsn_inner_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_dst_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_dst_masked_t *
of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_eth_dst_masked_t *src)
{
    of_oxm_bsn_inner_eth_dst_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_dst_masked_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_masked_value_set(dst, mac_addr);

    of_oxm_bsn_inner_eth_dst_masked_value_mask_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_dst_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_src_t *
of_oxm_bsn_inner_eth_src_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_eth_src_t *src)
{
    of_oxm_bsn_inner_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_src_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_eth_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_eth_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_eth_src_masked_t *
of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_eth_src_masked_t *src)
{
    of_oxm_bsn_inner_eth_src_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_bsn_inner_eth_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_eth_src_masked_value_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_masked_value_set(dst, mac_addr);

    of_oxm_bsn_inner_eth_src_masked_value_mask_get(src, &mac_addr);
    of_oxm_bsn_inner_eth_src_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_vlan_vid_t *
of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_vlan_vid_t *src)
{
    of_oxm_bsn_inner_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_inner_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_vlan_vid_value_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_inner_vlan_vid_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_inner_vlan_vid_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_inner_vlan_vid_masked_t *
of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_inner_vlan_vid_masked_t *src)
{
    of_oxm_bsn_inner_vlan_vid_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_inner_vlan_vid_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_inner_vlan_vid_masked_value_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_masked_value_set(dst, val16);

    of_oxm_bsn_inner_vlan_vid_masked_value_mask_get(src, &val16);
    of_oxm_bsn_inner_vlan_vid_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_t *
of_oxm_bsn_ip_fragmentation_OF_VERSION_1_4_dup(
    of_oxm_bsn_ip_fragmentation_t *src)
{
    of_oxm_bsn_ip_fragmentation_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_ip_fragmentation_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_ip_fragmentation_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_ip_fragmentation_masked_t *
of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_ip_fragmentation_masked_t *src)
{
    of_oxm_bsn_ip_fragmentation_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_ip_fragmentation_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_ip_fragmentation_masked_value_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_set(dst, val8);

    of_oxm_bsn_ip_fragmentation_masked_value_mask_get(src, &val8);
    of_oxm_bsn_ip_fragmentation_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_t *
of_oxm_bsn_l2_cache_hit_OF_VERSION_1_4_dup(
    of_oxm_bsn_l2_cache_hit_t *src)
{
    of_oxm_bsn_l2_cache_hit_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l2_cache_hit_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l2_cache_hit_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l2_cache_hit_masked_t *
of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_l2_cache_hit_masked_t *src)
{
    of_oxm_bsn_l2_cache_hit_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_bsn_l2_cache_hit_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l2_cache_hit_masked_value_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_set(dst, val8);

    of_oxm_bsn_l2_cache_hit_masked_value_mask_get(src, &val8);
    of_oxm_bsn_l2_cache_hit_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_t *
of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_l3_interface_class_id_t *src)
{
    of_oxm_bsn_l3_interface_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_interface_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_interface_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_interface_class_id_masked_t *
of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_l3_interface_class_id_masked_t *src)
{
    of_oxm_bsn_l3_interface_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_interface_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_interface_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_interface_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_interface_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_t *
of_oxm_bsn_l3_src_class_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_l3_src_class_id_t *src)
{
    of_oxm_bsn_l3_src_class_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_l3_src_class_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_l3_src_class_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_l3_src_class_id_masked_t *
of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_l3_src_class_id_masked_t *src)
{
    of_oxm_bsn_l3_src_class_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_l3_src_class_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_l3_src_class_id_masked_value_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_set(dst, val32);

    of_oxm_bsn_l3_src_class_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_l3_src_class_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_t *
of_oxm_bsn_lag_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_lag_id_t *src)
{
    of_oxm_bsn_lag_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_value_get(src, &val32);
    of_oxm_bsn_lag_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_lag_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_lag_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_lag_id_masked_t *
of_oxm_bsn_lag_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_lag_id_masked_t *src)
{
    of_oxm_bsn_lag_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_lag_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_lag_id_masked_value_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_set(dst, val32);

    of_oxm_bsn_lag_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_lag_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_t *
of_oxm_bsn_tcp_flags_OF_VERSION_1_4_dup(
    of_oxm_bsn_tcp_flags_t *src)
{
    of_oxm_bsn_tcp_flags_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_tcp_flags_masked_t *
of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_tcp_flags_masked_t *src)
{
    of_oxm_bsn_tcp_flags_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_tcp_flags_masked_value_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_set(dst, val16);

    of_oxm_bsn_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_bsn_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_t *
of_oxm_bsn_udf0_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf0_t *src)
{
    of_oxm_bsn_udf0_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_value_get(src, &val32);
    of_oxm_bsn_udf0_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf0_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf0_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf0_masked_t *
of_oxm_bsn_udf0_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf0_masked_t *src)
{
    of_oxm_bsn_udf0_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf0_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf0_masked_value_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_set(dst, val32);

    of_oxm_bsn_udf0_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf0_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_t *
of_oxm_bsn_udf1_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf1_t *src)
{
    of_oxm_bsn_udf1_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_value_get(src, &val32);
    of_oxm_bsn_udf1_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf1_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf1_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf1_masked_t *
of_oxm_bsn_udf1_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf1_masked_t *src)
{
    of_oxm_bsn_udf1_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf1_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf1_masked_value_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_set(dst, val32);

    of_oxm_bsn_udf1_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf1_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_t *
of_oxm_bsn_udf2_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf2_t *src)
{
    of_oxm_bsn_udf2_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_value_get(src, &val32);
    of_oxm_bsn_udf2_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf2_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf2_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf2_masked_t *
of_oxm_bsn_udf2_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf2_masked_t *src)
{
    of_oxm_bsn_udf2_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf2_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf2_masked_value_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_set(dst, val32);

    of_oxm_bsn_udf2_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf2_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_t *
of_oxm_bsn_udf3_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf3_t *src)
{
    of_oxm_bsn_udf3_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_value_get(src, &val32);
    of_oxm_bsn_udf3_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf3_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf3_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf3_masked_t *
of_oxm_bsn_udf3_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf3_masked_t *src)
{
    of_oxm_bsn_udf3_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf3_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf3_masked_value_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_set(dst, val32);

    of_oxm_bsn_udf3_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf3_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_t *
of_oxm_bsn_udf4_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf4_t *src)
{
    of_oxm_bsn_udf4_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_value_get(src, &val32);
    of_oxm_bsn_udf4_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf4_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf4_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf4_masked_t *
of_oxm_bsn_udf4_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf4_masked_t *src)
{
    of_oxm_bsn_udf4_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf4_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf4_masked_value_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_set(dst, val32);

    of_oxm_bsn_udf4_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf4_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_t *
of_oxm_bsn_udf5_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf5_t *src)
{
    of_oxm_bsn_udf5_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_value_get(src, &val32);
    of_oxm_bsn_udf5_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf5_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf5_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf5_masked_t *
of_oxm_bsn_udf5_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf5_masked_t *src)
{
    of_oxm_bsn_udf5_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf5_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf5_masked_value_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_set(dst, val32);

    of_oxm_bsn_udf5_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf5_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_t *
of_oxm_bsn_udf6_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf6_t *src)
{
    of_oxm_bsn_udf6_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_value_get(src, &val32);
    of_oxm_bsn_udf6_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf6_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf6_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf6_masked_t *
of_oxm_bsn_udf6_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf6_masked_t *src)
{
    of_oxm_bsn_udf6_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf6_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf6_masked_value_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_set(dst, val32);

    of_oxm_bsn_udf6_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf6_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_t *
of_oxm_bsn_udf7_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf7_t *src)
{
    of_oxm_bsn_udf7_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_value_get(src, &val32);
    of_oxm_bsn_udf7_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_udf7_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_udf7_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_udf7_masked_t *
of_oxm_bsn_udf7_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_udf7_masked_t *src)
{
    of_oxm_bsn_udf7_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_udf7_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_udf7_masked_value_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_set(dst, val32);

    of_oxm_bsn_udf7_masked_value_mask_get(src, &val32);
    of_oxm_bsn_udf7_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vfi
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vfi.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vfi_t *
of_oxm_bsn_vfi_OF_VERSION_1_4_dup(
    of_oxm_bsn_vfi_t *src)
{
    of_oxm_bsn_vfi_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_vfi_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vfi_value_get(src, &val16);
    of_oxm_bsn_vfi_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vfi_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vfi_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vfi_masked_t *
of_oxm_bsn_vfi_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_vfi_masked_t *src)
{
    of_oxm_bsn_vfi_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_bsn_vfi_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vfi_masked_value_get(src, &val16);
    of_oxm_bsn_vfi_masked_value_set(dst, val16);

    of_oxm_bsn_vfi_masked_value_mask_get(src, &val16);
    of_oxm_bsn_vfi_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_t *
of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vlan_xlate_port_group_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vlan_xlate_port_group_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vlan_xlate_port_group_id_masked_t *
of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *src)
{
    of_oxm_bsn_vlan_xlate_port_group_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vlan_xlate_port_group_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_set(dst, val32);

    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vlan_xlate_port_group_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_t *
of_oxm_bsn_vrf_OF_VERSION_1_4_dup(
    of_oxm_bsn_vrf_t *src)
{
    of_oxm_bsn_vrf_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_value_get(src, &val32);
    of_oxm_bsn_vrf_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vrf_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vrf_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vrf_masked_t *
of_oxm_bsn_vrf_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_vrf_masked_t *src)
{
    of_oxm_bsn_vrf_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vrf_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vrf_masked_value_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_set(dst, val32);

    of_oxm_bsn_vrf_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vrf_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vxlan_network_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vxlan_network_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vxlan_network_id_t *
of_oxm_bsn_vxlan_network_id_OF_VERSION_1_4_dup(
    of_oxm_bsn_vxlan_network_id_t *src)
{
    of_oxm_bsn_vxlan_network_id_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vxlan_network_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vxlan_network_id_value_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_bsn_vxlan_network_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_bsn_vxlan_network_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_bsn_vxlan_network_id_masked_t *
of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_4_dup(
    of_oxm_bsn_vxlan_network_id_masked_t *src)
{
    of_oxm_bsn_vxlan_network_id_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_bsn_vxlan_network_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_bsn_vxlan_network_id_masked_value_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_masked_value_set(dst, val32);

    of_oxm_bsn_vxlan_network_id_masked_value_mask_get(src, &val32);
    of_oxm_bsn_vxlan_network_id_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_t *
of_oxm_eth_dst_OF_VERSION_1_4_dup(
    of_oxm_eth_dst_t *src)
{
    of_oxm_eth_dst_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_value_get(src, &mac_addr);
    of_oxm_eth_dst_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_dst_masked_t *
of_oxm_eth_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_eth_dst_masked_t *src)
{
    of_oxm_eth_dst_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_dst_masked_value_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_set(dst, mac_addr);

    of_oxm_eth_dst_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_dst_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_t *
of_oxm_eth_src_OF_VERSION_1_4_dup(
    of_oxm_eth_src_t *src)
{
    of_oxm_eth_src_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_value_get(src, &mac_addr);
    of_oxm_eth_src_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_src_masked_t *
of_oxm_eth_src_masked_OF_VERSION_1_4_dup(
    of_oxm_eth_src_masked_t *src)
{
    of_oxm_eth_src_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_eth_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_src_masked_value_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_set(dst, mac_addr);

    of_oxm_eth_src_masked_value_mask_get(src, &mac_addr);
    of_oxm_eth_src_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_t *
of_oxm_eth_type_OF_VERSION_1_4_dup(
    of_oxm_eth_type_t *src)
{
    of_oxm_eth_type_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_value_get(src, &val16);
    of_oxm_eth_type_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_eth_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_eth_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_eth_type_masked_t *
of_oxm_eth_type_masked_OF_VERSION_1_4_dup(
    of_oxm_eth_type_masked_t *src)
{
    of_oxm_eth_type_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_eth_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_eth_type_masked_value_get(src, &val16);
    of_oxm_eth_type_masked_value_set(dst, val16);

    of_oxm_eth_type_masked_value_mask_get(src, &val16);
    of_oxm_eth_type_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_t *
of_oxm_icmpv4_code_OF_VERSION_1_4_dup(
    of_oxm_icmpv4_code_t *src)
{
    of_oxm_icmpv4_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_value_get(src, &val8);
    of_oxm_icmpv4_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_code_masked_t *
of_oxm_icmpv4_code_masked_OF_VERSION_1_4_dup(
    of_oxm_icmpv4_code_masked_t *src)
{
    of_oxm_icmpv4_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_code_masked_value_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_set(dst, val8);

    of_oxm_icmpv4_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_t *
of_oxm_icmpv4_type_OF_VERSION_1_4_dup(
    of_oxm_icmpv4_type_t *src)
{
    of_oxm_icmpv4_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_value_get(src, &val8);
    of_oxm_icmpv4_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv4_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv4_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv4_type_masked_t *
of_oxm_icmpv4_type_masked_OF_VERSION_1_4_dup(
    of_oxm_icmpv4_type_masked_t *src)
{
    of_oxm_icmpv4_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv4_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv4_type_masked_value_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_set(dst, val8);

    of_oxm_icmpv4_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv4_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_t *
of_oxm_icmpv6_code_OF_VERSION_1_4_dup(
    of_oxm_icmpv6_code_t *src)
{
    of_oxm_icmpv6_code_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_value_get(src, &val8);
    of_oxm_icmpv6_code_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_code_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_code_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_code_masked_t *
of_oxm_icmpv6_code_masked_OF_VERSION_1_4_dup(
    of_oxm_icmpv6_code_masked_t *src)
{
    of_oxm_icmpv6_code_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_code_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_code_masked_value_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_set(dst, val8);

    of_oxm_icmpv6_code_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_code_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_t *
of_oxm_icmpv6_type_OF_VERSION_1_4_dup(
    of_oxm_icmpv6_type_t *src)
{
    of_oxm_icmpv6_type_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_value_get(src, &val8);
    of_oxm_icmpv6_type_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_icmpv6_type_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_icmpv6_type_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_icmpv6_type_masked_t *
of_oxm_icmpv6_type_masked_OF_VERSION_1_4_dup(
    of_oxm_icmpv6_type_masked_t *src)
{
    of_oxm_icmpv6_type_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_icmpv6_type_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_icmpv6_type_masked_value_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_set(dst, val8);

    of_oxm_icmpv6_type_masked_value_mask_get(src, &val8);
    of_oxm_icmpv6_type_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_t *
of_oxm_in_phy_port_OF_VERSION_1_4_dup(
    of_oxm_in_phy_port_t *src)
{
    of_oxm_in_phy_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_value_get(src, &port_no);
    of_oxm_in_phy_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_phy_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_phy_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_phy_port_masked_t *
of_oxm_in_phy_port_masked_OF_VERSION_1_4_dup(
    of_oxm_in_phy_port_masked_t *src)
{
    of_oxm_in_phy_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_phy_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_phy_port_masked_value_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_set(dst, port_no);

    of_oxm_in_phy_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_phy_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_t *
of_oxm_in_port_OF_VERSION_1_4_dup(
    of_oxm_in_port_t *src)
{
    of_oxm_in_port_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_value_get(src, &port_no);
    of_oxm_in_port_value_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_in_port_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_in_port_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_in_port_masked_t *
of_oxm_in_port_masked_OF_VERSION_1_4_dup(
    of_oxm_in_port_masked_t *src)
{
    of_oxm_in_port_masked_t *dst;
    of_port_no_t port_no;

    if ((dst = of_oxm_in_port_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_in_port_masked_value_get(src, &port_no);
    of_oxm_in_port_masked_value_set(dst, port_no);

    of_oxm_in_port_masked_value_mask_get(src, &port_no);
    of_oxm_in_port_masked_value_mask_set(dst, port_no);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_t *
of_oxm_ip_dscp_OF_VERSION_1_4_dup(
    of_oxm_ip_dscp_t *src)
{
    of_oxm_ip_dscp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_value_get(src, &val8);
    of_oxm_ip_dscp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_dscp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_dscp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_dscp_masked_t *
of_oxm_ip_dscp_masked_OF_VERSION_1_4_dup(
    of_oxm_ip_dscp_masked_t *src)
{
    of_oxm_ip_dscp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_dscp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_dscp_masked_value_get(src, &val8);
    of_oxm_ip_dscp_masked_value_set(dst, val8);

    of_oxm_ip_dscp_masked_value_mask_get(src, &val8);
    of_oxm_ip_dscp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_t *
of_oxm_ip_ecn_OF_VERSION_1_4_dup(
    of_oxm_ip_ecn_t *src)
{
    of_oxm_ip_ecn_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_value_get(src, &val8);
    of_oxm_ip_ecn_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_ecn_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_ecn_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_ecn_masked_t *
of_oxm_ip_ecn_masked_OF_VERSION_1_4_dup(
    of_oxm_ip_ecn_masked_t *src)
{
    of_oxm_ip_ecn_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_ecn_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_ecn_masked_value_get(src, &val8);
    of_oxm_ip_ecn_masked_value_set(dst, val8);

    of_oxm_ip_ecn_masked_value_mask_get(src, &val8);
    of_oxm_ip_ecn_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_t *
of_oxm_ip_proto_OF_VERSION_1_4_dup(
    of_oxm_ip_proto_t *src)
{
    of_oxm_ip_proto_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_value_get(src, &val8);
    of_oxm_ip_proto_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ip_proto_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ip_proto_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ip_proto_masked_t *
of_oxm_ip_proto_masked_OF_VERSION_1_4_dup(
    of_oxm_ip_proto_masked_t *src)
{
    of_oxm_ip_proto_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_ip_proto_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ip_proto_masked_value_get(src, &val8);
    of_oxm_ip_proto_masked_value_set(dst, val8);

    of_oxm_ip_proto_masked_value_mask_get(src, &val8);
    of_oxm_ip_proto_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_t *
of_oxm_ipv4_dst_OF_VERSION_1_4_dup(
    of_oxm_ipv4_dst_t *src)
{
    of_oxm_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_value_get(src, &ipv4);
    of_oxm_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_dst_masked_t *
of_oxm_ipv4_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv4_dst_masked_t *src)
{
    of_oxm_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_t *
of_oxm_ipv4_src_OF_VERSION_1_4_dup(
    of_oxm_ipv4_src_t *src)
{
    of_oxm_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_value_get(src, &ipv4);
    of_oxm_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv4_src_masked_t *
of_oxm_ipv4_src_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv4_src_masked_t *src)
{
    of_oxm_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_t *
of_oxm_ipv6_dst_OF_VERSION_1_4_dup(
    of_oxm_ipv6_dst_t *src)
{
    of_oxm_ipv6_dst_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_value_get(src, &ipv6);
    of_oxm_ipv6_dst_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_dst_masked_t *
of_oxm_ipv6_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_dst_masked_t *src)
{
    of_oxm_ipv6_dst_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_dst_masked_value_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_set(dst, ipv6);

    of_oxm_ipv6_dst_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_dst_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_exthdr
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_exthdr.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_exthdr_t *
of_oxm_ipv6_exthdr_OF_VERSION_1_4_dup(
    of_oxm_ipv6_exthdr_t *src)
{
    of_oxm_ipv6_exthdr_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_ipv6_exthdr_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_exthdr_value_get(src, &val16);
    of_oxm_ipv6_exthdr_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_exthdr_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_exthdr_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_exthdr_masked_t *
of_oxm_ipv6_exthdr_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_exthdr_masked_t *src)
{
    of_oxm_ipv6_exthdr_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_ipv6_exthdr_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_exthdr_masked_value_get(src, &val16);
    of_oxm_ipv6_exthdr_masked_value_set(dst, val16);

    of_oxm_ipv6_exthdr_masked_value_mask_get(src, &val16);
    of_oxm_ipv6_exthdr_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_t *
of_oxm_ipv6_flabel_OF_VERSION_1_4_dup(
    of_oxm_ipv6_flabel_t *src)
{
    of_oxm_ipv6_flabel_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_value_get(src, &val32);
    of_oxm_ipv6_flabel_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_flabel_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_flabel_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_flabel_masked_t *
of_oxm_ipv6_flabel_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_flabel_masked_t *src)
{
    of_oxm_ipv6_flabel_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_ipv6_flabel_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_flabel_masked_value_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_set(dst, val32);

    of_oxm_ipv6_flabel_masked_value_mask_get(src, &val32);
    of_oxm_ipv6_flabel_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_t *
of_oxm_ipv6_nd_sll_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_sll_t *src)
{
    of_oxm_ipv6_nd_sll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_sll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_sll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_sll_masked_t *
of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_sll_masked_t *src)
{
    of_oxm_ipv6_nd_sll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_sll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_sll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_sll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_sll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_t *
of_oxm_ipv6_nd_target_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_target_t *src)
{
    of_oxm_ipv6_nd_target_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_target_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_target_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_target_masked_t *
of_oxm_ipv6_nd_target_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_target_masked_t *src)
{
    of_oxm_ipv6_nd_target_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_nd_target_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_target_masked_value_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_set(dst, ipv6);

    of_oxm_ipv6_nd_target_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_nd_target_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_t *
of_oxm_ipv6_nd_tll_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_tll_t *src)
{
    of_oxm_ipv6_nd_tll_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_value_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_nd_tll_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_nd_tll_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_nd_tll_masked_t *
of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_nd_tll_masked_t *src)
{
    of_oxm_ipv6_nd_tll_masked_t *dst;
    of_mac_addr_t mac_addr;

    if ((dst = of_oxm_ipv6_nd_tll_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_nd_tll_masked_value_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_set(dst, mac_addr);

    of_oxm_ipv6_nd_tll_masked_value_mask_get(src, &mac_addr);
    of_oxm_ipv6_nd_tll_masked_value_mask_set(dst, mac_addr);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_t *
of_oxm_ipv6_src_OF_VERSION_1_4_dup(
    of_oxm_ipv6_src_t *src)
{
    of_oxm_ipv6_src_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_value_get(src, &ipv6);
    of_oxm_ipv6_src_value_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ipv6_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ipv6_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ipv6_src_masked_t *
of_oxm_ipv6_src_masked_OF_VERSION_1_4_dup(
    of_oxm_ipv6_src_masked_t *src)
{
    of_oxm_ipv6_src_masked_t *dst;
    of_ipv6_t ipv6;

    if ((dst = of_oxm_ipv6_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ipv6_src_masked_value_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_set(dst, ipv6);

    of_oxm_ipv6_src_masked_value_mask_get(src, &ipv6);
    of_oxm_ipv6_src_masked_value_mask_set(dst, ipv6);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_t *
of_oxm_metadata_OF_VERSION_1_4_dup(
    of_oxm_metadata_t *src)
{
    of_oxm_metadata_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_value_get(src, &val64);
    of_oxm_metadata_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_metadata_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_metadata_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_metadata_masked_t *
of_oxm_metadata_masked_OF_VERSION_1_4_dup(
    of_oxm_metadata_masked_t *src)
{
    of_oxm_metadata_masked_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_metadata_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_metadata_masked_value_get(src, &val64);
    of_oxm_metadata_masked_value_set(dst, val64);

    of_oxm_metadata_masked_value_mask_get(src, &val64);
    of_oxm_metadata_masked_value_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_bos
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_bos.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_bos_t *
of_oxm_mpls_bos_OF_VERSION_1_4_dup(
    of_oxm_mpls_bos_t *src)
{
    of_oxm_mpls_bos_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_bos_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_bos_value_get(src, &val8);
    of_oxm_mpls_bos_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_bos_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_bos_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_bos_masked_t *
of_oxm_mpls_bos_masked_OF_VERSION_1_4_dup(
    of_oxm_mpls_bos_masked_t *src)
{
    of_oxm_mpls_bos_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_bos_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_bos_masked_value_get(src, &val8);
    of_oxm_mpls_bos_masked_value_set(dst, val8);

    of_oxm_mpls_bos_masked_value_mask_get(src, &val8);
    of_oxm_mpls_bos_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_t *
of_oxm_mpls_label_OF_VERSION_1_4_dup(
    of_oxm_mpls_label_t *src)
{
    of_oxm_mpls_label_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_value_get(src, &val32);
    of_oxm_mpls_label_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_label_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_label_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_label_masked_t *
of_oxm_mpls_label_masked_OF_VERSION_1_4_dup(
    of_oxm_mpls_label_masked_t *src)
{
    of_oxm_mpls_label_masked_t *dst;
    uint32_t val32;

    if ((dst = of_oxm_mpls_label_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_label_masked_value_get(src, &val32);
    of_oxm_mpls_label_masked_value_set(dst, val32);

    of_oxm_mpls_label_masked_value_mask_get(src, &val32);
    of_oxm_mpls_label_masked_value_mask_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_t *
of_oxm_mpls_tc_OF_VERSION_1_4_dup(
    of_oxm_mpls_tc_t *src)
{
    of_oxm_mpls_tc_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_value_get(src, &val8);
    of_oxm_mpls_tc_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_mpls_tc_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_mpls_tc_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_mpls_tc_masked_t *
of_oxm_mpls_tc_masked_OF_VERSION_1_4_dup(
    of_oxm_mpls_tc_masked_t *src)
{
    of_oxm_mpls_tc_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_mpls_tc_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_mpls_tc_masked_value_get(src, &val8);
    of_oxm_mpls_tc_masked_value_set(dst, val8);

    of_oxm_mpls_tc_masked_value_mask_get(src, &val8);
    of_oxm_mpls_tc_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_t *
of_oxm_ovs_tcp_flags_OF_VERSION_1_4_dup(
    of_oxm_ovs_tcp_flags_t *src)
{
    of_oxm_ovs_tcp_flags_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_ovs_tcp_flags_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_ovs_tcp_flags_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_ovs_tcp_flags_masked_t *
of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_4_dup(
    of_oxm_ovs_tcp_flags_masked_t *src)
{
    of_oxm_ovs_tcp_flags_masked_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_oxm_ovs_tcp_flags_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_ovs_tcp_flags_masked_experimenter_id_get(src, &val32);
    of_oxm_ovs_tcp_flags_masked_experimenter_id_set(dst, val32);

    of_oxm_ovs_tcp_flags_masked_value_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_set(dst, val16);

    of_oxm_ovs_tcp_flags_masked_value_mask_get(src, &val16);
    of_oxm_ovs_tcp_flags_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_pbb_uca
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_pbb_uca.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_pbb_uca_t *
of_oxm_pbb_uca_OF_VERSION_1_4_dup(
    of_oxm_pbb_uca_t *src)
{
    of_oxm_pbb_uca_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_pbb_uca_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_pbb_uca_value_get(src, &val8);
    of_oxm_pbb_uca_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_pbb_uca_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_pbb_uca_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_pbb_uca_masked_t *
of_oxm_pbb_uca_masked_OF_VERSION_1_4_dup(
    of_oxm_pbb_uca_masked_t *src)
{
    of_oxm_pbb_uca_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_pbb_uca_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_pbb_uca_masked_value_get(src, &val8);
    of_oxm_pbb_uca_masked_value_set(dst, val8);

    of_oxm_pbb_uca_masked_value_mask_get(src, &val8);
    of_oxm_pbb_uca_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_t *
of_oxm_sctp_dst_OF_VERSION_1_4_dup(
    of_oxm_sctp_dst_t *src)
{
    of_oxm_sctp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_value_get(src, &val16);
    of_oxm_sctp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_dst_masked_t *
of_oxm_sctp_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_sctp_dst_masked_t *src)
{
    of_oxm_sctp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_dst_masked_value_get(src, &val16);
    of_oxm_sctp_dst_masked_value_set(dst, val16);

    of_oxm_sctp_dst_masked_value_mask_get(src, &val16);
    of_oxm_sctp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_t *
of_oxm_sctp_src_OF_VERSION_1_4_dup(
    of_oxm_sctp_src_t *src)
{
    of_oxm_sctp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_value_get(src, &val16);
    of_oxm_sctp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_sctp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_sctp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_sctp_src_masked_t *
of_oxm_sctp_src_masked_OF_VERSION_1_4_dup(
    of_oxm_sctp_src_masked_t *src)
{
    of_oxm_sctp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_sctp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_sctp_src_masked_value_get(src, &val16);
    of_oxm_sctp_src_masked_value_set(dst, val16);

    of_oxm_sctp_src_masked_value_mask_get(src, &val16);
    of_oxm_sctp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_t *
of_oxm_tcp_dst_OF_VERSION_1_4_dup(
    of_oxm_tcp_dst_t *src)
{
    of_oxm_tcp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_value_get(src, &val16);
    of_oxm_tcp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_dst_masked_t *
of_oxm_tcp_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_tcp_dst_masked_t *src)
{
    of_oxm_tcp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_dst_masked_value_get(src, &val16);
    of_oxm_tcp_dst_masked_value_set(dst, val16);

    of_oxm_tcp_dst_masked_value_mask_get(src, &val16);
    of_oxm_tcp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_t *
of_oxm_tcp_src_OF_VERSION_1_4_dup(
    of_oxm_tcp_src_t *src)
{
    of_oxm_tcp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_value_get(src, &val16);
    of_oxm_tcp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tcp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tcp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tcp_src_masked_t *
of_oxm_tcp_src_masked_OF_VERSION_1_4_dup(
    of_oxm_tcp_src_masked_t *src)
{
    of_oxm_tcp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_tcp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tcp_src_masked_value_get(src, &val16);
    of_oxm_tcp_src_masked_value_set(dst, val16);

    of_oxm_tcp_src_masked_value_mask_get(src, &val16);
    of_oxm_tcp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_id_t *
of_oxm_tunnel_id_OF_VERSION_1_4_dup(
    of_oxm_tunnel_id_t *src)
{
    of_oxm_tunnel_id_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_tunnel_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_id_value_get(src, &val64);
    of_oxm_tunnel_id_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_id_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_id_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_id_masked_t *
of_oxm_tunnel_id_masked_OF_VERSION_1_4_dup(
    of_oxm_tunnel_id_masked_t *src)
{
    of_oxm_tunnel_id_masked_t *dst;
    uint64_t val64;

    if ((dst = of_oxm_tunnel_id_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_id_masked_value_get(src, &val64);
    of_oxm_tunnel_id_masked_value_set(dst, val64);

    of_oxm_tunnel_id_masked_value_mask_get(src, &val64);
    of_oxm_tunnel_id_masked_value_mask_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_t *
of_oxm_tunnel_ipv4_dst_OF_VERSION_1_4_dup(
    of_oxm_tunnel_ipv4_dst_t *src)
{
    of_oxm_tunnel_ipv4_dst_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_dst_masked_t *
of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_tunnel_ipv4_dst_masked_t *src)
{
    of_oxm_tunnel_ipv4_dst_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_dst_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_dst_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_dst_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_t *
of_oxm_tunnel_ipv4_src_OF_VERSION_1_4_dup(
    of_oxm_tunnel_ipv4_src_t *src)
{
    of_oxm_tunnel_ipv4_src_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_value_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_tunnel_ipv4_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_tunnel_ipv4_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_tunnel_ipv4_src_masked_t *
of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_4_dup(
    of_oxm_tunnel_ipv4_src_masked_t *src)
{
    of_oxm_tunnel_ipv4_src_masked_t *dst;
    of_ipv4_t ipv4;

    if ((dst = of_oxm_tunnel_ipv4_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_tunnel_ipv4_src_masked_value_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_set(dst, ipv4);

    of_oxm_tunnel_ipv4_src_masked_value_mask_get(src, &ipv4);
    of_oxm_tunnel_ipv4_src_masked_value_mask_set(dst, ipv4);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_t *
of_oxm_udp_dst_OF_VERSION_1_4_dup(
    of_oxm_udp_dst_t *src)
{
    of_oxm_udp_dst_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_value_get(src, &val16);
    of_oxm_udp_dst_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_dst_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_dst_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_dst_masked_t *
of_oxm_udp_dst_masked_OF_VERSION_1_4_dup(
    of_oxm_udp_dst_masked_t *src)
{
    of_oxm_udp_dst_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_dst_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_dst_masked_value_get(src, &val16);
    of_oxm_udp_dst_masked_value_set(dst, val16);

    of_oxm_udp_dst_masked_value_mask_get(src, &val16);
    of_oxm_udp_dst_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_t *
of_oxm_udp_src_OF_VERSION_1_4_dup(
    of_oxm_udp_src_t *src)
{
    of_oxm_udp_src_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_value_get(src, &val16);
    of_oxm_udp_src_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_udp_src_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_udp_src_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_udp_src_masked_t *
of_oxm_udp_src_masked_OF_VERSION_1_4_dup(
    of_oxm_udp_src_masked_t *src)
{
    of_oxm_udp_src_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_udp_src_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_udp_src_masked_value_get(src, &val16);
    of_oxm_udp_src_masked_value_set(dst, val16);

    of_oxm_udp_src_masked_value_mask_get(src, &val16);
    of_oxm_udp_src_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_t *
of_oxm_vlan_pcp_OF_VERSION_1_4_dup(
    of_oxm_vlan_pcp_t *src)
{
    of_oxm_vlan_pcp_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_value_get(src, &val8);
    of_oxm_vlan_pcp_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_pcp_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_pcp_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_pcp_masked_t *
of_oxm_vlan_pcp_masked_OF_VERSION_1_4_dup(
    of_oxm_vlan_pcp_masked_t *src)
{
    of_oxm_vlan_pcp_masked_t *dst;
    uint8_t val8;

    if ((dst = of_oxm_vlan_pcp_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_pcp_masked_value_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_set(dst, val8);

    of_oxm_vlan_pcp_masked_value_mask_get(src, &val8);
    of_oxm_vlan_pcp_masked_value_mask_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_t *
of_oxm_vlan_vid_OF_VERSION_1_4_dup(
    of_oxm_vlan_vid_t *src)
{
    of_oxm_vlan_vid_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_value_get(src, &val16);
    of_oxm_vlan_vid_value_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_oxm_vlan_vid_masked
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_oxm_vlan_vid_masked.
 *
 * The caller is responsible for deleting the returned value
 */
of_oxm_vlan_vid_masked_t *
of_oxm_vlan_vid_masked_OF_VERSION_1_4_dup(
    of_oxm_vlan_vid_masked_t *src)
{
    of_oxm_vlan_vid_masked_t *dst;
    uint16_t val16;

    if ((dst = of_oxm_vlan_vid_masked_new(src->version)) == NULL) {
        return NULL;
    }

    of_oxm_vlan_vid_masked_value_get(src, &val16);
    of_oxm_vlan_vid_masked_value_set(dst, val16);

    of_oxm_vlan_vid_masked_value_mask_get(src, &val16);
    of_oxm_vlan_vid_masked_value_mask_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_packet_queue
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_packet_queue.
 *
 * The caller is responsible for deleting the returned value
 */
of_packet_queue_t *
of_packet_queue_OF_VERSION_1_4_dup(
    of_packet_queue_t *src)
{
    of_packet_queue_t *dst;
    uint32_t val32;
    of_port_no_t port_no;

    of_list_queue_prop_t src_list;
    of_list_queue_prop_t *dst_list;

    if ((dst = of_packet_queue_new(src->version)) == NULL) {
        return NULL;
    }

    of_packet_queue_queue_id_get(src, &val32);
    of_packet_queue_queue_id_set(dst, val32);

    of_packet_queue_port_get(src, &port_no);
    of_packet_queue_port_set(dst, port_no);

    of_packet_queue_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_packet_queue_delete(dst);
        return NULL;
    }
    of_packet_queue_properties_set(dst, dst_list);
    of_list_queue_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_t *
of_port_desc_OF_VERSION_1_4_dup(
    of_port_desc_t *src)
{
    of_port_desc_t *dst;
    of_port_no_t port_no;
    of_mac_addr_t mac_addr;
    of_port_name_t port_name;
    uint32_t val32;

    of_list_port_desc_prop_t src_list;
    of_list_port_desc_prop_t *dst_list;

    if ((dst = of_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_port_no_get(src, &port_no);
    of_port_desc_port_no_set(dst, port_no);

    of_port_desc_hw_addr_get(src, &mac_addr);
    of_port_desc_hw_addr_set(dst, mac_addr);

    of_port_desc_name_get(src, &port_name);
    of_port_desc_name_set(dst, port_name);

    of_port_desc_config_get(src, &val32);
    of_port_desc_config_set(dst, val32);

    of_port_desc_state_get(src, &val32);
    of_port_desc_state_set(dst, val32);

    of_port_desc_properties_bind(
        src, &src_list);
    dst_list = of_list_port_desc_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_port_desc_delete(dst);
        return NULL;
    }
    of_port_desc_properties_set(dst, dst_list);
    of_list_port_desc_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_port_desc_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_port_desc_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_PORT_DESC_PROP_ETHERNET) {
        return of_port_desc_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_DESC_PROP_OPTICAL) {
        return of_port_desc_prop_optical_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_DESC_PROP_BSN_GENERATION_ID) {
        return of_port_desc_prop_bsn_generation_id_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_DESC_PROP_BSN_UPLINK) {
        return of_port_desc_prop_bsn_uplink_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_DESC_PROP_BSN_FORWARD_ERROR_CORRECTION) {
        return of_port_desc_prop_bsn_forward_error_correction_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_port_desc_prop_bsn_forward_error_correction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop_bsn_forward_error_correction.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_prop_bsn_forward_error_correction_t *
of_port_desc_prop_bsn_forward_error_correction_OF_VERSION_1_4_dup(
    of_port_desc_prop_bsn_forward_error_correction_t *src)
{
    of_port_desc_prop_bsn_forward_error_correction_t *dst;
    uint32_t val32;

    if ((dst = of_port_desc_prop_bsn_forward_error_correction_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_prop_bsn_forward_error_correction_experimenter_get(src, &val32);
    of_port_desc_prop_bsn_forward_error_correction_experimenter_set(dst, val32);

    of_port_desc_prop_bsn_forward_error_correction_exp_type_get(src, &val32);
    of_port_desc_prop_bsn_forward_error_correction_exp_type_set(dst, val32);

    of_port_desc_prop_bsn_forward_error_correction_configured_get(src, &val32);
    of_port_desc_prop_bsn_forward_error_correction_configured_set(dst, val32);

    of_port_desc_prop_bsn_forward_error_correction_enabled_get(src, &val32);
    of_port_desc_prop_bsn_forward_error_correction_enabled_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_prop_bsn_generation_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop_bsn_generation_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_prop_bsn_generation_id_t *
of_port_desc_prop_bsn_generation_id_OF_VERSION_1_4_dup(
    of_port_desc_prop_bsn_generation_id_t *src)
{
    of_port_desc_prop_bsn_generation_id_t *dst;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_port_desc_prop_bsn_generation_id_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_prop_bsn_generation_id_experimenter_get(src, &val32);
    of_port_desc_prop_bsn_generation_id_experimenter_set(dst, val32);

    of_port_desc_prop_bsn_generation_id_exp_type_get(src, &val32);
    of_port_desc_prop_bsn_generation_id_exp_type_set(dst, val32);

    of_port_desc_prop_bsn_generation_id_generation_id_get(src, &val64);
    of_port_desc_prop_bsn_generation_id_generation_id_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_prop_bsn_uplink
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop_bsn_uplink.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_prop_bsn_uplink_t *
of_port_desc_prop_bsn_uplink_OF_VERSION_1_4_dup(
    of_port_desc_prop_bsn_uplink_t *src)
{
    of_port_desc_prop_bsn_uplink_t *dst;
    uint32_t val32;

    if ((dst = of_port_desc_prop_bsn_uplink_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_prop_bsn_uplink_experimenter_get(src, &val32);
    of_port_desc_prop_bsn_uplink_experimenter_set(dst, val32);

    of_port_desc_prop_bsn_uplink_exp_type_get(src, &val32);
    of_port_desc_prop_bsn_uplink_exp_type_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_prop_ethernet
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop_ethernet.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_prop_ethernet_t *
of_port_desc_prop_ethernet_OF_VERSION_1_4_dup(
    of_port_desc_prop_ethernet_t *src)
{
    of_port_desc_prop_ethernet_t *dst;
    uint32_t val32;

    if ((dst = of_port_desc_prop_ethernet_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_prop_ethernet_curr_get(src, &val32);
    of_port_desc_prop_ethernet_curr_set(dst, val32);

    of_port_desc_prop_ethernet_advertised_get(src, &val32);
    of_port_desc_prop_ethernet_advertised_set(dst, val32);

    of_port_desc_prop_ethernet_supported_get(src, &val32);
    of_port_desc_prop_ethernet_supported_set(dst, val32);

    of_port_desc_prop_ethernet_peer_get(src, &val32);
    of_port_desc_prop_ethernet_peer_set(dst, val32);

    of_port_desc_prop_ethernet_curr_speed_get(src, &val32);
    of_port_desc_prop_ethernet_curr_speed_set(dst, val32);

    of_port_desc_prop_ethernet_max_speed_get(src, &val32);
    of_port_desc_prop_ethernet_max_speed_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_desc_prop_optical
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_desc_prop_optical.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_desc_prop_optical_t *
of_port_desc_prop_optical_OF_VERSION_1_4_dup(
    of_port_desc_prop_optical_t *src)
{
    of_port_desc_prop_optical_t *dst;
    uint32_t val32;

    if ((dst = of_port_desc_prop_optical_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_desc_prop_optical_supported_get(src, &val32);
    of_port_desc_prop_optical_supported_set(dst, val32);

    of_port_desc_prop_optical_tx_min_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_tx_min_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_tx_max_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_tx_max_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_tx_grid_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_tx_grid_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_rx_min_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_rx_min_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_rx_max_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_rx_max_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_rx_grid_freq_lmda_get(src, &val32);
    of_port_desc_prop_optical_rx_grid_freq_lmda_set(dst, val32);

    of_port_desc_prop_optical_tx_pwr_min_get(src, &val32);
    of_port_desc_prop_optical_tx_pwr_min_set(dst, val32);

    of_port_desc_prop_optical_tx_pwr_max_get(src, &val32);
    of_port_desc_prop_optical_tx_pwr_max_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_port_mod_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_port_mod_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_PORT_MOD_PROP_ETHERNET) {
        return of_port_mod_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_MOD_PROP_OPTICAL) {
        return of_port_mod_prop_optical_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_port_mod_prop_ethernet
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_prop_ethernet.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_prop_ethernet_t *
of_port_mod_prop_ethernet_OF_VERSION_1_4_dup(
    of_port_mod_prop_ethernet_t *src)
{
    of_port_mod_prop_ethernet_t *dst;
    uint32_t val32;

    if ((dst = of_port_mod_prop_ethernet_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_prop_ethernet_advertise_get(src, &val32);
    of_port_mod_prop_ethernet_advertise_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_mod_prop_optical
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_mod_prop_optical.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_mod_prop_optical_t *
of_port_mod_prop_optical_OF_VERSION_1_4_dup(
    of_port_mod_prop_optical_t *src)
{
    of_port_mod_prop_optical_t *dst;
    uint32_t val32;

    if ((dst = of_port_mod_prop_optical_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_mod_prop_optical_configure_get(src, &val32);
    of_port_mod_prop_optical_configure_set(dst, val32);

    of_port_mod_prop_optical_freq_ldma_get(src, &val32);
    of_port_mod_prop_optical_freq_ldma_set(dst, val32);

    of_port_mod_prop_optical_fl_offset_get(src, &val32);
    of_port_mod_prop_optical_fl_offset_set(dst, val32);

    of_port_mod_prop_optical_grid_span_get(src, &val32);
    of_port_mod_prop_optical_grid_span_set(dst, val32);

    of_port_mod_prop_optical_tx_pwr_get(src, &val32);
    of_port_mod_prop_optical_tx_pwr_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_entry_t *
of_port_stats_entry_OF_VERSION_1_4_dup(
    of_port_stats_entry_t *src)
{
    of_port_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    of_list_port_stats_prop_t src_list;
    of_list_port_stats_prop_t *dst_list;

    if ((dst = of_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_entry_port_no_get(src, &port_no);
    of_port_stats_entry_port_no_set(dst, port_no);

    of_port_stats_entry_duration_sec_get(src, &val32);
    of_port_stats_entry_duration_sec_set(dst, val32);

    of_port_stats_entry_duration_nsec_get(src, &val32);
    of_port_stats_entry_duration_nsec_set(dst, val32);

    of_port_stats_entry_rx_packets_get(src, &val64);
    of_port_stats_entry_rx_packets_set(dst, val64);

    of_port_stats_entry_tx_packets_get(src, &val64);
    of_port_stats_entry_tx_packets_set(dst, val64);

    of_port_stats_entry_rx_bytes_get(src, &val64);
    of_port_stats_entry_rx_bytes_set(dst, val64);

    of_port_stats_entry_tx_bytes_get(src, &val64);
    of_port_stats_entry_tx_bytes_set(dst, val64);

    of_port_stats_entry_rx_dropped_get(src, &val64);
    of_port_stats_entry_rx_dropped_set(dst, val64);

    of_port_stats_entry_tx_dropped_get(src, &val64);
    of_port_stats_entry_tx_dropped_set(dst, val64);

    of_port_stats_entry_rx_errors_get(src, &val64);
    of_port_stats_entry_rx_errors_set(dst, val64);

    of_port_stats_entry_tx_errors_get(src, &val64);
    of_port_stats_entry_tx_errors_set(dst, val64);

    of_port_stats_entry_properties_bind(
        src, &src_list);
    dst_list = of_list_port_stats_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_port_stats_entry_delete(dst);
        return NULL;
    }
    of_port_stats_entry_properties_set(dst, dst_list);
    of_list_port_stats_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_port_stats_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_port_stats_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_PORT_STATS_PROP_ETHERNET) {
        return of_port_stats_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_PORT_STATS_PROP_OPTICAL) {
        return of_port_stats_prop_optical_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_port_stats_prop_ethernet
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_prop_ethernet.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_prop_ethernet_t *
of_port_stats_prop_ethernet_OF_VERSION_1_4_dup(
    of_port_stats_prop_ethernet_t *src)
{
    of_port_stats_prop_ethernet_t *dst;
    uint64_t val64;

    if ((dst = of_port_stats_prop_ethernet_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_prop_ethernet_rx_frame_err_get(src, &val64);
    of_port_stats_prop_ethernet_rx_frame_err_set(dst, val64);

    of_port_stats_prop_ethernet_rx_over_err_get(src, &val64);
    of_port_stats_prop_ethernet_rx_over_err_set(dst, val64);

    of_port_stats_prop_ethernet_rx_crc_err_get(src, &val64);
    of_port_stats_prop_ethernet_rx_crc_err_set(dst, val64);

    of_port_stats_prop_ethernet_collisions_get(src, &val64);
    of_port_stats_prop_ethernet_collisions_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_port_stats_prop_optical
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_port_stats_prop_optical.
 *
 * The caller is responsible for deleting the returned value
 */
of_port_stats_prop_optical_t *
of_port_stats_prop_optical_OF_VERSION_1_4_dup(
    of_port_stats_prop_optical_t *src)
{
    of_port_stats_prop_optical_t *dst;
    uint32_t val32;
    uint16_t val16;

    if ((dst = of_port_stats_prop_optical_new(src->version)) == NULL) {
        return NULL;
    }

    of_port_stats_prop_optical_flags_get(src, &val32);
    of_port_stats_prop_optical_flags_set(dst, val32);

    of_port_stats_prop_optical_tx_freq_lmda_get(src, &val32);
    of_port_stats_prop_optical_tx_freq_lmda_set(dst, val32);

    of_port_stats_prop_optical_tx_offset_get(src, &val32);
    of_port_stats_prop_optical_tx_offset_set(dst, val32);

    of_port_stats_prop_optical_tx_grid_span_get(src, &val32);
    of_port_stats_prop_optical_tx_grid_span_set(dst, val32);

    of_port_stats_prop_optical_rx_freq_lmda_get(src, &val32);
    of_port_stats_prop_optical_rx_freq_lmda_set(dst, val32);

    of_port_stats_prop_optical_rx_offset_get(src, &val32);
    of_port_stats_prop_optical_rx_offset_set(dst, val32);

    of_port_stats_prop_optical_rx_grid_span_get(src, &val32);
    of_port_stats_prop_optical_rx_grid_span_set(dst, val32);

    of_port_stats_prop_optical_tx_pwr_get(src, &val16);
    of_port_stats_prop_optical_tx_pwr_set(dst, val16);

    of_port_stats_prop_optical_rx_pwr_get(src, &val16);
    of_port_stats_prop_optical_rx_pwr_set(dst, val16);

    of_port_stats_prop_optical_bias_current_get(src, &val16);
    of_port_stats_prop_optical_bias_current_set(dst, val16);

    of_port_stats_prop_optical_temperature_get(src, &val16);
    of_port_stats_prop_optical_temperature_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_t *
of_queue_desc_OF_VERSION_1_4_dup(
    of_queue_desc_t *src)
{
    of_queue_desc_t *dst;
    uint32_t val32;

    of_list_queue_desc_prop_t src_list;
    of_list_queue_desc_prop_t *dst_list;

    if ((dst = of_queue_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_port_no_get(src, &val32);
    of_queue_desc_port_no_set(dst, val32);

    of_queue_desc_queue_id_get(src, &val32);
    of_queue_desc_queue_id_set(dst, val32);

    of_queue_desc_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_desc_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_desc_delete(dst);
        return NULL;
    }
    of_queue_desc_properties_set(dst, dst_list);
    of_list_queue_desc_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_desc_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_desc_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_DESC_PROP_MIN_RATE) {
        return of_queue_desc_prop_min_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_QUEUE_DESC_PROP_BSN_QUEUE_NAME) {
        return of_queue_desc_prop_bsn_queue_name_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_QUEUE_DESC_PROP_MAX_RATE) {
        return of_queue_desc_prop_max_rate_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_desc_prop_bsn_queue_name
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_prop_bsn_queue_name.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_prop_bsn_queue_name_t *
of_queue_desc_prop_bsn_queue_name_OF_VERSION_1_4_dup(
    of_queue_desc_prop_bsn_queue_name_t *src)
{
    of_queue_desc_prop_bsn_queue_name_t *dst;
    uint32_t val32;
    of_octets_t octets;

    if ((dst = of_queue_desc_prop_bsn_queue_name_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_prop_bsn_queue_name_experimenter_get(src, &val32);
    of_queue_desc_prop_bsn_queue_name_experimenter_set(dst, val32);

    of_queue_desc_prop_bsn_queue_name_exp_type_get(src, &val32);
    of_queue_desc_prop_bsn_queue_name_exp_type_set(dst, val32);

    of_queue_desc_prop_bsn_queue_name_name_get(src, &octets);
    of_queue_desc_prop_bsn_queue_name_name_set(dst, &octets);

    return dst;
}

/**
 * Duplicate an object of type of_queue_desc_prop_max_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_prop_max_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_prop_max_rate_t *
of_queue_desc_prop_max_rate_OF_VERSION_1_4_dup(
    of_queue_desc_prop_max_rate_t *src)
{
    of_queue_desc_prop_max_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_desc_prop_max_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_prop_max_rate_rate_get(src, &val16);
    of_queue_desc_prop_max_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_desc_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_desc_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_desc_prop_min_rate_t *
of_queue_desc_prop_min_rate_OF_VERSION_1_4_dup(
    of_queue_desc_prop_min_rate_t *src)
{
    of_queue_desc_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_desc_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_desc_prop_min_rate_rate_get(src, &val16);
    of_queue_desc_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_QUEUE_PROP_MAX_RATE) {
        return of_queue_prop_max_rate_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_QUEUE_PROP_MIN_RATE) {
        return of_queue_prop_min_rate_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_queue_prop_max_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_max_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_max_rate_t *
of_queue_prop_max_rate_OF_VERSION_1_4_dup(
    of_queue_prop_max_rate_t *src)
{
    of_queue_prop_max_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_max_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_max_rate_rate_get(src, &val16);
    of_queue_prop_max_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_prop_min_rate
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_prop_min_rate.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_prop_min_rate_t *
of_queue_prop_min_rate_OF_VERSION_1_4_dup(
    of_queue_prop_min_rate_t *src)
{
    of_queue_prop_min_rate_t *dst;
    uint16_t val16;

    if ((dst = of_queue_prop_min_rate_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_prop_min_rate_rate_get(src, &val16);
    of_queue_prop_min_rate_rate_set(dst, val16);

    return dst;
}

/**
 * Duplicate an object of type of_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_queue_stats_entry_t *
of_queue_stats_entry_OF_VERSION_1_4_dup(
    of_queue_stats_entry_t *src)
{
    of_queue_stats_entry_t *dst;
    of_port_no_t port_no;
    uint32_t val32;
    uint64_t val64;

    of_list_queue_stats_prop_t src_list;
    of_list_queue_stats_prop_t *dst_list;

    if ((dst = of_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_queue_stats_entry_port_no_get(src, &port_no);
    of_queue_stats_entry_port_no_set(dst, port_no);

    of_queue_stats_entry_queue_id_get(src, &val32);
    of_queue_stats_entry_queue_id_set(dst, val32);

    of_queue_stats_entry_tx_bytes_get(src, &val64);
    of_queue_stats_entry_tx_bytes_set(dst, val64);

    of_queue_stats_entry_tx_packets_get(src, &val64);
    of_queue_stats_entry_tx_packets_set(dst, val64);

    of_queue_stats_entry_tx_errors_get(src, &val64);
    of_queue_stats_entry_tx_errors_set(dst, val64);

    of_queue_stats_entry_duration_sec_get(src, &val32);
    of_queue_stats_entry_duration_sec_set(dst, val32);

    of_queue_stats_entry_duration_nsec_get(src, &val32);
    of_queue_stats_entry_duration_nsec_set(dst, val32);

    of_queue_stats_entry_properties_bind(
        src, &src_list);
    dst_list = of_list_queue_stats_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_queue_stats_entry_delete(dst);
        return NULL;
    }
    of_queue_stats_entry_properties_set(dst, dst_list);
    of_list_queue_stats_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_queue_stats_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_queue_stats_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_queue_stats_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate a super class object of type of_role_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_role_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_role_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_table_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_desc_t *
of_table_desc_OF_VERSION_1_4_dup(
    of_table_desc_t *src)
{
    of_table_desc_t *dst;
    uint8_t val8;
    uint32_t val32;

    if ((dst = of_table_desc_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_desc_table_id_get(src, &val8);
    of_table_desc_table_id_set(dst, val8);

    of_table_desc_config_get(src, &val32);
    of_table_desc_config_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_table_feature_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_table_feature_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_SETFIELD_MISS) {
        return of_table_feature_prop_write_setfield_miss_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_ACTIONS) {
        return of_table_feature_prop_apply_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_NEXT_TABLES) {
        return of_table_feature_prop_next_tables_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_SETFIELD_MISS) {
        return of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_ACTIONS) {
        return of_table_feature_prop_write_actions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_INSTRUCTIONS_MISS) {
        return of_table_feature_prop_instructions_miss_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WILDCARDS) {
        return of_table_feature_prop_wildcards_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_TABLE_SYNC_FROM) {
        return of_table_feature_prop_table_sync_from_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_INSTRUCTIONS) {
        return of_table_feature_prop_instructions_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_SETFIELD) {
        return of_table_feature_prop_write_setfield_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_SETFIELD) {
        return of_table_feature_prop_apply_setfield_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_APPLY_ACTIONS_MISS) {
        return of_table_feature_prop_apply_actions_miss_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_MATCH) {
        return of_table_feature_prop_match_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_NEXT_TABLES_MISS) {
        return of_table_feature_prop_next_tables_miss_OF_VERSION_1_4_dup(src);
    }

    if (src->object_id == OF_TABLE_FEATURE_PROP_WRITE_ACTIONS_MISS) {
        return of_table_feature_prop_write_actions_miss_OF_VERSION_1_4_dup(src);
    }

    return NULL;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_actions_t *
of_table_feature_prop_apply_actions_OF_VERSION_1_4_dup(
    of_table_feature_prop_apply_actions_t *src)
{
    of_table_feature_prop_apply_actions_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_apply_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_actions_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_actions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_actions_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_actions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_actions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_actions_miss_t *
of_table_feature_prop_apply_actions_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_apply_actions_miss_t *src)
{
    of_table_feature_prop_apply_actions_miss_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_apply_actions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_actions_miss_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_actions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_actions_miss_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_setfield
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_setfield.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_setfield_t *
of_table_feature_prop_apply_setfield_OF_VERSION_1_4_dup(
    of_table_feature_prop_apply_setfield_t *src)
{
    of_table_feature_prop_apply_setfield_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_apply_setfield_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_setfield_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_setfield_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_setfield_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_apply_setfield_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_apply_setfield_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_apply_setfield_miss_t *
of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_apply_setfield_miss_t *src)
{
    of_table_feature_prop_apply_setfield_miss_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_apply_setfield_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_apply_setfield_miss_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_apply_setfield_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_apply_setfield_miss_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_instructions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_instructions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_instructions_t *
of_table_feature_prop_instructions_OF_VERSION_1_4_dup(
    of_table_feature_prop_instructions_t *src)
{
    of_table_feature_prop_instructions_t *dst;

    of_list_instruction_id_t src_list;
    of_list_instruction_id_t *dst_list;

    if ((dst = of_table_feature_prop_instructions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_instructions_instruction_ids_bind(
        src, &src_list);
    dst_list = of_list_instruction_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_instructions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_instructions_instruction_ids_set(dst, dst_list);
    of_list_instruction_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_instructions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_instructions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_instructions_miss_t *
of_table_feature_prop_instructions_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_instructions_miss_t *src)
{
    of_table_feature_prop_instructions_miss_t *dst;

    of_list_instruction_id_t src_list;
    of_list_instruction_id_t *dst_list;

    if ((dst = of_table_feature_prop_instructions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_instructions_miss_instruction_ids_bind(
        src, &src_list);
    dst_list = of_list_instruction_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_instructions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_instructions_miss_instruction_ids_set(dst, dst_list);
    of_list_instruction_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_match
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_match.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_match_t *
of_table_feature_prop_match_OF_VERSION_1_4_dup(
    of_table_feature_prop_match_t *src)
{
    of_table_feature_prop_match_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_match_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_match_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_match_delete(dst);
        return NULL;
    }
    of_table_feature_prop_match_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_next_tables
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_next_tables.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_next_tables_t *
of_table_feature_prop_next_tables_OF_VERSION_1_4_dup(
    of_table_feature_prop_next_tables_t *src)
{
    of_table_feature_prop_next_tables_t *dst;

    of_list_uint8_t src_list;
    of_list_uint8_t *dst_list;

    if ((dst = of_table_feature_prop_next_tables_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_next_tables_next_table_ids_bind(
        src, &src_list);
    dst_list = of_list_uint8_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_next_tables_delete(dst);
        return NULL;
    }
    of_table_feature_prop_next_tables_next_table_ids_set(dst, dst_list);
    of_list_uint8_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_next_tables_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_next_tables_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_next_tables_miss_t *
of_table_feature_prop_next_tables_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_next_tables_miss_t *src)
{
    of_table_feature_prop_next_tables_miss_t *dst;

    of_list_uint8_t src_list;
    of_list_uint8_t *dst_list;

    if ((dst = of_table_feature_prop_next_tables_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_next_tables_miss_next_table_ids_bind(
        src, &src_list);
    dst_list = of_list_uint8_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_next_tables_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_next_tables_miss_next_table_ids_set(dst, dst_list);
    of_list_uint8_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_table_sync_from
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_table_sync_from.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_table_sync_from_t *
of_table_feature_prop_table_sync_from_OF_VERSION_1_4_dup(
    of_table_feature_prop_table_sync_from_t *src)
{
    of_table_feature_prop_table_sync_from_t *dst;

    of_list_uint8_t src_list;
    of_list_uint8_t *dst_list;

    if ((dst = of_table_feature_prop_table_sync_from_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_table_sync_from_table_ids_bind(
        src, &src_list);
    dst_list = of_list_uint8_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_table_sync_from_delete(dst);
        return NULL;
    }
    of_table_feature_prop_table_sync_from_table_ids_set(dst, dst_list);
    of_list_uint8_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_wildcards
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_wildcards.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_wildcards_t *
of_table_feature_prop_wildcards_OF_VERSION_1_4_dup(
    of_table_feature_prop_wildcards_t *src)
{
    of_table_feature_prop_wildcards_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_wildcards_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_wildcards_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_wildcards_delete(dst);
        return NULL;
    }
    of_table_feature_prop_wildcards_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_actions
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_actions.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_actions_t *
of_table_feature_prop_write_actions_OF_VERSION_1_4_dup(
    of_table_feature_prop_write_actions_t *src)
{
    of_table_feature_prop_write_actions_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_write_actions_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_actions_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_actions_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_actions_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_actions_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_actions_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_actions_miss_t *
of_table_feature_prop_write_actions_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_write_actions_miss_t *src)
{
    of_table_feature_prop_write_actions_miss_t *dst;

    of_list_action_id_t src_list;
    of_list_action_id_t *dst_list;

    if ((dst = of_table_feature_prop_write_actions_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_actions_miss_action_ids_bind(
        src, &src_list);
    dst_list = of_list_action_id_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_actions_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_actions_miss_action_ids_set(dst, dst_list);
    of_list_action_id_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_setfield
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_setfield.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_setfield_t *
of_table_feature_prop_write_setfield_OF_VERSION_1_4_dup(
    of_table_feature_prop_write_setfield_t *src)
{
    of_table_feature_prop_write_setfield_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_write_setfield_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_setfield_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_setfield_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_setfield_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_feature_prop_write_setfield_miss
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_feature_prop_write_setfield_miss.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_feature_prop_write_setfield_miss_t *
of_table_feature_prop_write_setfield_miss_OF_VERSION_1_4_dup(
    of_table_feature_prop_write_setfield_miss_t *src)
{
    of_table_feature_prop_write_setfield_miss_t *dst;

    of_list_uint32_t src_list;
    of_list_uint32_t *dst_list;

    if ((dst = of_table_feature_prop_write_setfield_miss_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_feature_prop_write_setfield_miss_oxm_ids_bind(
        src, &src_list);
    dst_list = of_list_uint32_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_feature_prop_write_setfield_miss_delete(dst);
        return NULL;
    }
    of_table_feature_prop_write_setfield_miss_oxm_ids_set(dst, dst_list);
    of_list_uint32_delete(dst_list);

    return dst;
}

/**
 * Duplicate an object of type of_table_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_features_t *
of_table_features_OF_VERSION_1_4_dup(
    of_table_features_t *src)
{
    of_table_features_t *dst;
    uint8_t val8;
    of_table_name_t table_name;
    uint64_t val64;
    uint32_t val32;

    of_list_table_feature_prop_t src_list;
    of_list_table_feature_prop_t *dst_list;

    if ((dst = of_table_features_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_features_table_id_get(src, &val8);
    of_table_features_table_id_set(dst, val8);

    of_table_features_name_get(src, &table_name);
    of_table_features_name_set(dst, table_name);

    of_table_features_metadata_match_get(src, &val64);
    of_table_features_metadata_match_set(dst, val64);

    of_table_features_metadata_write_get(src, &val64);
    of_table_features_metadata_write_set(dst, val64);

    of_table_features_config_get(src, &val32);
    of_table_features_config_set(dst, val32);

    of_table_features_max_entries_get(src, &val32);
    of_table_features_max_entries_set(dst, val32);

    of_table_features_properties_bind(
        src, &src_list);
    dst_list = of_list_table_feature_prop_OF_VERSION_1_4_dup(&src_list);
    if (dst_list == NULL) {
        of_table_features_delete(dst);
        return NULL;
    }
    of_table_features_properties_set(dst, dst_list);
    of_list_table_feature_prop_delete(dst_list);

    return dst;
}

/**
 * Duplicate a super class object of type of_table_mod_prop
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_table_mod_prop_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_table_mod_prop_eviction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_prop_eviction.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_prop_eviction_t *
of_table_mod_prop_eviction_OF_VERSION_1_4_dup(
    of_table_mod_prop_eviction_t *src)
{
    of_table_mod_prop_eviction_t *dst;
    uint32_t val32;

    if ((dst = of_table_mod_prop_eviction_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_prop_eviction_flags_get(src, &val32);
    of_table_mod_prop_eviction_flags_set(dst, val32);

    return dst;
}

/**
 * Duplicate a super class object of type of_table_mod_prop_experimenter
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_prop_experimenter.
 *
 * The caller is responsible for deleting the returned value
 */
of_object_t *
of_table_mod_prop_experimenter_OF_VERSION_1_4_dup(
    of_object_t *src)
{

    return NULL;
}

/**
 * Duplicate an object of type of_table_mod_prop_vacancy
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_mod_prop_vacancy.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_mod_prop_vacancy_t *
of_table_mod_prop_vacancy_OF_VERSION_1_4_dup(
    of_table_mod_prop_vacancy_t *src)
{
    of_table_mod_prop_vacancy_t *dst;
    uint8_t val8;

    if ((dst = of_table_mod_prop_vacancy_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_mod_prop_vacancy_vacancy_down_get(src, &val8);
    of_table_mod_prop_vacancy_vacancy_down_set(dst, val8);

    of_table_mod_prop_vacancy_vacancy_up_get(src, &val8);
    of_table_mod_prop_vacancy_vacancy_up_set(dst, val8);

    of_table_mod_prop_vacancy_vacancy_get(src, &val8);
    of_table_mod_prop_vacancy_vacancy_set(dst, val8);

    return dst;
}

/**
 * Duplicate an object of type of_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_table_stats_entry_t *
of_table_stats_entry_OF_VERSION_1_4_dup(
    of_table_stats_entry_t *src)
{
    of_table_stats_entry_t *dst;
    uint8_t val8;
    uint32_t val32;
    uint64_t val64;

    if ((dst = of_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    of_table_stats_entry_table_id_get(src, &val8);
    of_table_stats_entry_table_id_set(dst, val8);

    of_table_stats_entry_active_count_get(src, &val32);
    of_table_stats_entry_active_count_set(dst, val32);

    of_table_stats_entry_lookup_count_get(src, &val64);
    of_table_stats_entry_lookup_count_set(dst, val64);

    of_table_stats_entry_matched_count_get(src, &val64);
    of_table_stats_entry_matched_count_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_uint32
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint32.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint32_t *
of_uint32_OF_VERSION_1_4_dup(
    of_uint32_t *src)
{
    of_uint32_t *dst;
    uint32_t val32;

    if ((dst = of_uint32_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint32_value_get(src, &val32);
    of_uint32_value_set(dst, val32);

    return dst;
}

/**
 * Duplicate an object of type of_uint64
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint64.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint64_t *
of_uint64_OF_VERSION_1_4_dup(
    of_uint64_t *src)
{
    of_uint64_t *dst;
    uint64_t val64;

    if ((dst = of_uint64_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint64_value_get(src, &val64);
    of_uint64_value_set(dst, val64);

    return dst;
}

/**
 * Duplicate an object of type of_uint8
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_uint8.
 *
 * The caller is responsible for deleting the returned value
 */
of_uint8_t *
of_uint8_OF_VERSION_1_4_dup(
    of_uint8_t *src)
{
    of_uint8_t *dst;
    uint8_t val8;

    if ((dst = of_uint8_new(src->version)) == NULL) {
        return NULL;
    }

    of_uint8_value_get(src, &val8);
    of_uint8_value_set(dst, val8);

    return dst;
}

/**
 * Duplicate a list of type of_list_action
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_t *
of_list_action_OF_VERSION_1_4_dup(
    of_list_action_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_t *dst;

    if ((dst = of_list_action_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_action_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_action_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_action_id_t *
of_list_action_id_OF_VERSION_1_4_dup(
    of_list_action_id_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_action_id_t *dst;

    if ((dst = of_list_action_id_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ACTION_ID_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_action_id_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_action_id_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_async_config_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_async_config_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_async_config_prop_t *
of_list_async_config_prop_OF_VERSION_1_4_dup(
    of_list_async_config_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_async_config_prop_t *dst;

    if ((dst = of_list_async_config_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ASYNC_CONFIG_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_async_config_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_async_config_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_controller_connection
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_controller_connection.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_controller_connection_t *
of_list_bsn_controller_connection_OF_VERSION_1_4_dup(
    of_list_bsn_controller_connection_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_controller_connection_t *dst;

    if ((dst = of_list_bsn_controller_connection_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_CONTROLLER_CONNECTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_controller_connection_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_controller_connection_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_debug_counter_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_debug_counter_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_debug_counter_desc_stats_entry_t *
of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_debug_counter_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_debug_counter_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_debug_counter_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_DEBUG_COUNTER_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_debug_counter_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_debug_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_debug_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_debug_counter_stats_entry_t *
of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_debug_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_debug_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_debug_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_DEBUG_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_debug_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_flow_checksum_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_flow_checksum_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_flow_checksum_bucket_stats_entry_t *
of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_flow_checksum_bucket_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_flow_checksum_bucket_stats_entry_t *dst;

    if ((dst = of_list_bsn_flow_checksum_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_FLOW_CHECKSUM_BUCKET_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_flow_checksum_bucket_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_generic_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_generic_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_generic_stats_entry_t *
of_list_bsn_generic_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_generic_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_generic_stats_entry_t *dst;

    if ((dst = of_list_bsn_generic_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENERIC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_generic_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_generic_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_bucket_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_bucket_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_bucket_stats_entry_t *
of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_gentable_bucket_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_bucket_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_bucket_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_BUCKET_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_bucket_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_desc_stats_entry_t *
of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_gentable_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_entry_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_entry_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_entry_desc_stats_entry_t *
of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_gentable_entry_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_entry_desc_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_entry_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_ENTRY_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_entry_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_entry_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_entry_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_entry_stats_entry_t *
of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_gentable_entry_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_entry_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_entry_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_ENTRY_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_entry_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_gentable_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_gentable_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_gentable_stats_entry_t *
of_list_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_gentable_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_gentable_stats_entry_t *dst;

    if ((dst = of_list_bsn_gentable_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_GENTABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_gentable_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_interface
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_interface.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_interface_t *
of_list_bsn_interface_OF_VERSION_1_4_dup(
    of_list_bsn_interface_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_interface_t *dst;

    if ((dst = of_list_bsn_interface_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_INTERFACE_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_interface_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_interface_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_lacp_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_lacp_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_lacp_stats_entry_t *
of_list_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_lacp_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_lacp_stats_entry_t *dst;

    if ((dst = of_list_bsn_lacp_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_LACP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_lacp_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_port_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_port_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_port_counter_stats_entry_t *
of_list_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_port_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_port_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_port_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_PORT_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_port_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_switch_pipeline_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_switch_pipeline_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_switch_pipeline_stats_entry_t *
of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_switch_pipeline_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_switch_pipeline_stats_entry_t *dst;

    if ((dst = of_list_bsn_switch_pipeline_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_SWITCH_PIPELINE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_switch_pipeline_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_table_checksum_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_table_checksum_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_table_checksum_stats_entry_t *
of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_table_checksum_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_table_checksum_stats_entry_t *dst;

    if ((dst = of_list_bsn_table_checksum_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_TABLE_CHECKSUM_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_table_checksum_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_tlv
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_tlv.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_tlv_t *
of_list_bsn_tlv_OF_VERSION_1_4_dup(
    of_list_bsn_tlv_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_tlv_t *dst;

    if ((dst = of_list_bsn_tlv_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_TLV_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_tlv_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_tlv_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vlan_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vlan_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vlan_counter_stats_entry_t *
of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_vlan_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vlan_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_vlan_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VLAN_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vlan_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vlan_mac
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vlan_mac.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vlan_mac_t *
of_list_bsn_vlan_mac_OF_VERSION_1_4_dup(
    of_list_bsn_vlan_mac_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vlan_mac_t *dst;

    if ((dst = of_list_bsn_vlan_mac_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VLAN_MAC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vlan_mac_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vlan_mac_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bsn_vrf_counter_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bsn_vrf_counter_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bsn_vrf_counter_stats_entry_t *
of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(
    of_list_bsn_vrf_counter_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bsn_vrf_counter_stats_entry_t *dst;

    if ((dst = of_list_bsn_vrf_counter_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BSN_VRF_COUNTER_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bsn_vrf_counter_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_t *
of_list_bucket_OF_VERSION_1_4_dup(
    of_list_bucket_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_t *dst;

    if ((dst = of_list_bucket_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bucket_counter
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bucket_counter.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bucket_counter_t *
of_list_bucket_counter_OF_VERSION_1_4_dup(
    of_list_bucket_counter_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bucket_counter_t *dst;

    if ((dst = of_list_bucket_counter_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUCKET_COUNTER_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bucket_counter_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bucket_counter_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_bundle_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_bundle_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_bundle_prop_t *
of_list_bundle_prop_OF_VERSION_1_4_dup(
    of_list_bundle_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_bundle_prop_t *dst;

    if ((dst = of_list_bundle_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_BUNDLE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_bundle_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_bundle_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_flow_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_flow_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_flow_stats_entry_t *
of_list_flow_stats_entry_OF_VERSION_1_4_dup(
    of_list_flow_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_flow_stats_entry_t *dst;

    if ((dst = of_list_flow_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_FLOW_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_flow_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_flow_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_desc_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_desc_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_desc_stats_entry_t *
of_list_group_desc_stats_entry_OF_VERSION_1_4_dup(
    of_list_group_desc_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_desc_stats_entry_t *dst;

    if ((dst = of_list_group_desc_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_DESC_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_desc_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_desc_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_group_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_group_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_group_stats_entry_t *
of_list_group_stats_entry_OF_VERSION_1_4_dup(
    of_list_group_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_group_stats_entry_t *dst;

    if ((dst = of_list_group_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_GROUP_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_group_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_group_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_hello_elem
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_hello_elem.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_hello_elem_t *
of_list_hello_elem_OF_VERSION_1_4_dup(
    of_list_hello_elem_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_hello_elem_t *dst;

    if ((dst = of_list_hello_elem_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_HELLO_ELEM_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_hello_elem_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_hello_elem_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_t *
of_list_instruction_OF_VERSION_1_4_dup(
    of_list_instruction_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_t *dst;

    if ((dst = of_list_instruction_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_instruction_id
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_instruction_id.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_instruction_id_t *
of_list_instruction_id_OF_VERSION_1_4_dup(
    of_list_instruction_id_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_instruction_id_t *dst;

    if ((dst = of_list_instruction_id_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_INSTRUCTION_ID_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_instruction_id_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_instruction_id_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_band
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_band.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_band_t *
of_list_meter_band_OF_VERSION_1_4_dup(
    of_list_meter_band_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_band_t *dst;

    if ((dst = of_list_meter_band_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_BAND_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_band_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_band_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_band_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_band_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_band_stats_t *
of_list_meter_band_stats_OF_VERSION_1_4_dup(
    of_list_meter_band_stats_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_band_stats_t *dst;

    if ((dst = of_list_meter_band_stats_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_BAND_STATS_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_band_stats_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_band_stats_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_config
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_config.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_config_t *
of_list_meter_config_OF_VERSION_1_4_dup(
    of_list_meter_config_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_config_t *dst;

    if ((dst = of_list_meter_config_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_CONFIG_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_config_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_config_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_meter_stats
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_meter_stats.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_meter_stats_t *
of_list_meter_stats_OF_VERSION_1_4_dup(
    of_list_meter_stats_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_meter_stats_t *dst;

    if ((dst = of_list_meter_stats_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_METER_STATS_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_meter_stats_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_meter_stats_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_oxm
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_oxm.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_oxm_t *
of_list_oxm_OF_VERSION_1_4_dup(
    of_list_oxm_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_oxm_t *dst;

    if ((dst = of_list_oxm_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_OXM_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_oxm_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_oxm_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_t *
of_list_port_desc_OF_VERSION_1_4_dup(
    of_list_port_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_t *dst;

    if ((dst = of_list_port_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_desc_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_desc_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_desc_prop_t *
of_list_port_desc_prop_OF_VERSION_1_4_dup(
    of_list_port_desc_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_desc_prop_t *dst;

    if ((dst = of_list_port_desc_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_DESC_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_desc_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_desc_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_mod_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_mod_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_mod_prop_t *
of_list_port_mod_prop_OF_VERSION_1_4_dup(
    of_list_port_mod_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_mod_prop_t *dst;

    if ((dst = of_list_port_mod_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_MOD_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_mod_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_mod_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_entry_t *
of_list_port_stats_entry_OF_VERSION_1_4_dup(
    of_list_port_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_entry_t *dst;

    if ((dst = of_list_port_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_port_stats_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_port_stats_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_port_stats_prop_t *
of_list_port_stats_prop_OF_VERSION_1_4_dup(
    of_list_port_stats_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_port_stats_prop_t *dst;

    if ((dst = of_list_port_stats_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_PORT_STATS_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_port_stats_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_port_stats_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_desc_t *
of_list_queue_desc_OF_VERSION_1_4_dup(
    of_list_queue_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_desc_t *dst;

    if ((dst = of_list_queue_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_desc_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_desc_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_desc_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_desc_prop_t *
of_list_queue_desc_prop_OF_VERSION_1_4_dup(
    of_list_queue_desc_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_desc_prop_t *dst;

    if ((dst = of_list_queue_desc_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_DESC_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_desc_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_desc_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_prop_t *
of_list_queue_prop_OF_VERSION_1_4_dup(
    of_list_queue_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_prop_t *dst;

    if ((dst = of_list_queue_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_entry_t *
of_list_queue_stats_entry_OF_VERSION_1_4_dup(
    of_list_queue_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_entry_t *dst;

    if ((dst = of_list_queue_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_queue_stats_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_queue_stats_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_queue_stats_prop_t *
of_list_queue_stats_prop_OF_VERSION_1_4_dup(
    of_list_queue_stats_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_queue_stats_prop_t *dst;

    if ((dst = of_list_queue_stats_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_QUEUE_STATS_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_queue_stats_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_queue_stats_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_role_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_role_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_role_prop_t *
of_list_role_prop_OF_VERSION_1_4_dup(
    of_list_role_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_role_prop_t *dst;

    if ((dst = of_list_role_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_ROLE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_role_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_role_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_desc
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_desc.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_desc_t *
of_list_table_desc_OF_VERSION_1_4_dup(
    of_list_table_desc_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_desc_t *dst;

    if ((dst = of_list_table_desc_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_DESC_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_desc_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_desc_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_feature_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_feature_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_feature_prop_t *
of_list_table_feature_prop_OF_VERSION_1_4_dup(
    of_list_table_feature_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_feature_prop_t *dst;

    if ((dst = of_list_table_feature_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_FEATURE_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_feature_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_feature_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_features
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_features.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_features_t *
of_list_table_features_OF_VERSION_1_4_dup(
    of_list_table_features_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_features_t *dst;

    if ((dst = of_list_table_features_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_FEATURES_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_features_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_features_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_mod_prop
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_mod_prop.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_mod_prop_t *
of_list_table_mod_prop_OF_VERSION_1_4_dup(
    of_list_table_mod_prop_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_mod_prop_t *dst;

    if ((dst = of_list_table_mod_prop_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_MOD_PROP_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_mod_prop_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_mod_prop_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_table_stats_entry
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_table_stats_entry.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_table_stats_entry_t *
of_list_table_stats_entry_OF_VERSION_1_4_dup(
    of_list_table_stats_entry_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_table_stats_entry_t *dst;

    if ((dst = of_list_table_stats_entry_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_TABLE_STATS_ENTRY_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_table_stats_entry_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_table_stats_entry_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint32
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint32.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint32_t *
of_list_uint32_OF_VERSION_1_4_dup(
    of_list_uint32_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint32_t *dst;

    if ((dst = of_list_uint32_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT32_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint32_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint32_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint64
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint64.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint64_t *
of_list_uint64_OF_VERSION_1_4_dup(
    of_list_uint64_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint64_t *dst;

    if ((dst = of_list_uint64_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT64_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint64_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint64_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

/**
 * Duplicate a list of type of_list_uint8
 * using accessor functions
 * @param src Pointer to object to be duplicated
 * @returns A new object of type of_list_uint8.
 *
 * The caller is responsible for deleting the returned value
 */
of_list_uint8_t *
of_list_uint8_OF_VERSION_1_4_dup(
    of_list_uint8_t *src)
{
    of_object_t src_elt;
    of_object_t *dst_elt;
    int rv;
    of_list_uint8_t *dst;

    if ((dst = of_list_uint8_new(src->version)) == NULL) {
        return NULL;
    }

    OF_LIST_UINT8_ITER(src, &src_elt, rv) {
        if ((dst_elt = of_uint8_OF_VERSION_1_4_dup(&src_elt)) == NULL) {
            of_object_delete((of_object_t *)dst);
            return NULL;
        }
        _TRY_FREE(of_list_uint8_append(dst, dst_elt),
            dst, NULL);
        of_object_delete((of_object_t *)dst_elt);
    }

    return dst;
}

of_object_t *
of_aggregate_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_aggregate_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_aggregate_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_aggregate_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_aggregate_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_aggregate_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_aggregate_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_aggregate_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_aggregate_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_aggregate_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_aggregate_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_aggregate_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_get_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_async_get_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_async_get_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_get_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_async_get_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_async_get_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_set_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_async_set_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_async_set_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bad_action_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bad_action_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bad_action_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bad_action_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bad_action_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bad_action_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bad_instruction_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_bad_instruction_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bad_instruction_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bad_instruction_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bad_instruction_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bad_match_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_bad_match_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bad_match_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bad_match_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bad_match_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bad_property_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bad_property_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bad_request_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bad_request_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bad_request_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bad_request_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bad_request_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bad_request_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_barrier_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_barrier_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_barrier_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_barrier_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_barrier_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_barrier_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_barrier_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_barrier_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_barrier_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_barrier_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_barrier_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_barrier_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_arp_idle_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_arp_idle_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_arp_idle_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_base_error_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_clear_data_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_clear_data_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_clear_data_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_clear_data_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_clear_data_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_clear_data_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_clear_data_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_clear_data_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_clear_data_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_clear_data_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_clear_data_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_clear_data_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_enable_get_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_enable_get_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_enable_get_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_enable_get_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_enable_get_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_enable_get_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_enable_get_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_enable_get_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_enable_get_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_enable_get_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_enable_get_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_enable_get_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_enable_set_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_enable_set_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_enable_set_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_enable_set_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_enable_set_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_enable_set_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_bw_enable_set_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_bw_enable_set_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_bw_enable_set_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_bw_enable_set_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_bw_enable_set_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_bw_enable_set_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_controller_connections_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_controller_connections_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_controller_connections_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_controller_connections_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_controller_connections_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_controller_connections_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_error_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_error_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_error_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_checksum_bucket_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_checksum_bucket_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_checksum_bucket_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_checksum_bucket_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_checksum_bucket_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_checksum_bucket_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_idle_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_idle_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_idle_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_idle_enable_get_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_idle_enable_get_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_idle_enable_get_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_idle_enable_get_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_idle_enable_get_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_idle_enable_get_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_idle_enable_set_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_idle_enable_set_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_idle_enable_set_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_idle_enable_set_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_idle_enable_set_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_idle_enable_set_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_generic_async_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_generic_async_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_generic_command_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_generic_command_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_generic_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_generic_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_generic_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_generic_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_generic_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_generic_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_bucket_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_bucket_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_bucket_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_bucket_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_bucket_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_bucket_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_clear_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_clear_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_clear_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_clear_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_clear_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_clear_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_add_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_add_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_add_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_delete_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_delete_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_delete_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_error_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_error_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_error_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_full_error_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_full_error_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_full_error_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_set_buckets_size_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_set_buckets_size_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_set_buckets_size_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_interfaces_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_interfaces_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_get_interfaces_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_get_interfaces_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_interfaces_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_interfaces_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_interfaces_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_interfaces_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_get_interfaces_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_get_interfaces_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_interfaces_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_interfaces_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_ip_mask_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_ip_mask_reply_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_ip_mask_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_ip_mask_request_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_l2_table_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_l2_table_reply_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_l2_table_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_l2_table_request_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_mirroring_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_mirroring_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_get_mirroring_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_get_mirroring_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_mirroring_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_mirroring_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_mirroring_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_get_mirroring_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_get_mirroring_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_get_mirroring_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_mirroring_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_mirroring_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_switch_pipeline_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_switch_pipeline_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_switch_pipeline_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_get_switch_pipeline_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_get_switch_pipeline_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_get_switch_pipeline_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_header_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_hybrid_get_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_hybrid_get_reply_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_hybrid_get_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_hybrid_get_request_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_image_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_image_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_image_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_image_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_image_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_image_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lacp_convergence_notif_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lacp_convergence_notif_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lacp_convergence_notif_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lacp_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lacp_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lacp_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lacp_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lacp_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lacp_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_log_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_log_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_log_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lua_command_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lua_command_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lua_command_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lua_command_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lua_command_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lua_command_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lua_notification_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lua_notification_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lua_notification_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lua_upload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lua_upload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lua_upload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_pdu_rx_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_pdu_rx_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_pdu_rx_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_pdu_rx_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_pdu_rx_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_pdu_rx_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_pdu_rx_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_pdu_rx_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_pdu_rx_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_pdu_rx_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_pdu_rx_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_pdu_rx_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_pdu_rx_timeout_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_pdu_rx_timeout_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_pdu_rx_timeout_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_pdu_rx_timeout_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_pdu_rx_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_pdu_rx_timeout_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_pdu_tx_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_pdu_tx_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_pdu_tx_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_pdu_tx_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_pdu_tx_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_pdu_tx_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_pdu_tx_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_pdu_tx_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_pdu_tx_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_pdu_tx_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_pdu_tx_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_pdu_tx_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_port_counter_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_port_counter_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_port_counter_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_port_counter_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_port_counter_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_port_counter_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_role_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_role_status_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_aux_cxns_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_aux_cxns_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_aux_cxns_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_aux_cxns_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_aux_cxns_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_aux_cxns_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_ip_mask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_ip_mask_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_l2_table_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_l2_table_reply_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_l2_table_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_l2_table_request_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_lacp_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_lacp_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_lacp_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_lacp_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_lacp_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_lacp_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_mirroring_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_mirroring_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_set_mirroring_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_set_mirroring_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_mirroring_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_mirroring_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_pktin_suppression_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_pktin_suppression_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_set_pktin_suppression_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_set_pktin_suppression_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_pktin_suppression_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_pktin_suppression_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_pktin_suppression_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_set_pktin_suppression_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_set_pktin_suppression_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_set_pktin_suppression_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_pktin_suppression_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_pktin_suppression_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_switch_pipeline_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_switch_pipeline_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_switch_pipeline_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_set_switch_pipeline_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_set_switch_pipeline_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_set_switch_pipeline_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_shell_command_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_shell_command_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_shell_output_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_shell_output_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_shell_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_shell_status_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_stats_reply_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_stats_request_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_switch_pipeline_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_switch_pipeline_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_switch_pipeline_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_switch_pipeline_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_switch_pipeline_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_switch_pipeline_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_table_checksum_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_table_checksum_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_table_checksum_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_table_checksum_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_table_checksum_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_table_checksum_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_table_set_buckets_size_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_table_set_buckets_size_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_table_set_buckets_size_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_takeover_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_takeover_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_time_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_time_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_time_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_time_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_time_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_time_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_virtual_port_create_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_virtual_port_create_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_virtual_port_create_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_virtual_port_create_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_virtual_port_create_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_virtual_port_create_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_virtual_port_create_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_virtual_port_create_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_virtual_port_create_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_virtual_port_create_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_virtual_port_create_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_virtual_port_create_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_virtual_port_remove_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_virtual_port_remove_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_virtual_port_remove_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_virtual_port_remove_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_virtual_port_remove_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_virtual_port_remove_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_virtual_port_remove_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_virtual_port_remove_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_virtual_port_remove_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_virtual_port_remove_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_virtual_port_remove_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_virtual_port_remove_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vlan_counter_clear_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vlan_counter_clear_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vlan_counter_clear_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vlan_counter_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vlan_counter_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vlan_counter_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vlan_counter_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vlan_counter_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vlan_counter_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vrf_counter_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vrf_counter_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vrf_counter_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vrf_counter_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vrf_counter_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vrf_counter_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bundle_add_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bundle_add_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bundle_ctrl_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bundle_ctrl_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bundle_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bundle_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_desc_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_desc_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_desc_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_desc_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_desc_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_desc_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_echo_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_echo_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_echo_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_echo_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_echo_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_echo_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_echo_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_echo_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_echo_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_echo_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_echo_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_echo_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_error_msg_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_experimenter_error_msg_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_experimenter_stats_reply_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_experimenter_stats_request_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_features_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_features_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_features_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_features_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_features_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_features_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_features_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_features_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_features_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_features_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_features_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_features_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_add_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_add_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_add_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_add_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_add_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_add_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_delete_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_delete_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_delete_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_delete_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_delete_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_delete_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_delete_strict_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_delete_strict_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_delete_strict_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_delete_strict_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_delete_strict_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_delete_strict_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_mod_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_mod_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_mod_failed_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_mod_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_mod_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_mod_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_mod_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_modify_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_modify_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_modify_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_modify_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_modify_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_modify_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_modify_strict_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_modify_strict_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_modify_strict_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_modify_strict_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_modify_strict_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_modify_strict_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_monitor_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_flow_monitor_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_removed_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_removed_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_removed_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_removed_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_removed_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_removed_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_get_config_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_get_config_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_get_config_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_get_config_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_get_config_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_get_config_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_get_config_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_get_config_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_get_config_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_get_config_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_get_config_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_get_config_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_add_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_add_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_add_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_add_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_add_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_delete_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_delete_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_delete_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_delete_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_delete_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_desc_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_desc_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_desc_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_desc_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_features_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_group_features_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_features_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_features_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_features_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_group_features_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_features_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_features_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_mod_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_mod_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_mod_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_mod_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_mod_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_mod_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_modify_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_modify_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_modify_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_modify_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_modify_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_header_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_header_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_header_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_header_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_header_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_header_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_hello_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_hello_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_hello_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_hello_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_hello_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_hello_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_hello_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_hello_failed_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_hello_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_hello_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_hello_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_hello_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_config_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_config_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_config_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_config_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_config_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_config_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_features_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_features_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_features_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_features_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_features_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_features_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_mod_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_mod_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_mod_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_mod_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_mod_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_mod_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_nicira_controller_role_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_nicira_controller_role_reply_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_nicira_controller_role_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_nicira_controller_role_request_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_nicira_header_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_packet_in_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_packet_in_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_packet_in_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_packet_in_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_packet_in_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_packet_in_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_packet_out_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_packet_out_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_packet_out_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_packet_out_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_packet_out_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_packet_out_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_port_desc_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_port_desc_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_mod_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_mod_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_mod_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_mod_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_mod_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_mod_failed_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_mod_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_mod_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_mod_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_mod_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_status_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_status_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_status_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_status_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_status_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_get_config_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_get_config_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_get_config_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_get_config_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_get_config_reply_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_get_config_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_get_config_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_get_config_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_get_config_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_get_config_request_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_op_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_op_failed_error_msg_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_op_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_op_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_op_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_op_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_requestforward_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_requestforward_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_role_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_role_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_role_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_role_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_role_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_role_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_request_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_role_request_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_role_request_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_role_request_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_role_status_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_set_config_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_set_config_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_set_config_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_set_config_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_set_config_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_set_config_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_stats_reply_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_stats_request_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_switch_config_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_switch_config_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_switch_config_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_switch_config_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_switch_config_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_desc_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_desc_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_desc_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_desc_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_features_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_features_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_features_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_features_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_features_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_features_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_features_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_features_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_features_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_table_mod_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_table_mod_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_table_mod_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_table_mod_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_failed_error_msg_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_table_mod_failed_error_msg_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_table_mod_failed_error_msg_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_table_mod_failed_error_msg_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_failed_error_msg_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_stats_reply_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_table_stats_reply_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_table_stats_reply_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_table_stats_reply_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_table_stats_reply_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_stats_reply_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_stats_request_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_table_stats_request_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_table_stats_request_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_table_stats_request_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_table_stats_request_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_stats_request_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_status_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_bsn_checksum_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_bsn_checksum_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_bsn_checksum_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_bsn_checksum_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_bsn_checksum_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_bsn_checksum_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_bsn_gentable_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_bsn_gentable_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_bsn_gentable_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_bsn_mirror_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_bsn_mirror_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_bsn_mirror_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_bsn_mirror_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_bsn_mirror_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_bsn_mirror_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_bsn_set_tunnel_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_copy_ttl_in_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_copy_ttl_in_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_copy_ttl_in_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_copy_ttl_in_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_copy_ttl_in_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_copy_ttl_out_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_copy_ttl_out_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_copy_ttl_out_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_copy_ttl_out_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_copy_ttl_out_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_dec_mpls_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_dec_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_dec_nw_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_dec_nw_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_dec_nw_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_dec_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_dec_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_enqueue_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_enqueue_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_group_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_group_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_group_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_group_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_group_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_bsn_checksum_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_bsn_checksum_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_bsn_checksum_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_bsn_gentable_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_bsn_gentable_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_bsn_gentable_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_bsn_mirror_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_bsn_mirror_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_bsn_mirror_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_bsn_set_tunnel_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_bsn_set_tunnel_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_copy_ttl_in_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_copy_ttl_in_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_copy_ttl_in_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_copy_ttl_out_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_copy_ttl_out_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_copy_ttl_out_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_dec_mpls_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_dec_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_dec_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_dec_nw_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_dec_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_dec_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_group_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_group_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_group_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_nicira_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_nicira_dec_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_nicira_dec_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_nicira_dec_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_output_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_output_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_output_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_pop_mpls_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_pop_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_pop_mpls_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_pop_pbb_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_pop_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_pop_pbb_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_pop_vlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_pop_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_pop_vlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_push_mpls_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_push_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_push_mpls_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_push_pbb_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_push_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_push_pbb_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_push_vlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_push_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_push_vlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_set_field_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_set_field_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_set_field_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_set_mpls_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_set_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_set_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_set_nw_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_set_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_set_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_id_set_queue_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_id_set_queue_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_id_set_queue_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_nicira_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_nicira_dec_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_nicira_dec_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_output_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_output_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_output_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_output_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_output_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_output_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_pop_mpls_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_pop_mpls_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_pop_mpls_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_pop_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_pop_mpls_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_pop_pbb_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_pop_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_pop_pbb_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_pop_vlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_pop_vlan_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_pop_vlan_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_pop_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_pop_vlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_push_mpls_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_push_mpls_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_push_mpls_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_push_mpls_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_push_mpls_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_push_pbb_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_action_push_pbb_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_push_pbb_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_push_vlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_push_vlan_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_push_vlan_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_push_vlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_push_vlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_dl_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_dl_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_dl_dst_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_dl_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_dl_src_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_dl_src_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_field_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_action_set_field_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_set_field_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_set_field_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_mpls_label_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_mpls_label_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_mpls_tc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_mpls_tc_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_mpls_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_mpls_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_set_mpls_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_set_mpls_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_set_mpls_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_nw_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_nw_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_nw_dst_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_nw_ecn_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_nw_ecn_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_nw_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_nw_src_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_nw_src_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_nw_tos_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_nw_tos_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_nw_tos_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_nw_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_nw_ttl_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_set_nw_ttl_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_set_nw_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_set_nw_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_queue_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_queue_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_action_set_queue_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_action_set_queue_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_action_set_queue_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_tp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_tp_dst_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_tp_dst_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_tp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_tp_src_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_tp_src_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_vlan_pcp_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_vlan_pcp_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_vlan_pcp_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_set_vlan_vid_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_set_vlan_vid_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_action_set_vlan_vid_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_action_strip_vlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_action_strip_vlan_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_experimenter_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_experimenter_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_experimenter_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_experimenter_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_flow_removed_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_flow_removed_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_flow_removed_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_flow_removed_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_packet_in_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_packet_in_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_packet_in_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_packet_in_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_port_status_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_port_status_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_port_status_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_port_status_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_requestforward_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_requestforward_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_requestforward_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_requestforward_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_role_status_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_role_status_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_role_status_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_role_status_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_table_status_master_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_table_status_master_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_async_config_prop_table_status_slave_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_async_config_prop_table_status_slave_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_controller_connection_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_controller_connection_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_controller_connection_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_debug_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_flow_checksum_bucket_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_generic_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_generic_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_generic_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_bucket_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_entry_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_gentable_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_interface_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_interface_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_interface_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_interface_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_interface_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_interface_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_lacp_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_port_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_switch_pipeline_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_table_checksum_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_key_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_key_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_key_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_port_num_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_port_num_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_port_num_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_port_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_port_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_port_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_state_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_state_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_state_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_system_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_system_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_system_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_actor_system_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_actor_system_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_actor_system_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_anchor_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_anchor_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_anchor_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_apply_bytes_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_apply_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_apply_bytes_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_apply_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_apply_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_apply_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_auto_negotiation_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_broadcast_query_timeout_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_broadcast_query_timeout_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_broadcast_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_broadcast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_broadcast_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_bucket_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_bucket_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_bucket_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_circuit_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_circuit_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_circuit_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_convergence_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_convergence_status_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_convergence_status_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_cpu_lag_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_cpu_lag_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_cpu_lag_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_crc_enabled_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_crc_enabled_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_crc_enabled_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_data_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_data_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_data_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_data_mask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_data_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_data_mask_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_decap_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_decap_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_decap_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_disable_src_mac_check_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_drop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_drop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_drop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_dscp_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_dscp_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_dscp_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ecn_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ecn_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ecn_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_egress_only_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_egress_only_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_egress_only_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_egress_port_group_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_egress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_egress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_enhanced_hash_capability_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_enhanced_hash_capability_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_eth_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_eth_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_eth_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_eth_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_eth_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_eth_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_eth_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_external_gateway_ip_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_external_gateway_ip_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_external_gateway_ip_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_external_gateway_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_external_gateway_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_external_gateway_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_external_ip_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_external_ip_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_external_ip_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_external_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_external_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_external_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_external_netmask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_external_netmask_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_external_netmask_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_generation_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_generation_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_generation_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_gtp_header_match_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_gtp_header_match_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_gtp_port_match_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_gtp_port_match_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_packet_field_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_packet_field_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_packet_field_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_packet_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_packet_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_packet_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_seed_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_seed_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_seed_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_hash_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_hash_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_hash_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_header_size_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_header_size_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_header_size_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_icmp_code_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_icmp_code_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_icmp_code_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_icmp_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_icmp_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_icmp_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_icmp_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_icmp_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_icmp_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_icmpv6_chksum_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_icmpv6_chksum_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_idle_notification_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_idle_notification_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_idle_notification_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_idle_time_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_idle_time_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_idle_time_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_idle_timeout_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_idle_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_idle_timeout_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_igmp_snooping_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_igmp_snooping_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_igmp_snooping_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ingress_port_group_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ingress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_internal_gateway_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_internal_gateway_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_internal_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_internal_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_internal_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_interval_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_interval_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_interval_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ip_proto_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ip_proto_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ip_proto_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv4_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv4_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv4_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv4_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv4_netmask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv4_netmask_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv4_netmask_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv4_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv6_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv6_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv6_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv6_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv6_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv6_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv6_prefix_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv6_prefix_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv6_prefix_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ipv6_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ipv6_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ipv6_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_known_multicast_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_known_multicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_known_multicast_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_l2_multicast_lookup_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_l2_multicast_lookup_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_l3_dst_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_l3_dst_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_l3_interface_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_l3_interface_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_l3_src_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_l3_src_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_l3_src_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_loopback_port_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_loopback_port_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_loopback_port_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mac_mask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mac_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mac_mask_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mcg_type_vxlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mcg_type_vxlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_miss_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_miss_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_miss_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mpls_control_word_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mpls_control_word_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mpls_control_word_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mpls_label_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mpls_label_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mpls_label_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_mpls_sequenced_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_mpls_sequenced_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_mpls_sequenced_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_multicast_interface_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_multicast_interface_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_multicast_interface_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_name_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_name_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_name_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ndp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ndp_static_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ndp_static_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ndp_static_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_negate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_negate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_negate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_next_hop_ipv4_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_next_hop_ipv4_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_next_hop_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_next_hop_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_next_hop_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_nexthop_type_vxlan_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_nexthop_type_vxlan_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_offset_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_offset_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_offset_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_parent_port_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_parent_port_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_parent_port_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_key_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_key_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_key_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_port_num_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_port_num_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_port_num_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_port_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_port_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_port_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_state_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_state_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_state_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_system_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_system_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_system_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_partner_system_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_partner_system_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_partner_system_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_port_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_port_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_port_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_port_usage_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_port_usage_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_port_usage_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_port_vxlan_mode_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_port_vxlan_mode_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_push_vlan_on_ingress_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_push_vlan_on_ingress_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_qos_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_qos_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_qos_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_queue_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_queue_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_queue_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_queue_weight_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_queue_weight_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_queue_weight_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_rate_limit_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_rate_limit_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_rate_limit_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_rate_unit_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_rate_unit_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_rate_unit_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_reference_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_reference_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_reference_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_reply_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_reply_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_reply_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_request_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_request_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_request_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_rx_bytes_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_rx_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_rx_bytes_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_rx_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_rx_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_rx_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_sampling_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_sampling_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_sampling_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_set_loopback_mode_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_set_loopback_mode_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_set_loopback_mode_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_status_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_status_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_status_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_strip_mpls_l2_on_ingress_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_strip_mpls_l2_on_ingress_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_strip_mpls_l3_on_ingress_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_strip_mpls_l3_on_ingress_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_strip_vlan_on_egress_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_strip_vlan_on_egress_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_sub_agent_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_sub_agent_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_sub_agent_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tcp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tcp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tcp_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tcp_flags_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tcp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tcp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tcp_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_ttl_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_ttl_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_ttl_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tunnel_capability_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tunnel_capability_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tunnel_capability_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tx_bytes_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tx_bytes_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tx_bytes_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_tx_packets_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_tx_packets_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_tx_packets_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udf_anchor_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udf_anchor_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udf_anchor_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udf_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udf_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udf_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udf_length_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udf_length_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udf_length_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udf_offset_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udf_offset_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udf_offset_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udp_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_udp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_udp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_udp_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_uint64_list_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_uint64_list_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_uint64_list_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_unicast_query_timeout_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_unicast_query_timeout_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_unicast_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_unicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_unicast_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_unknown_multicast_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_unknown_multicast_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_untagged_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_untagged_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_untagged_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_use_packet_state_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_use_packet_state_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_use_packet_state_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vfi_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vfi_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vfi_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vfp_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vfp_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vfp_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vlan_mac_list_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vlan_mac_list_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vlan_mac_list_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vlan_pcp_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vlan_pcp_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vlan_pcp_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vlan_vid_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vlan_vid_mask_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vlan_vid_mask_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vni_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vni_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vni_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vpn_key_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vpn_key_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vpn_key_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vrf_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vrf_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vrf_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_tlv_vxlan_egress_lag_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_tlv_vxlan_egress_lag_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vlan_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vlan_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vlan_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vlan_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vport_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_vport_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_vport_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_vport_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vport_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vport_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vport_l2gre_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_vport_l2gre_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_vport_l2gre_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_vport_l2gre_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vport_l2gre_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vport_l2gre_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vport_q_in_q_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vport_q_in_q_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bsn_vrf_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bucket_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_bucket_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bucket_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bucket_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bucket_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bucket_counter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_bucket_counter_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_bucket_counter_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_bucket_counter_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_bucket_counter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bundle_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_bundle_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_bundle_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_flow_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_flow_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_flow_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_flow_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_flow_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_flow_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_desc_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_desc_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_group_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_group_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_group_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_group_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_group_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_hello_elem_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_hello_elem_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_hello_elem_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_hello_elem_versionbitmap_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_hello_elem_versionbitmap_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_hello_elem_versionbitmap_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_apply_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_apply_actions_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_apply_actions_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_apply_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_arp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_arp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_arp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_auto_negotiation_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_deny_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_deny_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_deny_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_dhcp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_dhcp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_dhcp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_disable_l3_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_disable_l3_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_disable_l3_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_disable_split_horizon_check_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_disable_src_mac_check_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_disable_vlan_counters_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_hash_select_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_hash_select_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_hash_select_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_internal_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_internal_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_internal_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_ndp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_packet_of_death_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_packet_of_death_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_packet_of_death_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_permit_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_permit_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_permit_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_prioritize_pdus_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_prioritize_pdus_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_prioritize_pdus_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_require_vlan_xlate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_bsn_span_destination_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_bsn_span_destination_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_bsn_span_destination_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_clear_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_clear_actions_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_clear_actions_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_clear_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_clear_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_goto_table_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_goto_table_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_goto_table_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_goto_table_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_goto_table_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_apply_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_apply_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_arp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_arp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_arp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_auto_negotiation_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_auto_negotiation_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_deny_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_deny_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_deny_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_dhcp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_dhcp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_disable_l3_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_disable_l3_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_disable_l3_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_disable_split_horizon_check_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_disable_split_horizon_check_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_disable_src_mac_check_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_disable_src_mac_check_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_disable_vlan_counters_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_disable_vlan_counters_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_hash_select_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_hash_select_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_hash_select_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_internal_priority_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_internal_priority_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_internal_priority_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_ndp_offload_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_ndp_offload_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_ndp_offload_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_packet_of_death_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_packet_of_death_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_packet_of_death_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_permit_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_permit_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_permit_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_prioritize_pdus_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_prioritize_pdus_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_require_vlan_xlate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_require_vlan_xlate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_bsn_span_destination_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_bsn_span_destination_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_bsn_span_destination_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_clear_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_clear_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_clear_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_goto_table_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_goto_table_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_goto_table_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_meter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_meter_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_meter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_write_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_write_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_id_write_metadata_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_id_write_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_id_write_metadata_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_meter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_meter_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_meter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_write_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_write_actions_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_write_actions_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_write_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_instruction_write_metadata_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_instruction_write_metadata_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_instruction_write_metadata_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_instruction_write_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_instruction_write_metadata_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_match_v1_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_match_v1_OF_VERSION_1_0_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_match_v2_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_match_v2_OF_VERSION_1_1_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_match_v3_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_match_v3_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_match_v3_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_match_v3_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_band_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_band_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_band_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_band_drop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_band_drop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_band_drop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_band_dscp_remark_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_band_dscp_remark_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_band_dscp_remark_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_band_experimenter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_band_experimenter_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_band_experimenter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_band_stats_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_band_stats_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_band_stats_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_config_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_config_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_config_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_features_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_features_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_features_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_meter_stats_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_meter_stats_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_meter_stats_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_op_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_op_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_op_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_op_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_op_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_op_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_op_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_op_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_sha_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_sha_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_sha_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_sha_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_sha_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_sha_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_spa_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_spa_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_spa_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_spa_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_spa_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_spa_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_tha_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_tha_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_tha_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_tha_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_tha_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_tha_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_tpa_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_tpa_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_tpa_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_tpa_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_arp_tpa_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_arp_tpa_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_egr_port_group_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_egr_port_group_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_egr_port_group_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_egr_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_global_vrf_allowed_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_global_vrf_allowed_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_global_vrf_allowed_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_global_vrf_allowed_masked_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_in_ports_128_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_in_ports_128_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_in_ports_128_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_in_ports_128_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_in_ports_512_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_in_ports_512_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_in_ports_512_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_in_ports_512_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_ingress_port_group_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_ingress_port_group_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_ingress_port_group_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_ingress_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_eth_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_eth_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_eth_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_eth_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_eth_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_eth_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_eth_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_eth_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_vlan_vid_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_inner_vlan_vid_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_inner_vlan_vid_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_ip_fragmentation_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_ip_fragmentation_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_ip_fragmentation_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_ip_fragmentation_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l2_cache_hit_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l2_cache_hit_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l2_cache_hit_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l2_cache_hit_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_dst_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_dst_class_id_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_dst_class_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_dst_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_interface_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l3_interface_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_interface_class_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l3_interface_class_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_src_class_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l3_src_class_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_l3_src_class_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_l3_src_class_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_lag_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_lag_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_lag_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_lag_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_tcp_flags_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_tcp_flags_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_tcp_flags_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf0_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf0_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf0_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf0_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf0_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf0_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf1_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf1_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf1_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf1_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf1_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf1_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf2_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf2_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf2_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf2_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf2_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf2_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf3_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf3_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf3_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf3_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf3_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf3_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf4_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf4_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf4_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf4_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf4_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf4_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf5_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf5_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf5_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf5_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf5_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf5_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf6_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf6_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf6_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf6_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf6_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf6_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf7_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf7_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf7_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf7_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_udf7_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_udf7_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vfi_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vfi_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vfi_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vfi_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vfi_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vfi_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vlan_xlate_port_group_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vlan_xlate_port_group_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vlan_xlate_port_group_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vlan_xlate_port_group_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vrf_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_vrf_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vrf_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vrf_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vrf_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vrf_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vxlan_network_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vxlan_network_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vxlan_network_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_bsn_vxlan_network_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_bsn_vxlan_network_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_type_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_eth_type_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_eth_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_eth_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_eth_type_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv4_code_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv4_code_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv4_code_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv4_code_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv4_code_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv4_code_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv4_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv4_type_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv4_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv4_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv4_type_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv4_type_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv6_code_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv6_code_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv6_code_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv6_code_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv6_code_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv6_code_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv6_type_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv6_type_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv6_type_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv6_type_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_icmpv6_type_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_icmpv6_type_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_in_phy_port_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_in_phy_port_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_in_phy_port_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_in_phy_port_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_in_phy_port_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_in_phy_port_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_in_port_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_in_port_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_in_port_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_in_port_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_in_port_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_in_port_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_in_port_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_in_port_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_dscp_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_dscp_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_dscp_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_dscp_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_dscp_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_dscp_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_ecn_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_ecn_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_ecn_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_ecn_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_ecn_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_ecn_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_proto_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_proto_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_proto_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_proto_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ip_proto_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ip_proto_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv4_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv4_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv4_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv4_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv4_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv4_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv4_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv4_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_exthdr_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_exthdr_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_exthdr_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_exthdr_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_exthdr_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_exthdr_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_flabel_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_flabel_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_flabel_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_flabel_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_sll_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_sll_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_sll_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_sll_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_target_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_target_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_target_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_target_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_tll_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_tll_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_nd_tll_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_nd_tll_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ipv6_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ipv6_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_metadata_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_metadata_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_metadata_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_metadata_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_metadata_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_metadata_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_metadata_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_metadata_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_bos_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_bos_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_bos_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_bos_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_bos_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_bos_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_label_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_mpls_label_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_label_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_label_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_label_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_label_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_tc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_mpls_tc_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_tc_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_tc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_mpls_tc_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_mpls_tc_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ovs_tcp_flags_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ovs_tcp_flags_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_ovs_tcp_flags_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_ovs_tcp_flags_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_pbb_uca_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_pbb_uca_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_pbb_uca_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_pbb_uca_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_sctp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_sctp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_sctp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_sctp_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_sctp_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_sctp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_sctp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_sctp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_sctp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_sctp_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_sctp_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_sctp_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tcp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tcp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tcp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tcp_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tcp_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tcp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tcp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tcp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tcp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tcp_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tcp_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tcp_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_id_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_id_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_id_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_ipv4_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_ipv4_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_ipv4_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_ipv4_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_ipv4_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_ipv4_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_tunnel_ipv4_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_tunnel_ipv4_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_udp_dst_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_udp_dst_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_udp_dst_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_udp_dst_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_udp_dst_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_udp_dst_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_udp_src_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_udp_src_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_udp_src_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_udp_src_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_udp_src_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_udp_src_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_udp_src_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_udp_src_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_vlan_pcp_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_vlan_pcp_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_vlan_pcp_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_vlan_pcp_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_vlan_pcp_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_vlan_pcp_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_vlan_vid_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_vlan_vid_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_vlan_vid_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_vlan_vid_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_oxm_vlan_vid_masked_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_oxm_vlan_vid_masked_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_packet_queue_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_packet_queue_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_packet_queue_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_packet_queue_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_packet_queue_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_packet_queue_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_desc_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_desc_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_desc_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_desc_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_bsn_forward_error_correction_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_bsn_forward_error_correction_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_bsn_generation_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_bsn_generation_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_bsn_uplink_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_bsn_uplink_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_ethernet_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_desc_prop_optical_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_desc_prop_optical_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_mod_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_prop_ethernet_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_mod_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_mod_prop_optical_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_mod_prop_optical_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_port_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_port_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_port_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_port_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_prop_ethernet_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_prop_ethernet_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_port_stats_prop_optical_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_port_stats_prop_optical_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_bsn_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_bsn_queue_name_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_prop_bsn_queue_name_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_max_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_prop_max_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_desc_prop_min_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_desc_prop_min_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_prop_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_prop_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_prop_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_prop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_prop_max_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_queue_prop_max_rate_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_prop_max_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_prop_max_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_prop_min_rate_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_prop_min_rate_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_prop_min_rate_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_prop_min_rate_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_prop_min_rate_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_prop_min_rate_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_queue_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_queue_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_queue_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_queue_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_queue_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_stats_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_queue_stats_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_queue_stats_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_role_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_role_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_apply_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_apply_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_apply_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_apply_actions_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_apply_actions_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_apply_actions_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_apply_setfield_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_apply_setfield_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_apply_setfield_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_apply_setfield_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_apply_setfield_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_experimenter_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_experimenter_miss_dup(
    of_object_t *src)
{

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_instructions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_instructions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_instructions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_instructions_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_instructions_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_instructions_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_match_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_match_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_match_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_next_tables_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_next_tables_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_next_tables_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_next_tables_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_next_tables_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_next_tables_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_table_sync_from_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_table_sync_from_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_wildcards_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_wildcards_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_wildcards_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_write_actions_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_write_actions_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_write_actions_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_write_actions_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_write_actions_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_write_actions_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_write_setfield_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_write_setfield_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_write_setfield_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_feature_prop_write_setfield_miss_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_feature_prop_write_setfield_miss_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_feature_prop_write_setfield_miss_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_features_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_table_features_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_features_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_prop_eviction_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_prop_eviction_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_prop_experimenter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_prop_experimenter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_mod_prop_vacancy_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_table_mod_prop_vacancy_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_table_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_table_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_table_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_table_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_table_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_table_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_uint32_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_uint32_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_uint32_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_uint64_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_uint64_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_uint64_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_uint8_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_uint8_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_uint8_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_action_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_action_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_action_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_action_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_action_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_action_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_action_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_action_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_action_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_async_config_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_async_config_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_controller_connection_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_controller_connection_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_controller_connection_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_debug_counter_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_debug_counter_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_debug_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_debug_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_flow_checksum_bucket_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_flow_checksum_bucket_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_generic_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_generic_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_generic_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_gentable_bucket_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_gentable_bucket_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_gentable_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_gentable_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_gentable_entry_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_gentable_entry_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_gentable_entry_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_gentable_entry_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_gentable_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_gentable_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_gentable_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_interface_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_bsn_interface_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_bsn_interface_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_bsn_interface_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_interface_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_interface_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_lacp_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_lacp_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_lacp_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_port_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_port_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_port_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_switch_pipeline_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_switch_pipeline_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_table_checksum_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_table_checksum_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_tlv_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_tlv_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_tlv_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_vlan_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_vlan_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_vlan_mac_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_vlan_mac_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_vlan_mac_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bsn_vrf_counter_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bsn_vrf_counter_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bucket_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_list_bucket_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_bucket_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_bucket_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bucket_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bucket_counter_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_list_bucket_counter_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_bucket_counter_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_bucket_counter_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_bucket_counter_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_bundle_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_bundle_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_flow_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_flow_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_flow_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_flow_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_flow_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_flow_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_group_desc_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_list_group_desc_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_group_desc_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_group_desc_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_group_desc_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_group_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_list_group_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_group_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_group_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_group_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_hello_elem_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_hello_elem_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_hello_elem_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_instruction_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_1) {
        return of_list_instruction_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_instruction_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_instruction_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_instruction_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_instruction_id_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_instruction_id_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_instruction_id_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_meter_band_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_meter_band_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_meter_band_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_meter_band_stats_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_meter_band_stats_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_meter_band_stats_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_meter_config_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_meter_config_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_meter_config_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_meter_stats_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_meter_stats_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_meter_stats_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_oxm_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_2) {
        return of_list_oxm_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_oxm_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_oxm_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_packet_queue_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_packet_queue_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_packet_queue_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_packet_queue_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_packet_queue_OF_VERSION_1_3_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_port_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_port_desc_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_port_desc_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_port_desc_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_port_desc_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_port_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_port_desc_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_port_desc_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_port_mod_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_port_mod_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_port_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_port_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_port_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_port_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_port_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_port_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_port_stats_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_port_stats_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_queue_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_queue_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_queue_desc_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_queue_desc_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_queue_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_queue_prop_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_queue_prop_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_queue_prop_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_queue_prop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_queue_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_queue_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_queue_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_queue_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_queue_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_queue_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_queue_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_queue_stats_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_queue_stats_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_role_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_role_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_table_desc_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_table_desc_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_table_feature_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_table_feature_prop_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_table_feature_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_table_features_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_table_features_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_table_features_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_table_mod_prop_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_4) {
        return of_list_table_mod_prop_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_table_stats_entry_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_0) {
        return of_list_table_stats_entry_OF_VERSION_1_0_dup(src);
    }

    if (src->version == OF_VERSION_1_1) {
        return of_list_table_stats_entry_OF_VERSION_1_1_dup(src);
    }

    if (src->version == OF_VERSION_1_2) {
        return of_list_table_stats_entry_OF_VERSION_1_2_dup(src);
    }

    if (src->version == OF_VERSION_1_3) {
        return of_list_table_stats_entry_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_table_stats_entry_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_uint32_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_uint32_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_uint32_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_uint64_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_uint64_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_uint64_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}

of_object_t *
of_list_uint8_dup(
    of_object_t *src)
{

    if (src->version == OF_VERSION_1_3) {
        return of_list_uint8_OF_VERSION_1_3_dup(src);
    }

    if (src->version == OF_VERSION_1_4) {
        return of_list_uint8_OF_VERSION_1_4_dup(src);
    }

    /* Class not supported in given version */
    return NULL;
}
